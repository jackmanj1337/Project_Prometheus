# Session Notes — 2026-05-10

## What Was Done

### Milestone 0 — Project Setup (Complete)
- Created Godot 4.3 project (`project.godot`) with:
  - 1280×720 viewport, canvas_items stretch, pixel snap
  - Full input map (cursor WASD/arrows, confirm Z/Enter/LMB, cancel X/Esc/RMB, etc.)
  - Autoloads registered in order: EventBus, SettingsManager, GameState, DataManager
- Created full folder structure per GDD_01
- Removed `CLAUDE/` from .gitignore (session notes must be tracked)
- Added `default_bus_layout.tres` placeholder (audio buses need editor setup)

### Milestone 1 — Data Layer (Complete)
- **6 Resource GDScript definitions** in `scripts/resources/`:
  ClassData, WeaponData, ItemData, SkillData, UnitData, MapData
- **4 Autoload singletons** in `scripts/autoloads/`:
  EventBus, GameState, DataManager, SettingsManager
- **6 class .tres files** — Soldier, Mercenary, Archer, Mage, Cleric, Knight
- **10 weapon .tres files** — Iron Sword, Steel Sword, Iron Lance, Javelin, Iron Bow,
  Fire, Elfire, Thunder, Wind, Heal Staff
- **2 item .tres files** — Vulnerary, Elixir
- **6 skill .tres files** — Renewal, Vantage, Nihil, Resolve, Miracle, Wrath
- **6 default roster UnitData** in `data/roster/default/`
- **8 enemy UnitData** in `data/maps/map_001_rout/enemies/`
- **MapData .tres** for map_001_rout with all enemy placements
- Data layer test script: **31/31 tests passing**

## Commits
- `33d3226` — Implement Milestone 0 (project setup) and Milestone 1 (data layer)

## Known Issues / Decisions Made
1. **`DataManager.get_class_data()`** — renamed from `get_class()` to avoid conflict with
   Godot's built-in `Object.get_class() -> String`. All future code should call
   `DataManager.get_class_data(id)` not `get_class(id)`.

2. **TILE_SIZE = 64** — GDD_01 says 32px, GDD_06 says 64px. Using 64 to match the
   tileset spec and checklist sprite sizes. GDD_01's "40×22 visible tiles" is based
   on the old 32px assumption; at 64px you see ~20×11 tiles.

3. **Audio buses** — `default_bus_layout.tres` is a placeholder. The Music and SFX
   buses must be added by opening the project in the Godot editor and adding them
   in the Audio panel. Until then, SettingsManager will print errors on startup.

4. **.tres type format** — All .tres files use `type="Resource"` (not `type="ClassData"` etc.)
   because custom GDScript class names are not resolvable in `--script` headless mode.
   This is correct behavior and works fine — the `script = ExtResource(...)` property
   assigns the actual type. The Godot editor will update these to the proper type names
   when opened.

## Plans for Next Session (Milestone 2 — Grid and Map Rendering)
- Create the shared `TileSet` resource (terrain_tileset.tres) with 8 terrain types
  and custom data layer "terrain_type"
- Create the overlay TileSet (4 colored transparent tiles)
- Build `GameMap.tscn` scene with the node tree from GDD_01
- Create `scripts/core/GridManager.gd` — all terrain, movement, and overlay methods
- Create `scripts/core/MapCursor.gd` — keyboard/mouse cursor with key-repeat and
  camera edge-scrolling
- Paint map_001 tile layout per the 42×26 grid in GDD_06
- Create `MapData` reference in map_001_data.tres pointing to the new scene
- Tests: cursor moves within bounds; camera doesn't show black space; terrain info updates
- **Note:** TileSet and TileMap painting requires the Godot editor — may need to
  create them programmatically or use headless `--import` tricks
