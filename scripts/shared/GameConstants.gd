extends Node
# Central project constants. Edit TILE_SIZE here to resize tiles project-wide.
# Registered as an autoload (GameConstants) so any game script can access it directly.
# Tool scripts (generate_*.gd) preload this file instead.

const TILE_SIZE: int = 64
