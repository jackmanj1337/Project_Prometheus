# v0.6.0 Return Fix Goal — Next-Session Handoff

> **Status:** Active — next-session goal handoff
> **Last verified:** 2026-08-02

## Start the next session with this goal

After reading the newest session note and running
`scripts/agent-work --repo Project_Prometheus status --agent`, create an explicit goal
with this objective (no token budget unless the owner supplies one):

> Implement and verify the approved v0.6.0 return fixes in dependency order, preserving
> the accepted expand/free-resize behavior, using unit/integration tests and Playwright
> where authoritative, and isolating all work that still needs native Windows visual or
> input verification on a dedicated playtest branch.

Do not mark the goal complete merely because code is written. Completion requires every
automatable exit below, updated tracker rows, a code review of the resulting diff, and a
new Windows handoff for the native-only checks. The actual Windows pass may remain the
external acceptance gate.

## Ground truth

- Exact tested build source: `cbd1f83257bc68fad1ccdec0bbcb8d5faa7df295`.
- Returned evidence:
  `AGENT/Docs/playtests/evidence/v0.6.0/`.
- Root-cause review:
  `AGENT/Code Reviews/playtest_v0.6.0_root_cause_review_2026-08-02.md`.
- Existing patches base:
  `agent/from-v0.6.0-visual-pass/playtest-patches`.
- Triage branch (documents/evidence only):
  `agent/from-from-v0.6.0-visual-pass-playtest-patches/v060-return-triage`.

At goal start, fetch and inspect current refs and tracker state. Do not assume the branch
tips above remained unchanged. Compare content before merging because later work may have
landed independently.

## Branch and delivery model

The fixes are product work. Start from the exact baked-code patches line unless tracker
state shows it has already advanced appropriately. Use a new `agent/**` implementation
branch registered through `scripts/agent-work`.

Commit independently confirmed fixes in small slices. Before changes requiring native
Windows validation are treated as accepted, place the combined candidate on a separate
`agent/**` playtest-build branch. The container/browser evidence reduces the Windows
matrix but does not replace it. Product ultimately returns through the release line; it
does not shortcut to `agent/staging-area`.

## Ordered implementation slices

### Slice 1 — Restore imported-package unit spawning

Tracker:
`FIX-V060-PRESERVE-ENGINE-REGISTRIES-2026-08-02`.

Preserve or reload the engine-owned registry baseline during Tier-2 package activation;
do not empty `occupancy_policies`. Add an end-to-end regression that activates a real
Tier-2 package and then calls `OccupancyService.place()` with `nearest_free`.

Automated exit:

- Existing built-in and package activation tests remain green.
- A package activation → live occupancy test fails on the baked source and passes after
  the fix.
- Failed package activation remains atomic and leaves the prior catalogue intact.
- Both bundled campaign fixtures reach a map with their expected blue/red units in a
  headless or instrumented runtime check.

Do not fold the long-term composition design into this release repair. Keep
`DESIGN-IMPLEMENT-PACKAGE-REGISTRY-COMPOSITION-2026-08-02` scheduled as the explicit
successor, coordinated with the one-engine-primitive-list work.

### Slice 2 — Reusable text-entry service and grid keyboard

Tracker:
`IMPL-REUSABLE-GRID-KEYBOARD-2026-08-02`.

Build one `TextEntryService` owning sessions, validation, layout/presenter selection and
arbitration. Build/prebuild one reusable GridKeyboard scene. Requests carry target,
allowed characters, initial text, dismissal policy and host viewport. Construct controls
outside focus/input dispatch and show them deferred with re-entry/cancellation guards.

Automated exit:

- Pure tests cover insert/backspace, validation, visible Space, layers, disabled keys,
  navigation, submit/cancel and competing-session arbitration.
- Playwright drives a production-service-backed in-game test surface using real input;
  a read-only bridge exposes active session/presenter, target text, focus and control
  rectangles.
- Playwright covers open/close, arrows/WASD, characters, backspace, Space, layer switch,
  disabled characters, submit/cancel, focus withdrawal/re-entry, reuse across two fields,
  and containment at the supported viewport matrix.
- No browser console/page errors.

### Slice 3 — Explicit FileDialog filename-edit ownership

Tracker:
`FIX-FILEDIALOG-EXPLICIT-EDIT-STATE-2026-08-02`.

Use the shared service, not a second input seam. Entering filename-edit state explicitly
focuses the LineEdit, establishes selection/caret placement and scopes physical-Escape
ownership. First Escape ends editing and focuses the real file ItemList; second Escape
closes normally. Every exit path releases the scope. Remove the redundant four-hook
strategy after the native owner is demonstrated.

Automated exit:

- Pure/dispatched-event tests cover the edit-state transition and cleanup on every exit.
- Existing FileDialog input tests remain green.
- Playwright checks the shared state machine on the in-game surface only; it must not be
  cited as native FileDialog proof.

Native Windows exit:

- Repeatedly open the FileDialog grid, enter text, use X/Z/WASD normally, observe a real
  caret, press Escape once to focus the file list, and press Escape again to close.
- Repeat several times with no crash and return the owning-stage/state telemetry.

### Slice 4 — Bounded responsive temporary windows

Tracker:
`IMPL-BOUNDED-RESPONSIVE-TEMP-WINDOWS-2026-08-02`.

After resolving the remaining safe-area/accessibility values in
`DISCUSS-RESPONSIVE-DISPLAY-LAYERS-2026-08-02`, migrate centered temporary windows to
full-rect CenterContainers with preferred/minimum sizes, safe-viewport-relative maximum
bounds, and scrolling/wrapping where content can exceed them. Percentage values are
caps, not the only sizing rule. Reflow after size/scale changes once minimum sizes settle.

Automated/Playwright exit:

- Audit every temporary screen; Campaign Library must no longer use absolute design
  offsets.
- Extend the test-only harness seed so menu scale and viewport/content scale are real
  runtime inputs, not geometry-model annotations.
- Assert every relevant frame is centered or correctly contextual, contained within the
  safe rectangle, and has reachable scroll content at the agreed matrix, including the
  returned 2×/2× and 4×/2× combinations.
- Include 16:9, 16:10, ultrawide and small supported viewport samples.
- Capture screenshots and bridge rectangle reports; no manual coordinate guessing.

Native Windows exit remains required for window dragging/maximize/restore and visual
quality, but layout geometry should already be mechanically green.

### Slice 5 — HUD panel-to-viewport attachment pairs

Tracker:
`IMPL-HUD-ATTACHMENT-PAIR-LAYOUT-2026-08-02`.

Persist each panel as panel attachment (four corners/four edge midpoints), safe-viewport
attachment (same eight points), logical offset, independent scale, and a versioned schema
prepared for later opacity. Compute:

```text
viewport attachment + offset - scaled panel attachment
```

Dragging changes offset; it does not silently change attachments. Reset restores authored
relationships against the live viewport. Clamp the whole scaled panel by default. Migrate
old absolute layouts only with defensible reference-size evidence; otherwise fall back to
authored defaults.

Automated/Playwright exit:

- Cover all 8×8 attachment combinations, resize/maximize-equivalent geometry, safe-area
  changes, scale/text-size/content changes, reset, save/reload and migration.
- Assert the full scaled panel remains reachable.
- Playwright checks representative attachment pairs and resize sequences on a map/HUD
  route once the bridge exposes HUD rectangles and attachment state.
- Native Windows confirms the HUD editor interaction and maximize/restore behavior.

### Slice 6 — Structured package recovery and save provenance snapshot

Tracker:
`IMPL-SAVE-PACKAGE-RECOVERY-PROVENANCE-2026-08-02`.

Coordinate with all active pack-save schema/migration/export owners before editing the
record. Snapshot package ID, display name, version, canonical package fingerprint/hash,
author and author recovery note in run/save records. These are informational provenance,
never trusted replacement content. Bound and sanitize authored text; render the recovery
note as escaped plain text.

Automated exit:

- Deterministic serialization and migration from older saves.
- Tampering with duplicated metadata cannot activate or override content.
- Structured failures distinguish missing package, missing version, hash mismatch,
  corrupt/invalid, incompatible, and missing campaign/node.
- End-to-end UI tests show actionable recovery using saved display metadata, ID fallback,
  recovery note, and the assurance that progress was not restored or modified.
- Technical paths stay in logs, not ordinary player-facing copy.

### Slice 7 — Controller-transition telemetry and diagnostic watchdog

Tracker:
`INSTRUMENT-CONTROLLER-TRANSITION-LOCKOUT-2026-08-02`.

Add bounded correlated records for attack, combat, EXP/level-up, end-turn, modal stack,
focus, input mode/device, input-suppression gates and turn state. Add a one-shot,
diagnostic-only watchdog for apparently ownerless prolonged input suppression. It must
name all relevant owners/state and must not clear gates, close modals, move focus or
advance gameplay.

Automated exit:

- Balanced acquire/release and correlation-ID tests.
- Legitimate long transitions do not trigger the watchdog.
- Ownerless synthetic suppression triggers exactly one structured snapshot.
- Watchdog changes no behavior or state.

Native Windows exit:

- Repeated controller sequences around attacks with/without level-up, level-up dismissal,
  and end-turn confirmation; exercise joystick/triggers immediately after each boundary.
- Return the correlated log if the lockout recurs.

## Cross-cutting gates

For every slice:

1. Add a regression that fails for the measured defect, where mechanically possible.
2. Run focused tests during development.
3. Run `python3 AGENT/Docs/check_docs.py` and `bash run_tests.sh` before commits/handoff.
4. Run relevant Playwright matrices through `scripts/playwright-drive.sh`; state clearly
   when a path is not authoritative on web.
5. Preserve the already-passing controller hot-plug logger and expand/free-resize behavior.
6. Update affected GDD plus `GDD_10_Roadmap.md` in the same behavior commit per DoD#1.
7. Update tracker status/evidence after each slice; do not leave completion only here.
8. Run an implementation diff review before producing the Windows build.

## Goal completion and handoff

The next-session goal may be marked complete when all seven implementation slices have
their automated exits green, the diff review has no unresolved blocker, exact-HEAD full
receipts exist, tracker rows point to commits/evidence, and the native-only remainder is
packaged as a precise Windows checklist/build handoff. If Windows validation itself is
not available in that session, describe the goal as implementation-complete with the
release acceptance gate outstanding; do not call v0.6 accepted.
