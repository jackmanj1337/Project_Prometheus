---
Role: dated
Type: plan
Status: Planned - ready for next-session execution
Last verified: 2026-07-21
---

# v0.5.3 Final Review, Build, and Checklist Handoff - 2026-07-21

Release-line ownership remains in the
[`Project Control Plane`](project_control_plane_2026-06-29.md). This handoff is
the execution source for the open v0.5.3 final-review, build, and playtest gate.

## Resume point

- Checkout `agent/playtest-release-v0.5.3-telemetry` at `042bff70186d` or its
  published descendant. The working tree was clean when this handoff was written.
- This branch contains the full v0.5.3 fix branch plus automatic playtest
  provenance logging. Its release-line base is
  `origin/agent/playtest-release-v0.5-fixes` at `db270cb32576`.
- Fix implementation commits:
  - `acbe81e0cdda` - FileDialog printable-key ownership;
  - `54ae1dc78fc5` - the reviewed nine-fix v0.5.3 repair batch;
  - `3a009484307c` - automatic host/controller/campaign provenance.
- Read first:
  - [`2026-07-21b.md`](../../Session%20Notes/2026-07-21b.md) for repair evidence;
  - [`2026-07-21d.md`](../../Session%20Notes/2026-07-21d.md) for log capture;
  - [`playtest_checklist_v0.5.2.md`](../playtests/playtest_checklist_v0.5.2.md)
    as the regression baseline.
- Canonical task: `PP-V053-BUILD-RETURN` in the workspace
  `coordination/tasks.json`. Keep it open until the Windows return is triaged.

## Session objective

Perform one final source/test review of every v0.5.3 repair, correct any concrete
defect found, update release metadata to v0.5.3, produce the release-grade Windows
artifact, and write the focused v0.5.3 checklist and build manifest. Do not perform
the live Windows acceptance pass in the container.

## 1. Final review boundary

Review the complete range, not only the last telemetry commit:

```bash
git diff --check origin/agent/playtest-release-v0.5-fixes..HEAD
git diff --stat origin/agent/playtest-release-v0.5-fixes..HEAD
git diff origin/agent/playtest-release-v0.5-fixes..HEAD -- \
  scripts scenes data AGENT/GDD
```

Confirm each repair against its regression evidence:

1. `map_005_defend` is pure Survive, with no hold tiles or turn-limit defeat;
   future hold-map text uses one-based coordinates.
2. Manual campaign saves use automatic contextual IDs/labels, collision-safe
   suffixes, confirmation before same-label replacement, and write-new-before-
   delete-old failure safety.
3. Paired escape restores both lead and support roster coordinates to the escape
   tile before separation/free.
4. Full-history Rewind retains all activations, truncates the staged serialized
   ledger, restores charges transactionally, and rolls back on rejected saves.
5. Every FileDialog text field accepts printable bindings without leaking Cancel
   or Confirm actions, while normal FileDialog behavior remains intact.
6. Manual End Turn records one deterministic Wait transaction/history entry per
   remaining unit and does not double-end the phase.
7. Map-menu and defeat-screen Rewind selectors own the shared modal lock, hide
   their host controls, restore focus, and release the lock on every exit path.
8. Campaign successor errors distinguish inactive, missing, and empty-successor
   states; they offer Return to Menu and never falsely label Finish Campaign.
9. Startup logging captures build/host/display/GPU/controller data and campaign
   start/restore/node-launch JSON captures exact campaign/package identity.

Pay particular attention to transaction rejection paths, modal teardown paths,
same-timestamp save collisions, and the fact that `godot.log` may contain several
launches. Fix only evidence-backed defects found during this review; record any
new product decision as a question rather than guessing.

## 2. Required automated gates

Run the exact release-line gates after the review and again after release-metadata
edits:

```bash
bash run_tests.sh
python3 AGENT/Docs/check_docs.py
bash scripts/ci/check_gdscript_style.sh
bash scripts/ci/check_rng_usage.sh
git diff --check
```

The current baseline is 103 suites, 235 tracked GDScript files, 40 documentation
checks, and a green RNG guard. Counts may increase; no existing suite may disappear
without an explained repository change. Finish with:

```bash
../../scripts/agent-work --repo Project_Prometheus check full
```

The full receipt must match exact `HEAD` before release export.

## 3. v0.5.3 release metadata

Advance the version as one coherent release-metadata change. At minimum inspect
and update all contracts enforced by `test_release_metadata.gd`:

- `export_presets.cfg`: preset name, output path, and Windows product version;
- `scenes/ui/MainMenu.tscn`: visible `v0.5.3` label;
- `scripts/tests/test_release_metadata.gd`: expected version;
- workspace `godot_prometheus_setup.md`: current version and executable name;
- new `AGENT/Docs/playtests/playtest_checklist_v0.5.3.md`;
- new `AGENT/Docs/playtests/playtest_build_v0.5.3.md` after export.

Do not hand-edit or commit `build_info.json`; the release export's baked stamp must
name the exact final source commit and UTC build time. Do not create or push a
release tag for this playtest build.

## 4. Checklist requirements

Write a focused checklist that directly exercises the nine review rows above and
retains the relevant v0.5.2/v0.5.0 regression coverage. Keep these return rules
explicit:

- The tester defaults to the project owner unless the return names someone else.
- Do not require a separate PASS/FAIL checkbox. Triage derives the verdict from
  the tester's comments, completed checks, logs, screenshots, and failures.
- Treat the framed `RUNTIME ENVIRONMENT` block as authoritative for OS, CPU, GPU,
  display backend, renderer, locale/time zone, and window/screen data.
- Treat `PLAYTEST CONTROLLER` lines as authoritative controller evidence.
- Treat `PLAYTEST CONTEXT` JSON lines as authoritative campaign, node, and package
  evidence. The checklist must still tell the tester exactly which campaign and
  escape scenario to run so the log can prove the requested path was exercised.
- Human comments remain required for observed behavior, reproduction steps,
  expected/actual results, and anything not run.
- Request the original `godot.log` and the focused screenshots needed to evaluate
  layout, modal behavior, results progression, and escape/rewind state.

Do not ask the tester to retype data already present in the log.

## 5. Release-grade Windows export

After metadata, checklist, final review fixes, and their session-note claims are
committed, rerun the exact-HEAD full check. Then use the required wrapper in release
mode with a stamp containing the exact seven-character source commit, version, and
UTC time:

```bash
../../scripts/export-windows.sh \
  --repo Project_Prometheus \
  --mode release \
  --build-stamp "v0.5.3 commit <7-char-HEAD> built <UTC>"
```

Release mode must remain non-overwriting. If the output directory already exists,
preserve it and resolve the exact target deliberately; do not use `--force`.

Record in `playtest_build_v0.5.3.md`:

- source branch, full commit, tree, baked version/commit/time;
- Godot version, preset, platform/architecture, and embedded-PCK status;
- executable name, exact byte size, and SHA-256;
- artifact-manifest path and its recorded build-stamp evidence;
- automated gate results and exact suite/doc/style counts;
- embedded BUILD STAMP inspection result;
- any nonfatal exporter warnings, distinguished from failures;
- the fact that live Windows behavior remains unverified.

Verify the executable actually contains the expected v0.5.3 stamp before handing
it off. Copy/package the required campaign fixture beside it if the checklist uses
one, and record that fixture's exact size and SHA-256 too.

## 6. Delivery and tracker closeout

- Keep product work on this release sibling or merge it into the appropriate
  `agent/playtest-release*` line only after every automated gate passes.
- Push the exact `agent/**` branch with hooks active. The current generic push
  verifier may incorrectly compare release siblings against `agent/integration`;
  if that recurs, document the recovery reason and use an exact-ref `git push`
  without bypassing the repository pre-push hooks.
- Update `PP-V053-BUILD-RETURN` with the final build/checklist paths and artifact
  identity. Set it to `playtesting`, not `completed`, while the Windows return is
  outstanding.
- Regenerate `coordination/ACTIVE_WORK.md` and `AGENT/WAITING_WORK.md`, validate
  with `coordination/check_tasks.py`, and publish the workspace tracker change.

## Exit condition

- Every v0.5.3 fix and its edge paths have received a final source/test review.
- Any review finding is fixed, tested, documented, committed, and claimed.
- Release metadata consistently identifies v0.5.3.
- Exact-HEAD automated gates are green.
- The Windows artifact, artifact manifest, build manifest, focused checklist, and
  any fixture exist with recorded hashes and sizes.
- The artifact's embedded BUILD STAMP matches its exact source commit.
- The tracker points to the handoff artifacts and waits in `playtesting` for the
  live Windows return.
