---
Role: dated
Type: playtest
Status: Active
Last verified: 2026-08-08
---

# v0.7.1 playtest waiting-work handoff

This handoff is tracked as `RESEARCH-WHILE-V071-PLAYTEST-PENDING-2026-08-08` in
`coordination/tasks.json`.

## Operating rule

The Windows candidate is frozen at `agent/playtest-release-v0.7.1` commit `0db30fd1`.
Do not merge new product work into that branch while its playtest is outstanding.
Research below belongs on fresh `agent/**` branches from `agent/integration` and may
merge back there without changing the candidate.

If a return arrives, pause this queue. Preserve the returned ZIP, logs, and screenshots
before interpreting them; verify BUILD STAMP `0.7.1/0db30fd1`; then register a dedicated
v0.7.1 return-triage row.

## Recommended research and discussion queue

### 1. Skill and status feedback

Resume `DISCUSS-SKILL-STATUS-FEEDBACK-2026-07-23`. Open the `SKF` research and
owner-question packet on top of the resolved shared `CFB-1..18` vocabulary. Reuse its
channel, timing, visibility, log, settings-category, and fallback decisions; do not
invent a competing feedback vocabulary.

Exit: a sourced research document, an `SKF` register with stable question ids, and
explicit links from every inherited choice to the applicable `CFB` decision.

### 2. Combat actions UX

Resume `DISCUSS-COMBAT-ACTIONS-UX-2026-07-24`. Audit its named action families against
`[CFB-9]` choreography and `[CFB-12]` categories. Separate unresolved presentation
questions from ratified mechanics.

Exit: a source-backed `CAU` packet listing only open presentation decisions and the
future UI/runtime seam that would consume each answer.

### 3. Difficulty and death UX

Resume `DISCUSS-DIFFICULTY-DEATH-UX-2026-07-23` after SKF and CAU so their language is
reused. Scope the `DUX` packet to player-facing New Game placement, defaults, warnings,
locked values, and mutability. Do not reopen resolved difficulty, death, validation, or
save mechanics.

Exit: a research document and stable owner-question register distinguishing difficulty
selection, death-mode explanation, and irreversible-choice warnings.

### 4. Responsive display layers

Resume `DISCUSS-RESPONSIVE-DISPLAY-LAYERS-2026-08-02`. Use the measured Pixel 7 failure
as evidence, not as permission to patch the candidate: New Game measured `x=-23`, width
`458`, in a `412`-wide viewport. Discuss ownership boundaries among the host viewport,
map viewport, HUD, modal frames, and temporary windows.

Exit: one agreed layer model and a disposition feeding
`SMALL-SCREEN-UI-REDESIGN-2026-08-05` and `MOBILE-WEB-CONTROLLER-2026-08-04`.

## Safe secondary work

- Schedule or retire the stale discussion rows grouped by
  `GENERAL-PLANNING-SCHEDULING-2026-08-06`.
- Perform source audits, contract reviews, fixture design, and research that does not
  change candidate-owned release files.
- Improve future automated tests on isolated feature branches, without re-exporting or
  amending the frozen candidate.

## Do not start while waiting

- No candidate fixes without a returned blocker and a new explicitly named recut branch.
- No public/web publication: `PACK0-ASSET-EXTRACTION-2026-07-20` and
  `FE-EXPORT-GUARD-2026-07-20` remain distribution gates.
- No broad conversion of the Windows surfaces under test before return triage; that
  would make failures hard to compare with the candidate.
- No `v0.7.1` release tag. This bundle gathers evidence; it is not an accepted release.

## Return preemption checklist

Preserve evidence, verify stamp and hashes, map every finding to a tracker row, classify
blocker/non-blocker/known issue, and decide accept, reject, or recut before resuming this
queue.
