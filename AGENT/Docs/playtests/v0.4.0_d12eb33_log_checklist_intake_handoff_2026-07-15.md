---
Role: dated
Type: playtest
Status: Active - next-session intake handoff
Last verified: 2026-07-15
---

# v0.4.0 `d12eb33` Log + Checklist Intake Handoff

## Next-session objective

Intake and triage the checklist and original `godot.log` belonging to this exact
BUILD STAMP:

```text
version=0.4.0  commit=d12eb33  built_at=2026-07-14T07:01:49Z
```

This is the earlier v0.4.0 release-smoke artifact. It is **not** the later
`dd4f971` campaign/save follow-up artifact and must not be evaluated against the
later expanded campaign checklist.

## Authoritative identity pair

- Build manifest:
  [`playtest_build_v0.4.0.md`](playtest_build_v0.4.0.md)
- Matching checklist:
  [`playtest_checklist_v0.4.0.md`](playtest_checklist_v0.4.0.md)
- Artifact: `Project_Prometheus_v0.4.0_debug.exe`
- Source commit: `d12eb337cf1bed4dd7e89eaa4c6445a80ad5d8a2`
- Expected size: `101840832` bytes
- Expected SHA-256:
  `651bc28deca99724bef1a7a438350defc47fb6cdebd479050d4ad8140cc326a2`
- Expected Godot: `4.6.3.stable.official.7d41c59c4`

The original returned log/checklist are not yet present in this workspace. Do
not infer a PASS from the BUILD STAMP alone.

## Intake sequence

1. Preserve the returned files byte-for-byte before editing any checklist copy.
2. Verify the log BUILD STAMP exactly matches `d12eb33` and the timestamp above.
3. Verify the returned checklist is `playtest_checklist_v0.4.0.md`, not either
   campaign-test checklist.
4. Record Windows/device, input, monitor, filename, size, and hash metadata.
5. Evaluate every checkbox in Sections 1-7. `NOT RUN` is allowed only where the
   checklist explicitly permits it, such as unavailable controller hardware.
6. Search the complete original log for crashes, assertions, registry/resource
   failures, transaction failures, and repeated release-blocking errors.
7. Archive the original log and screenshots under `AGENT/Docs/archive/evidence/`;
   create a returned checklist copy under `AGENT/Docs/playtests/`.
8. Write a triage record for every failed, ambiguous, or unsupported assertion.
   Do not silently convert missing evidence into a pass.
9. Keep the current campaign/save follow-up goal separate: a PASS for `d12eb33`
   does not satisfy the newer `dd4f971` Phase 6 campaign/save checklist.
10. Run documentation, style, session-ownership, and relevant automated gates
    after intake changes; commit the intake/triage as its own logical unit.

## Expected output

- Archived original matching `godot.log`.
- Returned checklist with exact identity and platform/input metadata.
- Archived screenshots for failures.
- Result triage with PASS/FAIL/NOT RUN evidence per section.
- Clear routing of fixes or remaining validation without changing unrelated
  `Pending validation` rows.
