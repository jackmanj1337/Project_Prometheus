---
Type: playtest
Status: Active
Last verified: 2026-08-08
---

# v0.7.1 waiting-work handoff — palette review and next slice

## Release boundary

The frozen Windows candidate remains `agent/playtest-release-v0.7.1` at `0db30fd1`.
A return preempts this queue: preserve evidence, verify BUILD STAMP `0.7.1/0db30fd1`, and
register return triage before changing the candidate.

## Completed headless work

The palette-swap/tint restructure is pushed on
`agent/from-integration/unit-palette-swap`. It adds `palette_swap@1`, explicit supported-swap
ids on sprite assets, Tier-2 activation, exact RGBA shader material construction, keyed
`(faction, state)` lookup, unconditional tint fallback with structured repair evidence, and
HP-bar independence. The full gate is 136 suites, all green.

Do not merge that branch until Windows Compatibility-backend evidence shows two factions in
normal/done states, a forced fallback, unchanged HP-bar faction identity, and movement frames.

The Combat Actions UX research and stable `CAU-1..10` owner-question packet are drafted on
`agent/from-integration/combat-actions-ux-packet`. It reuses resolved `CFB-1..18` and does not
reopen action mechanics.

## Next safe implementation

After the CAU owner walk, implement the shared action-preview record and registered preview
service as its own integration-based slice. Until those presentation choices are answered, the
next decision-independent work is the DUX research packet from the standing v0.7.1 queue, not
an action UI guessed in code.

## Return gate

No release tag, candidate amendment, export replacement, or release-branch merge while the
return is outstanding. Keep every waiting slice isolated from the candidate.

