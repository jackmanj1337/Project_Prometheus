---
Role: dated
Type: playtest
Status: Active
Last verified: 2026-08-08
---

# v0.7.1 waiting-work implementation handoff

## Playtest boundary

The Windows candidate remains frozen at `agent/playtest-release-v0.7.1`
`0db30fd1`. None of this waiting work was merged into that branch. A returned
v0.7.1 evidence package preempts the queue: preserve it, verify BUILD STAMP
`0.7.1/0db30fd1`, and register return triage before changing the candidate.

## Completed while waiting

`IMP-IMPORTER-CORE-2026-07-20` is complete and merged to `agent/integration`.
`SpriteSheetFramesBuilder` now builds deterministic in-memory `SpriteFrames`
from arbitrary two-point rectangles, carries explicit or bottom-centre pivot
metadata, validates malformed/out-of-sheet frames, and emits disableable
non-integer scale warnings. Its contract test has seven assertions. The partial
implementation incorrectly required an `idle` animation; that was removed per
the resolved `[CSA-10]` decision.

`IMP-UNIT-ANIMATED-SPRITE-2026-07-20` is implemented on
`agent/from-integration/unit-animated-sprite`. `Unit.tscn` now uses
`AnimatedSprite2D`, keeps the built-in placeholder as its default animation,
falls back deterministically when a requested animation is absent, and resolves
the normal art identity through `UnitData.class_id` to `ClassData.sprite_id`.
The palette-swap/tint restructure was deliberately excluded. All 134 suites pass,
including suspend/resume appearance coverage and four focused assertions.

The first research item is now open for owner review. The sourced SKF packet lives at
`AGENT/Docs/design/skill_status_feedback_research_2026-08-08.md`; its twelve stable
questions live at
`AGENT/Docs/registers/skill_status_feedback_open_questions_2026-08-08.md`. It inherits
the resolved CFB channel, choreography, visibility, attribution, settings, and animation
choices explicitly. No skill or condition mechanics were reopened, and the packet does
not authorize the broad M9 build.

## Next safe work

The next bounded implementation is the separately tracked pack-sprite runtime resolver:
connect the existing class `sprite_id` and sidecar frame builder to the active pack's raw
art catalogue, with deterministic placeholder fallback and headless tests. Keep it on its
own integration-based branch; it must not merge into the frozen candidate.

Do not wire pack asset loading into `Unit` as an untracked extension of the node
conversion. That runtime resolver/catalogue seam needs its own tracker row and
tests. Do not fold in `IMPL-CSA-PALETTE-SWAP-TINT-RESTRUCTURE-2026-07-31`; its
visual behavior remains a separate slice.

## Verification and handoff state

- Importer core is pushed and merged to `agent/integration`.
- Unit animated-sprite branch was pushed and merged to `agent/integration`
  at `ce5077d0`.
- SKF research and `SKF-1..12` owner-question register are drafted; owner answers remain
  open.
- Full gate: 134 suites, all green.
- Frozen candidate and bundle hashes remain unchanged.
