# Session Note — 2026-06-23f

**Topic:** Owner **re-sequenced the design plan**: finish defining **all player-facing features** first,
*then* decide the campaign builder's authority, *then* implementation. Surveyed existing definition,
found a `GDD_Feature_Index` already inventories the surface, and produced a **player-facing scope map**
(the GAP worklist). Docs-only; check_docs **17/17**; no `__pycache__`.

(Continues 2026-06-23e, which resolved `[ICO-1..6]` and queued the 4a–4e authoring contract next — now
deferred behind this player-facing pass.)

---

## 1. Owner direction
"Finish defining all the player-facing features we want to support → then decide what decisions the
campaign builder gets to make → then decide how to make it all work." Sound top-down order; corrects a
risk (we were about to define builder authority before the player-facing surface was fully firmed).

## 2. Survey finding (not starting from zero)
`AGENT/GDD/` already has **`GDD_Feature_Index.md`** (≈18 feature groups + backlog, status/owner/anchors
routing table) and **`GDD_Adoption_Matrix.md`** (corpus adoption decisions per chapter). So the
player-facing surface is largely **inventoried** — "finish defining" = firm the **gaps**, not redo the
GDD. The §2 save/campaign A–J firming is the template for the kind of pass each gap needs.

## 3. Deliverable — player-facing scope map
`AGENT/Docs/player_facing_scope_map_2026-06-23.md` (navigation, **not** a second spec — DOC-005). Adds a
**player-facing-definition lens** (Firmed / Designed / Partial / GAP / Out-of-scope) over the Feature
Index, distinct from impl status. Findings:
- **Firmed** (player experience fully specified, built or designed): combat/RNG, weapon triangle, WEXP,
  EXP/promotion/reclass, classes, terrain/movement, objectives, faction/hotseat, save/suspend/rewind,
  UI/input/accessibility.
- **Designed, awaiting build** (not gaps): prep/deploy, FOW, DCH, DTR, MET, STW, MRD/TUR, BWN, status
  conditions (M8), AI (AIP/vision).
- **The GAP worklist** (player-facing behavior still undefined — the step-(1) work): **convoy/inventory
  (D), shop/economy (E), recruit (F), Support (H2), Rescue (H3), PvP/scenario mode**; lighter =
  per-skill UX (M9b) + status-condition UX check-backs (can ride their build milestones).

## 4. Sequencing recorded
(1) finish the GAP worklist (A–J-style firming each) → (2) campaign-builder **authority** pass (the
deferred 4a–4e authoring contract — uses the scope-map feature list as its checklist) → (3)
implementation. Not gated by Package A.

## 5. Edits
- New `player_facing_scope_map_2026-06-23.md` (scope map + GAP worklist + sequencing).
- Framing doc `campaign_save_expectations_and_foundations_2026-06-23.md` §6 — walk order re-sequenced
  (4a–4e deferred behind the player-facing pass).
- `GDD_10_Roadmap.md` §H — walk pointer re-sequenced (+ scope-map ref). (`Last verified` already 2026-06-23.)
- This note + INDEX row.

## 6. DoD
- **DoD#1:** planning/navigation only — no behavior change; each gap cluster's GDD chapter +
  `GDD_Feature_Index` row + roadmap flip land **with its build**, per the §2 firming pattern. The scope
  map is explicitly not a spec (DOC-005).
- **DoD#2:** no checkable rule ratified.

## 7. NOT done / still open
- No gap firmed yet — scope map only. GAP worklist = D/E/F/H2/H3/PvP (+ M9b/M8 UX).
- 4a–4e authoring contract deferred behind the worklist. `[ICO]`/`[DMR]` already resolved.
- v0.2.3→`main` merge (owner); IMP/LEG OPEN.

## 8. Verification
- `check_docs.py` → **17/17 PASS**, docs-only, no `__pycache__`.

## 9. Next session
- **Pick the first GAP cluster to firm** (rec order: economy spine first — **convoy (D) → shop (E)** are
  tightly coupled and unblock prep-screen scope; then **recruit (F)**; **Support (H2) / Rescue (H3)** are
  large standalone systems; **PvP** is independent). Run an A–J-style player-facing firming on it.
- Build track unchanged: **Package A Step 1** is the next *execution* step.
