# Session Note - 2026-07-03b

## Topic
Write the **Band 5 implementation plans** (the four grouped plans scoped by
`band5_implementation_plan_handoff_2026-07-02.md`) and a review doc of questions
and concerns.

## What was done
Drafted the four Band 5 plans against the settled Q1-Q7 walkthrough decisions
(2026-07-01), matching the established per-slice plan shape (Purpose / Scope /
Non-Goals / Source Docs / Decisions-Not-To-Reopen / Dependency Note / Existing
Code Touchpoints / ordered slices with Files/Steps/Tests/F1/DoD obligations /
Implementation Commit Order / Verification Checklist):

- `AGENT/Docs/plans/band5_conditions_skills_implementation_plan_2026-07-03.md` —
  `B5-CONDITIONS` + `B5-DURATION-LIFECYCLE` + `B5-SKILLS-EFFECTS` +
  `B5-LOADOUT-CAPS` (shell + skills adapter). Fills the `ConditionManager` M8
  stub with a `ConditionDef`/capability-tag registry, one shared `LifecycleStore`
  (until_unequipped/until_end_of_map/fixed-N), condition behavior + general
  capability gating, `SkillHandler` registry conversion + `on_level_up`, and the
  `LoadoutPanel` shell iterating registered category adapters.
- `AGENT/Docs/plans/band5_source_style_implementation_plan_2026-07-03.md` —
  `B5-SOURCE-STYLE` + `B5-UTILITY-STAVES`. `EffectSpec` set + `StyleDef` + the
  four registries (effect-kind / target-filter / shape / cost), combo resolution
  (source effects ∪ style effects, plain attack = null style), generalized effect
  forecast, two proof consumers (one hostile style + Heal staff built once), then
  Restore/Rescue/inflict staves as data, plus the styles/sources loadout adapters.
- `AGENT/Docs/plans/band5_action_economy_implementation_plan_2026-07-03.md` —
  `B5-ACTION-GRANT` + `B5-SECONDARY-MOVEMENT`. General per-unit-per-turn
  action-budget guard, single-target `refresh_full_turn` grant on the shared
  effect pipeline, remaining/flat secondary movement via `B2-OCCUPANCY`.
- `AGENT/Docs/plans/band5_ai_implementation_plan_2026-07-03.md` —
  `B5-AI-COMPOSITION` + `B5-AI-MIN-SCORER`. `AIProfileDef` registry (weights keyed
  by scorer-term id), `set_ai`/seek-tile/group-wake/`ai_awake`, single-ply scorer
  over legal `(action, target, weapon/source)` tuples with registry-backed terms,
  reusing the same projection the player sees. Built behind the existing
  `TurnManager._ai_controller`/`EnemyAI` seam.

Grounded every plan's "Existing Code Touchpoints" against the live tree:
`ConditionManager.gd` (M8 stub, five condition constants), `SkillHandler.gd`
(hardcoded `_dispatch` in `_ready()`), `SkillData.gd` (`on_level_up` trigger
listed but unwired), `UnitData.gd` (`conditions`, `skills`, `skill_use_counters`,
`ai_profile`), `TurnManager.gd` (`_ai_controller`/`set_ai_controller`/
`start_enemy_phase`), `MapData` unit entries carry `ai_profile:String`,
`WeaponData.effect_tags: Array[String]`, `ItemHandler.is_healing_staff()`.

Wrote the review doc `AGENT/Code Reviews/band5_plans_review_2026-07-03.md`:
carries the four handoff owner questions (Q-B5-1..4) with provisional calls and
surfaces seven new concerns (C1 shared-lifecycle-store owner / C2 `conditions`
save-schema migration / C3 `B2-ACTION-EFFECT` vs `B5-SKILLS-EFFECTS` ownership /
C4 cross-plan loadout-shell ordering / C5 does v1 AI need to score styles /
C6 berserk + scorer / C7 capability gating should show disabled-with-reason).

Pointed all ten Band 5 control-plane rows at the new plans (`B5-LOADOUT-CAPS`
noted as two-phase; `B5-DURATION-LIFECYCLE` flags the C1 store-owner decision).

## Validation
- `python3 AGENT/Docs/gen_docs_index.py` then `check_docs.py`: 21/21 green.
- Docs-only change; no Godot suite run (planning artifacts, no code).

## Commits
- (this commit) — four Band 5 implementation plans + review doc + control-plane
  wiring + INDEX regen.

## Plans / Next Session
- Owner walks the review doc: Q-B5-1 (Q2 draft-now vs demo-first), C1
  (which plan physically creates `LifecycleStore` — lean: `B4-IEQ` owns it,
  Band 5 adds modes+producers), C5 (do v1 demo enemies wield styles/staves),
  Q-B5-4 (forging pull). These are the only open Band 5 planning gates.
- Band 5 implementation stays gated behind Band 1-3 + `B4-IEQ`; the plans are
  artifacts, not build authorizations.
- Parallel threads unchanged: deliver v0.2.5 to the tester (`VAL-V023-DISPLAY`);
  the Q2 effect/staff manifest waits on the demo-campaign design pass.

## Follow-up (same session) — all four owner questions resolved
Owner resolved the two remaining questions:
- **Q-B5-1 — build the full manifest now** (reverses the 2026-07-01 defer-to-demo
  watchout). Wrote `AGENT/Docs/design/band5_v1_content_manifest_2026-07-03.md` —
  the v1 content floor as a feature-showcase spec (≥1 showcase per coded feature):
  5 conditions + params, cure hooks, the ~50 already-coded skill effects certified
  as the floor + `on_level_up` + a grant skill, the 4 utility-staff archetypes
  (Heal HAVE, Restore/Rescue/inflict NEW), the Source+Style effect kinds + demo
  styles (Wrath Strike, Poison Edge), Dance/Canto, the 5 AI profiles + scorer
  terms, and a §9 code-feature→showcase coverage table. Grounded in
  `SkillHandler` dispatch (~50 ids), `ItemHandler.IMPLEMENTED_EFFECT_IDS`,
  `heal_staff.tres`. Dependency inverted: manifest first, demo consumes it.
- **Q-B5-4 — pull Source+Style forward.** `B5-SOURCE-STYLE` now runs as early as
  `B4-IEQ`/`B3-RESOURCE-POOLS`/conditions allow, parallel with skills/loadout, not
  after — the AI scorer (C5) and Band 7 forging `[FRG-18]` both gate on it.
  Forging v1 no longer waits on a separate schedule.

Folded into: the four plans (manifest links, dropped Q2-deferred framing, pull-
forward notes), the review doc (Q-B5-1/Q-B5-4 marked resolved — all four + C5 now
closed), the control-plane Band 5 note + `B5-SOURCE-STYLE` row, the forging plan
`[FRG-18]` note, and the demo-campaign memory (dependency inverted). 21/21 checks
green. Commit `<this>`.

## Follow-up 2 — showpiece = the campaign BUILDER; "widen everything"
Owner reframed the v1 showpiece as the **campaign builder** ("full variety of
things to build with"), not a linear demo. Captured that the authoring stack
(`B2-REGISTRY`/`B2-ACTION-EFFECT`/Band 3) IS the builder substrate; public GUI is
`B8-PUBLIC-BUILDER` (post-v1) and `REL-WEB-DEMO` already names builder-authorship
as the portfolio target. Reframed the manifest: content floors are **varied
builder palettes** (breadth = data not engine), and **prune-not-carry is relaxed**
(palette content unused by one campaign is retained if coherent/tested). Updated
the `project_v1_demo_campaign` memory + MEMORY.md.

Then asked the breadth scope question and owner chose **WIDEN EVERYTHING** —
pull AoE shapes, gambits, and capture-carry into v1 as authorable primitives.
This **deliberately reverses ratified Q5/Q6** ("single-tile shapes; gambits/AoE/
capture are later consumers") for Band 5 v1; recorded as an explicit override in
the review doc §0. Folded in (grounded in STY-6 capture / STY-7 gambit-A1 /
`displacement_carry` DSP):
- Manifest §4 widened (full STY-16 effect kinds; shapes single_tile/line/blast/
  cross; per-map charges) + new §4b (displacement primitive, capture non-lethal→
  sleep+carry, gambit-as-style) + §9 coverage rows; scope-decision section = WIDEN
  EVERYTHING.
- Plan 2 grew 6→8 slices: Slice 1 builds real AoE shapes + full kind set; **new
  Slice 6** = displacement primitive (rescue/capture/shove/swap/pivot on one
  occupancy primitive) + capture + gambit-as-style; loadout adapters → Slice 7.
  `B2-OCCUPANCY` now a heavy gate.
- Plan 4 scorer enumerates AoE/gambit/capture tuples through the one forecast.
- **Boundary preserved:** full battalion *entity* (STY-7 A2) stays deferred;
  gambit-as-style = granted AoE source + charges only.
- Control plane: `B6-RESCUE-CARRY` → expansion-only note; `B7-BATTALION` → entity-
  only note (foundations moved to B5 v1).
21/21 checks green.

## Read First Next Session
1. `AGENT/Docs/design/band5_v1_content_manifest_2026-07-03.md` (the content floor + §4b widened palette).
2. `AGENT/Code Reviews/band5_plans_review_2026-07-03.md` (the questions/concerns).
3. The four `band5_*_implementation_plan_2026-07-03.md` plans.
4. `AGENT/Docs/plans/band5_implementation_plan_handoff_2026-07-02.md` (the scope).
5. `band5_plus_preimplementation_questions_review_2026-06-30.md` → Walkthrough
   Decisions (Q1-Q7).
