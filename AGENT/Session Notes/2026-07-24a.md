# Session Note - 2026-07-24a

## What was done

- Continued the owner design discussion over the campaign-library owner-questions
  packet (dependency-ordered branches A–K). Resolved **Branch E — Launch / New Game /
  rule profiles** (CL-LAUNCH-01/03/04/05; CL-LAUNCH-02 was pre-answered by Branch A —
  rule profile is a Run property).
- Owner decisions:
  - **CL-LAUNCH-01** — selecting a campaign row opens its **details pane** with explicit
    Continue / New Run actions; it does not launch on select. Speed lives in the Main
    Menu **Continue** (Branch B), so the Library is the context/confirmation surface.
  - **CL-LAUNCH-03** — the New Run screen uses **source-labelled controls**: each rule is
    tagged with its value source (schema / profile / campaign default), mandated rules
    carry lock text, and a **changed-value summary** lists what the player altered. Cheap
    given flat `CampaignRules` fields and the fixed mandate→…→resolved-default precedence.
  - **CL-LAUNCH-04** — two-tier validation: a lightweight scan **status badge** (Ready /
    Modified / Invalid) plus **full Tier-2 reference + profile resolution immediately
    before New Run / Resume commits**. Matches the Branch D checkpoint table and closes
    the stale-mutation window.
  - **CL-LAUNCH-05** — persist **last-focused campaign + sort/filter** in `settings.cfg`;
    deliberately not view-mode (avoid view-toggle proliferation in v1). Two new
    `SettingsManager` keys, kept distinct from per-save gameplay rules.
- Linked the player-facing New Run presentation contract (CL-LAUNCH-02/03/04) into the
  already-tracked **`IMPL-RULE-PROFILES`** row rather than spinning out a premature UI
  row; the umbrella Library launch UI folds into the consolidated implementation pass
  after Branches F–K. No runtime, scene, save-schema, or release-line behaviour changed —
  design/planning only.

## Commits claimed

- `4f59d61598b30d85860cae8723440e6ac205d61f` — Record campaign-library Branch E launch decisions (CL-LAUNCH-01/03/04/05)

## Gates

- `python3 AGENT/Docs/gen_docs_index.py` + `python3 AGENT/Docs/check_docs.py`: all
  documentation checks green (INDEX regen was a no-op).
- Workspace `coordination/check_tasks.py`: 136 tasks valid, no conflicts (container-repo
  commit `da7d53a` on `agent/from-staging-area/campaign-research-findings-tracker`).

## Next

Resume at **Branch F — Runs & saves detail** (CL-SAVE-02/03/04/05). Then G → K. Copy each
branch's ids/answers into `AGENT/Docs/design/campaign_library_ux_decisions_2026-07-24.md`
and create implementation tracker rows only for accepted scope.
