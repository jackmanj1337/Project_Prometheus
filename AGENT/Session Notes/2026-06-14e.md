# Session Notes — 2026-06-14e

## Topic
Worked the deferred code/doc items from the 2026-06-14 audit rollup (#7, #9, #10).

## What was done
- **Rollup #7 — decouple `PairUpRegistry` from the scene `TurnManager`** (`fa49ef8`):
  added `EventBus.support_orphaned(support)`; the registry now emits it on a
  player-phase lead death instead of reaching into `/root/GameMap/TurnManager` by
  literal path. `TurnManager` connects the signal and marks the support DONE.
  Removes the autoload→scene coupling that silently no-op'd on a tree reorg.
  Reworked the registry test to assert the decoupled contract (emits phase-aware)
  rather than poking a TurnManager stub.
- **Rollup #9 — Pair Up invariant harness** (`fa49ef8`): added a model-based test
  that drives a scripted pair/swap/separate sequence over a fixed roster and
  asserts the structural invariants after every op (symmetric + reciprocal links,
  complementary roles, no self-pair, lead/support mutually exclusive), plus a
  serialize/restore round-trip invariant. Addresses the top recurring defect class.
  `test_pair_up_registry` now 19/19.
- **Rollup #10 — doc cleanup** (`4e0079c`): added DOC-002a "catalog-section
  variant" to the governance doc, blessing the GDD_06/07/08 catalog layout
  (Pillar 2's recommendation over forcing the feature template); removed the
  prohibited word "Canonical" from `decision_index.md` (the `:123` Title cell and
  a column header).

## Verification
- Full suite green at the code commit (pre-commit ran it); `check_docs.py` green
  (all 10 checks).

## Spotted, NOT fixed (needs a decision)
- `decision_index.md` PL#8/PL#9 rows describe **playtest-list** items (PL#9 =
  "Minimal SFX"), which collide by name with **AGENTS.md** PL#8/PL#9 (the
  documentation/enforcement definition-of-done rules). PL#8 happens to line up;
  PL#9 does not. This naming overlap is confusing and worth disambiguating, but it
  was outside the audit findings — flagged for a future call.

## Still deferred
- **Procedure fix pass** (meta-review §4): tree-completeness preflight,
  delta-discovery step, same-day report naming, tool-probe, anchored score header.
- Push: `main` merge + tags are still local (from 2026-06-14d).
