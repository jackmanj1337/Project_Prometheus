# Session Note - 2026-07-03d

## Topic
Draft the **remaining three Band 6 implementation plans** — `B6-FOW`, `B6-DTR`,
`B6-PREP-PROGRESSION` — after the owner confirmed **all three are in v1** (the open
scope question left by 2026-07-03c). These were register-RESOLVED but NOT part of the
Q8-Q13 v1 walkthrough, so 2026-07-03c held them pending a v1-scope call.

## Scope decision
Owner: "plan on all of it being in v1." All three settled registers are writeable
(design settled), so drafted plans in the established per-slice shape. **Held the
control-plane band split:** Arena stays `B7-ARENA` and recruit-purchase (`[THL-8]`)
stays with the B7-PVP/property-recruitment cluster — both flagged as adjacent/deferred
in the prep-progression plan (owner can pull them into v1 as extra slices + gates).

## What was done
Wrote three Band 6 plans (Purpose / Scope / Non-Goals / Source Docs / Decisions-Not-
To-Reopen / Dependency Note / Existing Code Touchpoints / ordered slices with
Files/Steps/Tests/F1/DoD / Commit Order / Verification Checklist):

- `AGENT/Docs/plans/band6_fog_of_war_implementation_plan_2026-07-03.md` — `B6-FOW`
  (`[FOW-1..7]`). Radius fog behind one `compute_visible_tiles()` seam (occlusion-swap
  later, no caller change); `MapData.fog_enabled` encounter-layer flag; fog-mask render
  + enemy-node hiding; **per-step ambush interrupt** (the one real complexity — hooks
  `Unit.move_along_path` l.548); AI cheats (one seam `_living_hostiles_for_faction`
  l.319, zero change); `discovered_units` = only new save field; authored reveal tools
  (event rooms via MET `reveal_tiles`, lightable braziers as `map_objects`). **Overlay
  coordination:** MRD reserves source 4, so fog takes `OVERLAY_FOG` source 5 and
  registers in MRD's precedence-overlay registry as a base layer. Slices 1-4 buildable
  once `fog_enabled` exists; save trails suspend; reveals trail MET/DCH.
- `AGENT/Docs/plans/band6_destructible_terrain_implementation_plan_2026-07-03.md` —
  `B6-DTR` (`[DTR-1..8]`). One generic breakable = a **real `Unit` quarantined from the
  roster** (`is_destructible_object` — main risk = enumerating every "for each unit"
  skip guard); full damage pipeline (forced hit, no counter, anything-breaks-anything,
  emergent HP+Def durability); `on_break` typed event list (v1 = terrain + `reveal_tiles`
  bridge; spawn/flag/loot stubbed); zero new save (rides DCH `map_objects_state`).
  Hard gate **`B4-MAP-OBJECTS` (DCH)**; `on_break` runner migrates to MET's
  `object_broken` trigger. Grounded `GameMap._CHAR_TO_SOURCE` l.6, `GridManager.
  get_attackable_enemies_from_tile` l.444, `MapCursorTargeting` l.72.
- `AGENT/Docs/plans/band6_prep_progression_implementation_plan_2026-07-03.md` —
  `B6-PREP-PROGRESSION` (`[BEA-1..9]` + `[THL-1..8]`, arena/recruit excluded). Bonus-EXP
  PHB panel (banked pool via `grant_bonus_exp` award → `Unit.add_exp` l.610, no new
  leveling code) + training-hall PHB panel (open registry of benefit types routed to
  owning systems). Two genuinely-new ops: the shared **permanent stat-gain primitive**
  `apply_permanent_stat_gain(stat,n,cap)` (`[THL-3]`, also backs stat-boosters) and the
  **two-scope resource wallet** (`party_gold` l.153 → `{resource_id:amount}` +
  per-unit F7 pools). Gates: `B3-PHB`, `B3-STAT-REGISTRY`, `B2-RESOURCE-LEDGER`,
  `B4-PXP`, `B3-TCV`/`B3-REQ`.

Grounded touchpoints against the live tree (`UnitData.line_of_sight` l.33 /
`ClassData.base_line_of_sight` l.24 / `Unit.gd:1059`; `MoreInfoContent.gd:34` already
documents LoS "once that system is active"; overlays `OVERLAY_BLUE..DARK_RED` 0-3;
`add_exp`/`level_up`/`LevelUpScreen`; `party_gold` snapshot). Confirmed **no**
`map_objects`/PHB/`extra_stats`/`advance_proficiency` in code yet — drafted against the
planned Band 1-4 APIs, same caveat as the other Band 6 plans.

## Validation
- `python3 AGENT/Docs/gen_docs_index.py` then `check_docs.py`: 21/21 green.
- Docs-only change; no Godot suite run (planning artifacts, no code).

## Commits
- (this commit) — three Band 6 plans (FOW/DTR/PREP-PROGRESSION) + control-plane wiring
  (3 rows flipped Target design → Planned, pointed at the new plans) + INDEX regen.

## Next Session
- **All v1 Band 6 rows now have plans.** Remaining un-planned Band 6 rows are
  `B6-RESCUE-CARRY` / `B6-PROPERTY-CAPTURE` (narrowed to *expansion* — foundations in
  Band 5 / `campaign_status_property_recruitment_plan`), `B6-INPUT` (3 plans exist),
  `B6-WEB-DEBUG` (plan exists), `B6-SPRITE-IMPORTER` (IMP decisions still open, Q12
  held). No further Band 6 v1 planning outstanding.
- **Owner scope forks left open (pull if wanted):** Arena (`B7-ARENA`) and
  recruit-purchase (`[THL-8]`, B7-PVP/recruitment) — both would be extra slices + their
  gates if pulled into v1.
- Small Band 5 tidy-ups still pending (C6 berserk+scorer test, C7 disabled-with-reason,
  manifest open content picks).
- Parallel thread unchanged: when the v0.2.5 playtest returns, triage
  `VAL-V023-DISPLAY` → flip → `VAL-V023-MERGE-CHECK`. Ungated build option: `B1-PKGA`
  (RngService), the root gate.

## Read First Next Session
1. This note's scope section (all Band 6 v1 rows are now planned).
2. The three `band6_*_implementation_plan_2026-07-03.md` plans (FOW/DTR/PREP).
3. `band5_plus_preimplementation_questions_review_2026-06-30.md` for the surrounding
   Q8-Q13 Band 6 design context.
