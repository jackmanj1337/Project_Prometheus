# Session Note — 2026-06-20i

**Topic:** Full docs review focused on TODO/deferred/blocked/vaguely-referenced item
findability, then consolidated **all open work onto the existing roadmap** (GDD_10) so
there is one single place to find it. Docs-only; check_docs 16/16 green.

## 1. Audit — where the "single place" promise was breaking

GDD_10 (roadmap), `decision_index.md`, and `GDD_Feature_Index.md` already work as canonical
registries. Gaps found:

1. **Orphaned forward workstream:** the **debug Web build** and the **input-mode / gamepad
   architecture** lived only in handoff/design docs + session notes + memory — not tracked in
   the roadmap. The gamepad layer is called the *keystone* dependency yet had no roadmap entry.
2. **`v0.2.2_review_checkbacks_2026-06-20.md` was a parallel deferral tracker** — its
   live-verify check-backs and the **V021-09 `until_unequipped`/`until_end_of_map` forward
   primitives (rendered, no producer)** existed only there.
3. **Pending-validation items scattered per playtest round; 8.4 Strength Tonic appeared
   unresolved in BOTH the v0.1.5.0 and v0.2.0 sections** (duplicated).
4. **Input-mode's 4 open design decisions** were not in any tracked registry.

User decisions (AskUserQuestion): (Q1) **track ALL items on the existing roadmap**, not a new
register doc; (Q2) put unresolved **design decisions inline in the roadmap entry**.

## 2. Changes (GDD_10)

- **New `## Open Items Register`** after the Status Snapshot — single-scroll index of every
  open/deferred/blocked/to-verify thread in 7 grouped tables (A near-term builds & forward
  workstreams, B open v0.2.1 findings, C live-verify check-backs, D forward primitives with no
  producer, E outstanding validation/rerun — 8.4 unified, F release gates & blockers, G
  deferred milestones & backlog). Each row = status + pointer to detail home.
- **New `## Forward Platform Workstreams (Web Debug + Input Modes)`** — the two orphaned
  workstreams now have a tracked build/sequencing home: Debug Web build (plan/handoff links +
  carry-forward decisions + 4 slices + risks) and Input-mode/gamepad architecture with the **4
  open design decisions inline** + DoD. (Release Gates §Renderer & Platform Targets stays the
  platform-policy owner.)
- **Appendix C** gained rows for the 4 forward docs.

## 3. Anti-drift pointers added

So nothing re-orphans, each previously-only-home doc now points back to the roadmap as the
single index: `v0.2.2_review_checkbacks_2026-06-20.md`,
`input_mode_architecture_design_2026-06-20.md`, `handoff_2026-06-20_web_debug.md`.

## Verification

`python3 AGENT/Docs/check_docs.py` → 16/16 PASS (new headings unique, all backticked paths
exist, GDD_10 exempt from status-word/label checks). No new `__pycache__` artifact. Docs-only,
no runtime code touched, suite not re-run.

## 4. Follow-on fix — V021-09 doc-sync guard (check_docs [17])

User asked which register items are fixable now. Triaged: most are gated (live verify,
playtester, no `gh`/token, milestone, decision, or release-time — the §F debug-aid removals
must NOT happen now, they're in active playtest use). The one clean headless win: the V021-09
check-back left a doc-sync question open — the duration vocabulary was enforced code-side (test
invariant) but nothing kept GDD_07's list in sync with `GameConstants.VALID_DURATION_TYPES`.

Added `check_docs.py` check **[17] `check_duration_type_vocabulary`** (mirrors the mouse_cursor
check [14]): asserts GDD_07 documents every `VALID_DURATION_TYPES` value. Verified with a
negative test (injected a bogus const value → check [17] FAILs; reverted → green). Marked the
check-back **RESOLVED** in `v0.2.2_review_checkbacks_2026-06-20.md`. check_docs now **17/17**.

## Plans for next session

Unchanged by this session — still: finish the v0.2.3 branch → `main` PR merge, run Part I live
verify of `playtest_checklist_v0.2.3.md`, flip `V021-18/19` → `[x]`, triage the return, then
the debug Web build. All now indexed in GDD_10 *Open Items Register* §A.

## Note for future consideration

Considered a `check_docs.py` guard that every forward design/handoff doc must be referenced
from GDD_10 (durable anti-orphan enforcement, DoD#2 spirit) but skipped it — high false-positive
risk against historical/superseded handoffs. The Open Items Register + DoD#1 sync rule is the
current enforcement. Revisit if orphaning recurs.
