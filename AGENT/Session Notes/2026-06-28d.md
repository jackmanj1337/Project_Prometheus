# Session Note — 2026-06-28d

## Topic
Pre-GDD triage follow-ups for activities and calendar-lite counters.

## What was done
- Updated `AGENT/Docs/plans/planned_unimplemented_feature_triage_2026-06-28.md` so the minigame work is
  visible before the unified GDD rewrite.
- Split the old broad "mini-game module" row into:
  - `ActivityRegistry / ActivityRunner seam` as a V1-optional shared launch/result bridge;
  - three activity-template feasibility prototypes as Parked;
  - arbitrary public scripting VM as Parked.
- Added a note that shop/convoy panel UI/UX should be designed at the start of the Band 3 convoy/shop
  implementation slice, after F9/IEQ/resource contracts are in place.
- Added a calendar-lite triage note for `total_maps_played` and `story_maps_played`, supporting
  garden/brewing timers, restocks, and territory-pressure mechanics without a full calendar.
- Updated `AGENT/Docs/plans/unified_gdd_pass_followups_2026-06-28.md` with the activity-scope and
  calendar-lite reminders for the GDD rewrite.

## Answers captured
- Shop/convoy UI/UX is not scheduled as a separate early design track. Current plan: Band 0-2 foundations
  first, then design the detailed shop/convoy panel UX at the start of the Band 3 convoy/shop slice.
- The map-counter idea should be generic campaign cadence state exposed through TCV/REQ/MET, not a
  garden-specific subsystem.

## Validation
- `python3 AGENT/Docs/check_docs.py` — PASS
- Commit hooks also ran `check_docs.py` and `check_rng_usage`; docs-only commits skipped Godot tests.

## Commits
- `3792f92` — `Update pre-GDD triage for activity templates`
- `90bb8ce` — `Add calendar-lite counters to pre-GDD triage`

## Next session
- If continuing scheduling work, decide whether `ActivityRegistry / ActivityRunner seam` remains
  V1-optional or moves into a V1-lean slice with one validated template.
