# Session Note - 2026-07-03e

## Topic
Take care of the two v1 scope-forks left open by 2026-07-03d (**Arena** +
**recruit-purchase**) and the outstanding **Band 5 tidy-ups** (C6, C7, manifest
content picks). Owner: "let's take care of the arena, recruit purchase and the tidy
ups" — i.e. pull both forks into v1 and close the tidy-ups.

## What was done

### Arena (pulled into v1)
Wrote `AGENT/Docs/plans/band7_arena_implementation_plan_2026-07-03.md` (`B7-ARENA`,
`[BEA-4..9]`). Keeps its `B7-ARENA` id but is now a v1 builder-palette feature. Five
slices: (1) `arena` PHB panel + config (`risk: lethal|safe`, `structure:
single|escalating`, opponent source, bet/reward; `[SAC]` on-map dual-surface);
(2) the **shared parametric unit generator** `generate_unit(spec)` — opponents =
authored table OR parametric spec, the SAME generator `[THL-8]` `generated` recruits
consume (one generator, two consumers); (3) sandboxed real `CombatResolver` 1v1 +
gold/EXP/wexp payout, atomic, `RngService`-deterministic; (4) risk resolution
(`lethal` → single `handle_death` gated by `death_mode`; `safe` → forfeit bet, no
death) + UI warning; (5) escalating continue/cash-out ladder. **No new save**
(atomic match, bets reuse the gold ledger). Gates: `B3-PHB`, `B2-DEATH-LIFECYCLE`,
`B2-RESOURCE-LEDGER`; reuses live `CombatResolver`. Flipped the control-plane row
Target design → Planned.

### Recruit-purchase (pulled into v1)
Promoted the prep-progression plan's deferred "Slice 5" into a **real slice**
(`[THL-8]`): a roster-**add** offer reusing the slice-4 offer/cost/gate/cap
machinery, adding a unit via the `[RCR-3]` `recruit()` API. Four source modes —
`grunt` / `authored` / `generated` (reuses the arena Slice 2 generator, not a second
one) / `ransom` (`[RCR-5]` captured prisoner). Also the `[PVP-3]` buy-phase
mechanism. No new save (bought recruits ride the roster save; param specs are
authoring data). Updated the prep-progression Purpose / Non-Goals / Scope /
Dependency / Commit Order and the control-plane `B6-PREP-PROGRESSION` row
(now "Bonus EXP, training halls, recruit-purchase"; added `B4-RECRUIT-BASIC` +
`B7-ARENA` generator gates for Slice 5).

### Band 5 tidy-ups (C6, C7, manifest picks)
- **C6** (`band5_ai_implementation_plan`, Slice 3): added an explicit berserk step +
  test — a berserked AI unit's scorer uses a **hostile-to-all** target set (own side
  included) and **bypasses the profile weights**, honoring the condition's
  targeting-override as one widened enumeration path (not a special scorer).
- **C7** (`band5_conditions_skills_implementation_plan`, Slice 3): capability gating
  now adopts the Band 4 `shown_disabled` convention — a suppressed action row is
  **shown greyed with the condition's reason** ("Silenced"), not removed + a test.
- **Manifest content picks** (`band5_v1_content_manifest`): resolved all four —
  poison = **flat 3 HP/turn** (% stays a data option), fixed-N counts sleep 3/stun
  1/berserk 3; `on_level_up` showcase = **Aptitude** (growth skill), grant/revoke =
  generalized `rally_skill`; combat arts = Wrath Strike + Poison Edge + Fire AoE +
  **Sunder** (+Crit/-Hit) + **Windsweep** (no-counter melee); **Berserk staff IN v1**
  (only showcase of `overrides_targeting`; C6 wires the scorer). Added Sunder/
  Windsweep to §4's concrete-styles list; updated the review doc §D step 2 to
  RESOLVED.

## Validation
- `python3 AGENT/Docs/gen_docs_index.py` then `check_docs.py`: 21/21 green.
- Docs-only change; no Godot suite run (planning artifacts, no code).

## Commits
- (this commit) — arena plan + recruit-purchase promotion + Band 5 tidy-ups (C6/C7 +
  manifest picks) + control-plane rows (`B7-ARENA` → Planned/v1;
  `B6-PREP-PROGRESSION` recruit-purchase) + INDEX regen.

## Next Session
- **All identified v1 planning is now written.** Band 5 (4 plans) + Band 6 (FOW/DTR/
  prep/relationships/MRD/mutable-state) + Arena all have implementation plans; the
  Band 5 review has no open owner gates (C1-C7 all resolved).
- Remaining un-planned rows are expansion/optional, not v1: `B6-RESCUE-CARRY` /
  `B6-PROPERTY-CAPTURE` (expansion), `B6-SPRITE-IMPORTER` (IMP decisions open),
  `B7-*` optional packs, `B7-PVP` / `B7-PROPERTY-RECRUITMENT` (now unblocked to
  consume the recruit-purchase slice + arena generator).
- Parallel thread unchanged: when the v0.2.5 playtest returns, triage
  `VAL-V023-DISPLAY` → flip → `VAL-V023-MERGE-CHECK`. Ungated build option:
  `B1-PKGA` (RngService), the root gate — the natural place to START building.

## Read First Next Session
1. This note (v1 planning is complete; the build gates are Band 1-3 + `B4-IEQ`).
2. `band5_plans_review_2026-07-03.md` §D (all concerns resolved).
3. The two new/updated plans: `band7_arena_implementation_plan_2026-07-03.md` +
   `band6_prep_progression_implementation_plan_2026-07-03.md` (Slice 5).
