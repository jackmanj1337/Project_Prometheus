# Session Note 2026-07-08d — v0.3.0 full-scan Pass 4

## Goal

Continue the resumable v0.3.0 full code-pillar scan
(`AGENT/Code Reviews/v030_full_scan/`). Cadence is **one pass per invocation**;
`_TRACKER.md` is the resume anchor. First TODO was **Pass 4 — Input display &
rebind UI** (2 files), which must re-confirm the carried Medium (rebind UI omits
`more_info`, `peek_range`, `zoom_in/out/reset`) and land the Pass-4 half of the
Pass-3 input-mode-refresh Medium.

## Progress

- Read `_TRACKER.md` + the Pass 3 note; working tree = head `b7bcfd2` for review
  (HEAD was `b125c30`, docs only — no production lands after `b7bcfd2`).
- Read at head: `SettingsScreen.gd` (952 lines) + `InputDisplay.gd` (266 lines),
  their diffs (`+379/−12`, `+220`), and their suites (`test_settings_screen` 24,
  `test_input_display` 7). Cross-read `InputModeManager` and `project.godot [input]`.
- Wrote `AGENT/Code Reviews/v030_full_scan/04_input_display.md`: **1 Medium
  (carried, re-CONFIRMED) + the Pass-4 half of Pass-3's Medium; 2 Low.** No new
  correctness bugs.
  - **M1 (carried Medium, re-CONFIRMED):** `_KEYBIND_LABELS`
    (`SettingsScreen.gd:630-643`) lists 12 actions but the InputMap defines 5 more
    non-debug player actions — `more_info`, `peek_range`, `zoom_in`, `zoom_out`,
    `zoom_reset` — all grep-confirmed live consumers (`MapCursor`, `HUD`,
    `AttackPreview`, `UnitDetailsScreen`, `InputDisplay`). Two consequences: (1)
    unbindable shipped controls; (2) **silent conflict blind spot** —
    `_recompute_keybind_conflicts` (`:843`) only scans the 12, so rebinding a listed
    action onto e.g. default `more_info`=`F` raises no red row / no Apply block. Same
    closed-vocabulary-vs-registry smell `AGENTS.md` warns about. Fix: add the 5 rows,
    or (stronger) derive the editable set from the InputMap minus debug/`ui_*` with a
    display-label override map.
  - **M2 (NOT double-counted — SettingsScreen half of Pass-3's M1):** the `input_mode`
    row (`:104-113`) has no `"apply"` hook and `_reset_keybindings_to_defaults` (`:806`)
    rewrites `input_mode` without refreshing `InputModeManager`, so the active mode /
    prompts stay stale. **Fix-shape wrinkle:** the `"apply"` hook calls
    `sm.call(schema_row["apply"])` — a method on **SettingsManager**, so Pass-3's
    proposed `InputModeManager.refresh_from_settings()` can't be reached directly.
    Prefer having InputModeManager subscribe to a SettingsManager settings-changed
    signal (covers both the row and the reset path). `refresh_from_settings` still
    absent (correct — doc-only).
  - **L1:** `pad_rebind` stored via `get_node_or_null("BtnPadRebind_%s")` re-fetch
    (`:718`) instead of the in-scope `pad_button` local — asymmetric vs `rebind`/`clear`,
    fragile on the name string. Hoist the local.
  - **L2:** `more_info_hint_for` hardcodes an `"F"` fallback (`InputDisplay.gd:199`)
    when `more_info` is unbound → hint advertises a key that isn't bound. Prefer a
    neutral fallback.
- **Positive contract check:** InputDisplay/SettingsScreen mode strings
  (`"auto"/"gamepad"/"touch"/"mouse_keyboard"`) match `InputModeManager` `MODE_*`
  constants exactly, which match the `_ENUM_SETTINGS` input_mode row values — one
  vocabulary across all three sites. Prompt functions are pure + cleanly testable.
- Flipped tracker Pass 4 → DONE + Log line.
- Pre-commit green: `check_docs.py` all 26, RNG guard PASS, Godot skipped (docs-only).

## Next

- **Pass 5 — Map/turn core** (5 files: `MapCursor.gd`, `MapCursorTargeting.gd`,
  `GridManager.gd`, `GameMap.gd`, `TurnManager.gd`). This pass carries the two biggest
  deferred items: (1) the **carried High fix site** — `GameMap` never calls
  `RngService.start_map()` before the first snapshot/RNG event (Pass 2 H1); confirm the
  seed-before-`take_map_snapshot` ordering; (2) the **`MapCursor` arm-on-decode
  ordering** flagged in Pass 3 (decode MOVE edge + `poll_direction` analog poll both
  drive the cursor; safe only if the repeat timer arms on the same frame the decode
  MOVE is consumed). Also fold M1's note that zoom actions bind pad triggers/axes.

## Commits

- `ad059b9` — v0.3.0 full-scan Pass 4: input display & rebind UI (1 Med + carried half, 2 Low).
- Session note + INDEX row in the closeout commit after `ad059b9`.
