# Session Note — 2026-06-21

**Topic:** Planning-only. Closed out the **input/controls cluster (§1)** drafting: wrote the
two remaining implementation plans (broader input-mode resolver; key-rebind UI) **decision-
free**, and consolidated every choice they couldn't make into one open-decisions register.
Docs-only; check_docs 17/17; no `__pycache__` tracked.

## Method
Per the standing method note, read the actual code before drafting: `SettingsManager.gd`
(persistence, `[controls]`, `reset_section_to_defaults`, `rebind_action`, the `ui_*` mirror
+ baseline), `SettingsScreen.gd` (the **read-only** keybinding list, "Rebinding deferred to
Phase 2"), `check_docs.py` (`[14] check_mouse_cursor_modes` value-set pattern), and
`project.godot` (the 19 actions). Grounded every draft in those facts.

## Deliverables (3 new docs)
1. **`input_mode_resolver_implementation_plan_2026-06-21.md`** — the broader Auto/Touch/K&M
   resolver the gamepad plan left out: state model (`VALID_INPUT_MODES` /
   `VALID_TOUCH_CONTROLS` consts + `normalize_*` back-door), device-class detection, the
   pure detect-floor + conditional-promotion resolver (signal de-dup), `[controls]`
   persistence + the `mouse_cursor` relocation/migration out of `[gameplay]`, gray-don't-
   hide + back-door availability (with the `DEDICATED_TOUCH_READY` flag), **two new
   `check_docs` guards** (DoD#2), headless test plan, 3 slices. Slice 1 (persistence +
   guards) is independent of the owner decision.
2. **`key_rebind_ui_implementation_plan_2026-06-21.md`** — Phase 2 of the keybind list:
   capture flow, conflict policy, Reset escape hatch, **per-device-slot binding model** so a
   K&M rebind can't wipe the pad event once the gamepad layer ships, Esc-reserved + mirror-
   aware conflict detection, composition with the input-mode setting, headless tests,
   3 slices. Slice 1 (K&M rebinding) is build-ready under the recommended answers.
3. **`input_controls_open_decisions_2026-06-21.md`** — the single decisions register.
   **[ICD-1]** active-mode owner (`SettingsManager` vs new `InputModeManager` autoload —
   *rec: dedicated autoload*); **[ICD-2]** resolver landing point (fold into gamepad slice 3
   — *rec: fold*); **[ICD-3]** touch-vs-`emulate_mouse_from_touch` detection (*rec: touch-
   first, smoke on iOS*); **[ICD-4]** binding model (*rec: per-device slots*); **[ICD-5]**
   rebindable set + pad-capture-now (*rec: all non-debug; capture any device, glyphs later*);
   **[ICD-6]** conflict policy (*rec: block + warn*); **[ICD-7]** carried tune-live items
   (not blocking). Each has options + a recommendation + a resolution log.

## Key findings that shaped the drafts
- **`rebind_action` is destructive** (`keybindings[action] = [event]` overwrites the whole
  list) → the moment the gamepad plan adds joypad events, a keyboard rebind would wipe them.
  Hence [ICD-4] (per-device slots) and the rebind plan drafted around it.
- **`emulate_mouse_from_touch`** means a tap emits *both* a touch and a mouse event → the
  resolver's detection needs an explicit touch-vs-emulated rule ([ICD-3]); also load-bearing
  for the web build's V021-17 tap path.
- **The `ui_*` mirror + baseline already solves** the "old key stuck on ui_accept" edge — the
  rebind UI reuses it, doesn't re-implement it; conflict detection checks *game* actions only.

## Verification
`check_docs.py` → **17/17 PASS** (new docs match the Status/Last-verified header format; no
new value-sets shipped yet so no guard added this session — those land *with* the code per
DoD#2). All `__pycache__` are gitignored; nothing untracked but the 3 docs + backlog edit.

## Bookkeeping
Updated `planning_backlog_2026-06-20.md` §1 (both items → ✅ PLAN DRAFTED) + the Recommended-
focus section (next §1 step = a **decisions pass** on [ICD-1..7], not more drafting) +
Last verified 2026-06-21.

## Plans for next session
1. **Decisions pass on `input_controls_open_decisions_2026-06-21.md`** — resolve [ICD-1] +
   [ICD-4] first (they unblock the most build work), then [ICD-2/3/5/6]. After that, §1
   moves from planning → execution.
2. **Or** the execution track (needs screen / `gh`): v0.2.3 `main` PR merge → live-verify
   Part I → flip V021-18/19 → triage → debug Web build.
3. After §1 closes: **§2 Campaign / save** cluster planning.
