# Session Note - 2026-06-29i

## Topic
Planned CampaignStatusRecord carry-forward, on-map property capture, recruit
stores, cursor-triggered map objects, and AI recruitment choices.

## What was done
- Added
  `AGENT/Docs/plans/campaign_status_property_recruitment_plan_2026-06-29.md`.
- Recorded `CampaignStatusRecord` / `CampaignCompletionRecord` as a compact
  portable artifact for same-campaign NG+ and same-author sequel imports.
- Added control-plane rows:
  `B6-CAMPAIGN-STATUS`, `B6-PROPERTY-CAPTURE`,
  `B7-PROPERTY-RECRUITMENT`, and `B7-AI-RECRUITMENT`.
- Updated the map-object component contract with `cursor_activatable` and
  `capturable_property` component families.
- Updated planned feature triage, `GDD_10`, and `GDD_Feature_Index` so the new
  work appears in the proper build-order homes.
- Regenerated `AGENT/Docs/INDEX.md`.

## Validation
- `python3 AGENT/Docs/gen_docs_index.py`
- `python3 AGENT/Docs/check_docs.py`
- `git diff --check`
- Commit hook also ran `check_docs.py` and `check_rng_usage`; docs-only change
  skipped the Godot suite.

## Commits this thread
- `2a4e6b9` - `Track campaign status and property systems`

## Next session
- Return to the existing Band 1 implementation queue unless the owner changes
  priority: `B1-PKGA` -> `B1-F1` -> `B1-SAVECODEC` / `B1-CST`.
- When F1 is locked, reserve rows for imported campaign-status record id,
  checksum, derived vars/resources, property owner/capture state, recruit-store
  purchases, produced units, and delayed spawns.
- When property recruitment is planned, reuse the PvP recruitment UI and MET
  spawn occupancy policy instead of creating a separate production screen.
