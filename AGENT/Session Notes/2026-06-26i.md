# Session Note — 2026-06-26i  (Interceptor-family gap-audit — 3 composition closers + `[ICP-1..6]` residuals)

**Topic:** Deliberate gap-audit of the three-phase interceptor family (`[RDR]`+`[CVR]`+`[RCT]`): find major
gaps, close what composes, log the residuals + closure cost. Branch `docs-reorg-2026-06-23`. Planning only.

**Outcome:** Three composition closers added; the family's operation set is complete bar *delay*. Residual
gaps captured as new register **`[ICP-1..6]` RESOLVED** (closure paths set — mostly forward-reqs). RDR/CVR
ranges bumped (RDR-1..14, CVR-1..7); atlas F5 sharpened; candidate I updated. INDEX/REGISTERS regen; **19
checks green**. 1 commit.

---

## 1. Lens used
"What operations can you do to an incoming effect?" (block/reduce/reflect/redirect/absorb/split/convert/
delay/transform/copy) + "what events/timing does the family miss?"

## 2. Closed by composition (small params, fit the model)
- **`[RDR-14]` `gain` clause** — symmetric inverse of RDR-13 `cost`: **fill** a pool (HP/mana/custom
  `stored`) by a term (`absorbed_value`). Closes **lifesteal / absorb-into-resource / charge**.
- **`[RDR-2]` `emit.kind`** (default = incoming kind) — **output-kind change** (damage→heal vampiric
  reflect; debuff→buff).
- **`[CVR-7]` `share_disposition: to_protector | negate`** — **in-place ward** (negate part of an ally's
  hit, nobody takes the rest; `share:1.0+negate` = reactive full block, gate with trigger/cost).
- **`[RCT-6]` board re-eval rule** — each phase's selector sees prior-phase mutations (the swap).
- Already-composing: **full negate/parry** (`absorb:full`+no target+`unconditional`), **copy** (`absorb:0`+
  target). ⇒ operation set complete except **delay** (ICP-3).

## 3. Residual gaps — `[ICP-1..6]` (closure paths, not core-model forks)
- **ICP-1 per-strike vs per-exchange** (brave/follow-up fire per strike → `uses:1`/"first N" mis-count):
  add `trigger_scope: per_strike|per_exchange` + a latch keyed to `exchange_id` (reuse REQ-10 latch);
  **forward-req:** event carries `exchange_id`+`strike_index`. Small-moderate.
- **ICP-2 universal effect event** (environmental/tile/staff): **forward-req** sharpening RDR-7 — all
  application through `effect_applied`, **nullable `source`** (absorb/ward still work; cover/RCT correctly
  don't fire). No cost beyond M8 honoring it.
- **ICP-3 delayed/stored release:** composable half done (`gain`→`stored` pool + F11 on-attack read);
  **auto-release deferred** to `[MET]` scheduling.
- **ICP-4 forecast dry-run** of all 3 phases incl. counter: **forward-req** extending the REQ-15
  projection API to a pure dry-run. Engineering, no new design.
- **ICP-5 rewind/undo** across the reaction window: **forward-req** — declare→react→resolve→counter = one
  atomic undoable transaction (F1 reserves the boundary; REQ-10 latch already assumes rewind-safe).
- **ICP-6 layered cover** (two protectors split one hit): **deferred, minor** (priority picks one).

## 4. Files / commit
- `registers/redirect_effect_open_questions_2026-06-26.md` — RDR-14 `gain` + RDR-2 `emit.kind` amend;
  spine step 5 (drain+fill) + board-re-eval note; recipes (vampiric); frontmatter → RDR-1..14.
- `registers/cover_intercept_open_questions_2026-06-26.md` — CVR-7 `share_disposition`; frontmatter → CVR-1..7.
- `registers/reactive_reposition_open_questions_2026-06-26.md` — RCT-6 board re-eval rule.
- `registers/interceptor_family_gaps_open_questions_2026-06-26.md` — **NEW** `[ICP-1..6]` RESOLVED.
- `design/candidate_systems_2026-06-23.md` — candidate I gap-audit bullet.
- `plans/feature_dependency_atlas_2026-06-23.md` — F5 row sharpened (universal event + exchange ids +
  projection dry-run).
- INDEX/REGISTERS regen; `check_docs.py` 19 green. Commit: _(this session)_.

## 5. NEXT SESSION
- Interceptor family (`[RDR]`+`[CVR]`+`[RCT]`+`[ICP]`) **fully walked + gap-audited.** All build-time
  forward-reqs are on the atlas F5/F1/MET rows.
- Return to the pre-A5 candidate-systems path (the whole family was an owner detour from candidate G).
