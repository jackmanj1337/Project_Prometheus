# Session Note - 2026-08-04 v0.7.0 consolidation and the claim-model handoff

## Branch context

- Branch: `agent/integration`
- Base branch: `agent/integration`
- Base SHA: `cd7797fe0985999bac16fd4c6b46e1e299c580fe`
- Coordination Work ID: `SESSION-CLAIM-MODEL-CONTRADICTION-2026-08-04`

## What was done

**Brought the v0.6.1 release line back onto `agent/integration` and consolidated
every remaining product branch** as the v0.7.0 base. The build is deliberately
NOT cut.

The consolidation was much smaller than the branch list suggested: the release
tip `dedicated-touch-controls` had already folded in the whole v0.6.x chain plus
`web-transfer-and-identity`, `web-export-preset`, `viewport-anchoring`,
`text-entry-implementation`, `terrain-variants-pack-terrain`,
`docs-line-plans-hook`, and `pwa-playtest-hosting`. Only 7 docs-line commits on
integration were missing from it. Nine merges landed the lot; conflicts were
almost entirely the both-add top of `AGENT/Session Notes/INDEX.md`.

Two conflict resolutions were judgement calls worth recording:

- `GDD_07_Input_Cursor.md` — took the `text-entry-implementation` side. It is
  newer and describes the built state, and it already carries the
  `text-entry-strategy` branch's TEXT-06 rule revision, so nothing was lost.
- `project_control_plane_2026-06-29.md` — took HEAD, then **corrected** its claim
  that "the v0.6.1 build record and checklist … are not merged here yet", which
  these merges made false.

Then built `[TER-1]`/`[TER-2]` (see the terrain-variants note) and wrote the
claim-model handoff below.

## Commits claimed

- `d55b4f1892a87a0993359d45654f09d1eec0746e` — Hand off the session-claim model contradiction
- `678b8720061f2684a2fa79e89e78f4945fce4414` — Point the claim-contradiction handoff at its real tracker id

## Gates

- `bash run_tests.sh` — all suites green after every merge and at the tip.
- `python3 AGENT/Docs/check_docs.py` — green (DoD#1 required bumping
  `GDD_10_Roadmap.md`'s Last verified; its content was already accurate).
- `agent-push.sh` — integration pushed at `d7a62d7f`, 157 commits verified from
  the merge base.

## Next

Read
[`session_claim_model_contradiction_handoff_2026-08-04.md`](../Docs/plans/session_claim_model_contradiction_handoff_2026-08-04.md)
first — this very note exists because of the contradiction it describes.

Still open before a v0.7.0 build: the Windows visual pass, which gates BOTH
`IMPL-TERRAIN-VARIANTS-AND-PACK-TERRAIN-2026-08-01` and the outstanding
`IMPL-VIEWPORT-ANCHORING` owner pass, so one Windows session closes two rows.

Zero-content Slice 3 (base-pack extraction) remains blocked on owner product
decisions: the FE numeric audit found 10 of 12 weapons and all 23 non-placeholder
classes reproduce an older TTRPG table, and `LEG-ENGINE-ASSET-PROVENANCE` (51 art
assets) is unreviewed.
