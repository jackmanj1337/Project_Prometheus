# Session Note 2026-07-08 — v0.3.0 full-scan Pass 1

## Goal

Continue the resumable v0.3.0 full code-pillar scan
(`AGENT/Code Reviews/v030_full_scan/`). Cadence is **one pass per invocation**;
`_TRACKER.md` is the resume anchor.

## Progress

- Read `_TRACKER.md` + `00_scope.md`; first TODO was **Pass 1 —
  Save/persistence codec** (3 files).
- Read at head `b7bcfd2` (via working tree — no production lands after `b7bcfd2`):
  `scripts/save/SaveData.gd`, `scripts/save/SaveCodec.gd`,
  `scripts/autoloads/SaveManager.gd`, plus their three test suites.
- Verified two falsifiable claims by grep: `_vector_array_from_variant` is
  defined-but-never-called; `has_continue_save` reaches production via
  `MainMenu.gd:42-43` yet both if/else branches return `has_suspend()`.
- Wrote `AGENT/Code Reviews/v030_full_scan/01_save_persistence.md`: **7 findings,
  all Low.** No correctness bugs. Highlights — L1 dead if/else + needless index
  disk read on the menu path; L2 dead helper; L3 redundant merge-then-overwrite in
  `_normalize_rules`; L4 duplicated JSON coercers across SaveData/SaveCodec; L6
  non-atomic single-slot `save_suspend` write. The carried **High**
  (`RngService.start_map()` on fresh maps) is out of scope here — it belongs to
  Pass 2/5.
- Flipped the tracker Pass 1 row to DONE (7 Low) + added a Log line.
- Pre-commit green: `check_docs.py` all 26 checks, RNG guard PASS, Godot suite
  skipped (docs-only).

## Next

- **Pass 2 — Determinism: state capture + RNG + combat** (7 files: `GameState`,
  `RngService`, `CombatResolver`, `SkillHandler`, `EnemyAI`, `CampaignRules`,
  `UnitData`). Re-confirm the carried High (`start_map()` lifecycle) in-context.

## Commits

- `5faecc4` — v0.3.0 full-scan Pass 1: save/persistence codec (7 Low findings).
- Session note + INDEX row in the closeout commit after `5faecc4`.
