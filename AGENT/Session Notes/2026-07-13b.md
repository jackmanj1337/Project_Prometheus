# Session Note - 2026-07-13b

## What was done

Picked up the v0.4 handoff and implemented the behavior-neutral
`B2-ACTION-EFFECT` contract groundwork.

- Added structured `ActionRequest`, `ActionContext`, and `ActionResult` records.
- Added the registry-backed `ActionPrimitiveRunner` and `ActionEffectRunner`
  autoload. Unknown primitives, missing handlers/subjects, and malformed params
  fail before mutation; dry-run follows the same validation path without commit.
- Corrected the `apply_active_modifier` preset schema to describe the live
  modifier parameters and declare `UnitData.active_modifiers` as its save field.
- Added a registry guard requiring every `kind = "mutation"` entry to declare
  `save_fields`.
- Migrated only `ItemHandler`'s existing `stat_buff` case to the shared primitive.
  Strength/debuff tonic behavior and consumption remain unchanged.
- Added focused tests for unknown/malformed no-mutation, deterministic handler
  lookup, dry-run, structured success, and reuse from `item` and `map_event`
  domains.
- Updated `GDD_01`, `GDD_10`, the feature index, and the control-plane row.

## Validation

- `scripts/check_env.sh`: passed (pre-existing local `.env` warning only).
- `AGENT/Docs/check_docs.py`: 27/27 passed.
- `scripts/ci/check_rng_usage.sh`: passed.
- Full `run_tests.sh`: 71 suites passed.
- The pre-commit hook repeated docs, RNG, and all 71 suites successfully.

## Commit

- `924459a` - Add registry-backed action effect runner.

## Next

- Keep requirement-gated availability/execution, promotion recovery UI, and
  broader domain migrations deferred to their owning tracks.
- The next Band 2 plan-order slice is `B2-RESOURCE-LEDGER`; review its F1 wallet
  rows and preserve victory-reward behavior before migrating any consumer.
- A returned v0.3.2 live playtest still preempts new v0.4 work at a clean commit.
