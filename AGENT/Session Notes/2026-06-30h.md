# Session Note - 2026-06-30h

## Topic
Band 1/2 plan + doc-structure review, autoload-order enforcement, Band 1 Slice 1
split, author-selectable hit-roll resolver, and the Band 3 planning handoff.

## What was done
- Reviewed the new TYPE-sorted documentation structure and both the Band 1 and
  Band 2 implementation plans. Found no blocking errors; flagged Slice 1 size,
  the two-RN player-visible math change, and the cross-plan autoload-order
  coupling.
- Added `check_docs.py` **check 21 (autoload order)**: a both-present guard that
  enforces `EventBus -> RngService -> {SettingsManager, GameState}` (Band 1) and
  `RegistryManager -> DataManager` (Band 2). Inert until those autoloads exist,
  then locks the order. Verified it catches a misordered sequence.
- Split Band 1 **Slice 1 into sub-slices 1a-1d** for reviewable, bisectable
  commits. Surfaced and fixed a real ordering bug in the original suggestion: the
  raw-RNG lint guard must land **last** (1d), not in 1a, because raw RNG still
  exists until the 1b/1c migrations remove it.
- Made the **hit-roll formula author-selectable** per owner decision: built-in
  `single_roll` / `two_roll` presets plus a sandboxed `Expression` author tier
  (fork-only GDScript handlers), with the seam baked into Band 1 Slice 1b now.
  - Captured the design in a new register
    `combat_roll_resolver_open_questions_2026-06-30.md` (`[CRR-1..8]`).
  - Reframed `RULE-001` two-RN from "the hit rule" to the **default resolver
    preset** (governance update owed in the Slice 1b implementation commit).
  - Resolver contract = declared fixed `rn_count` + pure
    `did_hit(displayed_hit, rns)` over engine-drawn RNs, preserving RNG-1/2/4.
  - Amended Band 1 Slice 1b to build the pure-predicate seam, added `hit_formula`
    to the Slice 6 CampaignRules field list, and added control-plane row
    `B3-COMBAT-ROLL-RESOLVER` for the registry-promotion follow-on.
- Wrote the Band 3 implementation-plan handoff
  (`band3_implementation_plan_handoff_2026-06-30.md`).

## Commits this thread
- `80731f4` - `Add check 21: enforce Band 1/2 autoload order`
- `7837a60` - `Split Band 1 Slice 1 into reviewable sub-slices 1a-1d`
- `0b01b74` - `Make hit-roll formula author-selectable (CRR-1..8)`
- (this note + handoff committed together)

## Validation
- Each commit: `python3 AGENT/Docs/gen_docs_index.py` (when index-affecting),
  `python3 AGENT/Docs/check_docs.py` (21 checks green), `git diff --check`. The
  pre-commit hook also ran `check_docs.py` and `check_rng_usage`.
- check 21 logic verified by simulation (misordered sequence flags the three
  expected violations; correct order flags none).
- All changes were docs/tooling-only; the Godot suite was skipped by the hook.

## Current State
- Band 1 and Band 2 implementation plans exist; Band 1 Slice 1 is now 1a-1d.
- Hit-roll authorability is a settled design (`[CRR-1..8]`), not yet built.
- Autoload order is now machine-enforced.
- Implementation priority remains `B1-PKGA` Slice 1a unless the owner keeps
  planning docs ahead of implementation.

## Next Session Target
Write the combined Band 3 implementation plan per
`band3_implementation_plan_handoff_2026-06-30.md`:
`AGENT/Docs/plans/band3_core_authoring_foundations_implementation_plan_2026-07-XX.md`.

## Read First Next Session
1. `AGENT/Session Notes/2026-06-30h.md` (this note).
2. `AGENT/Docs/plans/band3_implementation_plan_handoff_2026-06-30.md`.
3. The read-first list inside that handoff.

## Watchouts
- Do not start Band 3 implementation before `B2-REGISTRY` and the relevant Band 2
  services exist.
- The Slice 1b implementation commit must update `GDD_01`, `GDD_02`, the RNG
  design doc, and the decision log to reframe `RULE-001` (DoD#1).
- Keep the `B3-TCV` -> `B3-REQ` bootstrap order explicit in the Band 3 plan.
