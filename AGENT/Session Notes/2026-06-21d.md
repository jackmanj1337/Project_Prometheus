# Session Note — 2026-06-21d

**Topic:** Planning-only. **Drafted code-grounded plans + one-by-one OPEN-QUESTIONS registers
for every READY item on the 2026-06-21c planning-candidate list** (Tier 1 + Tier 2 + the
DOC-012 research item; GATED items intentionally skipped). One doc per item, mirroring the
§1 ICD / §2 CST register pattern. Read the actual code before drafting each, per the standing
rule. Docs-only; check_docs **17/17**; no `__pycache__`.

## Scope (user-chosen, 3 up-front questions)
- **Which items:** Tier 1 + Tier 2 + DOC-012; **skip GATED** (§2b deferrals, rewind mechanic,
  PvP mode, cross-version save — real upstream blockers).
- **Format:** ONE questions doc per item (`<item>_open_questions_2026-06-21.md`), not consolidated.
- **Pacing:** all items in one pass.

## Method
Read the grounding code first: `EnemyAI.gd`, `DataManager.gd`, `TileActions.gd`, `MapData.gd`,
`CampaignRules.gd`, `UnitData.gd`, `WeaponData.gd`, `ItemData.gd`, `InventoryEntry.gd`,
`GridManager.gd` (API survey), `rng_determinism_design_2026-06-11.md`,
`fe_map_sprite_importer_guide.md`, and `GDD_10` Open Items Register (D-A, OPEN-5, DOC-012,
importer 4 decisions at lines 2510–2528). Key code findings that shaped the drafts:
- AI-profile vocabulary is a CLOSED const (`DataManager._VALID_AI_PROFILES`); `UnitData.gd:66`
  already names the future four. Adding a profile = extend the const (+ DoD#2 guard).
- `TileActions` already reserves `activate`/`visit`/`shop` placeholders → the wiring point for
  doors/chests AND siege weapons. `ItemData.item_type` already includes `"key"`.
- `WeaponData` range is formula-based → a ballista is data; the missing piece is the mount/
  occupancy object model (shared with doors/chests).
- `InventoryEntry.uses_remaining == 0` is the broken-weapon state to reinterpret under the rule.
- The RngService build guide is near-complete → its register is GAPS ONLY (roadmap home,
  build-order scope, where `rewind_charges` lands, two-RN confirm).

## Deliverables (11 new docs in `AGENT/Docs/`)
**Tier 1:**
1. `package_a_rngservice_open_questions_2026-06-21.md` — [PKGA-1..4] (gaps only; design guide
   already exists). Roadmap home, Steps-1–2-vs-1–4 pre-§2 scope, `rewind_charges` placement,
   two-RN confirm.
2. `map_readability_open_questions_2026-06-21.md` — [MRD-1..6] (hover-peek + path arrows +
   designed threat range + grid-dim slider). Overlay precedence, hover trigger, aggregate vs
   individual threat, live-recompute perf, slider scope, build order.
3. `fog_of_war_los_open_questions_2026-06-21.md` — [FOW-1..6]. Radius vs true-LoS, per-map vs
   rule toggle, **AI-cheats-vs-symmetric (headline)**, reveal cadence, `discovered_units` save
   field, vision modifiers. (`UnitData.line_of_sight=4` already exists, unread today.)

**Tier 2:**
4. `ai_profiles_open_questions_2026-06-21.md` — [AIP-1..5]. Taxonomy (`boss` = `is_boss` +
   `guard_tile`, not its own profile), leash/anchor data home, wake-latch semantics, `ai_awake`
   snapshot field.
5. `doors_chests_open_questions_2026-06-21.md` — [DCH-1..6]. Key-first vs lockpick/staff, door-
   as-terrain vs object list, fixed loot, no-AI-v1, loot-to-opener (convoy deferred), opened-
   state snapshot.
6. `stationary_weapons_open_questions_2026-06-21.md` — [STW-1..6]. Shares the doors/chests
   `map_objects` model; occupy-tile mount, move-and-fire restriction, equipped-weapon override,
   emplacement ammo, no-AI-v1.
7. `map_sprite_importer_open_questions_2026-06-21.md` — [IMP-1..6] (the roadmap's 4 decisions
   pre-loaded + 2 gaps). SpriteFrames-only output, `Sprite2D`→`AnimatedSprite2D`, `data/` for
   generated, pure-logic extraction for headless test, class-keyed frame selection, P1 scope.
8. `datamanager_decomposition_open_questions_2026-06-21.md` — [DMR-1..3] (pure refactor; short).
   Phase-naming + unify error channels, keep load errors inline, add `_validate_all` test.
9. `broken_weapon_mode_open_questions_2026-06-21.md` — [BWN-1..5]. Reuse `uses_remaining==0`,
   global penalty const, repair-item-now (shop deferred), combat-stat application point.
10. `public_identity_rename_open_questions_2026-06-21.md` — [REN-1..5]. **Rename DISPLAY NAMES
    ONLY, keep ids** (save-transparent), data→prose→labels order, `check_docs` placeholder
    guard = completion signal. [REN-1] needs the user's actual naming direction.

**Research/decision (different treatment):**
11. `legal_licensing_open_questions_2026-06-21.md` — [LEG-1..5]. NOT a code plan. Identify
    source corpus + license, clean-reimplement vs license, attribution artifact, third-party
    asset audit, runs after D-A. [LEG-1] needs the owner to name the corpus; flagged "not legal
    advice / consult counsel."

## Cross-cutting patterns that emerged
- **Shared `map_objects` model:** doors, chests, AND siege weapons all want one
  "interactive thing at a tile" structure (+ one validation + one snapshot field). Sequence
  doors/chests first; siege inherits it.
- **§2 schema asks (reserve room, per the firming's data-growth principle):** `discovered_units`
  (fog), `ai_awake` (territorial), `map_objects_state` (doors/chests/siege), `rewind_charges`
  (Package A defers TO §2's CampaignRules consolidation). All small later data-growth.
- **"Cheap-now" bias honored** (per the mobile-web memory): radius fog over true LoS, AI-cheats
  over symmetric, fixed loot over RNG tables, display-name-only rename, global penalty consts.
- **No-AI-v1** on doors/chests/siege; each notes an enemy-behavior follow-up that overlaps the
  AI-profiles item.

## Audit pass (same session — duplication + base-assumption check)
Re-read the code behind every asserted claim. Verified-correct: `Unit` uses `Sprite2D`
(IMP-2), `GameMap._spawn_unit` (IMP), `ItemHandler.apply_item` `match` + `IMPLEMENTED_EFFECT_IDS`
(BWN/DCH), `_VALID_AI_PROFILES`/`is_boss` (AIP), `ItemData "key"` + `TileActions`
placeholders (DCH/STW), `WeaponData` formula range (STW), `InventoryEntry.uses_remaining`
(BWN), `UnitData.line_of_sight=4` unread (FOW), `SettingsManager` owns the settings (MRD-5),
no path-arrow renderer + `cursor_moved` is the hover/arrow hook (MRD).

**One real error found + fixed — MRD (duplicated/contradicted existing work):** the
aggregate "all-enemies danger zone" is **already built** (`GridManager.get_enemy_danger_tiles`
/ `show_enemy_danger_zone`, MMB, `OVERLAY_DARK_RED` source 3) AND already integrated by the
threat-range design (`_danger_mode = full|selected|combined|none`; watch layer = NEW source
4). My `[MRD-3]` re-litigated that and its draft rec ("add all hostiles to `_watch_set`")
**contradicted** the design (which keeps faction src 3 and watch src 4 on separate layers).
Fixed: §1 relabeled (src 3 = existing faction danger, not "threat-added"); `[MRD-3]` marked
**RESOLVED-BY-EXISTING-DESIGN / withdrawn** (ID kept so MRD-4..6 don't renumber); `[MRD-1]`
+ `[MRD-4]` sharpened with the existing paint-order + `cursor_moved` hook.

**Minor refinements:** PKGA-1 made precise (Package A IS in GDD_10 as a *deferred dependency*
— no near-term execution milestone, not literally "no roadmap home"); AIP gained a
cross-register convergence note so fog-scout ([FOW-3]) / chest-looter ([DCH-4]) / siege-
operator ([STW-6]) land as profiles HERE, not as parallel mini-AI plans.

**No other duplication:** doors/chests + siege already share one `map_objects` model (by
design); the §2-schema reservations are field-reservations (data-growth), not a re-plan of
§2's serializer.

## Resume-from-roadmap wiring (GDD_10 Open Items Register)
So work picks up from the roadmap, not just the backlog:
- **§A:** added a **Package A — `RngService` (build FIRST)** row + a **map-readability
  cluster** row; updated the §2 row (technical plan + [CST] register drafted) and the
  threat-range row (cross-ref the cluster register).
- **§F (release gates):** D-A + DOC-012 rows now point at `[REN]`/`[LEG]`; **added an
  OPEN-5 broken-weapon `[BWN]` row**.
- **§H:** added the full register index (all 11, tiered) + the forward §2-schema reservations
  + the GATED list, pointing at session note 2026-06-21d as the resume entry point.
- **Backlog §2:** suspend-serializer forward-dep line now lists the 4 reserved fields.

## Verification
check_docs.py **17/17 PASS** (re-run after the audit edits + roadmap wiring); docs-only;
no `__pycache__`.

## Plans for next session
These are DRAFTS with OPEN registers — **none are resolved yet.** Next planning step = **walk a
register one-by-one with the user** (the §1/§2 method) to resolve it, starting with the
recommended **Package A (`RngService`)** (smallest register — gaps only — and the agreed-first
build step). Two registers have a user-owned blocking input: **[REN-1]** (the actual owned
names) and **[LEG-1]** (naming the source corpus + license). Execution alternatives unchanged:
Package A build, §1 input/controls, or the v0.2.3 verify/Web track.

## Session status: CLOSED.
