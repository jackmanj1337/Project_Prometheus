# Session Note - 2026-07-04h

## Topic
The v0.2.6 playtest RETURNED — triaged all results into the tracking system, diagnosed
every reported defect against source, implemented the mechanical fixes with regression
tests, and set up the owner-review walkthrough for the judgment calls.

## What was done
- **Filed the return:** checklist →
  `playtest_checklist_v0.2.6_returned_2026-07-04.md`; 8 screenshots + the returned
  `godot.log` → `AGENT/Docs/archive/evidence/` (descriptive names). The log came back
  for the **first time in four builds** — via the `%APPDATA%` fallback, because the
  `._sc_` self-contained marker does nothing in exported builds (editor-only feature;
  the returned build stamp proves `user_data_dir` stayed `%APPDATA%`).
- **Triage doc:** `playtest_v0.2.6_results_triage_plan_2026-07-04.md` (`V026-01..08`).
  Headline results: §1.2/§1.5/§1.6/§1.7 + Part II + regression pass now PASS live;
  §1.1/§1.3/§1.4 PARTIAL. The display gate shrinks to three surfaces.
- **Root causes found & fixed (all with tests that fail pre-fix where feasible):**
  - `V026-03`/`V026-04a` (menu + forecast wrong at high zoom, "normalizes on a no-op
    zoom step") = ONE bug: Camera2D defers its viewport scroll update to end-of-frame,
    so the synchronous re-anchor read a stale canvas transform. Fix:
    `CameraController._flush_scroll()` (`force_update_scroll()`) after camera writes.
  - `V026-01a` (Settings off-center on FIRST apply at 2x, settles when wiggled):
    `MenuScale._recenter()` centered scroll-frame panels at their stale size, then the
    deferred layout grew them rightward (h-scroll now disabled → content min width
    propagates). Fix: size from authored base + content min (viewport-capped) before
    centering.
  - `V026-05c` ("only the top class option reachable via keys"): focus DID move —
    `OptionsScroll` lacked `follow_focus`, so the focused button scrolled out of view.
    Fixed in PromotionScreen + ReclassScreen; test injects a real `KEY_DOWN` via
    `push_input`.
  - `V026-01b` scrollbar padding (new `Margin` container, 14px right),
    `V026-01c` missing hotseat debug keybind row, `V026-04b` no-counter Hit/Crit
    render as plain dash rows so advantage icons align.
- **Review doc:** `AGENT/Code Reviews/playtest_v0.2.6_triage_review_2026-07-04.md`,
  Q1-Q7 OPEN: flicker repro + truncated §1.1 comment (Q1), character-sheet Back via
  directional keys — `_input` consumes all four directions (Q2), portable-log strategy
  after the `._sc_` failure (Q3, recommend: drop marker, document `%APPDATA%` as
  primary), promotion-picker sizing (Q4), non-turn-ending re-trigger "Promote" action
  (Q5 — route to the action registry, not a hardcoded ActionMenu row), victory screen
  must wait for the progression-modal queue (Q6 — sequencing, own slice, not a
  display-gate item), handbook display explainer digest (Q7).
- **Control plane / GDD_10:** `VAL-V023-DISPLAY` row rewritten to the v0.2.6 state;
  next action = owner walkthrough → cut **v0.2.7** rerun of §1.1/§1.3/§1.4 only.

## Commits (this session)
- `508dc95` — file the return (checklist + evidence)
- `f93b5f8` — Fix [V026-01]: settings first-apply centering, scrollbar margin, hotseat row
- `6a512d9` — Fix [V026-03/04a]: camera scroll flush for zoom re-anchoring
- `17e7f38` — Fix [V026-04b]: no-counter Hit/Crit dash rows
- `76e7e18` — Fix [V026-05c]: promotion/reclass follow_focus
- `9ba6733` — triage plan + review Q1-Q7 + control-plane/GDD_10 update
- (pending) — this note + Session Notes INDEX row

## Validation
- Full test suite green (`run_tests.sh`, all suites) after each fix commit;
  `check_docs.py` 21/21 green. The camera-flush and settings-centering tests were
  verified to FAIL with the fix reverted (input-routing/test-fidelity rule honored:
  `push_input` event injection where routing is under test).

## Next Session
- **Owner walkthrough of Q1-Q7** in `playtest_v0.2.6_triage_review_2026-07-04.md`,
  then route decisions (Q6 likely becomes its own control-plane slice).
- **Cut v0.2.7** rerun build afterwards: Part I = §1.1 first-apply centering +
  padding + hotseat row, §1.3/§1.4 at high zoom; §3.2 documents `%APPDATA%` as the
  primary log path (per Q3 decision) and fixes `prepare_build.sh`'s `._sc_` claim.
- `VAL-V023-DISPLAY` flips only when that rerun's Part I passes live.
