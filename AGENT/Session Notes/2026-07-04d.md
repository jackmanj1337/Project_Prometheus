# Session Note - 2026-07-04d

## Topic
Cut the v0.2.6 playtester build and closed out the session.

## What was done
- **Version bump 0.2.5 → 0.2.6** across the release-metadata-coupled files:
  `export_presets.cfg` (name/export_path/product_version), `MainMenu.tscn`
  `VersionLabel`, and `environment_setup.md` references. `test_release_metadata.gd`
  green at v0.2.6.
- **Wrote the tester handbook** `playtest_checklist_v0.2.6.md` — Part I retests the
  V025 fixes (Menu Scale apply-on-release + no h-scroll §1.1, sheet wrap/Back/More-Info
  §1.2, menu hug-at-zoom §1.3, forecast re-anchor §1.4, level-up narrow/click +
  promotion 2.0× §1.5, applied-size readout §1.6, terrain click paging §1.7); Part II
  Map 950 content (skill cap + weapon selection + grind/stat caps); Part III regression
  + a prominent **§3.2 log request** pointing at the new next-to-the-exe `logs/godot.log`
  and the BUILD STAMP block.
- **Exported the build:** ran `scripts/tools/prepare_build.sh` (baked `build_info.json`
  = version 0.2.6 / commit `75b3379` / built_at, + dropped `builds/._sc_`) then
  `godot --headless --export-debug`. Confirmed `res://build_info.json` is packed into the
  pck (so the exported stamp shows the real commit). Artifact:
  `builds/Project_Prometheus_v0.2.6_debug.exe`, 101,295,520 bytes,
  SHA-256 `90a673e1e08b5bf8b5793d61fd45e2c40ff5db4d77479550015ec85a4fb4e401`.
- **Wrote the build manifest** `playtest_build_v0.2.6.md` (artifact facts, what changed,
  deferred Q5/Q6 + UI-INSPECTION items, known limitations incl. the two input-routing +
  self-contained-log live confirmations, verification).
- Tracked the Godot sidecars the export's import scan generated: `.uid` for
  `BuildInfo.gd` + `test_build_info.gd` (required by check_docs) and `.import` sidecars
  for the archive evidence PNGs that had been committed without them.

## Commits (this session-day, build-cut portion)
- `75b3379` — bump version + v0.2.6 tester checklist.
- `6e6e388` — build manifest + Godot sidecars.
(Earlier today: `d42b10c` decisions, `45e923e`/`e975f16`/`09415bc`/`c049c99`/`38f5639`/
`8fa3109` v0.2.6 fixes, `facc3e5` godot.log path fix, `9813dd8` self-contained log +
build stamp, `81a5721` triage-plan + note.)

## Validation
- Full `run_tests.sh` green in every pre-commit; `check_docs.py` 21/21;
  `test_release_metadata.gd` green at v0.2.6.
- The exe/`._sc_`/`build_info.json` are gitignored per-build artifacts (not committed).

## Ship list (three files)
`builds/Project_Prometheus_v0.2.6_debug.exe` + `builds/._sc_` (MUST stay beside the exe)
+ `AGENT/Docs/playtests/playtest_checklist_v0.2.6.md`.

## Next Session
- **Live Windows display rerun** of `playtest_checklist_v0.2.6.md`; two things need a real
  desktop pass that headless can't prove: the input-routing fixes (§1.5 level-up
  left-click, §1.7 terrain click paging) and the self-contained log landing next to the
  exe (§3.2, BUILD STAMP at the top). `VAL-V023-DISPLAY` flips only when Part I passes.
- **Resume `[CNC-3]`** node-type vocabulary (register
  `campaign_node_composition_open_questions_2026-07-03.md`) — reopened at session end;
  `[CNC-1/2]` resolved, `[CNC-3]` rec = keep `battle|hub` for v1, reserve an open node-type
  registry for later. Then `[CNC-4..10]`, then back to `[PUG-4..7]`.
