# Session Note — 2026-06-24a  (Firm F10 — Canto as a parameterized skill)

**Topic:** Continued the player-facing target-firming roadmap (atlas §3b). Firmed the **last open
foundation, F10 — Canto** (move-after-acting), and resolved a repo-wide **naming collision** it
exposed. Docs-only; check_docs **19/19**, working tree clean. Branch `docs-reorg-2026-06-23`.

---

## What we firmed — `[CAN-1..11]` (= foundation F10)
Owner keystone call: **canto is not a bespoke engine — it is a parameterized SKILL** granted through
the **existing** skill-grant mechanisms. Resolved:
- **CAN-1** canto = a `SkillData`, `effect_id="canto"`, **passive** (engine queries the holder's
  active skills at action-resolution). No new resource, no new trigger string.
- **CAN-2** movement model = a **skill parameter**: `effect_params.movement_mode ∈ {remaining, flat}`
  (+ `flat_amount`). One skill kind, two behaviors.
- **CAN-3** trigger set = **author-configurable** `effect_params.canto_actions` (default = all
  turn-ending actions; **Wait never** grants canto).
- **CAN-4** conferral = **existing mechanisms** — `ClassData.skill_unlocks` (mounted carry it by
  default), `[SKL-4]` grant/revoke, `[IEQ]` accessory effect_ids (**Knight Ring**), F6 story,
  `[PXP-4]` on-crossing. **Supersedes** GDD_10 M10's "automatic for all mounted."
- **CAN-5** the one new engine piece = a `UNIT_CANTO` MapCursor action-flow window (reuses the
  movement-range computation + `grant_extra_turn`), ending in **Wait only** (CAN-6).
- **CAN-7** remaining = `move − tiles spent reaching the action tile`. **CAN-8** rescue/carry
  interaction **deferred to the Rescue (#6) firming**. **CAN-9** no undo once acted; window optional.
  **CAN-10** AI uses the same capability. **CAN-11** no new save field (the skill grant persists).

## Naming collision (the error caught + fixed)
GDD_10 M10 used **"Canto" two ways**: an **ally-refresh** skill (Bard/Heron grant-an-ally-a-turn)
*and* — under "Mounted-unit movement remainder" — the move-after-acting mechanic. Everywhere else
(scope-map #9, atlas F10, GDD_02/03) "Canto" = move-after-acting (FE-canonical). Owner's call:
**"Canto" = move-after-acting; rename the ally-refresh.** Owner then chose the rename **Reinvigorate**
(drop the musical connotation from the code). Renamed across **GDD_10** (M10 section, code samples,
checklists), **GDD_05** (starting-skill example), **GDD_02/03**. Promos (Resonance/Battle Cry/
Reverberate) unchanged. **Deferred:** the parked `GDD/Content Expansion/Old_Deferred/` corpus still
uses "Canto" for the ally-refresh — sweep to Reinvigorate when that content is un-deferred (noted in
the register §5).

## Reconciled docs
Atlas F10 row → decided; critical-path + §3b Wave-0 + §4 updated → **all F1–F12 end-shapes now
decided** (only F1's schema-lock pass remains before builds). Foundations end-shapes doc: F10 moved
from "still open" to decided. Scope-map #9 firmed.

## Commits
1. `f7ecdb0` — Firm canto `[CAN-1..11]` = F10 + Canto/Reinvigorate name-clash resolution
   (+ the session-note commit for this file).

## Verification
`check_docs.py` **19/19 PASS**; RNG guard PASS; manifests regenerated; no new `__pycache__`;
working tree clean.

## Next session — continue the roadmap (atlas §3b)
**Wave 0 remaining:** **F1 save-schema lock** — consolidate + reserve every field the recent
firmings generated (`proficiency_xp`, equipped-slot pointers, accessory slot-capacity map, pool
state, `known_spells`, condition state, campaign flags (F6), `personal_skills`, `granted_skills`;
canto adds **none**). Then **Wave 1** ⚠ items (flexible weapon triangle `[CEX-9..12]`, per-map-use
items `[CEX-13]`, learned spells `[CEX-5..8]`, story items `[CEX-14..16]`, per-skill UX M9b), then
**Wave 2** ✗ v1 worklist. Session task list tracks the queue.
