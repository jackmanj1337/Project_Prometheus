# Session Note - 2026-07-01b

## Topic
Opened the `FORGE` design pass (Q15 next-session target): researched forging in
Fire Emblem + similar SRPGs, mapped it onto the existing substrate, verified
`B4-IEQ` leaves per-instance room, and wrote a first-round open-questions register.

## What was done
- **Re-read** the prior note (`2026-07-01.md`) and the referenced substrate:
  the Band 5-8 review "Walkthrough Decisions" Q15 (forging elevated), the Band 4
  `IEQ` / `SHOP-ECONOMY` / `CONVOY` plans, `items_equipment_unified_model_2026-06-23`,
  `resource_ledger_cost_resolver_contract_2026-06-28`, and the `forged_mods` /
  M10 references. Inspected code: `WeaponData.gd` (stats `mt/hit/crit/wt/uses` read
  directly), `InventoryEntry.gd` (`forged_mods: Dictionary` exists, no reader).
- **Researched** forging systems (web + code): FE9 PoR (free-point allocation,
  gold, 1/chapter), FE10 RD (allocation + ores), FE14 Fates (+1..+7, 2 copies +
  minerals), FE16 3H (stats + repair + craft, gold + smithing stones, rank-gated),
  FE17 Engage (refine +1..+5 exponential + Transform; separate Engrave stat-package,
  exclusive, Bond Fragments). Similar titles: Triangle Strategy (material+coin rank
  nodes granting stats AND abilities), Disgaea Item World (play-driven, out of
  genre), FFT (loot-chain, no forge).
- **Wrote the register**
  `AGENT/Docs/registers/forging_open_questions_2026-07-01.md` (`[FRG-1..18]`,
  Status OPEN): research tables, code-grounded substrate, open-registry
  architecture stance, and 18 grouped open questions (A upgrade model, B cost,
  C per-instance `forged_mods`, D service/UI, E v1 demo scope, F engrave stretch),
  each with a recommendation. Regenerated the docs index (INDEX + REGISTERS).

## Key findings / recommendations seeded
- **Per-instance room CONFIRMED** (Q15 ask): `InventoryEntry.forged_mods` is
  reserved + unread; B4-IEQ Non-Goals explicitly keep it as an instance overlay.
  No B4-IEQ change needed. The one new engine seam forging adds is an
  **effective-weapon-stat resolver** (`base + forged_mods`) so combat/range/UI stop
  reading raw `weapon.mt` — `[FRG-10]`, the load-bearing change.
- Forge = **open registry of upgrade ops + recipes read by a service** (per
  AGENTS.md), NOT a hardcoded forge table. Costs via `ResourceLedger`; service +
  PHB/on-map/dialogue panel mirrors `ShopService`/`ShopPanel`.
- Leaned recommendations: v1 = fixed `+N` stat-bump upgrade path, gold-only
  (schema multi-resource), permanent, prep panel, `forged_mods` stores the applied
  upgrade **ops** (not raw deltas) so caps/reset/preview are derivable. Materials,
  allocation UI, repair, transform, and engrave-effect-packages deferred as later
  data/consumers.

## Validation
- `python3 AGENT/Docs/gen_docs_index.py` — wrote INDEX.md / REGISTERS.md.
- `python3 AGENT/Docs/check_docs.py` — all 21 checks green.
- Docs-only change; no Godot suite run.

## Current State
- `[FRG-1..18]` register is OPEN and ready for an owner walkthrough (same
  one-question-at-a-time pattern as the Band 5-8 review). No implementation plan
  written; forging still builds after Band 1-4 gates.

## Next Session Target
Owner walkthrough of `[FRG-1..18]` to settle the forging model, then convert the
settled register into a `B7-FORGING` implementation plan and the control-plane
status bump (near-term). Confirm F1 reservations for the richer `forged_mods`
schema, smith-rank var, and non-mergeable-instance identity when `[FRG-9]/[FRG-11]`
settle.

## Read First Next Session
1. This note.
2. `AGENT/Docs/registers/forging_open_questions_2026-07-01.md` (`[FRG-1..18]`).
3. `AGENT/Docs/plans/band4_items_equipment_implementation_plan_2026-06-30.md`
   (the substrate forging builds on).

## Watchouts
- Do not build the effective-weapon-stat resolver or `forged_mods` reader as
  scattered call-site edits — it must be one seam (`[FRG-10]`).
- Keep every forge vocabulary (upgrade ops, cost resources, forgeable attributes,
  gates) registry-backed — no `enum`+`match` forge table.
- Engrave effect-packages (`[FRG-18]`) depend on the Band 5 Q5 Source/Style effect
  registry; don't pull them into the v1 forging slice.
