# Session Notes — 2026-06-20 (v0.2.1 triage: forward-compat review + handoff, approved)

## What was done

Continued the v0.2.1 playtest triage (started 2026-06-19e). Ran a **forward-compatibility
review** of the plan, the user made the remaining decisions, then I fleshed out the plan,
wrote the implementation **handoff**, and pushed. Still **plan/docs only — no code**.

### Forward-compatibility review (6 findings, all folded into the plan)

- **F1 — V021-01 is M15 architecture.** Second fix at the hotseat seam (after V020-04). The
  activation-boundary commit it sets up is inherited by M15B (online serialize-on-handoff),
  M10 (extra activation), and M8 (sleep/stun set DONE at start of activation). Reuse the
  existing snapshot/serialization seam; don't build F9-only logic.
- **F2 — V021-09 can't overload `"combat"`.** M8 Hex / M9 procs use that duration_type;
  led to the duration taxonomy below.
- **F3 — V021-11 corrections.** `great_knight` is `["armoured","mounted"]` **today** → the
  precedence hierarchy is load-bearing now, not forward-looking; DoD check is "**≥1**
  movement type" not "exactly one"; `special_qualities` also holds `dragon/beast/laguz`
  (M12/13 type tags the resolver must ignore); GDD_03's valid list is already missing
  `light_footed`; `GridManager` must keep **skill-override-first** (Acrobat/Pass/Nimble/dash).
- **F4 — V021-18+19 re-open just-shipped Menu Scale.** Split them into their own build.
- **F5 — input-layer items feed the rebind milestone.** Build V021-15 as one **shared
  selector**; V021-17's `mouse_cursor` tri-state seeds an input-mode concept + mobile.
- **F6 — V021-14/10 grow the D-A rename surface** (awareness only).

### Decisions (user, this session)

- **Split the builds:** **v0.2.2** = fixes + UI + designed systems; **v0.2.3 "Display
  Scaling & Resolution"** = V021-18 + V021-19 only.
- **Duration-tag taxonomy:** `this combat` (one engagement incl. follow-ups/counters),
  `until separated`, `until unequipped`, `until end of map`, `x turns`. Drives V021-09;
  inherited by M8/M9.
- **V021-01 depth:** minimal, principled activation-boundary fix (commit-then-yield); full
  rollback deferred to M15B (my recommendation, unvetoed).

### Docs updated / written

- Fleshed out `playtest_v0.2.1_triage_plan_2026-06-19.md`: status → approved; build split;
  duration taxonomy; Forward-Compat F1–F6 section; rewrote V021-01 / V021-09 / V021-11 /
  V021-15 workstreams; updated order + verification.
- `GDD_10_Roadmap.md`: bumped Last verified → 2026-06-20; approval + build-split note;
  rewrote V021-09 (taxonomy) and V021-18/19 (v0.2.3 tags).
- **New `AGENT/Docs/handoff_2026-06-20.md`** — ordered v0.2.2 implementation playbook
  (per-item files/accept/tests/docs), shared primitives, two pre-coding questions
  (EnemyAI move application; flying cost rule), v0.2.3 deferral, build-cut closeout.

## Verification

- `check_docs.py` green. No code, no Godot suite run.

## State

- Branch `code-review-followups-2026-06-17`. This session's docs committed **and pushed**.

## Next

- Implement **v0.2.2** per `handoff_2026-06-20.md` order (V021-01 → 02/03/04 → 06/09 → 07 →
  08 → 10/11 → 05/15 → 13/14/16 → 17/12). Resolve the two pre-coding questions first.
- Then cut the v0.2.2 build + tester handbook (force the Error-log check). v0.2.3 after.
- Open: `main` merge decision (~289 ahead).
