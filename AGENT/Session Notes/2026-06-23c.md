# Session Note — 2026-06-23c

**Topic:** Started the campaign/save **design-side** push (the next major task named in 2026-06-23b).
Produced a **framing / foundations doc** mapping what the save + campaign features build on and how
players vs designers interact with them — firmed-vs-open. Docs-only; check_docs **17/17**; no `__pycache__`.

(Continues 2026-06-23b, which flagged the design side of the campaign + campaign-builder as the next
major task.)

---

## 1. Method
Read the three firmed §2 docs end-to-end first (no re-deriving): player-facing firming (A–J),
technical plan, decisions register (`[CST-1..12]` RESOLVED, `[CST-13]` deferred). Confirmed the
**player** interaction surface is firmed; the **designer/authoring** surface is the open frontier.
User chose **"one framing doc first"** before walking any register.

## 2. Deliverable
`AGENT/Docs/campaign_save_expectations_and_foundations_2026-06-23.md` — navigation/seed doc (decides
nothing). Contents:
- **Core insight:** save = state-by-id + binds to a campaign id; everything resolves by id against
  `defaults ∪ campaign overlay`. Player = progress/persist that state (firmed); designer = author the
  defaults/overlay/graph/rules (open). Seam = `DataManager` base+overlay (`[DMR-1..3]`, doesn't exist yet).
- **Substrate stack L0–L4:** L0 `RngService`/Package A (gates §2 exec) · L1 `SaveManager`/`SaveData`/
  `SaveCodec` (firmed) · **L2 `DataManager` base+overlay — OPEN keystone** · L3 `CampaignData` graph
  (firmed) + content library (**overlay/I3 OPEN**) · L4 `CampaignRules` (firmed). Three data tiers:
  `res://` defaults → campaign package (overlay, art lives here) → save (state by id).
- **Player surface — FIRMED** (pointers only): campaign-select→rules→prep/deploy→battle→results/advance;
  slots + export/import; Continue/Load; suspend; defeat menu; read-only rules + flip notice.
- **Designer surface — OPEN frontier (4a–4e):** progression authoring · rules mandates/defaults +
  protected_fields + story-flip · **content overlay (I3, sub-decisions a–e unratified)** + net-new item
  `icon` field · packaging/distribution (pack format) · the GUI editor (vs hand JSON).
- **Firmed-vs-open index** + **recommended walk order**: `[DMR-1..3]` → I3 content-overlay register →
  designer authoring contract; player side stays firmed.

## 3. Edits
- New framing doc (above).
- `GDD_10_Roadmap.md` §H §2 bullet — refreshed (technical plan is written, not "next step") + added the
  DESIGN-side frontier pointer (framing doc + the OPEN designer authoring contract 4a–4e, no register
  yet) + recommended walk; bumped `Last verified` → 2026-06-23 (check 6).
- This note + INDEX row.

## 4. DoD
- **DoD#1:** planning/navigation only — no behavior change, no GDD_01–08 contract touched. The roadmap
  edit is the Open-Items-Register sync for a new open thread (designer authoring contract), per the
  "every open thread has a row here" rule.
- **DoD#2:** no checkable rule ratified (the framing doc decides nothing). Item-`icon` value/field guard
  + content-overlay version/namespace guards land WITH the I3 build, as already tracked.

## 5. NOT done / still open
- No register walked yet — framing only. `[DMR-1..3]` OPEN; I3 content-overlay register doesn't exist;
  designer authoring contract (4a–4e) has no register; item `icon` field not added.
- v0.2.3→`main` merge still open (owner). IMP/LEG OPEN from prior sessions.

## 6. Verification
- `check_docs.py` → **17/17 PASS**, docs-only, no `__pycache__`. DMR doc reference name verified to exist.

## 7. Next session
- Begin the walk at **`[DMR-1..3]`** (L2 base+overlay load path) — the keystone the designer side
  resolves through — then open the **I3 content-overlay register** (ratify a–e, add the item `icon`
  field), then draft the **designer authoring contract (4a–4e)**.
- Build track unchanged: **Package A Step 1** is still the next *execution* step (design above is not
  gated by it).
