# Session Note - 2026-08-18-04-35-07Z — `R1` walk closeout

## Branch context

- Branch: `agent/integration` (documentation-only; the docs-guard refuses `AGENT/Docs/plans/**`
  on a feature branch)
- Base branch: `agent/integration`
- Base SHA: `9ed6d379`
- Coordination Work ID: `R1-PLAN-CORPUS-COHESION-REVIEW-2026-08-16`

## What was done

Finished `R1`'s §8 walk — items 4 and 5 of five — and closed the row. The closeout is **§9 of the
precedence diff**, written into the diff itself so the next sweep reads one document rather than
three session notes.

### Instance (c) — the unified UI programme, re-derived

`unified_ui_programme_2026-08-12.md` cited `UUI` and `UITH` and nothing else. Five registers
governing screens it sequences have been ruled since. **Sixteen corrections**, of which three
change the order and two retire gates the plan was still waiting on:

1. **`ResponsiveLayout` context-scoping enters Phase 0 as item 0**, ahead of the `dense` token
   column — `[CEUI-S3]` requires it, and you do not add a column to a file about to be
   restructured. It is dated work: one production consumer today, a second on the held Main Menu
   branch, and more with every conversion.
2. **New Phase 2b, the localization seam, before the conversions.** `[L10N-7]`'s 1.4× extent and
   `[L10N-11]`/`[L10N-12]`'s declared direction harden into each scene as it is written.
3. **`UUI-15`'s album hold is discharged** — all three walks closed, all three albums approved,
   and `[DSX-S29]` makes album approval the gate rather than walk closure.
4. **Both "things everything else is queued behind" are `completed`** in the tracker
   (`V07X-ACCEPTANCE-GATE`, `IMPL-FILEDIALOG-ESCAPE-TEXTINPUT`). The paragraph now names what is
   actually queued behind a Windows session, read from `tasks.json` rather than from prose.

Also: the token file ends at **four** columns, not three (`[CEUI-S1]`'s editor column at
`min_target = 24`); the row token is a **floor, not a height** (`[DSX-S22]`); Settings carries
**seven** things, not five; **prep is a rebuild, not a conversion**; the eight workstreams were
missing the distribution shell and the dependent-choice layer entirely; and the **editor sits
outside this programme's size-class system**.

### Instance (d) — verified, not edited

The owed `~1.3×` → **1.4×** correction *has* landed in `responsive_ui_redesign_2026-08-06.md`
(lines 212–219) with the declared-direction clause beside it. Its **other** owed edit — the Compact
row budget, 4.3 vs 3.9 — has **not**, and stays with `SMALL-SCREEN-UI-REDESIGN-2026-08-05`.

One trap recorded in the plan: `[L10N-7]` and `[DSX-S11]` both cite **4.3 rows at the floor**, but
that is the shop album measuring its own chrome, not corroboration of the redesign's arithmetic.
Two documents reaching the same number from different inputs is a coincidence, and reading it as a
second source would close a discrepancy that is still open.

### §2.1's nine documents, dispositioned individually

Two corrected (`doc_role_manifest` — `UUI-1..17` → `1..19`; `open_questions_inventory` dated as a
snapshot rather than rewritten, because re-deriving a snapshot rewrites history). **Two stale
headers reported to their holders rather than edited** — the `UBS` agenda to `RESEARCH-SEQUENCING`
and `campaign_library_ux_decisions` to `DISCUSS-CAMPAIGN-LIBRARY-UX`. In both the *body* is current
and only the front-matter `Status` misleads, which is the more dangerous shape: a reader who
believes the header never reaches the amendment. Four confirmed unaffected. One escalated.

### The escalation — `BAND4-PREP-V1-BOUNDARY-2026-08-18`

**`B4-SHOP-ECONOMY-2026-07-23` and `PREP-V1-S05` both build the shop, from plans seven weeks apart,
with no dependency edge in either direction.** This is instance (e)'s defect a second time, in a
place §4.5 could not see: that pass checked the four shared `EPUX` primitives *inside* the epic
graph, and this pair is not connected to it.

`band4_shop_economy_implementation_plan_2026-06-30.md` predates `EPUX`, `TSV`, `CUR`, `SHC` and
`DSX`. Its non-goal "**stock is author-defined and infinite**" is contradicted outright by
`PREP-V1-S05`'s cadence-driven restock; its `ResourceLedger` quote/commit rests on a method the
prep/economy plan **deletes**; its "gold only" predates `CUR-1..7`; its rough `B3-PHB` panel is now
an adapter on the distribution shell. Two items survive, and one of them — the on-map trigger — is
the part `PREP-V1-S05` does *not* build. `B4-CONVOY` is the same shape with an edge already in
place, so its boundary is undrawn rather than absent.

Recommendation recorded, **not applied**: it changes what two `planned` rows build.

### The escalation, ruled the same session

**Owner approved it, and added: the restocking store is the eventual goal.** Checking that clause
changed the disposition rather than confirming it — `[EPUX-16]` had **already** ruled restocking
*pulled forward and generalized* on 2026-07-25, because `[EPUX-01]`'s revisitable nodes require a
defined second-visit behaviour. So infinite is the **default value** of an author-set quantity,
not a stage. The Band-4 plan said the opposite twice and its `ShopStockEntry` has **no quantity
field at all** — which is the concrete way to fail the clause: not by deciding against restocking,
but by shipping a schema with nowhere to put a count. The field is now schema (default
`unlimited`, plus a restock cadence reference), both non-goals are retired, and the two
`F1 obligations: no saved stock state` lines are corrected.

**The recommendation was wrong on one point, caught while wiring the edges.** It had Band 4
keeping "the on-map trigger" — but the on-map *presentation* is a distribution-surface adapter, so
it needs the shell `PREP-V1-S05` builds, while `PREP-V1-S05` needs Band 4's price and stock data.
That is a **cycle**. Split: the tile-action *declaration* stays in Band 4, the presentation goes to
`PREP-V1-S06`, which already builds map placement as a property of *any* Explore activity. The
clean seam underneath is that cadence is **one trigger engine at `PREP-V1-S01`** with stock as one
subscriber of four — schema in Band 4, entity in `S05`, engine below both.

Verified over the whole 435-row graph after the edit: **acyclic, every producer-before-consumer
edge holds**, `B4-SHOP-ECONOMY` twelve layers ahead of `PREP-V1-S05`.

### The generator defect, three live instances in one session

§6.4 recorded `_dominant_register`'s fabricated ranges and left the fix to `R3`. This session
tripped it twice more, and both were self-inflicted by *citing registers correctly*:

- The re-derivation's new `[RPD-n]` citations crossed `_heuristic_type`'s three-mention threshold,
  so **the plan was typed as a register** and `REGISTERS.md` gained a fabricated `RPD-1..8`.
- Dating `open_questions_inventory` added `[UUI-n]` citations and produced `UUI-1..15` — for a
  family already catalogued at `UUI-1..19`. That document was **already** wrong before this walk,
  because the heuristic matches the substring `open_questions` **in the filename**; an inventory
  *of* open questions is not a register of decisions.

Both fixed by declaring `Type: plan`. The common cause is that type and register are inferred from
a document's surface **when the document does not declare them**, which sharpens `R3`'s task from
"stop inferring ranges" to "make the declaration mandatory under `plans/`".

## Commits

Ownership is in `CLAIMS.tsv`. One documentation commit: the instance (c) re-derivation, the
instance (d) verification recorded in §9.1, the §9.2 dispositions with their two corrections, the
§9.3 escalation, the §9.4 generator findings, and the regenerated `INDEX.md`/`REGISTERS.md`. They
are one logical step — the walk closeout — and the index regeneration is the *evidence* for §9.4
rather than a separate chore.

## Gates

- `python3 AGENT/Docs/check_docs.py` — **PASS**, 46 checks.
- `python3 AGENT/Docs/gen_docs_index.py`, then the diff of `REGISTERS.md` was **read** rather than
  the PASS trusted. That is what surfaced §9.4: both fabricated rows passed every check.
- `python3 coordination/check_tasks.py` — **OK: 435 tasks valid, no conflicts.** One pre-existing
  stale-row warning (`DESIGN-IMPLEMENT-PACKAGE-REGISTRY-COMPOSITION-2026-08-02`), untouched here.
- `python3 coordination/gen_active_work.py` — regenerated, no diff (the tracker scripts had already
  written it).
- Godot suite skipped — documentation-only change.

## Next

`R1` is **closed**. Its exit condition is met and recorded in §9.5.

1. **`R2`** — UI corpus and album release review. Unaffected by anything here, and its gate
   (all three albums approved) closed on 2026-08-16.
2. ~~The `BAND4-PREP-V1-BOUNDARY` owner call.~~ **Ruled and applied the same session** — see
   above. The row is closed.
3. **Phase 0**, whose `ResponsiveLayout` context-scoping deadline closes when the v0.8.0 release
   window opens — now item 0 of the re-derived programme rather than an undated note.
4. `S7`, and blocks C–F of the owner review.
