# Session Note 2026-07-07n - v0.3.0 release-delta review handoff

## Purpose

Next session should run a short **full release-delta code review** from the shipped
v0.2.8 build source to the planned v0.3.0 build snapshot. This is broader than the
July 7 Pillar 1 review, which only covered the unreviewed code since the previous
code-review checkpoint.

Keep this pass document-only. Any fixes found, especially the known RNG lifecycle
issue, should land afterward as separate focused commits.

## Read first

- `AGENT/Session Notes/2026-07-07m.md`
- `AGENT/Code Reviews/code_review_2026-07-07.md`
- `AGENT/Code Reviews/code_review_2026-07-05.md`
- `AGENT/Review Procedures/00_Master_Review_Procedure.md`
- `AGENT/Review Procedures/01_Code_Pillar.md`

## Known boundary facts

- v0.2.8 executable source commit: `ab81a21`
  (`AGENT/Docs/playtests/playtest_build_v0.2.8.md`)
- July 5 review snapshot: `914dd025`
- July 7 review snapshot: `7c6378e`
- Current handoff head after the review/session-note commits: `74fd837`
- Existing dirty file at handoff: `AGENT/Docs/playtests/playtest_checklist_v0.3.0.md`
  was already modified before the review work and should not be treated as part of
  the review unless explicitly included.

Important distinction: the July 7 review alone does **not** cover the entire
v0.2.8-to-v0.3.0 delta. The July 5 and July 7 reviews together cover most of the
production GDScript surface, but the next review should explicitly verify the full
release boundary.

## Next-session plan

1. **Set the exact review boundary.**
   - Base: `ab81a21`.
   - Head: the actual planned v0.3.0 build commit.
   - If no build commit exists yet, use current `HEAD` and label the artifact
     clearly as a pre-build snapshot review.
   - Record dirty files before starting.

2. **Build a release-delta coverage matrix.**
   - Run `git diff --name-status ab81a21..HEAD`.
   - Split changes into production GDScript, tests, scenes/resources/project settings,
     export/build metadata, docs/playtest materials, and generated/import sidecars.
   - Mark each bucket as covered by July 5 review, covered by July 7 review, or not
     yet reviewed.

3. **Re-check weak or uncovered areas.**
   - Earlier MRD/threat-overlay work outside the July 7 delta:
     `scripts/core/GridManager.gd`,
     `scripts/tools/generate_placeholder_assets.gd`,
     `scripts/tools/generate_tilesets.gd`.
   - Display changes after v0.2.8, especially the v0.2.9 section 1.6 fixes.
   - All July 7 findings against the then-current code:
     fresh-map `RngService.start_map()` lifecycle,
     Settings -> Input Mode immediate refresh,
     rebind UI coverage for `more_info` / `peek_range` / zoom actions,
     and carried-forward closed registry debt.

4. **Do the release-risk pass.**
   - Save/suspend/resume.
   - RNG determinism and raw-RNG guard.
   - Input/gamepad/rebind/prompt behavior.
   - MRD/threat overlays and display/window behavior.
   - Release metadata, export version, build stamp, and playtest checklist alignment.

5. **Run verification for the review snapshot.**
   - `python3 AGENT/Docs/check_docs.py`
   - RNG usage guard (`scripts/tools/check_rng_usage.sh` or the repo's hook path)
   - Full Godot test suite if time allows.
   - If the full suite is skipped, run focused suites for any touched/flagged areas
     and state the skipped coverage plainly.

6. **Write the review artifact.**
   - Suggested path:
     `AGENT/Code Reviews/code_review_v0.3.0_release_delta_2026-07-XX.md`
   - Include exact base/head commits, dirty-tree note, coverage matrix, findings
     ordered by severity, prior-review coverage, remaining build blockers, and a
     prioritized action plan.

7. **Commit in two logical steps.**
   - First commit: the release-delta review artifact only.
   - Second commit: session note plus `AGENT/Session Notes/INDEX.md` row.

## Expected starting finding

Unless fixed before the review begins, the known High item from July 7 should still
lead the action plan: production fresh-map startup does not call
`RngService.start_map()`, so RNG seed/history lifecycle can be wrong before the
v0.3.0 build cut.
