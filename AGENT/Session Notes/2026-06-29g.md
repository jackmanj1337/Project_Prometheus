# Session Note - 2026-06-29g

## Topic
Recorded the class EXP / PXP boundary decision after reviewing the downside of
merging class EXP into the proficiency system.

## What was done
- Added
  `AGENT/Docs/plans/class_exp_pxp_boundary_plan_2026-06-29.md`.
- Recorded the direction that class EXP remains separate from PXP storage and
  lifecycle, while award/profile authoring uses shared idioms.
- Updated `GDD_02_Core_Mechanics.md` so the EXP section points at the boundary
  plan and treats combat/staff EXP numbers as developer presets.
- Updated the `B4-PXP` Project Control Plane row to use the boundary plan as its
  build source.
- Updated `GDD_Feature_Index.md` and regenerated `AGENT/Docs/INDEX.md`.

## Validation
- `python3 AGENT/Docs/gen_docs_index.py`
- `python3 AGENT/Docs/check_docs.py`
- `git diff --check`
- Commit hook also ran `check_docs.py` and `check_rng_usage`; docs-only change
  skipped the Godot suite.

## Commits this thread
- `10410a0` - `Document class EXP and PXP boundary`

## Next session
- When `B4-PXP` starts, implement PXP tracks and data-driven class-EXP awards as
  sibling benefit paths.
- Keep `Unit.add_exp()` as the class-level progression entry point.
- Revisit a shared lower-level progression helper only after PXP migration and
  save schema are stable.
