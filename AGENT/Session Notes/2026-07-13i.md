# Session Note - 2026-07-13i

## What was done

Started the v0.4.0 review fix pass from
`AGENT/Docs/plans/v0.4.0_review_fix_handoff_2026-07-13.md` and closed all three
Medium RC-blocking findings.

- Removed `CombatResolver`'s circular `Unit.handle_death()` fallbacks. A missing
  `/root/DeathLifecycle` now reports an error and stops death processing.
- Replaced the weak mutual-kill assertion with resolver-level lifecycle evidence:
  shared simultaneous group, responsible actors, defender-first ordering, and
  both units' entry tile/inventory snapshots before disposition.
- Changed normal map-start placement to `nearest_free`, with a warning that names
  the unit, authored tile, and landing tile when fallback is used.
- Made individual placement failures skip the unit without making `_spawn_units()`
  fail; structural launch failures remain fatal.
- Added scene-level regressions for occupied-tile fallback and no-free-tile skip.
- Confirmed the handoff's requested enemy placement bounds check already exists
  in `DataManager` (since `60bac00d`) and is already asserted by
  `test_data_manager`; no duplicate validation was added.
- Updated the affected GDD architecture/mechanics contracts and roadmap routing.

## Commits

- `9fa2453` — Fix shared combat death funnel.
- `1c11906` — Keep map placement failures nonfatal.

## Validation

- `bash scripts/check_env.sh`: passed; expected local `.env` warning only.
- `python3 AGENT/Docs/check_docs.py`: passed all 29 checks.
- `bash scripts/ci/check_rng_usage.sh`: passed.
- `bash run_tests.sh`: all 75 suites passed after each code commit.
- Focused suites: `test_combat` 47/47; `test_game_map_scene` 49/49.

## Next

Continue with handoff item 3, the Low-finding cleanup batch: optional action
params, stale Attack Preview entries/comment, refund result reporting, delayed
occupancy reset, and deletion of the unused `DataManager.is_registered_id()`.
Then add projection no-mutation coverage and correct its contract wording.
