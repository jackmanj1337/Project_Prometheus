# Session Note - 2026-07-14f

## What was done

- Confirmed the v0.4.0 Windows playtest has **not** returned (no
  `playtest_checklist_v0.4.0_returned_*`), so save-spine work was not preempted
  and `B1-CST` proceeded per
  `AGENT/Docs/plans/b1_cst_save_spine_handoff_2026-07-14.md`.
- **`B1-CST` Slice 1 (CampaignData seeding) is Implemented.** The campaign layer
  above the existing save seam now exists:
  - `scripts/resources/CampaignData.gd` + `scripts/resources/CampaignNode.gd`
    carry the ordered progression graph (node ids, edges, per-node map binding).
    Authored as JSON, not `.tres`, per `[CST-3]`, so a campaign stays one
    portable document for `B6-CAMPAIGN-SHARING`.
  - `DataManager` loads `data/campaigns/` in the same catalogue pass as
    classes/weapons/items/skills, with two loud validation stages matching the
    map registry and registry catalogue: `CampaignData.parse` for structural
    defects and `collect_campaign_validation_errors` for a node bound to a
    `map_id` outside the map registry. An unrepresentable campaign parses to
    `null` rather than half-loading a run the player cannot finish.
  - `data/campaigns/proving_grounds.json` seeds a real five-node campaign over
    the shipped objective maps (rout, seize, boss, escape, defend), so the
    resource is exercised by production data, not only a fixture.
  - Node ordering is the authored array order — no dictionary iteration, no path
    sort.
- Slice 1 adds **no new persisted save fields**, so the F1 manifest is
  unchanged: the `campaign.*` rows already exist and authored campaign
  definitions fall under `no_save.authoring.content_definitions`. Slice 3
  registers the campaign envelope.
- Tracked a pre-existing hygiene gap surfaced by running `--import`: 23 `.uid`
  sidecars (actions/, death/, placement/, projection/, the Band 2 autoloads,
  `CostSpec`, `ResourceTransaction`, and their suites) and 11 archived-evidence
  `.import` files were never committed. `check_docs` check 9 flags these because
  a fresh clone or CI cannot resolve `uid://` without them. Landed as its own
  commit, separate from the slice.
- Gates green on both commits: `check_env.sh`, `check_docs.py`,
  `check_rng_usage.sh`, and `run_tests.sh` (all suites pass; `test_campaign_data`
  17/17, `test_data_manager` 25/25).

## Commits

- `74f9d9f` - Track missing Godot `.uid` and `.import` sidecars (no behavior change).
- `bad3317` - `B1-CST` Slice 1: CampaignData progression graph.

Both pushed to `agent/claude/2026-07-14/save-spine-handoff`. No PR opened; human
review and merge remain manual.

## Next session

1. If v0.4.0 playtest evidence has returned, triage it first — it preempts this
   work and belongs on the v0.4.0 build branch, not the save-spine branch.
2. Otherwise continue at **Slice 2 (prep/results flow)** in the same handoff:
   prep -> map -> victory/defeat -> results -> next node.
   - **Already done, do not rebuild:** victory gold *already* routes through
     `ResourceLedger` (`TurnManager._apply_victory_rewards`, which commits a
     negative `party_gold` `CostSpec`). Slice 2's ledger requirement is
     satisfied; only the node-advance/results flow is missing.
   - The seam to add is the campaign runtime layer that Slice 1 deliberately left
     out: active campaign id, current node, cleared nodes, and the transitions
     driven off `EventBus.map_victory` / `map_defeat` / `map_resolved`
     (`TurnManager` already emits all three, and `GameOverScreen` already
     consumes the standings). A `CampaignManager` autoload alongside
     `SaveManager`/`DataManager` is the natural home; it would resolve
     `CampaignNode.map_id` through `map_registry` and drive
     `GameState.configure_next_map`.
   - Keep roster selection, trade, and deployment UI out — `B4-PREP-DEPLOYMENT`
     owns those. This slice owns the flow.
   - Tests owed: node advance on victory, defeat handling, results state handoff.
3. Slice 3 (full campaign saves) follows; every new persisted field registers in
   the F1 manifest in the same commit.
