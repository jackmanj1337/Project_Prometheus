# Session Note - 2026-07-24

## What was done

- Ran an owner design discussion over the campaign-library owner-questions packet,
  reorganised as dependency-ordered branches A–K. Reached shared decisions on
  **Branches A–D** (object model & hierarchy; library role & shape; first-run &
  import architecture; install & pack lifecycle), including the fingerprint case of
  CL-MISSING-04 pulled forward.
- Key outcomes: Campaign is the launch object grouped under Pack; Campaign→Run→Save
  hierarchy (rule profile is a run property); Main Menu = Continue + Manage Library;
  fixed app-managed **inbox** exchange folder + native OS picker + drag-drop, with an
  in-engine list primary for Steam Deck (native portal dialog is not gamepad-navigable);
  explicit one-tap import → promote to verified store → content-hash fingerprint
  re-verified on scan/load, mismatch = **non-blocking** warning; manual version updates
  (no migration engine in v1); action set Import / Export clean / with runs / run(s) only
  / campaign status record(s) / Delete (no Disable); deletion double-confirm when a pack
  has non-completed runs.
- Connected the **campaign status record** to current code: already implemented at the
  data layer (`CampaignStatusRecord.gd`, `CampaignStatusStore.gd`) with an open-dictionary
  payload, SHA-256 checksum, provenance, and author-declared `compatible_status_sources`
  cross-pack matching. Remaining work is player-facing Library/inbox/extension integration
  only.
- Recorded decisions durably in a new decisions doc; updated the owner packet status and
  the `B6-CAMPAIGN-LIBRARY-UX` control-plane row; logged the full-library-backup deferral
  in the workspace tracker. No runtime, scene, save-schema, or release-line behaviour
  changed — research/planning only.

## Commits claimed

- `27be6e693a5a879a1216975ccc89af778f378b4a` — Record campaign-library owner decisions (Branches A-D)

## Gates

- `python3 AGENT/Docs/gen_docs_index.py` + `python3 AGENT/Docs/check_docs.py`: all
  documentation checks green.
- Workspace `coordination/check_tasks.py`: run at closeout (see container-repo commit).

## Next

Resume at **Branch E — Launch / New Game / rule profiles** (CL-LAUNCH-01/03/04/05;
CL-LAUNCH-02 already answered). Then F → K. Copy each branch's ids/answers into
`AGENT/Docs/design/campaign_library_ux_decisions_2026-07-24.md` and create implementation
tracker rows only for accepted scope.
