# Session Note — 2026-06-26f  (`[RDR-1..11]` full walk RESOLVED + `absorb` added + sibling `[CVR]` cover spawned)

**Topic:** Walk the `redirect` register `[RDR]` and all edge cases. Branch `docs-reorg-2026-06-23`.
Planning only — no code.

**Outcome:** **`[RDR-1..11]` RESOLVED.** Three owner scope-calls + an owner-added `absorb` dimension;
`redirect`'s design is settled. Spawned a **sibling primitive `cover`** → new OPEN register **`[CVR-1..6]`**.
Atlas forward-refs wired (F5 event, A5 death-ordering, STY-9 selector). INDEX/REGISTERS regen; **19 checks
green**. 1 commit.

---

## 1. Owner scope-calls (AskUserQuestion, 2026-06-26)
- **Cover sibling:** **spec a sibling `cover` primitive now** (pre-application reassignment) — NOT a
  redirect mode. → register `[CVR]`.
- **Effect kinds:** **include non-scalar displacement/forced-move** in v1 (not scalar-only) → per-kind
  transform; couples to DSP.
- **DoT-tick:** **direct-apply in the preset; per-tick opt-in** (carries the "conditions store source" M8 req).
- **`absorb` default when target unavailable:** **proportional** — `absorb × N_valid/N_intended`.

## 2. The model (firmed)
`redirect` = an **effect interceptor**: on a matching incoming effect it **(a)** optionally **absorbs** a
portion off the holder (pre-application) and **(b)** emits a **transformed effect at a `[STY-9]`-selected
target-set** (post-application). "Reflect" = preset `target={source}`; **full absorb + bounce** = the
parry/full-reflect fantasy. **Deterministic spine:** intercept → predicate → resolve target-set
(`N_intended`/`N_valid`) → compute absorb+redirected → gate absorb (RDR-11) → holder takes
`incoming−absorb` → emit to valid targets (non-redirectable) → death snapshot-resolve.

## 3. Resolutions (RDR-1..11)
- **RDR-1** full-to-each + `target_count` term var. **RDR-2** identity/`{from→to}` map + term; emitted via
  target's normal apply; **non-scalar = per-kind transform** (recompute shove direction). **RDR-3**
  post-mitigation read, target's pipeline (+`[REQ-15]` floor), fires on the *event* (misses don't),
  **non-redirectable flag rides the instance** (one bounce). **RDR-4** anchor `{source,holder}` def
  `source`; **`tile` dropped**; selector = **`[STY-9]`**. **RDR-5** independent fire, deterministic order;
  preview shows absorb + all targets + combined + `fires_on_death`; chance→odds. **RDR-6** v1 = damage +
  scalar conditions **+ non-scalar displacement** (owner). **RDR-7** forward-reqs: F5 source-bearing
  `effect_applied`+`redirectable` flag + conditions-store-source; selector=STY-9; hook in
  `CombatResolver`. **RDR-8** → A5 "simultaneous deaths": **snapshot-then-resolve**, deterministic
  disposition order, mutual kills both die; **kill-credit/EXP → holder even if dead**. **RDR-9** holder
  excluded by default; allies = share/radiate; **scope boundary → `cover` sibling**. **RDR-10** `absorb`
  term (pre-application reduction; `full`=parry; `absorbed_value` term var; non-scalar=all-or-nothing).
  **RDR-11** `absorb_on_unavailable: proportional(def)|gated|unconditional` — proportional by **count**,
  `N_intended==0`→0 absorb.

## 4. Sibling `cover` (`[CVR-1..6]`, OPEN)
Pre-application **reassignment** (mirror of redirect): moves the **original** effect's target to a
protector before it lands — protector's own mitigation vs the original attacker, **original**
kill-attribution = FE Aegis/guardian "take the hit *instead of*". Distinct from `redirect.absorb=1`
(which emits a new holder-attributed effect). Shares the interceptor family, `[STY-9]` selector,
`CombatResolver` hook, M8/A5 deps. **To walk next.**

## 5. Files / commit
- `registers/redirect_effect_open_questions_2026-06-26.md` — rewritten RDR-1..11 RESOLVED + resolution
  spine + new edge cases; frontmatter `Status: RESOLVED`.
- `registers/cover_intercept_open_questions_2026-06-26.md` — **NEW** `[CVR-1..6]` OPEN.
- `design/candidate_systems_2026-06-23.md` — candidate G updated (walked/RESOLVED + `absorb`); **new
  candidate H** (`cover`).
- `plans/feature_dependency_atlas_2026-06-23.md` — F5 row forward-req (effect_applied event), A5
  simultaneous-deaths bullet (snapshot-then-resolve + kill-credit), STY-9 (shared selector).
- INDEX/REGISTERS regen; `check_docs.py` 19 green. Commit: _(this session)_.

## 6. NEXT SESSION
- **Walk `cover` `[CVR-1..6]`** (pre-application reassignment), reusing the redirect spine + STY-9.
- Confirm RDR-7 (F5 `effect_applied` event) + RDR-8 (A5 snapshot-then-resolve) are picked up when those
  builds happen (now on the atlas F5 + A5 rows).
