# Session Note - 2026-06-28f

## Topic
Pre-GDD feature triage reprioritization.

## What was done
- Read the latest handoff (`2026-06-28e.md`), session-note index, generated docs
  index/register catalog, feature dependency atlas, F1 source/lock docs, shared
  foundation contracts, and the active GDD roadmap/overview context.
- Reworked `AGENT/Docs/plans/planned_unimplemented_feature_triage_2026-06-28.md`
  from a rough list into a dependency-banded scheduling input for the unified GDD:
  - Bands 1-5 = conservative v1-core path.
  - Band 6 = v1-lean/stretch.
  - Band 7 = optional after stable core.
  - Band 8 = post-v1 / parked.
- Added explicit scheduling rules: open registries for author-facing vocabulary,
  F1 rows before save fields, action/effect primitives for mutations, resource
  ledger for costs, occupancy service for non-standard placement, death funnel
  for all death causes, projection for forecasts, and `launch_activity` for
  scheduled side activities.
- Decided the `ActivityRegistry` / `ActivityRunner` seam stays `V1-optional`.
  Promote it only if v1 commits to one side activity; then build one first-party
  template and no public scripting VM.
- Updated `AGENT/Docs/plans/unified_gdd_pass_followups_2026-06-28.md` so the
  later GDD rewrite consumes the new dependency bands and keeps the public VM
  parked.

## Validation
- `python3 AGENT/Docs/check_docs.py` - PASS
- Commit hook also ran `check_docs.py` and `check_rng_usage`; docs-only commit
  skipped the Godot suite.

## Commits this thread
- `7496dbd` - `Reprioritize pre-GDD feature triage`
- Session note/index commit - `Record pre-GDD reprioritization session`

## Next session
- Use the reprioritized triage as the source for the unified GDD consolidation.
- Rewrite GDD/GDD_10 around the band split rather than the older milestone
  sequence.
- During consolidation, keep side activities conditional and public scripting VM
  support parked unless the owner changes v1 scope.
