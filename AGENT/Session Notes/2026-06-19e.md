# Session Notes — 2026-06-19e (v0.2.1 playtest return: triage + plan, no code)

## What was done

The tester (Jacob Jackman, Win11, 1080p) returned the v0.2.1 playtest handbook. Triaged
it into the regular Todo pipeline and developed a full fix/design plan. **Plan-only — no
source changes; implementation waits on approval.**

### Re-verification result

v0.2.0 fixes that **passed**: 1.1 camera jitter, 1.2 live Map Zoom slider, 1.3 forecast
placement, 1.5 one-based Seize coords. Fixes that came back: **1.4 F9 hotseat** and
**1.6 HUD reset / terrain**. Error-log check returned unmarked (treat as NOT RUN).

### Merged into the pipeline

- Archived the verbatim return → `AGENT/Docs/playtest_checklist_v0.2.1_returned_2026-06-19.md`.
- Added a newest-first **"v0.2.1 findings"** action list to `GDD_10_Roadmap.md`, items
  `V021-01..19`, all open.
- Wrote the triage/fix plan → `AGENT/Docs/playtest_v0.2.1_triage_plan_2026-06-19.md`
  (per-item workstreams A–F, likely files, recommended order, verification + DoD).

### Decisions taken (user, this session)

1. **Scope of v0.2.2 → everything**, including the design projects.
2. **Map HUD pair-up line (V021-07) → remove the per-stat line** (breakdown stays on the
   `I` sheet), keep `Support: <name>`, raise the default block so it isn't clipped.
3. **Movement type (V021-11) → keep the tag model, no new field.** Add an explicit default
   `infantry` tag, define precedence `flying > mounted > armoured > light_footed >
   infantry` to resolve multi-tag units, surface the resolved type in class More Info.
   (Investigated first: movement type is currently *implicit* — `GridManager` reads
   `has_quality("mounted"/"armoured"/"light_footed")` for the desert rule; flying is only
   an effectiveness/vulnerability concept, no terrain-cost class; `special_qualities` has
   no allowlist so `infantry` is safe to add.)
4. **Design depth → full designs for both**, written this session:
   - `AGENT/Docs/terrain_more_info_paging_design_2026-06-19.md` (V021-05): `F`-flipped
     terrain pages (Description / Movement), a real Hidden state to reclaim map area,
     reflow hardening that also addresses the V021-02 reset bug; couples with V021-07/15.
   - `AGENT/Docs/mouse_only_cursor_mode_design_2026-06-19.md` (V021-17): re-introduce a
     third `mouse_cursor` value (`follow|click|disabled`) — `click` = first click moves
     cursor, second click on the same tile selects; terrain panel click cycles pages.
     (Found `mouse_cursor` was historically tri-state with a legacy `snap` value.)

## Verification

- `check_docs.py` green after each doc change (broken-path check forced creating the
  referenced plan/design docs). No code, no tests run this session.
- `__pycache__` under AGENT/Docs is gitignored; ran Python with `PYTHONDONTWRITEBYTECODE=1`.

## State

- Branch `code-review-followups-2026-06-17`. This session's docs committed; v0.2.1 tag
  and earlier commits status per 2026-06-19d (were local — confirm push state).

## Next

- **Get plan approval, then implement v0.2.2** in the recommended order (Workstream A
  hotseat → B HUD editor → C sheet/movement → D map HUD/menus → terrain paging → reopened
  quick wins → larger reworks V021-18/19).
- Each behavior change: GDD_01–08 + GDD_10 status flip in the same commit (DoD#1); new
  mechanical rules get a `check_docs.py` check (DoD#2).
- Request a tester rerun of the Error-log check (NOT RUN this pass).
