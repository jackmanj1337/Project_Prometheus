# Session Notes — 2026-05-24j

## Focus
Audit the long-form documentation for stale API/schema references and resync the
 docs that still described deprecated systems as current.

## What was done
- Updated `AGENT/GDD/GDD_01_Architecture.md` so the autoload, unit, turn-flow,
  weapon, class, and map-resource sections match the current faction-driven,
  Pair Up-aware, promotion/reclass-capable codebase.
- Updated `AGENT/GDD/GDD_06_Maps_Objectives.md` to document the current
  `ObjectiveCondition`-based objective system instead of the removed
  `objective_type` / `objective_params` / `required_survivor_ids` schema.
- Updated `AGENT/GDD/GDD_03_Units_Classes.md` to reflect the current default
  roster, WEXP-driven class model, and implemented promotion / reclass flow.
- Added explicit historical-context notes to `GDD_09_Checklist.md` and
  `GDD_10_Roadmap.md` so older milestone text is not mistaken for the live
  implementation contract.

## Tests run
- `bash run_tests.sh`

## Commits made this session
- This note is included in the documentation audit commit for this session.

## Plans for next session
1. Use the refreshed architecture/content docs as the baseline during future
   feature work and playtesting.
2. If a future migration leaves an old field name behind, either rewrite that
   section immediately or mark it explicitly as historical the same day.
