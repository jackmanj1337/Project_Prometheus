# Session Notes — 2026-05-21

## Context

User asked to clean up the codebase for the new dual-agent Claude Code + Codex
setup after the shared Docker container work.

## What Was Done

- Consolidated historical agent documentation from `CLAUDE/` into `AGENT/`.
- Added `AGENTS.md` as the shared instruction file for Codex and other agents.
- Changed `CLAUDE.md` to a compatibility shim that delegates to `AGENTS.md`.
- Updated stale internal documentation references from `CLAUDE/` to `AGENT/`.
- Added `.codex` to `.gitignore` so local Codex auth/session state is not tracked.
- Updated the `GameState.gd` comment for unenforced skill/inventory caps to point
  at the existing roadmap file.

## Verification

- `bash run_tests.sh` passed: all suites green.
- The pre-commit hook ran `bash run_tests.sh` again and passed.
- `rg -n "CLAUDE/|CLAUDE.md" AGENT scripts AGENTS.md CLAUDE.md` returned no
  stale shared-documentation references.

## Commits

- `034b096` — Consolidate shared agent docs

## Plans for Next Session

- Consider whether `AGENTS.md` should be tightened into a shorter operational
  checklist now that both agents share it.
- Push the cleanup commits when ready.
