# Session Note - 2026-07-15e

## What was done

Resumed the dedicated `prep-save-followup` worktree after confirming the four
post-build save defects had already landed on `main` in `a2c953c`. Completed the
remaining `B4-PREP-DEPLOYMENT` Slices 2-3:

- Added `PrepScreen`, a destination screen that lists living eligible units,
  locks required units, supports deploy/bench choices, and orders selected units
  onto numbered player start tiles.
- Kept prep a pure plan author: `CampaignManager` resolves the map and roster,
  prep only stages `GameState.next_map_deployment`, and `GameMap` remains the
  revalidating consumer.
- Routed campaign launch and non-suspend campaign Retry through prep. Bare-map
  and suspend-resumed Retry retain direct reload behavior.
- Added manual campaign save on prep using the existing transactional slot seam;
  unsafe player-supplied slot ids fail without writing.
- Updated the data/surface contracts and roadmap rows from Planned/Split to the
  implemented behavior.

## Validation

- `test_prep_screen`: 5/0 (legal seed, reorder, bench, unsafe/valid manual save).
- `python3 AGENT/Docs/check_docs.py`: 31/31.
- `bash scripts/ci/check_rng_usage.sh`: green.
- `bash run_tests.sh`: all 82 suites green.
- `git diff --check`.

## Commits

- One implementation commit follows this note.

## Next session

Render the new PrepScreen in the next Windows playtest and check keyboard/gamepad
focus, long roster scrolling, and text fit. Service panels (convoy/trade/shop)
remain later `B3-PHB` work; they were deliberately not folded into this slice.
