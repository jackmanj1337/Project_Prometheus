# Session Note — 2026-06-24h  (A3 branch #4: Recruit/Capture roster side firmed `[RCR-1..7]`; A3 cluster complete)

**Topic:** Walked the third and last A3 branch — **Recruit / Capture (#4)** — firming the
**roster-facing side** `[RCR-1..7]` and reviewing the conversation (A4) side for context. Completes
sync-cluster **A3**. Branch `docs-reorg-2026-06-23`.

---

## Conversation-side review (context that shaped the roster decisions)
- **Faction model exists:** `Unit.team` (`blue`/`red`, green/yellow planned) + `MapData.factions` →
  recruitment = a **faction flip** to the player faction. Clean slate otherwise.
- **Conversation side is MET's (A4):** MET (F8) has planned `dialogue` + `flag` actions but **no
  `talk`/`recruit` action yet**. So the roster side exposes a **state + transition API** the MET
  action calls — it does NOT own a trigger. REL-6's `unlock_conversation` verb is reused.
- **Capture is a 3-way straddler:** roster-state (A3) + physical carry/jail (A2 rescue/displacement)
  + trigger (A4). Firming capture's carry here would re-firm it at A2.

## Decisions `[RCR-1..7]`
- **Model:** faction flip → **persistent roster member** (RCR-1).
- **Recruited-state:** first-class **roster membership + auto-set F6 flag** `recruited:<id>` (RCR-2);
  membership = "in my army", flag = "did I recruit X" for branching. Both reserved in F1.
- **A3/A4 seam:** roster = `recruit()`/`capture()` API + state; **MET (A4)** = `talk` trigger +
  `recruit`/`dialogue` actions calling it. Wrote the **MET-action contract** A4 builds against (RCR-3).
- **Eligibility split:** unit carries identity/reward (recruitable, by-whom hint, join reward); MET
  trigger carries firing-conditions (recruiter present, flags, "not attacked") (RCR-4).
- **Capture scope:** talk-recruit + recruited-state firmed now; capture roster end-state = captured →
  recruitable; **physical carry/jail deferred to A2** (RCR-5).
- **Main-character eligibility:** a recruited unit may be a `[MCH]` main character; recruit convos
  reuse **F13** + REL-6 (RCR-6).

## A3 cluster complete
All three branches have their roster-facing side firmed: `[REL-1..9]` · `[MCH-1..8]` (+ foundation
**F13**) · `[RCR-1..7]`. The A3 roster/save schema is defined for the F1 lock. Atlas §3b A3 bullet +
#4 row reconciled; the lingering "support" wording in the A3 bullet corrected to "relationship".

## Files
- New register `registers/recruit_capture_open_questions_2026-06-24.md` `[RCR-1..7]` RESOLVED.
- Atlas: #4 row GAP → firmed (roster side); A3 cluster bullet status + naming fix.
- Regenerated `INDEX.md` + `REGISTERS.md`.

## Next session
**Cluster A1 — combat capabilities & attack/target flow.** Head = finish the **CEX cluster-B eval**
(separate spell Qs `CEX-5..8` from the weapon-source/equip scope `CEX-20..23`), then combat arts
(#15), utility staves (#10), gambit attack side (#16 ⇄ A2). Then A2, A4, A5 → F1 schema-lock.
