# Session Note — 2026-06-22a

**Topic:** Planning-only continuation of the AI-profiles work (2026-06-21k). Walked the full
profile target list in implementation-level detail, then **pressure-tested the resolved plan
against the Fire Emblem catalog** and recorded a gap analysis + four held MVP-spec refinements
into the AIP register. No new decisions ratified — owner is holding spec refinements until the
eventual AI design plan is firmed. Docs-only; check_docs **17/17**; no `__pycache__`.

---

## 1. What happened
- Went profile-by-profile (built + MVP + fast-follow + gated) with the per-activation decision
  procedure, data reads, compose rules, and edge cases for each.
- Four MVP-spec refinements surfaced; owner chose to **hold** them (record, don't ratify):
  territorial wake-on-attack; patrol = leashed-return-home (not route-walker); flee optional
  `goal_tile`; `weakest` = KO-first metric threaded through move-tile choice.
- Owner then asked the key question: **what FE archetypes/scenarios can't the current plan
  express?** Produced a three-layer gap analysis.

## 2. Gap analysis result (register §8)
Disposition axis ≈ complete. Three real gaps FE leans on heavily:
1. **Event/turn-driven aggression** (biggest; rides MET) — wake model is proximity-only;
   "army charges on turn 6" isn't expressible. Fix: MET `set_aggro`/`wake` action **or**
   flag-honoring `territorial` (the proximity↔event aggro bridge).
2. **Goal-tile / objective-seeking movement** — all dispositions target units, not tiles.
   Defend chapters (enemies seize *your* throne) + escape AI need a `seek_tile`/`advance`
   disposition + `flee goal_tile`. Ties to the FOW encounter-layer idea.
3. **Per-engagement combat sophistication** — a SEPARATE "combat AI" workstream: auto-equip
   best weapon, decline unfavorable trades, item-use, value-based targeting.
- Smaller: `unit_hp_below` MET trigger (boss enrages at half HP); confirm `spawn`-acts-
  immediately (ambush spawns); berserk breaks `_living_hostiles_for_faction` (all-faction
  targeting); AI door-open/wall-break deferred by plan; escort/lure niche.

## 3. Edits
- `ai_profiles_open_questions_2026-06-21.md` — new **§7 (four held refinements, not ratified)**
  + **§8 (FE gap analysis, three layers + asterisked items)**.
- `map_events_triggers_open_questions_2026-06-21.md` [MET-3] — AIP gap cross-ref: candidate
  `set_aggro`/`wake` action, `unit_hp_below` trigger, `spawn`-acts-immediately flag.
- `GDD_10_Roadmap.md` §H — "Forward design items surfaced by the AIP gap analysis" bullet
  (event-aggro bridge, goal-tile disposition, combat-AI workstream, MET growth).
- DoD#1: planning only (no behavior change). DoD#2: no rule ratified.

## 4. Verification
check_docs.py **17/17 PASS**; docs-only; no `__pycache__`.

## Session status: CLOSED.
AIP register now carries its resolved decisions ([AIP-1..5]), the canonical target list (§2a),
the build slice (§4), four held refinements (§7), and the FE gap analysis (§8). **Nothing new is
scheduled** — the three gaps + held refinements are a forward record to revisit when firming the
AI design plan. Build order unchanged: **Package A Step 1 first.** Next = build Package A, firm the
AI design plan (would resolve §7 + decide §8 Layer 1/2 scope), or walk STW / a release-gate register.
