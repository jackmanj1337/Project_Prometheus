# Session Note — 2026-06-21f

**Topic:** Planning-only. **Walked + RESOLVED the Package A / `RngService` register
(`[PKGA-1..4]`)** — the build-first Layer-0 item the 2026-06-21e sort named as the next move.
All four resolved to the recommended option. Package A is now **build-ready** with its own
top-level roadmap milestone. Docs-only; check_docs **17/17**; no `__pycache__`.

---

## 1. What was resolved (all = recommended option, via AskUserQuestion)

| Item | Question | Resolution |
| --- | --- | --- |
| **[PKGA-1]** | Roadmap home | **A** — new top-level milestone "Package A / Determinism" ahead of §2; Steps 1–5 checklist; GDD_10 Open Items row. Single source of truth over the design doc's old Bucket B/E split. |
| **[PKGA-2]** | Pre-§2 build scope | **A** — **only Steps 1–2 gate §2** (service + migration sweep + snapshot contract). Steps 3 (suspend) + 4 (rewind) interleave with §2's save plumbing → minimises the pre-§2 critical path, avoids building suspend twice. |
| **[PKGA-3]** | `rewind_charges` field home | **B** — defer to §2's `CampaignRules` consolidation ([CST-4]). Package A Steps 1–2 don't need it; the snapshot reads it only if present. Avoids a half-wired field outside the ~70-ref consolidation pass. |
| **[PKGA-4]** | Two-RN "true hit" timing | **A** — ship two-RN (RULE-001: hit when `floor((r1+r2)/2) < displayed_hit`) **with Step 1**. One change, no migration debt, M9 skills land on the final model. **Flag the balance shift in the next playtest note** so it's observed, not silent. |

> Net effect: the planning gap on Package A is closed. The algorithm was never the gap — the
> build guide (`rng_determinism_design_2026-06-11.md`) was already implementation-ready; these
> four were sequencing/placement calls. Execution can start cold from the milestone checklist.

---

## 2. Build steps (now on the roadmap, from build guide §12)

- **Step 1 — `RngService` + migration sweep (GATES §2).** Autoload after `EventBus`/before
  `GameState`; seed mixer; replace raw `randi()/randf()` in `CombatResolver`, `Unit.level_up()`,
  `SkillHandler`; **two-RN true hit**; tests T1/T3/T4/T5/T7. T5 raw-RNG lint guard → `run_tests.sh`/CI (DoD#2).
- **Step 2 — Snapshot contract (GATES §2).** One JSON-safe `to_save_dict` across §8.1; migrate
  the in-memory Retry snapshot onto it (also fixes [CST-2]); T2 deep-equal round-trip.
- **Step 3 — Suspend save** (interleaves with §2). §8.2 + Continue flow + T6. Player-facing.
- **Step 4 — Rewind** (interleaves with §2; ties to [CST-13]). Checkpoint ring + Map Menu UI;
  `rewind_charges` arrives via §2's [CST-4] consolidation. Player-facing; mostly UI.
- **Step 5 (deferred, with M15B)** — result-payload protocol per §9.

---

## 3. Edits made
- `package_a_rngservice_open_questions_2026-06-21.md` — header status flipped to RESOLVED;
  each [PKGA-1..4] resolution line filled in.
- `GDD_10_Roadmap.md` — (a) Open Items Register §A row rewritten as a build-ready milestone row
  with the four resolutions inline; (b) **new `## Milestone — Package A / Determinism` section**
  (status, build-guide pointer, the four resolutions, the 5-step checklist with §2 gates marked,
  DoD); (c) §H index note updated: PKGA RESOLVED, others still OPEN.
- DoD#1: behavior unchanged (planning only) — status flips + detail home landed in the same
  edit. No new check_docs rule needed (no new mechanical vocab/path rule ratified; the durable
  Package A check is the T5 raw-RNG lint test, which lands with Step 1 in `run_tests.sh`/CI).

## 4. Verification
check_docs.py **17/17 PASS**; docs-only; no `__pycache__`.

## Session status: CLOSED.
**Next session = START THE PACKAGE A BUILD — Step 1** (`RngService` autoload + migration sweep +
two-RN true hit + T1/T3/T4/T5/T7), then Step 2 (snapshot contract). Steps 1–2 unblock the §2
campaign/save spine. Independent parallel tracks still available if a break from the spine is
wanted: **DMR** (pure refactor), **MRD** slices 1-3 (player-visible), **IMP** (tooling). Owner
inputs **[REN-1]** (rename names) / **[LEG-1]** (legal corpus) gatherable any time before the
release-gate push.
