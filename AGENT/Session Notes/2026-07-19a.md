# Session Note - 2026-07-19a

## What was done

- Added the side-effect-free `CombatResolver.project_exchange()` ordered exchange
  projection on the ratified AI-decision branch stack without changing any shipped
  AI consumer or the existing `preview_combat()` implementation.
- Modeled first strike, multi-strike, follow-up, weapon-break, hit/critical, and
  death-stop outcome branches with bounded probabilities, symmetric style slots,
  explicit proc-policy selection, and the ratified tile-independent cache identity.
- Added purity, ordering, cache, durability, and extreme-bound regression fixtures;
  updated the owning GDD, roadmap, control plane, and waiting-work evidence matrix.

## Commits claimed

- `3cf44e8867b479aabb69b1c40fa1620dd42bd613` — Add ordered combat exchange projection

## Gates

- `bash scripts/check_env.sh` — passed relevant checks; expected local `.env` warning.
- `python3 AGENT/Docs/check_docs.py` — all 40 documentation checks passed.
- `bash scripts/ci/check_rng_usage.sh` — passed.
- `godot --headless --path . --script res://scripts/tests/test_project_exchange.gd`
  — 6 passed, 0 failed.
- `bash run_tests.sh` — all 103 suites passed, including 47 combat assertions and
  26 unchanged enemy-AI assertions.
- Exact staged-tree receipt:
  `audit/check-receipts/Project_Prometheus-fast.json`.
- `AGENT/Incoming/v0.5.2/` was absent immediately before the implementation commit.

## Next

Close `AI-SLICE-A-PROJECT-EXCHANGE-2026-07-19` in the canonical workspace tracker,
then continue the decision-complete queue. Do not begin AI Slice B or C until this
stack is reviewed and merged.
