# Session Note — 2026-06-26c  (`[EXT]` walk — EXT-1..4 + EXT-5 mechanism RESOLVED off the REQ-16 worked example)

**Topic:** Owner asked to walk the parked author-extensibility questions `[EXT-1..6]` and "see how many
have obvious answers now" — given the `[REQ-16]` arithmetic-terms walk (2026-06-26b) was a deliberate
worked example that exercised every EXT dimension. Branch `docs-reorg-2026-06-23`. (Continues 2026-06-26b.)

**Outcome:** **EXT-1, EXT-2, EXT-3, EXT-4 RESOLVED** + **EXT-5 *mechanism* RESOLVED**; register stays
**OPEN** for **EXT-5 cadence/process** + **EXT-6 per-vocabulary confirmation** only. Atlas F16 row
updated; INDEX/REGISTERS regen; **19 checks green**. Planning only — no code.

---

## 1. The assessment (per-question verdicts)
REQ-16 front-loaded the dedicated EXT walk. Verdicts:
- **EXT-1 (model A/B/C) — OBVIOUS → A ("A-plus").** REQ-16 proved A is sufficient + content-clean for the
  hardest case; DLG effects / F4 profiles / MET already behave as Option-A data-composition today. B = a
  narrow evidence-gated future exception; C rejected.
- **EXT-2 (content-model fit) — OBVIOUS → yes, A preserves no-code.** Compositions are data trees; a
  string front-end is build-time sugar that emits a validated tree (runtime evaluates data, not strings).
- **EXT-3 (validation) — OBVIOUS (pattern) → structural load-validation against the engine primitive
  registry, fail-loud.** REQ-16's rules (op∈set, arity, typed operands, required params, budget, reject
  impure refs) are the uniform template; each vocabulary enumerates its own registry.
- **EXT-4 (determinism) — OBVIOUS → pure + side-effect-free; only RNG = Package-A latched `chance`;
  fixed-point for cross-platform determinism.** The pure/impure split governs what can ever inline.
- **EXT-5 (primitives + cadence) — MOSTLY.** Mechanism settled (named-composition relief valve +
  primitive-request channel that doubles as a contributor on-ramp). Remaining = the formal cadence/triage
  *process* (lightweight, not a fork).
- **EXT-6 (one model for all vocabularies) — DIRECTION OBVIOUS, confirmation owed.** Answer = one model =
  A uniformly; no vocabulary has a genuine B-requirement. Remaining = an explicit per-vocabulary check
  (DLG effects · F4 profiles · MET) — asserting ≠ checking.

**Tally: 4 obvious (1–4), 1 mostly (5), 1 directional-with-confirmation (6).**

## 2. Owner call — ratify the settled core
Owner chose **"Ratify 1–4 + EXT-5 mechanism"** (over "ratify all" / "assessment-only"). So:
- EXT-1..4 → **RESOLVED**; EXT-5 → **PARTIAL** (mechanism RESOLVED, cadence OPEN); EXT-6 → **OPEN but
  narrowed** to a per-vocabulary confirmation.
- Register frontmatter `Resolved-in` + body Status updated; candidate-direction A marked ✅ CHOSEN.
- The remaining EXT walk is **much lighter than originally scoped** and **likely off the A5 critical
  path** (it was previously scheduled as a full dedicated session before A5).

## 3. Files / commit
- `authoring_extensibility_open_questions_2026-06-26.md` — frontmatter `Resolved-in`; body Status; A=✅
  CHOSEN; EXT-1..4 `[RESOLVED]`, EXT-5 `[PARTIAL]`, EXT-6 `[OPEN — narrowed]` with resolutions.
- `feature_dependency_atlas_2026-06-23.md` — F16 row EXT note updated to the partial-resolution state.
- INDEX/REGISTERS regen; `check_docs.py` 19 green. Commit: _(this session)_.

## 4. NEXT SESSION — pre-A5 plan now lighter
- **EXT remaining (light):** EXT-5 **cadence/process** + EXT-6 **per-vocabulary confirmation** (DLG
  effects · F4 profiles · MET adopt A) — no longer a full dedicated session; can fold into the relevant
  vocabulary walks.
- **`reflect` effect primitive (owner — discuss before A5):** candidate G in
  `design/candidate_systems_2026-06-23.md` (⚠ disambiguate from the `[DLG-9]` dialogue-visual reflect).
- Then **A5 walk** (incl. shop/`activate` `[VIL-2]` configs) → **F1 schema-lock**. Sweep owes F14
  `[STM]` · F15 v1 slice (`[DLG-1..14]`) · **F16 `[REQ-1..16]`** · `[BAT-14..16]`.
