# Session 2026-07-10d - v0.3.1 review + build cut

Branch: `v0.3.0-features`. Reviewed the v0.3.0.d diagnoses/fixes for playtest
readiness, fixed one real bug found in review, then cut the v0.3.1 focused rerun
build.

## What was done

- **Code review of the v0.3.0.d fix pass** (`0e46452`/`533734a`/`2d1fc11`)
  against the triage plan. Diagnoses are well-grounded in returned live
  logs/screenshots; fixes are correct. Ran the full 69-suite (green) and
  check_docs (27/27) locally to verify, not just trusting the notes.
  - Targeting stick-cycle path is safe from double-step (`arm_repeat` sets the
    delay before the `_process` poll ticks).
  - Maximize label branch is reachable; maximize is correctly excluded from
    write-back.
  - DSP-02 one-axis drag fix is a **hypothesis** (added root-Window
    `size_changed` on the theory it fires where the viewport signal did not) —
    flagged as the item most likely to still need the live log.
- **Fixed one real bug found in review** (`7a7a1ce`): during a keybind capture,
  `SettingsScreen._input` correctly bypasses base nav, but the base
  `ModalScreen._process` polls `ui_up`/`ui_down` strength directly, so holding a
  direction to rebind it would scroll focus off the capture row. Overrode
  `_modal_focus_repeat_enabled()` to gate on `_capturing_action`. Added a
  `test_settings_screen` assertion (30 -> 31).
- **Cut v0.3.1** (owner chose `v0.3.1` over the plan doc's `v0.3.0.e`):
  - Bumped export preset name/path/product_version, Main Menu `VersionLabel`,
    and `environment_setup.md` to `v0.3.1` (`c7ce311`).
  - Wrote the self-contained tester handbook
    `playtest_checklist_v0.3.1.md` tailored to the post-v0.3.0.d fixes.
  - Tracked the 5 stray `.png.import` evidence sidecars from the v0.3.0.d triage
    commit (hygiene).
  - Stamped `build_info.json` and exported
    `builds/Project_Prometheus_v0.3.1_debug.exe`.
  - Wrote `playtest_build_v0.3.1.md` (`3baf711`).

## Commits

- `7a7a1ce` - `Suppress modal focus repeat during keybind capture`
- `c7ce311` - `Bump release metadata to v0.3.1 + focused rerun handbook`
- `3baf711` - `Add v0.3.1 build manifest`

## Build artifact

- `builds/Project_Prometheus_v0.3.1_debug.exe`
- Source commit `c7ce311`; size `101685192` bytes;
  SHA-256 `ac4b079a5fdde815823f0c6c6fe883a1e23f59792fe66d4b761995bdb1205332`;
  Godot `4.6.stable`; `build_info.json` stamp version `0.3.1`.

## Validation

- Full 69-suite green on the pre-commit hook of each code commit.
- `test_settings_screen` 31/0; modal/cursor suites (menu_repeat_policy,
  new_game_screen, promotion_screen, reclass_screen, unit_details_screen,
  map_cursor) all green.
- Release-metadata test 5/5 (preset/label/checklist/setup all at `v0.3.1`).
- `check_docs` 27/27; docs INDEX regenerated.

## Decisions / next session

- v0.3.1 ships as **two files**: the exe + `playtest_checklist_v0.3.1.md`.
- `VAL-V030-GAMEPAD` and `VAL-V023-DISPLAY` remain **Pending** by design — this
  build is the live-validation vehicle. DSP-02 (one-axis drag) is the likeliest
  to bounce; §4 exists to capture the `V030-DSP-TRACE` log if it does.
- After the live return: flip gates on evidence, pick the MRD-7 shipped look
  (§5), then remove the temporary F8 cycle and the `V030-NG-FOCUS`/
  `V030-DSP-TRACE` instrumentation.
- Main Menu 2.0x overlap stays routed to `UI-INSPECTION`.
