# Session Note — 2026-06-21j

**Topic:** Planning-only. **Walked AND resolved the Fog of War / Line-of-Sight register
`[FOW-1..7]`** (FOW-7 added this session). Owner's answers expanded v1 scope with two authored
reveal tools that fold cleanly into already-resolved registers (MET, DCH). Build-ready, gated
behind §2's `discovered_units` save slice. Docs-only; check_docs **17/17**; no `__pycache__`.

---

## 1. Why FOW next
Last session (2026-06-21i) closed MET as the fourth resolved register and flagged FOW + AIP as the
next candidates. Picked **FOW** (Tier 1): bigger unknown, owns DTR's deferred LoS, and its headline
call **[FOW-3]** (does AI cheat under fog?) is the upstream decision for AIP's fog-scout profile —
resolving FOW first unblocks a clean AIP pass.

**Code re-verification before walking** (draft was 2026-06-21d): `UnitData.line_of_sight` exists,
nothing reads it; **also found `ClassData.base_line_of_sight`** feeding it via reclass deltas
(`Unit.gd:1059`) — so the vision input is the resolved `unit.data.line_of_sight` (single int, already
in `test_snapshot_coverage`). `_living_hostiles_for_faction` is the single AI-acquisition seam
(`EnemyAI.gd:319`). Overlay infra (`_paint_overlay`) present. No `fog_enabled`/`visible_tiles` yet.

## 2. Resolutions ([FOW-1..7])

| Item | Decision |
| --- | --- |
| **[FOW-1]** | **A — flat radius** + a `compute_visible_tiles()` seam so true-LoS occlusion (B) is a later expansion. Owner added 2 v1 reveal tools (rooms-on-event, braziers). |
| **[FOW-2]** | **Encounter/scenario property, not terrain, not run-wide.** Fog lives with the roster + difficulty. v1: `MapData.fog_enabled` (the roster already lives on MapData). Forward: MapData terrain/encounter decomposition; timed/weather fog via MET `set_fog`. |
| **[FOW-3]** | **A — AI cheats v1**; symmetric fog (B) reserved as a future `CampaignRules.ai_respects_fog` rule. Constraint: keep AI acquisition funnelled through `_living_hostiles_for_faction` so the rule wraps one seam. |
| **[FOW-4]** | **A-full — per-step mid-tween reveal WITH ambush interrupt** (move halts on the step that reveals an enemy). The one real v1 complexity. |
| **[FOW-5]** | **A — `discovered_units` is the only new save field.** Rooms re-reveal from `map_events_fired`; brazier `lit` from `map_objects_state` (both already reserved). |
| **[FOW-6]** | **A — flat radius v1.** Fast-follows: terrain LoS bonus, torch *items*, concealment (hidden-in-forest). |
| **[FOW-7]** *(new)* | Brazier = **`map_object`** (DCH model) with `lit` + `vision_radius`; lit by **unit action AND new MET `light` action** (both). One-way for v1. |

## 3. New v1 scope (owner-added) — converges on resolved registers
Owner's [FOW-1] answer expanded v1 beyond pure unit-LoS fog with two **authored reveal tools**, both
riding registers already resolved, adding **zero new save fields**:
1. **Closed rooms that reveal on map events** = MET's existing `reveal_tiles` action (`[MET-3]`)
   writing into fog's persistent revealed set; re-reveals on load from `map_events_fired`.
2. **Lightable braziers** = `map_objects` vision sources ([FOW-7]); new MET `light` action added to
   MET's vocabulary.

v1 visible-set: `⋃(unit LoS discs) ∪ ⋃(lit-brazier discs) ∪ (event-revealed regions)`.

## 4. Edits
- `fog_of_war_los_open_questions_2026-06-21.md` — all of [FOW-1..7] resolved; new §2a (added scope),
  §6 (forward reservations); revised §4 slice + §5 tests; added [FOW-7]; status → RESOLVED.
- `map_events_triggers_open_questions_2026-06-21.md` [MET-3] — FOW convergence note: `reveal_tiles`
  = the room-reveal tool; new `light` action; future `set_fog`/`set_weather` action.
- `GDD_10_Roadmap.md` §H — FOW marked RESOLVED in the resolved-note + Tier-1 index; AIP line notes
  the fog-scout upstream is now decided; new "Forward design items surfaced by FOW" bullet
  (MapData terrain/encounter decomposition, `ai_respects_fog`, MET `light`/`set_fog`).
- DoD#1: planning only (no behavior change). DoD#2: no new mechanical rule ratified (the fog
  vocabulary checks land when code lands).

## 5. Verification
check_docs.py **17/17 PASS**; docs-only; no `__pycache__`.

## Session status: CLOSED.
**Five registers resolved** across 2026-06-21f–j: **PKGA · DCH · DTR · MET · FOW.** FOW's only §2
ask is `discovered_units`; its reveal tools reuse MET/DCH. Build order unchanged: **Package A Step 1
first**, then §2 spine, then the map cluster (DCH → STW/DTR → MET → FOW) once §2's save slice exists.
**Next planning candidate: AIP `[AIP-1..5]`** — its fog-scout upstream ([AIP-2]) is now decided, and
it absorbs chest-looter ([DCH-4]) + siege-operator ([STW-6]) too. Owner inputs still pending:
[REN-1]/[LEG-1] before the release-gate push.
