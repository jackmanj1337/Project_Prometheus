# Session Notes — Master Index

One line per session note, newest first. **Add a row here whenever you create a
session note** (mirrors how `MEMORY.md` indexes memories). Notes are Historical
records — keep them; this index is the navigation layer.

- [2026-07-08](2026-07-08.md) — **v0.3.0 full-scan Pass 1 (save/persistence codec) DONE on `v0.3.0-features` (`5faecc4`).** Read `SaveData`/`SaveCodec`/`SaveManager` at head `b7bcfd2` + their 3 suites; wrote `v030_full_scan/01_save_persistence.md` — **7 findings, all Low, no correctness bugs.** Highlights: `has_continue_save()` dead if/else + needless index disk read on the MainMenu path (L1); dead `_vector_array_from_variant` (L2); non-atomic single-slot `save_suspend` write (L6). Carried High (`RngService.start_map()`) is Pass 2/5 scope, not here. Tracker Pass 1 → DONE; check_docs + RNG guard green (docs-only). **Next: Pass 2 — determinism (state+RNG+combat, 7 files); re-confirm the `start_map()` lifecycle in-context.**
- [2026-07-07p](2026-07-07p.md) — **v0.3.0 release-delta review completed on `v0.3.0-features` (target snapshot `b7bcfd2`, base `ab81a21`) — score 6/10.** Wrote `code_review_v0.3.0_release_delta_2026-07-07.md`; verification green (`check_docs.py`, active RNG guard via `bash`, full 66-suite run, scene integrity, analyzer tests). Carried release blockers: fresh maps never call `RngService.start_map()` before Retry snapshot/first RNG event; Settings Input Mode does not refresh prompts immediately; rebind UI omits `more_info`, `peek_range`, and zoom actions; v0.3.0 metadata/build manifest not cut. **Next: fix RNG lifecycle first, then input-mode refresh and rebind coverage before cutting v0.3.0.**
- [2026-07-07o](2026-07-07o.md) — **v0.3.0 release-delta prep finalized on `v0.3.0-features` (`3737a2a`).** Marked `playtest_checklist_v0.3.0.md` as an explicit draft instead of leaving it ambiguous, and added `AGENT/Code Reviews/code_review_v0.3.0_release_delta_prep_2026-07-07.md` with the next-session boundary (`ab81a21` -> planned v0.3.0 build/pre-build head), coverage-map commands, prior review coverage, known gaps, must-recheck findings, verification commands, and output shape. Docs index generator produced no tracked changes; hook checks green. **Next: run the document-only release-delta review, then land fixes in focused commits.**
- [2026-07-07n](2026-07-07n.md) — **v0.3.0 release-delta review handoff written for next session.** Next pass should review the full boundary from the v0.2.8 executable source commit `ab81a21` to the planned v0.3.0 build snapshot, using the July 5 and July 7 code reviews as prior coverage rather than redoing them blindly. Plan: set exact base/head, build a coverage matrix across code/tests/scenes/resources/project settings/export metadata/docs, re-check weak or uncovered areas (MRD/threat overlay, v0.2.9 display fixes, July 7 findings), run docs/RNG/full-suite-or-focused verification, then write `AGENT/Code Reviews/code_review_v0.3.0_release_delta_2026-07-XX.md`. **Keep it document-only; fixes land afterward as separate focused commits.**
- [2026-07-07m](2026-07-07m.md) — **v0.3.0 Pillar 1 code review completed on `v0.3.0-features` (`a0c1efc`) — score 7/10.** Reviewed non-test `scripts/**/*.gd` since `914dd025` against the code-pillar procedure; wrote `AGENT/Code Reviews/code_review_2026-07-07.md`. High finding: fresh maps never call `RngService.start_map()` in production, so RNG seed/history lifecycle can start wrong or inherit prior-map history. Medium findings: Settings Input Mode does not refresh prompts immediately; rebind UI omits shipped gameplay actions (`more_info`, `peek_range`, zoom actions); closed author-facing dispatch lists remain carried-forward registry debt. Confirmed the July 5 spawn-seam findings are fixed. **Next: fix RNG lifecycle first, then input-mode refresh, then rebind coverage, before expanding the v0.3.0 playtest checklist and cutting the build.**
- [2026-07-07l](2026-07-07l.md) — **`B6-INPUT` selector adoption + prompt swapping FINISHED on `v0.3.0-features` (`d0801ac`, `2b7e3fc`, `5a7f2fe`) — headless code path COMPLETE.** (1) AttackPreview and (2) the HUD terrain pager now route More Info navigation through the shared `SelectionCursor` (forecast = 1-D cycle; terrain = -1 Hidden inactive stop), so all three More-Info surfaces share one core (the single gamepad d-pad attach point); both pure refactors with mirror fields so readers/tests are untouched. (3) Prompt swapping with the requested gamepad-brand research: SDL normalizes button POSITION (`JOY_BUTTON_A` = physical bottom on every pad) so bindings are brand-correct with zero per-brand code — only LABELS differ (Nintendo swaps A/B & X/Y positions, PlayStation prints ✕○□△), and Godot has no native controller-type API so brand is heuristic `Input.get_joy_name()` matching (wrong guess = cosmetic, never mis-input). `InputDisplay` gained `detect_brand`/`joypad_button_label`/`action_prompt`/`more_info_hint`; the level-up "Press X to continue" + forecast/sheet/terrain More Info hints render the active mode's key or brand-correct pad label and re-render live on `input_mode_changed` (`ModalScreen._refresh_input_prompts` virtual). New `test_input_display` 7/0; full 66-suite + check_docs green each commit; DoD#1 docs in GDD_07/10 + control plane + design-doc note. **Next: expand `playtest_checklist_v0.3.0.md` (input-mode selector + mode-switch focus/prompts), then cut the v0.3.0 build (folds §1.6 display fix + gamepad + input-mode into one tester pass). Remaining B6-INPUT (Rebuild C arbiter, touch-modal Map Menu, page-control review) is non-blocking. Live gamepad/display validation still external-blocked.**
- [2026-07-07k](2026-07-07k.md) — **`B6-INPUT` focus seam CONTINUED on `v0.3.0-features` (`c978eb2`, `50fe576`).** Landed the two pieces the 2026-07-07j note flagged next. (1) Settings gray-state input-mode selector: new `Input Mode` row + `_ENUM_SETTINGS` `"availability"` marker; `_apply_mode_availability` reads `InputModeManager.available_modes()` and DISABLES (not hides) unsupported modes (e.g. `touch` on desktop), resolver still falls back so stale saved modes are safe. (2) Focus-grab subscribers on the shared `ModalScreen` base: switch-to-gamepad grabs `_focus_default()` (base = first focusable; `SettingsScreen` → Back; `UnitDetailsScreen` seeds its `SelectionCursor`), switch-to-touch drops the stale highlight, switch-to-`mouse_keyboard` left alone (keyboard nav keeps highlight); hidden modals ignore it. DoD#1 docs updated across GDD_01/07/10, Feature Index, Control Plane. Hooks green: check_docs, RNG guard, full Godot suite; `test_settings_screen` 24/0, `test_unit_details_screen` 29/0. **Next: broader selector/arbiter adoption (AttackPreview / HUD terrain More Info) + prompt swapping; live controller/display validation still external-blocked.**
- [2026-07-07j](2026-07-07j.md) — **`B6-INPUT` focus seam STARTED on `v0.3.0-features` (`fe9f28c`, `8be6537`, `267a51c`, `43ea40f`).** Added pure `SelectionCursor` + tests; wired `UnitDetailsScreen` through it and closed `V026-02e` by adding a terminal Back focus zone (`confirm` closes); persisted `[controls].input_mode`, `[controls].touch_controls`, and relocated `[controls].mouse_cursor` with legacy gameplay cursor reads; added DOC-011 guards for the new vocabularies; added `InputModeManager` autoload after `SettingsManager` with event classification, detect-floor resolver, and `input_mode_changed` de-dupe tests. Hooks green: check_docs, RNG guard, full 65-suite Godot run. **Next: focus-grab subscribers + Settings gray-state selector, then broader selector/arbiter adoption; live controller/display validation still external-blocked.**
- [2026-07-07i](2026-07-07i.md) — **B6-INPUT key-rebind pass IMPLEMENTED on `v0.3.0-features` (`7efcbf1`, `afb9307`, `4a2f648`).** `SettingsManager` now persists profile-ready `[controls].profiles` plain-token slots (`kbd`/`pad`) with old `Object(InputEvent...)` migration, invalid-token default fallback, slot-preserving `rebind_action`, and batch `apply_keybindings`; `SettingsScreen` now stages keyboard/mouse and gamepad captures, marks same-slot conflicts red, blocks Apply until Clear/Revert/recapture resolves them, keeps Reset Controls available, and leaves debug rows read-only. Docs updated in GDD_01/07/10, Feature Index, and Project Control Plane. Hooks green: check_docs, RNG guard, full 63-suite Godot run. **Next: separate `B6-INPUT` input-mode/focus seam + shared selector (V026-02e Back-button gap / `VAL-V021-12`); live controller/display validation still external-blocked.**
- [2026-07-07h](2026-07-07h.md) — **B6-INPUT key-rebind design walk on `v0.3.0-features` (`ffcb777`, docs-only) — conflict model + profiles decided; rebind plan now build-ready.** Cleared a stale flag (ICD-4/5/6 were already RESOLVED 2026-06-21, no walk owed). Three owner calls: (1) human-readable cfg confirmed as slice-1 foundation; (2) **ICD-6 AMENDED** — live two-way *swap* → **staged batch apply-block** (pending buffer, same-slot conflicts turn both rows red + disable Apply, per-row Clear, Revert; stronger self-trap safety since staged edits never go live mid-edit); (3) **ICD-8 ADDED** — named control-scheme profiles belong in **v1 not v0.3.0**, so slice 1 ships the profile-ready *shape* (implicit `"Default"` nesting) making the later profile UI additive with no re-migration. Updated the ICD register + rebind plan (§1a/2/5/6/7/9); regenerated INDEX/REGISTERS; check_docs green. **Next: build slice 1 (persistence foundation — per-device-slot dict + human-readable tokens + `"Default"` profile nesting + old-form migration + `apply_keybindings`, headless, `SettingsManager` only). Settle the token↔InputEvent map first. Then slice 2 (editable UI), slice 3 (pad capture). Build cut / `VAL-V030-GAMEPAD` / `VAL-V023-DISPLAY` still blocked on external hardware/tester.**
- [2026-07-07g](2026-07-07g.md) — **ObjectDB leak audit CLOSED on `v0.3.0-features` (`7697cfe`, `[ODB-1]` RESOLVED) — the recurring `ObjectDB instances leaked at exit` warning is benign test-teardown, not a production leak.** Verbose per-suite scan: 51/62 clean; the 11 noisy suites' counts scale with unfreed test fixtures (`test_combat` 53) not production instances (`GameMap.tscn` ×5 → 1 orphan); production has zero `remove_child()`; every transient dialog `queue_free()`s on confirm AND cancel. Added `scripts/tests/test_node_lifecycle_leak.gd` (63rd suite, 3/0, itself leak-clean) — an orphan-count guard so the exit noise can't mask a real regression — plus the audit register and a `CLEAN-OBJDB-LEAK` Implemented row. Full 63-suite + check_docs green. **Next (LOCKED GOAL, owner 2026-07-07): two coupled `B6-INPUT` pieces — (1) key-rebind capture UI (primitive `SettingsManager.rebind_action` already done+tested; only the `SettingsScreen` capture flow/conflict/reset UI is missing; resolve `[ICD-4/5/6]` first) and (2) input-mode/focus seam + shared selector (carries the V026-02e Back-button gap). Build cut + `VAL-V030-GAMEPAD` still blocked on external hardware/screen.**
- [2026-07-07f](2026-07-07f.md) — **v0.2.9 section 1.6-only rerun build CUT on `v0.3.0-features` (`3dddefc` source, `1d06c56` manifest).** Bumped metadata to v0.2.9 (export preset / MainMenu label / environment_setup), wrote `playtest_checklist_v0.2.9.md` (Part I = §1.6 only: `client W×H` readout + size vocabulary for V028-02, Windows-maximize-stays-centered + un-maximize-restores for V028-03) and `playtest_build_v0.2.9.md`. Exe `builds/Project_Prometheus_v0.2.9_debug.exe` (101,375,272 B, SHA-256 `fb064f2a…816cf3f8`, source `3dddefc`, build_info packed). `test_release_metadata` 5/0, check_docs green. Ships as two files. **Next: ship to tester; flip `VAL-V023-DISPLAY` only when §1.6 passes live (esp. Windows maximize @ 2.0× + non-preset drag).**
- [2026-07-07e](2026-07-07e.md) — **v0.2.8 section 1.6 fix pass IMPLEMENTED on `v0.3.0-features` (`65dde85`) — root-cause menu re-center + maximize/size-readout policy.** V028-03/Q2: `MenuScale._recenter` installs a reactive `resized` re-center (re-entrancy guarded) so centering re-runs at the exact frame the engine settles the panel size, retiring the per-trigger deferred bakes; `SettingsManager.resize_write_back_action` makes maximize a window STATE (never persisted) that restores the saved windowed size on un-maximize. V028-02/Q1: `windowed_size_status()` separates preset request from custom client size; `SettingsScreen` shows `client WxH` with no re-clamp. Kept the viewport `size_changed` hook for fit-clamp + write-back (NOT fully retired — it also does the V021-08 shrink fit-clamp; flagged). Tests: `test_settings_manager` + `test_menu_scale`; full 62-suite green. DoD#1 docs: GDD_01/07 + display guide vocab, GDD_10 + control plane + triage plan. **Next: cut a section-1.6-only rerun build + live Windows maximize check; flip `VAL-V023-DISPLAY` only when it passes live.**
- [2026-07-07d](2026-07-07d.md) — **v0.2.8 triage owner decisions Q1-Q3 recorded on `v0.3.0-features` — section 1.6 fix pass scoped.** Q1=A structured display-size status API; Q2=root-cause fix of the recurring menu-recenter bug (imperative one-shot offset-bake in `MenuScale._recenter()` goes stale on the deferred layout pass — patched per-trigger at V025-05a/V026-01a/V027-04a, now fixed at cause via a reactive `resized` re-center) + Option A no-persist-maximize write-back; Q3=A route aspect to `UI-VIEWPORT-ASPECT` (carries the deferred CenterContainer refactor). Docs-only. **Next: clear context, implement the section 1.6 fix pass, cut a focused rerun.**
- [2026-07-07c](2026-07-07c.md) — **v0.2.8 playtest RETURNED and triaged on `v0.3.0-features` (`45ee8da`, `b8ec8e9`) — display gate narrowed to section 1.6 only.** Archived the returned checklist/logs/screenshots into permanent docs/evidence paths, wrote the v0.2.8 triage plan + owner review Q1-Q3, updated `VAL-V023-DISPLAY`, added `UI-VIEWPORT-ASPECT`, and clarified the custom-size readout known issue. Verification: docs/RNG guards green; Godot skipped as docs/evidence only. **Next: owner walkthrough Q1-Q3, then the section 1.6 fix pass and focused rerun.**
- [2026-07-07b](2026-07-07b.md) — **`B6-INPUT` Slice 2 headless path landed on `v0.3.0-features` (`33dbf72`) — controller map-cursor decoding, left-stick repeat, and held trigger zoom are implemented under tests.** `MapCursorInput.decode(event)` now accepts d-pad/pad buttons; left-stick movement polls `Input.get_vector()` into the existing repeat timer; LT/RT zoom polls action strength with scaled repeat; keyboard zoom echo is suppressed; v0.3.0 live controller checks moved into `playtest_checklist_v0.3.0.md` and `VAL-V030-GAMEPAD`. Verification: docs/RNG guards green and full 62-suite Godot pre-hook green. **Next: live controller validation or the input-mode/focus seam.**
- [2026-07-07](2026-07-07.md) — **`B6-INPUT` Slice 1 landed on `v0.3.0-features` (`54e96f6`) — default gamepad bindings and menu-control parity are in place.** Added Pad A/B/X/Y, LB/RB, View/Back, Start, L3/R3, D-pad, left-stick, LT/RT bindings; preserved pad defaults through keyboard-only saved rebind data; expanded input-label rendering in Settings; added `test_input_bindings.gd`; and locked the binding contract with `check_docs.py` check 24. Verification: docs/RNG guards green and full 62-suite Godot pre-hook green. **Next: controller map-cursor pass with device-agnostic cursor decoding, left-stick repeat, and held-trigger zoom.**
- [2026-07-06g](2026-07-06g.md) — **`B1-SUSPEND` disk lifecycle landed on `v0.3.0-features` (`58eee64`) — active-map suspend is now writeable, resumable, and cleaned up on map result.** Fixed stale path docs to `user://saves/suspend.json`; added `SaveManager` disk I/O + `saves_index.json`; wired Map Menu `Suspend & Quit`, Main Menu `Continue`, load-failure dialogs, and result-time delete; updated GDD/control-plane docs and added focused tests (`test_save_manager`, `test_main_menu`, `test_map_menu`, `test_map_cursor`, `test_game_over_sequencing`). Verification: docs/RNG guards green and full 61-suite Godot pre-hook green. **Next: `B6-INPUT` gamepad slice unless v0.2.8 tester return preempts.**
- [2026-07-06f](2026-07-06f.md) — **`B1-SUSPEND` Slice 1 landed on `v0.3.0-features` (`1c2af66`) — active-map suspend capture and scene restore foundation implemented.** `SaveData.map_runtime` now carries map identity, live units, turn/scheduler state, PairUp, RNG as JSON-safe decimal strings, and MRD cursor/watch state; `GameMap` resumes from runtime units instead of authored placements; `test_suspend_map_runtime.gd` covers enemy state, ALTERNATING scheduler state, PairUp, RNG, cursor, and watch-set restore. Verification: `check_docs.py` green, RNG guard green, `git diff --check` green, full 59-suite Godot run green in pre-hook. **Next: finish disk SaveManager / Map Menu Suspend & Quit / Main Menu Continue/delete lifecycle; then `B6-INPUT` unless v0.2.8 tester return preempts.**
- [2026-07-06e](2026-07-06e.md) — **`B1-CST` CampaignRules consolidation kickoff landed on `v0.3.0-features` (`625c00d`) — live rules now sit under `GameState.campaign_rules`.** Loose per-save rule fields were removed from `GameState`, runtime/UI/tests migrated to `CampaignRules`, `rewind_charges_per_map` added, `SaveData` rule defaults expanded, and required `.uid` sidecars tracked. Verification: loose-field grep clean, `check_docs.py` green, RNG guard green, full 58-suite Godot run green in pre-hook. **Next: `B1-SUSPEND`; v0.2.8 tester return still preempts; then `B6-INPUT` gamepad slice / ObjectDB audit.**
- [2026-07-06d](2026-07-06d.md) — **`B1-SAVECODEC` Slice 5 landed on `v0.3.0-features` (`edc1c5f`) — I/O-free `SaveData` envelope added.** New `scripts/save/SaveData.gd` writes the locked top-level sections, centralizes defaults, preserves unknown nested manifest-backed fields, handles old-save defaults/migrations (`integrity.whole/protected`, legacy party gold/roster, `permadeath_enabled` → `death_mode`), normalizes JSON-parsed RNG ints, and adds skeletal inventory reference validation. Added `test_save_data.gd` (4/0) and updated `GDD_01`, `GDD_10`, Feature Index, Project Control Plane, and F1 manifest. Verification: focused save/RNG suites green, `check_docs.py` green, RNG guard green, `git diff --check` green, full 58-suite `run_tests.sh` green in pre-hook. **Next: `B1-CST` CampaignRules consolidation kickoff; v0.2.8 tester return still preempts.**
- [2026-07-06c](2026-07-06c.md) — **`B1-SAVECODEC` Slice 4 COMPLETE on `v0.3.0-features` (`0edc82c`) — JSON-safe `SaveCodec` foundation landed.** New `scripts/save/SaveCodec.gd` serializes Vector2i as `{x,y}`, `InventoryEntry` as dictionaries (type/id/uses/forged_mods/equip modifiers), and Retry-facing `UnitData` snapshots through `unit_data_to_dict` / `apply_unit_dict`; `GameState` delegates unit snapshot/restore/validation to it while `take_map_snapshot` / `restore_map_snapshot` behavior stays stable. Added `DataManager.has_weapon()` for structured reference validation and `test_save_codec.gd` (6/0); updated snapshot/data-layer tests for JSON-safe internals and GDD/control-plane/manifest docs. Verification: focused save/snapshot suites green, `check_docs.py` green, RNG guard green, full 57-suite `run_tests.sh` green in pre-hook. **Next: `B1-SAVECODEC` Slice 5 `SaveData` envelope + old-save defaults; v0.2.8 tester return still preempts.**
- [2026-07-06b](2026-07-06b.md) — **B1-F1 COMPLETE on `v0.3.0-features` (`35b2a02`) — save-schema manifest lock landed.** New `f1_save_schema_manifest_2026-07-06.md` assigns field-family paths, owners, scopes, lifecycles, defaults/migration behavior, serializer owners, Retry/suspend behavior, fixtures, row status, and explicit no-save guards for the F1 save shape. Contract/template updated with row status; Band 1 plan placeholder refs resolved; control plane marks `B1-F1` Implemented; `GDD_10` queue now starts with `B1-SAVECODEC`; Feature Index points at the manifest. Added `check_docs.py` check 23 for manifest table/row-shape enforcement. Verification: docs index regen, `check_docs.py` green, `git diff --check` green, commit hook green (docs + RNG guard; Godot skipped as docs-only). **Next: `B1-SAVECODEC` Slice 4 codec foundation, then Slice 5 `SaveData` envelope; v0.2.8 tester return still preempts feature work.**
- [2026-07-06](2026-07-06.md) — **B1-PKGA COMPLETE (Steps 1-2) on `v0.3.0-features` — the campaign/save gate is open.** Five slice commits: `f42f570` RngService autoload (RNG-1 mixer/event API, 9/9); `ec77403` combat migration behind the CRR-2 resolver seam (`two_roll` RULE-001 default / `single_roll`, `CampaignRules.hit_formula`, event records + `get_action_start_tile`, T1/T3/T7 13/13, DoD#1 docs via subagent); `ef8fcc7` growth `levelup` events + activation rolls from `context["rng"]` with loud-fail; `cba54f9` non-dice commits (wait/seize/escape/item/staff/pair actions, player+AI) + raw-RNG lint T5 + equip neutrality T4; `9a55e21` Retry snapshot carries `{map_seed, history_hash}` (T2 5/5). 55 suites green per commit. Gotchas: autoloads attach AFTER test `_init` (late-added same-name stubs get renamed); null-`action_menu` fallback commits a wait. **v0.3.0 queue ratified with owner: 1) `B1-F1` manifest → 2) `B1-SAVECODEC` Slices 4-5 → 3) gamepad slice 1 → 4) ObjectDB leak audit → 5) `B1-SUSPEND` capstone (closes MRD Slice 6); rewind stays hooks-only ([CST-13]); build cut blocked on the v0.2.8 gate; checklist must carry the two-RN hit note.**
- [2026-07-05j](2026-07-05j.md) — **Release line merged INTO `v0.3.0-features` (`f999f91`); both branches pushed.** Owner-approved reverse merge: the V027 fix pass + v0.2.8 cut (`e471c93`) now underpin the MRD/gamepad work. 4 conflicts resolved (export preset `tools/**`+v0.2.8 union; `SettingsManager._ready` keeps `_apply_grid_dim` AND the `size_changed` hook; environment_setup; session-notes INDEX row union). 22/22 docs + 52/52 suites green (`test_map_cursor` 58/0 with release+feature cases coexisting). **One-way flow: features does NOT merge back until the v0.2.8 gate comes back good.** **Next: ship v0.2.8 to the tester (release line); gamepad slice 1 or B1-PKGA RngService (feature branch).**
- [2026-07-05i](2026-07-05i.md) — **v0.2.8 rerun build CUT (subagent) + v0.3.0 track surveyed.** Exe `builds/Project_Prometheus_v0.2.8_debug.exe` (101,301,264 B, SHA `850c5f87…`, source `ab81a21`, BUILD-STAMP-verified); checklist = §1.3/§1.4/§1.6 only with the CORRECTED §1.6 explainer (drag write-back + `Custom (WxH)` + no recentre + gray-out/`native WxH`); ships as two files. Mystery solved: session notes 05b-f + `test_mrd_scene.gd` live on the **`v0.3.0-features`** branch (origin copy was 11 commits behind) (19 commits: B4 spawn seam Slice 1, B6-MRD Slices 1-5 + overlay registry + check [22], gamepad R3, audit pass). Merge assessment: NOTHING merges into the release line pre-gate (MRD repaints the §1.3/§1.4 surfaces under test); the valuable merge is REVERSE (release → features, branch predates the V027 camera fixes) + push the branch. **Next: ship to tester; on owner OK do the reverse merge; meanwhile B1-PKGA RngService headless.**
- [2026-07-05h](2026-07-05h.md) — **V027 fix pass LANDED — all six walkthrough decisions built with regression tests (release line).** Q1 `5446882` far-edge action-menu anchor (V025-03 test rewritten for the new model); Q2 `dbf3568` forecast deferred re-pass + hold-transparent + generation guard; Q3 `9e3d566` **repro found the real root cause** — geometry sound on both walls, but `keep_cursor_in_view` was the one camera write without `_flush_scroll()` (same-frame placements read a stale transform, wall-agnostic) — fixed + coalesced one-frame re-anchor self-heal; Q4 `95d33d1` `size_changed` → deferred coalesced `_apply_menu_scale` (gotcha: same-value `content_scale_factor` writes emit `size_changed` → infinite requeue, now guarded); Q5 `2f75659` full write-back (`apply_resize_write_back` + `resolution_written_back` + "Custom (WxH)" dropdown state + guide recentre-sentence correction); Q6 `d31aabc` Resolution gray-out + native readout outside Windowed; `7eba052` GDD_01/07 + control plane + GDD_10 DoD#1 addendum. **Next: cut v0.2.8 (§1.3/§1.4/§1.6-only rerun) with the corrected §1.6 explainer.**
- [2026-07-05g](2026-07-05g.md) — **v0.2.7 playtest RETURNED — sorted, diagnosed (`V027-01..05`), owner walkthrough Q1-Q7 done (release line).** Gate does NOT close: §1.1/§1.5 PASS (V026-01d/e close), but §1.3 (menu drifts over unit at zoom >1 — the V025-03 cap measures one unzoomed tile from the tile's LEFT edge), §1.4 (first-show inflated-minimum dead space; left-wall misplacement UNPINNED, needs headless repro) and §1.6 (no `size_changed` hook anywhere → 4K-switch stretch; applied readout never tracked OS drags — handbook overpromised) FAILED. Decisions: Q1-Q4/Q6/Q7→A, **Q5→B full write-back** (drag updates saved resolution + "Custom W×H" dropdown state; NO drag-recentre). §1.1 notes → `UI-INSPECTION`. **Next: V027 fix pass with regression tests, then cut v0.2.8 (§1.3/§1.4/§1.6 rerun).**
- [2026-07-05f](2026-07-05f.md) — **Headless MRD coverage + simulated gamepad R3 (the 2026-07-05e next items).** `b4acb01` widens `MapCursor._input` so pad-bound `show_danger_zone` (R3) / `peek_range` joypad events route through the danger-zone resolver — the key/mouse type gates silently dropped ALL `InputEventJoypadButton`s; real pad bindings still trail gamepad slice 1 — plus simulated-pad tests (`test_map_cursor` 58/0). `6e99e95` adds `test_mrd_scene.gd` (11/0, 52nd suite): durable headless replication of the 2026-07-05b throwaway live-verify on the shipped `GameMap.tscn` (watch/combined/marker/lock-unlock/peek/arrows/grid-dim + an engine-dispatched R3 press via `Input.parse_input_event`). **Next: v0.2.7 triage when it returns, else gamepad slice 1 or Band 5/6; pixel polish waits for a display.**
- [2026-07-05e](2026-07-05e.md) — **Fixed the actionable 2026-07-05 full-audit findings.** `34384ae` aligns the enemy placement spawn seam end-to-end (runtime exact-one `unit_data_path`/`unit_data`, inline `ai_profile` preservation with explicit placement override, stronger `test_spawn_seam`, GDD/control-plane docs). `03cc8cf` fixes the export `tools/**` exclusion + active docs, removes the stale Map 950 hardcoded count, updates the manual skill-cap fixture note, and adds the soft full-audit closeout reminder. Full 51-suite hooks green on both commits. **Remaining by design: registry migrations and gdtoolkit lint/format gate. Next: headless MRD automated coverage + simulated gamepad R3 checks; display/controller polish later.**
- [2026-07-05d](2026-07-05d.md) — **Ran the second full five-pillar project audit** on `v0.3.0-features` @ `914dd02`. Baseline green: docs 22/22, RNG, analyzer 12/12, scene integrity, full 51-suite Godot tests. Reports: Code 7, Docs 7, Data/Assets 9, Tests/CI/Build 7, Process 7 → **overall 7/10**. Headline cross-pillar issue: inline spawn-seam contract drift (runtime ambiguous-source behavior + `ai_profile` overwrite + stale `test_spawn_seam` + missing GDD_01/GDD_06 update). **Next: fix spawn seam end-to-end, then Map 950/playbook/export cleanup.**
- [2026-07-05c](2026-07-05c.md) — **Code-review fix pass after the v0.3.0 review.** Fixed MapCursor hover-peek handoff + retained threat repaint regressions (`0996891`), then aligned DataManager enemy-placement validation with inline `unit_data` spawn-seam docs (`355d2a8`). Focused MapCursor/DataManager suites passed; both commits' hooks ran docs/RNG/full 51-suite tests green. **Next: no review fixes remain; resume MRD pixel polish/live verification and gamepad R3 when ready.**
- [2026-07-05b](2026-07-05b.md) — **Built both buildable-now candidates while v0.2.7 is out: `B4-ENCOUNTER-MODEL` Slice 1 + `B6-MRD` Slices 1-5.** B4 Slice 1 (`5b5d322`): `_spawn_units` accepts an in-memory `unit_data` via one `_resolve_placement_unit_data` seam (`[PUG-3]`; `test_spawn_seam`). B6-MRD: Slice 1 (`e043411`) extract `get_unit_threat_tiles` (`[TUR-1]`); Slice 2 (`3997e10`) watch-set/`_danger_mode` resolver + `[MRD-1]` overlay precedence registry + `OVERLAY_DARKER_RED` + "D" markers + **check_docs [22]** danger-mode vocab guard + gamepad §4 R3 edge SATISFIED; Slice 3 (`a506fc3`) hover-to-peek `peek_range`/E; Slices 4-5 (`682aa16`) path arrows + `grid_dim` Terrain Dim slider. All no-save view state. Then authored the source-4 darker-red watch tile (`2a2e48b`) + upgraded the paint test to real-overlay output. **Owner decision: NO new tester build** (v0.2.7 stays the focused display-validation rerun); this work is the **base of a later v0.3.0 build**, split onto a new branch **`v0.3.0-features`** (local-only commits moved there; `docs-reorg-2026-06-23` realigned back to `dd23656`). **Live-verified all 7 features headless on the real `GameMap.tscn`** (no display here, so pixel appearance unverified). **Only Slice 6 (watch-set save) remains, gated on `B1-SUSPEND`.** **Next (on `v0.3.0-features`): visual polish/live-verify on a display, gamepad §4 R3; (on `docs-reorg`): v0.2.7 live rerun when the tester returns.**
- [2026-07-05](2026-07-05.md) — **Walked v0.2.6 review Q1-Q7, cut the v0.2.7 rerun build, then built `[B5-VICTORY-PROGRESSION-SEQ]` (Q6).** Decisions: Q1/Q3/Q4/Q5/Q6/Q7 A, **Q2 C** (Back-button→`B6-INPUT`); routed to control plane (`1e5130e`). v0.2.7 (`26af9f4`): dropped the `._sc_` marker for `%APPDATA%` logs (Q3), raised promotion/reclass pickers to 840×760 (Q4), new `playtest_checklist_v0.2.7.md` + `_build_` manifest (Q1/Q7), version bump; exe SHA `5c02a0c8…`, ships as two files. Q6 (`9ff2e2f`): `GameOverScreen` defers the victory overlay until the level-up/promotion queue drains (`test_game_over_sequencing.gd`, row → Implemented). Subagents: Explore + background checklist author. **Next: build `B4-ENCOUNTER-MODEL` Slice 1 + `B6-MRD` Slices 1-5; v0.2.7 live rerun when the tester returns.**
- [2026-07-04h](2026-07-04h.md) — **v0.2.6 playtest RETURNED — triaged (`V026-01..08`), root-caused, and fixed the mechanical items same-session.** Gate progress: §1.2/§1.5/§1.6/§1.7 + Part II + regression PASS live; the display gate shrinks to §1.1/§1.3/§1.4. Two real root causes: §1.3+§1.4 high-zoom misplacement = ONE stale-canvas-transform bug (Camera2D defers its scroll update; fix = `force_update_scroll()` flush in CameraController — regression test fails pre-fix) and §1.1 first-apply off-center = `MenuScale._recenter` centering scroll panels at their stale size before the deferred layout grew them (fix = size from authored base + content min first). Also fixed: promotion/reclass `follow_focus` (keyboard focus moved invisibly out of the scroll frame — read as "only the top class reachable"), Settings scrollbar right margin, missing hotseat debug keybind row, no-counter Hit/Crit dash rows. `godot.log` RETURNED for the first time — via `%APPDATA%`; the `._sc_` marker is editor-only and did nothing in the export (the returned build stamp proves it). Review doc Q1-Q7 OPEN (`playtest_v0.2.6_triage_review_2026-07-04.md`): Back-button keyboard access, portable-log strategy, picker sizing, promote re-trigger action, victory-screen-waits-for-progression sequencing, handbook display explainer. **Next: owner walkthrough Q1-Q7 → cut the v0.2.7 rerun of §1.1/§1.3/§1.4.**
- [2026-07-04g](2026-07-04g.md) — **Promoted the resolved `[CNC]`/`[PUG]` design into a real implementation plan + wired it to the control plane.** Wrote `skirmish_encounter_generation_implementation_plan_2026-07-04.md` (per-slice shape, track IDs `B4-ENCOUNTER-MODEL` + `B7-SKIRMISH`): 5 slices — (1) spawn-seam generalization `[PUG-3]` (buildable now), (2) `BattleMapDef`/`BattleEncounterDef` split `[CNC-4]` (+`enemy_start_tiles`, legacy `map_id` adapter), (3) `ForceSpec`+scaling registry `[PUG-4/7]`, (4) generated encounter modes + skirmish PHB panel `[PUG-5/6]` via the shared `[CNC-8/9]` launch primitive, (5) editor generate-and-bake `[PUG-8]`. Generator CORE stays the arena plan's Slice 2 (reused, not forked). Added two Planned control-plane rows pointing at the plan; cross-linked the PUG §4 sketch + CNC-4 implication back to it. Grounded against live `GameMap._spawn_units:168` + `MapData.gd`. 21/21 docs green (control-plane schema/prefix/reachability incl.). **Next: encounter/generation thread fully planned — implementation gated behind `B1-PKGA`+`B1-CST`+`B7-ARENA` Slice 2+`B3-PHB`, Slice 1 buildable now; v0.2.6 live display rerun still outstanding.**
- [2026-07-04f](2026-07-04f.md) — **Walked the `[PUG]` parametric-unit-generation register to completion — resolved `[PUG-4..8]`, closing it (immediately after the CNC pass that unblocked it).** Keystone reconciliation: PUG's sketched `EncounterDef {ForceSpec, map_source, reward}` **collapses into CNC-4's `BattleEncounterDef`** — a skirmish is a `BattleEncounterDef` whose force (`force_spec` vs authored `enemy_placements`) and map (`map_pool` vs fixed `battle_map_id`) are generated/pooled. `[PUG-4]`=A first-class `ForceSpec {entries:[{UnitSpec,count}], scaling}`; `[PUG-5]` unify into BattleEncounterDef; `[PUG-6]`=A skirmish PHB panel + **added `enemy_start_tiles` to CNC-4's BattleMapDef** (spawn zones so a generated force fits any pooled map); `[PUG-7]` open registry of scaling terms over [DIF]/[TCV], default player-avg+diff offset; `[PUG-8]`=A editor freezes the same `generate_unit` roll. §4 slice sketch now resolution-backed. 21/21 docs green; REGISTERS lists PUG RESOLVED. **Next: promote the generator/skirmish/encounter design into a real build plan (gated behind RngService B1-PKGA); v0.2.6 live display rerun still outstanding.**
- [2026-07-04e](2026-07-04e.md) — **Walked the `[CNC]` campaign-node register to completion — resolved `[CNC-4..10]`, closing it and unblocking `[PUG-4..7]`.** Owner chose the **canonical split** (`[CNC-4]`=A): monolithic `MapData` → `BattleMapDef` (terrain: grid/tilemap/start-tiles/camera) + `BattleEncounterDef` (payload: placements/factions/turn-order/objectives/rewards), node points `encounter_id → battle_map_id`, legacy `map_id` = migration adapter (runtime compose-or-refactor deferred to the campaign band). Rest: `[CNC-5]` chapter = story metadata over nodes (node stays the primitive); `[CNC-6]` composition **validation matrix** in the loader (`battle` needs exactly one target, `hub` forbids one); `[CNC-7]` pre-1.0 hard save-incompat (resync deferred); `[CNC-8]` node completion = action/effect primitive dispatched on `node_type` as DATA (keeps `[CNC-3]` open, no `match`); `[CNC-9]`=C shared launch primitive, node OR PHB `skirmish` panel; `[CNC-10]` JSON source-of-truth, GUI writes it. Updated `[PUG]` status: `[PUG-4..7]` no longer CNC-blocked. 21/21 docs green; `REGISTERS.md` lists CNC RESOLVED. **Next: walk `[PUG-4..7]` (ForceSpec/EncounterDef/skirmish/scaling); the v0.2.6 live Windows display rerun still outstanding.**
- [2026-07-04d](2026-07-04d.md) — **Cut the v0.2.6 playtester build + session closeout.** Bumped version 0.2.5→0.2.6 (export preset / MainMenu label / environment_setup), wrote `playtest_checklist_v0.2.6.md` (Part I retests the V025 fixes; §3.2 log request now points next to the exe) and the `playtest_build_v0.2.6.md` manifest; ran `prepare_build.sh` + `--export-debug` → `builds/Project_Prometheus_v0.2.6_debug.exe` (commit `75b3379`, SHA-256 `90a673e1…`, `build_info.json` confirmed packed). Ships as three files (exe + `._sc_` + handbook). Tracked the export's `.uid`/`.import` sidecars. Then reopened `[CNC]` and **resolved `[CNC-3]`**: `node_type = battle|hub` for v1 but read as DATA (open-registry dispatch, not a closed enum) so a 3rd type is additive later. **Next: live Windows display rerun (§1.5/§1.7 input-routing + §3.2 self-contained log need a real desktop pass); then resume the `[CNC]` walk at `[CNC-4]` (battle-map/encounter split).**
- [2026-07-04c](2026-07-04c.md) — **Made the engine log land next to the build exe (self-contained mode) and self-identify via a startup BUILD STAMP.** Shipped builds get a `._sc_` marker (redirects `user://` incl. `logs/godot.log` next to the exe); `Boot.gd` + new `scripts/shared/BuildInfo.gd` print a framed stamp as the first log lines — version, git commit, built_at, a fresh per-launch `started_at` (proves the log wrote this run), and resolved `exe`/`user_data_dir`/`log` paths. `scripts/tools/prepare_build.sh` (run before export) bakes commit+version+timestamp into gitignored `res://build_info.json` + drops the marker; editor falls back to live git (`-dev`). Docs updated (`environment_setup.md`, `display_and_settings_guide.md`); `test_build_info.gd` added. **Next: cut v0.2.6 via prepare_build.sh + export, ship `._sc_` in the zip, confirm the log lands next to the exe on Windows.**
- [2026-07-04b](2026-07-04b.md) — **Walked the triage review Q1-Q14 with the owner (all recommendations + Q2 panel-widen & Q14 grind-units adds), then implemented the full v0.2.6 fix pass.** Six workstream commits (`45e923e` E1-E3+G level-up/promotion/terrain routing, `e975f16` A settings slider apply-on-release + h-scroll + widen, `09415bc` B1-B2+Q3 sheet wrap/Back/More-Info, `c049c99` D3+C preview re-anchor + menu offset cap/stickiness, `38f5639` E5 5th skill + weapons + 10 grind units, `8fa3109` F/Q11/Q13 display guide + applied-size readout + test-fidelity rule + Q5/Q6→Band 5). Every input-routing fix got a structural-invariant test (Q13). Full suite + 21 doc checks green. **Next: cut the v0.2.6 build + checklist (prominent `godot.log` ask + live-confirm lines), then the display rerun; `[CNC-3]` after.**
- [2026-07-04](2026-07-04.md) — **v0.2.5 playtest return triaged into `V025-01..10` with code-grounded diagnoses; artifacts moved to permanent homes; owner-review doc written for next session.** Big wins: §1.4 AttackPreview + §1.7 + all Part II PASS. Fails diagnosed: level-up narrow panel (first-show autowrap min-size race, now reproduced), left-click no-dismiss (GUI-phase STOP-root click consumption; headless tests bypass real routing), promotion 2.0x cutoff (`open_for()` rebuilds after the one-time scale/clamp), terrain click paging (RichTextLabels' default STOP eats clicks — v0.2.3 repeat). Wrote `playtest_v0.2.5_results_triage_plan_2026-07-04.md` + `Code Reviews/playtest_v0.2.5_triage_review_2026-07-04.md` (Q1-Q14, options/drawbacks/recommendations). Skill cap = 5 but `M950_Hero_SkillCap` has 4 skills; `godot.log` missing a 3rd time. **Next: walk Q1-Q14 with the owner, then the v0.2.6 fix pass; `[CNC-3]` after.**
- [2026-07-03j](2026-07-03j.md) — **Session closeout: `[PUG]` generator core + `[CNC]` campaign-node composition pinned; raw v0.2.5 playtest return artifacts indexed for next session.** This session resolved the generator core (`6fdc342`), added `[CNC-1..10]` with stable node identity (`f9190cc`), and resolved explicit node reference slots + panel instance refs (`b9604c6`). Remaining `[CNC]` walk is paused at `[CNC-3]` (`battle|hub` lean). **Next: start with v0.2.5 playtest results; resume `[CNC-3]` after that if still relevant.**
- [2026-07-03i](2026-07-03i.md) — **Resolved `[CNC-2]`: campaign nodes use explicit reference slots and `prep_panels` entries can point to specific panel instances.** Node slots now include `chapter_id`, `prep_panels`, `encounter_id`, plus temporary legacy `map_id` while `MapData` remains monolithic. `prep_panels` is an array of panel refs (`panel_type` + optional `panel_instance_id`), so a node can expose a specific shop stock list, arena roster, training-hall variant, or skirmish table. 21/21 docs checks green. **Next: `[CNC-3]` node type vocabulary.**
- [2026-07-03h](2026-07-03h.md) — **Added the `[CNC-1..10]` campaign-node composition register to settle map/chapter/encounter vocabulary before skirmish format.** Captured owner constraint `[CNC-1]`: `CampaignNode.node_id` is durable save/progression identity while the node's type and refs are mutable (`prep_panels`, chapter/story metadata, encounter pointer, map pointer). Updated the campaign/save technical plan with the stable-node/mutable-reference rule and pointed `[PUG-4..7]` at `[CNC]` as the blocker for `ForceSpec`/`EncounterDef`/skirmish schema. Open questions now cover node reference slots, node-type vocabulary, Battle Map vs Battle Encounter split, chapter semantics, valid composition matrix, save/resync on changed refs, launch/completion semantics, skirmish attachment, and JSON-vs-GUI authoring. 21/21 docs checks green. **Next: walk `[CNC-2..10]`, then return to `[PUG-4..7]`.**
- [2026-07-03g](2026-07-03g.md) — **Partial `[PUG]` resolution: generator core settled, skirmish format deferred pending campaign-map structure.** Resolved `[PUG-1]` (`UnitSpec` stat-generation modes = explicit stat ranges + class-growth rolled + fixed growths), `[PUG-2]` explicit `generate_unit(spec, seed)` from `RngService`, `[PUG-3]` one spawn seam accepting path or in-memory `UnitData`, `[PUG-9]` equipment fixed or weighted table, and `[PUG-10]` one reusable generator for any fresh-unit consumer. Left `[PUG-4..7]` OPEN/deferred until a campaign-map/skirmish-structure pass; left `[PUG-8]` OPEN for editor bake/provenance details. **Next: walk campaign-map structure, then close remaining `[PUG]` and draft the implementation plan.**
- [2026-07-03f](2026-07-03f.md) — **Design walk: the parametric unit generator across arena / skirmish / GUI editor → captured as the `[PUG-1..10]` register (OPEN).** Corrected the framing (ratified scope already has skirmish as a prep-option panel with auto/random-leveled rosters, **no free-roam overworld**). Owner decisions: skirmish = **"panel now, overworld-ready shape later"** (a shared `EncounterDef` fired by a trigger surface — panel now, overworld tile later, additive not a rewrite); capture = an open-questions register. Wrote `parametric_unit_generation_open_questions_2026-07-03.md`: one atom `generate_unit(spec, seed)` (`UnitSpec` = class·level/range·stat-ranges·equipment, deterministic via `RngService`); three surfaces differ on two axes — **one unit vs a `ForceSpec` roster** and **ephemeral runtime vs baked authoring**. Key questions: `[PUG-3]` the **spawn seam** (generalize `enemy_placements`/`_spawn_units` to accept an in-memory `UnitData`, not only a path — load-bearing, unblocks every consumer), `[PUG-1]` roll mode (explicit ranges vs class-growth-rolled → both), `[PUG-4]` `ForceSpec`, `[PUG-5]` `EncounterDef` (scope decided/shape open), `[PUG-6]` skirmish = `[PHB]` panel + map pool, `[PUG-7]` scaling via `[DIF]`/`[TCV]`, `[PUG-8]` editor generate-and-bake (same primitive, editor freezes the roll), `[PUG-10]` one generator backs arena/recruits/skirmish/editor/reinforcements/property-recruitment/PvP. Grounded against live `MapData.enemy_placements`/`GameMap._spawn_units:168` (path-only today) + the arena plan Slice 2 generator this generalizes. 21/21 green. **Next: resolve the 9 OPEN `[PUG]` questions, then a real plan + skirmish/editor tracks; `B1-PKGA` RngService is the prerequisite + ungated start.**
- [2026-07-03e](2026-07-03e.md) — **Pulled Arena + recruit-purchase into v1 and closed the Band 5 tidy-ups (C6/C7 + manifest picks).** Wrote `band7_arena_implementation_plan_2026-07-03.md` (`B7-ARENA`, `[BEA-4..9]`, keeps its id but now v1): PHB panel + config (lethal/safe, single/escalating), the **shared parametric unit generator** (opponents = table OR spec; the same generator `[THL-8]` `generated` recruits use), sandboxed real `CombatResolver` 1v1 + gold/EXP/wexp payout (atomic, `RngService`), risk resolution (lethal→`handle_death` by `death_mode`, safe→forfeit), escalating ladder — no new save; flipped the row to Planned. Promoted the prep-progression plan's deferred Slice 5 into a **real recruit-purchase slice** (`[THL-8]`: roster-add via `recruit()`, four source modes — grunt/authored/generated(reuses the arena generator)/ransom(`[RCR-5]`); also the `[PVP-3]` buy-phase mechanism), updating its Purpose/Non-Goals/Scope/Dependency/Commit-Order + the control-plane row (now "Bonus EXP, training halls, recruit-purchase"). Tidy-ups: **C6** — AI plan Slice 3 gains a berserk step+test (hostile-to-all target set, profile weights bypassed, one widened enumeration path); **C7** — conditions plan Slice 3 adopts the `shown_disabled` convention (suppressed rows shown greyed with reason, e.g. "Silenced", not removed) + test; **manifest picks** resolved (poison = flat 3 HP/turn; `on_level_up` = Aptitude, grant/revoke = generalized `rally_skill`; combat arts = Wrath Strike + Poison Edge + Fire AoE + Sunder + Windsweep; Berserk staff in v1), review §D marked RESOLVED. 21/21 checks green. **Next: all identified v1 planning is written (Band 5 + Band 6 + Arena); build gates are Band 1-3 + `B4-IEQ`, with `B1-PKGA` RngService the natural start.**
- [2026-07-03d](2026-07-03d.md) — **Wrote the remaining three Band 6 plans (FOW / DTR / prep-progression) — owner confirmed all three in v1.** Resolved the open v1-scope question from 2026-07-03c (these were register-RESOLVED but outside the Q8-Q13 walkthrough). `band6_fog_of_war_implementation_plan_2026-07-03.md` = `B6-FOW` (`[FOW-1..7]`): radius fog behind one `compute_visible_tiles()` seam, `MapData.fog_enabled` encounter flag, fog-mask render + enemy hiding, **per-step ambush interrupt** (hooks `Unit.move_along_path`), AI-cheats one-seam (zero `EnemyAI` change), `discovered_units` = only new save field, authored reveal tools (event rooms via MET `reveal_tiles` + lightable braziers as `map_objects`); coordinates `OVERLAY_FOG` source 5 with MRD's source 4. `band6_destructible_terrain_implementation_plan_2026-07-03.md` = `B6-DTR` (`[DTR-1..8]`): one generic breakable = a **real `Unit` quarantined from the roster** (`is_destructible_object`, main risk = enumerating roster-skip guards), full damage pipeline (forced hit / no counter / anything-breaks-anything / emergent HP+Def durability), `on_break` typed event list (v1 = terrain + `reveal_tiles`), zero new save (rides DCH `map_objects_state`); hard gate `B4-MAP-OBJECTS`, `on_break` runner migrates to MET's `object_broken`. `band6_prep_progression_implementation_plan_2026-07-03.md` = `B6-PREP-PROGRESSION` (`[BEA-1..9]`+`[THL-1..8]`, arena/recruit excluded): bonus-EXP PHB panel (banked pool → `add_exp`, no new leveling code) + training-hall PHB panel (open registry of benefit types routed to owning systems); two new ops = the shared permanent stat-gain primitive (`[THL-3]`, also backs stat-boosters) + the two-scope resource wallet (`party_gold` → `{resource_id:amount}` + F7 pools). Held the band split — Arena stays `B7-ARENA`, recruit-purchase (`[THL-8]`) stays with the B7-PVP/recruitment cluster (both flagged as pull-if-wanted). 3 control-plane rows flipped Target design → Planned; grounded against the live tree; 21/21 checks green. **Next: no Band 6 v1 planning outstanding; remaining Band 5 tidy-ups + the v0.2.5 playtest-return triage are the open threads.**
- [2026-07-03c](2026-07-03c.md) — **Wrote three Band 6 implementation plans (the Q8-Q13 settled set).** Owner chose the settled-set scope; drafted grouped plans in the established per-slice shape and wired 5 control-plane rows (all flipped to Planned). `band6_mutable_campaign_state_implementation_plan_2026-07-03.md` = `B6-CAMPAIGN-SHARING` + `B6-CAMPAIGN-STATUS` + `B6-PER-MAP-OVERRIDES`, **unified into one mutable-campaign-state store per Q13 Impl 2**: the 3-layer rule resolver (mid-map `apply_rule_flip`+`revert_scope` > per-map > campaign default, mandate short-circuit), the permanent-flip patch-log + carry-forward-facts F1 region, self-contained package export/import via `AssetResolver`, and `CampaignStatusRecord` carry-forward (facts seed TCV). `band6_relationships_min_implementation_plan_2026-07-03.md` = `B6-RELATIONSHIP-MIN` (Q9/`[REL-1..9]`): Slice 0 extracts the `[PXP]` resolver pair-agnostic (`[REL-9]`), then pair-keyed graph + `RelationshipProfileDef` ranks + per-pair per-map anti-grind cap, the three pair-keyed gain sources + shared proximity primitive, and the rank-scaled proximity benefit aura + on-crossing `unlock_conversation`/`set_flag`. `band6_map_readability_implementation_plan_2026-07-03.md` = `B6-MRD` (Q10/`[MRD-1..6]`+`[TUR-1..4]`), sequenced by save-dependency: Slices 1-5 no-save view state buildable now (threat extraction, `_watch_set`/`_danger_mode` + precedence-ordered overlay registry, hold-to-peek, path arrows, `grid_dim`), Slice 6 watch-set save trails `B1-SUSPEND`. Grounded against live `CampaignRules.gd`/`GridManager.gd`; drafted against planned Band 1/3 APIs. 21/21 checks green. **Next: confirm v1-scope for `B6-FOW`/`B6-DTR`/`B6-PREP-PROGRESSION` (register-resolved but not in the Q8-Q13 walkthrough), then draft if in v1.**
- [2026-07-03b](2026-07-03b.md) — **Wrote the four Band 5 implementation plans + a review doc.** Drafted the grouped Band 5 plans against the settled Q1-Q7 decisions: `band5_conditions_skills_implementation_plan_2026-07-03.md` (conditions + capability-gating + shared `LifecycleStore` + skill effect-registry conversion + `on_level_up` + `LoadoutPanel` shell/skills adapter), `band5_source_style_implementation_plan_2026-07-03.md` (`EffectSpec`/`StyleDef` + 4 registries + combo resolution + generalized forecast + 2 proof consumers + utility staves + styles/sources loadout adapters), `band5_action_economy_implementation_plan_2026-07-03.md` (general action-budget guard + `refresh_full_turn` grant + remaining/flat secondary movement), `band5_ai_implementation_plan_2026-07-03.md` (`AIProfileDef` registry + `set_ai`/seek/group-wake/`ai_awake` + single-ply registry-term scorer reusing the player forecast). Grounded every "Existing Code Touchpoints" against the live tree (`ConditionManager` M8 stub, `SkillHandler._dispatch`, `TurnManager._ai_controller` seam, `WeaponData.effect_tags`). Review doc `band5_plans_review_2026-07-03.md` carries Q-B5-1..4 + 7 new concerns (C1 lifecycle-store owner, C2 `conditions` save migration, C3 B2/B5 effect-registry ownership, C4 loadout-shell cross-plan ordering, C5 does v1 AI score styles, C6 berserk+scorer, C7 disabled-with-reason). Wired all 10 control-plane rows to the plans; 21/21 checks green. **Follow-up same session: all four owner questions resolved** — Q-B5-1 (build the full v1 content manifest now, reversing the defer-to-demo watchout → wrote `band5_v1_content_manifest_2026-07-03.md`, a feature-showcase floor: 5 conditions, cure hooks, ~50 coded skill effects certified + `on_level_up` + grant skill, 4 staff archetypes, Source+Style kinds + demo styles, Dance/Canto, 5 AI profiles + scorer terms, §9 coverage table), Q-B5-4 (pull `B5-SOURCE-STYLE` forward — parallel with skills, gates the AI scorer + forging `[FRG-18]`), plus C1/C5 from the first pass. Manifest+resolutions folded into the plans, review doc, control plane, forging plan, and demo-campaign memory (dependency inverted: manifest first, demo consumes it). **Follow-up 2:** owner reframed the v1 showpiece as the campaign BUILDER ("full variety to build with") — content floors are now varied builder palettes (breadth = data), prune-not-carry relaxed; then chose **WIDEN EVERYTHING**, pulling AoE shapes + gambits + capture-carry into v1 (deliberately reverses ratified Q5/Q6, recorded as an override in review §0). Plan 2 grew 6→8 slices (real AoE shapes; new Slice 6 = displacement primitive + capture non-lethal→sleep + gambit-as-style; `B2-OCCUPANCY` a heavy gate); battalion *entity* stays A2-deferred; `B6-RESCUE-CARRY`/`B7-BATTALION` narrowed to expansion/entity. **Next session: draft the Band 6 plans** (the other half of v1; six rows unplanned, design settled by the Q8-Q13 walkthrough). Band 5 implementation stays gated behind Band 1-3 + `B4-IEQ`.
- [2026-07-03](2026-07-03.md) — **Drafted the `UiThemeDef` & asset-resolution presentation contract.** The thin author-facing note the band-UI review gated Band 4-6 panel plans on — now unblocked by the retired raw-load spike (font/audio tokens name real load calls). Two open-registry seams: `UiThemeDef` (presentational tokens only, `PHB-6`, ids are authored data) + `AssetResolver` (`resolve(kind, id_or_path)`, the single load site). Locks a thin grouped v1 token schema (colors incl. overlay-modulate tokens, 9-slice chrome, SDF/tabular/title fonts, default icon atlas + registry-metadata per-entry icons, cursor, backgrounds, stage frames, audio slots), the id-vs-path convention, and the 5-level fallback chain. Spelled out how it composes with the live `MenuScale` runtime-Theme seam. Grounded `FactionData.color`/`ClassData.sprite_id`/`MenuScale.gd:94` before citing. 21/21 checks green. Next: Band 4-6 panel plans reference it (convoy/shop = first icon consumer); Band 5 plans + v0.2.5 delivery still parallel.
- [2026-07-02g](2026-07-02g.md) — **Retired the OGG/TTF raw-load spike (Godot 4.6).** Proved the runtime raw-load API from `user://` works outside the `.import` pipeline — in a real exported Linux template (`running_in_editor=false`), not just the editor: OGG decoded (`AudioStreamOggVorbis.load_from_file`/`load_from_buffer`, 6.1 s), TTF loaded (`FontFile.load_dynamic_font`, real metrics), WAV round-tripped (`save_to_wav`→`load_from_file`); MP3 loader also present. Updated the asset-taxonomy doc's OPEN SPIKE → **SPIKE RETIRED** block with the exact proven load calls + evidence, flipped open-question #1 to RESOLVED, and noted the DoD#2 `check_docs.py` guards fire when the loader is built. All spike artifacts (throwaway `spike_export/` scene, temp Linux preset, `user://` fixtures) removed; tree left clean, nothing binary committed. Next: draft the `UiThemeDef` note (now unblocked to name real font/audio load calls).
- [2026-07-02f](2026-07-02f.md) — **UI/UX groundwork: art-asset research + project asset inventory/reuse map.** Wrote two reference (non-spec) design notes ahead of the eventual UI/UX pass: `ui_ux_art_asset_research_2026-07-02.md` (SRPG genre conventions — combat forecast, threat range, weapon-type color coding, `x2`/`x4` badges; clarity-over-immersion / safe-zone principles; the 9-slice/atlas/SDF-font pipeline; mood-board→wireframe→mockup workflow) and `ui_ux_asset_inventory_and_reuse_2026-07-02.md` (10 asset-class tables grounded in the live tree — confirmed all-placeholder art today; special requirements + reuse levers per row). Linked the inventory to the art importer (`B6-SPRITE-IMPORTER`/`[IMP-1..6]`), which owns the animation/`SPRITE_SOURCE_SIZE` decisions. Commits `5584f39`, `e8afe49` (+ this). Next: a thin `UiThemeDef`/asset-resolution note (token schema + fallback chain) before Band 4-6 panels multiply.
- [2026-07-02e](2026-07-02e.md) — **Fixed Menu Scale vertical drift + migration guard; cut v0.2.5.** Review of the v0.2.3→v0.2.4 diff confirmed the owner's report: the `V023-01` column lock only held the slider's x-axis (~481 px vertical drift 1.0x→2.0x). `SettingsScreen.apply_menu_scale` now scroll-anchors the slider's row; `menu_scale_schema_version` migration now shifts only actually-stored indices (pre-menu-scale cfgs keep 1.0x). Row-y anchor + migration-matrix tests added; GDD_07/triage plan updated (commit `d0c26f6`). Sonnet subagent cut the v0.2.5 rerun build (`a24a635`, `8734136`): exe SHA-256 `d1d758c2…`, manifest + handbook, `VAL-V023-DISPLAY` repointed. Next: deliver exe + handbook to the tester; Band 5 plan drafting remains the parallel thread.
- [2026-07-02d](2026-07-02d.md) — **Wrote the Band 5 handoff; audit fully closed.** `band5_implementation_plan_handoff_2026-07-02.md` drafts against the already-settled Q1-Q7 walkthrough decisions (2026-07-01): two-chain bootstrap order (conditions→skills→loadout→Source+Style→staves→action-flow ∥ AI composition→min-scorer), four recommended grouped plans, owner questions Q-B5-1..4 (Q2 demo-campaign gate, grouping, duration-lifecycle/IEQ boundary, forging FRG-18 pull). Control plane Band 5 note added; C1 marked resolved — all Band 1-6 audit findings closed. Next: draft the four Band 5 plans (or v0.2.4 triage if the playtest returns first).
- [2026-07-02c](2026-07-02c.md) — **Applied all remaining band-plan audit fixes (commit `a319da8`).** Band 1: phantom-API wording, `# rng-allow:` guard convention, T-matrix mapping (T2→Slice 2, T6→B1-SUSPEND). Band 2: `gs.campaign_rules.permadeath_enabled`, occupancy policy tiers, `project_combat()` wrapper, `DeathContext.object_ref`. Band 3: `visited_trail` link, boolean notation, text family scaling, calendar victory-only advance. Band 4: `InventoryEntry.instance_id`, ItemDef id-parity invariant, default PXP rank, `shown_disabled` degradation docs, `ConvoyService` helper owner, verification checklists on all four plans. Only audit C1 (Band 5 handoff) remains — next session's target.
- [2026-07-02b](2026-07-02b.md) — **Resolved audit owner decisions 4S-1 + C2.** `[SHP-6]`: sell price = campaign-default author `REQ-16` formula (50% of value × durability% remaining) + per-shop incoming-price modifier symmetric with buy modifiers; stock-entry `sell_yields` = override — wired into the shop plan. Wrote the thin `band4_campaign_loop_implementation_plan_2026-07-02.md` (D2 minimum loop: `battle -> hub -> battle` fixture, victory/defeat/suspend legs, convoy→shop attachments) and recorded handoff D5 grouping the seven unplanned Band 4 tracks into enablers / map-content / campaign-wrappers plans. Commits `97cdd87` + two follow-ups. Next: high/medium audit plan edits; Band 5 handoff.
- [2026-07-02](2026-07-02.md) — **Audited the Band 1-6 implementation plans (review doc).** Wrote `AGENT/Code Reviews/band_plans_audit_2026-07-02.md` with 22 findings: Bands 5-6 have no plans/handoff (C1), the `B4-CAMPAIGN-LOOP` plan owed by handoff D1 is missing (C2), Band 1 Slice 0 cites a nonexistent `get_action_start_tile` API (1-1), Band 2 Slice 5 reads the `permadeath_enabled` loose field Band 1 deletes (2-1), IEQ needs an item instance id no slice creates (4I-1), and the shop sell-price source is an unresolved owner decision (4S-1). Code-grounding spot checks otherwise verified. Commit `158c650`. Next: owner decisions on 4S-1/C2, then the high-severity plan edits.
- [2026-07-01g](2026-07-01g.md) — **Cut the v0.2.4 display-rerun build + manifest + handbook.** Bumped release metadata to v0.2.4, exported `Project_Prometheus_v0.2.4_debug.exe` (SHA-256 `45ed14f1…`), wrote `playtest_build_v0.2.4.md` (source commit `3741999`) and the focused `playtest_checklist_v0.2.4.md` re-testing the `V023-01..06`/`08a`/`09a` repairs (flags §1.4 weapon row + §1.5 wheel input, requests `godot.log`), and pointed `VAL-V023-DISPLAY` at the build. 48 suites + 21 docs checks green; exe hash/size match the manifest. Commits `3741999`, `d0f0627`. Next: triage the returned v0.2.4 results; if Part I passes, flip `VAL-V023-DISPLAY` and move to merge.
- [2026-07-01f](2026-07-01f.md) — **Implemented immediate v0.2.3 display repair pass.** Fixed `V023-01..06`, `V023-08a`, and `V023-09a`: Menu Scale `0.5x` + stable Settings slider, character-sheet scroll frame, contextual menu re-anchoring after zoom, AttackPreview weapon rows/Neutral/More Info fit, Level-Up wheel blocking, windowed resolution clamp, archer copy, and terrain click paging. Full 48-suite run + docs checks green. Commit `fd0f5f4`. Next: focused live Windows display rerun before flipping `VAL-V023-DISPLAY`.
- [2026-07-01e](2026-07-01e.md) — **Deferred v0.2.3 playtest fixes routed into home bands.** Updated the triage plan with the next-session fix set (`V023-01..06`, `V023-08a`, `V023-09a`), wired deferred items into Project Control Plane homes (`UI-INSPECTION`, `B4-MAP-OBJECTS`/`SAC`, `B6-INPUT`, `VAL-V021-04`, `VAL-PLAYTEST-RERUN`), regenerated the docs index, and validated docs. Commit `cdef5c9`. Next: implement the immediate playtest fixes and rerun focused display validation.
- [2026-07-01d](2026-07-01d.md) — **Returned v0.2.3 playtest results triaged into `V023-01..11`.** Copied the returned checklist and screenshot evidence into the docs tree, researched every tester comment against the UI/display code and tests, and wrote `playtest_v0.2.3_results_triage_plan_2026-07-01.md` with likely causes, fix plans, pipeline mapping, and owner questions. Commit `34c1099`. Next: walk owner questions, merge approved rows into the Project Control Plane, then start display-gate fixes `V023-01..06`.
- [2026-07-01c](2026-07-01c.md) — **Walked `[FRG-1..20]` to RESOLVED + wrote the `B7-FORGING` plan.** Owner walked the full forge register one question at a time; settled every parameter and added authoritative decisions (§3c), refinements (§3d), and a two-pane UI draft (§3e). **v1 slice grew from "+N stat bump" to point allocation + live durability/repair + player-rename.** Key model: `forged_mods` op-overlay, one effective-weapon-stat resolver seam, per-forge-instance cap envelope (`min(item, forge)`), and a new **target-item predicate subject** (refuse key/over-upgraded items) as the one gating dimension over shops. Bumped the `B7-FORGING` control-plane row and wrote `band7_forging_implementation_plan_2026-07-01.md` (Slices 0-9). 21 checks green. Commits `549256c`, `2f3229a`, `4abaa61`, `acebc4d`, `eba6813`. Next: update the most recent playtest results and debug the build; forge work parked (plan builds after Band 1-4 gates).
- [2026-07-01b](2026-07-01b.md) — **Opened the `FORGE` design pass — `[FRG-1..18]` register.** Researched forging in Fire Emblem (PoR free-point allocation, RD ores, Fates +N/2-copies, 3H stats+repair+craft rank-gated, Engage refine+transform+engrave) and similar SRPGs (Triangle Strategy rank nodes granting stats+abilities, Disgaea Item World, FFT); mapped onto the existing substrate and wrote `forging_open_questions_2026-07-01.md` (18 grouped questions + recommendations, open-registry stance). **Verified Q15 ask:** `InventoryEntry.forged_mods` is reserved+unread and B4-IEQ keeps it as an instance overlay — per-instance room confirmed; the one new engine seam is an effective-weapon-stat resolver (`[FRG-10]`). No implementation plan. 21 checks green. Next: owner walkthrough of `[FRG-1..18]`, then a `B7-FORGING` plan + control-plane bump.
- [2026-07-01](2026-07-01.md) — **Band 5-8 review walkthrough: Q1-Q17 settled.** Owner walked `band5_plus_preimplementation_questions_review` one question at a time; added a "Walkthrough Decisions (2026-07-01)" section. Reframed v1 = Band 5+6 completion + bugfix + UI polish. Band 5 Q1-Q7 settled (condition substrate w/ general capability-gating, single-target refresh, loadout shell + category adapters, Source+Style substrate + 2 consumers, utility staves incl. condition-inflict, single-ply registry AI scorer); Band 6 Q8-Q13 settled (campaign sharing, minimal relationships, MRD no-save-first, `SelectionCursor` Component 1 -> Band 4, sprite importer HELD, per-map overrides FIRMED into a 3-layer resolver). Decided the campaign asset taxonomy + **JSON-canonical** Tier-2 format; designed PvP multi-prepper prep (shared draft pool, public zones/hidden placements, hotseat); elevated forging to a near-term `FORGE` design pass. No implementation plans written. Commit: `7449f23`. Next: convert decisions into follow-up work (asset-format note, selector split into Band 4 plans, `FORGE` register).
- [2026-06-30m](2026-06-30m.md) — **Band 5+ pre-plan and UI design reviews written.** Added `band5_plus_preimplementation_questions_review_2026-06-30.md` (Band 5-8 questions to settle before implementation plans, grouped Band 5 recommendation, Band 6-8 gates) and `band_ui_initial_designs_review_2026-06-30.md` (all-band UI surface inventory plus user theme/asset/default recommendations). Commit: `8fce805`. Next: review the questions, settle Band 5 content floor/grouping, and consider a thin `UiThemeDef`/asset-resolution design note before more PHB panel plans.
- [2026-06-30l](2026-06-30l.md) — **Band 4 convoy/shop plans written.** Added active implementation plans for `B4-CONVOY` (`band4_convoy_implementation_plan_2026-06-30.md`: state-preserving convoy store, capacity/overflow, PHB panel, shared selector, death/reward hooks) and `B4-SHOP-ECONOMY` (`band4_shop_economy_implementation_plan_2026-06-30.md`: resource-keyed shop config, ResourceLedger transactions, shopper-bound dynamic pricing/stock gates, PHB panel, on-map/dialogue hooks); wired both control-plane rows and regenerated the docs index. Commit: `f5c2a13`. Next: grouped Band 4 consumer plan(s), then thin campaign-loop integration plan.
- [2026-06-30k](2026-06-30k.md) — **Band 4 implementation-plan writing started.** Added active implementation plans for the two Band 4 internal sub-foundations: `band4_items_equipment_implementation_plan_2026-06-30.md` (`B4-IEQ`, staged item/equipment migration) and `band4_map_objects_implementation_plan_2026-06-30.md` (`B4-MAP-OBJECTS`, registry-backed activation/runtime-state substrate); wired both Project Control Plane rows and regenerated the docs index. Commit: `3991b28`. Next: continue with separate `B4-CONVOY` and `B4-SHOP-ECONOMY` plans, pairing their rough PHB panel/selector UX.
- [2026-06-30j](2026-06-30j.md) — **Band 4 implementation-plan handoff written.** Wrote `band4_implementation_plan_handoff_2026-06-30.md` (modeled on the Band 3 handoff) for the 14-row Band 4 campaign-loop vertical slice: identified the two internal sub-foundations (`B4-IEQ` items, `B4-MAP-OBJECTS` activation) that gate the band, recommended a 12-step build order ending in `B4-CAMPAIGN-LOOP` with `B4-PROMOTION-UI` deferred, and surfaced four owner questions (one-plan-vs-split, minimum playable loop, convoy/shop UI timing, dialogue v1 command set). 21 checks green. Next: settle the plan-split question, then draft the Band 4 plan(s).
- [2026-06-30i](2026-06-30i.md) — **Band 3 combined implementation plan written.** Drafted `band3_core_authoring_foundations_implementation_plan_2026-06-30.md` from the 2026-06-30h handoff: ordered slices following the fixed bootstrap order (registry -> stat/movement -> TEXT -> TCV -> REQ -> CAMPAIGN-RULES -> COMBAT-ROLL-RESOLVER -> MET/PHB -> RESOURCE-POOLS; CALENDAR-LITE deferred), reusing the movement/stat sub-plans by reference; surfaced the three handoff owner questions with provisional answers in `band3_implementation_plan_review_2026-06-30.md`; pointed the eight buildable Band 3 control-plane rows at the new plan; regenerated the index (21 checks green). Next: review the three owner questions, else default to `B1-PKGA` Slice 1a.
- [2026-06-30h](2026-06-30h.md) — **Plan/doc review, autoload check, Slice 1 split, author-selectable hit roll, Band 3 handoff.** Reviewed the doc structure + Band 1/2 plans; added `check_docs.py` check 21 (autoload order); split Band 1 Slice 1 into 1a-1d (raw-RNG guard must land last); made the hit-roll formula author-selectable (`[CRR-1..8]`: built-in presets + sandboxed expression, RULE-001 reframed as the default preset, seam baked into Slice 1b, registry promotion tracked as `B3-COMBAT-ROLL-RESOLVER`); wrote the Band 3 implementation-plan handoff. Commits: `80731f4`, `7837a60`, `0b01b74`. Next: write the combined Band 3 implementation plan per the handoff.
- [2026-06-30g](2026-06-30g.md) — **Session wrap + Band 3 planning handoff.** Recorded the final wrap for the Band 2 plan and Band 1 question closeout, then set next-session target to write a combined Band 3 implementation plan covering CampaignRules profiles, TCV, REQ, MET, PHB, text, movement/vulnerability, stat registry, resource pools, and optional calendar-lite. Next: write `band3_core_authoring_foundations_implementation_plan_2026-07-XX.md` unless priority returns to `B1-PKGA`.
- [2026-06-30f](2026-06-30f.md) — **Band 1 implementation questions resolved.** Officially adopted the combat RNG event-record API (`get_action_start_tile` + explicit `event_record`) and resolved `[CST-13]` to hooks-only in the campaign/save spine with full Turnwheel as the immediate follow-on. Commit: `76e47ec`. Next: start `B1-PKGA` Step 1 unless priority changes.
- [2026-06-30e](2026-06-30e.md) — **Band 2 implementation plan created.** Added the active Band 2 shared runtime contracts implementation plan, linked all Band 2 control-plane rows to it, regenerated the docs index, and validated docs. Commit: `9f913b2`. Next: default back to `B1-PKGA` Step 1 unless priority changes; Band 2 starts with Slice 0/1 only after Band 1 gates.
- [2026-06-30d](2026-06-30d.md) — **Band 2 implementation-planning handoff.** Wrote the next-session handoff for drafting a combined `B2-REGISTRY` -> `B2-ACTION-EFFECT` -> `B2-RESOURCE-LEDGER`/`B2-OCCUPANCY`/`B2-DEATH-LIFECYCLE`/`B2-PROJECTION`/`B2-DATAMANAGER-SEAMS` implementation plan, including read-first docs, slices, non-reopened decisions, and owner questions. Next: write the Band 2 shared runtime contracts implementation plan unless priority returns to `B1-PKGA`.
- [2026-06-30c](2026-06-30c.md) — **Band 1 implementation plan created.** Added the active `B1-PKGA` -> `B1-F1` -> `B1-SAVECODEC`/`B1-CST` implementation plan, linked it from the control plane, captured Band 1 questions in a review-style doc, and regenerated the docs index. Commit: `ac047a2`. Next: implement `B1-PKGA` Step 1 (`RngService` + RNG migration/tests).
- [2026-06-30b](2026-06-30b.md) — **Band 1 implementation-planning handoff.** Wrote the next-session handoff for drafting the `B1-PKGA` -> `B1-F1` -> `B1-SAVECODEC`/`B1-CST` implementation plan, including read-first docs, recommended plan shape, slices, non-reopened decisions, and owner questions. Next: write the active Band 1 implementation plan under `AGENT/Docs/plans/`.
- [2026-06-30](2026-06-30.md) — **Band 0 tracking review closed.** Removed completed tracking rows from the `GDD_10` next-work queue, marked `B0-GDD10-REWRITE`/`B0-FEATURE-INDEX-WIRING`/`CLEAN-STALE-GDD10` implemented, added `B0-GDD-ANCHORS` for later exact section anchors, and filled Feature Index coverage for every control-plane Track ID. Commit: `d2eb897`. Next: start Band 1 (`B1-PKGA` -> `B1-F1` -> `B1-SAVECODEC`/`B1-CST`) unless priority changes.
- [2026-06-29i](2026-06-29i.md) — **Campaign status + property systems planned.** Added the carry-forward/status-record plan plus control-plane rows for `B6-CAMPAIGN-STATUS`, `B6-PROPERTY-CAPTURE`, `B7-PROPERTY-RECRUITMENT`, and `B7-AI-RECRUITMENT`; updated map-object cursor/capture component contract, triage, `GDD_10`, feature index, and generated docs index. Commit: `2a4e6b9`. Next: keep Band 1 implementation order unless priority changes; reserve F1 rows for status imports and property/recruitment state when F1 locks.
- [2026-06-29h](2026-06-29h.md) — **Session-end cleanup and handoff.** Confirmed the tree was clean after the registry and EXP-boundary planning work, then recorded the next session start point: `B1-PKGA` / Package A `RngService`, followed by `B1-F1`, `B1-SAVECODEC`/`B1-CST`, and `B2-REGISTRY`. Recent commits covered: `e580a88`, `2674056`, `10410a0`, `ab0d721`.
- [2026-06-29g](2026-06-29g.md) — **Class EXP / PXP boundary recorded.** Added the boundary plan, updated `GDD_02`, the `B4-PXP` control-plane row, feature index, and generated docs index so class EXP stays separate from PXP storage/lifecycle while award/profile authoring uses shared idioms. Commit: `10410a0`. Next: build PXP tracks and data-driven class-EXP awards as sibling benefit paths.
- [2026-06-29f](2026-06-29f.md) — **Movement/vulnerability + stat registry implementation planning.** Added `B3-MOVEMENT-VULN-REGISTRY`, wrote movement/vulnerability and stat registry implementation plans, wired them into the shared foundation todo, Project Control Plane, registry checklist/manifest, vocabulary manifest, feature index, and generated docs index. Commit: `e580a88`. Next: build the registry loader seam before movement/vulnerability work; reserve F1 rows for `UnitData.extra_stats`; decide EXP ownership separately.
- [2026-06-29e](2026-06-29e.md) — **Scope reframe executed + EXT/preset GDD audit.** Added the SET-011..014 decision record, reframed `GDD_00`, tracked `REL-WEB-DEMO`, audited `GDD_01`-`08` and the Adoption Matrix so numbers/relationships read as developer presets over registries, and logged three implementation-facing uncertainty findings. Commits: `73b8394`, `cff3633`, `43a5f8d`. Next: exact GDD anchors, then Band 1 (`B1-PKGA` -> `B1-F1` -> `B1-SAVECODEC`/`B1-CST`).
- [2026-06-29d](2026-06-29d.md) — **Project-scope reframe + planned GDD stale-assumption audit.** Walked the doc structure with the owner, then wrote a next-session plan to reframe scope (learning/portfolio primary, tactical-RPG builder secondary, bounded power-user access) and audit `GDD_00`–`08`/`GDD_10`/Feature Index/Adoption Matrix for stale assumptions. Recorded owner decisions: scope approved as drafted; data-only authoring first with a sandboxed-scripting ceiling (MIT+Commons-Clause source = full-access-by-fork); slice-first playable web demo as the portfolio target; reframe is framing-only (no band re-sequencing). Commits: `e5cdc7a`, `65f3e8d`. Next: write the decision record, update `GDD_00`, add the web-demo tracker row, then run the framing-only chapter audit.
- [2026-06-29c](2026-06-29c.md) — **Control-plane manifests, GDD_10 rewrite, feature-index wiring, and tracker enforcement.** Added role/vocabulary manifests, rewrote `GDD_10` as a Track-ID build guide, archived the legacy roadmap copy, rewired the feature index to control-plane Track IDs/code/tests/sources, and added `check_docs.py` enforcement for tracker schema, Track ID reachability, allowed bands/statuses, local links, and allowed prefixes. Commits: `880f21e`, `94b3937`, `e3cbb7e`, `68dbf1a`, `39d542a` plus bookkeeping `cbc2e62`. Next: review row granularity, rewrite numbered GDD chapters with exact anchors, then start Band 1 implementation (`B1-PKGA` -> `B1-F1` -> `B1-SAVECODEC`/`B1-CST`).
- [2026-06-29b](2026-06-29b.md) — **GDD_10 coverage matrix + Project Control Plane draft.** Mapped live `GDD_10` rows, Phase 3 backlog entries, validation/release/cleanup queues, and missing triage rows into the new dependency-band/control-plane model; then drafted the strict Project Control Plane row schema and seeded tracker rows for Bands 0-8 plus validation, release, cleanup, content, polish, UI, and enforcement queues. Commits: `4f4ac60`, `18660f1`. Next: review row granularity/Track IDs, draft vocabulary/role manifests, then rewrite `GDD_10` as the human build guide.
- [2026-06-29](2026-06-29.md) — **Living project tracking system plan.** Added the plan for a Project Control Plane tying GDD chapters, GDD_10, feature index, registers, plans, validation, release gates, and cleanup into one tracking system before the unified GDD rewrite; then folded in owner decisions that campaign sharing/exporting is v1, side activities are parked, and enforcement must be machine-checkable. Commits: `1d65c9f`, `8d4e974`. Next: create the GDD_10 active-work coverage matrix and Project Control Plane manifest/schema.
- [2026-06-28f](2026-06-28f.md) — **Pre-GDD feature triage reprioritization.** Reworked the planned/unimplemented feature triage into dependency bands for GDD consolidation: Bands 1-5 v1-core, Band 6 v1-lean/stretch, Band 7 optional, Band 8 post-v1/parked; kept ActivityRegistry/ActivityRunner V1-optional unless one side activity enters v1. Commit: `7496dbd`. Next: rewrite GDD/GDD_10 around the new band split.
- [2026-06-28e](2026-06-28e.md) — **Session-end audit for minigame/activity planning.** Confirmed durable coverage for launch surfaces, scripting/tool research, activity specs, PuzzleScript ladder, dialogue tooling, pre-GDD triage, shop/convoy UI timing, and calendar-lite map counters; added the missing tool/project pros/cons comparator to the research doc. Commit: `ac6ce2d`. Next: decide whether the ActivityRegistry/ActivityRunner seam stays V1-optional or moves to V1-lean.
- [2026-06-28d](2026-06-28d.md) — **Pre-GDD triage follow-ups.** Split minigame work into ActivityRegistry/ActivityRunner seam, three activity-template prototypes, and arbitrary public scripting VM; added shop/convoy UI/UX timing guidance for Band 3; added calendar-lite `total_maps_played`/`story_maps_played` counters for gardens, brewing, restocks, and territory pressure. Commits: `3792f92`, `90bb8ce`. Next: decide whether the activity seam stays V1-optional or moves to V1-lean.
- [2026-06-28c](2026-06-28c.md) — **Minigame activity type specs.** Added initial specs for PuzzleScript-style grid puzzles, QTE/timing challenges, and blackjack/card-table activities; recorded the PuzzleScript compatibility ladder; assessed dialogue/cutscene tooling and chose shared StagePresentation reuse while keeping DLG and ActivityRunner separate. Commit: `b6f4f80`. Next: draft ActivityRegistry/ActivityRunner implementation plan and minimal schemas.
- [2026-06-28b](2026-06-28b.md) — **Minigame activity scripting research.** Reviewed the A5 minigame seam and adjacent PHB/SAC/DLG/MET/EXT decisions, documented the `launch_activity` requirement across prep, map, dialogue, and story events, researched sandboxed activity-runtime options, and recommended building the registry/result seam before any public scripting VM. Commit: `4b74bcb`. Next: prototype blackjack, QTE lockpick, and a grid puzzle to validate API needs.
- [2026-06-28](2026-06-28.md) — **Design review → foundation contracts → F1 prep → initial v1 triage.** Reviewed unimplemented planned systems, walked H1-H4/L1-L2 follow-ups, created shared foundation contracts, fixed the STW move-and-fire test-note contradiction, tracked stale GDD navigation cleanup, created the F1 source inventory + F1 schema-lock design, and compiled the planned/unimplemented feature triage. Next: walk triage for v1 scope, ratify F1 lock decisions, create exact F1 manifest, then outline/rewrite the unified GDD.
- [2026-06-27d](2026-06-27d.md) — **A5 walk COMPLETED + the entire define-all design sweep CLOSED → ready for Phase B (F1 lock).** This one (long) session walked all of A5 + fold-ins + both pre-F1 keystones. Began with the death-inventory disposition keystone `[DTH-1..12]` RESOLVED. Mapped the A5 cluster (death-disposition · `[AGT §6]` EXP path · `shop`/`activate` configs · Difficulty/Casual-Phoenix #12 · Arena #14 · PvP #7 · loadout cap · fold-in BAT-14..16 + optional F14) + audited connected registers (atlas A5 bullet already current; only `[BAT-16]`/`[DSP-14]` snapshot-then-resolve touch-ups owed — closed this session). Walked the **keystone** end-shape-first: substrate verified (`handle_death` `Unit.gd:488` has **no inventory step today** → greenfield; convoy unbuilt → `to_convoy` is a Phase-C dep; `CombatResolver:779–807` already does snapshot-then-resolve in miniature). **Model = author-ordered fall-through CHAIN walked per-item** (recipient `transfer_to_killer`/`nearest_ally`/`main_character` + placement `to_convoy`/`drop_on_tile` + terminal `lost`; roster-order tie-break; overflow continues chain). Owner forks: **disposition always runs; Phoenix-revived units return EMPTY** (revised same session — drops snapshot/restore, `[DTH-1]`) · **campaign default + per-faction** (player `[to_convoy]`, enemy `[lost]`+droppable, `[DTH-3]`) · Key-Items on a **separate `lost`-banned chain** (`[DTH-5]`) · `drop_on_tile` parameterized `{pickup, on_map_end-chain}` (`[DTH-6]`) · no special no-killer case + **credit≠transfer** (`[DTH-7]`) · snapshot-then-resolve roster order (`[DTH-8]`) · single `handle_death(ctx)` funnel + `DeathDisposition` resolver (`[DTH-9]`) · **`key_item_removed_from_map` = custody state machine** `{player_held,in_convoy,enemy_held,on_tile,off_play}` designed in full (`[DTH-10]`). INDEX/REGISTERS regen, 19 checks green. Commit `8447a4f`. **Then A5 #2 — Difficulty + Casual/Phoenix (#12) → `[DIF-1..7]` RESOLVED:** two orthogonal axes — **`death_mode` enum {classic/casual/phoenix}** replacing the `permadeath_enabled` binary (casual=next-map, phoenix=next-turn, both return EMPTY per `[DTH-1]` — closes the DTH-1 Casual/PvP scope) + **difficulty = authored content VARIANTS ("palettes")** + AIP overlay (not a stat-formula); player picks from an author-allowed set. The difficulty answer expanded into **author-exposed tunable knobs + author-defined typed variables driving tag-scoped parametric effects** ("+x enemy stats", "+n levels to tagged enemies") — one mechanism hinging on **F6 becoming a TYPED variable store**, converging with the dialogue→win/loss objective-extensibility pin + `[REQ-16]`. **Owner: fold all into ONE consolidated pre-F1 walk → new pinned register `[TCV-1..6]`** (typed campaign-variable store; absorbs the objective-extensibility pin). Side-check earlier: **no dialogue→victory/defeat path exists** (closed `ObjectiveCondition` enum + no F6 store + MET unbuilt) → pinned, now part of `[TCV-4]`. **Then A5 #3 — non-combat-action EXP/proficiency (`[AGT §6]`) → RESOLVED (mostly REUSE):** level-EXP = a per-action authored `exp_award` (default 0) + CampaignRules default via one `award_action_exp` step (replaces `STAFF_HEAL_EXP`; anti-farm via `[AGT §5]`); non-weapon proficiency = **extend `[PXP]`** (an action *source* declares `proficiency_tracks`, PXP-2 generalized "item declares"→"any source declares") — no new system/save field. **Then A5 #4 — Bonus-EXP (#18) + Arena (#14) → `[BEA-1..9]` RESOLVED (heavy reuse):** both = `[PHB]` opt-in prep panels; **Bonus-EXP** = a banked pool from authored `grant_bonus_exp` awards (objective/`[TCV]` plumbing) + optional difficulty scaling, spent via `add_exp` (reuses level path) at a default-1:1 author-configurable cost curve; **Arena** = a sandboxed real `CombatResolver` fight, **author lethal/safe toggle per arena** (lethal respects `death_mode`: classic=real, casual/phoenix=no loss), **author-choice single-or-escalating** match loop, opponents = authored table; reuses gold ledger; resolves `[DIF-6]`. Also filed: atlas **Phase C deferred-review findings** (un-wired `on_level_up` trigger; raw-`randi` growth = M9a migration) + **`[STM-5]`** missing/undefined stat-info handling. **Then A5 #5 — loadout cap → `[LDC-1..7]` RESOLVED (confirm-and-generalize):** the skills system already implements the earned-superset/equipped-subset/cap pattern + `[SKL-3]` `requires_equip`; generalize to styles + granted sources with **separate author-configurable per-category caps** (`max_skills`/`max_styles`/`max_sources`, not a unified pool), **swap-free-in-prep** (earned superset never lost) + optional author-gated `forget`, a `[PHB]` prep panel (prep-only); **`[LDC-8]` caps are criteria-modifiable** (base + `[REQ]`/F16-gated modifiers reusing the modifier/`[REQ-16]`/`[TCV-3]` machinery — tier-3→+2 skills, item→+2 styles; cap-shrink → excess auto-unequips, never lost) + **`[LDC-9]` style cap partitions per author-defined `style_group`** (sword-arts cap independent of fire-magic cap). `[CEX-7]`/`[STY-3]` pins → RESOLVED. **Then A5 #6 — `shop`+`activate` configs → `[SAC-1..12]` RESOLVED:** one **unified `map_objects`/`activate` model** (generalizes `[DCH]`; doors/chests = specialized types; **each activate instance self-labeled** so multi-option tiles aren't ambiguous); on-map panel-triggers reuse the **`[PHB]` panel UI** (dual-surface) with inline+named-ref variation; **every shop assumes a SHOPPER** (on-map activator / prep selected-first) = the **F16 subject** for **dynamic pricing** (`price=base×f(shopper.charm)`, REQ-16), **conditional stock** (per-entry F16 gate, VIL-6/7 hidden/shown-disabled), and **dialogue-wrapped shops** (`shop` as a DLG command) — all **full-built now** (owner); destination unified buy→shopper/overflow→convoy (collapses `[SHP-4]`). Updates `[SHP §4]`/`[SHP-4]`/`[DCH]`. Also: minigame INVESTIGATE note (casino/fishing/garden → scope map #23 + `[PHB]`). **Then A5 #7 — PvP → `[PVP-1..8]` RESOLVED:** scopes the deferred **D20(b) bring-your-own-army** as a **PvP campaign type** (hub + map-selector + per-faction **buy phase reusing the prep panels** recruit/shop/training-hall-#19/bonus-EXP on an author budget → battle → best-of-N + author inter-round bonuses via `[TCV]`); **freeform buy** + **fully author-defined resources**; **social-contract trust, no anti-cheat** (matches ratified D18); hotseat-M15/online reuse; no permanent loss via `death_mode`; **build-dep = training-hall #19 + M15**. Also `[D21]` (co-op / human-GM multiplayer on regular campaigns = author decision; reuses ratified D12/D5/M15). **Training-hall #19 put officially on the pre-F1 list.** **Then the A5 FOLD-INS:** `[BAT-14..16]` RESOLVED (BAT-14 flat per-rank block may touch combat+derived+F14 stats; **BAT-15 two pools** charges+endurance; **BAT-16** exhausted-repairable default + campaign/per-battalion `disband` override; host-death→`[DTH]`) + **F14/`[STM]` RATIFIED** (owner took author-extensible stats, STM-3 path, not hardcode-Charisma; Phase B reserve now unconditional). **Then the pre-F1 KEYSTONE — `[TCV-1..6]` RESOLVED** (typed campaign-variable store; OPEN→RESOLVED): substrate wins (the **modifier system + tags + REQ-16 already exist**) → mostly assemble. **TCV-1** `CampaignVars` store of **bool/int/enum** at **campaign+map scope**; **TCV-2** registry tunables `{exposed: locked|start|mid_run}` over built-in knobs + custom vars, mid-run via **player options menu + `[MET]` `set_var`**; **TCV-3** (owner) scope effects via the **`[REQ]` predicate system** (add a group-membership `in_group`/`has_trait` predicate over an author-assignable per-unit `groups` field) — stat effects = **`add_modifier` at spawn** with a `[REQ-16]`-formula delta; **TCV-4** = **both** a flag/predicate `ObjectiveCondition` type **and** an `end_map` action + **event-driven re-check** (resolves the objective-extensibility/dialogue-win-loss pin + `[DTH-10]` custody). **Then Training Halls #19 → `[THL-1..7]` RESOLVED:** generalizes `[PXP-9]`'s `{benefit,amount,cost}` offer to all benefit types (class XP→`add_exp`, weapon XP→`[PXP-9]`, skill→`earned_skills`+`[LDC]`, **stat→a new shared `apply_permanent_stat_gain` primitive also backing FE stat-booster items**); **two-scope resource model** = roster multi-resource wallet (`party_gold`+author resources e.g. activity points) + per-unit F7 pools (motivation); PHB cadence + per-offer caps; **also sells attack `source`s/`style`s (→`[CEX]`/`[STY]`+`[LDC]`) and `[THL-8]` buy-recruits** (4 modes: grunt · authored · parametrically-generated via a **shared generator with `[BEA-5]`** · ransomed prisoner via `[RCR-3]`); IS the `[PVP-3]` buy-phase. Then `[THL-2]`/`[THL-8]` (source/style purchase + buy-recruits 4 modes), the **mini-game module seam** pin (don't block arbitrary minigames; `[SAC]` forward-note + scope #23 split + an **AGENTS.md open-registry-over-closed-enum** architecture principle), and **`[DSP-6/9/11]` firmed → `[DSP]` CLOSED** (authorable `drop_range`; action-economy confirmed; reserve→Phase B). Plus two **displacement/spawn policy decisions** (owner Q on shove-destination): **AoE-shove order** (`[DSP-15]` inv.7 — farthest-from-origin first along the push vector, roster-order tie-break) + **spawn occupancy** (`[MET]` `spawn` note — `on_blocked = nearest_free→delay`, author-overridable), and a code finding (`GameMap._spawn_unit` does no occupancy check → atlas Phase C). **THE DEFINE-ALL SWEEP IS COMPLETE — every A5 cluster + fold-in + keystone + residual RESOLVED; all decisions live in their registers/atlas (agent-agnostic).** ~24 commits. **NEXT: Phase B — F1 schema-lock** (collate the now-large reserve list in atlas §3b, sanity-check, lock) → Phase C builds.
- [2026-06-27c](2026-06-27c.md) — **Thought-experiment: optional hex-grid rule → new register `[HEX-1..9]` RESOLVED + build PARKED for triage.** Researched the spatial layer (keystone seam = `GridManager.DIRS` + the ~8 inlined Manhattan sites that must collapse into one `tile_distance()`; move cost already topology-agnostic; Godot `TileMapLayer` does hex natively). Walked all 9 one at a time: **HEX-1** topology = authored content, **campaign-default + per-map override** (mirrors `[DSP-17]`), NOT a live flip (square terrain doesn't map 1:1 → dual-authoring); **HEX-2** offset `Vector2i` stays the stored identity, axial/cube only inside the seam; **HEX-3** park BOTH phases (Phase 1 = centralize `tile_distance`/neighbour accessor · Phase 2 = `HexTopology`) for the re-evaluation/triage phase, only HEX-9 acts now; **HEX-4** range stays `tile_distance ≤ N` + own balance pass (6-vs-4 coverage inherent); **HEX-5** `CampaignRules.grid_topology` default + `MapData` override (`""`=inherit); **HEX-6** pointer-primary, native 6-key → `[ICD]`; **HEX-7** separate hex tileset, art+orientation deferred; **HEX-8** maps authored fresh, no auto-convert; **HEX-9** the one action now = forward-ward (seam comment at `GridManager.DIRS` + `[DSP]` forward-note so new direction code doesn't hard-code 4-way). Side detour — **A5 walk still NEXT** (see 2026-06-27b §4). INDEX/REGISTERS regen, 19 checks + full test suite green. Commit `f35a78b`.
- [2026-06-27b](2026-06-27b.md) — **AI / prediction model review → AI Combat Valuation / Engagement Brain walked, `[VAL-1..13]` RESOLVED (the keystone PER-4/PER-12/CVR-4/RCT-1 waited on).** Reviewed today's AI (flat `match ai_profile`; nearest-target; greedy two-step; never reads the forecast; activation = placement order). Broad end-shape walk → new register `[VAL]`: **A** whole-action scoring (`{move_tile,action,target,weapon}`, weapon-select free) · **B** configurable `search_depth` (negamax seam now, depth=0 v1, leaf = forecast + post-move-exposure; recursion + the triple-use pure **forward-model `[VAL-4]`** staged) · **C** score = **F16/REQ-16 term-tree** + engine **forecast term-sources `[VAL-6]`** (`expected_damage`/`would_kill`/`damage_taken`/`post_move_exposure`/`target_value`, REQ-15 pattern; default sum shipped AS a tree) · **D** **author-selectable `ai_activation_order ∈ {fixed, priority_sort, greedy_best_first, random}`** — build all four; greedy reuses the scorer + needs no forward-model; random = **seeded Package-A shuffle**. Plus `[VAL-7]` closes PER-4 (`ai_target_weight` = Stage-2 multiplier); `[VAL-10]` the **AIP pure-planner seam is the unbuilt prerequisite**; `[VAL-11]` non-combat actions fold in, **dancer-timing = the hard edge (PARTIAL)**; `[VAL-12]` wires CVR-4/RCT-1. **Thread-(b) deep-dive (same session): `[VAL-6]` = REQ-15 applied to combat** (delegate to a new `CombatResolver.forecast_outcome` API — naive trees overcount via strike-series early-exit; `preview_combat` gains a `{from_tile,weapon}` ctx; forecast terms context-scoped like REQ-3 subjects; `would_kill` needs no floor-delegation) **+ `[VAL-11]` dancer-timing RESOLVED** (greedy handles the consequence with no forward-model; v1 = a bounded enablement heuristic, complete fix scales with `search_depth≥1`). Cross-refs into PER/CVR/RCT/AIP/REQ. **Also this session:** AIP design review (AIP-1..16 sound/unbuilt; **one valuation path** — `nearest`/`weakest` = default `[VAL]` scoring presets; activation-order = faction-phase layer above the AISpec; AIP-14↔`[VAL]` cross-ref); **parked** the requirement-driven-dynamic-shops + dialogue forward-idea (SHP §4 + atlas A5); **pre-A5/economy orientation** banked in §1e. INDEX/REGISTERS regen, 19 checks green. 5 commits. **NEXT: START THE A5 WALK** (last design cluster before F1 — difficulty/Casual-Phoenix, Bonus-EXP, Arena, PvP, loadout cap, death-inventory disposition, `[AGT §6]` non-combat EXP, shop/activate configs; fold in `[BAT-14..16]` + optional F14; then Phase B F1 lock). VAL build = separate build-track, not blocking A5.
- [2026-06-27a](2026-06-27a.md) — **Prediction-engine review → full perception/masking walk `[PER-1..12]` RESOLVED + contest-system durability fix.** Answered the 26j detour. Prediction engine = `CombatResolver.preview_combat()` (:553); the hide-from-forecast/fire-for-real split already exists (`SkillHandler.apply_trigger:172`). **Correction:** the AI does NOT read the forecast today (`EnemyAI._find_nearest` = nearest-target, no scoring) → forecast-fidelity AI lever is downstream of the valuation AI `[CVR-4]`/`[RCT-1]`. New register `[PER-1..12]`: 3-stage pipeline (enumeration/valuation/forecast); knob on the perceived unit; **PER-7** reveal=union (no precedence); **PER-8** occupancy in v1 (`around|through`+`on_cross`/`on_stop`, reuses `[DSP-1/2/12/14]`); **PER-9** forecast fidelity = two-channel CampaignRules constant (A=humans see, B=AI evaluates, may be equal) + debug `forecast_reveal_all`, sibling of `[FOW-3]`; **PER-10** filters projection only, never `resolve_combat`; **PER-11** no softlock + **two-hook finding** (mask both `_living_hostiles_for_faction` AND `get_attackable_enemies_from_tile`); **PER-12** detection vs appraisal = two F16 contest axes (one term=unified sight, two=sight/insight). **Durability:** the pierce/stealth contest = `[REQ-10]`/F16 (re-derived 3× before found) → bidirectional PER↔REQ↔FOW cross-refs + DO-NOT-RE-DERIVE callout + 2 auto-memories. **Guide:** map_authoring "Deceiving the player" (swap-teleport / MET condition-change). PER RESOLVED bar inert PER-4. 5 commits, 19 checks green. **NEXT: clear context, then review/improve the AI/prediction model (the pre-A5 valuation AI `[CVR-4]`/`[RCT-1]` that PER-4/PER-12 wait on).**
- [2026-06-26j](2026-06-26j.md) — **Context-clear handoff + pending prediction-engine / forecast-fidelity detour (GROUNDED).** Prep entry (no design work). **State:** interceptor family (`[RDR-1..14]`/`[CVR-1..7]`/`[RCT-1..6]`/`[ICP-1..6]`) fully walked + RESOLVED; all forward-reqs on the atlas; standing next-up = pre-A5 candidate path (the family was an owner detour from candidate G). **Pending detour (ask after clear):** how the prediction engine works + can features be hidden from the prediction without hiding them from reality, for (a) AI-dumbing on easier difficulties + (b) baiting enemies into traps. **Grounding banked:** prediction engine = `CombatResolver.preview_combat()` (scripts/core/CombatResolver.gd:553) + `_collect_combat_modifiers(preview, dry_run)` (:134) + `AttackPreview.gd`. **KEY FINDING — the split ALREADY EXISTS:** `preview=true` applies deterministic skills but **excludes random-activation skills from the forecast while they still fire in real combat** (CombatResolver.gd:557–561); `dry_run=true` = no use-counter persistence. So "hide from prediction, not reality" is an established pattern, not new capability — the detour = generalize it (a `forecast_visibility` knob; 3 audiences = AI-eval / player-display / both; resolution stays authoritative). Connects to `[ICP-4]` (forecast dry-run), `[CVR-4]`/`[RCT-1]` (AI reads same forecast → fidelity = AI capability), `[STY-10]`, `[REQ-15]`. **NEXT: answer the detour, then pre-A5 path.**
- [2026-06-26i](2026-06-26i.md) — **Interceptor-family gap-audit — 3 composition closers + `[ICP-1..6]` residuals.** Audited the family (`[RDR]`+`[CVR]`+`[RCT]`) via "what operations on an incoming effect / what events does it miss." **Closed by composition:** `[RDR-14]` **`gain`** clause (symmetric to RDR-13 cost → lifesteal/charge/absorb-into-pool), `[RDR-2]` **`emit.kind`** (damage→heal vampiric reflect, debuff→buff), `[CVR-7]` **`share_disposition: negate`** (in-place ward — reduce an ally's hit, nobody takes the rest; `1.0+negate` = reactive full block), `[RCT-6]` board-re-eval rule (each phase sees prior-phase mutations). Operation set now complete (block/reduce/reflect/redirect/absorb/convert/split/copy/ward) **bar *delay***. **Residual gaps `[ICP-1..6]` (closure paths, mostly forward-reqs):** ICP-1 per-strike vs per-exchange (`trigger_scope` + `exchange_id` latch — needs event ids); ICP-2 universal effect event (nullable `source`, sharpens RDR-7); ICP-3 delayed release (composable half via `gain`→`stored`+F11; auto-release deferred to MET); ICP-4 forecast pure dry-run of all 3 phases (extend REQ-15 projection API); ICP-5 undo transaction (declare→react→resolve = atomic, F1 reserves); ICP-6 layered cover (deferred, minor). None reopen the core model. RDR→1..14, CVR→1..7; new register `[ICP-1..6]` RESOLVED; candidate I + atlas F5 sharpened. INDEX/REGISTERS regen, 19 green. 1 commit. **NEXT: family fully walked+audited; return to pre-A5 path.**
- [2026-06-26h](2026-06-26h.md) — **`[RCT-1..6]` reactive-reposition (phase-0) RESOLVED — the interceptor family = 3 phases.** Owner scenario (attack the healer → adjacent general **swaps places** with it → attack resolves fully against the general incl. evade/skills/**counter** → general stays put). **Reframe:** this is **not cover** (cover keeps the ally's evade) — it's a **reactive `[DSP]` position-swap before resolution**, after which normal combat runs against the tile's new occupant, so full substitution incl. counter is **automatic** (no special hook); "stays in the square" = the DSP move persists. **The family is 3 phases** (same STY-9 selector + REQ/event trigger + RDR-13 cost, differ by hook timing): **phase 0** `reactive-reposition` `[RCT]` (full sub + counter) → **phase 1** `cover` `[CVR]` (mitigation only, ally's evade, no counter) → **phase 2** `redirect` `[RDR]` (emit elsewhere); "how much substitutes = which phase." **One new cost (RCT-1):** a NEW pre-resolution reaction trigger → bumps the **F11 no-new-triggers discipline** → build-time sign-off; forecast must show post-swap defender. **Follow-ups:** retarget to an attacker-allied target one-time = RCT forced swap (new target evades + **can counter**) OR RDR full-absorb+emit (damage only, no dodge/counter); new defender **can counter** via swap, not via redirect-emit. Everything else = DSP + STY-9 + REQ + RDR-13 reuse. New register `[RCT-1..6]` RESOLVED; candidate I + 3-phase family note; CVR/RDR boundary+recipe notes; atlas F11 flag. INDEX/REGISTERS regen, 19 green. 1 commit. **NEXT: interceptor family fully walked (RDR+CVR+RCT); return to pre-A5 path.**
- [2026-06-26g](2026-06-26g.md) — **`[CVR-1..6]` cover walk RESOLVED — per-hit intercept, distinct from provoke.** Walked the `cover` sibling (pre-application reassignment). **Two findings:** (1) cover hooks a **different `CombatResolver` phase** than redirect — **pre-mitigation defender-swap** (so the protector's own DEF/RES mitigates the original hit) vs redirect's post-mitigation emit → new forward-req for an earlier hook; (2) **whole-exchange cover = `[PRV]` provoke** (aggro/diplomacy), so **per-hit** keeps cover purely defensive + complementary. **Owner calls:** substitution = **per-hit** (to-hit vs the ally, **mitigation+HP vs the protector**; asked for + got a full provoke explanation first); scope = **damage + conditions + displacement** (displacement = shove vector re-applied to the protector from its own tile via DSP, all-or-nothing). **Resolutions:** CVR-1 pre-mitigation hook upstream of redirect, reassignments non-coverable (one hop); CVR-2 default full + `share` term; CVR-3 `[STY-9]` selector + deterministic priority + **`cover_if_lethal`** flag (default sacrifice) + RDR-13 resource gate; CVR-4 protector's mitigation vs original attacker, **kill-credit+EXP→attacker**, forecast shows protector as defender (AI reads same); CVR-5 rides `[RDR-8]` snapshot-then-resolve, ally unharmed; CVR-6 damage/conditions/displacement. Reuses RDR-12 `event` binding + RDR-13 cost model + STY-9 + one-hop termination. Atlas F5 (pre-mitigation defender hook) + A5 (+`[CVR-5]` cover-sacrifice) + candidate H updated. Register **RESOLVED**; INDEX/REGISTERS regen, 19 green. 1 commit. **NEXT: interceptor family fully walked; return to the pre-A5 path; confirm forward-reqs at build time.**
- [2026-06-26f](2026-06-26f.md) — **`[RDR-1..11]` full walk RESOLVED + owner-added `absorb` + sibling `[CVR]` cover spawned.** Walked the redirect register and all edge cases. **Owner scope-calls:** (1) **spec a sibling `cover` primitive now** (pre-application reassignment — "take the hit *instead of*", NOT a redirect mode); (2) **include non-scalar displacement/forced-move** in v1 (per-kind transform, DSP coupling); (3) DoT-tick = **direct-apply preset, per-tick opt-in** (needs conditions-store-source). **Owner-added `absorb` (RDR-10):** a `[REQ-16]` term removes a portion off the holder pre-application (`0`=thorns, `full`=**parry/full-reflect**; `absorbed_value` term var; non-scalar=all-or-nothing). **`absorb_on_unavailable` (RDR-11):** owner chose **proportional** default = `absorb × N_valid/N_intended` (by count; `N_intended==0`→0 absorb). **Model = interceptor with a deterministic spine:** intercept→predicate→resolve target-set→compute absorb+redirected→gate absorb→holder takes `incoming−absorb`→emit to valid targets (non-redirectable, one-bounce)→**death snapshot-then-resolve**. RDR-1 full-to-each+`target_count`; RDR-3 post-mitigation read + target pipeline (`[REQ-15]` floor) + misses-don't-fire; RDR-4 anchor `{source,holder}` (dropped `tile`), selector=**`[STY-9]`**; RDR-8 → A5 "simultaneous deaths": snapshot-then-resolve + deterministic disposition order + mutual kills both die + **kill-credit→holder even if dead**. **Forward-refs wired to atlas:** F5 row (uniform source-bearing `effect_applied` event + `redirectable` flag + conditions-store-source), A5 death-disposition bullet, STY-9 (shared selector). New OPEN register **`[CVR-1..6]`** (cover, to walk next). Candidate G updated + **new candidate H** (cover). **Then (same session) composition stress-tests added `[RDR-12]` `event`-context binding** (forward-req on F16/REQ — `event.kind/damage_class/magnitude/is_crit/range/source/condition_id/stacks`; the one new vocab surface) **+ owner-added `[RDR-13]` resource coupling** (`cost: {pool, amount(term), subject}` reusing the **F7/candidate-A** cost-pool model — one clause = uses · scaled mana-cost · depleting barrier `amount: absorbed_value`; gate=pure read, drain=class-2 side-effect; works on existing HP/item-`uses` pools now); five worked recipes recorded as build acceptance tests; spine gained a resource-gate + deplete step; atlas F7 + F16 forward-refs added. Register → **RDR-1..13 RESOLVED**. INDEX/REGISTERS regen, 19 green. 2 commits. **NEXT: walk `cover` `[CVR-1..6]`.**
- [2026-06-26e](2026-06-26e.md) — **`redirect` primitive walk — candidate G firmed + `[RDR-1..9]` opened.** Walked the owner's "reflect" effect primitive (discuss-before-A5). **Model: an effect INTERCEPTOR** — subscribes to incoming effect-application events on its holder and, for events matching a `[REQ]` predicate, emits a **transformed effect at a selected target-set**; "reflect" (bounce at the dealer) is just the **preset `target = {source}`**. **Owner end-shape calls:** subject = damage · conditions · other combat effects (general); carrier = **any source** (status/equipment/class all GRANT one effect — granted-source pattern); `fires_on_death` = per-effect author flag (default dying-thorns). Then **"reflect to people matching criteria"** → promote `target` from constant `source` to a **target-selector** (`anchor` + spatial `scope` + `[REQ]` predicate, composing `GridManager._tiles_in_range` + `_get_units()`); owner **named it `redirect`**. **Three new engine rules:** read **post-mitigation actual** + apply through target's pipeline (`[REQ-15]` floor); **one-bounce non-redirectable flag** (termination — kills thorns-vs-thorns + radiate cascades); death deferred to safe point → fires per flag → **A5** resolves order (so `redirect` is a **co-input to the A5 death-disposition decision** = why pre-A5). **Verified the real cost:** `ConditionManager.gd` is a 37-line no-op until **M8** and `Unit.take_damage` carries **no source** → general `redirect` requires the M8 build to expose **(a)** a uniform source-bearing `effect_applied` event + **(b)** a shared unit-selector (same one the spell/style AoE needs); hook lives in `CombatResolver`. Determinism **class-2**. Disambiguated from the `[DLG-9]` visual reflect. Candidate G rewritten; **NEW register `[RDR-1..9]`** (OPEN); INDEX/REGISTERS regen, 19 green. 1 commit. **NEXT: `[RDR]` build-walk before A5 — raise RDR-7 (M8 foundation) + RDR-8 (A5 ordering) against those owners now.**
- [2026-06-26d](2026-06-26d.md) — **`[EXT]` walk COMPLETE — EXT-5 cadence + EXT-6 DLG/F4/MET confirmation; register `[EXT-1..6]` CLOSED.** Finished the remaining extensibility questions. **Headline finding: "one model = A" was already the convergent design, not an imposition** — `[DLG-3]` effects self-describe as adding ids "without format change, **mirroring the F4/F5 profile philosophy**"; F4 profiles are the **reference A impl** (`linear/sigmoid/table` + author-custom; `table` = arbitrary author curve as data); `[MET]` = declarative `trigger→guard→action-list` composition. **EXT-6 per-vocabulary confirmation:** none of F4 / DLG effects / MET has a genuine B-requirement — every B-temptation (e.g. DLG custom easing) resolves to "ship a primitive" or "use the `table` pattern"; MET side-effects live in the **engine action primitives** (run deterministically at safe points, RNG via Package A), the author composes but never authors mutation logic. **Substantive refinement (amends EXT-4): determinism is per-OUTPUT-PATH** — (1) decision/predicate (REQ·MET guards·`chance`) = pure+fixed-point+PkgA; (2) state-mutation (MET actions) = ordered engine primitives+PkgA RNG; (3) presentation (DLG visuals) = **determinism-EXEMPT** (gating rides REQ) → an F4 profile feeding `chance` odds inherits fixed-point, a DLG cosmetic doesn't. **EXT-5 cadence RESOLVED:** primitives ride normal release cadence; **3-way triage** (compose-recipe / new-primitive-batched-with-its-composition-as-spec / B-tail logged as evidence); the **volume of the B-tail is the trigger to revisit the deferred B-exception**. Register **CLOSED** (frontmatter `Status: RESOLVED`); atlas F16 EXT note → fully resolved; INDEX/REGISTERS regen, 19 green. 1 commit. **`[EXT]` now fully off the pre-A5 path; NEXT: `reflect` candidate G discussion → A5 → F1.**
- [2026-06-26c](2026-06-26c.md) — **`[EXT]` walk — EXT-1..4 + EXT-5 mechanism RESOLVED off the REQ-16 worked example.** Owner asked to walk the parked author-extensibility questions and "see how many have obvious answers now." Because `[REQ-16]` (the 2026-06-26b math walk) deliberately exercised every EXT dimension, most are pre-answered. **Verdicts:** **EXT-1** model = **A ("A-plus": data-composition; B a narrow evidence-gated future exception; C rejected)** — DLG effects / F4 profiles (`table`=arbitrary author curves as data) / MET already behave as Option-A today; **EXT-2** A preserves **no-code** (compositions = data trees; string front-end = build-time sugar emitting a validated tree); **EXT-3** **structural load-validation against the engine primitive registry, fail-loud** (REQ-16 rules = the uniform template); **EXT-4** **pure + side-effect-free; only RNG = Package-A latched `chance`; fixed-point** for cross-platform determinism (the pure/impure split governs inlining); **EXT-5** mechanism = **named-composition relief valve + primitive-request channel that doubles as a contributor on-ramp**; **EXT-6** direction = **one model = A uniformly** (no vocabulary has a genuine B-requirement). **Tally: 4 obvious (1–4), 1 mostly (5), 1 directional (6).** **Owner call: ratify 1–4 + EXT-5 mechanism** — EXT-1..4 `[RESOLVED]`, EXT-5 `[PARTIAL]` (cadence/process still OPEN), EXT-6 `[OPEN — narrowed]` to a per-vocabulary confirmation (DLG effects · F4 profiles · MET adopt A). Register stays OPEN but the **remaining walk is much lighter** and **likely off the A5 critical path** (was a full dedicated session). Register frontmatter/Status + candidate-direction-A=✅CHOSEN + atlas F16 EXT note updated; INDEX/REGISTERS regen, 19 green. 1 commit. **NEXT: light EXT remainder (fold into vocab walks) + `reflect` candidate G discussion → A5 → F1.**
- [2026-06-26b](2026-06-26b.md) — **Author-extensibility research + `[REQ-16]` arithmetic-value-term walk.** Owner asked to research the parked `[EXT-1..6]` extensibility options and explain each, then walked a concrete sub-system to a full v1 spec. **Research:** weighed the three `[EXT]` directions against the two binding constraints (no-code data+art packs; Package-A determinism) — **A** data-composition / **B** sandboxed-expression / **C** engine-only — and recommended **A as an "A-plus" hybrid** (ship A; keep B as a narrow evidence-driven future exception reusing A's eval tree; the A/B line is **porous** — most "B-only" examples dissolve into "ship one more primitive"). **`[REQ-16]` arithmetic value terms** (RESOLVED 2026-06-26) — basic math for the F16 vocab ("STR+MAG > RES", "A/B > C^D"), built **inside Option A** as a recursive compound **data tree** (not a parsed string) so it stays no-code + deterministic. Owner calls: **fixed-point ×1000** (bit-deterministic across desktop/web/lockstep — float `pow`/`div` desyncs; also restores safe `==`), ops `add/sub/mul/div/pow/min/max/abs/neg` + number-domain booleans `not/and/or/truthy` (`>0`=true, output `1.0/0`); **`pow` integer-exp only** (`0^0≜1`; fractional deferred); **half-up rounding** w/ per-node `floor/ceil` (generalizes the owner's div-round-up/down idea); **required explicit `on_zero`** on `div` (`to_max/to_zero/to_value` — a single global sentinel is *wrong*; builder forces the choice, omission = validate error; no undefined-propagation); **comparisons (`gt/lt/eq…`)/`xor` = named compositions** (derive from `{not,truthy,sub,abs}`); **predicate-bridge DEFERRED** → **flag-upstream pattern** (predicate writes `[F6]`, term reads `0/1`); terms substitutable in numeric value slots incl. **REQ-10 skew `input`/`base`** (subsumes `difference|ratio` → author **custom contest curves**, still pure, no new RNG surface) + **effect magnitudes**; **complexity budget = author-declared FULL HEADROOM** (F4 `max_formula_depth`/`max_formula_nodes`, no universal mobile ceiling — depth alone is cheap, node-count is the real proxy; only an **absolute safety ceiling** + **iterative evaluator**; guardrail is **social** — warn players, request a primitive, **join the dev team** = the primitive-request channel doubles as a **contributor on-ramp**); tree-builder v1, string front-end later parses-to-tree (still Option A). **First `[EXT]` worked example** (recorded in the EXT register; does NOT pre-decide EXT-1 for DLG/F4/MET). F16 `REQ-1..15→REQ-1..16` (still RESOLVED); REQ-10 amended; atlas F16 row + EXT note; INDEX/REGISTERS regen, 19 green. 1 commit. **NEXT unchanged: `[EXT]` walk → A5 → F1** (REQ-16 settled only the math case).
- [2026-06-26a](2026-06-26a.md) — **F16 predicate vocab extended — two-value `compare` `[REQ-9]` + comparison-skewed `chance` gate `[REQ-10]` + item-property terms `[REQ-11]`.** Owner added (1) deterministic comparison of two *dynamic* values and (2) a simple chance gate skewed by a comparison. **`[REQ-9]`:** a reusable **value term** = `{subject, source}` (`level`/`stat:<name>` string-keyed/`proficiency:<track>`/`item_count`/`skill_count`/`gold`/literal) + a **`compare(lhs term, op, rhs term)`** with both sides dynamic and different subjects allowed ("my STR > your DEF", "more sword skill than you"); generalizes the REQ-2 constant predicates (those = the literal-rhs case). **`[REQ-10]`:** a **`chance`** gate — the one impure predicate — `success% = base + F4 skew profile(operand(lhs,rhs))`; owner calls: skew = **author-configurable CampaignRules F4 profile** (linear default/sigmoid/table), operand = **author's choice difference|ratio**, timing = **roll-once-and-latch**. Routes through **RngService/Package A** (seeded, rewind-safe; never engine RNG; not built until PKGA lands); latch rides `[DLG-11]` `visited_trail`/`[F6]` → save/reload/rewind can't re-roll (no save-scum). Displays odds, evaluated on commit not preview. A **general persuade/steal/contest** primitive (mirrors hit math), not dialogue-only. **`[REQ-11]`** lets terms/predicates read **item properties** of an **equipped/held/targeted** item (targeted = context-resolved): `[IEQ]`-grounded vocab (mt/hit/wt/cost/`combat_family`/`effect_tags`…) — numeric/string via `compare`, sets via `item_has_tag`, identity via `item_is` ("my weapon's Mt > yours", "targeted item is a sword"). `[REQ-12..14]` (vocab-completeness pass): **REQ-12** HP/F7-pools(`pct`)/per-map-ability-availability/STY-style-availability/identity sources; **REQ-13** owner folded in **all four gap families** combinations couldn't express — **spatial** (adjacency/distance/terrain/region) · **runtime-state** (acted/moved/paired/carried, TurnManager/PairUp/Carry) · **`[REL]` support-rank** between units · **aggregate `count(set, requirement) op N`** (generalizes any/all); **REQ-14** active-condition `condition_potency`/`duration`/`count` over F5 — **potency pins a forward-requirement on the F5 status model**. **REQ-15** generalizes to **any `condition_param`** + a **F5-delegated outcome projection** (`condition_would_kill`/`would_floor`/`next_tick_damage` — "will the poison kill or leave at 1 HP"; delegated because the lethal/floor + tick math live in F5, so a naive `potency≥hp` is wrong) → **paired forward-req on F5**: expose params introspectably + a lethal/floor param + a next-resolution projection API (the same one the damage-preview UI uses; noted on the atlas F5 row). No new save surface. `[REQ]` REQ-1..8→**REQ-1..15** (still RESOLVED); atlas F16+F5 rows + Last-verified 2026-06-26; INDEX/REGISTERS regen, 19 green. Commits `3dbc514`/`34ce497`/`de481f7`/`c09fe4b`. **End-of-session loose-ends audit:** flagged that **"author-extensible"** (REQ vocab + DLG effects + F4 profiles) is an unpinned promise given the no-code data+art content model → new **OPEN register `[EXT-1..6]`** (`authoring_extensibility_open_questions_2026-06-26.md`; 3 directions: data-composition / sandboxed-expression / engine-only + content-fit/validation/determinism/cadence/one-model-for-all) — **owner scheduled it as a DEDICATED walk NEXT, before A5.** Parked future-discussions in EXT: dialogue editor `[DLG-8]` timing, F16 v1 build-scope, chance-gate→contest feature, REQ-15 projection↔F5↔preview sharing. **TRAJECTORY NOW: `[EXT]` extensibility walk → A5 (incl. shop/activate) → F1 schema-lock.** **NEXT: A5 walk** (incl. shop/activate) → F1; sweep owes F14 + F15 slice + **F16 vocab (incl. compare/chance, chance gated on Package A)** + `[BAT-14..16]`.
- [2026-06-25r](2026-06-25r.md) — **Shared Requirement/Predicate system = foundation F16 `[REQ-1..8]` + dialogue branch gating `[DLG-14]`.** Owner asked how conversation history is stored, how branching is authored, and how hard it'd be to gate branches on conditions (flags · unit ids · class/proficiency/stat level · skill/trait · items held/equipped/convoy · …). **Findings:** history = two levels (transient `[DLG-11]` `visited_trail` for resume + persistent `choice` `set_flag`→`[F6]`; no transcript); authoring = labeled segments + `choice`→`goto` (`[DLG-2/5]`, visualized by the `[DLG-8]` editor); gating = **low-to-moderate** difficulty. **Key insight: those conditions are NOT dialogue-specific** — same set MET (`[MET-4]`), `[VIL-6]` tile-action requirements, `[RCR-4]` recruit eligibility, `[IEQ]` `req_flags`, objectives all want → firmed **ONE shared author-extensible Requirement foundation F16**: an `all/any/not` tree of typed predicates, each a ~1-line read over an existing accessor (`level`/`skills`/`get_effective_stat` string-keyed/`weapon_rank_for_wexp`/`inventory`/`[CNV]`/`[F6]`); per-predicate **subject selector** (speaker/participant/unit/party — owner: both modes); renders to text for "[Requires: X]". **Net-simplifying** (replaces scattered ad-hoc condition code), reads reserved state → **no new save surface**. Owner calls: shared (not local) · subject both modes · granularity **option+segment+whole-conversation** · gated-out = **per-option hidden|shown-disabled** (`[VIL-6/7]` model). `[DLG-14]`: dialogue gating consumes F16; conversation-level reconciles with the launching MET condition (no dup). Consumers adopt non-breakingly (not relitigated). New register `[REQ-1..8]` (F16); `[DLG]`→DLG-1..14; atlas F16 row + critical-path; INDEX/REGISTERS regen, 19 green. Commit `867b0f3`. **NEXT: A5 walk** (incl. shop/activate) → F1; sweep now owes F14 `[STM]` + F15 v1 build-slice + **F16 `[REQ]` v1 vocab** + `[BAT-14..16]`.
- [2026-06-25q](2026-06-25q.md) — **F15 dialogue end-user shape fleshed `[DLG-1..10]`.** Owner asked to define the dialogue *viewing* shape now and see what it forces on data/tooling. **Verdict: ONE full-screen overlay, ONE renderer** — a layered **scene region** (background = **live map (toggleable transparency) | authored special-bg** + animated portraits) over a **script-style chat-log**; because the background can BE the map, battle one-liner and story scene are the same overlay (collapses the in-map-box-vs-scene fork). **It made the data/tool calls obvious:** data = a **flat addressable entry list** (`line`/`choice`/`command`/`label`; background change = a `command`; branching = `label`+`goto`; F13 keys) → **plain data now + a dedicated conversation editor later** (the dialogue answer to deferred 4a–4e GUI-vs-JSON). **Three-tier effect taxonomy** (owner expansion): **(A)** character expression · **(B)** generic portrait transforms (flip/mirror=simple inversion, move, scale) · **(C)** scene-wide filters (rain, fog over characters, wavy flashback) — each `loop | once | loop_until<cond>` (loop_until headline = **mouth-flap until text finishes scrolling**) + a `speed` param (+ possible global animation-speed setting); composable, author-extensible. **Branching choices** first-class; **pacing** author-configurable (manual / skip-to-decision / auto). **Build STAGED** (v1 slice + full reserved). **Reflect effect `[DLG-9]` + stage layers `[DLG-12]` + speaker-decoupling `[DLG-13]` ALL walked same session** — reflect = a two-mode (`in_place` facing-flip | `copy` mirror/pool) LIVE mirror, both scopes, full params (rotation = build-time investigate); interaction matrix resolved by ONE fixed live pipeline (`asset→A expr→B transforms→reflect→layer→C filters`); edges: in_place-same-axis-as-flip=no-op, copy recursion depth-1, scene-reflect suspended during transitions. `[DLG-12]` explicit per-element/per-effect z-order (`set_layer`); `[DLG-13]` stage holds speaker-independent **stage elements** (can render animated non-speaking entities) — portraits are the common case, not the model. Plus mid-conversation **`[DLG-11]` "between speaker" save** (entry-boundary cursor `{conversation_id,cursor}`, last-speaker derived, log replay-derivable / `visited_trail` for branching; reserves `conversation_resume`). New register **`[DLG-1..13]` ALL RESOLVED**; `[RCV-1]` points here; atlas F15 row + critical-path upgraded; INDEX/REGISTERS regen, 19 green. Commits `88f8cab`/`702a3f7`/`eab2924`/`1c3401f`. Also **shop/`activate` interactive-trigger configs FOLDED INTO A5** (`3ca1ad4`; economics `[SHP]`/`[CNV]` already firmed). **NEXT: A5 walk** (now incl. shop/activate configs) → F1 schema-lock; sweep owes F14 `[STM]` + **F15 v1 build-slice** + `[BAT-14..16]` (+ a DLG rotation investigate).
- [2026-06-25p](2026-06-25p.md) — **A4 COMPLETE — provoke `[PRV-1..7]` + capture-victory `[VIL-9]`.** Finished the A4 walk (all four sub-clusters firmed across n/o/p). **Provoke / runtime faction-relationship MET action** (`[STY-17]` transition side), end-shape-first. **Verdict: one transition primitive, two callers** — `[STY-17]` owns the matrix/resolver/data; **PRV owns the runtime override store + one `set_relationship(from,to,stance,scope)` primitive**, called by the **`set_relationship` MET action** (scripted) + a reactive **`provoke_on_attacked`** stance flag (not two systems). Owner calls: granularity = **faction-pair + per-unit override** (two-layer store; one attacked villager turns hostile while its faction stays neutral; resolver gains a unit-aware entry); persistence = **map default + campaign option** (mirrors F6); reactive provoke = **pin the hook here**. PRV-3 directed action (`mutual` convenience), trigger-agnostic, beside `set_ai` · PRV-5 full 3-state set (provoke/make-peace/ally), counter-regardless-of-stance kept · PRV-6 **F1 reserves store = two layers × two scopes** · PRV-7 AI reads fresh per activation. **Distinct from `[RCR-1]` recruit team-flip.** AI initiate behavior firms with the AI pass. **Capture-victory `[VIL-9]`** (closes the `[DSP]` pin): no fork — `ObjectiveCondition.type=capture` over `unit_ids` satisfied by the `captured:<id>` flag (set by A2 capture-carry); mirrors `seize`/`defeat_boss`. New register `[PRV-1..7]`; `[VIL]`→`[VIL-9]`; atlas A4 **DESIGN ✅ COMPLETE** + F1 reserve line; INDEX/REGISTERS regen, 19 green. 3 commits (`d6654f9`/`d8243ec`/+note). **A4 net-new shared substrates:** `[VIL-2]` interactive-trigger · F15 dialogue · `[VIL-8]` objective removal-disposition · `[PRV-6]` relationship-override store. **NEXT: A5** (last cluster before F1 — difficulty/Casual-Phoenix, bonus-EXP, arena, PvP, loadout cap, the death/removal-disposition path co-owned by `[VIL-8]`+`[BAT-16]`+death-inventory, `[AGT §6]` EXP, battalion-EXP `[BAT-6]`) → **F1 schema-lock** (sweep owes F14 `[STM]` + **F15 dialogue v1 schema** + `[BAT-14..16]`).
- [2026-06-25o](2026-06-25o.md) — **A4 cont. — Recruit conversation side firmed `[RCV-1..6]` + dialogue foundation F15.** Second A4 sub-cluster, end-shape-first. **Verdict: the recruit conversation side is thin** — `talk` = a `[VIL-2]` interactive-trigger config (unit-targeted twin of Visit) + a **trigger-agnostic `recruit` MET action** over the `[RCR-3]` roster API + author-composed conditions. **Roster side `[RCR-1..7]` NOT relitigated.** **The substantial piece — the Dialogue/Conversation system — is NOT recruit-specific** (shared by recruit/village `[VIL-4]`/support `[REL-6]`/MCH/story); code check found NO dialogue system anywhere and F13 explicitly waited on "the dialogue-data format" → **pinned as new foundation F15** (🔶 rough end-shape, build deferred). Owner calls: dialogue = "pin a rough plan so the foundation isn't replaced"; talk direction = **author's choice per recruit**; damage-forfeit = **author-composed condition only**. Items: **RCV-1** F15 dialogue — conversation = id-referenced ordered **forward-compatible entries** (`line` v1 \| reserved `choice` \| reserved `command`), `line`={speaker, **F13 key (no concat)**, portrait}, played by a `dialogue` MET action; data format + hook now, presentation deferred, choice/command reserved so v1 line-only data survives · **RCV-2** `talk` = `[VIL-2]` config · **RCV-3** directionality author's-choice (`directed|symmetric`) · **RCV-4** `recruit` action trigger-agnostic (talk/village/turn/flag) over `[RCR-3]` · **RCV-5** damage-forfeit = author condition only, no engine rule · **RCV-6** no new save beyond `[RCR-7]`; `conversations_seen` flagged-on-demand. New register `[RCV-1..6]`; atlas F15 row + critical-path + recruit row + A4 note; INDEX/REGISTERS regen, 19 checks green. 1 commit `6412b7c`. **NEXT: finish A4** — `[STY-17]` provoke-MET action → Capture-victory (mostly absorbed by `[VIL-8]`) → A5 → F1. Sweep before F1 now also owes **F15 dialogue v1 schema** (alongside F14 `[STM]`, `[BAT-14..16]`).
- [2026-06-25n](2026-06-25n.md) — **A4 walk opened — Village #11 firmed `[VIL-1..8]` + the interactive-trigger keystone.** End-shape-first walk of the first A4 sub-cluster. **Verdict: a village = config over four reused substrates, NOT a bespoke system** — DTR destructible object (`Unit`-in-disguise, HP/Def, `object_broken`; `hp:1`=one-hit raze) + MET trigger→action runner + `[F6]` flags + a **new player-initiated interactive MET trigger**. **The keystone (`[VIL-2]`) isn't village-specific:** every existing MET trigger is a *passive* EventBus reaction; Village's **Visit** and Recruit's **`talk`** (`[RCR-3]`) are a new class fired from a `TileActions`/action-menu entry gated like Seize/Escape — code already reserves `shop`/`visit`/`activate` placeholder ids. **Visit + `talk` + reserved `shop`/`activate` are its configs — design once; Recruit's conversation side reuses it.** Items: **VIL-1** config-not-system · **VIL-2** interactive-trigger substrate · **VIL-3** geometry = author's choice → both models (occupied-object/adjacent-visit/DTR-raze + passable-tile/stand-on/`tile_seized`-raze) · **VIL-4** outcome = full growing MET action list · **VIL-5** F6 flags + `once` latch, no new save field · **VIL-6 (owner add)** terrain More-Info enumerates ALL tile actions + **required characteristics**; evolve `TileActions.is_available()→{available,requirement,hidden}` · **VIL-7 (owner add)** per-action **hidden/secret** (flag-revealable) · **VIL-8 (owner add + carried `[DSP]` Capture-victory pin)** objective removal-disposition rule (forward-pin): **Rout/Eliminate key on hostile-to-player presence, not death** — death/recruit-to-player = satisfied, **recruited-by-still-hostile = NOT**, **escape+story-removal = author `pass|fail`** (`fail`→defeat); cross-pins `[RCR]`/`[DSP]`/Escape/A5. New register `[VIL-1..8]`; atlas A4 row + village row; INDEX/REGISTERS regen, 19 checks green. 1 commit `278e914`. **NEXT: continue A4** — Recruit conversation side (reuses `[VIL-2]`) → `[STY-17]` provoke-MET → (Capture-victory mostly absorbed by `[VIL-8]`) → A5 → F1.
- [2026-06-25m](2026-06-25m.md) — **Battalion content/lifecycle re-OPENed `[BAT-14..16]`.** Owner asked to mark the battalion system to better define bonuses, resources, resource use/replenishment, destruction, and host-death. `[BAT]` register Status flipped **RESOLVED→OPEN** (architecture `[BAT-1..13]` stays RESOLVED, not relitigated); range now `BAT-1..16`. **Why re-OPEN not just defer:** `[BAT-15]`/`[BAT-16]` are persistent-state → must be in the define-all sweep before F1, not hand-waved at build. Items: **BAT-14** bonus content/shape (refines BAT-3 pipeline) — stat axes (7/derived/Charm-Command F14), conditional vs always-on (lean always-on flat block), rank scaling (lean per-rank table) · **BAT-15** resource model (firms the BAT-5 lean) — lean = **two resources**: `charges` (per-map gambit budget via `[AGT §5]`) + `endurance` (persistent attrition meter); charges refill per map, endurance via authored repair · **BAT-16** lifecycle (the gap) — destruction = `disband` vs `exhausted` (softer default); host death = **detach→unassigned pool** by default (harsh override `lost_with_host`), routed through the **single `handle_death` hook** the A5 death-inventory disposition rule set already mandates (sibling to dropped/convoyed inventory + `[DSP-5]`; no second death hook). Atlas battalion row + A2 note + F1 reserve updated. INDEX/REGISTERS regen, 19 checks green. **NEXT: A4 → A5** (death-disposition now co-owns `[BAT-16]`) **→ F1**; BAT-14..16 join the define-all sweep.
- [2026-06-25l](2026-06-25l.md) — **Author-extensible STAT MODEL pinned as foundation F14 (`[STM-1..5]`, NOT firmed).** Three feasibility Qs off the battalion pass: (1) battalion command/leadership check, (2) Charisma-equivalent stat, (3) author-defined stats. **Decisive finding: the stat model is already half data-driven** — read path is string-keyed (`get_effective_stat(name)`→`data.get(name)`, `Unit.gd:300`) + growths/caps/wexp are dicts, but **base-stat storage is hardcoded `@export` fields** and the **stat list is enumerated in ~5 literals** (`STAT_KEYS`, `_GROWTH_STATS`, `LevelUpScreen._STAT_NAMES`, `StatBreakdown.STAT_LABELS`, DataManager). **No Charisma/Command stat exists.** Answers: **STM-2** battalion command = cheap (Charm=stat / Authority=proficiency rank via the existing `weapon_rank_for_wexp` helpers, the `[BAT-6]` reuse; *earning* a non-weapon proficiency = the `[AGT §6]`/A5 gap, shared not new) · **STM-1** add ONE stat (Charisma) ≈ 1 day (8 hardcoded touch-points, math free) · **STM-3** author-defined stats = a foundation pass (several days), *evolution not rewrite*: `extra_stats` dict + `ClassData` extra bases/caps/growths + a `CampaignRules` stat registry + **one** `get_effective_stat` fallback + de-hardcode the ~5 literals; **legacy fields stay** (comparability); gotchas = lint banning new direct field reads, stable iteration order, F1 reserve · **STM-4 (owner call, deferred)** sequencing = do STM-3 first if author-stats wanted at all (Charisma then = a data row, no engine change). **Pinned durably:** new **foundation F14** (atlas §1, 🔶 not firmed, the one foundation w/o a ratified end-shape → decide in the define-all sweep before F1; optional — degrades to STM-1) + **Phase-B/F1 schema-lock list** now reserves `extra_stats`+registry **and** the previously-missing `[BAT-11]` battalion attach state + `[BAT-6]` cross-ref. New register `extensible_stat_model_open_questions_2026-06-25.md`; INDEX/REGISTERS regen, 19 checks green. **NEXT unchanged: A4 → A5 → F1** (F14 now part of the define-all sweep).
- [2026-06-25k](2026-06-25k.md) — **A2 BATTALION ENTITY firmed `[BAT-1..13]` — closes `[STY-11]` and completes A2 design.** Last A2 sub-cluster. **Verdict (`[BAT-1]`, owner call): neither merge-into-items nor fully bespoke — a thin `BattalionData` + `BattalionRegistry` over FOUR reused substrates.** Reframed "the battalion" as the canonical config of a generic **attached-augment entity** (5 composable axes: attach · passive bonus · granted source/style · charge/endurance · rank, +optional granted-skills). Substrate map: **BAT-2** attach = the `PairUpRegistry` *pattern* (sibling registry `unit_id→battalion_id`, GameState snapshot/retry-restore) — a **data entity, not a roster unit** · **BAT-3** passive bonus = the `PairUpBonusResolver`/`StatContributions` combat-modifier pipeline (+drift-guard test) · **BAT-4** gambit source = already A1 (`[STY-7]`, reconcile-don't-relitigate) · **BAT-5** endurance = a per-map charge pool drawing the `[AGT §5]` rate-limit primitive (lean; depletion/replenish → build) · **BAT-6** rank = reuse the pure `weapon_rank_for_wexp` threshold helpers but **store progress on the entity, NOT `weapon_wexp`** (not a weapon track), EXP rides `[AGT §6]`/A5. **Altitude ladder + 2 trope tests the owner raised:** **BAT-7** Emblem/Engage ring = the *maximal config* (flip granted-skills on, endurance→gauge) — validates the abstraction (NOT on roadmap; stress-test) · **BAT-8** **adjutant = Pair-Up, not this** (attaches a real roster unit → the actual `PairUpRegistry` instance + an intervention flag; resolves last pass's open Q) · **BAT-9** **accessory/bond-ring = `[IEQ]` equip + skill-grant**, not this (single-axis → don't pull in). **BAT-10..13** prep-hub UI · F1 save reserve · `CampaignRules` overridable · AI. **Consumes** the `[AGT §5]` rate-limit + `[AGT §6]` non-combat-EXP pins. `[STY-1..17]` register now fully RESOLVED; atlas A2 = design ✅. New register `battalion_attached_augment_open_questions_2026-06-25.md`; INDEX/REGISTERS regen, all 19 checks green. **NEXT = A4** (provoke-MET + Capture-victory pin) **→ A5** (support-action + battalion EXP, loadout cap, death-inventory pin) **→ F1 schema-lock.**
- [2026-06-25j](2026-06-25j.md) — **A2 ACTION-GRANT firmed `[AGT-1..13]` (Dancer/Reinvigorate #8) + one forward-pin.** Second A2 sub-cluster. Key finding: action-grant = the **symmetric twin of F10 Secondary Movement** over the *same* `grant_extra_turn(unit,{can_move,can_act,is_self})` substrate — SMV = passive/self/move-only, **Reinvigorate = activated/ally/full-turn** (`is_self:false`) — so almost the whole `[SMV]` register transposes, **no bespoke engine**. Code-grounded on `TurnManager.UnitState {READY,MOVED,DONE}` (a refresh = "set a DONE/MOVED ally back to READY", which `_refresh_faction_units` already does per phase). Highlights: **AGT-1** activated parameterized `SkillData` (`effect_id="grant_action"`) · **AGT-2** scope param `grant_mode∈{full(default),move_only,act_only}` (owner call: **default full**) · **AGT-3** targeted via `[STY-16]`/`[STY-17]` gate, **valid target = state≠READY** (settles "re-grant an already-acted unit") · **AGT-4** existing skill-grant conferral (Dancer/Bard class) · **AGT-5** Dance **is** caster's turn-ending action (net spend-1-refresh-1; composes with caster's SMV) · **AGT-7 (owner call) anti-loop & balance = author TOOLS not a baked rule** (four levers, any closes the A↔B loop): a transient `actions_taken_this_turn` counter + refresh cap · a generic per-source per-phase/round action-rate-limit (→ forward-pin) · **resource cost** on the skill · **source scarcity** (author one Dancer) · **AGT-8** fully composable w/ F10 · **AGT-9 (owner call)** on-turn-only v1 + reserve `off_turn_grant` flag · **AGT-11** transient counters flagged for F1 (mid-phase save) · **AGT-12** `CampaignRules` overridable (mirror `[DSP-17]`) · **AGT-13** targeting cardinality = single/**multi-target** (reuses `[STY-9]` AoE)/**self** ("second wind", most loop-prone). **Two forward-pins:** generic action-rate-limit primitive (§5) + **non-combat-action EXP/proficiency path** (§6 → A5: `add_exp` plumbing exists w/ staff precedent, but proficiency `add_wexp` is weapon-track-keyed so non-weapon skills have no path; mirror-pinned atlas A5). **Forward-pin:** generic **action-rate-limit primitive** (broader than A2; also serves battalion gambits/staves; → action-flow foundation). New register `action_grant_open_questions_2026-06-25.md`; INDEX/REGISTERS regen, all 19 checks green. **NEXT = A2 battalion entity `[STY-11]`** (last A2 sub-cluster; watch it consume the rate-limit pin) → A4 → A5 → F1 lock.
- [2026-06-25i](2026-06-25i.md) — **A2 DISPLACEMENT ARC — CONSOLIDATED (`[DSP-1..17]` + two forward pins); A2 action-grant scheduled next.** Consolidates the four per-session fragments 2026-06-25e…2026-06-25h (archived under `archive/`). **A2 displacement sub-cluster thorough.** Owner sub-clustered A2 and firmed the **displacement primitive first**: rescue (#6) + capture-carry (`[RCR-5]`) + movement-assists (#17/smite) all ride **one occupancy/carry primitive** over `DisplacementService`, grounded on the existing **Pair-Up off-map-attach** substrate and the engine's **"teleported-but-still-READY"** state. Highlights: **DSP-1** one relocate primitive · **DSP-2** carry reuses the Pair-Up `OFF_MAP_TILE` sentinel + a `CarryRegistry` · **DSP-3** Rescue **distinct** from Pair-Up (shared substrate, exclusivity prior) · **DSP-4** carry gating = F4 `CampaignRules` "carry" profile · **DSP-5** **capture-carry = same substrate + deltas** (settles `[RCR-5]`) · **DSP-7** push family = a **`displace` EFFECT KIND** on the A1 `[STY]` axis (standalone skill-actions AND layered into sources/styles; smite = potent shove; **amends STY `EffectSpec.kind`**) · cross-cutting ruleset **DSP-12** off-turn allowed/never-interrupts · **DSP-13** composable resistance (tag/stat/accuracy; v1 = tag) · **DSP-14** non-exclusive invalid-destination outcomes (`fail` v1 · collision_damage · chain_push · **`force_onto_invalid`** ring-out) + **harmful-consequence relationship gate** · **DSP-15** six invariants (forced-entry==normal-entry) · **DSP-16** preview/RNG · **DSP-17** every rule a **campaign default overridable per source**. **Two forward pins surfaced:** a **"Capture" victory type** (`ObjectiveCondition.type=capture` + `captured:<id>` flag → A4/objective-system) and an **optional death-inventory disposition rule set** (→ A5). New register `displacement_carry_open_questions_2026-06-25.md` + design doc `nonstandard_movement_and_displacement_2026-06-25.md`. **NEXT SESSION = A2 action-grant (Dancer/refresh #8)** over M10 `grant_extra_turn`; then battalion entity (`[STY-11]`) → A4/A5 → F1 lock.
  - *(archived per-session fragments with full detail: `archive/2026-06-25e.md` … `archive/2026-06-25h.md`)*
- [2026-06-25d](2026-06-25d.md) — **A1 DESIGN ARC — CONSOLIDATED (CEX cluster-B eval `[CEX-5..8,20..24]` + Source+Style model `[STY-1..17]`); A2 scheduled next.** Consolidates the nine per-session fragments 2026-06-24i…2026-06-25c (archived under `archive/`). **A1 design complete & audited clean** — every design item resolved bar `[STY-11]` (battalion entity → A2). Highlights: separated CEX cluster B (weapon-source/equip foundation + spells riding on it); built the **source + style** model (attack = source + optional style — arts/gambits/capture/staves all unify; one select→preview→target→pay pipeline; absorbs CEX-23); composable multi-resource cost (style may override source cost); capture = non-lethal → `sleep`; AoE/multi-target vocab incl. **`rectangle(W,L,anchor_side)`**; multi-effect combos (`EffectSpec` set); one generalized **effect-forecast** preview (footprint + focused-target cycle); **directed 3-state faction relationship matrix** (`are_hostile`→`relationship`); **full F5 `ConditionManager` pulled onto A1's critical path**; **CEX-22** auto-equip fallback refined (player order + equip-history/MRU + opt-in range-aware skill); universal **`no_attack`** source `[CEX-24]`. Loose ends pinned: provoke transitions → **A4**, loadout cap → **A5**. New design doc `design/source_style_player_and_authoring_2026-06-24.md`; F1 reservations expanded. **NEXT SESSION = A2 design** (battalion entity, capture carry, movement assists; then A4/A5 → F1 schema-lock).
  - *(archived per-session fragments with full detail: `archive/2026-06-24i.md` … `archive/2026-06-25c.md`)*
- [2026-06-24h](2026-06-24h.md) — **A3 branch #4: Recruit/Capture roster side firmed `[RCR-1..7]`; A3 cluster complete.** Walked the last A3 branch, firming the **roster-facing side** and reviewing the conversation (A4) side for context. Grounding: `Unit.team`/`MapData.factions` exist → recruitment = a **faction flip** to the player faction; the conversation side is **MET's (A4)** — MET has planned `dialogue`/`flag` actions but **no `talk`/`recruit` action yet**, so the roster side exposes a **state + transition API** the MET action calls (doesn't own a trigger). **Capture is a 3-way straddler** (roster A3 + carry A2 + trigger A4). Decisions: **faction flip → persistent roster member** (RCR-1); recruited-state = **first-class roster membership + auto-set F6 flag** `recruited:<id>` (RCR-2, both reserved in F1); **A3/A4 seam** = roster `recruit()`/`capture()` API + state, MET (A4) = `talk` trigger + `recruit`/`dialogue` actions calling it — wrote the **MET-action contract** A4 builds against (RCR-3); **eligibility split** = unit holds identity/reward, MET trigger holds firing-conditions (RCR-4); **capture scope** = talk-recruit + recruited-state now, capture roster end-state = captured→recruitable, **physical carry/jail deferred to A2** (RCR-5); recruited unit may be a `[MCH]` main character, convos reuse F13 + REL-6 (RCR-6). New register `[RCR-1..7]`; atlas #4 GAP→firmed + A3 bullet status/naming fix; index regen. **A3 COMPLETE** (`[REL]`·`[MCH]`+F13·`[RCR]`) — roster/save schema defined for F1. **Next: cluster A1** (combat capabilities; head = finish CEX cluster-B eval `CEX-5..8`/`20..23`).
- [2026-06-24g](2026-06-24g.md) — **A3 branch #20: Main Character / Avatar firmed `[MCH-1..8]`; new foundation F13.** Unified **Avatar / My Unit** and **story-defined main characters** into one **class-orthogonal "main character" role** on `UnitData` (differ only by a **provenance** flag: author-fixed vs player-authored). "Not based on a class" = identity is a **unit property**, not a Lord class (start any class, reclass freely, keep the role). **Role bundle** (defaults-on, each overridable): must-survive · relationship hub (REL-8 slot) · dialogue name-sub · **forced deployment** (overridable per-character AND per-map). **Must-survive** = role auto-adds a campaign-wide `protect`/defeat default (reuses the existing `ObjectiveCondition` `protect` type), author overrides per-map (game-over even in casual/phoenix unless overridden). **Avatar player-authored fields** = author opts the player in **per field**: name · portrait (pick-from-set OR format-constrained upload, rides the campaign-pack raw-load art pipeline) · starting class · growths · special skill(s). Researched **game localization** → the `unit_id`-pointer/name-substitution decision IS the i18n foundation → spun out new foundation **F13** (text indirection: ID-keyed templates + `unit_id` name lookup, **no concatenation**, Godot `tr()` when localized; **convention adopted now, multi-locale build deferred**; check_docs enforcement explicitly deferred until the dialogue-data format exists). New register `[MCH-1..8]` + F13 in foundations doc & atlas (F1–F12 → F1–F13; #20 DISCUSS → firmed); index regen. **Next:** A3 branch **#4 Recruit/Capture** (roster side; conversation/MET side → A4).
- [2026-06-24f](2026-06-24f.md) — **A3 cluster review + Relationship system (#5) firmed `[REL-1..9]`.** Opened the define-all sweep at sync-cluster **A3** (roster identity/relationships, do-first). Set walk order **Support(#5)→Avatar(#20)→Recruit(#4 ⇄ A4)** and the naming rule (**"relationship"** system; **"support"** stays the pair-up combat role). Firmed #5 into a new register `[REL-1..9]`: **reuse the unbuilt `[PXP]` track engine** (accumulator→named CampaignRules profile→on-crossing triggers) made **pair-agnostic** — relationships are **edges**, so NOT in the per-unit `proficiency_xp` store; **class EXP NOT rebuilt on it** (divergent cyclic/growth-roll payload; `[PXP-5]` `class_exp_share` stays the seam). **Complete pair-keyed graph**; three new gain sources (`co_deployment_survival` · `end_turn_proximity` scaled, pair-up/rescue=0=max · `class_exp_relationship_share` same proximity machinery, independent knobs) under a **per-map cap**; on-crossing extends PXP vocab (`unlock_conversation`/`set_flag`, conversations sparse/optional); **benefit payload** = per-unit directional list, `active_modifiers`/Charm-aura vocab, **proximity-gated aura** (one radius primitive powers gain+share+aura), **rank-scaled**, stacks independently of pair-up+Charm; **top rank exclusive** (FE S-rank/marriage) behind a CampaignRules toggle (Avatar hooks it); `[REL-9]` hands the PXP build a pair-agnostic-resolver requirement + F1 reservations. 1 register + index regen, check_docs **19/19**. **Next:** A3 branch **#20 Avatar/My Unit**, then **#4 Recruit/Capture**.
- [2026-06-24e](2026-06-24e.md) — **Learnable spells → non-inventory weapon-source merge; CEX cluster B expanded `[CEX-20..23]`.** Explored spells `[CEX-5..8]`; key finding: **combat is source-agnostic** — `CombatResolver` only consumes a `WeaponData` from `get_equipped_weapon()`, never reads inventory, so a learned spell, a Laguz natural weapon, and unarmed fists are the **same structural thing** (a non-inventory `WeaponData` with non-`InventoryEntry` charges); the whole seam = two `Unit.gd` funcs. **Hybrid lean** (non-binding): capability = `[IEQ]` `ItemDef{WeaponComponent}`; known/equip/learn reuses the **skill** model; no `known_spells`/Cast action — revises `CEX-5..8`. **Verified equip reality:** the `WeaponMenu` swap path is built+wired (`GameMap.tscn`) but **never surfaced in play** (Equip needs ≥2 weapons; every roster unit ships 1; `WeaponMenu` untested). NEW **OPEN** items folded into cluster B for the eval session: **`[CEX-20]`** **two-source** enumeration — **inventory** (physical: slots/trade/durability) vs a **granted list** (non-inventory refs: spells/natural/accessory/event), refined from three sources (provenance is a per-row `[SKL]`-style tag, not a separate path; class-innate is recomputed not stored); wires the dangling `is_natural_weapon`/`natural_weapon_type`, **`[CEX-21]`** equipped-source-as-reference (inventory-entry vs granted-list) + action-menu split (Attack/Equip Weapon/Inventory), **`[CEX-22]`** equipped method unavailable on another player's turn (counter fallback), **`[CEX-23]`** rework attack-select + targeting for source/maneuver combos. Demo asset `data/weapons/fists.tres` (0 Mt, ∞ uses, new `fist` family/track). Also **fixed a pre-existing reorg bug**: `test_release_metadata` pointed at old flat doc paths (checklist→`playtests/`, env→`guides/`), red since `1578d8d`, blocking the pre-commit. Bump register `CEX-1..19`→`CEX-1..23`. 2 commits, check_docs **19/19**, full suite green. Then **reprioritized the roadmap** (atlas §3/§3b): a **full define-all sweep of every un-firmed feature** becomes the priority blocker ahead of **F1 schema-lock** (chain = sweep → F1 → builds; only next playtest runs beside it), and **grouped Phase A by shared-system overlap into 5 sync-clusters** (A1 combat capabilities/attack-flow · A2 action-economy/movement · A3 roster identity/relationships · A4 story-event content · A5 campaign meta/economy; straddlers Recruit A3⇄A4, Battalions A1⇄A2). 4 commits total (`5cf57ed`…`97cd1bb`+grouping). **Next:** start the sweep in order **A3→A1→A2→A4→A5** (A3 first = highest F1-schema risk), heading cluster B's eval/sort into A1.
- [2026-06-24d](2026-06-24d.md) — **Firm story / key items `[CEX-14..16, 18, 19]`.** Wave-1 firming (atlas §3b), cluster E. Owner expanded scope from tracking+lock into a richer system. Firmed: `ItemDef.story` + author-configurable **`no_sell`/`no_drop`/`no_trade`** (default: no-sell+no-drop, trade-OK; auto-explained in More Info); **holding item X = a live `[MET]` predicate** (no F6) for side-quests/recruitment, persistent branching-state rides **F6**; convoy **"Key Items" view** + **capacity-exempt** (reconcile `[CNV-2]`); designer/editor key-items registry. NEW **`[CEX-18]` event item-mutation** (upgrade/weaken/steal/destroy/grant_item `[MET]` actions; reuse `forged_mods`/`[PXP]` item-bond) + **`[CEX-19]` validation** (warn on key item with finite uses + no repair path; `DataManager.validate()` at build). Reserved 4 `ItemDef` flags in the IEQ model; reconciled CEX (`CEX-1..17`→`CEX-1..19`; **A/C/D/E resolved, only B open**), CNV, MET, atlas. 1 commit, check_docs **19/19**. **Next:** learnable spells `[CEX-5..8]` (last open CEX cluster).
- [2026-06-24c](2026-06-24c.md) — **Firm per-map-use items `[CEX-13]`.** Wave-1 firming (atlas §3b), cluster D. Grounding: consumables decrement `InventoryEntry.uses_remaining` permanently; skills already have a per-map counter pattern (`skill_use_counters` reset by `reset_map_state`). Firmed **pure recharge**: `ConsumableComponent.uses_per_map` (-1=off); when set the item is **never consumed** (`uses_remaining` stays -1) and a **per-instance `InventoryEntry.map_uses_remaining`** counter (refilled at map start) gates use; **no finite total cap** in v1. UI = **distinct "N/max ⟳" badge** + "resets each map" tooltip (consumed shows `×N`). Reserved the two fields in the IEQ model; reconciled register cluster D (A/C/D resolved, B/E open), candidate-systems design doc, atlas. 1 commit, check_docs **19/19**. **Next:** learned spells `[CEX-5..8]` or story items `[CEX-14..16]`.
- [2026-06-24b](2026-06-24b.md) — **Firm the author-flexible weapon triangle `[CEX-9..12, 17]`.** Wave-1 firming (atlas §3b). Grounding: `WEAPON_TRIANGLE` is **already an arbitrary directed matrix**; magnitude is hardcoded flat; GDD_04 already specs an unimplemented rank-scaled table. Firmed: lift relationships/magnitudes/families into a **`CampaignRules` `triangle` profile (F4)** — author-extensible `families` (becomes the `triangle_family` validation source), arbitrary `matrix`, **stat-mod `effects`** (`[CEX-10]`; conditions deferred to the **F5** build), flat OR per-rank tables. **`[CEX-12]` scaling = flat default (non-breaking) + opt-in built-in `rank_scaled` profile** (GDD_04 table). **`[CEX-9]` UI** = data-driven preview/more-info readout. **NEW `[CEX-17]` reaver (owner asked to firm):** per-weapon `weapon_component.reverses_triangle`; **odd** count across the two combatants inverts the result ×`reaver_multiplier` (default 2), **even cancels** (GBA-style). Reconciled register block C (`CEX-1..16`→`CEX-1..17`), candidate-systems design doc, atlas Cluster-B, GDD_02/04 (design-firmed notes, no status flip). 1 commit, check_docs **19/19**. **Next:** per-map-use items `[CEX-13]`, then learned spells `[CEX-5..8]`.
- [2026-06-24a](2026-06-24a.md) — **Firm F10 — Secondary Movement as a parameterized skill `[SMV-1..11]`.** Continued the target-firming roadmap (atlas §3b) on the last open foundation. Owner keystone: **it's not a bespoke engine — it's a SKILL** (`effect_id="secondary_movement"`, `effect_params {movement_mode remaining|flat, secondary_move_actions}`, **passive**) granted via the **existing** skill-grant mechanisms (`skill_unlocks` default on mounted · `[SKL-4]` grant · `[IEQ]` accessory effect_id = **Knight Ring** · F6 story · `[PXP-4]`). One new engine piece = a `UNIT_SECONDARY_MOVE` action-flow window ending in **Wait only**. **Supersedes** GDD_10 M10's automatic-for-all-mounted approach. **Two renames:** (1) caught a GDD_10 M10 collision where "Canto" named *both* the move-after-acting mechanic AND a Bard/Heron **ally-refresh** skill → renamed the ally-refresh **→ Reinvigorate**; (2) then renamed the mechanic itself **"Canto" → "Secondary Movement"** (new-user clarity), removing "Canto" from **all live docs** (survives only in historical docs); register `[CAN]`→**`[SMV]`**. Reconciled atlas F10 + foundations doc + scope-map #9 + IEQ §2f → **all F1–F12 end-shapes now decided** (only F1 schema-lock remains before builds). 3 commits (`f7ecdb0`…), check_docs **19/19**, tree clean. **Next:** F1 save-schema lock, then Wave-1 (triangle/per-map items/learned spells/story items/M9b).
- [2026-06-23l](2026-06-23l.md) — **Design marathon: items/equipment + proficiency/XP + candidate systems + foundations.** Audited the whole stack: weapons + WEXP **healthy** (match Implemented claims); **equipment half-built + model-split** (6 findings — `ItemData.item_type=="equip"` disconnected from the `InventoryEntry` inline 4-field bonus read at `CombatResolver.gd:225`; passive-while-held misnomer; orphaned `until_unequipped`; forging-conflated comment; named items exceed 4 fields; GDD_04 status mismatch). Owner chose **unified design doc + fresh register**: created `design/items_equipment_unified_model_2026-06-23.md` + `[IEQ-1..9]`, **superseded `[EQP-1..5]`**. Walked in dependency order, re-evaluating after the keystone → **firmed COMPOSITION**: one **`ItemDef` base** + optional `weapon`/`consumable`/`accessory` components (**multi-component ships v1**), `InventoryEntry` = thin per-slot instance → `def_id`; **per-component independent legality** (accessory = `item_wexp` track + `req_flags`); **typed accessory slots** (`slot_type` + per-type capacity, campaign base + ClassData add/remove override, per-unit deferred); conferral held|equipped|both; benefit **tier table**; modifier+effect-hook model wiring `until_unequipped`; `ItemData.item_type` enum dissolves. **Build STAGED** (define → migrate weapons→consumables→accessories). Then **owner expanded XP scope** → unified **Proficiency/XP Framework `[PXP-1..8]`** (weapons + items): one `proficiency_xp` store; track kinds weapon-family/**item-group** (skill-w/-shields)/**item-bond** (relic); rank profiles (thresholds **+ names**) = **CampaignRules**, named per-track, default E–S non-breaking; per-threshold **on-crossing events** (grant_skill, absorbs s_rank_mastery hardcode); author-defined **multi-source gain** (active_use/class_exp_share/per_map_carry/action_gated, track-default + per-item override). **Intentionally revisits firmed weapon WEXP** non-breakingly. Then **`[PXP-9]` training halls** (authored XP-for-resources PHB panel; Arms-Scroll twin) + **IEQ §2f effect/trigger checklist** mapping every drafted item to the effect_id to add (`negate_effectiveness`/`negate_crit`/`advance_proficiency`/`permanent_stat`; no new triggers — GDD_05 discipline) and flagging two model gaps (canto/M11, growth-rate modifiers). Then folded the 2 model gaps (growth-rate channel, canto/M11) into the build plan, and **drafted 5 candidate systems** (owner: initial designs + feasibility/scope + question list, NOT firmed): **(A)** resource pools (stamina/mana/HP cost model; Moderate) · **(B)** 3H learned spells (no-inventory known-list, per-map/pool; High) · **(C)** author-flexible weapon triangle (CampaignRules profile + stat/condition effects; Moderate) · **(D)** per-map-use items (Easy) · **(E)** story/plot-item tracking (flag+lock+panel cheap; branching needs a story-state store that doesn't exist). Captured in `design/candidate_systems_2026-06-23.md` + `[CEX-1..16]` OPEN; scope-map #22. Cross-cutting deps: `ConditionManager` (stub) trending foundational, A→B, missing story-state store. Then built a **feature dependency atlas** (`plans/feature_dependency_atlas_2026-06-23.md` — every planned feature grouped into clusters A–G by foundation, T-shirt sized; **11 foundations F1–F11** tabled) and **decided 3 foundation end-shapes** (`design/foundations_end_shapes_2026-06-23.md`): **F4** one generic CampaignRules author-profile mechanism · **F5** full author-extensible status system (`ConditionData`) · **F6** generic campaign-flag/variable store. Open foundations: F7 pools (w/ `[CEX-A]`), F10 canto; F1 save-schema "lock reserved fields first." Then a **player-facing target coverage walk** (firmed core has targets; the v1 worklist #4–12 + DISCUSS #14–20 have only one-line intent = the gap) → owner: **firm in dependency order**; captured a **target-firming roadmap** (atlas §3b) and **firmed resource pools `[CEX-1..4]` = foundation F7** (CampaignRules pool types, author refill modes + restore items/Regen skills, gates weapon/spell/skill use). **Only F10 canto open** of F1–F11. Then **firmed the skill-model expansion `[SKL-1..6]`** (owner): universal rule — skills the player can't unequip via the standard menu get their own sheet category + don't count against `max_skills`; **personal_skills** (own slot); **class skills** with two author flags (`retain_on_reclass` + `requires_equip`, available by current class level); **grant/revoke API = foundation F12** driven by story(F6)/shops/items/skill/attack/PXP-4 triggers. 13 commits (`f402a65`…), **19/19** throughout. **Next: continue dep-ordered firming** (F10 canto, F1 schema-lock, Wave-1 triangle/spells/etc.); scheduling after. Lower-priority queue (avatar #20, recruit #4, difficulty #12).
- [2026-06-23k](2026-06-23k.md) — **Open-items triage + doc reconciliation.** Surveyed all open planning/discussion items → two tracks: **Track A content firming** (scope map §3b worklist; recommended decide-order = prep-as-hub keystone → convoy→shop→gold economy spine → equip → avatar feasibility early → recruit/difficulty/hub-options → Support/Rescue/Dancer/staves/village → DISCUSS items → builder-authority pass; drafted per-system firming questions) and **Track B gates/tooling** (`REN` needs names, `LEG` needs corpus, `IMP` walk-and-confirm, `CST-13` leave deferred). Then **reconciled docs**: grep "overlay" hits were mostly *not* stale (AI/fog overlays, or correct reversal text); **3 real fixes** — GDD_10 ~230 DMR-4 base+overlay→self-contained replace-load (contradicted its own §167 ICO note); DMR register AMENDED banner (merge→replace-load, body kept for provenance); backlog §2b banner (convoy/shop/recruit/support/rescue now committed **v1**, still out of §2 *save-cluster* MVP — left firming-doc "deferred" wording, that's save-cluster scope). Then **walked the player-facing firming worklist** (scope map §4 order): **`[PHB-1..7]` prep-as-hub** RESOLVED (keystone — flat panel list + theme, opt-in `prep_panels`, **`node_type` battle|hub** author-switchable, free-nav single commit), **`[CNV-1..7]` convoy** RESOLVED (`Array[InventoryEntry]` shared store absorbing `party_items`, author-defined enforced `max_inventory` + `convoy_capacity`, faction-scoped prep trade, prep-only v1), **`[SHP-1..5]` shop/economy** RESOLVED (**resource-keyed** buy/sell gold-only-v1, author per-shop stock, prep→convoy / **battlefield-shop**→unit, ledger reward+sell/shop-buy). **Economy spine complete.** Also **opened `[EQP-1..5]` equip-items** (code-audit: passive-while-held 4-field mod at `CombatResolver.gd:225`, orphaned `until_unequipped`, stale/forging-conflated comments) but **walk paused** — owner redirected. ~11 commits, **19/19** throughout, scope map reconciled. **Next session: GROUND-UP review of all equipment, items & weapon proficiencies** (re-derive the data model; folds EQP in; deliverable = a unified items/equipment design doc). Lower-priority queue: avatar feasibility (#20), recruit/difficulty/hub-options.
- [2026-06-23j](2026-06-23j.md) — **Documentation sorting/storage/retrieval reorg (owner-requested).** Design-walk: audited the flat 126-entry `AGENT/Docs/` → ratified design register (DSR-1..5: full **by-type** layout · **generated-from-headers** manifests · **archive-everything** no-deletion) → implemented in 8 commits. New: `gen_docs_index.py` generates `INDEX.md` (doc map) + `REGISTERS.md` (the first catalog of the ~19 `[XXX-n]` registers with OPEN/RESOLVED + resolved-where; roadmap §H only had prose). check_docs **17→19** (+check 18 manifest no-diff, +check 19 archive markers; `_is_historical()` now matches `Superseded`). `git mv` ~127 files into `guides/ governance/ decisions/ registers/ design/ plans/ playtests/` + `archive/{consolidation,plans,playtests,handoffs,reference,evidence}/`; nothing deleted, all archived `.md` carry a Historical marker. Repaired 38 broken GDD/guide paths + `check_docs._ACTIVE_GUIDE_FILES` + decision_index homes; bumped GDD_00–08 Last verified (link-only edits). AGENTS.md documents the layout + regenerate-in-same-change rule. Content thread (scope map/foundations/DMR/ICO + notes a–i) untouched. **19/19, no `__pycache__`.** Next: resume content (prep-as-hub → convoy/shop); OPEN registers LEG/IMP/REN/CST-13.
- [2026-06-23i](2026-06-23i.md) — **DAY ROLLUP / table of contents for 2026-06-23** (one continuous thread across 8 detail notes a–h; notes kept, this is the single entry point). Arc: confirmed v0.2.3 next playtest build + named the campaign+builder DESIGN side the next major task (b) → framing/foundations doc, substrate L0–L4 (c) → `[DMR-1..4]` resolved, campaign load seam parameterized (d) → `[ICO-1..6]`: **owner reframed content model to SELF-CONTAINED**, reversing the base+overlay direction, collapsing DMR-4 merge→replace-load (e) → **re-sequenced**: finish player-facing features → then builder authority → then impl; produced the player-facing scope map (f) → equip-items GAP + genre/FE feature scan (g) → **broadly defined v1 player-facing scope**: all Tier-1 in, skirmish auto/random-level, **no wander area → prep screen = author-parameterized hub**, capture under recruit, training halls (NEW) + arena/combat-arts/battalions/movement-assists/bonus-EXP to discuss, avatar to investigate (h). Net: registers `[DMR]`/`[ICO]` resolved; live drivers = scope map + foundations doc; **next session = documentation cleanup/organization** (prompt drafted for owner). check_docs **17/17**.
- [2026-06-23h](2026-06-23h.md) — **Owner broadly defined the v1 player-facing scope: triaged the genre scan, added cross-cutting design decisions, and introduced a NEW feature (training halls).** **FIRM for v1:** convoy(D), shop(E), equip-items, recruit(F)+**capture**, support(H2), rescue(H3), PvP, **all Tier-1 staples** (dancer/refresh, canto, utility staves Warp/Rescue/Hammerne, village-visit **+razable**, **difficulty modes + Casual/Phoenix** permadeath variants), and **skirmish encounters with auto/random-leveled rosters**. **DISCUSS & PLAN:** arena, combat arts, battalions/gambits(±adjutants), movement assists, bonus-EXP, **training halls (NEW)** — spend resources for **NON-TRANSFERABLE** per-character gains (class XP/weapon XP/stat bonuses/skills/other). **INVESTIGATE:** avatar/My Unit ("look into the possibility" → feasibility dive). **Deferred out of v1:** 2nd-gen children, fatigue, biorhythm, durability-free weapons, Triangle Attack. **Cross-cutting decisions:** **NO wander area — the prep screen is the author-parameterized between-chapter hub** (author declares which option panels + theme are available per node/location/time: convoy/shop/arena/training-hall/recruit/skirmish) → **expands the firmed §2 branch C** (revises C4 "empty for MVP"), feeds the 4a–4e builder-authority pass; **skirmish** via prep option (not free-roam), auto/random-level ties `[AIP-11]` + EXP/economy; **capture** = recruit mechanism. Reconciliations flagged (prep-as-hub expands C not contradicts; training halls = character-bound vs transferable items, resource type OPEN). Edits: scope map §3 rewritten (3a decisions / 3b commitment-tagged worklist / 3c deferred) + §4 sequencing; `GDD_10` §H walk pointer. DoD#1 planning only; DoD#2 none. check_docs **17/17**, no `__pycache__`. **Next (rec): firm the prep-as-hub framing (foundational), then convoy→shop spine; avatar feasibility dive available.**
- [2026-06-23g](2026-06-23g.md) — **Logged equip-items/accessories as a player-facing GAP (code-grounded), and ran a genre/FE feature scan for mechanics not yet on the scope map.** Equip non-weapon items: scaffolding exists (`InventoryEntry.entry_type=="equip"` + `accuracy/damage/crit/dodge` fields, applied by `CombatResolver._apply_equip_item_modifiers()` for both combatants; GDD_04 names Full Guard/Iron Rune/Knight Ring/Wing Guard/Laguz Guard — all **Planned**), but **no equip/unequip action or slot** (bonuses apply passively to every equip entry held, always-on, stacking); `until_unequipped` label renders with **no producer**; 4 combat fields can't express +STR/+MOV; **stale `InventoryEntry.gd` comment** ("M10 forging — no code reads this") contradicts `CombatResolver`. Added as GAP item 3 (pairs w/ convoy/inventory). **Genre scan (raised, NOT triaged):** Tier 1 staples missing/implicit = **dancer/refresh** (M10 substrate but no Dance action), **canto**, **utility staves** (Warp/Rescue/Hammerne), **village/house visit (+razable)**, **difficulty modes + Casual/Phoenix** permadeath variants; Tier 2 = avatar/My Unit, base hub (My Castle/monastery), skirmish/grind encounters, arena, capture, 2nd-gen children (likely defer); Tier 3 (lower for Awakening base) = combat arts, battalions/gambits/adjutants, fatigue, biorhythm, durability-free weapons, movement assists, bonus-EXP, Triangle Attack. Already-covered not re-listed (FOW, siege, destructible terrain, rewind, forging, pair-up, conditions, AI). Edits: scope map GAP item 3 + genre-scan block. Owner to triage Tier 1/2 onto/off the worklist. check_docs **17/17**, no `__pycache__`. **Next: triage genre candidates + pick first GAP cluster (rec: convoy D → shop E).**
- [2026-06-23f](2026-06-23f.md) — **Owner RE-SEQUENCED the design plan: finish defining ALL player-facing features first → then decide the campaign builder's authority → then implementation.** Corrects a risk (we were about to define builder authority before the player-facing surface was firmed). **Survey finding:** not starting from zero — `AGENT/GDD/` already has `GDD_Feature_Index.md` (≈18 feature groups + backlog) + `GDD_Adoption_Matrix.md`, so the surface is **inventoried**; "finish defining" = firm the **gaps**, not redo the GDD (the §2 A–J firming is the template). **Deliverable:** `player_facing_scope_map_2026-06-23.md` — navigation, NOT a second spec (DOC-005); adds a player-facing-definition lens (Firmed/Designed/Partial/GAP/Out-of-scope) over the Feature Index. **Firmed:** combat/RNG, triangle, WEXP, EXP/promotion/reclass, classes, terrain, objectives, faction/hotseat, save/suspend/rewind, UI/input. **Designed-not-built (not gaps):** prep/deploy, FOW/DCH/DTR/MET/STW/MRD/TUR/BWN, conditions(M8), AI(AIP). **GAP worklist (player-facing undefined — the step-1 work):** convoy/inventory (D), shop/economy (E), recruit (F), Support (H2), Rescue (H3), PvP/scenario; lighter = per-skill UX (M9b) + condition UX check-backs. **Sequencing:** finish GAPs → builder authority pass (the deferred 4a–4e, uses the scope-map list as its checklist) → implementation; not gated by Package A. Edits: new scope map; framing doc §6 + `GDD_10` §H walk pointers re-sequenced (4a–4e deferred behind the player-facing pass). DoD#1 planning only (per-cluster GDD/Feature-Index/roadmap flips land with each build); DoD#2 none. check_docs **17/17**, no `__pycache__`. **Next: pick the first GAP cluster to firm (rec: convoy D → shop E economy spine first).**
- [2026-06-23e](2026-06-23e.md) — **Walked the I3 content-overlay register `[ICO-1..6]` — and the owner REFRAMED the campaign content model from "base + overlay" to SELF-CONTAINED, everything user-defined.** Grounded in code (`WeaponData`/`ItemData` have `id`+`display_name`, **no `icon`**; DataManager id-keyed dicts; `DUPLICATE_ID` fails loud). Drafted the register, then **mid-walk the owner chose [ICO-1] "self-contained" + [ICO-5] "everything a user-defined asset"** — a model shift, so **paused and reconciled** (it reverses 2026-06-23a, collapses the runtime merge, makes [ICO-2]/[ICO-3] partly moot) before confirming via a 2nd round. **Reframed model:** each campaign = a **complete independent** bundle; **no runtime overlay** — `select_campaign(c)` *replaces* content with c's full set; defaults ship with the **builder** as a copy-from palette, **copied `res://`→`user://` on first run**, uniform `user://` enumeration, all art **raw-loaded**. **Costs accepted:** duplication/bloat + **no central patch propagation** (fix a default → re-ship each campaign). **Resolutions:** [ICO-1] self-contained; [ICO-2] A whole-resource (trivial, no merge); [ICO-3] override-intent kept as authoring-time provenance `forked_from` only (runtime validation role gone — owner accepted downgrade); [ICO-4] provenance stamp `builder_content_version` only (no gate); [ICO-5] copy to `user://` first run (new scope = seed-copy + reset/repair, the heaviest piece); [ICO-6] A `icon: String` on WeaponData+ItemData + `resolve_icon()` raw-Image, field+seam now / **UI render deferred**. **Consequence:** `[DMR-4]`'s `_apply_overlay()` **merge → replace-load** (`_clear_content()`+`_load_all(campaign.dir)`+`_validate_all()`); runtime *simpler*, build weight moved to ICO-5. **Ripple edits (6 docs + memory):** new ICO register (RESOLVED + replace-load contract + log); DMR register DMR-4 UPDATE note; project memory `project_campaign_content_model` rewritten to self-contained (+MEMORY hook); planning_backlog §2b I3 → RESOLVED/self-contained; framing doc core-insight/two-tier/L2-L3/§4c-d/§5/§6; `GDD_10` §H walk pointer. DoD#1 planning only (GDD_03/04 + first-run seed-copy at build); DoD#2 guards shifted (per-campaign dup-id + `icon`-resolves; dropped namespace/version-compare guards no longer owed). check_docs **17/17**, no `__pycache__`. **Next: draft + walk the designer authoring contract (4a–4e).**
- [2026-06-23d](2026-06-23d.md) — **Walked AND resolved the `DataManager` decomposition register `[DMR-1..4]` — the L2 keystone of the campaign design-side push.** Register was scoped as a **pure refactor** (3 Qs) but carried the campaign-critical forward-dep note ("keep `_load_all()` parameterizable for base+overlay") that designed nothing. Held the split: **DMR owns the load-path SHAPE, I3 owns the MERGE SEMANTICS.** Owner took every rec. **[DMR-1]=A** unify error channels (one `_validate_all()` folds the `skill.validate()` loop + both `collect_*` sets → single `_report` sink; keep skill bool return). **[DMR-2]=A** load errors (missing dir/dup id) stay **inline** at the I/O site (`LoadResult`); only validation feeds the channel — two failure classes kept distinct. **[DMR-3]=A** add a `_validate_all()` composition test + a [DMR-4] seam test. **[DMR-4] NEW=A** — **parameterize the campaign base+overlay load seam now, defer merge semantics to I3:** `_load_all(base, overlay_source:=null)` (null = identical boot, behavior-preserving), new public `select_campaign(campaign)` = load DEFAULTS→overlay→re-resolve id caches (present but **uncalled** until §2/I3 wire it), and `_apply_overlay()` as a **no-op stub that fails loud** on a non-empty overlay. Forward dep: `campaign.content_dir`/overlay-source shape is an I3+§2 `CampaignData` field; DMR only assumes "a source the loader can enumerate." **Net:** the `defaults ∪ overlay` load path is now a real, testable seam while unratified I3 rules stay out; I3's job is concretely "fill `_apply_overlay()` body + add item `icon` field." Edits: DMR register (RESOLVED + 3 resolutions + new [DMR-4] + plan/notes/log), `GDD_10` §H DMR line `[DMR-1..3]`→`[DMR-1..4]` RESOLVED + walk-pointer (DMR done→next I3), framing doc L2 row + §6 walk order. DoD#1 no behavior change (seam inert) → §H register-status sync only; DoD#2 none (id-namespace/version guards ride the I3 body). check_docs **17/17**, docs-only, no `__pycache__`. **Next: open the I3 content-overlay register (ratify a–e + item `icon` field) — it fills the `_apply_overlay()` stub.**
- [2026-06-23c](2026-06-23c.md) — **Started the campaign/save DESIGN-side push (next major task from 2026-06-23b): produced a framing/foundations doc mapping what the features build on + how players vs designers interact, firmed-vs-open.** Read the three firmed §2 docs first (player firming A–J, technical plan, `[CST-1..12]` RESOLVED) — confirmed the **player** surface is firmed, the **designer/authoring** surface is the open frontier. User chose **"one framing doc first."** Deliverable: `campaign_save_expectations_and_foundations_2026-06-23.md` (navigation/seed, decides nothing). **Core insight:** save = state-by-id binding to a campaign id; everything resolves by id against `defaults ∪ campaign overlay`; the seam is the **`DataManager` base+overlay load (`[DMR-1..3]`), which doesn't exist yet**. **Substrate L0–L4:** L0 `RngService`/Package A (gates §2 *exec*) · L1 `SaveManager`/`SaveData`/`SaveCodec` firmed · **L2 DataManager base+overlay OPEN (keystone)** · L3 `CampaignData` graph firmed + content library overlay **(I3) OPEN** · L4 `CampaignRules` firmed. Three tiers: `res://` defaults → campaign **package** (overlay; art lives here, never in save) → save (state by id). **Player surface FIRMED** (campaign-select→rules→prep/deploy→battle→results; slots+export/import; Continue/Load; suspend; defeat menu; read-only rules+flip notice). **Designer surface OPEN (4a–4e):** progression authoring · rules mandates/defaults+protected_fields+story-flip · **content overlay I3 (sub-decisions a–e unratified) + net-new item `icon` field** · packaging/pack format · GUI editor vs hand-JSON. **Recommended walk:** `[DMR-1..3]` → I3 content-overlay register → designer authoring contract; player side stays firmed; **design NOT gated by Package A** (that gates §2 execution only). Edits: framing doc, `GDD_10` §H §2 bullet refresh + DESIGN-frontier pointer + `Last verified`→2026-06-23. DoD#1 planning/Open-Items-Register sync only; DoD#2 none (framing decides nothing; icon/version/namespace guards land with I3 build). check_docs **17/17**, no `__pycache__`. **Next: walk `[DMR-1..3]` then open the I3 register.**
- [2026-06-23b](2026-06-23b.md) — **Q&A / direction-setting: confirmed v0.2.3 playtest-build status, then named the DESIGN side of the campaign + campaign-builder system the NEXT MAJOR TASK.** Build: next playtest = **v0.2.3 "Display Scaling & Resolution"**, already **cut** (`Project_Prometheus_v0.2.3_debug.exe`, SHA `b92301f…ae107`, tag `v0.2.3`@`d9d02c8`) + bundled with `playtest_checklist_v0.2.3.md`; **ready to distribute** (the `V021-18/19` `[~]` live crispness/resolution verify IS the playtest pass — "build is the verify vehicle"); the `v0.2.3→main` fast-forward is open (protected branch, PR-only, no `gh`/token) but is git housekeeping, not a distribution blocker (**owner: merge later**). **Campaign blocker map (the substance):** DESIGN blockers ≠ BUILD blockers. **§2 campaign/save play system is already DESIGNED/build-ready** (`campaign_save_technical_plan_2026-06-21` + `[CST-1..12]` RESOLVED). The **campaign BUILDER (GUI editor) + campaign-PACK format (branch I3) is NOT designed** — only "direction set" (2026-06-23a); **no dedicated register**, sub-decisions a–e (include/override/namespace/version/`res://`-vs-`user://`) have recs but **unratified**. **Main design blocker:** the builder authors against a data model that isn't locked — locking I3's content-overlay model depends on (1) the **§2 save schema** (save↔campaign-id binding) and (2) the **`DataManager` decomposition `[DMR-1..3]`** (still OPEN; needs base-load+overlay path, not global-only); plus item **ICONS are net-new** (no `icon` field on `WeaponData`/`ItemData`). **Package A/`RngService` is a BUILD blocker only, NOT a design blocker** ("none blocked from *planning* by execution blockers"). **Owner direction:** next major task = the design-side push (unblocked by Package A): walk the **I3 content-overlay register** (ratify a–e) → resolve **`[DMR-1..3]`** → add the item-`icon` schema field → toward the GUI-editor design. **NOT done:** I3 register doesn't exist yet; DMR-1..3 OPEN; icon field not added; v0.2.3→main merge open; IMP/LEG OPEN. Notes-only, DoD#1/#2 none owed, no `__pycache__`. **Next: begin the I3 + DMR design walk; build track still Package A Step 1 first.**
- [2026-06-23](2026-06-23.md) — **Planning-only: set the campaign-content model direction for the deferred campaign-PACK format (branch I3).** Question: how hard to bundle art + item labels into separated campaign data (Iron Sword vs Bronze Katana per game)? Code-grounded: content is **id-keyed + resource-driven**, `display_name` already lives ON the `WeaponData`/`ItemData` resource (relabel = override `.tres`, free), but **no item icon field exists** (items text-only → item art net-new). **Owner decision:** ship the basic content as a **shared DEFAULT library**; the **GUI editor** + campaign designers **include subset / modify / add** = the **base + per-campaign overlay** fork. **Three layers:** core defaults (`res://data/`) → **campaign package** (authored content + labels + art = the overlay) → per-playthrough **save** (state by id, binds to campaign id); **art in the package, never the save**. Mostly cheap (id-keyed model); real work = campaign-overlay loading + save↔campaign binding (already §2/DMR scope). Sub-decisions flagged w/ recs (NOT ratified): include=inherit-all-then-add/override/exclude; override=whole-resource-by-id v1; ids share default namespace+collision-validate; pack records default-content version; `res://` shipped vs `user://` authored. Edits: planning_backlog §2b branch I3 (content model + sub-decisions + DMR/GUI/icon deps), DMR register forward-dep note (keep `_load_all()` parameterizable for base+overlay), project memory `project_campaign_content_model`. **NOT done:** branch I3 has no register yet (direction only, deferred past §2); IMP `[IMP-1..6]` still OPEN; LEG OPEN. DoD#1 docs-only; DoD#2 none (override/namespace/version guards land with I3 build). check_docs **17/17**, no `__pycache__`. **Next: resume IMP walk and/or Package A Step 1 build.**
- [2026-06-22i](2026-06-22i.md) — **Planning/research-only: art rendering-scale + asset-licensing discussion → recorded an art-asset licensing policy in the legal register + a pre-import gate in the sprite importer.** Owner-stated: `TILE_SIZE`=64 stays, introduce `SPRITE_SOURCE_SIZE` (render scale = TILE_SIZE/SPRITE_SOURCE_SIZE) to decouple art fidelity from grid; **first release = 32px** FE-style (units+terrain uniform) scaled **2×** (integer → crisper zooms), **later tier = 64px** (constant flip); **release license = MIT + Commons Clause** (source-available, no-sell). Licensing core: **project license ≠ asset license**; a **source-available repo redistributes every committed file**, so committed art must be **CC0 > OGA-BY/CC-BY** (avoid NC = forecloses monetization, CC-BY-SA/GPL copyleft+DRM, and no-redistribute paid packs); **FE fan art/rips = dev placeholder only, never in public source** (no project license cures Nintendo IP); **commission w/ copyright assignment = cleanest** (covers fliers/mounts no pack has). Resolution-tier insight: best public-repo-safe breadth source (LPC CC0/OGA-BY) is **~64px → later tier**; 32px first release = fragmented CC0 + commission (HEROES 99 = 32px GBA-FE but overworld 4-dir still in dev; PIPOYA = 4-dir but no-redistribute→build-only). Edits: legal register new **§4 art-asset licensing policy** (+ [LEG-4] pointer, old Notes→§5); IMP register **asset-licensing gate banner** (check legal §4 before import). **NOT done:** IMP `[IMP-1..6]` still OPEN — `SPRITE_SOURCE_SIZE`/32px discussed but not yet written as [IMP-1] resolution; LEG still OPEN (blocked on [LEG-1] corpus). DoD#1 docs-only; DoD#2 none (whitelist→`ASSET_LICENSES` manifest noted, not landed). check_docs **17/17**, docs-only, no `__pycache__`. **Next: resume IMP walk (resolve [IMP-1..6]); build track = Package A Step 1.**
- [2026-06-22h](2026-06-22h.md) — **Planning-only: walked AND resolved the Broken-Weapon Degraded-Mode register `[BWN-1..5]`** (GDD §3 / OPEN-5); build-ready after §2 [CST-4]. **Owner reframed it from a single global toggle into per-weapon authoring.** **[BWN-1]** representation = keep rec A's *derived* Broken (`uses_remaining==0`, no new `InventoryEntry` field) **but per-weapon**: add `WeaponData.break_behavior` (`destroy`/`degrade`/inherit) + a `CampaignRules` campaign default (default `destroy`) — designers flip plot/special weapons to degrade, common weapons destroy. **[BWN-2]** penalty = A+B hybrid (global `GameConstants` defaults + optional per-weapon `broken_mt`/`broken_hit` overrides; Crit unaffected). **[BWN-3]** repair = **B, OWNER OVERRIDE** (defer to §2b/E shop; **no v1 repair item** — degrade weapons stay broken until the economy lands). **[BWN-4]** apply at `CombatResolver` stat assembly (NOT `active_modifiers`) so forecast==resolve, **+ v1 UI req: degraded stats shown debuff-red on the character sheet** via a weapon-derived stat-delta path; **forward-reserved:** a unified weapon-stat-delta display shared with the future weapon-upgrade system. **[BWN-5]** default = `destroy` (strictly additive, default game untouched). **Scope note:** now a `WeaponData` schema + `CampaignRules` default + `DataManager` validation + sheet-display path, larger than the draft. Code re-verified (`uses_remaining`/`has_uses`, `CampaignRules` fields, `ItemHandler` effect dispatch, `CombatResolver` chokepoint, `WeaponData` `@export`s). Edits: BWN register (RESOLVED + reframe + 5 resolutions + slice/test rewrite), GDD_10 §H OPEN-5 row, planning_backlog §3 + §5 STATUS 2026-06-22h. DoD#1 planning-only (GDD_04 at build); DoD#2 `break_behavior` value-set guard pre-registered IF string-encoded. check_docs **17/17**, docs-only, no `__pycache__`. **Remaining planning: IMP/DMR (REN/LEG owner-blocked); build track = Package A Step 1 first.**
- [2026-06-22g](2026-06-22g.md) — **Planning-only: walked AND resolved the Map-Readability cluster `[MRD-1..6]`** (GDD §4); cluster now build-ready. Delivers hover-to-peek range · movement path arrows · individual+aggregate threat range · grid-dim slider (threat-range `[TUR-1..4]` design folds in). **[MRD-1]** overlay stacking = **C** (range∩threat layers BLEND for the "reachable AND dangerous" tint; hover-peek + path-arrows are exclusive OPAQUE top layers; preserves threat-design's faction-src-3/watch-src-4 order). **[MRD-2]** hover-peek = **B** (hold-a-button; gamepad-friendly, computes once on press). **[MRD-3]** = WITHDRAWN (aggregate danger zone already built + relationship already designed; kept numbered). **[MRD-4]** recompute perf = **B** (ratified follow-on of MRD-2: compute-on-press + cache, no per-cursor-tick flood; rides `EventBus.cursor_moved`). **[MRD-5]** grid slider = **A** (one `SettingsManager` float `grid_dim` 0.0–0.5 dimming terrain layer only). **[MRD-6]** build order = **A** (threat-range slices 1–3 → layer precedence → hover-peek → path-arrows → slider; banks hardest piece, unblocks gamepad R3). All owner calls took the recs. Code re-verified: `cursor_moved`, `GridManager` overlay/danger infra, `SettingsManager` plumbing all intact. Edits: MRD register (RESOLVED + 5 resolutions + slice heading), GDD_10 §H + feature row, planning_backlog §5 STATUS 2026-06-22g. DoD#1 planning-only (GDD_06 at build); DoD#2 no guard owed (floats/palette/build-order, not string vocab). check_docs **17/17**, docs-only, no `__pycache__`. **Remaining planning: IMP/DMR/BWN (REN/LEG owner-blocked); build track = Package A Step 1 first.**
- [2026-06-22f](2026-06-22f.md) — **Planning-only: walked AND resolved the Stationary Weapons register `[STW-1..6]`** (ballista/onager, GDD §5); STW now build-ready (gated behind Package A Step 1 + the DCH `map_objects` build it inherits). **[STW-1]** map-object model = A (forced — `type:"siege_weapon"` rides DCH's unified `map_objects`). **[STW-2]** occupancy = A (unit ends movement ON the siege tile; reuses `can_end_on_tile`). **[STW-3]** move-and-fire = **B, OWNER OVERRIDE** of rec A (may move onto the tile and fire same turn — no MOVED gate, *simpler* build; safe because the long range is a `[STW-4]` effective-weapon override that holds only *while on* the tile, so nothing is ferried). **[STW-4]** weapon swap = A (scoped equipped-weapon override; targeting math reused). **[STW-5]** ammo = A (`ammo:int` on the object, `-1`=∞, shared pool, snapshotted in `map_objects_state`). **[STW-6]** AI siege use = **B, OWNER OVERRIDE** of rec A (enemy AI mans + fires in v1). **Cross-cutting consequence:** [STW-6] B promotes the `siege_operator` AI profile from gated/build-later into **STW v1 scope**, coupling STW to the AI composition engine — recorded in AIP §2a row + gated-bucket note + GDD_10 §H. Edits: STW register (header RESOLVED + all six §3 resolutions + §1 move-fire note + §4 slice sketch rewritten with DoD#2 check_docs notes), AIP register, GDD_10 §H, planning_backlog §5 STATUS 2026-06-22f. DoD#1: planning only. DoD#2: no rule ratified now — `"siege_weapon"` map_objects value + `siege_operator` profile value-set guards pre-registered to land WITH the STW code. check_docs **17/17**, docs-only, no `__pycache__`. **All campaign-feeding registers now RESOLVED (PKGA·FOW·DCH·DTR·MET·AIP·STW). Next: IMP/DMR/BWN/MRD planning (REN/LEG owner-blocked); build track = Package A Step 1 first.**
- [2026-06-22e](2026-06-22e.md) — **Planning-only: walked AND resolved the second AI cluster — Group B `[AIP-11..16]`; the AIP register is now FULLY resolved (AI cluster design-complete for first build + full vision).** **[AIP-11]** difficulty bands = **NUMBERS ONLY** (stat scaling + roster/reinforcement adds; **no** activation/disposition/engagement-tier swaps — owner override of the vision §3 rec). **[AIP-12]** action-preview gating = **all three layered/OR** (per-chapter flag · per-band marker · player accessibility toggle; accessibility wins; non-binding). **[AIP-13]** disposition = **character-sheet item + More Info page**, NOT an always-on map glyph in v1 (reframes the vision §1 "visible by default" pillar → on-map telegraph = danger/threat overlays only; disposition = inspection lookup). **[AIP-14]** combat-AI workstream **deferred to its own later milestone** (engagement stays v1 "attack best target" seam; smarter tier gated per-chapter/global, **not** a band). **[AIP-15]** `unit_hp_below` MET trigger **deferred** to a later MET extension. **[AIP-16]** enemy/allied Pair-Up/Rescue **deferred — player-only in v1**. **Two consequences recorded:** [AIP-11] kills the engagement-tier↔band coupling + re-gates ML Option A (vision §6, per-chapter/global not a band); [AIP-13] reframes the §1 telegraph pillar. Edits: AIP register §10 (Group B COMPLETE + banner), vision §1/§3/§5/§6, GDD_10 §H (Group B RESOLVED block + ML re-gate). Also updated `ai_first_build_design` §10 deferred-seam contracts to the final Group B scope + `planning_backlog` §5 status line; **no new buildable plan was unblocked** (every Group B call is a defer or a parent-feature-time gating/UX decision). DoD#1: planning only. DoD#2: no checkable rule ratified. check_docs **17/17**, docs-only, no `__pycache__`. **Six registers resolved (PKGA·DCH·DTR·MET·FOW·AIP). Next session: pick up STW `[STW-1..6]` (owner-chosen; inherits DCH's `map_objects`); then IMP/DMR/BWN/MRD, REN/LEG gated. Build track = Package A Step 1 first.**
- [2026-06-22d](2026-06-22d.md) — **Planning-only: closed the final AI first-build decisions ([AIP-A11]/[AIP-A12]) + wrote the consolidated first-build design doc.** [AIP-A11] `set_ai` targets unit OR `group_id`, payload = partial `AISpec` patch (wake = patch Activation; role-flip = patch preset). [AIP-A12] groups wake together (any member's aggro/damage wakes the `group_id`; `ai_awake` latch keyed per-group). **NEW `ai_first_build_design_2026-06-22.md`** = buildable spec (composition engine `resolve_ai_spec`→`AISpec`→pure `plan_action` replacing the `match`; 4 invariants; data model; 9 presets; per-disposition procedures; `set_ai`/grouping; reinforcements/allied/boss; determinism; ordered build slice + tests; deferred seams). **AI cluster is design-complete for the first build** (gated only by Package A + §2 `ai_awake` slice). Group B [AIP-11..16] = next AI session. Register + vision + GDD_10 §H cross-reffed. check_docs **17/17**, docs-only, no `__pycache__`. Branch pushed. **Next session: finish AI Group B, then remaining planning items (STW/IMP/DMR/BWN/MRD/REN/LEG).**
- [2026-06-22c](2026-06-22c.md) — **Planning-only: audited every still-open AI decision; recorded continued register `[AIP-6..16]`** (Group A blocks the first AI build; Group B blocks the full vision). Group A = [AIP-6] ratify §7 refinements · [AIP-7] starter preset library + precedence · [AIP-8] gap-scope (gaps 1/2 MVP-or-fast-follow, driven by early content) · [AIP-9] reinforcement-unit AI config (new) · [AIP-10] allied/green-NPC AI (new). Group B = [AIP-11..16] difficulty band vocab / action-preview gating / disposition visuals / combat-AI timing / MET growth / AI pair-up-rescue. Noted ALREADY-decided (not open): determinism+tie-breaks (GDD_08), AI runs any faction, fog-cheat v1, no AI break/loot v1, boss=flag, leash data/thresholds/wake-save. **Group A walk deferred this session** (proposals carried in note §2; [AIP-8] needs owner's early-content vision). Register §10 added. check_docs **17/17**, docs-only, no `__pycache__`. **Session OPEN — resume by walking Group A.**
- [2026-06-22b](2026-06-22b.md) — **Planning-only: worked through the AI system design VISION — player-facing + casual campaign-builder + code architecture.** New doc `ai_system_design_vision_2026-06-22.md`. Unifying decision: **AI = a composition engine, not a profile enum** — three axes (Activation / Disposition / Engagement) + grouping + difficulty; profiles become **presets**, each §8 gap = "extend one axis." Owner vision: **difficulty = author-defined bands** (0..N per campaign, player picks; a band = a modifier overlay of stat mods / extra units / reinforcements / AI-option swaps on the base encounter — ties FOW-2 encounter layer + §2); full manual author control (level/stat/skill/every AI option) with recommended presets in docs + eventual **GUI campaign editor**; legibility = danger zones **+ disposition shown by default** plus an opt-in tutorial/easy **action-preview** (non-binding, recomputes as earlier AI act); **presets-first** data authoring (campaign-save branch I). Code: planner replaces the `match`; **four cheap MVP "don't-paint-into-a-corner" rules** (profiles→`AISpec`; disposition target unit-or-tile; activation reads optional flag; engagement is a function seam) keep difficulty/preview/gaps additive. AIP register §9 pointer + GDD_10 §H bullet added; Last verified bumped. **Addendum:** ML faction-controller side note recorded in vision **§6** — Option A (learned deterministic eval function, Engagement-axis seam, optional high-difficulty tier) = **seriously consider before 1.0**; Option C (full RL commander) = personal experiment, OFF the product roadmap. check_docs **17/17**, docs-only, no `__pycache__`. **Next = build Package A Step 1, firm the AI design plan (vision §5 opens + AIP §7), or walk STW / release-gate.**
- [2026-06-22a](2026-06-22a.md) — **Planning-only continuation of AIP: detailed profile walkthrough + FE gap analysis (recorded, nothing newly scheduled).** Went profile-by-profile (decision procedure / data / edge cases). Four MVP-spec refinements surfaced but **held** by owner → recorded in register §7 (territorial wake-on-attack; patrol=leashed-return-home not route-walker; flee optional `goal_tile`; `weakest`=KO-first threaded through move-tile). Then pressure-tested the plan vs the FE catalog → **§8 gap analysis, three layers:** (1) **event/turn-driven aggression** (biggest; wake is proximity-only — needs MET `set_aggro`/`wake` action or flag-honoring territorial = the proximity↔event aggro bridge); (2) **goal-tile/objective-seeking movement** (Defend chapters seize-your-throne + escape AI — needs `seek_tile`/`advance` disposition + `flee goal_tile`); (3) **per-engagement combat AI** as a SEPARATE workstream (auto-equip best weapon, decline bad trades, item-use, value targeting). Asterisked: `unit_hp_below` MET trigger (boss enrages), `spawn`-acts-immediately (ambush spawns), berserk breaks `_living_hostiles_for_faction`, AI door/wall-break deferred. MET [MET-3] + GDD_10 §H cross-reffed. check_docs **17/17**, docs-only, no `__pycache__`. **Next = build Package A Step 1, firm the AI design plan (resolves §7 + scopes §8 gaps 1/2), or walk STW / release-gate.**
- [2026-06-21k](2026-06-21k.md) — **Planning-only: walked AND resolved the Additional AI Profiles register `[AIP-1..5]`** + drafted the **full canonical profile target list** (§2a) at the owner's request. Code re-check: `_VALID_AI_PROFILES=["basic","passive","healer"]` (closed, validated 2 sites), `_act()` matches `ai_profile`, `is_boss` a real placement key (boss `e8` = `basic`+`is_boss`), no leash/flee/target logic today. **Taxonomy reframe** = two orthogonal axes (disposition / role) + a `target_policy` modifier; boss = `is_boss` compose, not a profile. Resolutions: **[AIP-1]** axes+modifier, MVP set grew; **[AIP-2]** A+C leash data = optional `enemy_placements` keys (home defaults to spawn); **[AIP-3]** BOTH as two profiles — `territorial`=latch (`ai_awake` saved), `patrol`=re-evaluate/return-home; **[AIP-4]** B global `GameConstants` defaults + override; **[AIP-5]** A `ai_awake` bool = only new §2 field. **MVP profiles** = `territorial`+`guard_tile`+`patrol`+`flee`+`target_policy`(`nearest`+`weakest`)+`is_boss` compose (owner pulled flee/patrol/focus_weakest into MVP). **Fast-follow (ungated):** retreat_when_low, kite, hunt policy. **Gated (land with parent feature):** fog_scout(FOW)/chest_looter(DCH)/siege_operator(STW)/buffer(M9)/thief_steal/dancer. DoD#2 noted: extend boot validator + check_docs guard for the profile + target_policy value-sets at build. Register + GDD_10 §H updated. check_docs **17/17**, docs-only, no `__pycache__`. **Six registers now resolved (PKGA/DCH/DTR/MET/FOW/AIP). Next = build Package A Step 1, or walk STW / a release-gate register.**
- [2026-06-21j](2026-06-21j.md) — **Planning-only: walked AND resolved the Fog of War / Line-of-Sight register `[FOW-1..7]`** (FOW-7 added this session). Picked FOW over AIP because its headline call [FOW-3] is the upstream for AIP's fog-scout. Code re-check found `ClassData.base_line_of_sight` also feeds the resolved `unit.data.line_of_sight` (reclass deltas, `Unit.gd:1059`); `_living_hostiles_for_faction` is the single AI seam. Resolutions: **[FOW-1]** flat radius + `compute_visible_tiles()` seam (true-LoS occlusion = later expansion); **[FOW-2 RESHAPED by owner]** fog is **encounter/scenario data (lives with roster + difficulty), NOT terrain-grid, NOT run-wide CampaignRules** — reuse terrain across chapters (castle defend→attack, summer-clear vs winter-blizzard); v1 = `MapData.fog_enabled` (roster already on MapData); forward = MapData terrain/encounter decomposition + timed/weather fog via MET `set_fog`; **[FOW-3]** AI cheats v1, symmetric fog reserved as future `CampaignRules.ai_respects_fog` rule (keep acquisition funnelled through the one seam); **[FOW-4]** A-full per-step mid-tween reveal WITH ambush interrupt; **[FOW-5]** `discovered_units` the ONLY new save field; **[FOW-6]** flat radius v1 (terrain-bonus/torch/concealment fast-follows); **[FOW-7 new]** lightable braziers = `map_objects` with `lit`+`vision_radius`, lit by unit action AND new MET `light` action. **Owner-added v1 scope folds into resolved registers, zero new save fields:** event-revealed rooms = MET `reveal_tiles`, braziers = DCH `map_objects` + MET `light`. FOW register + MET [MET-3] + GDD_10 §H updated. check_docs **17/17**, docs-only, no `__pycache__`. **Five registers now resolved (PKGA/DCH/DTR/MET/FOW). Next = build Package A Step 1, or walk AIP (fog-scout upstream now decided).**
- [2026-06-21i](2026-06-21i.md) — **Planning-only: drafted AND resolved a NEW register — Map Events / Triggers framework `[MET-1..9]`**, prompted by the user asking if general "trigger events on a specific unit's death" hooks existed. Finding: partial mechanism only (`EventBus.unit_died` broadcast, but reactions hardcoded; `ObjectiveCondition` watches `unit_ids` for win/lose only; **no flag store, no mid-map spawn seam, no general event framework**). Key insight: the unit-death hook is the *trigger half* and DTR-5's `on_break` is the *action half* of the SAME system → user chose to draft the full unified framework now. All 9 resolved to recommendation: **[MET-1]** author as `MapData.map_events: Array[Dictionary]`; **[MET-2]** v1 triggers `unit_died`/`turn_reached`/`object_broken`; **[MET-3]** v1 actions `reveal_tiles`/`flag`/`spawn` (shared `on_break` vocabulary); **[MET-4]** optional flag-predicate guard (branching); **[MET-5]** `once` + snapshot `map_events_fired` in §2; **[MET-6]** two flag scopes (map + campaign); **[MET-7]** keep `ObjectiveCondition` separate; **[MET-8]** extract `GameMap.spawn_unit` seam + deferred action timing; **[MET-9]** DTR's `on_break` folds in as the `object_broken` action list. A `MapEventManager` matches EventBus signals → flag-guard → deferred run → fire-once latch. Unifies DTR `on_break` + [CST-11] story-flip + DTR's reinforcement-spawn sub-feature into one runner. DTR register + GDD_10 §H updated; new §2 reservations: `map_events_fired` + map/campaign flag stores. check_docs **17/17**, docs-only, no `__pycache__`. **Four registers now resolved (PKGA/DCH/DTR/MET). Next = build Package A Step 1, or walk AIP/FOW.**
- [2026-06-21h](2026-06-21h.md) — **Planning-only: drafted AND resolved a NEW register — Destructible Terrain `[DTR-1..8]`** (breakable walls / fellable trees / breakable magic crystals), prompted by the user asking if any plan existed (none did — only the `MapData` save-TODO groundwork). Built on DCH's `map_objects` spine; code-grounded (`"W"` walls are static terrain; `CombatResolver` is duck-typed on the Unit interface). Resolutions: **[DTR-1]** one generic breakable damaged like a unit on the tile; **[DTR-2 OVERRIDE]** full unit damage pipeline (not flat-mt), no dodge/counter, durability via HP+Def; **[DTR-3]** breakable = a **real Unit in disguise, quarantined from the roster** via an `is_destructible_object` flag (combat + forecast reused unchanged; saved via `map_objects_state` not the roster); **[DTR-4 OVERRIDE]** anything breaks anything; **[DTR-5 EXPANDED]** on-break = reveal terrain + a general **event list** (bridge/`reveal_tiles`, spawn reinforcements, loot, flag); **[DTR-6 OVERRIDE]** no LoS concept at all (deferred wholesale to FOW); **[DTR-7]** no AI breaking v1; **[DTR-8]** rides `map_objects_state`, zero new save field. Main build risk = adding the quarantine skip-guard to every "for each unit" loop. Forward sub-feature surfaced: a mid-map reinforcement-spawn seam (`enemy_placements` is initial-only today). GDD_10 §H updated. check_docs **17/17**, docs-only, no `__pycache__`. **Next = build Package A Step 1, or walk AIP/FOW/STW.**
- [2026-06-21g](2026-06-21g.md) — **Planning-only: walked + RESOLVED the Doors & Chests register `[DCH-1..6]`** (the Layer-2 item that defines the shared `map_objects` model STW inherits). Verified code claims first (`TileActions` reserves `activate`; `ItemData` has `item_type "key"`; `MapData` has no object field + a runtime-terrain save-TODO), then resolved all six — **one owner override: [DCH-2] → UNIFIED `map_objects` model** (doors/chests/levers/stationary-weapons in one `MapData.map_objects`, not the recommended split). Others: **[DCH-1]** keys first (skill/staff fast-follow); **[DCH-3]** authored fixed loot (no RNG dep); **[DCH-4]** no AI-loot v1 (enemy chest-race → later AIP looter profile); **[DCH-5]** loot→opener (convoy deferred); **[DCH-6]** snapshot `map_objects_state` (reserve in §2). **Override consequence:** doors aren't terrain codes → need a runtime passability overlay from `map_objects` state (`GridManager` treats closed-door tiles as blocked) = slice 1's main work; upside = authored terrain `grid` stays immutable + the `MapData` save-TODO is fully resolved via `map_objects_state`. STW register updated (model settled, rec A confirmed); GDD_10 §H marks DCH build-ready. check_docs **17/17**, docs-only, no `__pycache__`. **Next = walk AIP/FOW/STW, or build Package A Step 1.**
- [2026-06-21f](2026-06-21f.md) — **Planning-only: walked + RESOLVED the Package A / `RngService` register `[PKGA-1..4]`** (the Layer-0 build-first item from the 2026-06-21e sort). All four → recommended option: **[PKGA-1]** own top-level milestone "Package A / Determinism" ahead of §2; **[PKGA-2]** only Steps 1–2 (service + snapshot contract) gate §2, Steps 3–4 (suspend/rewind) interleave with §2; **[PKGA-3]** `rewind_charges` deferred to §2's [CST-4] CampaignRules consolidation; **[PKGA-4]** two-RN "true hit" ships with Step 1 (flag the balance shift in the next playtest note). Planning gap on Package A is closed — the build guide (`rng_determinism_design_2026-06-11.md`) was already implementation-ready; these were sequencing/placement calls. Edits: register flipped RESOLVED; GDD_10 got a **new `## Milestone — Package A / Determinism` section** (5-step checklist, §2 gates marked, DoD) + §A row rewritten build-ready + §H note updated. check_docs **17/17**, docs-only, no `__pycache__`. **Next = START the Package A build — Step 1 (`RngService` + migration sweep + two-RN) then Step 2 (snapshot contract); unblocks §2. DMR/MRD/IMP available as parallel independent tracks.**
- [2026-06-21e](2026-06-21e.md) — **Planning-only: sorted ALL open planning items by FEATURE and by DEPENDENCY into one build/planning order to resume from.** Covers the 11 OPEN registers (drafted 2026-06-21d) + the §2 spine + Package A. **By feature:** F1 determinism/save (PKGA→§2), F2 map-readability (MRD), F3 map-objects (DCH→STW, shared `map_objects` model), F4 enemy AI (AIP core + fog-scout/chest-looter/siege-operator cross-overs), F5 fog (FOW), F6 tooling/code-health (IMP, DMR), F7 weapon rules (BWN), F8 release gates (REN→LEG). **By dependency (topological):** Layer 0 = **PKGA build-first** ([CST-12], unblocks §2+rewind); Layer 1 = **§2 spine** (defines the save schema reserving `discovered_units`/`ai_awake`/`map_objects_state`/`rewind_charges`); Layer 2 = FOW/AIP-core/DCH/BWN (core logic independent, save slice → §2); Layer 3 = STW (inherits DCH's `map_objects`) + AI cross-over profiles; **Independent track (any time):** DMR (pure refactor), IMP (tooling), MRD slices 1-3,5 (slice 4 save → §2); **Release-gate track (late, sequential, owner-input-blocked):** REN (needs owner's names)→LEG (needs owner to name corpus). **Critical path to playable campaign (D-D):** PKGA→§2→(DCH,AIP,FOW save slices). Added a GDD_10 §H pointer to this note as the dependency-sorted build order. check_docs **17/17**, docs-only, no `__pycache__`. **Next = walk+resolve PKGA then build Package A; DMR/MRD/IMP available as parallel independent tracks; gather [REN-1]/[LEG-1] owner inputs before the release push.**
- [2026-06-21d](2026-06-21d.md) — **Planning-only: drafted code-grounded plans + one-by-one OPEN-QUESTIONS registers for EVERY ready item on the 2026-06-21c candidate list** (Tier 1 + Tier 2 + DOC-012; GATED items skipped). User chose: one doc per item, all in one pass. Read the code first per item. **11 new `AGENT/Docs/*_open_questions_2026-06-21.md`:** (1) Package A `RngService` `[PKGA-1..4]` (gaps only — guide already exists: roadmap home, pre-§2 build scope, `rewind_charges` placement, two-RN confirm); (2) map-readability `[MRD-1..6]` (hover-peek + path arrows + designed threat range + grid-dim slider; overlay precedence is the core fork); (3) fog-of-war/LoS `[FOW-1..6]` (radius vs true-LoS, per-map vs rule, **AI-cheats-vs-symmetric headline**, reveal cadence, `discovered_units` save field; `UnitData.line_of_sight=4` already exists unread); (4) AI profiles `[AIP-1..5]` (`boss`=`is_boss`+`guard_tile`, leash/anchor data home, wake-latch, `ai_awake` snapshot); (5) doors/chests `[DCH-1..6]` (key-first, door-as-terrain vs object list, fixed loot, loot-to-opener since convoy deferred); (6) stationary weapons `[STW-1..6]` (shares doors/chests `map_objects` model, occupy-tile mount, move-and-fire gate, equipped-weapon override, emplacement ammo); (7) sprite importer `[IMP-1..6]` (roadmap's 4 decisions pre-loaded + 2 gaps: SpriteFrames-only output, `Sprite2D`→`AnimatedSprite2D`, `data/` for generated, pure-logic extraction for headless test, class-keyed frames); (8) `DataManager._ready()` decomposition `[DMR-1..3]` (pure refactor — phase-naming + unify error channels); (9) broken-weapon OPEN-5 `[BWN-1..5]` (reuse `uses_remaining==0`, global penalty const, repair-item-now); (10) D-A rename `[REN-1..5]` (**display-names-only, keep ids = save-transparent**, `check_docs` placeholder guard = completion signal; [REN-1] needs user's names); (11) DOC-012 legal `[LEG-1..5]` (research/decision doc, NOT a code plan — identify corpus+license, clean-reimplement vs license, attribution + asset audit; "not legal advice"; [LEG-1] needs owner to name the corpus). **Cross-cutting:** a shared `map_objects` tile-object model unifies items 5+6; §2 schema should reserve `discovered_units`/`ai_awake`/`map_objects_state`; "cheap-now" bias honored throughout. **Then ran an AUDIT pass** (re-read code behind every claim): all assumptions verified EXCEPT one real error — **`[MRD-3]` re-litigated the aggregate danger zone, which is ALREADY built (`get_enemy_danger_tiles`/`show_enemy_danger_zone`, MMB, src 3) and ALREADY integrated by the threat-range design** (its draft rec even contradicted that design); fixed §1 + withdrew [MRD-3] (ID kept, no renumber). Minor: PKGA-1 made precise, AIP gained a convergence note (fog-scout/chest-looter/siege-operator land as profiles there, not parallel plans). **Wired all 11 registers into GDD_10 Open Items Register (§A Package A + map-readability rows; §F D-A/DOC-012/OPEN-5 rows; §H full tiered index + forward §2-schema reservations) so work resumes from the roadmap.** **All registers OPEN — nothing resolved yet.** check_docs **17/17**, docs-only, no `__pycache__`. **Next = walk a register one-by-one with the user (start: Package A — smallest); [REN-1]/[LEG-1] need user-owned inputs.**
- [2026-06-21c](2026-06-21c.md) — **Planning-only: started the TECHNICAL side of §2 Campaign/Save.** Read the code first, then wrote two docs: `campaign_save_technical_plan_2026-06-21.md` (SaveManager autoload + I/O-free `SaveData` seam + `SaveCodec`, `CampaignData` progression graph, prep/results/defeat screens, JSON save schema, suspend design, **slice 0 = Package A + slices 1–7**) and `campaign_save_open_decisions_2026-06-21.md` (register `[CST-1..13]`, dependency-ordered). **Walked the register one-by-one with the user (4 batches) — resolved [CST-1..12]:** [CST-1] **A** new `SaveManager` autoload + I/O-free `SaveData` seam · [CST-2] **B (override)** ONE JSON-safe serializer for BOTH Retry and save (→ `InventoryEntry` lossless round-trip = hard test obligation) · [CST-3] **A+JSON** new `CampaignData` graph referencing map ids · [CST-4] **B (override)** hard-migrate ~70 rule call sites to `gs.campaign_rules.*` + delete loose fields · [CST-5] **B** deployment required/excluded/cap on the campaign node, `player_start_tiles` stay on MapData · [CST-6] **"everything is a campaign"** — every map auto-wraps as a 1-node campaign (one code path, no debug launcher), selector defaults last-started/most-recent, **NEW: CampaignData carries rule MANDATES(locked)/DEFAULTS(editable)** · [CST-7] **A** split MapResultsScreen(victory)/GameOverScreen(defeat menu), **preserve standings renderer for a future PvP scenario mode** · [CST-8] **between committed actions, any human-controlled faction** (upgraded from phase-start after a code check — delta = only `TurnManager._unit_states` + activation cursor; **F9 hotseat ⇒ free mid-enemy-phase suspend**; gate = idle FREE + human-driven; excludes mid-move/anim/attack + AI phases) · [CST-9] **A + author-designatable protected fields** (two SHA-256 hashes; `CampaignData.protected_fields` on a mandatory baseline) · [CST-10] **A** `user://saves/` manual slots + dedicated autosave + suspend + `saves_index.json` last-played pointer · [CST-11] **A** story-flip seam + `campaign_rule_flipped` notification only (trigger deferred) · **[CST-12] C (override): Package A (`RngService`) built FIRST, then §2** → re-sequences execution (Package A = next build step), makes suspend `rng` fields real, unblocks the rewind mechanic → surfaced **[CST-13]** (fold Turnwheel into §2 vs hooks-only+next; rec hooks-only; deferred to §2 execution kickoff). Scope expansions all user-driven (campaign rule mandates, author-protected fields, hotseat suspend, PvP mode). Updated `planning_backlog §2/§2b` (Package-A-first sequencing RESOLVED; PvP mode added as NEW deferred). check_docs **17/17**, docs-only, no `__pycache__`. **Next EXECUTION step = Package A plan/build, then §2 slices 1–7.**
- [2026-06-21b](2026-06-21b.md) — **Planning-only: firmed the §2 Campaign/Save cluster's PLAYER-FACING scope via a code-grounded, branch-by-branch interview** (before any code design). Read the code first → key finding: **no campaign loop exists today** (single-map loop, roster/gold/items persist in memory only, no disk save, no next-map/prep). Wrote `AGENT/Docs/campaign_save_player_facing_firming_2026-06-21.md` — a design-tree firming doc (branches **A–J**, each question/options/rec/resolution + a one-glance MVP scope + resolution log). **Resolved:** A — MVP spine first, **linear-now/overworld-ready** (progression-graph node model, not a hardcoded chain), between-map prep surface. B — in-app slots **+ filesystem export/import**, **persistent re-loadable** suspend, **reloading always free**, **multi-choice Game Over** menu, Continue = resume-most-recent, auto+manual save, slot menu (chapter/playtime/party/badges/editable label). C — prep = deploy/bench with per-map **required/excluded/cap** + player-assigned placement + save + launch; benched gain nothing. **D convoy / E shop / F recruit / H Pair-Up-persistence+Support+Rescue → all DEFERRED** (save reserves item/gold + full-roster persistence so later = data-growth). G — rules **player-locked + story-flippable** (scripted mutation seam + explicit flip notification) + tinkerer warning in save. I (NEW, homebrew) — **portable saves now, packs later**: human-readable JSON + `format_version` + `_warning` fields + editable `save_label` + **integrity hash**; load = **tamper-aware warn-and-continue** (not hard reject) + protected-field secondary warning; **export = single `.json`** with a **zip-vs-json sniffing importer** (`PK\x03\x04` vs `{`) so a future campaign-pack bundle imports with zero migration (Godot 4.6 `ZIPPacker`/`ZIPReader`). J (NEW, rewind) — **hooks now, mechanic later**: reuses `rng_determinism_design_2026-06-11.md` (per-action Turnwheel, per-map charge pool on `CampaignRules` default 3–5/`0=ironman`, snapshot carries `{map_seed,history_hash}`); cluster adds rule+charge persistence+defeat-menu entry; **hard-blocked on `RngService` (Package A)**. **I5 (user add): cross-version save transfer NOT a concern until 1.0** — keep `format_version`, write no migration pre-1.0. check_docs **17/17**, RNG guard PASS, docs-only, no `__pycache__`. Commit `49fdd62`. **Next session = the TECHNICAL §2 plan** (save schema + serialize/deserialize seam + progression-graph + prep screen + save/load/continue/export-import UI + suspend serializer incl. threat-range slice 4 + `CampaignRules` consolidation + story-flip seam + decisions register).
- [2026-06-21](2026-06-21.md) — **Planning-only: finished input/controls §1 drafting — 2 plans + 1 decisions register.** Wrote the two remaining §1 plans **decision-free**: `input_mode_resolver_implementation_plan_2026-06-21.md` (Auto/Touch/K&M detect-floor + conditional-promotion resolver, device-class detection w/ signal de-dup, `[controls]` persistence + `mouse_cursor` relocation, gray/back-door availability, **2 new `check_docs` value-set guards** for `input_mode`/`touch_controls`, 3 slices) and `key_rebind_ui_implementation_plan_2026-06-21.md` (Phase 2 capture flow, conflict policy, Reset escape hatch, **per-device-slot binding model** so a K&M rebind can't wipe the pad event, Esc-reserved + mirror-aware conflict check, 3 slices). Consolidated every open choice into `input_controls_open_decisions_2026-06-21.md` — **[ICD-1..7]** each with options + recommendation + resolution log: [ICD-1] active-mode owner (rec: dedicated `InputModeManager` autoload), [ICD-2] resolver landing point, [ICD-3] touch-vs-`emulate_mouse_from_touch` detection, [ICD-4] per-device binding model, [ICD-5] rebindable set + pad-capture-now, [ICD-6] conflict policy, [ICD-7] tune-live carry-overs. Code-grounded findings: `rebind_action` is destructive (would wipe pad bindings → [ICD-4]); `emulate_mouse_from_touch` double-emits (→[ICD-3]); the `ui_*` mirror+baseline already solves the stuck-key edge. **Then walked the register one-by-one with the user and RESOLVED ICD-1..6:** [ICD-1] new `InputModeManager` autoload; [ICD-2] resolver = gamepad slice 3 (no stub); [ICD-3] touch-first detection (iOS smoke gate); [ICD-4] per-device slots; [ICD-5a] ALL actions rebindable + always-on Reset + **new [ICD-5a-i]** human-readable cfg token serialization (so the hand-edit escape hatch works) + [ICD-5b] capture-any-device-now/glyphs-later; **[ICD-6] SWAP on conflict** (user overrode the block+warn rec). Baked every choice back into both plans, re-sliced the rebind plan so the persistence foundation lands before the UI, and flipped planning_backlog §1 to **build-ready (next step = execution, not planning)**. **Then planned the next cluster — individual unit threat range** (user pick; prerequisite for the gamepad contextual R3): read the code first → the per-unit threat calc is **already embedded** in `GridManager.get_enemy_danger_tiles`'s loop, so `individual_threat_range_design_2026-06-21.md` is a cheap **extraction** (`get_unit_threat_tiles`) + a **shared contextual resolver** (cursor over hostile enemy → per-unit, else faction) serving BOTH the mouse MMB toggle and gamepad R3; reuses `OVERLAY_DARK_RED` (no editor change); inline decisions **[TUR-1..3]** w/ recs; 3 slices (1–2 build-ready). Corrected the backlog's "mouse right-click" → it's **MMB** today (RMB = cancel). **Then resolved [TUR-1..3] with the user, who redesigned the model richer than the draft:** TUR-1 contextual MMB only; TUR-2 distinct **darker** red watch layer (new 5th overlay source) **+ watched-enemy markers**; TUR-3 **persistent watch set + display-mode cycle** — MMB-over-enemy adds/removes that enemy from a persistent `_watch_set`, MMB-over-empty cycles `_danger_mode` `full|selected|combined|none`, faction+watch shown **layered** in combined (set does NOT follow cursor). **Then resolved [TUR-4]:** auto-promote/demote the watch layer on empty↔non-empty; prune defeated enemies (store stable ids); set+mode **survive phases AND mid-map save/load** (teardown clears paint only, retains state; return-to-FREE repaints fresh; adds a forward dep on §2's suspend serializer); marker = a small **"D"** bottom-right of the tile (text, no asset; UI-polish placeholder). Reworked design §3–§9; updated backlog §2 (suspend-save dep) + §4. Wired the dependency both ways (gamepad §4/§10 ↔ this design). check_docs **17/17**, docs-only, no `__pycache__`. Next: threat-range slice 1 (extraction, headless) then slice 2 (one editor step = darker-red tile); or §1 slice-1s; or the v0.2.3 execution track; then §2 campaign/save (now also owns threat-range slice 4).
- [2026-06-20j](2026-06-20j.md) — **Planning-only: planned the input/controls cluster end-to-end (now COMPLETE).** Method = read the actual code before trusting the plan; it overturned two planned assumptions. (1) **Sequencing** (`cb619c0`): cross-ref found the input-mode design treats the debug-web shell as a *consumer* of the gamepad layer while the program order built it first with a throwaway `WebInputBridge` — decision = **plan the gamepad layer before the Web input approach**. (2) **Input-mode 4 decisions RESOLVED** (`a188cba`): Dedicated touch default (VG interim); **reuse the existing `[controls]` cfg section** (improved on the doc's `[input]` lean — `SettingsManager` already has `[controls]`+reset); glyph-swap = polish but build the `input_mode_changed` seam now; Deck aspect = accept letterbox. (3) **V021-15 selector design** (`68f04ad`): read all 3 surfaces → two presentation patterns (selector vs pager) over ONE nav pattern → extract a **`SelectionCursor` core + input arbiter**, NOT a unified render widget. (4) **Gamepad layer plan** (`858f742`): all 19 actions audited → locked map (zero combos); focus wiring resolved *with evidence* (hybrid model → bind BOTH `ui_*` + custom actions); R3 danger-zone made **contextual** (faction vs per-enemy) → depends on the unbuilt "individual threat range". (5) **Gamepad plan corrected** (`6be62e2`) after "can we rebuild?": `MapCursorInput` is keyboard-typed (`decode_key(InputEventKey)`, edge-based repeat) so d-pad/stick need a **decoder rebuild** — A (`decode(InputEvent)` + stick `get_vector` polling) + B (zoom → polled `get_action_strength` = analog) now; C (input-context owner → native menu focus) deferred to V021-15 (it subsumes the arbiter). (6) **Web shell input RESOLVED** (`4cacfc3`): code beat the binary — `mouse_cursor="click"` (V021-17) + default `emulate_mouse_from_touch` means a **tap already drives the cursor**, so **direct-touch primary now (+~7-button strip), virtual-gamepad optional later**; no gamepad dependency → Web ships right after v0.2.3 (smoke `emulate_mouse_from_touch` on iOS first). Docs-only, **check_docs 17/17** every commit, no `__pycache__`. Note lists the remaining planning candidates (broader input-mode resolver, key-rebind UI, individual threat range, campaign/save, release gates, UI/UX, systems, maps). Next: execution track (PR merge → v0.2.3 verify → Web build) or planning the next cluster.
- [2026-06-20i](2026-06-20i.md) — **Docs review → consolidated ALL open work onto the roadmap (GDD_10) for single-place findability.** Audit found the "single place" promise breaking in 4 spots: the **debug Web build** + **input-mode/gamepad architecture** were orphaned in handoff/design docs (the keystone gamepad layer had no roadmap entry); `v0.2.2_review_checkbacks` was a parallel deferral tracker (its live-verify check-backs + the **V021-09 `until_unequipped`/`until_end_of_map` forward primitives, rendered-but-no-producer** lived only there); pending-validation items were scattered per round with **8.4 Strength Tonic duplicated across v0.1.5.0 + v0.2.0**; and input-mode's **4 open design decisions** were untracked. User decisions: track everything **on the existing roadmap** (not a new doc), design decisions **inline**. Added **`## Open Items Register`** (7 grouped tables: near-term/forward, open v0.2.1, live-verify check-backs, no-producer primitives, validation/rerun unified, release gates/blockers, deferred milestones+backlog — each row status + detail pointer) and **`## Forward Platform Workstreams`** (Debug Web build + Input-mode arch with the 4 open decisions inline + DoD) after the Status Snapshot; Appendix C +4 rows. Anti-drift pointers added from the 3 previously-only-home docs back to the roadmap. check_docs **16/16**, no `__pycache__`, docs-only. Next session unchanged (PR merge → live verify → flip V021-18/19 → triage → Web build), now all indexed in the Open Items Register §A. **Follow-on:** asked which items are fixable now — most gated; landed the one clean headless win = V021-09 doc-sync guard `check_docs.py` **[17] check_duration_type_vocabulary** (GDD_07 list == `VALID_DURATION_TYPES`), negative-tested, check-back marked RESOLVED. check_docs 17/17. **Session-end:** assembled `planning_backlog_2026-06-20.md` — upcoming items lacking implementation-ready plans (input/controls, campaign/save, release-gate plans, UI/UX, other systems, maps), each with "what the plan should produce"; excluded already-plan-ready M8–M13/v0.2.3/Web; indexed from Register **§H** + Appendix C. **Next-session focus = input/controls cluster** (input-mode 4 decisions → V021-15 selector extraction design → key-rebind + gamepad audit).
- [2026-06-20h](2026-06-20h.md) — **Review since v0.2.1 + pre-verify cleanup + input-mode design + cut the v0.2.3 build.** Reviewed both unplayed rounds (v0.2.2 V021-01..17 + v0.2.3 display, ~1,300 LOC): #1 risk is **process** (two rounds, zero visual verify); flagged a latent `MenuScale` stale-min-size centre/clamp watch item (made it verify item #1, not a blind pre-fix). Landed only the headless-safe cleanups — `dcff28d`: `GridManager` movement-type now defers to `Unit.movement_type()` (dedup), `SettingsManager.save()` drops its `mouse_cursor` normalize side-effect. Long forward-looking **input-mode architecture** design (`dde2f88`, `AGENT/Docs/input_mode_architecture_design_2026-06-20.md`): model **modes not platforms** (gamepad layer = shared backbone of Deck + phone-controller + virtual-gamepad + web shell; **verified 0 gamepad bindings exist** = keystone); setting-vs-active split; nested tree (virtual gamepad under Touch, `mouse_cursor` under K&M); **detect-floor + conditional promotion** (boot + every device change) with platform-seeded cold start; **gray-don't-hide + back-door fallback**; dual focus/pointer UI tax; gamepad-first sequencing (Steam Deck eventual, < mobile). Then **cut v0.2.3** (user: build now, verify later): metadata bump (`test_release_metadata` 5/5), single self-contained `playtest_checklist_v0.2.3.md` covering **two unplayed rounds** (Part I display/crisp = gate, Part II v0.2.2, Part III regression), manifest, exported `Project_Prometheus_v0.2.3_debug.exe` (MZ, 101,264,440 B, SHA-256 `b92301f6…`), tagged **`v0.2.3`** at `d9d02c8`. **Held V021-18/19 at `[~]`** — build IS the verify vehicle. Suite 48/48 + check_docs 16/16. **Branch + tag `v0.2.3` PUSHED.** **`main` merge started + deferred:** the "~289 divergence" was stale local state — `origin/main` (`26c6a16`, PR #4) is a clean ancestor (82 ahead, 0 diverged, fast-forward), but direct push rejected (**protected branch — PR + `godot-tests-pr` required**) and no `gh`/token in this env, so finish via the GitHub PR later; local `main` ref is stale (`d291e12`). Next: finish the PR merge → live verify Part I → flip `[x]` → triage return → debug Web build.
- [2026-06-20g](2026-06-20g.md) — **Implemented v0.2.3 D2 — crisp Menu Scale (one pass).** Swapped the bitmap `Control.scale` zoom (the >1× blur source) for type scaling: `MenuScale.apply_to` keeps `Control.scale==1` and assigns a per-factor runtime-derived `Theme` (default font + container metrics, cached) for default text PLUS a descendant walk scaling each explicit `theme_override_font_sizes`/`constants` off a base captured in node meta (no compounding). `content_scale_factor` stays global 1 (HUD/map split holds). Centered panels recentre at natural size (`PRESET_CENTER`); a `ScrollContainer` subtree keeps its authored frame (Settings/UnitDetails), else shrink-wraps; contextual menus keep their cursor anchor. V021-08 clamp preserved by reducing the *font factor*, not the bitmap scale. **LevelUpScreen** `Panel`→`PanelContainer` (+`MarginContainer`); deleted the manual offset-juggling. `test_menu_scale` rewritten 14→**23** (scale==1, theme/override scaling, no-compounding, clamp); `test_level_up_screen` paths fixed. **Decision (AskUserQuestion):** chose runtime-theme+override-walk (A) over authoring a base Theme + stripping overrides (B) — A keeps scenes intact, factor 1 byte-identical, forward-compatible; "B from scratch, A for this codebase." DoD#1: GDD_07 + GDD_10 V021-18. **`V021-18/19` stay `[~]` — live crispness verify is the remaining gate.** Suite 48/48 + check_docs 16/16 green. Commit `94e1e2d`, **pushed**. Next: **code review** of the slice, then live-verify + v0.2.3 closeout.
- [2026-06-20f](2026-06-20f.md) — **Started v0.2.3 implementation: foundation + 1440p/4K + safe-area seam; deferred crisp Menu Scale.** Crash-recovery first (clean tree, nothing dangling; branch already on origin). Then 3 commits off `handoff_2026-06-20_v0.2.3.md`, no v0.2.2 checkpoint cut (user: start v0.2.3 directly): **`9249971` foundation** — D1 renderer→`gl_compatibility` (+`.mobile`; project boots clean = real validation), E5 explicit `stretch/aspect=keep`, E1 `is_display_config_supported()` desktop-gate on `_apply_display()` + the confirm-gated Settings rows; **`cfc431f` D4** — native `2560x1440`+`3840x2160` (16:9, reset 1280×720); **`0d79a38` D5/E6** — single `SettingsManager.get_safe_area_insets()` provider wired into `HUD._clamp_panel_on_screen` (zero on desktop), **D3 decided** HUD stays `Control.scale` so V021-04 stays Deferred. New `check_docs` guards **[15]** render/stretch keys + **[16]** 1440p/4K; `test_settings_manager` 14→16, `test_hud_layout` 14→15; all 48 suites + 16/16 checks green per commit. **Finding:** crisp Menu Scale (D2) is a per-scene, live-only-verifiable rework across ~11 heterogeneous scenes (reflow vs fixed-scroll vs plain-`Panel`) — **user chose to defer it to a dedicated visual slice** and land the headless-verifiable pieces now. GDD_10 `V021-18/19` marked `[~]` (Done/Remaining), **not flipped**. Branch pushed. Next: the D2 crisp slice, then v0.2.3 closeout/build-cut.
- [2026-06-20e](2026-06-20e.md) — **Planning-only: v0.2.3 design doc + program sequencing.** Wrote `display_scaling_resolution_design_2026-06-20.md` (web-aware) and got **D1–D6 signed off**: D1 renderer→Compatibility first, D2 crisp Menu Scale via one base Theme (no `Control.scale`; `content_scale_factor` stays global 1), D4 native 1440p/4K + explicit `stretch/aspect=keep`, D5 single safe-area provider, D6 no destructive save migration. Grounded in real config (stretch already `canvas_items`; no Theme exists; `content_scale_factor` reset to 1 today is backwards). Added effort-saving **E1–E6** for the coming mobile web release + DoD#2 `check_docs.py` guards. Researched cloud deploy → **itch.io + `butler` in GitHub Actions** (single-thread avoids COOP/COEP; handbook via GitHub Release + itch link), added as the web plan's "Release Infrastructure — Do First" section. **Sequenced: v0.2.3 NEXT, debug Web build AFTER** (web inherits the renderer/scaling foundation; its slice 1 shrinks to preset+metadata) — Program-order notes added to both handoffs. Docs-only; `check_docs.py` green; commits `cbb6200`/`156ae59`/`1aa017a` + this note. Branch 20 ahead, not pushed.
- [2026-06-20d](2026-06-20d.md) — **Debug Web playtest plan + implementation handoff.** Wrote `AGENT/Docs/debug_web_playtest_plan_2026-06-20.md` (`bec8bc4`) for an iPhone 14 Pro private debug Web channel: custom HTML emulator shell, 16:9 Godot canvas, non-overlapping touch controls, Web input bridge, single-thread/PWA-off export, itch.io hosting recommendation, and implementation/verification steps. Added `AGENT/Docs/handoff_2026-06-20_web_debug.md` after the itch.io vs GitHub Pages tradeoff discussion; next session should start with renderer + Web preset + metadata tests.
- [2026-06-20c](2026-06-20c.md) — **End-session cleanup + v0.2.3 handoff.** Moved **V021-12** class-skill More Info drilldown to the Polish phase (`3df09dc`), wrote `AGENT/Docs/handoff_2026-06-20_v0.2.3.md` for **V021-18/19 Display Scaling & Resolution** (`82370bb`), and added V021-17 click-mode live-verify notes to the v0.2.2 check-back list. Handoff is design-first: write `display_scaling_resolution_design_2026-06-20.md`, then implement crisp Menu/HUD scaling, native 1440p/4K choices, safe-area planning, docs/tests, and build closeout. Docs hooks green; no new runtime code. Next: decide whether to cut v0.2.2 checkpoint first, then start v0.2.3 from the handoff.
- [2026-06-20b](2026-06-20b.md) — **Resumed abrupt v0.2.2 implementation and landed V021-17 mouse/touch cursor mode** (`9c62a59`). Found worktree clean but implementation commits ahead of the latest note; GDD_10 showed V021-17 as the next non-deferred item. Added `mouse_cursor` `follow|click|disabled` with legacy migration (`enabled→follow`, `snap→click`), Settings UI labels, click-mode relocate-then-confirm behavior, terrain-panel click paging, DOC-011 value-set enforcement, and focused tests. Cleaned `test_map_cursor` fixture class ids to remove noisy class lookup errors. Verification: targeted settings/cursor tests, `check_docs.py`, full suite, and commit hook all green. Next: decide V021-12 stretch vs v0.2.2 closeout.
- [2026-06-20](2026-06-20.md) — **Forward-compat review of the v0.2.1 triage plan + approval + handoff** (docs only). Six findings folded in: V021-01 hotseat is **M15 activation-boundary** architecture (commit-then-yield; rollback deferred to M15B); V021-09 can't overload `"combat"` → adopted a **duration taxonomy** (`this combat`/`until separated`/`until unequipped`/`until end of map`/`x turns`) that M8/M9 inherit; V021-11 corrected (`great_knight` is armoured+mounted **today** so the precedence is load-bearing, DoD = **≥1** movement type, resolver ignores `dragon/beast/laguz`, keep skill-override-first, GDD_03 missing `light_footed`); **split V021-18+19 into a v0.2.3** "Display Scaling & Resolution" build; V021-15 built as one **shared selector**; V021-17 seeds an input-mode/mobile path. Fleshed out the triage plan, updated GDD_10 (Last verified→06-20), and wrote **`handoff_2026-06-20.md`** (ordered v0.2.2 playbook). Then three follow-up decisions: **V021-01 → per-selection rollback** (reuse `MapCursor._original_tiles`/`_undo_move_and_reselect`; M15B/M10 primitive); **V021-11 flying cost = 1 except walls**; and a **M8 tick-timing amendment → per-faction-phase duration ticking + activation-time enforcement** (reuses the existing `"turn"`/fort-heal tick point; updated GDD_01/02/10 + firming notes). check_docs green; **committed + pushed**. Next: implement v0.2.2 from the handoff.
- [2026-06-19e](2026-06-19e.md) — Triaged the **returned v0.2.1 playtest** into the Todo pipeline (plan-only, no code). Re-verify result: 1.1/1.2/1.3/1.5 passed; **1.4 F9 hotseat** and **1.6 HUD reset/terrain** came back. Archived the verbatim return, added a newest-first **"v0.2.1 findings"** action list (`V021-01..19`) to GDD_10, and wrote the fix plan `playtest_v0.2.1_triage_plan_2026-06-19.md`. Decisions: v0.2.2 scope = **everything incl. design projects**; map-HUD pair-up line **loses the stats** (keep `Support:` name, raise block); movement type stays a `special_qualities` tag — add explicit **`infantry`** default + precedence `flying>mounted>armoured>light_footed>infantry`, surface in More Info (no new field). Wrote **two full design docs**: terrain More Info **paging** (F-flipped pages + hidden state) and **mouse-only/touch cursor mode** (re-introduce a third `mouse_cursor` value `follow|click|disabled`). check_docs green; no tests run. **Next: get approval, implement v0.2.2.**
- [2026-06-19d](2026-06-19d.md) — Ran the scoped **v0.2.1 code review** (`5abaab4`, 9/10, no correctness bugs) and acted on it: bound the character-sheet pair-jump to `next_unit`/`prev_unit` so a controller can reach "View Support/Lead" (#2), unified the boosted/lowered stat colours (#3), and cached the HUD-editor styleboxes (#4) — `2f7f247`, `test_unit_details_screen` 18→19. Noted the project-wide gamepad-binding gap (actions are keyboard-only; pair-jump is controller-*ready* not reachable) under GDD_10 Platform Targets (`6ecbc3b`). Then **cut the v0.2.1 build**: exported `Project_Prometheus_v0.2.1_debug.exe` headless (Godot 4.6, exit 0, `MZ`-verified, 101,255,448 bytes, SHA-256 `334bf36f…`, from `6ecbc3b`), filled the build manifest, tracked the new `MenuScale` `.uid` sidecars the export generated, and tagged **`v0.2.1`** (`b848498`). Suite 48/48 + check_docs 12/12. **Branch + tag local — not yet pushed.** Bundle = exe + `playtest_checklist_v0.2.1.md`.
- [2026-06-19c](2026-06-19c.md) — Cut the **v0.2.1** playtest package and prepped a scoped code review. Bumped release metadata to v0.2.1 (`export_presets.cfg`, Main Menu `VersionLabel`, `environment_setup.md`; `test_release_metadata` 5/5). Wrote the single self-contained handbook `playtest_checklist_v0.2.1.md` (Part I bug-fix re-verify V020-01..06, Part II new sheet/More Info/scale surfaces + editor + Map 950 debuff, Part III display recheck **with the Borderless vs Fullscreen explanation V020-13**, Part IV regression pointer) + build manifest stub (export pending, SHA `<FILL AT EXPORT>`). Flipped `V020-13` `[x]` — completes the v0.2.0 triage list bar the deferred New Game items. Wrote `code_review_prep_v0.2.1_2026-06-19.md` scoping the review to `9c394b6..HEAD` (~570 lines/30 files) with a risk ranking; flagged the ~288-commit `main` divergence as a separate merge decision. Suite 48/48 + check_docs 12/12. **Branch pushed.**
- [2026-06-19b](2026-06-19b.md) — Implemented the approved v0.2.0 immediate fix round in three commits: camera/zoom cluster (`a0593c1`: high-zoom jitter + over-max no-op, live Map Zoom slider, defender-avoid combat forecast — `V020-01..03`); state/objective/HUD (`a501277`: F9-rerun phase-refresh guard, one-based Seize coordinate display, expanded Terrain More Info reflow on HUD reset — `V020-04..06`); and the approved menu/HUD scale split (`0bf7b3a`: new `Menu Scale` + `MenuScale.gd` helper across menu/modal screens, HUD owns its own size/pos — `V020-16`). Each commit updated related tests + GDD_01/07/10 (DoD#1); roadmap items flipped `[x]`. Then implemented the whole before-next-playtest Workstream D + validation + polish set: `V020-07` Int→Internal Lv, `V020-08` Pair Up `this combat` duration, `V020-09` HUD support name, `V020-15` CON/LoS sheet rows, `V020-11` class summary, `V020-10` weapon stat block in More Info + cursor/d-pad selector with `▶` highlight, `V020-14` Map 950 `debuff_tonic` (negative stat_buff), `V020-12` HUD-editor red/yellow outlines + `Scale Panel` labels + scaled sample text. `V020-13` (Borderless/Fullscreen) stays handbook-only. Full suite green (48 suites) + check_docs 12/12. **Branch not pushed.**
- [2026-06-19](2026-06-19.md) — Triaged the returned v0.2.0 playtest package into the live roadmap pipeline and wrote the plan-only fix handoff `playtest_v0.2.0_triage_plan_2026-06-19.md` (`5043dca`). User then resolved the open decisions (`4a78242`): split menu scale from HUD layout scale after bug fixes, add CON/LoS to the sheet, make full More Info selector before-next-playtest work, make the debuff tonic Map 950 test content, and put Borderless/Fullscreen wording in the handbook. No runtime code changed; targeted docs checks + `git diff --check` passed; full `check_docs.py` timed out at its feature-index scan.
- [2026-06-16b](2026-06-16b.md) — Code-reviewed the 2026-06-16 feedback slice and fixed 3 low-sev findings (`653a31f`: F9 cycle-guard refund + regression test, null-hotseat-controller comment, TILE_SIZE-derived `PU` badge). Then cut the combined playtest build — bumped to **v0.2.0** (minor bump: adds new features, not just fixes) and wrote a **single self-contained** handbook `playtest_checklist_v0.2.0.md` (folds the v0.1.5.0 base + Part I new features: character sheet, paired-unit, New Game, Display & Accessibility, F9). Exported `Project_Prometheus_v0.2.0_debug.exe` (SHA `0d73ef80…`) + manifest; tracked 7 orphaned `.uid` sidecars (`653a31f`, `9c394b6` + fills). Suite/docs/release-metadata green; visual confirmation pending on real Windows.
- [2026-06-16](2026-06-16.md) — Implemented initial playtester feedback: effective compact stats on the `I` sheet, paired-unit `PU` badge + partner sheet jump, New Game last-launched map selector semantics, and F9 all-faction hotseat debug override (`514f0fa`); docs/checks/full suite green.
- [2026-06-15d](2026-06-15d.md) — Session wrap. Planning push (plan branches off main): `plan-campaign-rules-save-load` (CampaignRules consolidation + JSON between-map save/load **+ deployment/shop/recruit** Tier-1 plans), `plan-sprite-importer` (4 decisions; `TILE_SIZE` stays 64), `plan-display-item4-panel-layout`. Finished/pushed item 4 + ran `/code-review high` → fixed 2 real findings (`c4bc4a8`: malformed-cfg crash guard + oversized-window clamp). New **confirm-or-revert** for display changes — 15s `DisplayConfirmDialog`, apply-now/save-on-confirm, generic `confirm:true` schema flag (`3633d98`, tests 7+8). Playtest handbook for today's 5 features (`3107239`). Saved `feedback_gdscript_lambda_capture` memory. Next session: bug-fix round — **#2.4 weapon-names-in-combat-preview** is the one implementable item; #8.5/3.1/8.4 await a tester re-run.
- [2026-06-15c](2026-06-15c.md) — Implemented Display & Accessibility **item 4** (per-panel HUD reposition + scale) on branch `hud-panel-layout-item4` off `display-accessibility-controls`. Core (`fa89acb`): `SettingsManager.hud_layout` + `HUD.apply_layout` (base-capture, scale clamp `[0.5,2]`, on-screen clamp, composes on item 3's global scale). Editor (`5125de2`): code-built `HudLayoutEditor` CanvasLayer (drag frames + Scale ± + Reset/Done/Cancel) launched from a new in-map "Edit HUD Layout" Settings button (HUD found via a `"hud"` group). Scope = the 5 persistent readouts only. Tests: `test_hud_layout` (12) + `test_hud_layout_editor` (6); drag UX playtest-verified. DoD#1 GDD_07/GDD_10 flipped. Not pushed; live verify pending.
- [2026-06-15b](2026-06-15b.md) — Plan-first implementation of the Near-Term Display & Accessibility controls (items 1–3; item 4 deferred). Item 1 **Map zoom** 0.25×–4×: root-caused + fixed the zoom retrofit risk by centralizing zoom-aware view math in `CameraController` (`_visible_world_size`), added the zoom API + `=`/`-`/`0`/wheel input + `map_zoom_index` persistence + the `AttackPreview` on-screen-tile fix (`a0e81da`, `c6316d6`). Items 2–3 **display**: new `[display]` settings — window mode + resolution via `DisplayServer`, and UI scale via `Window.content_scale_factor` (chosen over a font-only theme scalar for multi-platform; it's the OPEN-11 Steam Deck prerequisite), exposed in Settings (`74fc0ea`). New `test_camera_controller` (18) + settings coverage; DoD#1 across GDD_01/07/10; check_docs 12/12, full suite green. Not pushed; live visual verify pending.
- [2026-06-15](2026-06-15.md) — Cleaned up 5 orphaned headless-test godot processes + root-caused them (SceneTree `--script` tests idle forever if they error before `quit()`); hardened `run_tests.sh` with a per-suite `timeout` guard (`84800ee`) and saved a memory; ran the v0.1.6.0 distribution-readiness pass (binary hash matches docs, no placeholders, metadata 5/5) and made the 3-file distribution bundle explicit (`2d1d9be`). Branch pushed + in sync; build + handbook ready to distribute.
- [2026-06-14k](2026-06-14k.md) — Designed (plan-first) then implemented the comprehensive `I`-screen stat breakdown: per-stat personal base + class base + class cap (NO_CAP_DEFINED / "—") + every active bonus with source, boosted stats green. New drift-safe `StatContributions` collector (pair-up + stat skills) with a drift-guard test binding the sheet to combat; `test_class_stat_caps` DoD#2 cap invariant (Soldier = documented cap-less placeholder). Closes the #8.5 surface gap (pair-up now on the sheet, not just the HUD). Auras stay M9 (stubs). check_docs 12/12, suite green.
- [2026-06-14j](2026-06-14j.md) — Triaged the returned v0.1.5.0 playtest + tester godot.log into GDD_10. Re-verify pass confirmed all v0.1.4 fixes; fixed #8.6 (reclass option lines now autowrap, no horizontal scroll) and #8.3 (defender Battle Speed shown on no-counter previews, per decision). #8.5 pair-up re-reported but NOT reproducible — SHA-verified build contains the fix, a new real-roster E2E test passes, and the log is clean; left open pending a precise repro (working hypothesis: tester procedure). Logged 2.4 enhancement, flagged 3.1/8.4 NOT RUN.
- [2026-06-14i](2026-06-14i.md) — Session-end wrap: final new-machine-checklist accuracy pass (12-check count, PR-protected-main guidance, gh/gdtoolkit, CRLF gotcha) + day summary (review procedure designed→run→fixed→hardened). Next session: merge the integration PR; land the gdformat gate on a pip machine.
- [2026-06-14h](2026-06-14h.md) — Meta-review MR-9 polish (no decisions needed): clarified both Godot-4 UID mechanisms + empty-dir scope (Pillar 3), exempted catalog chapters via DOC-002a (Pillar 2), and acknowledged CROSS delta references (Pillar 1). Audit follow-up now complete except the env-blocked gdformat gate.
- [2026-06-14g](2026-06-14g.md) — Sorted the 4 remaining decisions: PL#8/PL#9→DoD#1/DoD#2 rename (parking-lot collision); scene-integrity CI gate (`check_scene_integrity.py`); procedure fix pass (meta-review §4 + check_docs checks 11/12, now 12 total); GDScript lint/format gate blocked (no pip) so wired into the new-machine plan instead.
- [2026-06-14f](2026-06-14f.md) — Pushed the audit work + 5 release tags; `main` push rejected (remote PR #1 diverged), so built/verified/pushed a clean `integrate-awakening-into-main` PR branch (all green, banned-path deletions preserved). Refreshed `new_machine_transfer_checklist.md` from what the audit exercised (10 checks, 39 suites, analyzer verify, `gh` CLI, tags note).
- [2026-06-14e](2026-06-14e.md) — Deferred audit items: decoupled `PairUpRegistry` from the scene `TurnManager` via a new `EventBus.support_orphaned` signal (#7), added a Pair Up invariant harness (#9, registry test 19/19), and doc cleanup — DOC-002a catalog-section variant + removed prohibited "Canonical" wording (#10). Flagged a PL#8/PL#9 naming collision between decision_index and AGENTS.md.
- [2026-06-14d](2026-06-14d.md) — Fix pass for the 2026-06-14 audit (4 user decisions): fort-heal floor bug + regression test (CR-2), tracked `.uid`/`.import` + new check_docs `.uid` guard (CR-1), un-rotted + CI-gated the analyzer suite & aligned pre-commit class-cache check (CR-3), retroactively tagged 5 releases + version↔tag check, removed empty `code/`, merged the branch into `main` (local, not pushed).
- [2026-06-14c](2026-06-14c.md) — First full execution of the review procedure: closed 4 coverage gaps, ran the parallel five-pillar audit at `e924bb4` (overall 6/10 — Code 9, Docs 8, Data 8, Process 8, Tests/CI/Build 6), and wrote the rollup + a meta-review of the procedure itself (8/10). Top finds: fort-heal floor bug (Doc↔Code), untracked `.uid` (3-pillar corroborated), analyzer suite red+ungated.
- [2026-06-14b](2026-06-14b.md) — Designed the full-project review procedure: new `AGENT/Review Procedures/` folder with a master orchestrator + 5 parallelizable pillars (Code, Documentation, Scenes/Data/Assets, Tests/CI/Build, Process/History incl. tooling recommendations); folded in and superseded the two old review-instruction docs.
- [2026-06-14](2026-06-14.md) — Triaged the returned v0.1.4 playtest package and resolved the whole round: 6 defects fixed (iron_axe, Pair Up Swap, rout-liveness + AI off-map guard, New Game persistence, promotion modal, pair-up bonus source collision) + 3 observations closed (combat-preview HUD avoidance, Battle Speed in preview, Soldier no-skill decision), each with a regression test.
- [2026-06-13n](2026-06-13n.md) — Prepared the project to move to a new machine, and closed out the session.
- [2026-06-13m](2026-06-13m.md) — Whole-codebase code review (8/10) + GDD field-test; implemented review fixes (RNG-usage guard, low-sev tests, roadmap + feature-index coverage).
- [2026-06-13l](2026-06-13l.md) — Built a documentation-review process (the doc analogue of the code-review instructions), executed it against the live doc set, and applied every…
- [2026-06-13k](2026-06-13k.md) — Executed Stage 6 — Documentation validation checks (DOC-011) and Stage 7 — Completion verification in a single session pass.
- [2026-06-13j](2026-06-13j.md) — Executed all of Stage 5 — Supporting documents cleanup in a single session pass.
- [2026-06-13i](2026-06-13i.md) — Stage 4 of the documentation consolidation: roadmap ownership consolidation, feature-index TBD cell filling, and Package G release decisions as…
- [2026-06-13h](2026-06-13h.md) — Finish the documentation consolidation Stage 3: rewrite the remaining system chapters (GDD_04, GDD_05, GDD_01, GDD_06, GDD_07, GDD_08) to the section…
- [2026-06-13g](2026-06-13g.md) — Continue the documentation consolidation: Stage 3.0 (adoption-matrix structure), 3.1 (GDD_02 + RNG merge), and 3.2 (GDD_03), then hand off.
- [2026-06-13f](2026-06-13f.md) — Execute Stage 2 of the consolidation master checklist, then hand off.
- [2026-06-13e](2026-06-13e.md) — Two things: (1) verify whether the existing handoff checklist actually covered the whole consolidation, and rebuild it as a complete plan; (2)…
- [2026-06-13d](2026-06-13d.md) — Resolve every remaining human-decision point in the documentation consolidation (the June update reference's open items) and reconcile the…
- [2026-06-13c](2026-06-13c.md) — Work through the documentation consolidation decision register one item at a time, closing the seven items the 2026-06-13 pre-implementation review…
- [2026-06-13b](2026-06-13b.md) — Review the documentation consolidation decision register and implementation plan for gaps that should be resolved before consolidation begins.
- [2026-06-13](2026-06-13.md) — Audit whether the v0.1.4 playtester handbook is fully usable by a tester who has never read the GDD or source code, then correct the gaps found.
- [2026-06-12d](2026-06-12d.md) — Review and reorganize the v0.1.4 playtester handbook before distribution.
- [2026-06-12c](2026-06-12c.md) — Bring the current playtester documentation up to date and produce a Windows playtester build that matches the current code.
- [2026-06-12b](2026-06-12b.md) — Start the documentation-consolidation project: define how the numbered GDD, Awakening corpus, roadmap, operational guides, decisions, tests, and…
- [2026-06-12](2026-06-12.md) — Implement the decisions from the GDD/codebase alignment review, synchronize the live GDD and roadmap with the resulting rules, commit the work, and…
- [2026-06-11](2026-06-11.md) — Start implementing the combat preview render-fix plan from 2026-06-10 by closing the zero-height forecast-row regression and hardening automated test…
- [2026-06-10](2026-06-10.md) — Diagnose the latest broken combat preview screenshot and produce a concrete implementation and verification plan without changing runtime code.
- [2026-06-09e](2026-06-09e.md) — Land the four "quick win" Workstream 6 polish items (W6a, W6e, W6f, W6i) that were deferred from session 2026-06-09d, ship a v0.1.3a follow-up build,…
- [2026-06-09d](2026-06-09d.md) — Close remaining workstreams from playtest_fix_plan_2026-06-09.md that can be verified through code + headless tests, ship a new playtest build at…
- [2026-06-09c](2026-06-09c.md) — Continue the 2026-06-09 playtest fix plan with Pair Up action integrity and combat preview / More Info fixes.
- [2026-06-09b](2026-06-09b.md) — Begin executing the 2026-06-09 playtest fix plan, starting with objective correctness and progression/class-state fixes.
- [2026-06-09](2026-06-09.md) — Review the latest playtest responses, settle implementation-critical behavior, and design an execution plan for all reported defects and usability…
- [2026-05-27](2026-05-27.md) — Bring the numbered GDD back in sync with the current code so a new developer can use the GDD as a reliable onboarding reference instead of stitching…
- [2026-05-26](2026-05-26.md) — Audit the roadmap docs against the session notes, then promote M9a ahead of M8 and start the first skill-engine implementation slice.
- [2026-05-25b](2026-05-25b.md) — Walk the four "decisions to review" buckets from 2026-05-25.md — M8, M9, M15 Part A, and the Objective-Map followup (Maps 002–005) — and fold every…
- [2026-05-25](2026-05-25.md) — Align the roadmap/backlog with the decision that pair_up, support, and rescue are campaign rules and capture the related design questions before the…
- [2026-05-24j](2026-05-24j.md) — Audit the long-form documentation for stale API/schema references and resync the docs that still described deprecated systems as current.
- [2026-05-24i](2026-05-24i.md) — Resync the project documentation so the docs match the current codebase, feature status, and remaining manual validation work.
- [2026-05-24h](2026-05-24h.md) — Expand the manual playtest checklist so it covers every player-facing feature added since the 2026-05-19 playtest wave, not just the new More Info…
- [2026-05-24g](2026-05-24g.md) — Merge the ad-hoc More Info playtest guidance into the authoritative manual task list so playtesting can resume from one checklist instead of…
- [2026-05-24f](2026-05-24f.md) — Close the two remaining review items from code_review_2026-05-24.md that still stood after the ActionMenu/HUD semantics clarification.
- [2026-05-24e](2026-05-24e.md) — Finish the Phase 1 More Info build order.
- [2026-05-24d](2026-05-24d.md) — Start implementing the More Info inspection mode using the locked plan in AGENT/Docs/more_info_mode_plan_2026-05-24.md.
- [2026-05-24c](2026-05-24c.md) — Lock the "More info" inspection-mode decisions into an implementation-ready handoff so the next session can start coding instead of reopening scope…
- [2026-05-24b](2026-05-24b.md) — Polish the promoted unit-details stat surface so active buffs/debuffs read more clearly in live play, and start the formal plan for the broader "More…
- [2026-05-24](2026-05-24.md) — Finish the remaining playtest follow-up items from the 2026-05-23h note: Pair Up disengage, mouse camera follow, Cavalier movement, debug hotkey UX,…
- [2026-05-23h](2026-05-23h.md) — Convert the current playtest comments into explicit action items, lock the new Pair Up lead-death behavior, and land a first bug-fix slice that helps…
- [2026-05-23g](2026-05-23g.md) — Unblock the Class / Skill Live Playtest checklist in AGENT/GDD/GDD_Manual_Tasks.md.
- [2026-05-23f](2026-05-23f.md) — Begin implementing the Pair Up combat refactor from the answers locked in this morning (2026-05-23e).
- [2026-05-23e](2026-05-23e.md) — Walk through the Pair Up / combat refactor questions one at a time and lock in answers as inputs for the implementation pass.
- [2026-05-23d](2026-05-23d.md) — Clean up the current Awakening refactor documentation and prepare the next session's Pair Up/combat refactor questions.
- [2026-05-23c](2026-05-23c.md) — Implement the next Awakening compatibility schema pass after the weapon/WEXP split: replace effective_level with internal_level, add class-side…
- [2026-05-23b](2026-05-23b.md) — Implement the first Awakening compatibility schema pass for weapon family and WEXP separation, including authored-data migration and validation…
- [2026-05-23](2026-05-23.md) — Resume the Awakening compatibility branch from the prior design session and resolve the open weapon/WEXP model questions before schema implementation.
- [2026-05-22c](2026-05-22c.md) — Track the new Awakening content expansion corpus, review its system scope against the current implementation, and document a compatibility-oriented…
- [2026-05-22b](2026-05-22b.md) — Port the safe fixes from code-review-fixes-2026-05-21 into class-skill-rebuild without regressing the newer class/reclass/promotion work.
- [2026-05-22](2026-05-22.md) — Set up the repo-side pieces for running the Godot test suite on GitHub Actions.
- [2026-05-21i](2026-05-21i.md) — Run a full repo review and resync the living verification/docs trail with the current codebase state.
- [2026-05-21h](2026-05-21h.md) — Implement the remaining class/skill follow-ups in order: - N6 level-1 class skill grant on unit creation - M7 Second Seal reclassing and demotion
- [2026-05-21g](2026-05-21g.md) — Finish M6 promotion implementation and add promotion-item restrictions by class and class group.
- [2026-05-21f](2026-05-21f.md) — Class & skill system rebuild to support the Awakening-style class descriptions in AGENT/GDD/Content Expansion/awakening_classes_and_skills.md.
- [2026-05-21e](2026-05-21e.md) — User asked to start tracking the outstanding content-expansion docs, then begin work on the real map registry source and the first hotseat validation…
- [2026-05-21d](2026-05-21d.md) — User asked how the planned map selector should handle rosters, requested the docs be updated to define that behavior explicitly, and then asked to…
- [2026-05-21c](2026-05-21c.md) — User asked for a plan for the hotseat validation map and requested a manual playtest checklist be added to the manual tasks document, including a…
- [2026-05-21b](2026-05-21b.md) — User asked to begin M15 Part A hotseat control work.
- [2026-05-21](2026-05-21.md) — User asked to clean up the codebase for the new dual-agent Claude Code + Codex setup after the shared Docker container work.
- [2026-05-20c](2026-05-20c.md) — User asked to fix the updated Docker container so one image can support both Claude Code and Codex without juggling separate containers.
- [2026-05-20b](2026-05-20b.md) — Picking up from the morning's 2026-05-20 session (C1 / M14 stages 1–3 shipped).
- [2026-05-20](2026-05-20.md) — Picking up from the 2026-05-19 session note.
- [2026-05-19](2026-05-19.md) — Second manual playtest produced AGENT/Docs/playtest2_findings_2026-05-19.md: 17 "bugs and oversight features" plus 8 "features to add to the to-do…
- [2026-05-18c](2026-05-18c.md) — First manual playtest of the MVP turned up 13 bugs, listed in AGENT/Docs/playtest1_findings_2026-05-18.md (renamed this session from…
- [2026-05-18b](2026-05-18b.md) — A review + documentation session — no game code changed.
- [2026-05-18](2026-05-18.md) — A planning / design session — no code changed, docs only.
- [2026-05-17](2026-05-17.md) — Execution session.
- [2026-05-16i](2026-05-16i.md) — Execution session.
- [2026-05-16h](2026-05-16h.md) — Review-and-plan session.
- [2026-05-16g](2026-05-16g.md) — Follow-up to Session F's review (code_review_2026-05-16c.md).
- [2026-05-16f](2026-05-16f.md) — Review-only session.
- [2026-05-16e](2026-05-16e.md) — Follow-up to Session D (the 2026-05-16 implementation plan A–E was complete).
- [2026-05-16d](2026-05-16d.md) — Continuation of Session C.
- [2026-05-16c](2026-05-16c.md) — Continuation of 2026-05-16 Session B (review + planning only).
- [2026-05-16b](2026-05-16b.md) — Continuation of 2026-05-16.
- [2026-05-16](2026-05-16.md) — Continuation from 2026-05-15.
- [2026-05-15](2026-05-15.md) — This session completed the full code review fix pass that started in the previous session.
- [2026-05-13c](2026-05-13c.md) — Worked through the implementation plan from code_review_2026-05-13c.md, completing items 2–10 and 13–14.
- [2026-05-13b](2026-05-13b.md) — LevelUpScreen.gd — Added is_instance_valid(self) guard in create_timer lambda to prevent calling methods on a freed node if the scene changes during…
- [2026-05-13_review-fixes](2026-05-13_review-fixes.md) — Scope: Full codebase review of all .gd scripts.
- [2026-05-13_a3-wiring](2026-05-13_a3-wiring.md) — Commit: Wire A2/A4 hooks and register ConditionManager autoload
- [2026-05-13](2026-05-13.md) — Resumed from 2026-05-13 code review (AGENT/code_review_2026-05-13.md) which was added as the most recent commit at session start.
- [2026-05-12b](2026-05-12b.md) — Performed a systematic review of all scripts.
- [2026-05-12_amendments-A1-A4](2026-05-12_amendments-A1-A4.md) — Amendment A1 — Data Layer Extensions - Added Phase 2 runtime fields to UnitData.gd: active_modifiers, skill_use_counters, damage_taken_this_map,…
- [2026-05-12](2026-05-12.md) — Phase label top-left, Turn counter top-right - UnitInfoPanel (bottom-left): name, class, HP/MaxHP, equipped weapon — shown on unit_selected, hidden…
- [2026-05-11b](2026-05-11b.md) — Added "defaultMode": "bypassPermissions" to /workspace/.claude/settings.local.json so Claude can read, write, and run Bash without per-operation…
- [2026-05-11](2026-05-11.md) — This session was a two-part code review and fix session covering the entire codebase through commit 62d0087 (the TILE_SIZE centralisation commit from…
- [2026-05-10](2026-05-10.md) — Created Godot 4.3 project (project.godot) with: - 1280×720 viewport, canvas_items stretch, pixel snap - Full input map (cursor WASD/arrows, confirm…
