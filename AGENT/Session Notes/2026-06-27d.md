# Session Note — 2026-06-27d  (A5 walk started → death-inventory disposition keystone `[DTH-1..12]` RESOLVED)

**Topic:** Started the **A5 — Campaign meta-rules & EXP/economy** walk (the last design cluster before the
F1 schema-lock). Mapped the cluster + its connected systems, audited the connected registers for staleness,
then walked the **keystone** sub-item — the **death-inventory disposition rule set** — end-shape-first to
RESOLVED and captured it as a new register `[DTH]`. Branch `docs-reorg-2026-06-23`. Design-capture only
(nothing built; no GDD/roadmap behavior change).

---

## 1. A5 cluster map + staleness audit (pre-walk)
Confirmed **A5 has no register yet** — it's the last cluster before Phase B. A5 owns: the **death-inventory
disposition rule set** (keystone), the **non-combat EXP/proficiency path** (`[AGT §6]`), the **`shop`+
`activate` interactive-trigger configs** (`[SHP-4b]`/`[VIL-2]` + the parked dynamic-shop forward-idea),
**Difficulty + Casual/Phoenix (#12) · Arena (#14) · PvP (#7)**, and the **style/source loadout cap**
(`[CEX-7]`/`[STY-3]`); plus fold-in of `[BAT-14..16]` and the optional F14/`[STM]` ratify.
**Staleness finding:** the atlas A5 bullet was already current (it had absorbed the post-2026-06-25 RDR-8/
CVR-5 snapshot-then-resolve + VAL credit-the-holder). Only two light touch-ups owed — `[BAT-16]` and the
`[DSP]` forward-pin under-named the snapshot-then-resolve ordering — **both closed this session** (now point
at `[DTH]`). Recommended start order: death-disposition → #12 → EXP path → loadout cap → shop/activate →
PvP → ratify F14 → Phase B.

## 2. The walk — `[DTH-1..12]` (RESOLVED, register `death_inventory_disposition_open_questions_2026-06-27.md`)
**Substrate reality verified:** `Unit.handle_death()` (`Unit.gd:488`) has **no inventory step today** (just
incapacitate/unregister/free) → disposition is greenfield. **Convoy is unbuilt** (`[CNV]` designed only) →
`to_convoy` is a Phase-C build dep. `CombatResolver` already does snapshot-then-resolve in miniature
(`:779–807`, defender-first credit) → `[DTH-8]` generalizes it.

**Model (owner-shaped):** disposition = an **author-ordered fall-through CHAIN walked per-item** — first
link that accepts an item wins. Links: **recipient** (`transfer_to_killer`/`nearest_ally`/`main_character`,
roster-order tie-break, "place what fits" → overflow continues the chain) · **placement** (`to_convoy`/
`drop_on_tile`) · **terminal** (`lost`).

**Owner decisions (8 forks):** (1) **disposition always runs; revived units return EMPTY** — initially
decided snapshot-and-restore, then **revised same session: Phoenix-revived units come back with an empty
inventory** (items disperse via the normal chain), to avoid re-handing items whose state diverged
(durability/broken/sold-from-convoy); drops the snapshot/restore machinery + the DTH-11(c) reservation;
makes the path fully uniform. Casual same (recommend, confirm in #12); PvP moot `[DTH-1]`; (2) **campaign default + per-faction**
override `[DTH-3]` — player `[to_convoy]`, enemy `[lost]`+droppable-on-a-separate-chain; (3) recipient
**overflow continues the chain per-item** `[DTH-4]`; owner **added `nearest_ally` + `main_character`**
recipient links; (4) **Key-Items walk a separate `lost`-banned chain** + per-item override `[DTH-5]`; (5)
**`drop_on_tile` parameterized** `{pickup: auto-on-entry|activate, on_map_end: chain}` — map-end fate
authorable (campaign default + per-drop override; key-item drops own) `[DTH-6]`; (6) **no special
no-killer case** — failed recipient → next link; **credit ≠ item transfer** (dead source still credited
EXP/objectives per `[VAL]`/`[RDR-8]`, but can't *receive*) `[DTH-7]`; (7) **snapshot-then-resolve** in
roster order, mutual kills both die `[DTH-8]`; (8) **`key_item_removed_from_map` designed in full** as a
**custody state machine** `{player_held,in_convoy,enemy_held,on_tile,off_play}` — author keys victory/defeat
on states, re-evaluated on custody transitions; composes F16 + the A4 objective system `[DTH-10]`.
**Seam `[DTH-9]`:** `handle_death(ctx={cause,killer})` single funnel + a `DeathDisposition` resolver; all
death causes (F5 ticks, hazard, `[DSP-14]` ring-out, scripted) must route through it — no second hook.
**F1 reserve `[DTH-11]`:** dropped-item tile stash (if drop used) · key-item custody (at-death snapshot
reservation removed by the DTH-1 revision).

## 2b. Side check — can a dialogue trigger victory/defeat? (asked this session)
**No — not built, not designed.** Objective system EXISTS (`ObjectiveCondition`, `TurnManager.
check_victory_conditions` → `EventBus.map_victory/defeat`) but the **`type` enum is CLOSED**
(`rout/defeat_boss/seize/escape/survive/protect/turn_limit` — no flag/event/predicate type) and is
**polled only at phase boundaries**. Four gaps block a DLG-driven win/loss: (1) no flag/predicate
condition type — the *same* closed-enum blocker `[DTH-10]` hit; (2) **no F6 flag store** (designed,
unbuilt; `GameState` has only debug flags); (3) **MET unbuilt + has no "end map" action** (v1 actions =
`reveal_tiles`/`flag`/`spawn`; DLG `command` can fire a MET action but none ends the map); (4)
event-timing (a mid-dialogue flag wouldn't re-check until the next phase boundary). **Two paths offered:**
(A) declarative — migrate the objective enum toward **F16 predicates** / add a `flag_set` type + build
F6 (also unlocks `[DTH-10]`); (B) imperative — a direct `end_map: victory|defeat` DLG/MET action.
**PINNED (owner 2026-06-27d):** a `[DTH-10]` forward-pin (design content) + the **atlas A4 objective
keystone** (scheduling), framed as an **objective-system-extensibility define-all-sweep item that MUST
precede the F1 lock (Phase B) — hence before the Phase C evaluation/triage session** (it's
schema-affecting, can't wait for triage). **Later this session this pin was CONSOLIDATED into `[TCV-4]`
(§2c).**

## 2c. A5 walk item #2 — Difficulty + Casual/Phoenix (#12) → `[DIF-1..7]` RESOLVED + `[TCV]` scaffolded
**Substrate:** death handling is one binary today (`permadeath_enabled`; On=`is_incapacitated`+no
redeploy, Off=returns next map = a crude casual); no difficulty tiers (but AIP already has a "difficulty
overlay" hook). **Two orthogonal axes.** Owner forks: **`death_mode` enum {classic/casual/phoenix}**
replacing the binary (`permadeath_enabled` derives; casual=return next map, phoenix=return next turn;
both return EMPTY per `[DTH-1]` — closes the DTH-1 Casual/PvP scope; phoenix returns mid-map empty =
accepted consequence) `[DIF-1/2]`; **difficulty = authored content VARIANTS ("palettes")** + a variant
selector (more/fewer enemies, terrain, on-map weapons, harder dialogue checks, less money, stricter loss
conditions), NOT an engine stat-formula — composes the campaign content model + map authoring + the AIP
overlay `[DIF-3]`; **player picks death-mode + difficulty from an author-allowed set** (extends
`NewGameScreen`) `[DIF-4]`.
**Big expansion → new pinned register `[TCV]`.** The difficulty answer grew two more layers: **(2)** every
rule knob declarable **author-locked vs player-exposed** (start and/or mid-run, within bounds) and **(4)**
**author-defined typed variables** driving **tag-scoped parametric effects** ("+x enemy stats", "+n levels
to tagged enemies"), money rates, etc. **These are one mechanism + hinge on F6 becoming a TYPED variable
store** — which is *also* what the §2b objective-extensibility pin and `[REQ-16]` want. **Owner: fold all
into ONE consolidated pre-F1 "typed campaign-variable store" walk** = `[TCV-1..6]` (OPEN/PINNED): TCV-1
F6→typed store · TCV-2 author-exposed-tunable mechanism · TCV-3 parametric tag-scoped effects (needs a tag
system + spawn-time modifier hook + REQ-16) · TCV-4 flag/predicate-driven objective conditions (**absorbs**
the §2b pin) · TCV-5 sequencing (pre-F1, schema-affecting) · TCV-6 schema reserve. `[DIF-5]` folds layers
2+4 into TCV; DIF owns only the death-mode/difficulty *selection* on top.

## 3. Commits this session
- `8447a4f` A5 walk: death-inventory disposition keystone RESOLVED `[DTH-1..12]` + cross-refs (atlas A5
  RESOLVED marker; BAT-16 + DSP-14 → `[DTH]`). INDEX/REGISTERS regen; 19 doc checks green.
- (this commit) Revise `[DTH-1]`: Phoenix-revived units return EMPTY (drop snapshot/restore + DTH-11(c));
  record the dialogue→victory/defeat side-check (§2b).
- (this commit) Pin objective-system extensibility as a pre-F1 sweep item (`[DTH-10]` forward-pin + atlas
  A4 keystone).
- (this commit) A5 #12 walk: `[DIF-1..7]` RESOLVED (difficulty & death-mode) + scaffold `[TCV-1..6]`
  (typed campaign-variable store, OPEN/PINNED) absorbing the objective-extensibility pin (§2c).

## 4. State / dependencies
- `[DTH]` RESOLVED. **Build deps (Phase C):** `[CNV]` convoy (the `to_convoy` default); `nearest_ally` uses
  the centralized `tile_distance` (`[HEX-9]` seam). **Forward to F1:** `[DTH-11]` reservations. **DoD#2
  candidate:** a check that every death cause routes through `handle_death(ctx)`.
- Closed the BAT-16 / DSP-14 staleness (both now reference `[DTH-8]`/`[DTH-9]`).

## 5. NEXT SESSION — CONTINUE THE A5 WALK
Done so far: `[DTH]` (keystone) + `[DIF]` (#12). Remaining A5 items in recommended order:
1. **Non-combat EXP / proficiency path** (`[AGT §6]`) — generalize the staff-EXP precedent + Bonus-EXP
   (#18) + Arena (#14). (Wire to `death_mode` + the difficulty selector per `[DIF-6]`.)
2. **Loadout cap** (`[CEX-7]`/`[STY-3]`, with `CampaignRules.max_skills`).
3. **`shop` + `activate` interactive-trigger configs** (`[SHP-4b]`/`[VIL-2]`) — fold in the parked
   requirement-driven dynamic-shops forward-idea (`SHP §4`).
4. **PvP (#7)** (composes the above).
5. Fold in **`[BAT-14..16]`** content/lifecycle + **ratify optional F14/`[STM]`** (STM's author-extensible
   stats overlap `[TCV-3]` tag-scoped stat effects — walk them aware of each other).
6. **`[TCV-1..6]` — the consolidated typed campaign-variable store walk** (OPEN/PINNED; absorbs the
   objective-extensibility/dialogue-win-loss pin + the `[DIF-5]` tuning/custom-variable layers).
   **Schema-affecting → MUST be walked before the F1 lock / Phase B, hence before the Phase C triage.**
7. → then **Phase B: F1 schema-lock**.
7. **Objective-system extensibility** (pinned §2b — enum→F16 predicates / `flag_set` type + F6 store +
   optional `end_map` action + re-check timing). **Schema-affecting → must be walked before the F1 lock /
   Phase B, hence before the Phase C triage.** Unlocks dialogue-driven win/loss + `[DTH-10]`.
8. → then **Phase B: F1 schema-lock**.
