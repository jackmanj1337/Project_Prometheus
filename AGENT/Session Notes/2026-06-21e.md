# Session Note — 2026-06-21e

**Topic:** Planning-only. **Sorted all open planning items (the 11 OPEN registers drafted
2026-06-21d + the §2 spine + Package A) by FEATURE and by DEPENDENCY**, producing a single
build/planning order to resume from next session. No code; no new registers; this note is the
sequencing layer over the existing docs. Docs-only; check_docs **17/17**; no `__pycache__`.

> **All registers are still OPEN** (drafted, not yet walked/resolved). This note orders *when*
> to walk/build each; it does not resolve any `[XXX-n]` item.

---

## 1. The items (each = a drafted register or a planned spine)

| Tag | Item | Doc | State |
| --- | --- | --- | --- |
| PKGA | Package A — `RngService` | `package_a_rngservice_open_questions_2026-06-21.md` + build guide `rng_determinism_design_2026-06-11.md` | register OPEN (gaps only); build guide ready |
| §2 | Campaign / save spine | `campaign_save_technical_plan_2026-06-21.md` + `campaign_save_open_decisions_2026-06-21.md` | [CST-1..12] RESOLVED; build-ready after PKGA |
| MRD | Map-readability cluster | `map_readability_open_questions_2026-06-21.md` | register OPEN ([MRD-3] withdrawn) |
| FOW | Fog of war / LoS | `fog_of_war_los_open_questions_2026-06-21.md` | register OPEN |
| AIP | Additional AI profiles | `ai_profiles_open_questions_2026-06-21.md` | register OPEN |
| DCH | Doors & chests | `doors_chests_open_questions_2026-06-21.md` | register OPEN |
| STW | Stationary weapons | `stationary_weapons_open_questions_2026-06-21.md` | register OPEN |
| IMP | Sprite importer productionization | `map_sprite_importer_open_questions_2026-06-21.md` | register OPEN |
| DMR | `DataManager._ready()` decomposition | `datamanager_decomposition_open_questions_2026-06-21.md` | register OPEN |
| BWN | Broken-weapon degraded mode (OPEN-5) | `broken_weapon_mode_open_questions_2026-06-21.md` | register OPEN |
| REN | D-A public-identity rename | `public_identity_rename_open_questions_2026-06-21.md` | register OPEN; needs owner's names |
| LEG | DOC-012 legal / licensing | `legal_licensing_open_questions_2026-06-21.md` | research register OPEN; needs owner to name corpus |

---

## 2. Sorted BY FEATURE (the "by feature" axis)

- **F1 · Determinism & Save foundation** — **PKGA**, then **§2 spine**. Substrate for almost
  everything; owns the save schema the other features reserve fields in.
- **F2 · Map readability / UI overlays** — **MRD** (hover-peek, path-arrows, grid-dim slider;
  individual threat range already DESIGNED). Mostly self-contained.
- **F3 · Map-interaction objects** — **DCH** → **STW** (both ride ONE `map_objects` tile-object
  model; DCH defines it, STW inherits it).
- **F4 · Enemy AI** — **AIP** core (leash/anchor: `territorial`, `guard_tile`), then the
  cross-over behaviors that converge here: **fog-scout** (needs FOW), **chest-looter** (needs
  DCH), **siege-operator** (needs STW).
- **F5 · Fog of war** — **FOW**. Rides F2's overlay infra; AI-under-fog headline is [FOW-3].
- **F6 · Tooling & code health** — **IMP**, **DMR**. Fully independent of gameplay features.
- **F7 · Weapon rules** — **BWN**. A `CampaignRules` toggle; cleanest after §2's rule
  consolidation ([CST-4]).
- **F8 · Release gates** — **REN** → **LEG** (sequential: rename first, then licensing). Both
  blocked on owner input.

---

## 3. Sorted BY DEPENDENCY (topological build/planning order)

```
LAYER 0 — Foundation (no upstream blocker)
  └─ PKGA  Package A / RngService          ── build FIRST ([CST-12]); unblocks §2 + rewind

LAYER 1 — Needs only Layer 0
  └─ §2    Campaign/save spine             ── gated behind PKGA; defines the save schema
            (reserves: discovered_units, ai_awake, map_objects_state, rewind_charges)

LAYER 2 — Core logic independent, but the SAVE/persistence slice needs §2
  ├─ FOW   fog of war        (save: discovered_units)        ← also rides F2 overlays
  ├─ AIP   AI profiles core  (save: ai_awake)
  ├─ DCH   doors & chests    (save: map_objects_state)       ← DEFINES map_objects model
  └─ BWN   broken-weapon     (CampaignRules field, [CST-4])

LAYER 3 — Needs a Layer-2 feature
  ├─ STW   stationary weapons        ← inherits DCH's map_objects model
  └─ AI cross-over profiles (land inside AIP):
       fog-scout      ← FOW + AIP
       chest-looter   ← DCH + AIP
       siege-operator ← STW + AIP

INDEPENDENT TRACK — no blocker, schedule any time (good palette-cleansers)
  ├─ DMR   DataManager decomposition   (pure refactor)
  ├─ IMP   sprite importer             (tooling; touches Unit Sprite2D→AnimatedSprite2D)
  └─ MRD   map-readability slices 1-3,5 (slice 4 = threat-range save → §2 forward dep)

RELEASE-GATE TRACK — late, sequential, OWNER-INPUT-BLOCKED
  └─ REN  D-A rename (needs owner's names) → LEG  DOC-012 (needs owner to name the corpus)
            run before the first public RC / 1.0; not blocking internal/playtest builds
```

**Critical path to the playable campaign (D-D):** PKGA → §2 → (DCH, AIP, FOW save slices).
Everything else hangs off the side of that spine.

---

## 4. Owner-input-blocked (raise these EARLY so the gate doesn't stall later)
- **[REN-1]** — the actual project-owned replacement names (factions/classes/items). I can
  draft candidates, but the names are the owner's creative call.
- **[LEG-1]** — name the source corpus/handbook + its license terms. The whole legal analysis
  hangs on this; consider qualified counsel for derivative-digital-works ambiguity.

These two don't block any of F1–F7; surface them whenever convenient before the public-release push.

---

## 5. Recommended next-session action
1. **Walk + resolve PKGA** (smallest register — 4 gap questions: roadmap home, pre-§2 build
   scope, where `rewind_charges` lands, two-RN confirm), **then start the Package A build**
   (the build guide is ready; it's the agreed-first execution step). This is the highest-leverage
   move — it unblocks Layer 1 (§2) and the rewind mechanic.
2. **In parallel / if a break from the spine is wanted:** walk + build a fully-independent item —
   **DMR** (lowest-risk refactor), **MRD** slices 1-3 (player-visible win, threat-range slice 1
   is already build-ready), or **IMP**.
3. **After §2 lands:** walk DCH (defines `map_objects`) → STW; AIP core; FOW; BWN. Then the
   AI cross-over profiles.
4. **Before public RC:** REN → LEG (get owner inputs first).

---

## 6. Wiring (so this is resumable from the roadmap)
- `GDD_10` Open Items Register §H already indexes all 11 registers (added 2026-06-21d); added a
  pointer from §H to **this note** as the dependency-sorted build order.
- No status flips — nothing's behavior changed; this is a sequencing note over OPEN registers.

## 7. Verification
check_docs.py **17/17 PASS**; docs-only; no `__pycache__`.

## Session status: CLOSED.
Next session = **walk + resolve PKGA, then build Package A** (Layer 0 → unblocks §2), with
DMR / MRD / IMP available as independent parallel tracks. Owner inputs [REN-1]/[LEG-1] can be
gathered any time before the release-gate push.
