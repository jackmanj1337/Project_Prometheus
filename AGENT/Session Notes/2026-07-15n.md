# Session Note - 2026-07-15n

## What was done

- Implemented B6 archive slice 3 with the non-autoload
  `CampaignPackInstaller`: successful preflight results extract only admitted
  paths beneath unique service-owned staging.
- Added full staged-tree manifest, Tier-2 schema/cross-reference, and optional
  media validation before atomic promotion to `installed/{id}/{version}`.
- Existing versions are rejected without replacement or merge behavior. Every
  extraction, validation, and promotion failure removes staging and preserves
  installed bytes; installation remains isolated from selection, activation,
  settings, and saves.
- Added real-ZIP tests for byte-exact installation, duplicate preservation,
  invalid second-pass content, injected transaction failures, optional-media
  repair reporting, invalid preflight rejection, and final-path containment.

## Next

Implement deterministic archive export: enumerate only manifest/catalogue and
approved Tier-1 files, write normalized paths in lexical order, exclude all
state/cache files, then re-preflight and byte-compare an export/import round trip.

