# Session Notes — 2026-06-14b

## Topic
Designed a complete-project review procedure: a master orchestrator + five
parallelizable pillar documents, folding in and improving the two predecessor
review-instruction docs.

## What was done
- Created `AGENT/Review Procedures/` with:
  - `00_Master_Review_Procedure.md` — orchestrator: when to run, the five-pillar
    index + coverage map, shared prerequisites/baseline (pin SHA, green
    check_docs + run_tests), parallel sub-agent execution model, shared severity
    rubric (single source of truth), scoring, the rollup report spec, the PL#8/PL#9
    feedback loop, the historical-data catalog, and a PL#9 enforcement-candidate
    backlog.
  - `01_Code_Pillar.md` — improved from `code_review_instructions.txt`;
    GDScript-specific, includes the known-footgun checklist (setter recursion,
    headless autoload cross-refs, exported node refs, typed arrays, class cache).
  - `02_Documentation_Pillar.md` — improved from
    `documentation_review_instructions.md`.
  - `03_Scenes_Data_Assets_Pillar.md` — new; scene wiring, .tres/data integrity,
    import pipeline, autoload registration; uses the godot-analyzer MCP.
  - `04_Tests_CI_Build_Pillar.md` — new; coverage mapping, run_tests/CI/hooks,
    check_docs enforcement audit, export/docker. Runs in a worktree.
  - `05_Process_History_Pillar.md` — new; workflow adherence (PL#8/PL#9/session/
    commit), git hygiene, decision traceability, process-effectiveness trends, and
    evidence-backed tooling/workflow recommendations.
- Each pillar has a consistent template ending in a copy-paste sub-agent dispatch
  brief, so the audit fans out to parallel agents and merges into one rollup.
- Marked the two predecessors **Superseded (2026-06-14)** with pointers to the new
  pillars; kept for provenance per the lifecycle governance.
- Decisions confirmed with the user up front: dedicated folder + master index;
  fold-and-improve the old procedures; process pillar covers decision
  traceability + workflow adherence + git hygiene + process effectiveness +
  tooling recommendations; orchestrated sub-reports + rollup; parallel sub-agents.
- `check_docs.py` still green (the new folder is outside its scan set, so no CI
  risk; backtick paths kept real anyway).

## Commits
- (this session) Add full-project review procedure folder + supersede old review docs.

## Next session / open items
- Optional: wire the PL#9 enforcement candidates (master §10) into `check_docs.py`
  and add the new folder to its scan set so procedure-doc paths are validated.
- Optional: do a first real full-audit run using the new procedure to validate it
  end-to-end (and produce the first rollup).
