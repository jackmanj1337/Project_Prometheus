# Session Note - 2026-07-03h

## Topic
Campaign-node composition vocabulary and open questions (`[CNC-1..10]`).

## What was done
Grounded the campaign/map/chapter/encounter vocabulary against the live `MapData` shape,
`map_registry.json`, the campaign/save technical plan, `[PHB]`, `[MET]`, and the partially
resolved `[PUG]` register. Created
`AGENT/Docs/registers/campaign_node_composition_open_questions_2026-07-03.md`.

## Decisions captured
- `[CNC-1]` RESOLVED: `CampaignNode.node_id` is the durable save/progression identity.
  The same node can change `node_type` and the references it carries (`prep_panels`,
  chapter/story metadata, encounter pointer, map pointer) without becoming a different
  node.
- Updated `campaign_save_technical_plan_2026-06-21.md` so the campaign plan explicitly
  preserves that stable-node / mutable-reference rule.
- Updated `[PUG]` so `[PUG-4..7]` point at `[CNC-1..10]` as the blocker for skirmish
  and encounter format.

## Open questions recorded
`[CNC-2]` node reference slots, `[CNC-3]` node type vocabulary, `[CNC-4]` Battle Map vs
Battle Encounter split, `[CNC-5]` chapter semantics, `[CNC-6]` valid node composition
matrix, `[CNC-7]` save/resync behavior when nodes change, `[CNC-8]` launch/completion
semantics, `[CNC-9]` skirmish/generated encounter attachment, and `[CNC-10]` JSON vs GUI
authoring shape.

## Validation
- `python3 AGENT/Docs/gen_docs_index.py`
- `python3 AGENT/Docs/check_docs.py` - PASS, all 21 checks green.

## Commits
- (this commit) - add `[CNC]` campaign-node composition register.

## Next Session
- Walk `[CNC-2..10]` before closing `[PUG-4..7]`.
- After `[CNC]` resolves, return to `[PUG]` and lock `ForceSpec`, `EncounterDef`, skirmish
  map source, and scaling defaults.
