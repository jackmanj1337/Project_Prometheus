# Session Note — 2026-06-22c

**Topic:** Planning-only. **Audited every still-open AI decision** and recorded them as a continued
register `[AIP-6..16]` in the AIP doc (Group A = blocks the first AI build; Group B = blocks the
full vision). Walking/resolving Group A was **not** done this session (owner deferred the structured
walk). Docs-only; check_docs **17/17**; no `__pycache__`.

---

## 1. The audit
`[AIP-1..5]` are RESOLVED (MVP profiles build-ready). Remaining opens, grouped:

**Already DECIDED (closed — not open):** determinism + tie-break stability (GDD_08); AI runs any
faction (`acting_faction` parameterized); fog = AI cheats v1 (FOW-3); no AI break/loot v1
(DTR-7/DCH); boss = `is_boss` compose (AIP-1); leash data/thresholds/wake-save (AIP-2/4/5).

**Group A — blocks the first AI build:**
- **[AIP-6]** ratify the four held §7 refinements.
- **[AIP-7]** starter preset library + composition-precedence rules.
- **[AIP-8]** §8 gap-scope: gaps 1 (event-aggro) & 2 (goal-tile) in MVP or fast-follow (driven by
  early content — Defend map forces gap 2; scripted "charge on turn N" forces gap 1).
- **[AIP-9]** reinforcement-unit AI config (MET-spawned units: profile/group/activation +
  `act_on_spawn`). *(new/homeless before this session)*
- **[AIP-10]** allied / green-NPC AI (same system + relation targeting). *(new/homeless)*

**Group B — blocks the full vision (post-MVP; several already in vision §5):**
- **[AIP-11]** difficulty band-modifier vocabulary · **[AIP-12]** action-preview gating + UX ·
  **[AIP-13]** disposition-indicator visual language · **[AIP-14]** combat-AI workstream timing +
  engagement-tier↔difficulty coupling · **[AIP-15]** MET growth (`set_aggro`/`unit_hp_below`/
  spawn-immediate) · **[AIP-16]** AI Pair-Up/Rescue (likely defer).
- *(pointers)* AI turn-pacing/skip = UI/UX backlog; ML Option A = vision §6 (pre-1.0).

## 2. Proposed (NOT yet ratified) for Group A — carried for the next walk
- [AIP-6] adopt all four. [AIP-9] reinforcements carry same AI keys + `act_on_spawn` (default
  false). [AIP-10] same composition system, faction-relation targeting, no special-casing.
- [AIP-7] starter presets: `grunt`/`sentry`/`guard`/`sleeper`/`patroller`/`coward`/`runner`/
  `hunter`/`healer`; precedence base→placement→group→difficulty; `flee` ignores `target_policy`.
- [AIP-8] needs owner's early-content vision (no recommendation possible blind).

## 3. Edits
- `ai_profiles_open_questions_2026-06-21.md` — new §10 open-decisions register `[AIP-6..16]`.
- DoD#1: planning only. DoD#2: no rule ratified.

## 4. Verification
check_docs.py **17/17 PASS**; docs-only; no `__pycache__`.

## 5. Group A walk — progress (conversational, one at a time)
- **[AIP-6] RESOLVED 2026-06-22c** — all four §7 refinements adopted as written ("good for
  starting points"). Owner requested a better name for the return-home profile → **renamed
  `patrol` → `tethered`** (working name; consistent with the `leash_radius` field, avoids the
  route-walker connotation of "patrol"). Rename propagated through the AIP register, MET [MET-3]
  cross-ref, and GDD_10 §H (live docs only; historical session notes left as-is). §7 flipped to
  RATIFIED.
- **[AIP-7] RESOLVED 2026-06-22c** — 8 starter presets (`grunt`/`guard`/`sleeper`/`tethered`/
  `coward`/`runner`/`hunter`/`healer`), flavor names kept, **`sentry` collapsed into `guard`**
  (`passive`+`guard_tile` = one `hold_tile` disposition, home defaults to spawn). Precedence
  base→placement→group→difficulty; `flee` ignores `target_policy`. Recorded in register §2b.
  **Owner requirement added: a MET `set_ai` action** to change a unit's/group's preset/axes on an
  event trigger (generalizes gap-1 wake; waking = `set_ai` on Activation only). Promoted in MET
  [MET-3] from candidate → confirmed; [AIP-15] updated; vision §5 + GDD_10 §H updated.
- **[AIP-8] RESOLVED 2026-06-22c** — owner: "make them available" → **both §8 gaps pulled into the
  first AI build.** Gap 1 (event/turn aggression) = the confirmed MET `set_ai` action + event/flag
  activation. Gap 2 (goal-tile seeking) = new **`seek_tile`** disposition (preset **`raider`**,
  working name) + the ratified `flee goal_tile` (`runner`). Enlarges the first build but unlocks
  Defend chapters + scripted activation from day one. Updated §2a (disposition table + buckets),
  §2b (9th preset `raider`), §8 (Layers 1 & 2 → MVP), §4 slice (steps for seek_tile + set_ai +
  `goal_tile`/`group_id` keys), MET [MET-3] (set_ai now first-build), GDD_10 §H.
- **[AIP-9] RESOLVED 2026-06-22c** — MET-spawned reinforcements carry the same AI keys as a normal
  placement + **`act_on_spawn: bool` (default false)** for FE ambush spawns (resolves the
  spawn-acts-immediately asterisk).
- **[AIP-10] RESOLVED 2026-06-22c** — allied/green-NPC factions use the same composition system,
  no special-casing; faction-relation targeting (`_living_hostiles_for_faction` already relational).
- **GROUP A COMPLETE** — the first AI build is spec-complete. Resolving the gaps surfaced two small
  **build-time schema details** (not design forks), recorded as **[AIP-A11]** (`set_ai` payload:
  unit/`group_id` target, whole-preset vs per-axis patch) and **[AIP-A12]** (`group_id` aggro
  semantics: does one unit spotting wake the whole group? rec yes). `raider` preset name confirmed
  (owner: "those both look good").

## Session status: CLOSED — Group A complete; cluster's remaining opens are Group B (post-MVP) + 2 build-time details.
**Remaining AI-cluster opens:** [AIP-A11]/[AIP-A12] (small, build-time) · Group B [AIP-11..16]
(difficulty band vocab, action-preview gating, disposition visuals, combat-AI workstream timing,
`unit_hp_below` trigger, AI pair-up/rescue) · pointers (AI turn-pacing → UI backlog; ML Option A →
vision §6, pre-1.0). None block the first AI build. Build order unchanged: **Package A Step 1 first.**
