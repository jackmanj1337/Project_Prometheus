# Session Note — 2026-06-22c

**Topic:** Planning-only. **Audited every still-open AI decision** and recorded them as a continued
register `[AIP-6..16]` in the AIP doc (Group A = blocks the first AI build; Group B = blocks the
full vision). Walking/resolving Group A was **not** done this session (owner deferred the structured
walk). Docs-only; check_docs **17/17**; no `__pycache__`.

---

## 1. The audit
`[AIP-1..5]` are RESOLVED (MVP profiles build-ready). Remaining opens, grouped:

**Already DECIDED (closed — not open):** determinism + tie-break stability (GDD_08); AI runs any
faction (`acting_faction` parameterized); fog = AI cheats v1 (FOW-3); no AI break/loot v1
(DTR-7/DCH); boss = `is_boss` compose (AIP-1); leash data/thresholds/wake-save (AIP-2/4/5).

**Group A — blocks the first AI build:**
- **[AIP-6]** ratify the four held §7 refinements.
- **[AIP-7]** starter preset library + composition-precedence rules.
- **[AIP-8]** §8 gap-scope: gaps 1 (event-aggro) & 2 (goal-tile) in MVP or fast-follow (driven by
  early content — Defend map forces gap 2; scripted "charge on turn N" forces gap 1).
- **[AIP-9]** reinforcement-unit AI config (MET-spawned units: profile/group/activation +
  `act_on_spawn`). *(new/homeless before this session)*
- **[AIP-10]** allied / green-NPC AI (same system + relation targeting). *(new/homeless)*

**Group B — blocks the full vision (post-MVP; several already in vision §5):**
- **[AIP-11]** difficulty band-modifier vocabulary · **[AIP-12]** action-preview gating + UX ·
  **[AIP-13]** disposition-indicator visual language · **[AIP-14]** combat-AI workstream timing +
  engagement-tier↔difficulty coupling · **[AIP-15]** MET growth (`set_aggro`/`unit_hp_below`/
  spawn-immediate) · **[AIP-16]** AI Pair-Up/Rescue (likely defer).
- *(pointers)* AI turn-pacing/skip = UI/UX backlog; ML Option A = vision §6 (pre-1.0).

## 2. Proposed (NOT yet ratified) for Group A — carried for the next walk
- [AIP-6] adopt all four. [AIP-9] reinforcements carry same AI keys + `act_on_spawn` (default
  false). [AIP-10] same composition system, faction-relation targeting, no special-casing.
- [AIP-7] starter presets: `grunt`/`sentry`/`guard`/`sleeper`/`patroller`/`coward`/`runner`/
  `hunter`/`healer`; precedence base→placement→group→difficulty; `flee` ignores `target_policy`.
- [AIP-8] needs owner's early-content vision (no recommendation possible blind).

## 3. Edits
- `ai_profiles_open_questions_2026-06-21.md` — new §10 open-decisions register `[AIP-6..16]`.
- DoD#1: planning only. DoD#2: no rule ratified.

## 4. Verification
check_docs.py **17/17 PASS**; docs-only; no `__pycache__`.

## Session status: OPEN — Group A walk pending.
Resume by walking Group A `[AIP-6..10]` (proposals in §2 above; [AIP-8] needs the early-content
vision). Group B rides the later "firm the AI design plan" session. Build order unchanged:
**Package A Step 1 first.**
