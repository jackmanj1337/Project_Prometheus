# Session Note - 2026-06-29d

## Topic
Project-scope reframe (learning/portfolio first, builder second, bounded
power-user access) and a planned GDD stale-assumption audit for next session.

## What was done
- Walked the documentation structure with the owner: the `AGENT/Docs/` type
  layout + three generated indexes, the GDD's place in the DOC-001 authority
  ladder, and the Project Control Plane / `GDD_10` / Feature Index tracking trio.
- Wrote the next-session plan
  `AGENT/Docs/plans/scope_reframe_and_gdd_stale_audit_plan_2026-06-29.md`:
  ratify the scope reframe first (decision record + `GDD_00`), then audit
  `GDD_00`-`08`, `GDD_10`, Feature Index, and Adoption Matrix for stale
  assumptions and unmarked historical decisions.
- Collected and recorded owner decisions on the reframe (see below). Folded each
  into the plan's scope statement (1) and resolved-decisions section (6).

## Owner decisions (ratified inputs for next session's decision record)
- **Scope wording — approved as drafted.** Learning/portfolio primary; flexible
  tactical-RPG builder (user-made, shareable campaigns w/ custom assets+rules)
  secondary; bounded power-user access.
- **Power-user/security — data-only first, sandboxed-scripting ceiling.** Public
  source release (likely MIT + Commons Clause) is the unlimited-access path
  (fork it); internal support never exceeds sandboxed scripting; ship data-only
  authoring first to reach a complete-looking project sooner.
- **Showable target — playable web demo, slice-first.** Lead with a polished
  playable slice, then show it was authored with the builder; add as a tracked
  Control Plane row next session.
- **Roadmap impact — framing only.** The reframe does NOT re-sequence the Band
  1->8 order or reopen prior v1-scope decisions. The audit removes/replaces stale
  text only; it must not propose reordering tracker rows.

## Validation
- `python3 AGENT/Docs/gen_docs_index.py` - run after adding the plan.
- `python3 AGENT/Docs/check_docs.py` - PASS.
- Commit hooks ran `check_docs.py` + `check_rng_usage`; docs-only, Godot suite
  skipped.

## Commits this thread
- `e5cdc7a` - `Plan next-session scope reframe + GDD stale-assumption audit`
- `65f3e8d` - `Record owner decisions on scope reframe (wording, web demo, security line)`
- (this note adds the roadmap-impact + slice-first demo decisions and the note itself)

## Next session (handoff) - scope reframe + GDD stale-assumption audit

This is the execution guide. Detailed rationale + per-file checklist live in
`AGENT/Docs/plans/scope_reframe_and_gdd_stale_audit_plan_2026-06-29.md`; read it
once, then work from the steps here. **All scope questions are resolved - no
owner input is needed before starting.**

### Fixed inputs (do not reopen)
- Scope: learning/portfolio primary; tactical-RPG builder secondary; bounded
  power-user access. (approved as drafted)
- Security: data-only authoring first; sandboxed-scripting is the ceiling;
  full access = fork the MIT+Commons-Clause source.
- Portfolio target: slice-first playable web demo (polished slice, then show it
  was authored with the builder).
- **Framing only:** do NOT re-sequence bands or reopen v1-scope decisions.
- **Authority scope:** amend `GDD_00` scope/overview prose only. Do NOT touch
  the DOC-001 authority ladder - the reframe changes goals, not the authority
  model. The two are independent.

### Step order
1. Write the dated decision record in `AGENT/Docs/decisions/` capturing the
   four fixed inputs above; add its ID(s) to `decision_index.md`.
2. Update `GDD_00_Overview.md` scope/overview to the new framing (status label,
   no banned words `current`/`complete`/`canonical`).
3. Add a Control Plane row for the slice-first web demo target.
4. Audit in order: `GDD_01`->`08`, then `GDD_10`, `GDD_Feature_Index`,
   `GDD_Adoption_Matrix`.

### What counts as "stale" (the rubric to apply per chapter)
- Superseded directions written as live (e.g. base+overlay campaign content,
  commercial-release / single-authored-game framing).
- Historical decisions not marked `Historical`/`Superseded` with a pointer.
- Status drift vs. the matching Control Plane row.
- Scope assumptions that contradict the learning/portfolio + builder framing.
- Closed enum+match where the open-registry principle (`[EXT]`) now applies, or
  access granted/denied without naming the security boundary.
- Retired vocabulary per `project_vocabulary_manifest_2026-06-29.md`.

### Edit-vs-park rule
Fix unambiguous stale items inline. Park anything needing an owner call as a
logged open question - do not make a silent judgment edit, and do not reorder
bands.

### Definition of done
- Decision record written + indexed.
- Any GDD behavior change paired with its `GDD_10`/Control-Plane row in the same
  commit (DoD#1); any new mechanical rule backed by a `check_docs.py` check
  (DoD#2).
- `python3 AGENT/Docs/gen_docs_index.py` run; `INDEX.md`/`REGISTERS.md`
  committed in the same change. `check_docs.py` green.
- Session note written + INDEX row added.

### After the doc pass
Implementation track is unchanged: Band 1 `B1-PKGA` -> `B1-F1` ->
`B1-SAVECODEC`/`B1-CST`.
