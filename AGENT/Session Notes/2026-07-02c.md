# Session Note - 2026-07-02c

## Topic
Applied the remaining Band 1-6 plan audit fixes (all highs, mediums, and lows
except the Band 5 handoff) and wrapped the audit session.

## What was done
- **Band 1 plan:** 1-1 Slice 0 no longer presents `get_action_start_tile` as an
  existing API (Slice 1b adds it); 1-2 the raw-RNG guard now names the existing
  `# rng-allow: <reason>` tag convention and requires the stale `pre-M9a` tags
  to be removed with the migration; 1-3 the T-numbering is mapped to the RNG
  design §10 matrix (T1/T3/T7 Slice 1b, T4/T5 Slice 1d, T2 Slice 2, T6
  `B1-SUSPEND`); 1-4 placeholder-manifest update obligation; 1-5
  mixer-constant fallback (hex literals, same math).
- **Band 2 plan:** 2-1 death lifecycle reads
  `gs.campaign_rules.permadeath_enabled` (the loose field is deleted by Band 1
  `[CST-4]` before Band 2 starts); 2-2 occupancy policies split into
  implemented-and-tested (`require_empty`/`nearest_free`/`delay`/`skip`) vs
  registered-stub (`swap`/`overlap_hidden`/`object_unit`) tiers; 2-3
  `project_combat()` defined as a typed wrapper over `project(ctx)`; 2-4
  `DeathContext` reserves an optional `object_ref` for map-object breaks.
- **Band 3 plan:** 3-1 `visited_trail` linked to the F1 source inventory; 3-2
  boolean output notation fixed ("`1.0` or `0.0`", fixed-point `1000`/`0`);
  3-3 `B3-TEXT` registers the *family*, not one entry per key; 3-4 calendar
  advances on battle-node **victory** only.
- **Band 4 plans:** 4I-1 `InventoryEntry.instance_id` added (Slice 2 step, F1
  rows, snapshot stability, Slice 5 source keys); 4I-2 ItemDef/legacy
  id-parity invariant with a validation check; 4I-3 default PXP rank = the
  base rank that gates nothing; 4M-1 the `shown_disabled -> hidden` degradation
  is documented on SAC/`GDD_07`/control plane; 4C-1 `ConvoyService` owns
  `give_item_to_unit_or_convoy()`; C3 verification checklists appended to all
  four Band 4 plans.
- Marked the applied fixes in `band_plans_audit_2026-07-02.md`; only **C1
  (Band 5 handoff)** remains open from the audit.

## Validation
- `python3 AGENT/Docs/check_docs.py`: 21/21 green; `git diff --check` clean;
  INDEX/REGISTERS regenerated.

## Commits
- `97cdd87` — Resolve SHP-6 sell-price model; wire it into the shop plan.
- `9706f39` — Add thin B4-CAMPAIGN-LOOP plan; record D5 grouping of remaining
  Band 4 tracks.
- `e43dcce` — Record audit resolutions and session note for SHP-6/C2 decisions.
- `a319da8` — Apply band plan audit fixes.
- (this commit) — Session wrap: audit applied-fixes note + this session note.

## Plans / Next Session
- **Draft the Band 5 handoff (audit C1)** in the Band 3/4 handoff pattern:
  rows to cover (`B5-CONDITIONS` .. `B5-UTILITY-STAVES`), bootstrap order,
  decisions not to reopen, owner questions — carrying the `B5-SKILLS-EFFECTS`
  demo-campaign gate (required v1 effect ids wait on the demo-campaign content
  pick) as an explicit owner question.
- Band 6 planning follows its own control-plane triggers; no handoff owed yet.

## Read First Next Session
1. `AGENT/Code Reviews/band_plans_audit_2026-07-02.md` (C1 is the open item).
2. `AGENT/Docs/plans/project_control_plane_2026-06-29.md` Band 5 rows.
