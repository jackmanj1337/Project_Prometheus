# Session Note - 2026-07-13f

## What was done

Implemented the `B2-PROJECTION` combat forecast slice.

- Added typed `ProjectionContext` and `ProjectionResult` contracts.
- Added the `ProjectionService` autoload with generic dispatch, structured
  failures, combat convenience construction, audience-visible/test outcomes,
  odds metadata, and guarded RNG/resource state.
- Routed `AttackPreview` through the service while retaining
  `CombatResolver.preview_combat()` as the authoritative combat calculator.
- Added projection fixtures for forecast parity, unit-state preservation,
  resource/RNG preservation, zero committed draws, and invalid targets.
- Updated architecture, UI, roadmap, and control-plane ownership/status.

## Validation

- `test_projection_service.gd`: 5 passed, 0 failed.
- Full `run_tests.sh`: 75 suites passed.
- `AGENT/Docs/check_docs.py`: 29/29 passed.
- `scripts/ci/check_rng_usage.sh` and `git diff --check`: passed.

## Next

- The next Band 2 plan-order work is the remaining `B2-DATAMANAGER-SEAMS`
  integration review; its behavior-neutral groundwork already landed, so first
  compare the plan against that implementation before adding anything.
- Effect, condition, AI, predicate, and perception projection adapters remain
  with their owning feature tracks.
- A returned v0.3.2 live playtest still preempts new v0.4 work at a clean commit.
