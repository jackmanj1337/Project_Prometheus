# Session Note - 2026-07-15r

## What was done

- Completed installed-campaign selection in the existing New Game Run control.
- New Game refreshes validated `CampaignPackRegistry` summaries, appends each
  campaign with visible pack id/version, and retains the shipped Proving Grounds
  and developer single-map choices.
- Starting an installed campaign activates its exact Tier-2 source before
  `CampaignManager.start_campaign`; the selected map and roster resolve through
  the package runtime adapter. Choosing a shipped run restores `res://data`.
- Added focused UI/source/launch tests for discovery rows, exact activation,
  package map/roster resolution, and switching back to shipped content.

## Next

Return to unified persistence/undo: implement Phase 3 live ledger pushes and
player rewind spending, then Phase 4's single slot namespace and Phase 5 save
policy/autosave registry. The archive APIs are complete; a file-picker-facing
import/export surface remains a separate UI unit if required for v1 delivery.

