# Session Note 2026-07-07c - v0.2.8 playtest return triage

## What was done

Triaged the returned **v0.2.8 display-gate playtest** on `v0.3.0-features`.

- Moved the returned data out of the temporary `AGENT/v0.2.8 playtest/` folder:
  - filled checklist -> `AGENT/Docs/playtests/playtest_checklist_v0.2.8_returned_2026-07-07.md`;
  - logs/screenshots -> `AGENT/Docs/archive/evidence/`;
  - duplicate build manifest dropped because it matched `playtest_build_v0.2.8.md`.
- Wrote `playtest_v0.2.8_results_triage_plan_2026-07-07.md`.
- Wrote `playtest_v0.2.8_triage_review_2026-07-07.md` with owner questions Q1-Q3.
- Updated `VAL-V023-DISPLAY` in `GDD_10` and the Project Control Plane:
  - section 1.3 action-menu anchoring PASS;
  - section 1.4 combat forecast first-open/both-walls PASS;
  - section 1.6 still FAILS on custom-size readout semantics and Windows maximize recentering.
- Added `UI-VIEWPORT-ASPECT` as the owner for the Steam Deck/mobile black-bars policy revisit.
- Updated `display_and_settings_guide.md` and `GDD_01` to clarify the v0.2.8 known issue:
  `Custom (WxH)` is an observed client size, while `-> applied` is a preset-request clamp label.

## Diagnosis

- `V028-02`: the Settings UI conflates preset resolution requests with custom observed
  client sizes. Source: `SettingsScreen._refresh_applied_size()` always asks
  `SettingsManager.applied_windowed_size()`, and that helper clamps the saved string
  even when the saved string came from OS resize write-back.
- `V028-03`: Windows maximize is treated as a write-back source and only gets one
  deferred menu-scale reapply. The returned screenshot indicates the panel can remain
  off-center until a later reapply.
- `V028-04`: aspect-ratio expansion is a real platform/UI policy question, not part
  of the section 1.6 bug fix.

## Commits

- `45ee8da` - `Archive v0.2.8 playtest return`
- `b8ec8e9` - `Triage v0.2.8 playtest results`

## Verification

- `python3 AGENT/Docs/check_docs.py` green before both commits.
- `git diff --check` green during the triage pass.
- Commit hooks reran `check_docs.py` and `scripts/ci/check_rng_usage.sh` green.
- Godot suite skipped by the hook because both commits were docs/evidence only.

## Next

1. Walk owner review Q1-Q3:
   - Q1 custom readout semantics;
   - Q2 maximize button policy;
   - Q3 route aspect-ratio expansion to `UI-VIEWPORT-ASPECT`.
2. Implement the section 1.6-only fix pass with SettingsManager/SettingsScreen tests
   and a live Windows maximize check in the next handbook.
3. Cut a focused section 1.6 rerun build; flip `VAL-V023-DISPLAY` only after it passes.
4. Then resume `B6-INPUT` input-mode/focus seam or proceed to `REL-V023-MERGE` /
   `B6-WEB-DEBUG` as the release gate dictates.
