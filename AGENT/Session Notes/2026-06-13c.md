# Session Notes - 2026-06-13c

## Focus

Work through the documentation consolidation decision register one item at a
time, closing the seven items the 2026-06-13 pre-implementation review flagged as
`Needs clarification`.

## What was done

Read the prior session note (2026-06-13b), the handoff, the pre-implementation
review, the decision register, and the awakening compatibility refactor plan
(for its `AWR-` milestone scheme) and `GDD_10_Roadmap.md` (milestone IDs).

Resolved all seven flagged decisions and recorded each in
`documentation_consolidation_decisions_2026-06-12.md`:

- **DOC-003** — Ratified the nine status labels with one example each, prohibited
  "current/complete/canonical" in status sections, and confirmed **split status**
  (separate `Implemented` + `Target design` lines per feature). Captured in new
  `AGENT/Docs/documentation_governance_2026-06-13.md`.
- **DOC-007** — Explicit accept: move `GDD_Manual_Tasks.md` ->
  `AGENT/Docs/manual_test_playbook.md` (move executed later, in the
  lifecycle/link-migration package).
- **RULE-003** — Defined the proportional WEXP migration formula + clamp so
  rounding never crosses the next rank threshold.
- **RULE-005** — Sub-decision: promotion modal opens only after the triggering
  action fully commits; all controllers blocked until the owner picks; no cancel.
- **RULE-008** — Test period: **replace** authored personal growths; effective
  growth = corpus archetype + corpus class.
- **RULE-011** — Terrain ID-mapping pass assigned roadmap ID **AWR-8**; GDD
  terrain mapping section stays `Open decision` until then.
- **RULE-012** — 1.0 Pair Up scope: keep shipped layer 1 (stat bonuses/actions),
  value migration Planned; Dual Strike/Guard later; layers 4-8 post-1.0.

## Tests and verification

- Commit hook: PASS on both commits, all 38 suites green.
- Documentation-only changes; no game code touched.

## Commits made

- `Resolve DOC-003: ratify status vocabulary with split status`
- `Resolve DOC-007, RULE-003/005/008/011/012 clarifications`

## State preserved

Per the prior handoff, left untouched: the untracked
`AGENT/GDD/gdd_update_reference_2026-06-12.md`,
`AGENT/GDD/rng_determinism_design_2026-06-11.md`, and the untracked combat-preview
screenshots/`.import` files. No file moves or deletions performed.

## Plans for next session

The decision register is now fully answered. Remaining readiness-gate work before
any GDD rewrite or deletion (from the pre-implementation review):

1. Import D-A..D-E, RNG-1..RNG-4, OPEN-1..OPEN-13 from the June update reference
   into the decision system and classify each; make DOC-001 explicitly supersede
   D-C and the older corpus-canon statements.
2. Add the GDD section template and the decision-record schema/unique-ID namespace
   to `documentation_governance_2026-06-13.md` (DOC-002, DOC-009).
3. Build the file lifecycle / link-migration table (DOC-004/006/007/008/010).
4. Update the RNG contract for the two-RN model (RULE-001) before `RngService`.
5. Reorder adoption-matrix rows to precede each corpus-derived GDD rewrite.
