# Session Note — 2026-06-22g

**Topic:** Planning-only. **Walked AND resolved the Map-Readability cluster `[MRD-1..6]`** (GDD §4).
Cluster is now build-ready. Docs-only; check_docs **17/17**; no `__pycache__`.

The cluster delivers four readability affordances: **hover-to-peek range · movement path arrows ·
individual + aggregate threat range · grid-dim slider.** The threat-range piece is already designed
(`individual_threat_range_design_2026-06-21.md`, `[TUR-1..4]`) and folds in.

---

## 1. Code re-verification (recs were code-grounded; confirmed still true)
- `EventBus.cursor_moved(tile)` exists (`MapCursor` emits per tile change) — the hook hover-peek +
  path-arrows ride.
- `GridManager` overlay infra intact: `OVERLAY_DARK_RED` (src 3), `get_enemy_danger_tiles`,
  `show_enemy_danger_zone`, `get_movement_path` all present.
- `SettingsManager` has the `movement_speed`-style load/save plumbing a new `grid_dim` float follows.

## 2. Five resolutions (`[MRD-3]` was already withdrawn pre-session)
- **[MRD-1] overlay stacking = C.** Range (move/attack/heal) ∩ threat layers **blend** (the
  "reachable AND dangerous" tint — the key FE info); hover-peek + path-arrows are exclusive
  **opaque** top layers that replace, not blend. Preserves the threat-range design's faction-src-3 /
  watch-src-4 internal order. Blend palette = build-time GDD_06 add (no string vocab → no guard).
- **[MRD-2] hover-peek trigger = B.** Hold-a-button to peek (press shows hovered unit's move+attack
  range, release hides). Gamepad-friendly; computes once on press. Auto-peek reserved as a later setting.
- **[MRD-3] aggregate-vs-individual threat = WITHDRAWN** (audit 2026-06-21d) — aggregate danger zone
  already built + its relationship to the watch set already designed; only the new hover/arrow
  compose-question remained, folded into [MRD-1]. Kept numbered to avoid renumbering.
- **[MRD-4] live-recompute perf = B** (ratified as the consequence of [MRD-2] B). Hover-peek computes
  range once on press and caches per hovered unit; path-arrows recompute only `get_movement_path` to
  the current cursor tile (cheap). No per-cursor-tick range flood.
- **[MRD-5] grid-visibility slider = A.** One `SettingsManager` float `grid_dim` (0.0–0.5, default
  0.0) dimming the **terrain layer only**; units + overlays stay full opacity. No `check_docs` guard
  (float, not string-keyed). Two-slider option B reserved.
- **[MRD-6] build order = A.** Threat-range slices 1–3 (slice 4 save defers to §2) → layer precedence
  ([MRD-1]) → hover-peek ([MRD-2]/[MRD-4]) → path-arrows → grid-dim slider. Banks the hardest, most-
  specified piece first and establishes the precedence the others depend on; unblocks gamepad R3.

All four owner-facing calls took the recs; [MRD-4] ratified as the technical follow-on of [MRD-2].

## 3. Edits
- `map_readability_open_questions_2026-06-21.md` — header → **RESOLVED 2026-06-22g** + decision
  summary; all five open §3 resolutions flipped with text; §4 slice-sketch heading marked resolved.
- `GDD_10_Roadmap.md` — §H Tier 1 MRD line → RESOLVED 2026-06-22g (decision digest); feature-table
  row status → "register RESOLVED".
- `planning_backlog_2026-06-20.md` §5 — STATUS UPDATE 2026-06-22g: MRD added to RESOLVED list;
  still-open list shrinks to IMP/DMR/BWN (+ release gates REN/LEG).
- **DoD#1:** planning only (no behavior change) — GDD_06 (UI/UX) updates at build time.
- **DoD#2:** no checkable rule ratified — all decided knobs are floats / palette colors / build-order
  (not string-keyed vocab), so no `check_docs.py` guard is owed.

## 4. Verification
- `check_docs.py` → **17/17 PASS**, docs-only, no `__pycache__` artifacts.

## 5. Next session
- **Remaining planning registers:** IMP `[IMP-1..6]` (sprite-importer tooling), DMR `[DMR-1..3]`
  (DataManager refactor — better at build time), BWN `[BWN-1..5]` (broken-weapon mode). Release
  gates REN/LEG are owner-input-blocked.
- **Build track unchanged:** **Package A Step 1 first** (critical path: Package A → §2 → DCH/AIP/FOW
  save slices). MRD threat-range slice 1 is build-ready and independent.
