# Session Note - 2026-06-29f

## Topic
Converted the GDD extensibility uncertainty findings for movement/vulnerability
groups and stats into implementation-ready planning rows.

## What was done
- Read the previous session note (`2026-06-29e`) and the uncertainty report.
- Added `B3-MOVEMENT-VULN-REGISTRY` to the Project Control Plane.
- Added the movement/vulnerability implementation plan:
  `AGENT/Docs/plans/movement_vulnerability_registry_implementation_plan_2026-06-29.md`.
- Updated the stat registry row to point at the new implementation plan:
  `AGENT/Docs/plans/stat_registry_implementation_plan_2026-06-29.md`.
- Added both items to the shared foundation todo list.
- Updated registry manifest/checklist/vocabulary/feature-index references so the
  movement and vulnerability registry work is discoverable.
- Captured the stat-reader policy: runtime stat reads return `0` for missing ids,
  while authored references to unregistered stats still fail validation.

## Validation
- `python3 AGENT/Docs/gen_docs_index.py`
- `python3 AGENT/Docs/check_docs.py`
- `git diff --check`
- Commit hook also ran `check_docs.py` and `check_rng_usage`; docs-only change
  skipped the Godot suite.

## Commits this thread
- `e580a88` - `Plan movement and stat registry migrations`

## Next session
- If implementation starts, build `B2-REGISTRY` first or provide the minimum
  registry loader seam needed by `B3-MOVEMENT-VULN-REGISTRY`.
- For stats, reserve the F1 schema rows for `UnitData.extra_stats` before adding
  new runtime state.
- Decide EXP ownership separately: keep it as class EXP profiles, merge into PXP,
  or share a lower-level XP track primitive.
