# Session Note - 2026-07-15p

## What was done

- Implemented inert installed-pack discovery with `CampaignPackRegistry`.
- Discovery scans deterministic `installed/{id}/{version}` candidates, validates
  manifest/path identity and the complete Tier-2 catalogue, and caches deep-copy
  summaries with pack provenance and campaign ids/labels.
- Broken and path-mismatched packs are excluded with actionable diagnostics;
  refresh rebuilds the cache so removed packs do not leave stale rows.
- Confirmed a pending integration mismatch: installed packs use canonical Tier-2
  JSON while the existing `DataManager.select_campaign_source()` expects the
  shipped `.tres` directory layout. Selection must gain a Tier-2 runtime adapter
  before New Game can safely activate an installed pack.

## Next

Implement the Tier-2 JSON runtime adapter for the validated campaign/map/roster/
class fixture contract. Then wire explicit New Game pack/campaign selection and
persist `{package_id, package_version}` in campaign saves before activation on
load.

