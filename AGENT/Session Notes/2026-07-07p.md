# Session Note 2026-07-07p - v0.3.0 release-delta review

## Goal

Run the document-only v0.3.0 release-delta review described in
`AGENT/Code Reviews/code_review_v0.3.0_release_delta_prep_2026-07-07.md`.

Target snapshot under review:

- Base: `ab81a21` - v0.2.8 executable source commit.
- Head: `b7bcfd2` - pre-build v0.3.0 snapshot before this review's docs.

## Progress

- Read the most recent session note (`2026-07-07o`) and release-delta prep doc.
- Read the active review procedures for code, documentation, scenes/data/assets,
  tests/CI/build, and process/history.
- Confirmed the working tree was clean before review work began.
- Pinned the review target to `b7bcfd2`.
- Ran `python3 AGENT/Docs/check_docs.py`: PASS.
- Tried the prep-listed RNG guard path `scripts/tools/check_rng_usage.sh`; it is
  stale in this checkout and needs path correction in the review artifact.
- Located the active RNG guard at `scripts/ci/check_rng_usage.sh`.
- Direct execution of `scripts/ci/check_rng_usage.sh` fails because the file is
  not executable, but CI/pre-commit call it via `bash`; `bash
  scripts/ci/check_rng_usage.sh` passed.
- Ran `./run_tests.sh`: PASS, 66 suites green.
- Ran `python3 scripts/ci/check_scene_integrity.py`: PASS, 17 scene-attached
  scripts validated.
- Ran `python3 tools/godot-analyzer-mcp/tests/test_tools.py`: PASS, 12 tests.
- Wrote
  `AGENT/Code Reviews/code_review_v0.3.0_release_delta_2026-07-07.md`.
- Release-delta score: 6/10. High finding is unchanged from the July 7 code
  review: fresh production maps never call `RngService.start_map()`, so the
  Retry snapshot and first gameplay RNG event can capture/use zero or stale
  RNG state.
- Medium findings carried forward: Settings Input Mode does not refresh
  `InputModeManager`/prompts immediately; the rebind UI omits `more_info`,
  `peek_range`, `zoom_in`, `zoom_out`, and `zoom_reset`; author-facing
  vocabularies still have closed dispatch lists.
- Release metadata/build state is still pre-build: export preset, Main Menu
  label, environment setup, and final v0.3.0 build manifest/hash are not cut.

## Next

- Fix the fresh-map `RngService.start_map()` lifecycle first, with a scene-level
  regression that proves a fresh `GameMap.tscn` seeds RNG before the Retry
  snapshot.
- Then fix Settings Input Mode refresh and the missing rebind action list before
  cutting the v0.3.0 build.
- After fixes, bump v0.3.0 metadata and create the final build manifest/hash.

## Commits

- `1dd5127` - Add v0.3.0 release-delta review.
- Session note and index row recorded in the closeout commit after `1dd5127`.
