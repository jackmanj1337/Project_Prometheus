# Session Note - 2026-07-15s

## What was done

- Completed `B1-LEDGER` Phase 3 live rewind.
- `TurnManager` now records coalesced post-activation checkpoints and refreshed
  round-start checkpoints, then applies the campaign-authored retention policy.
- `rewind_charges_per_map` is the authoritative spend meter; fine retention is
  reconciled to keep every authored charge reachable.
- Added the Map Menu `Rewind (N)` action and a transactional scene-reload restore
  that rolls back the full board, turn/cursor state, party economy, PairUp, and RNG
  timeline before truncating the abandoned future.
- Suspend capture/restore now preserves the party convoy as well as gold.
- Focused tests cover live pushes, charge spend/exhaustion, economy rollback,
  branch truncation, and identical-replay versus changed-decision RNG behavior.

## Next

Implement `B1-LEDGER` Phase 4: collapse `suspend.json` into the numbered/named
slot namespace, persist the whole rewind ledger in mid-map documents, add the
`origin` metadata, and update Continue/Load labels and lifecycle. Then implement
Phase 5 save-policy slot classes and the autosave trigger registry before the
remaining file-picker-facing campaign import/export surface.
