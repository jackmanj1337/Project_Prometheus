# Session Notes - 2026-06-11

## Focus

Start implementing the combat preview render-fix plan from 2026-06-10 by
closing the zero-height forecast-row regression and hardening automated test
coverage so the blank-preview failure cannot pass again.

## What was done

- Read the latest prior session note before making changes.
- Expanded `scripts/tests/test_attack_preview_selector.gd` so it now checks:
  - rendered forecast rows receive non-zero height after a layout frame
  - attacker, defender, and More Info columns remain distinct inside the panel
  - the no-counter case keeps the `No counter` defender row visible while the
    empty rows still collapse
  - the existing More Info selector behavior still works after the extra
    layout assertions
- Ran the focused selector suite against the pre-fix scene and confirmed the
  expected failure:
  - every visible forecast row reported `size.y == 0`
  - the no-counter defender row also reported `size.y == 0`
- Restored `fit_content = true` for the 14 attacker/defender forecast
  `RichTextLabel` nodes in `scenes/ui/AttackPreview.tscn`.
- Kept the June 9 layout constraints that should stay:
  - fixed forecast-column minimum widths
  - bounded, scrollable `InfoDescription`
  - compact content-sized outer panel behavior
- Verified the updated selector test now passes and still guards the regression.
- Reviewed the follow-up screenshot
  `AGENT/Docs/2026-06-10a broken combat preview.png` after the first fix.
- Diagnosed the second regression:
  - the rows were visible again, but the panel height and left-flip position
    were badly inflated
  - relying on `fit_content = true` for `RichTextLabel` forecast rows let
    control-internal sizing drive the whole panel geometry
- Reworked forecast row sizing again so the preview uses stable one-line row
  heights instead of `fit_content`:
  - set the 14 forecast `RichTextLabel` nodes back to `fit_content = false`
  - added `AttackPreview._refresh_forecast_row_heights()`
  - each visible row now gets a minimum height from the label's default font
    metrics plus small vertical padding
  - empty rows still collapse to `0` height
- Verified this keeps the panel compact while preserving the no-counter and
  More Info behavior.

## Tests run

- `godot --headless --path /workspace --script
  res://scripts/tests/test_attack_preview_selector.gd`
  - pre-fix: FAIL as expected on zero-height forecast rows
  - post-fix: PASS (12/12)
- `godot --headless --path /workspace --script
  res://scripts/tests/test_attack_preview_position.gd`
  - PASS (7/7)
- `godot --headless --path /workspace --script
  res://scripts/tests/test_attack_preview_selector.gd`
  - intermediate sizing attempt: FAIL (`preview panel size: (608.0, 1198.0)`)
  - corrected font-metric sizing: PASS (12/12)
- `bash run_tests.sh`
  - PASS: all suites green
- Commit pre-hook full suite
  - PASS: all suites green

## Commits made this session

- `27fd649` - `Fix combat preview forecast row sizing`
- `d47656b` - `Fix combat preview panel geometry`

## Plans for next session

1. Perform the manual visual validation still called for in the fix plan:
   `1280x720`, `960x540`, center target, edge targets, counter/no-counter,
   triangle, effectiveness, crit, and `Vantage`.
2. Capture an after-fix screenshot once the preview is confirmed visually.
3. Only touch `AttackPreview.gd` sizing constants or positioning if manual
   validation shows a remaining real viewport issue.

---

## GDD Alignment Audit

### Focus

Deep-check the live code, scenes, data, tests, GDD, roadmap, assumptions, dated
decision records, and Awakening reference corpus for contradictions.

### What was done

- Read the latest session notes, code-review instructions, design-decision log,
  campaign/online decisions, numbered GDD files, roadmap, assumptions, and
  active content-expansion corpus.
- Inventoried the current repository:
  - 24 class resources
  - 54 skill resources
  - 10 weapon resources
  - 7 item resources
  - 8 registered maps across 7 map directories
  - 36 automated test suites
- Cross-checked documented mechanics against the implementation and tests.
- Created
  `AGENT/Docs/gdd_codebase_alignment_audit_2026-06-11.md` with 51 classified
  differences, impact notes, and a recommended settlement for each.
- Identified two code-level violations of locked decisions as the highest
  priorities:
  - `WHOLE_PHASE` increments `turn_number` after blue instead of at full-cycle wrap
  - Seize still honors the superseded per-map `allowed_unit_ids` gate
- Separated code fixes from documentation resyncs and unresolved design choices.
- Left the four pre-existing untracked combat-preview image/import files unchanged.

### Tests run

- `bash run_tests.sh`
  - PASS: all 36 suites green
- Commit pre-hook full suite
  - PASS: all 36 suites green
- `git diff --no-index --check /dev/null
  AGENT/Docs/gdd_codebase_alignment_audit_2026-06-11.md`
  - PASS after removing two Markdown trailing-space hard breaks

### Commits made

- `f37b498` - `Document GDD and codebase alignment gaps`

### Recommended next work

1. Fix full-round `turn_number` semantics and replace tests that pin the current
   blue-phase increment.
2. Remove `ObjectiveCondition.allowed_unit_ids` and migrate Seize data/tests to
   the locked `can_seize`-only rule.
3. Establish the documentation authority order in `GDD_00`, then resync M14-M16
   roadmap status and checklists.
4. Add an authority banner and project-adoption matrix to the Awakening corpus
   before importing more class/skill content.
