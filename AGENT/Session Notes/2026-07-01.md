# Session Note - 2026-07-01

## Topic
Owner walkthrough of the Band 5+ preimplementation questions review, settling
Q1-Q17 one at a time, plus two design threads (campaign asset taxonomy/format,
PvP multi-prepper prep).

## Scope reframe this session
**v1 = Band 5 + Band 6 completion, plus bug fixes and a UI polish pass.** Bands
7-8 remain optional/parked. The v1 demo campaign is not yet written; it will
showcase everything coded (>=1 player-facing feature per code feature) and is the
gate for several content-floor decisions.

## What was done
- Re-read the prior note (`2026-06-30m.md`) and the source plans/registers:
  `band5_plus_preimplementation_questions_review_2026-06-30.md`,
  `project_control_plane_2026-06-29.md`, the `IMP` register, the
  shared-selector-extraction design, the Band 4 convoy/shop plans, and the
  campaign-save/content-pack designs.
- Recorded all decisions inline in a new **"Walkthrough Decisions (2026-07-01)"**
  section appended to
  `AGENT/Code Reviews/band5_plus_preimplementation_questions_review_2026-06-30.md`.

### Decisions (summary; full text in the review doc)
- **Band 5 Q1-Q7 all settled.** Combined condition+lifecycle substrate
  (poison/sleep/silence/berserk, silence = general capability-gating); Q2 effect
  manifest DEFERRED (demo-gated); single-target full refresh (extensible to AoE);
  one loadout panel shell + category adapters; full Source+Style substrate + 2
  proof consumers; utility staves heal/restore/rescue/condition-inflict (via F16
  contest); single-ply registry-backed AI scorer + `AIProfileDef`.
- **Band 6 Q8-Q13 all settled** (now in v1). Campaign sharing = one plan
  (export/import -> status record); minimal relationship substrate; MRD no-save
  first; `SelectionCursor` Component 1 -> Band 4 under `PanelSelector`
  (arbiter/gamepad stay Band 6); sprite importer HELD pending asset sourcing.
- **Campaign asset taxonomy + format DECIDED** (replaced the held Q12 scope):
  Tier 1 media raw-loaded = PNG / OGG / TTF; Tier 2 structured data = **JSON,
  canonical**; proposed `user://campaigns/<pack>/` layout; `AssetResolver` +
  fallback chain. Open spike: verify Godot 4 runtime raw-load API for OGG + TTF.
- **Q13 per-map overrides FIRMED into v1**: 3-layer resolver (mid-map > per-map >
  campaign), `revert_scope` (revert-at-map-end vs permanent), all suspend-safe;
  extends `apply_rule_flip`; makes campaign rules mutable runtime state (overlaps
  Q8 status record - unify into one mutable-campaign-state store).
- **Band 7-8:** Arena + emplaced weapons likely for demo; forging ELEVATED to a
  near-term `FORGE` design pass (demo includes forging); AI recruitment
  research-only; Band 8 all parked. **PvP prep designed**: prep-session iterating
  side-descriptors; shared draft pool + budget; public deployment zones but
  hidden unit->tile placements until battle; local hotseat + curtain.
- Updated two memories: added `project_v1_demo_campaign`; appended the asset
  taxonomy/format decision to `project_campaign_content_model`.

## Commits this thread
- `7449f23` - `Record Band 5-8 review walkthrough decisions`

## Validation
- `python3 AGENT/Docs/check_docs.py` - all 21 checks green.
- `git diff --check` - clean.
- Docs-only change; no Godot suite run.

## Current State
- Every Band 5-8 preimplementation question (Q1-Q17) now has a recorded decision
  in the review doc's "Walkthrough Decisions (2026-07-01)" section.
- No implementation plans were written. Implementation remains gated behind Bands
  1-3; `B1-PKGA` Slice 1a is still the code start point.

## Next Session Target
Turn the settled decisions into the follow-up work items below. Highest-leverage
first candidates: (a) the asset-format design note + doc updates + index regen;
(b) fold the `SelectionCursor` Component-1 split into the Band 4 convoy/shop plans
and the selector-extraction design; (c) open the `FORGE` design/register pass.

## Follow-up work items surfaced
1. Q2 effect manifest + `on_level_up` resolution - needs the demo campaign first.
2. Asset-format design note; update `campaign_save_expectations...` +
   `content_pack_compatibility_resync_contract`; regen docs index. **Spike:**
   Godot 4 runtime raw-load for OGG audio + TTF fonts from `user://` (exported).
3. Update Band 4 convoy/shop plans + `shared_selector_extraction_design` so
   `PanelSelector` builds on `SelectionCursor` (Component 1 lands in Band 4).
4. Unify Q8 `CampaignStatusRecord` + Q13 permanent campaign-default mutation into
   one mutable-campaign-state store; reserve F1 save rows.
5. Band 3 rule resolver must build the 3-layer stack (not just leave a seam).
6. `FORGE` design/register pass (soon - demo includes forging); verify `B4-IEQ`
   leaves room for per-instance item-upgrade state.
7. Control-plane status bumps: `B7-FORGING` (near-term), `B6-PER-MAP-OVERRIDES`
   (v1), `B7-ARENA` / `B7-STATIONARY-WEAPONS` (likely selected).
8. Confirm PvP minor open point: is drafted roster composition revealed or hidden
   pre-battle (lean: hidden). Map needs per-faction deployment zones.

## Read First Next Session
1. `AGENT/Session Notes/2026-07-01.md` (this note).
2. `AGENT/Code Reviews/band5_plus_preimplementation_questions_review_2026-06-30.md`
   -> "Walkthrough Decisions (2026-07-01)".
3. `AGENT/Docs/plans/project_control_plane_2026-06-29.md`.

## Watchouts
- Do not draft Band 5 content-floor plans (Q2 manifest, exact staff/effect ids)
  before the demo campaign is designed.
- Do not ratify the `IMP-1..6` sprite-importer register or draft
  `B6-SPRITE-IMPORTER` until asset sourcing is decided.
- Keep every growable vocabulary registry-backed (conditions, capability tags,
  effects, filters, shapes, grant modes, AI scorer terms/profiles, loadout
  categories, overlays, asset groups, prep side-descriptors).
