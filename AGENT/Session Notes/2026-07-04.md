# Session Note - 2026-07-04

## Topic
v0.2.5 playtest return: triage, diagnosis, permanent homes, and the owner-review doc
for next session's fix-shape walkthrough.

## What was done
- Moved the raw return artifacts out of the ad-hoc `AGENT/Docs/v0.2.5 playtest/` folder:
  returned checklist -> `Docs/playtests/playtest_checklist_v0.2.5_returned_2026-07-04.md`
  (Type/Status header added), six screenshots -> `Docs/archive/evidence/` with
  descriptive names, duplicate build-manifest copy deleted.
- Researched every tester comment against the live source and wrote
  `Docs/playtests/playtest_v0.2.5_results_triage_plan_2026-07-04.md` (`V025-01..10`).
  Result split: §1.4 AttackPreview + §1.7 archer copy + all of Part II PASS; §1.5
  level-up/promotion and §1.8 terrain click paging FAIL; §1.1/§1.2/§1.3/§1.6 PARTIAL.
  `VAL-V023-DISPLAY` stays Pending validation.
- Key diagnoses (grounded in code, see the triage doc):
  - Slider flicker = live re-scale changes the slider track geometry mid-drag, so the
    mouse-to-value mapping oscillates between adjacent steps.
  - Level-up narrow panel (now REPRODUCED, first show per map) = `MenuScale._recenter`
    sizes from `get_combined_minimum_size()` while the autowrap `LabelStats` has never
    been laid out.
  - Left-click not dismissing level-up = GUI-phase click consumption on the full-rect
    `mouse_filter = STOP` root; keyboard bypasses the GUI mouse phase. A headless
    `push_input` probe confirmed headless picking differs from desktop — which is why
    `test_level_up_screen.gd` (direct `_unhandled_input` calls) passes.
  - Promotion 2.0x top/bottom cutoff = `PromotionScreen.open_for()` rebuilds its
    option buttons after `ModalScreen._ready()`'s one-time scale/viewport-clamp pass;
    no re-apply, no ScrollContainer.
  - Terrain click paging (repeat of v0.2.3) = the three RichTextLabels under
    `TerrainCorner` keep RichTextLabel's default `STOP` mouse_filter and eat clicks;
    the v0.2.4 fix only extended the rect hit-test that the event never reaches.
  - Windowed 4K "desktop visible around the window" = the clamp working as designed;
    needs an explainer guide (also answers the settings.cfg ask), not a code fix.
  - Skill cap check: cap is 5, `M950_Hero_SkillCap` has only 4 skills — never exercises
    the cap; Map 950 units also need extra weapons for weapon-selection testing.
- Wrote `AGENT/Code Reviews/playtest_v0.2.5_triage_review_2026-07-04.md` — Q1-Q14
  (options + drawbacks + recommendation each) for the owner+Opus walkthrough: slider
  fix shape, Settings wrap policy, sheet More-Info layout, forecast-row registry home
  (Band 5 generalized forecast, per the open-registry principle), effectiveness
  presentation, level-up fix shapes, promotion minimal-fix-vs-redesign, windowed-clamp
  Settings feedback, terrain single-page redesign timing, the input-routing
  test-fidelity rule, and v0.2.6 scope.
- `godot.log` was not returned for the third pass; flagged for the v0.2.6 handbook.

## Commits
- `efb722a` - Move v0.2.5 playtest return artifacts to permanent homes.
- `9000dae` - Triage v0.2.5 playtest results: V025-01..10 diagnosis + fix plan +
  owner review doc.
- (this commit) - Session note.

## Validation
- `python3 AGENT/Docs/gen_docs_index.py` + `check_docs.py` - PASS, all 21 checks green
  (ran in pre-commit on both commits).
- One throwaway headless input-routing probe was run and deleted; no Godot suite run —
  changes are documentation/triage only.

## Next Session
- Walk `AGENT/Code Reviews/playtest_v0.2.5_triage_review_2026-07-04.md` Q1-Q14 one at a
  time with the owner; record decisions inline, update the triage plan + control-plane
  routing, then implement the v0.2.6 fix pass (event-injection tests for the
  input-routing fixes).
- After the playtest thread: resume `[CNC-3]` node type vocabulary if still relevant.
