# Session Note - 2026-07-09f

## Summary

Picked up the `2026-07-09e` handoff (`registry_nonschema_slices_handoff_2026-07-09.md`)
and, with `v0.3.0.d` still out for playtest, **drained the three non-schema
open-registry slices** it captured — all on `v0.3.0-features`, owner-scoped up
front ("narrow v1 guard" + "all three slices"). The non-schema open-registry
stream is now empty until the F1 schema-lock unblocks the storage halves.

## What Changed

- **Slice 1 — DoD#2 stat-registry guard (`b1dbfed`).** New check_docs `[27]`:
  bans re-introducing the two hardcoded shapes the `7221664` `StatRegistry`
  unification removed — a growth-stat **list** literal (string set ==
  `GROWTH_STAT_IDS`) or a stat-**label** map (`{"strength":"Str",…}`) outside
  `StatRegistry.gd`/`scripts/tests`. Shape-specific, NOT stat-id count, so it
  spares every legit individual read (SaveCodec field allow-list, CombatResolver
  stat reads, WeaponData scaling codes, MoreInfoContent sentence descriptions,
  PairUpBonusTable's 7-id `scaling_stats` subset). Owner-chose the **narrow v1**:
  direct base-stat field reads stay unguarded until the F1 storage slice makes
  them wrong. Verified green on the tree + failing on both deliberate violation
  shapes. Register `[STM-3]` gotcha note updated. (Docs-only commit path.)
- **Slice 2 — `[STM-5]` unregistered-stat = hard load error (`0e47ba2`).**
  Implemented the "referenced-but-unregistered = hard error; registered-but-unset
  = soft default" split for every authored stat-name reference site that exists
  today: `DataManager._check_stat_dict` now rejects **unknown** keys in class
  `player_growth_rates`/`enemy_growth_rates`/`stat_caps` (was presence-only);
  new `collect_pair_up_validation_errors` validates the Pair Up table's
  `scaling_stats` + `class_bonuses` inner keys (previously unvalidated — a typo
  scaled a phantom stat as 0); `collect_unit_validation_errors` rejects unknown
  keys in a unit's **partial** personal `growth_rates` override. New
  `StatRegistry.is_registered_stat` (growth ∪ display) is the reference
  vocabulary. `test_data_manager` +1 (23/0) covers all three sites +
  no-false-positive on registered stats + the shipped on-disk table clean.
- **Slice 3 — AI `weakest` target_policy via `hunter` (`83fa991`).** Extended
  the AISpec engagement axis (design §9): `EnemyAI._select_target(engagement)`
  dispatches `nearest`/`weakest` across the targeting dispositions (`pursue_unit`
  move + attack, `hold_tile` attack); the resolved `spec` now threads into every
  disposition handler. New `hunter` profile (`always`/`pursue_unit`/`weakest`) +
  `ENG_NEAREST`/`ENG_WEAKEST`/`VALID_ENGAGEMENTS`. `_find_weakest` = lowest
  current HP, tie-break toward nearer unit, deterministic. **Determinism
  preserved:** no RNG in selection, and the `nearest` path is byte-identical
  dispatch to `_find_nearest` (test asserts `sel_near == _find_nearest`), so
  existing all-nearest profiles' RNG chain is unchanged. `test_ai_profile_registry`
  +2 (9/0), `test_enemy_ai` +2 (26/0). Pulled forward from build-slice step 4
  because it needs no `ai_awake` save field.

## Scope Boundaries (what stayed gated)

- The stat **storage** slice (`UnitData.extra_stats`, `CampaignRules` author
  registry) and the AI **`ai_awake`** dispositions (territorial/tethered/flee/
  seek_tile) remain F1-gated (GDD_10 "Gated build items").
- The author-facing **per-placement `target_policy` key** (`[AIP]` §2a) rides the
  `enemy_placements` save-schema at F1 — only the profile-resolved axis is open.
- `[STM-5]` marked **RESOLVED (read/validation policy)**; save-migration /
  cross-pack / UI facets ride the storage slice.

## Verification

- Per-slice pre-commit green (check_docs 27/27, RNG-usage guard, full suite).
  Slice 1 = docs-only path; Slices 2-3 ran the full **69-suite** run + RNG
  determinism suites (`test_rng_combat_determinism` 13/0, `test_turn_manager`
  63/0) green.

## Next

1. **Playtest-gated work stays primary** (unchanged): deliver/triage `v0.3.0.d`
   — `V030-NG-FOCUS` / `V030-DSP-TRACE` + the MRD-7 pick; flip `B1-SUSPEND` /
   `VAL-V030-GAMEPAD` / `VAL-V023-DISPLAY` on live evidence.
2. **Headless stream is drained of non-schema slices.** Remaining headless
   candidates are all F1-gated or reference unbuilt systems (see the handoff's
   "What is NOT buildable now"). Do not re-derive; resume the storage slices +
   `[TCV]`/MET/effect systems once the F1 schema-lock lands.
