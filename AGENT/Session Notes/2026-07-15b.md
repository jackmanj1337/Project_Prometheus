# Session Note - 2026-07-15b

## What was done

Began building the persistence/undo plan from `2026-07-15a`. Landed **B1-LEDGER
Phases 0 and 1**, both on `agent/claude/2026-07-14/prep-deployment`, full suite
green. (No v0.4.0 playtest return had landed, so nothing preempted.)

### Phase 0 — the tracker row (commit `80733a9`, docs-only)
- Added the dedicated `B1-LEDGER` row to the Project Control Plane (Band 1,
  Target design), ending the persistence work's parking under `B1-PKGA`/`B1-CST`.
- The row links the design handoff (Decision source) and the implementation plan
  (Build source) directly, so both docs are now control-plane-owned; dropped their
  now-redundant `doc_role_manifest` ownership entries (their removal conditions —
  a tracker row linking them directly — were met).

### Phase 1 — the shared suspend-complete serializer + within-map history (commit `30abefd`)
- **The one structural win:** extracted `GameState._capture_map_runtime_entry()` —
  the single SUSPEND-complete board codec (all factions' unit dicts + turn state +
  cursor/threat block + Pair Up carry + RNG timeline). `capture_suspend_save`
  composes campaign/party/roster and then MERGES the codec's keys onto the SaveData
  section defaults, so the suspend document is **byte-identical** to the old inline
  block. Suspend and the ledger now serialize the live board through one path.
- Added the within-map history: `_map_history: Array[Dictionary]` +
  `push_history(turn_manager=null, cursor=null)` / `history_size()` /
  `peek_history(index)` (deep-copy read). `take_map_snapshot()` now also clears and
  seeds the **round-0 entry**; `reset_map_state()` clears the map-scoped history.
  The `GameMap.gd:130` round-0 call site is unchanged (no-arg).
- **Deliberately did NOT rewire Retry this phase** (a revision from the plan's
  original wording, recorded in the plan): `restore_map_snapshot` still reads the
  party-only `_map_start_snapshot` + economy fields. The entry carries all factions
  but NOT party gold/items (those live in the `party` layer), so deciding where
  party economy sits in a ledger entry is Phase 2 ledger-shape work; rewiring
  restore now would risk the gold-rollback Retry already gets. Phase 1 is purely
  additive — the entry is written, proven, and measured, not yet the restore source.
- **New test `test_ledger_entry.gd`** (real-board harness, mirrors
  `test_suspend_map_runtime`): asserts the entry round-trips **enemy** HP /
  board-position / faction and turn state through a JSON round-trip, and that
  `take_map_snapshot` seeds exactly one round-0 entry carrying all factions.
- **Phase 1 exit MEASUREMENT (gated Phase 3):** one suspend-complete entry on a
  14-unit board = **~28 KB binary / ~16 KB JSON (~2 KB/unit)**. A 20-deep fine tier
  ≈ 560 KB — **Rewind is NOT memory-bound**, so Phase 3 is clear to build and is not
  deferred on hardware grounds.
- Docs (DoD#1): `GDD_01_Runtime_Contracts.md` §Determinism, Snapshot & Online
  records the shared codec + history foundation (§8.1 was the right home, not
  Data_Contracts); `GDD_10_Roadmap.md` gained the `B1-LEDGER` phase table (Phases
  0-1 Implemented, 2-5 Planned/Target design); control-plane Next action updated.
- Gates green: `check_docs.py` (31 checks), `check_rng_usage.sh`, full `run_tests.sh`
  (79 suites; `test_ledger_entry` 3/3, all suspend/RNG/pair-up/snapshot suites
  unchanged and green).

## Commits

- `80733a9` — B1-LEDGER Phase 0: add the persistence/undo control-plane row.
- `30abefd` — B1-LEDGER Phase 1: the shared suspend-complete board serializer + history.

Both on `agent/claude/2026-07-14/prep-deployment`. **Unpushed** (awaiting the user's
OK to push, per standing preference). No PR opened.

## Next session

**Phase 2 — the two-tier decaying ledger + Retry-on-ledger** is the next unit, and
it is the deliberate SCRAP-old-code step, so give it focused attention:
1. Build the ledger as a preloaded object (no `class_name`, like `DeploymentPlan.gd`):
   fine tier (per-activation, keep last `undo_activations`) + coarse tier (per
   round-start, keep last `undo_rounds`, may be infinite). Prune = ONE data-driven
   function keeping `(last A activations) UNION (last R round-starts)` — not a mode
   `match`.
2. Add `undo_activations` / `undo_rounds` to `CampaignRules` AND to the rules codec —
   note the codec is `GameState._campaign_rules_to_dict` / `_apply_campaign_rules_dict`
   (`CampaignRules.gd` has no `to_dict`/`from_dict` of its own).
3. **Resolve the party-economy-in-entry question** deferred from Phase 1 before
   rewiring Retry: Retry must still roll back gold/items. Either fold party economy
   into each entry, or keep it as a parallel round-0 capture the restore reads.
4. Re-express Retry as `restore_history(0)`; re-point `GameOverScreen.gd:211-212`;
   SCRAP the party-only `_map_start_snapshot`/`_snapshot_*` path and
   `take_map_snapshot`/`restore_map_snapshot` as a distinct mechanism. Migrate
   `test_rng_snapshot` / `test_pair_up_registry` onto the ledger API.
5. Then Phases 4 (unified slot namespace) and 5 (save policy + autosave). Phase 3
   (Rewind) can slot after 2 — the measurement cleared it.

A returned v0.4.0 playtest still preempts and belongs on the v0.4.0 build branch.
