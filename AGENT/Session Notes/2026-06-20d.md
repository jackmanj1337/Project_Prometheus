# Session Notes - 2026-06-20d (debug Web playtest plan)

## What was done

Filled out an implementation-ready plan for a private debug Web playtest build aimed at
an iPhone 14 Pro playtester.

The plan lives at:

- `AGENT/Docs/debug_web_playtest_plan_2026-06-20.md`

Key decisions/recommendations captured:

- Use a custom Web HTML shell with a fixed 16:9 Godot canvas and emulator-style touch
  controls outside the game visuals.
- Add a `Project Prometheus Web Debug` export preset targeting `builds/web-debug/index.html`.
- Use a single-threaded Web export with PWA disabled for the first pass.
- Route HTML touch controls through a Godot-side `WebInputBridge.gd`, not synthetic
  keyboard events.
- Default new Web debug settings to `mouse_cursor = "click"`.
- Host the first playtest as an unlisted/passworded itch.io HTML5 page unless the user
  chooses another host.
- Treat this as a debug Web playtest channel, not full mobile support.

Cross-links added:

- `AGENT/Docs/handoff_2026-06-20_v0.2.3.md`
- `AGENT/GDD/GDD_10_Roadmap.md`

## Commits

- `bec8bc4` - `Plan debug web playtest build`

## Verification

- `python3 AGENT/Docs/check_docs.py` - PASS
- Commit hook - PASS (`check_docs.py`, RNG guard; docs-only skipped Godot suite)

## State / next

Branch: `code-review-followups-2026-06-17`.

Next implementation path:

1. Confirm the remaining product choices in the plan, especially host, orientation, and
   whether to expose the F9 debug drawer.
2. Implement renderer/preset changes first.
3. Add the custom HTML shell and `WebInputBridge`.
4. Export and smoke-test on desktop Web, then iPhone 14 Pro Safari.
