# Session Note - 2026-07-15z

## What was done

- Implemented portable checksummed `CampaignStatusRecord` artifacts as compact
  player state separate from full saves and campaign packages.
- Terminal campaign results now export completion metadata, open carry-forward
  facts, and counters into the status-record store.
- Extended campaign metadata with author/version and declared compatible source
  rows. New Game scans same-campaign/declared-sequel records, defaults to None,
  and offers an explicit manual foreign-record file import.
- Successful import writes facts and source identity/checksum into the shared
  `MutableCampaignState`, then seeds the normal campaign-variable store. Invalid
  JSON, schema, checksum, or automatic compatibility changes no run state.
- Added a mechanical envelope/open-facts documentation guard and focused
  regressions for export, scan, None, compatible/manual import, fact seeding, and
  tamper rejection. All 97 suites pass.

## Commit

- `Add portable campaign status records` - record/store services, completion
  export, New Game selection/import, metadata, documentation, and tests.

## Next

Finish the `[CST-7]` defeat multi-choice action contract: reload recent, choose
any save, Main Menu, and Rewind where available. Then derive and execute the final
requirement/evidence matrix before deciding whether the persistent goal is done.
