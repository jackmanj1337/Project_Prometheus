# Session Note 2026-07-08f — v0.3.0 full-scan Pass 6

## Goal

Continue the resumable v0.3.0 full code-pillar scan
(`AGENT/Code Reviews/v030_full_scan/`). Cadence is **one pass per invocation**;
`_TRACKER.md` is the resume anchor. The first TODO was **Pass 6 — UI screens,
selection & misc data (18 files)**, which carries the registry-debt Medium
(`DataManager` closed vocabularies).

## Progress

- Read `_TRACKER.md` + the Pass 5 note; working tree == head `b7bcfd2` for these 18
  files (HEAD `47068a7`, docs-only past the boundary).
- Read full diffs for all 18 (`ab81a21..b7bcfd2`) + full current context for the
  new/refactored cores: `SelectionCursor.gd` (new), `ModalScreen.gd` focus seam,
  `UnitDetailsScreen.gd`, `AttackPreview.gd`, `HUD.gd` terrain pager,
  `MenuScale._recenter`. Cross-read `RngService` event API, `SaveCodec:219`
  (`has_weapon` consumer), `NewGameScreen._on_start`. Sampled the selector/data/
  attack-preview/unit-details suites.
- Wrote `AGENT/Code Reviews/v030_full_scan/06_ui_misc.md`: **1 Medium (carried,
  re-CONFIRMED) + 3 Low (1 nit). No new correctness bugs.**
  - **M1 (carried Medium):** DataManager closed author-facing vocabularies
    (`_VALID_AI_PROFILES` `[AIP]`, `_VALID_OBJECTIVE_TYPES` `[TCV-4]`, `_VALID_STATS`
    `[STM]`, `_VALID_ROSTER_POLICIES`, `_VALID_ACTIVATION_MODES` at `:13-16, 124`) —
    closed allow-lists needing an engine edit to extend, contra the AGENTS.md
    open-registry principle. **PRE-EXISTING, not introduced by this delta** (the diff
    only touched the enemy-placement XOR validation + added `has_weapon`, both
    correct). Fix belongs to the register resolutions, not a point patch.
  - **L1:** `SelectionCursor.configure()` (`:705-715`) sets `index=-1` on a shrink
    without emitting `changed`; a consumer mirroring `index` could desync. Benign —
    all 3 consumers (AttackPreview, UnitDetailsScreen, HUD) re-render after configure.
  - **L2:** `NewGameScreen._on_start` launches even when `_persist_rules` push_errors
    + returns on null `campaign_rules`, silently dropping the player's rule choices.
    Dead-defensive (GameState always inits a `CampaignRules`); asymmetric with
    `open()`'s silent-skip.
  - **L3 (nit):** re-clicking the already-selected entry is now a no-op
    (`set_index`→no emit when unchanged); previously re-showed. Benign.
- **Positives recorded:** (1) **`Unit.level_up()` RNG event is CORRECT** — matches the
  documented `RngService` contract (dice = `begin_event`+`commit_event`; non-dice =
  commit-only; `begin_event` is pure/no-mutation), so `growth_fixed` committing without
  begin is intended and `history_hash` advances identically across methods — NOT a
  begin/commit-asymmetry bug (the thing I set out to check). (2) No raw RNG in the 18
  except the two `rng-allow` fallback lines in `_level_up_random`, which draw from the
  **passed event RNG instance**, not the global. (3) CampaignRules read-migration
  consistent + null-guarded across all 5 consumers. (4) MenuScale reactive re-center
  has a sound re-entrancy guard + connect-once hook. (5) ModalScreen focus seam is a
  clean virtual-override design. (6) DataManager placement XOR validation correct;
  `has_weapon` has a real consumer. (7) SelectionCursor has real targeted coverage.
- Flipped tracker Pass 6 → DONE + Log line.
- Pre-commit green: `check_docs.py` all 26, RNG guard PASS, Godot skipped (docs-only).

## Next

- **Pass 7 — General/integration & rollup (final pass):** read across passes 1–6,
  hunt cross-cutting/seam issues, and write the consolidated
  `code_review_v0.3.0_full_scan_2026-07-XX.md` with an updated score and a **38/38
  coverage assertion**. Carried items to fold into the rollup: the **High**
  (`RngService.start_map()` never called on the fresh path — fix site localized to
  `GameMap._ready()` `:114-115`, Pass 5 H1), the two input Mediums (Settings input-mode
  refresh, Pass 3/4; rebind UI omits 5 actions, Pass 4), and this registry-debt Medium
  (Pass 6 M1). check_docs [11] "Review tree completeness" + [12] "Rollup score header"
  will gate the rollup doc's shape.

## Commits

- `d9629f4` — v0.3.0 full-scan Pass 6: UI screens, selection & misc data (1 Med
  carried, 3 Low).
- Session note + INDEX row in the closeout commit after `d9629f4`.
