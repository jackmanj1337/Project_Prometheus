# Session Note — 2026-06-25c  (A1 audit: orphaned-item sweep + pin loose ends; chat-log secondary-question scan)

**Topic:** Reviewed every question/answer decided this multi-part session, swept for OPEN items with no
home, pinned the two genuine loose ends, and scanned the chat for secondary threads that never reached
the docs. Branch `docs-reorg-2026-06-23`. (Audit pass over 2026-06-24i … 2026-06-25b.)

---

## Audit result
- **Every fork asked this session was answered and recorded** (~19 forks → CEX-5..8,20..24 + STY-1..17
  + RCR-5 cross-ref).
- **Only one formally-OPEN register item remains:** `[STY-11]` (battalion entity) → already homed to **A2**.

## Loose ends pinned (deferred but previously un-owned)
- **`[STY-17]` dynamic "provoked" relationship transitions** → **A4** (a `[MET]` action that sets a
  faction relationship) **+ AI provoke** (neutral-attacked → hostile). STY-17 owns the matrix+state;
  A4/AI own the transitions.
- **`[CEX-7]`/`[STY-3]` loadout cap (forget/swap, `requires_equip`)** → **A5** (with
  `CampaignRules.max_skills`). Ever-growing list still ships v1.
- **Leans noted as firming at A2:** `[STY-8]` action economy (battalion edge-cases) + `[STY-17]`
  counter-vs-stance.

## Chat-log secondary-question scan — findings fixed
- **`effect_kind` → `effects` drift:** the `[STY-16]` migration (single `effect_kind` → an `effects`
  *set*) hadn't propagated to the atlas (#10 row + A1 bullet) or two STY in-text refs. Reconciled (the
  one *historical* "single-effect_kind framing of STY-13" reference left intact).
- **"Counters carry NO style":** a load-bearing rule that lived only in the `[CEX-22]` revisit — added a
  cross-ref in `[STY-8]` (styles are `player_activated`, so a counter is always source + null style).
- Re-checked the richer free-text answers (binding wielder-requirements, aggressive+beneficial styles,
  cost-override, MRU+range-aware-skill, rectangle shape, no_attack mandatory) — **all fully captured**;
  no dropped sub-clauses. The range-aware fallback **skill** is homed as A1-build content (GDD_05).

## Files
- `[STY]` register: STY-17 provoke→A4/AI + counter-vs-stance→A2; STY-8 counters-are-plain cross-ref;
  `effect_kind`→`effects` tidy.
- `[CEX]` register: CEX-7 cap pinned → A5.
- Atlas: A4 bullet (+relationship MET action/provoke), A5 bullet (+loadout cap), #10 row + A1 bullet
  `effects` terminology.
- Indexes regenerated; `check_docs` green.

## Status
A1 design complete and **audited clean** — no orphaned questions; all deferrals owned (A2/A4/A5/F5/post-
build). Only `[STY-11]` open (→A2).

## Next session
Begin the **A1 build**, or pivot to **A2 design** (battalion entity `[STY-11]` + capture carry + movement
assists + the STY-8/STY-17 leans). Then A4, A5 → **F1 schema-lock** (Phase B).
