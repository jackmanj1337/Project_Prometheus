# Session Note 2026-07-06d - B1-SAVECODEC Slice 5 SaveData envelope

## What was done

Built **`B1-SAVECODEC` Slice 5** on `v0.3.0-features`.

`scripts/save/SaveData.gd` now owns the I/O-free top-level save document seam:

- Writes the locked top-level keys: `format_version`, `save_label`, `integrity`,
  `header`, `campaign`, `party`, `roster`, `map_runtime`, and `suspend`.
- Centralizes absent-field defaults for the top-level sections.
- Preserves unknown nested fields inside known sections so later manifest-backed
  fields do not need ad hoc loader branches.
- Migrates old planning names/defaults where useful:
  - `integrity.whole` / `integrity.protected` load as
    `payload_hash` / `schema_hash`.
  - legacy `party.gold` / `party_gold` loads into
    `party.resources.party_gold`.
  - legacy `party.roster[]` loads into `roster.units[]`.
  - legacy `permadeath_enabled=false` maps to `death_mode="casual"` and
    `true` maps to `death_mode="classic"`.
- Normalizes JSON-parsed RNG values back to integer `map_seed` /
  `history_hash`.
- Adds skeletal SaveData-level reference validation for party convoy entries,
  roster unit inventory entries, and map-runtime unit inventory entries.

Added `scripts/tests/test_save_data.gd` covering:

- `test_save_data_campaign_roundtrip`
- `test_save_data_old_save_defaults`
- `test_reference_validation_unknown_ids`
- `save_data_version_guard`

Docs/tracking updated in the same implementation change:

- `GDD_01` records `SaveData` as the landed I/O-free envelope and lists
  `test_save_data.gd`.
- `GDD_10` next queue now starts with `B1-CST` CampaignRules consolidation.
- Feature Index points at `SaveData.gd` / `test_save_data.gd`.
- Project Control Plane marks `B1-SAVECODEC` Implemented and records Slices 4-5.
- F1 manifest `map_runtime.rng` serializer owner now names `SaveData` for the
  persistent/suspend envelope.

## Commit

- `edc1c5f` - `Add SaveData envelope`

## Verification

- Focused suites green:
  - `test_save_data.gd` 4/0
  - `test_save_codec.gd` 6/0
  - `test_rng_snapshot.gd` 5/0
- `python3 AGENT/Docs/check_docs.py` green.
- `bash scripts/ci/check_rng_usage.sh` green.
- `git diff --check` green.
- Full `bash run_tests.sh`: 58 suites green.
- Commit pre-hook reran docs, RNG guard, and all 58 suites green.

## Next

1. **`B1-CST` CampaignRules consolidation kickoff**: start the hard migration
   toward `gs.campaign_rules.*` and the campaign/save spine.
2. Keep the v0.2.8 tester return as a preempting release-line gate if it
   arrives.
3. After the Band 1 save spine is stable, resume the queued gamepad slice 1,
   ObjectDB leak audit, and `B1-SUSPEND` capstone priority check.
