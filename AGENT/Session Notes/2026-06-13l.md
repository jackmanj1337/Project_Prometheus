# Session Notes — 2026-06-13l

## What was done

Built a **documentation-review process** (the doc analogue of the code-review
instructions), **executed** it against the live doc set, and applied every finding in
the same session. Recorded the one judgment call as a ratified decision (**DOC-013**).

### New: documentation-review process

- `AGENT/Docs/documentation_review_instructions.md` — a reusable prompt styled after
  `code_review_instructions.txt`, but aimed at what `check_docs.py` *cannot* judge:
  doc↔code drift, governance-vocabulary compliance, single-source-of-truth, and
  navigability. It explicitly tells the reviewer to assume CI is green and not re-do it,
  and requires every drift finding to cite both the doc line and the contradicting
  source (`*.gd` / `data/` / decision ID).
- `AGENT/Docs/documentation_review_2026-06-13.md` — the executed review. Rating 8/10.
  Spot-checks against code/data verified accurate: 6 starter classes, AI profile list,
  8 registered maps, `CampaignRules` stub, `s_rank_mastery` still present.

### Findings applied

1. **DOC-013 — split-status phrasing.** ~13 `Status:` lines across GDD_02/03/04/05/08
   used the prohibited word "current" to name the shipped half of a split status.
   Reworded to **project** vs **corpus** (the DOC-001 authority axis, no shipped/
   aspirational ambiguity). Owner decision: project/corpus (not amending the rule).
   Bonus catch: `GDD_08` had `Status: **Not reviewed**` (not an approved label) →
   `Open decision`.
2. **S-rank constant duplication.** `+10/+5/+1` was stated in both GDD_02 and GDD_04;
   GDD_04 is the rule owner, so GDD_02 now points to it instead of restating.
3. **Code-comment drift.** `scripts/resources/UnitData.gd:60` listed `healer` as a
   future AI profile though it ships and is tested; comment corrected.

### check_docs.py hardened (closes the loop on finding 1)

Added **check 7** (prohibited status words: current/complete/canonical on a
status-bearing line) and **check 8** (every status line carries an approved governance
label). Both scoped to the numbered chapters **GDD_00–08** — the GDD_10 roadmap
("COMPLETE", "Stub created") and the indices ("Seed") keep their own tracker vocabulary.
Negative-tested: check 7 fires on "current" in a Status line but not on the domain
phrase "current rank"; check 8 rejects "Seed"/"Not reviewed". All **8** checks green.

### Decision recorded (DOC-009 workflow)

DOC-013 written into the governance doc (addendum under DOC-003), the consolidation
register, and `decision_index.md` (status **Applied**).

## Commits made this session

- `cf153d8` — Docs: add doc-review process + DOC-013 split-status phrasing (project/
  corpus) + check_docs 7-8
- (this commit) — Session notes 2026-06-13l

No behavior change in either commit, so per PL#8 no roadmap status flip was required.

## Tests

Pre-commit hook ran the full suite on `cf153d8`: **38 suites, all green** (incl.
`test_enemy_ai`, `test_combat`).

## Plans for next session

Unchanged from `2026-06-13k` — the doc work is closed; next is the W6 gameplay items
(quick wins W6a/W6f/W6e/W6i, then visual-tuning W6b/c/d/g/h), then the v0.1.4 version
bump. See `handoff_2026-06-13j.md`.

Optional follow-ups surfaced by this session (not blocking):
- Run the doc-review process again after the next GDD rewrite wave; it is now repeatable.
- Consider extending check 8's approved-label set only if a genuinely new status label
  is ratified (don't add tracker words like "Seed"/"COMPLETE" — those are out of scope
  by design).
