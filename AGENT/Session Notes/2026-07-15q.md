# Session Note - 2026-07-15q

## What was done

- Implemented the Tier-2 runtime adapter instead of sending JSON packs through
  the incompatible shipped `.tres` loader.
- Adapted validated campaign, map, class, and roster JSON into the engine's
  existing Resource objects in memory; added stable package-scoped map ids and
  campaign-pack roster launch support without generated files.
- `DataManager` now validates/builds the complete replacement source before an
  atomic registry swap. Invalid activation preserves the prior source.
- Added exact `campaign.package_id` and `campaign.package_version` to normalized
  saves, the F1 manifest, between-map capture, and suspend capture. Load resolves
  only the service-owned installed path and activates content before campaign or
  map references restore; partial identities are rejected.

## Next

Populate New Game from shipped content plus `CampaignPackRegistry` summaries.
Require an explicit package/campaign choice, activate it through the new Tier-2
seam before `CampaignManager.start_campaign`, and preserve the existing developer
single-map choice.

