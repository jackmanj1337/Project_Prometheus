# Session Note 2026-07-08i — v0.3.0 full-scan fix stream

## Goal

Start the fix effort from `2026-07-08h`: land the locked v0.3.0 full-scan fixes
in order, with focused tests and commits after each logical step.

## Progress

Landed the agreed release-facing fixes and selected low-priority cleanups:

- **H1 fixed:** fresh `GameMap._ready()` now seeds `RngService.start_map()` before
  taking the Retry snapshot. Regression coverage verifies a fresh map gets a
  non-zero `map_seed` and a second same-session fresh map resets
  `history_hash` before snapshot.
- **M1 fixed:** `SettingsManager.save()` emits `settings_changed`, and
  `InputModeManager` listens for it so prompt mode updates immediately after an
  input-mode save or controls reset.
- **M2 fixed:** keybind rows and conflict scans now derive from the live
  `InputMap`, excluding `ui_*` and `debug_*`; label data is now override-only.
  This adds the missing More Info, Peek Range, and Zoom rows without a closed
  action list.
- **L3 fixed:** removed the redundant `SaveData._normalize_rules()` merge helper.
- **L7 fixed:** tolerant save loaders now warn when malformed entries are dropped
  or malformed convoy entries fall back to legacy items.
- **L15 fixed:** removed unused overlay blend metadata and
  `overlay_layer_blends()`.
- **L4/L9/L10 fixed:** save coercion helpers now live in `SaveCodec`; duplicated
  local coercers were removed from save/runtime callers. Input-mode vocabulary
  now delegates from `InputModeManager` to `SettingsManager`.

Deferred / kept as decided:

- **M3 deferred** to `[TCV-4]` / `[AIP]` / `[STM]` register resolutions.
- **L17 kept** held-zoom auto-repeat.
- **L20 kept** re-click no-op for already-selected display rows.

## Verification

- Targeted Godot test suites were run for each code area before committing.
- Pre-commit hooks ran after each commit, including docs checks, RNG guard, and
  the full Godot suite where applicable.
- Final separate gates:
  - `python3 tools/godot-analyzer-mcp/tests/test_tools.py` — 12 tests OK.
  - `python3 scripts/ci/check_scene_integrity.py` — PASS, 17 scene-attached
    scripts validated.

## Commits

- `dffb7ea` — Fix fresh map RNG seeding
- `636e4ea` — Refresh input mode on settings save
- `0cb8a6c` — Derive rebind rows from InputMap
- `0eafcbd` — Remove redundant rules merge
- `05c92fd` — Warn on tolerant save drops
- `3891e61` — Remove unused overlay blend metadata
- `08399b4` — Consolidate save coercion helpers

## Next

Build cut remains externally blocked on the display gate and live
gamepad/display validation. Optional follow-up is to continue the remaining
low-priority review cleanup items not selected in `2026-07-08h`.
