# Session Note — 2026-06-22d

**Topic:** Planning-only. **Closed the AI first-build decisions ([AIP-A11]/[AIP-A12]) and wrote the
consolidated first-build design doc.** The AI cluster is now **design-complete for the first build**
(gated only by Package A + §2's `ai_awake` save slice). End-of-session cleanup + push. Docs-only;
check_docs **17/17**; no `__pycache__`.

---

## 1. Final two decisions
- **[AIP-A11] RESOLVED** — MET `set_ai` action targets **a unit id OR a `group_id`**; payload =
  **partial `AISpec` patch** (any subset of preset/axes). "Wake a group" = patch Activation only;
  role-flip = patch `preset`. One action covers wake / charge / retreat-script / objective-flip.
- **[AIP-A12] RESOLVED** — groups **wake together**: any member entering `aggro_radius` or taking
  damage wakes the whole `group_id`; `ai_awake` latch keyed **per-group** (per-unit for ungrouped).
  The FE "room wakes together" staple.

## 2. Deliverable — first-build design doc
**NEW `AGENT/Docs/ai_first_build_design_2026-06-22.md`** — the buildable spec synthesizing
`[AIP-1..10]` + `[AIP-A11/A12]`: §2 composition engine (`resolve_ai_spec` → `AISpec` → pure
`plan_action`, replacing the `match`) + the four don't-paint-into-a-corner invariants; §3 data model
(placement keys, validated sets, `GameConstants`, `ai_awake` per-group save); §4 the 9 starter
presets; §5 per-disposition procedures; §6 activation/`set_ai`/grouping; §7 reinforcements/allied/
boss; §8 determinism; §9 ordered build slice + test plan; §10 deferred seams.

## 3. Cluster status
- **Group A COMPLETE** ([AIP-6..10] + [AIP-A11/A12]) → first AI build fully spec'd.
- **Group B OPEN** (next AI session): [AIP-11] difficulty band vocab · [AIP-12] action-preview
  gating · [AIP-13] disposition visuals · [AIP-14] combat-AI workstream timing · [AIP-15]
  `unit_hp_below` trigger · [AIP-16] AI pair-up/rescue. Pointers: AI turn-pacing → UI backlog;
  ML Option A → vision §6 (pre-1.0).

## 4. Edits
- **NEW** `ai_first_build_design_2026-06-22.md`.
- `ai_profiles_open_questions_2026-06-21.md` — [AIP-A11]/[AIP-A12] RESOLVED; build-spec pointer.
- `ai_system_design_vision_2026-06-22.md` — first-build-spec pointer.
- `GDD_10_Roadmap.md` §H — A11/A12 resolved + design-doc pointer + "AI cluster design-complete
  for first build."
- DoD#1: planning only (no behavior change); `GDD_08` updates at build time. DoD#2: no rule ratified
  (the profile/policy value-set check_docs guard lands with the code).

## 5. Verification
check_docs.py **17/17 PASS**; docs-only; no `__pycache__`. Branch pushed.

## Session status: CLOSED.
**Seven planning registers resolved** (PKGA · DCH · DTR · MET · FOW · AIP-Group-A) + the AI vision
& first-build design docs. **Next session:** finish the AI cluster (Group B `[AIP-11..16]`), then
return to the remaining open planning items (STW, IMP, DMR, BWN, MRD, REN/LEG release gates). Build
order unchanged: **Package A Step 1 first** on the build track.
