# Session Notes — 2026-06-13m

## What was done

Continuation of 2026-06-13l. Ran a **whole-codebase code review** using the freshly
consolidated GDD as the reference spec (testing the docs), then walked the findings'
implementation **decisions one by one** with the owner and implemented the agreed fixes.

### Whole-codebase code review

Reviewed ~96 `.gd` files (`AGENT/Code Reviews/code_review_2026-06-13.md`), rating **8/10**.
Mature, defensively-written, heavily-tested. Findings (all structural, no correctness bugs):

1. **(Medium)** `DataManager._ready()` is a ~525-line monolith (load + validate + roster).
2. **(Medium)** Combat/skill/level-up RNG runs on the global `randi()` across 4 sites —
   blocks the roadmapped determinism/save/online work.
3. **(Low)** `GameState` snapshot field-lists are a hand-maintained `UnitData` mirror.
4. **(Low)** `compute_damage` no-context path can't reach the Giantkiller 4× branch.

**Documentation field-test verdict:** the GDD held up as a review spec. Every concrete
claim checked matched code (single-roll `randi()` hit, ±10/±2 triangle, ×3 crit, AI
profiles); split-status labels correctly predicted shipped vs. target. The code review's
top debt (RNG) is the same gap the docs already track as RngService/RNG-1 — strong
cross-validation.

### Owner decisions + fixes implemented

- **F1 (RNG):** owner chose "guard now, build at M9a". Added `scripts/ci/check_rng_usage.sh`
  (fails on new unmarked engine RNG), tagged the 4 known sites `# rng-allow: pre-M9a
  (RNG-1)`, wired the guard into pre-commit + both CI workflows. RngService itself stays M9a.
- **F2 (DataManager):** owner chose "roadmap it" — added a Code-Health backlog item to
  `GDD_10` Phase 3 Backlog (not refactored this session).
- **F3 + F4 (Low):** owner chose both. On implementing, found the codebase was already
  ahead of both findings, so scoped honestly to real remaining value:
  - F4: Giantkiller 4× was already covered (test H-3). Added H-3b pinning the *no-context*
    `compute_damage` 3× contract instead of a duplicate.
  - F3: `test_snapshot_coverage.gd` already enforces coverage reflectively. Added a
    snapshot-contract comment on `UnitData`; no new test logic needed.
- **Doc gap:** populated the 6 `Automated coverage TBD` cells in `GDD_Feature_Index.md`
  with verified suites; honestly marked status/campaign/online "none yet" (not built).

## Commits made this session (m portion)

- `7c81fa6` — whole-codebase code review 2026-06-13 (8/10) + GDD field-test
- `4b3241d` — code-review fixes: RNG-usage guard + low-severity test/comment hardening
- `e04463b` — docs: DataManager decomposition backlog + feature-index coverage cells
- (this commit) — session notes 2026-06-13m

(Earlier this session, under 2026-06-13l: doc-review process + DOC-013 + check_docs 7-8;
procedure improvements — check_docs in hook/CI, path-filtered suite, notes INDEX, PL#9.)

## Tests / gates

Full Godot suite green on `4b3241d` (38 suites; `test_combat` 44→45 with H-3b). All three
pre-commit gates exercised: check_docs (8 checks), RNG guard, Godot suite. check_docs
green on every commit; the RNG guard and the docs-only path-filter both verified live.

## Plans for next session

Unchanged baseline (W6 gameplay items, then v0.1.4 bump — see `handoff_2026-06-13j.md`).
Newly queued from this session:
- **M9a (when scheduled):** build the real `RngService` (chained, context-seeded) + two-RN
  hit (RULE-001) + T1–T7 fixture, and route the 4 `# rng-allow` sites through it (the guard
  will then flag any that remain bare).
- **Tech debt:** decompose `DataManager._ready()` (GDD_10 Phase 3 Backlog → Code Health).
- Optional: populate the remaining `Manual coverage TBD` cells in `GDD_Feature_Index.md`.
