# Session Note - 2026-06-30i

## Topic
Wrote the combined Band 3 core-authoring-foundations implementation plan per the
2026-06-30h handoff, plus an owner-questions review doc.

## What was done
- Read the most recent notes (`2026-06-30h`), the Band 3 handoff, the Band 1 and
  Band 2 implementation plans (for shape), the control-plane Band 3 block, and
  the resolved registers: `TCV-1..6`, `REQ-1..16`, `CRR-1..8`, `MET-1..9`,
  `PHB-1..7`, `DIF-1..7`, `THL-1..8`.
- Wrote
  `AGENT/Docs/plans/band3_core_authoring_foundations_implementation_plan_2026-06-30.md`:
  one combined plan with ordered slices following the fixed bootstrap order
  (`B2-REGISTRY` -> stat/movement registries -> `TEXT` -> `TCV` -> `REQ` ->
  `CAMPAIGN-RULES` -> `COMBAT-ROLL-RESOLVER` -> `MET`/`PHB` -> `RESOURCE-POOLS`;
  `CALENDAR-LITE` deferred). The two existing registry sub-plans (movement,
  stat) are reused by reference, not restated. Each slice carries files,
  steps, tests, F1 obligations, registry obligations, and DoD#2 obligations.
- Surfaced the three handoff owner questions in
  `AGENT/Code Reviews/band3_implementation_plan_review_2026-06-30.md` with a
  provisional answer + recommendation for each (per-map override scope =
  campaign-default-only; combat-roll-resolver = late slice in this plan;
  resource-pools = substrate-only slice now, consumers deferred). The plan
  encodes the provisional choices and links the review doc.
- Pointed the eight buildable Band 3 control-plane rows
  (`CAMPAIGN-RULES`, `COMBAT-ROLL-RESOLVER`, `TCV`, `REQ`, `MET`, `PHB`,
  `TEXT`, `RESOURCE-POOLS`) at the new plan; left movement/stat on their
  sub-plans and `CALENDAR-LITE` as `needs content use case`.
- Regenerated the docs index.

## Commits this thread
- (this note + plan + review doc + control-plane/index updates committed together)

## Validation
- `python3 AGENT/Docs/gen_docs_index.py` (INDEX/REGISTERS regenerated).
- `python3 AGENT/Docs/check_docs.py` — all 21 checks green.
- `git diff --check` — clean.
- Docs-only change; the Godot suite was not exercised.

## Current State
- Band 1, Band 2, and now Band 3 implementation plans all exist.
- Band 3 is a planning artifact only; implementation is gated behind
  `B1-PKGA`, `B1-F1`, `B2-REGISTRY`, `B2-ACTION-EFFECT`, and
  `B2-RESOURCE-LEDGER`.
- Three owner questions await review before any slice reorder/trim.

## Next Session Target
Review the three owner questions in
`band3_implementation_plan_review_2026-06-30.md`; otherwise implementation
priority remains `B1-PKGA` Slice 1a.

## Read First Next Session
1. `AGENT/Session Notes/2026-06-30i.md` (this note).
2. `AGENT/Docs/plans/band3_core_authoring_foundations_implementation_plan_2026-06-30.md`.
3. `AGENT/Code Reviews/band3_implementation_plan_review_2026-06-30.md`.

## Watchouts
- Do not start Band 3 implementation before the Band 1/2 gates land.
- `B3-REQ` Slice 5 adds the two `CampaignRules` formula-budget fields; the
  author-tunable profile layer is `B3-CAMPAIGN-RULES` Slice 6 — keep them split.
- Keep the `B3-TCV` -> `B3-REQ` bootstrap order; `TEXT` precedes `REQ` display.
