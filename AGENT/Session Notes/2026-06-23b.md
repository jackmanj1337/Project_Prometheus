# Session Note — 2026-06-23b

**Topic:** Q&A / direction-setting. Confirmed the next playtest build status, then established that
**working through the DESIGN side of the campaign + campaign-builder system is the next major task.**
No code/doc edits beyond this note + its INDEX row (planning direction only).

(Continues the 2026-06-23 (a) campaign-content-model direction thread — that note set the *content
model*; this one names the broader design-side push as the next major task and records the blocker map.)

---

## 1. Playtest-build status (asked + answered)
- **Next build = v0.2.3 "Display Scaling & Resolution"** — already **cut** (`Project_Prometheus_v0.2.3_debug.exe`,
  ~101 MB, SHA-256 `b92301f…ae107`, tag `v0.2.3` @ `d9d02c8`), bundled with `playtest_checklist_v0.2.3.md`
  (Part I = v0.2.3 display/crisp scaling = priority; Part II = unplayed v0.2.2 features; Part III = regression).
- **Ready to distribute? Yes.** The remaining `V021-18/19` `[~]` is the *live crispness/resolution
  verify*, which the playtest pass itself performs ("the build is the verify vehicle"). The
  `v0.2.3 → main` fast-forward merge is open (protected branch, PR-only, no `gh`/token in env) but
  is git housekeeping — it does **not** block distribution. **Owner: handle the merge later.**

## 2. Campaign / campaign-builder design — blocker map (the substance)
Key distinction made: **DESIGN blockers ≠ BUILD blockers.**
- **Campaign *play/save* system (§2)** is **already designed / build-ready** —
  `campaign_save_technical_plan_2026-06-21.md` + `[CST-1..12]` all RESOLVED (progression graph, prep/
  deploy, JSON saves + slots + export/import, suspend, Game Over, `CampaignRules` consolidation).
- **Campaign *builder* (GUI editor) + campaign-PACK format (branch I3)** is **NOT designed** — only
  **"direction set" (2026-06-23a)**: content model chosen (shared DEFAULT library + per-campaign
  include/modify/extend overlay), but **no dedicated register exists** and sub-decisions (a–e:
  include model / override granularity / id namespacing / default-content versioning / `res://` vs
  `user://`) have **recs but are NOT ratified**.
- **Main design blocker:** the builder authors against a data model that isn't locked. Locking the
  **I3 content-overlay model** depends on two upstream pieces that aren't settled:
  1. the **§2 save schema** (defines the save↔campaign-id binding the builder edits), and
  2. the **`DataManager` decomposition `[DMR-1..3]`** (today `_ready()` hard-loads global dirs; the
     overlay needs a **base-load + campaign-overlay** path; register still **OPEN**).
  Plus a concrete gap: **item ICONS are net-new** (no `icon` field on `WeaponData`/`ItemData`; items
  are text-only) → per-campaign item art has no field to design against yet.
- **Package A / `RngService` is a BUILD blocker, NOT a design blocker.** Per
  `planning_backlog_2026-06-20.md`: "None of these are blocked from *planning* by the current
  execution blockers." Package A only gates *building* §2, not *designing* anything.

## 3. Decision / direction (owner, 2026-06-23b)
**The next major task is the DESIGN side of the campaign + campaign-builder aspect**, i.e. the
planning track — not blocked by Package A. Unblocking moves, in order:
1. **Walk the I3 content-overlay register** — write a dedicated `[I3-…]` (or named) open-questions
   register and ratify sub-decisions a–e (recs already drafted in `planning_backlog_2026-06-20.md` §2b).
2. **Resolve `[DMR-1..3]`** (DataManager decomposition) so a campaign-overlay load path is designed in,
   not baked global-only.
3. Surface the **item-`icon` field** as a `WeaponData`/`ItemData` schema addition so item art is
   per-campaign for free.
4. Carry these toward the eventual **GUI campaign editor** design (AI-vision §2/§GUI authoring surface).

## 4. Edits
- This note + its INDEX.md row. **No other doc/code changes** (DoD#1: no behavior change → none owed;
  DoD#2: no checkable rule ratified — the include/override/namespace/version guards still land WITH the
  I3 build, as already noted in 2026-06-23a).

## 5. NOT done / still open
- I3 register still does not exist (direction only). `[DMR-1..3]` still OPEN. Item-`icon` field not added.
- v0.2.3 → `main` merge still open (owner will do later); `V021-18/19` `[~]` await live verify.
- IMP `[IMP-1..6]` and LEG (blocked on `[LEG-1]` corpus) still OPEN from prior sessions.

## 6. Verification
- Planning/notes only. No code touched; no `check_docs` rule affected by this note. No `__pycache__`.

## 7. Next session
- **Begin the campaign/builder design-side push** (this note's §3): walk the **I3 content-overlay
  register** (ratify a–e) and **resolve `[DMR-1..3]`** — both pure planning, no execution gate.
- Build track unchanged when execution resumes: **Package A Step 1 first**, then §2 spine.
