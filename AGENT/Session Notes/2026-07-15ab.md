# 2026-07-15ab — Campaign/save completion audit gaps closed

## What changed

- Reconciled `[CST-8]` with the shipped suspend gate: any idle, unsuppressed
  committed-action boundary owned by a locally controlled faction can now
  suspend. AI phases remain unavailable. A restored non-blue local phase
  re-enters `HotseatController`, retargets the cursor, and unlocks play.
- Finished the previously uncommitted audit fixes: campaign-authored dotted
  `protected_fields` now flow from `CampaignData` into the save integrity
  projection; story rule flips show a timed player notification; Prep shows the
  entire effective ruleset read-only and marks mandates as locked.
- Corrected stale GDD/control-plane text, including campaign fields that had
  accidentally appeared in the `ClassData` example and feature-index rows that
  still called the campaign save / Prep manual-save flow future work.
- Added/extended headless coverage for protected hashes, story-flip notice,
  complete Prep rules readout, local/AI suspend eligibility, and hotseat resume.

## Verification

- `bash run_tests.sh` — **98 suites passed**.
- `python3 AGENT/Docs/check_docs.py` — **35 checks passed** after staging the
  two new Godot UID sidecars.
- `bash scripts/ci/check_rng_usage.sh` — passed.
- `git diff --check` — passed.
- Headless editor import recognized the new resources; the container could not
  write its global `/home/vscode/.cache/godot` editor cache, which does not
  affect project imports or tests.

## Commit

- `Close campaign persistence audit gaps` (this logical session).

## Next

- Use the final requirement/evidence matrix to confirm that every defined
  campaign save, rewind, import/export, progression, Prep/results/defeat,
  mutable-rule, and status-carry requirement has direct current-state proof.
- If that audit remains fully green after the committed-tree verification,
  mark the persistent goal complete. Live Windows/UI checks remain release
  validation qualifiers rather than missing implementation.
