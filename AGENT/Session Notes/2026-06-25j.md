# Session Note — 2026-06-25j  (A2 ACTION-GRANT — `[AGT-1..12]` + one forward-pin)

**Topic:** Second A2 sub-cluster — **action-grant (Dancer / Reinvigorate, #8)**: the "give an ally
another turn" mechanic. Firmed it as the **symmetric twin of F10 Secondary Movement** over the *same*
`grant_extra_turn` substrate, plus the owner's **author-tunable anti-loop tools** call and a forward-pin.
Branch `docs-reorg-2026-06-23`.

**Outcome:** New register **`[AGT-1..12]`** (`registers/action_grant_open_questions_2026-06-25.md`,
RESOLVED) + INDEX/REGISTERS regenerated, all 19 `check_docs` green. **A2 action-grant is firmed.**
**Next = the battalion entity `[STY-11]`** (the last A2 sub-cluster).

---

## 1. The substrate finding (the spine of the pass)
Action-grant = the **`is_self:false`, full-turn, activated** branch of the **same**
`grant_extra_turn(unit, {can_move, can_act, is_self})` call that `[SMV]` uses for Secondary Movement.
SMV = passive/self/move-only; Reinvigorate = activated/ally/full-turn. So almost the whole SMV register
transposes — **no bespoke engine subsystem.** Code-grounded: the action economy is just
`TurnManager.UnitState {READY, MOVED, DONE}`; `can_unit_act` already accepts READY|MOVED, and
`_refresh_faction_units` already re-`READY`s a faction each phase — **a refresh is "set a DONE/MOVED ally
back to READY,"** a transition the engine already performs. Reinvigorate/grant_extra_turn are **unbuilt**
(grep clean), confirming greenfield-over-described-substrate.

## 2. `[AGT-1..12]` highlights
- **AGT-1** activated, parameterized `SkillData` (`effect_id="grant_action"`, `is_player_activated`);
  reuses the skill stack wholesale (mirror `[SMV-1]`).
- **AGT-2** scope = `effect_params.grant_mode ∈ {full(default), move_only, act_only}` → the substrate
  flags. **Default `full`** (Dancer canon) — owner call.
- **AGT-3** activated targeted skill: legal target via `[STY-16]` filter + `[STY-17]` relationship gate
  (allies/non-hostile), range a param (default adjacent), **valid target = `state != READY`** (must have
  already acted — settles the note's "re-grant an already-acted unit" edge), `[STY-10]` preview, no RNG.
- **AGT-4** conferral = existing skill-grant paths (mirror `[SMV-4]`); Dancer/Bard class default unlock.
- **AGT-5** Dance **is** the caster's turn-ending action (dancer→DONE) ⇒ net "spend one to refresh one";
  composes with the caster's own SMV (dance-then-reposition).
- **AGT-6** refresh = `grant_extra_turn` re-READYs the target, which re-enters the active controller
  (M14 stage 5); atomic, grants exactly the authored scope.
- **AGT-7 (owner call) — anti-loop & balance = author TOOLS, not a baked rule.** Four general levers, any
  one of which closes the A↔B loop: (1) net-new transient `Unit.actions_taken_this_turn` counter + author
  **refresh cap** (cap 1 = balanced default; a capped unit is an invalid grant target), cleared in
  `_refresh_faction_units`; (2) **generic per-source per-phase/round/map action-rate-limit** — broader
  than action-grant (→ forward-pin); (3) **resource cost** on the skill (the `[STY]` multi-resource cost
  axis; an expensive Dance throttles economically); (4) **source scarcity** — author *one* Dancer and
  total refreshes/turn cap at that unit's budget (free given `[AGT-4]`). No hardcoded "danced-can't-dance."
- **AGT-8** F10 interaction = fully composable, zero special case.
- **AGT-9 (owner call)** off-turn = **on-turn only v1**; reserve a per-source `off_turn_grant` flag
  (default off), inheriting `[DSP-12]` non-interrupt when built.
- **AGT-10** AI queries the same capability; heuristic at AI build (mirror `[SMV-10]`).
- **AGT-11** save: skill grant persists; the transient counters need **no field unless mid-phase save is
  in scope** → flagged for the F1 lock.
- **AGT-12** all tunables = `CampaignRules` defaults overridable per source/skill (mirror `[DSP-17]`).
- **AGT-13** targeting cardinality = **single (default) / multi-target / self**, all the same
  `grant_action` effect over reused A1 axes: multi reuses the **`[STY-9]` AoE footprint** (mass dance;
  caps apply per unit); **self** = an opt-in "second wind" (relaxes `target != caster`), the most
  loop-prone shape → leans hardest on the `[AGT-7]` levers.

## 3. Forward-pins (two)
- **Generic "action-rate-limit" primitive** (register §5). Owner's anti-loop tool #2 is **not
  action-grant-specific**: cap any action from any source per phase/round/map. Also serves battalion
  **gambits** ("once per round", `[STY-7/11]`), limited staves, etc. **Pinned to the action/turn-flow
  foundation** (lives with `TurnManager`'s round counters), consumed by action-grant, battalions, staves;
  reserve any persistent counters at F1 alongside `[AGT-11]`.
- **Non-combat-action EXP / proficiency path** (register §6 → **A5**). Confirm + generalize a
  **support-action EXP rule**. Code-grounded: level-EXP plumbing exists — `Unit.add_exp()` is shared and
  **staff use already calls it** (`STAFF_HEAL_EXP`), so the open part is an *authored EXP amount per
  support action*; **proficiency EXP is the real gap** — `add_wexp` is weapon-track keyed, so non-weapon
  skills (a Dance) have no proficiency path. Owner = **A5** (EXP economy / Bonus-EXP #18); generalize the
  staff-EXP precedent rather than special-case each action. Mirror-pinned in the atlas A5 bullet.

## 4. Commits (this session)
`665c335` `[AGT-1..12]` register + regen · `f5e5003` `[AGT-7]` cost/scarcity levers + `[AGT-13]`
multi/self · `(this)` non-combat-action EXP pin (`§6` + atlas A5).

## 5. NEXT SESSION — A2 battalion entity (`[STY-11]`)  *(fresh context — read this first)*
The **last A2 sub-cluster**: the **battalion entity** (full 3H scope) + #16⇄A1 deployment side.

**The framing question to settle first (owner-posed):** does the battalion get its **own bespoke
system**, or **fold into the existing item + proficiency systems**? Sketch of the two poles:
- **Merge** — a battalion = an **equippable/loadout entity** (rides `[IEQ]`/`[CEX]` equip + convoy) that
  carries a **proficiency-like rank** (rides the `add_wexp`/weapon-rank machinery, generalized to a
  "battalion rank" track). Cheapest if battalion behavior maps cleanly onto equip + rank-progression.
- **Bespoke** — a standalone unit-attachment entity with its own state/registry (cf. how `[DSP]` reused
  the **Pair-Up off-map-attach** substrate rather than inventing one). Right if battalions need
  attachment/morale/gambit state that equip+proficiency can't carry.
  Decide by **grounding in code first** (grep `proficiency`/`add_wexp`/`weapon_rank`, `[IEQ]` equip,
  PairUpRegistry) — same method as `[DSP]`/`[AGT]`: find the substrate, prefer reuse, justify any bespoke.

**Known leans into the battalion pass:**
- Gambit attack/area side already firmed A1 (`[STY-7]`, 2026-06-24j): gambit = an AoE **style** over a
  battalion-granted source. This pass owns the **battalion *entity*** (`[STY-11]`), not the gambit attack.
- It should **consume the `§3`/register-`§5` action-rate-limit pin** ("gambit once per round").
- Watch the **`[AGT §6]` non-combat-action EXP** question if a battalion rank is proficiency-shaped.

Then the `[STY-8]`/`[STY-17]` leans → **A4** (Capture flags + `capture` objective from `[DSP]`;
`[STY-17]` provoke MET) → **A5** (death-inventory `[DSP]`/A5 pin + loadout cap + `[AGT §6]` EXP pin) →
**F1 schema-lock**.
