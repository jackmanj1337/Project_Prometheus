# Session Note — 2026-06-23k  (Open-items triage + doc reconciliation)

**Topic:** Owner asked to (1) survey all open planning/discussion items, propose a decide-order
and a question set to pin down each system, then (2) **reconcile the docs**. Docs-only; check_docs
**19/19**, working tree clean.

---

## Part 1 — Open-items triage (analysis, not committed)

Mapped the open surface into **two tracks**:
- **Track A — content firming** (owner's stated priority, `player_facing_scope_map_2026-06-23.md` §4):
  the §3b v1 worklist, mostly *without* question registers yet. Recommended decide-order keyed on
  dependencies: **prep-as-hub (§3a) keystone FIRST** → economy spine **convoy(D)→shop(E)→gold** →
  equip-items → **avatar feasibility early** (in/out cascades into roster/save/story) → recruit+capture /
  difficulty modes / hub options (arena, skirmish, training halls) → large standalone (Support, Rescue+Canto,
  Dancer, staves, village) → DISCUSS items (combat arts, battalions, movement assists, bonus-EXP) →
  **builder-authority pass** (foundations §4a–4e) → build. Drafted firming-question lists per system.
- **Track B — release gates + tooling** (already have question registers w/ recs): `REN-1..5` (needs the
  names list), `LEG-1..5` (needs the source corpus named), `IMP-1..6` (walk-and-confirm), `CST-13`
  (leave deferred to §2 execution kickoff — do **not** decide now).

## Part 2 — Doc reconciliation (committed)

Survey found the "overlay" grep hits were **mostly not stale** (AI difficulty-band overlays, fog-mask
TileMapLayer overlays, or text *correctly* describing the reversal). **3 genuine discrepancies fixed:**
1. **GDD_10 line ~230** described `[DMR-4]` as a base+overlay merge seam (`_apply_overlay()`, "merge
   deferred to I3") — contradicting its **own** ICO reframe note at line ~167. → self-contained replace-load.
2. **`datamanager_decomposition` register** — resolved (06-23d) as an overlay/merge seam, then ICO reframed
   to self-contained (06-23e); body still described a merge engine. → added an **AMENDED post-resolution
   banner** (body kept for provenance, redirected to replace-load: `select_campaign(c)`→`_load_all(c.content_dir)`,
   no merge engine; heaviest build = ICO-5 seed-copy + `user://` enumeration).
3. **`planning_backlog §2b`** framed convoy/shop/recruit/support/rescue as "deferred, re-surface after §2
   spine" — superseded by scope map §3b firming them into **v1**. → banner: committed v1, each gets an A–J
   firming pass behind prep-as-hub; they stay OUT of the §2 *save-cluster* MVP so §2b remains valid as the
   **save-schema-reservation** tracker. Left the firming-doc/technical-plan "deferred from MVP" wording
   intact — that's *save-cluster* scope and still correct (two senses of "deferred").

## Commits
1. `3a835c4` — DMR-4 load seam = self-contained replace-load, not base+overlay merge (GDD_10 + DMR register)
2. `5408081` — backlog §2b marks convoy/shop/recruit/support/rescue as committed v1

## Verification
`check_docs.py` **19/19 PASS**; manifests unchanged (no header edits); RNG guard PASS; no `__pycache__`;
working tree clean. All edits on branch `docs-reorg-2026-06-23`.

## Next session
- **Start the content thread:** open a **prep-as-hub firming register** (§1 ICD / §2 CST pattern) and walk
  its questions, then roll into convoy→shop→gold. Run the **avatar feasibility dive** early.
- Quick wins whenever owner supplies input: close `REN` (needs the names list) and unblock `LEG-1` (name the
  source corpus + license).
- Question lists from Part 1 are the seed for each new firming register.
