# Session Note — 2026-06-26a  (F16 predicate vocab extended — two-value `compare` `[REQ-9]` + chance gate `[REQ-10]`)

**Topic:** Owner extended the shared Requirement/Predicate vocabulary (F16, firmed 2026-06-25r) with
(1) a **deterministic comparison of two dynamic values** and (2) a **simple chance gate skewed by a
comparison of two values**. Branch `docs-reorg-2026-06-23`. (Continues the 2026-06-25r F16 thread.)

**Outcome:** `[REQ]` extended `REQ-1..8 → REQ-1..10`, still RESOLVED; atlas F16 row + Last-verified
bump; INDEX/REGISTERS regen; **19 checks green**. 1 commit (`3dbc514`).

---

## 1. `[REQ-9]` value terms + two-value `compare`
Generalizes the REQ-2 constant comparisons to compare **two dynamic values** ("is my level > yours",
"is my STR > your DEF", "more sword skill than you").
- **Value term** = `{subject (REQ-3), source}` → a number. Sources: `level` · `stat:<name>`
  (`get_effective_stat`, string-keyed → F14) · `proficiency:<track>` · `item_count:{id,location}` ·
  `skill_count` · `gold` · **literal**. Author-extensible (F4-style registry).
- **`compare`** = `{lhs:term, op:<|<=|==|!=|>=|>, rhs:term}` — both sides dynamic, **different subjects
  allowed** (the point). Deterministic/pure.
- **Generalizes REQ-2:** the constant predicates (`stat`/`class_level`/`proficiency`) are the
  literal-`rhs` special case.

## 2. `[REQ-10]` chance gate (comparison-skewed probabilistic predicate)
A **`chance`** predicate — the **one stateful/impure** predicate (all others are pure reads).
- **Shape:** `{base, skew?:{lhs:term, rhs:term, operand:difference|ratio, profile:<skew>}, latch}`;
  `success% = skew_profile.apply(base, operand(lhs,rhs))` clamped.
- **Owner calls:** skew curve = **author-configurable CampaignRules (F4) profile** (`linear` default /
  `sigmoid` / `table`), per gate · operand = **author's choice `difference|ratio`** · timing =
  **roll-once-and-latch**.
- **RNG discipline (hard req):** routes through **`RngService`/Package A `[PKGA]`** (seeded, canonical
  roll order, **rewind-safe**); never engine RNG (`check_rng_usage`). **Not built until Package A lands.**
- **Latch:** rolls once, latches the outcome into the `[DLG-11]` `visited_trail` and/or an `[F6]` result
  key → save/reload/rewind/re-eval returns the same answer (**no save-scum**, matches PKGA anti-reroll).
  Author `re_rollable` opts out for intentional retries.
- **Display vs commit:** shows **odds** ("65%"), and **evaluation IS the roll** → evaluated **on commit**,
  not passive preview (pure predicates may preview; `chance` may not).
- **General primitive:** also serves persuade/steal/intimidate/status-infliction (a contest/check gate,
  mirrors combat hit math) — surfaced in F16, not dialogue-only.
- **Save:** the latch is new state but **rides** `visited_trail`/`[F6]` (no new top-level field;
  amends REQ-7).

## 2b. `[REQ-11]` item references + item-property terms (owner add, same day)
Value terms (REQ-9) and predicates can read **properties of a referenced item**.
- **Item ref:** `equipped(subject)` (`get_equipped_weapon`/`[CEX-21]` source) · `held(subject,
  item_id|slot)` (an `InventoryEntry`) · **`targeted`** (the action's targeted item — steal/forge/
  break-event/trade; **context-resolved** like speaker/participant; false where no target, never errors).
- **Property vocab (grounded in `[IEQ]`/WeaponData, author-extensible):** numeric (`mt`/`hit`/`crit`/`wt`/
  `uses_remaining`/`cost`/`range`/`required_rank`-ordinal/`strikes_per_attack`/`forged_mods`/equip-mods)
  → `compare`; scalar-string (`combat_family`/`triangle_family`/`item_type`/`effect_id`) → `compare`
  `==`/`!=`; **set** (`effect_tags`) → dedicated **`item_has_tag`/`item_has_effect`**; identity →
  `item_is`. `compare` now does scalar (num OR string) equality + numeric ordering.
- Subject selector reused → cross-subject item compares ("my weapon's Mt > yours" =
  `compare(item_property(equipped(speaker),mt) > item_property(equipped(participant:other),mt))`).

## 2c. `[REQ-12..14]` vocab completeness pass (owner: add missing properties + gap analysis)
- **REQ-12 (more term sources):** HP (`hp_current`/`hp_max`/**`hp_pct`**) · F7 pools
  (`pool:<id>.current/max/pct`) · per-map ability availability (`ability_uses_remaining`/`available`/
  `usable_ability_count` over `skill_use_counters`/`map_uses_remaining`/charges) · STY
  (`style_available`/`source_available`/`style_count`/`source_count`, respects the `[CEX-7]` cap) ·
  identity (`class_id`/`movement_type`/`promoted`/`is_main_character`). The `pct` pattern generalizes.
- **REQ-13 (gap analysis → owner folded in ALL FOUR families that combinations couldn't express):**
  **(a) spatial** — `adjacent`/`distance`(Manhattan)/`on_terrain`/`in_region` (Grid+MapData);
  **(b) runtime-state** — `has_acted`/`has_moved`/`can_act` (TurnManager), `is_deployed`/`is_paired`
  (PairUp), `is_carried`/`is_rescuing` (Carry), `is_captured`/`asleep`; **(c) `[REL]` relationship/
  support rank** between two units; **(d) aggregate** `count(<set>, <Requirement>) op N` — generalizes
  REQ-3 any/all. Build per-consumer; each a thin runtime read.
- **REQ-14 (owner Q — active conditions w/ potency+duration):** `condition_potency` ·
  `condition_duration` · `condition_count` value terms over F5 state. **`condition_potency` pins a
  FORWARD-REQUIREMENT on the F5 status model** (must carry a magnitude/stack dimension; degrades
  gracefully). Recorded on the atlas **F5 row** too.
- **No new save surface** (all read runtime/reserved state).

## 2d. `[REQ-15]` condition params + outcome projection (owner add — "will a poison kill or leave at 1 HP")
Generalizes REQ-14 to **any** condition parameter + a **projected outcome**.
- `condition_param:{subject, condition_id, param}` — read any authored ConditionData param.
- **Outcome projection DELEGATES to F5** (don't re-derive — naive `potency≥hp` breaks under
  floor/mitigation): `condition_next_tick_damage` (term) + `condition_would_kill` / `condition_would_floor`
  (bool). "Will the poison kill or leave at 1" = `would_kill` vs `would_floor`. Same projection the
  damage-preview UI uses.
- **Paired forward-req on F5/ConditionManager:** (i) expose params introspectably · (ii) carry a
  **lethal/floor** param (poison floors at 1 vs a killing condition) · (iii) a **next-resolution
  projection/preview API**. Recorded on the atlas **F5 row** + F16 row.

## 3. Commits
`3dbc514` — `[REQ-9/10]`. `34ce497` — `[REQ-11]` item refs + property vocab. `de481f7` —
`[REQ-12..14]` (unit/pool/availability sources + 4 gap families + condition potency/duration; atlas F16
+ F5 rows). `c09fe4b` — `[REQ-15]` condition params + F5-delegated outcome projection. REQ-1..8 →
**REQ-1..15**; all 19 green.

## 2e. Loose-ends audit (owner asked "anything to record/clarify/defer?")
- **Already captured forward-reqs:** F5 potency/lethal-floor/projection API (`[REQ-14/15]`, F5 row) ·
  REQ-10 `chance` gated on Package A + a new F4 skew profile (F16 row) · DLG-9 rotation investigate ·
  F16 builds per-consumer; sweep owes F14/F15-slice/F16-vocab/BAT-14..16.
- **One thing flagged for a DEDICATED next-session walk (owner call):** what **"author-extensible"**
  actually means across the REQ vocab + DLG effects + F4 profiles, given packs are **data+art, no code**.
  New OPEN register **`[EXT-1..6]`** (`authoring_extensibility_open_questions_2026-06-26.md`) — 3 candidate
  directions (data-composition only / sandboxed expression layer / engine-only), + EXT-2..6 (content-model
  fit, validation, determinism/save-safety, primitive cadence, **one model for all author vocabularies**).
- **Parked future-discussion topics** (in the EXT register): dialogue editor `[DLG-8]` timing · F16 v1
  build-scope (ship a subset) · the chance-gate → unified contest/skill-check feature · `[REQ-15]`
  projection ↔ F5 ↔ damage-preview UI sharing. **Minor build-deferred:** participant-role vocab,
  `targeted` contexts, `count` subject-sets, distance metric (Manhattan).

## 4. NEXT SESSION — **author-extensibility walk `[EXT]` FIRST, then A5** (owner call 2026-06-26)
- **`[EXT-1..6]` dedicated walk** — settle what "author-extensible" means (one model for REQ/DLG/F4),
  reconcile with the no-code content model + Package-A determinism. **Before A5.**
- **Then A5 walk** (last sync-cluster before F1; incl. shop/`activate` `[VIL-2]` configs).
- **Then F1 schema-lock.** Sweep owes: F14 `[STM]` · F15 v1 build-slice (`[DLG-1..14]`) · **F16 `[REQ-1..15]`
  v1 vocab + the `[EXT]` extension model** · `[BAT-14..16]`. **No builds until F1 is planned.**
