# Session Note - 2026-06-29e

## Topic
Executed the scope reframe and GDD/docs audit with emphasis on `[EXT]` open
registries, developer-provided presets, and avoiding hardcoded author-facing numbers or
relationships.

## What was done
- Read the previous session handoff (`2026-06-29d`) and the live docs indexes, then
  followed the planned order.
- Added the dated decision record
  `AGENT/Docs/decisions/decision_record_2026-06-29_scope_reframe.md` and indexed
  SET-011..014:
  - learning/portfolio first;
  - tactical-RPG builder second;
  - data-only authoring first, sandboxed scripting as the in-app ceiling, source fork
    for full access;
  - slice-first playable web demo without resequencing Bands 1-8.
- Updated `GDD_00_Overview.md` to replace the old adaptation-first framing with the new
  scope, portfolio web-demo target, security boundary, and preset-vs-engine distinction.
- Added `REL-WEB-DEMO` to the Project Control Plane and wired it through `GDD_10` and the
  feature index.
- Audited `GDD_01`-`GDD_08` plus `GDD_Adoption_Matrix.md` for stale closed-vocabulary
  assumptions:
  - `GDD_01`: added the authoring extension/security boundary and marked schema string
    ids as implemented built-ins with registry migration targets.
  - `GDD_02`: reframed terrain, topology, stats, follow-up threshold, EXP, and
    conditions as presets/registry targets.
  - `GDD_03`: marked movement/vulnerability tags and starter roster as preset
    vocabulary/content and flagged progression formulas as profile-owned.
  - `GDD_04`: reframed WEXP thresholds, S-rank bonuses, effectiveness tags, forging, and
    stationary weapons as data/profile/component targets.
  - `GDD_05`: replaced "new skill effect = add SkillHandler case" with
    data-composition/primitive-request guidance.
  - `GDD_06`: reframed objectives as predicate/action registry targets, replaced the old
    door/chest placeholder with the unified `map_objects` model, and marked validation
    map rules as content constraints rather than engine limits.
  - `GDD_07`: added PHB panel registry and data-only/public-builder boundary language.
  - `GDD_08`: replaced future AI profile switch language with AISpec/profile registry
    and `[VAL]` scorer preset language.
  - Adoption Matrix: added the rule that adopted corpus numbers/relationships are
    built-in presets unless a section explicitly marks an engine-only invariant.
- Created a code-review-style uncertainty note at
  `AGENT/Code Reviews/gdd_extensibility_uncertainty_review_2026-06-29.md` for items
  that should wait for implementation-slice decisions:
  movement-type registry/checker ownership, stat-registry storage shape, and EXP curve
  data/profile ownership.

## Validation
- `python3 AGENT/Docs/gen_docs_index.py` after adding the decision record — no generated
  index diff was needed.
- `python3 AGENT/Docs/check_docs.py` — PASS after each logical step.
- Commit hooks ran `check_docs.py` and `check_rng_usage`; docs-only changes skipped the
  Godot suite.
- `git diff --check` — PASS for the GDD audit patch.

## Commits this thread
- `73b8394` - `Ratify scope reframe and web demo target`
- `cff3633` - `Audit GDD specs for EXT registry presets`
- `43a5f8d` - `Record GDD extensibility uncertainty findings`

## Next session
- Review the exact GDD chapter anchors and decide whether `B0-FEATURE-INDEX-WIRING` can
  move from broad chapter references to section-level anchors.
- Start Band 1 implementation planning/build in the existing order:
  `B1-PKGA` -> `B1-F1` -> `B1-SAVECODEC` / `B1-CST`.
- When Band 3/4 registry work starts, use the uncertainty report to avoid preserving
  closed movement/stat/EXP constants by accident.

