# Session Note 2026-07-05i - v0.2.8 rerun build cut (subagent); v0.3.0 track surveyed

## What was done

### v0.2.8 build cut (background subagent, commits `ab81a21` + `70205be`)

Cut the **v0.2.8** display-gate rerun build on `docs-reorg-2026-06-23`, carrying
the V027 fix pass (Q1-Q6, session 2026-07-05h). Pure rerun — no feature work.

- **Exe:** `builds/Project_Prometheus_v0.2.8_debug.exe` — 101,301,264 bytes,
  SHA-256 `850c5f87a19a3ef6e861f4149d746239e6cdb8cfecb32ff8951418fcd2de53e5`,
  source commit `ab81a21` (BUILD STAMP verified inside the exe).
- `ab81a21` — version bump 0.2.7→0.2.8 (export preset / MainMenu VersionLabel /
  environment_setup) + `playtest_checklist_v0.2.8.md`.
- `70205be` — `playtest_build_v0.2.8.md` manifest + `VAL-V023-DISPLAY`
  next-action cell → "CUT, awaiting live rerun" + evidence-PNG `.import`
  sidecars.
- **Checklist shape:** Part I = §1.3 (far-edge anchor, full zoom walk), §1.4
  (fresh-boot first-open + BOTH walls at max zoom + open-while-scrolling),
  §1.6 (corrected explainer: drag write-back + `Custom (WxH)` + NO recentre —
  with the explicit note that v0.2.7's recentre claim was our error; gray-out
  + `native WxH` outside Windowed; 1440p→4K at 2.0× re-scale; write-back
  persist-across-relaunch). Part III (§3.1 regression, §3.2 `%APPDATA%` log +
  BUILD STAMP) carried. **Ships as two files** (exe + checklist).

### v0.3.0 track surveyed + merge assessment

Solved a small mystery: the "missing" 2026-07-05b-f session notes and the
`test_mrd_scene.gd` suite the Q3 triage doc name-checked live on the
**local-only `v0.3.0-features` branch** (19 commits over merge-base `dd23656`,
never pushed). Contents: `B4-ENCOUNTER-MODEL` Slice 1 spawn seam (`[PUG-3]` +
contract fixes), `B6-MRD` Slices 1-5 (threat extraction, watch-set/danger-mode
resolver + overlay precedence registry + check_docs [22], hover-to-peek, path
arrows, grid-dim slider, watch tile), gamepad §4 R3 via the resolver, an
audit/drift fix pass, and the durable MRD scene suite.

**Merge assessment (owner asked):** nothing merges INTO the release line before
the gate closes — MRD repaints exactly the §1.3/§1.4 surfaces under rerun, the
spawn seam is safe but adds nothing to the gate, gamepad R3 and the scene suite
depend on MRD. The valuable merge is the REVERSE (release → `v0.3.0-features`)
once v0.2.8 is pinned: the feature branch predates the whole V027 pass, so its
overlay/zoom work sits on pre-fix camera code (wants the `keep_cursor_in_view`
flush + far-edge anchor). Expect manageable conflicts in `MapCursor`,
`test_map_cursor.gd`, `check_docs.py`. Also recommended pushing the branch.

## Commits

- `ab81a21` - `Bump version to v0.2.8; add the §1.3/§1.4/§1.6 rerun checklist`
- `70205be` - `Add v0.2.8 build manifest; control plane: build CUT, awaiting rerun`
- (this commit) - session note.

## Verification

- Both build commits passed the full pre-commit suite (50 suites) +
  check_docs 21/21; `test_release_metadata.gd` 5/5 at v0.2.8.

## Next session

- Ship the two files to the tester; triage the v0.2.8 return when it lands
  (gate flips on a §1.3/§1.4/§1.6 pass → `REL-V023-MERGE` / `B6-WEB-DEBUG`).
- Pending owner confirmation: merge `docs-reorg-2026-06-23` →
  `v0.3.0-features` (so MRD/gamepad work sits on the V027 fixes) and push the
  branch to origin.
- While waiting, headless-friendly queue: `B1-PKGA` RngService Steps 1-2,
  gamepad layer slice 1 (on the feature branch after the sync), the
  `ObjectDB instances leaked at exit` audit.
