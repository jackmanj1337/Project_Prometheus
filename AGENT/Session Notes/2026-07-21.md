# Session Note - 2026-07-21

## What was done

Triaged the returned **v0.5.2** Windows playtest end to end and turned it into a
scheduled, reviewed fix plan.

- **Root-caused nine issues** from the return (defend map unwinnable, rewind
  depth cap + forward-history not discarded + manual-turn-end history, modal
  input bleed on the rewind selector, FileDialog `x`-binding, manual-save slug,
  paired-escape stale `(-1,-1)` roster coordinate, and the "Campaign Data Error"
  surface). Reviewed against the **exact build commit `06e0386`** on the v0.5
  release line, not `agent/integration` (which has diverged and lacks the rewind
  selector).
- **Headless verification** (`godot --headless`): falsified my first Issue 1
  cause (both escape paths reach `prepare_pending_advance` identically);
  confirmed Issue 2 (survive needs unmarked hold tiles + turn_limit-as-defeat
  loses at turn 9) and Issue 3b (rewind stages the pre-truncation ledger). Two
  repros preserved under `AGENT/Docs/archive/evidence/v0.5.2/`.
- **Two Codex reviews** (findings, then the implementation plan), both preserved
  verbatim as evidence. The plan review caught three wrong first-draft approaches
  (Issues 5, 8, 10) and several redundant/inaccurate steps; all corrections were
  verified against the code and folded in (`[rev]` flags in the plan).
- **Decisions locked** with the maintainer (defend→pure survive; keep z/x + text
  suppression; full_history⇒uncapped retention; manual end-turn Waits in roster
  order; faction gold deferred; manual save auto-generates the slot id with an
  overwrite confirm; base v0.5.3 off `origin/agent/playtest-release-v0.5-fixes`
  `db270cb`).
- **Scheduled** all findings in the coordination tracker (container repo): ten
  `PP-V053-*` fix rows + five deferred rows in their homes. That tracker change
  is committed separately in the container repo (`agent/staging-area`), not here.

This commit files the triage doc, the fix implementation plan, the returned
checklist + build manifest, and the evidence bundle under the PP doc tree.

## Commits claimed

- `22550ea11df34891dbe80b6eb357e5f189eba41f` — Add v0.5.2 playtest triage, fix plan, and evidence

## Gates

- `python3 AGENT/Docs/check_docs.py` — PASS (all 41 checks green).
- pre-commit hooks — PASS (check_docs, RNG guard, analyzer tests, scene
  integrity, session-claims, evidence matrices, gdformat/gdlint 238 files clean);
  Godot suite skipped (docs-only change).
- Headless repros ran green against build `06e0386`
  (`AGENT/Docs/archive/evidence/v0.5.2/repro_*.gd.txt`).
- `coordination/check_tasks.py` — OK, 78 tasks, no conflicts (container repo).

## Next

Implement v0.5.3 fixes on a new `agent/playtest-release-v0.5.3-fixes` branch cut
from `origin/agent/playtest-release-v0.5-fixes` (`db270cb`, fetch first; delete
the dead slash ref). Start with the low-risk, decision-complete fixes:
`PP-V053-DEFEND-MAP`, `PP-V053-SAVE-AUTOID`, `PP-V053-ESCAPE-COORD`. Full plan:
`AGENT/Docs/playtests/v0.5.2_fix_implementation_plan_2026-07-21.md`.
