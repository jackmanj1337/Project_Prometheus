# Session Note - 2026-07-12b

## What was done

**v0.3.1 playtest return INTAKEN + TRIAGED + DIAGNOSED on
`agent/claude/2026-07-12/v0.4-prep`.** Followed
`playtest_v0.3.1_return_triage_kit_2026-07-12.md`:

1. **Intake.** Returned handbook preserved as
   `playtest_checklist_v0.3.1_returned_2026-07-12.md` (tester wording intact).
   Three v0.3.1 logs (all BUILD STAMPs verify `0.3.1` / `c7ce311`), one stray
   v0.3.0.d log (provenance only), and three screenshots archived under stable
   names in `AGENT/Docs/archive/evidence/`. Temp `AGENT/v0.3.1 playtest/`
   folder removed.
2. **Triage.** `playtest_v0.3.1_results_triage_plan_2026-07-12.md` written,
   findings-first. Results: `B1-SUSPEND` holds; maximize readout + relaunch-to-
   saved-size + stick targeting + Settings repeat/scroll + keybind-capture
   containment all PASS live. Still open: V031-GP-02 (selector moves behind an
   open dropdown), V031-GP-05 (character sheet: no selection scrolling; View
   Support / View Lead unreachable), V031-GP-03/04 cadence + zoom tunes,
   V031-GP-01 scroll lookahead, V031-DSP-01 (one-axis drag size events stall
   mid-drag after stepwise write-back to `1125x633`; window grew to ~975 with a
   stale readout; relaunch restored the stale size), V031-DSP-01b (degenerate
   `491x1913` persisted), V031-MRD-01 (new dual dark+bright red outline
   candidate requested, above units, dark over bright). Both gates stay
   Pending validation.
3. **Routing.** GDD_10 Next Work Queue + validation rows and control-plane
   rows (`B6-MRD`, `B6-INPUT`, `VAL-V023-DISPLAY`, `VAL-V030-GAMEPAD`) updated
   from live evidence. Learned: V031-* issue ids stay UNbackticked in GDD_10 /
   control plane (check_docs 20 treats backticked ALL-CAPS as Track IDs).
4. **Diagnosis.** `playtest_v0.3.1_triage_review_2026-07-12.md` (Code Reviews)
   with source-confirmed root causes: `MenuRepeatPolicy` polls the global
   `Input` singleton so `ModalScreen._process` steps focus while an
   `OptionButton` popup captures events (R1); `apply_resize_write_back` runs a
   synchronous `save()` per drag event inside the OS resize loop with no
   reconciliation once size signals stop (R2) and no minimum clamp (R3);
   `UnitDetailsScreen.open()` registers only "back" as a control entry and its
   custom selector never drives `MainScroll` (R4); menu repeat borrows the
   0.25/0.10 map-cursor cadence (R5); zoom strength-lerp (R6); perimeter tile
   alternates can't render above units or split watch/danger unions (R8 —
   proposes a `ThreatPerimeterOverlay` Node2D draw surface).

## Commits

- `f15ad7b` Archive returned v0.3.1 playtest checklist and evidence
- `719540e` Triage v0.3.1 playtest return into V031-* workstreams
- `dc1f785` Route v0.3.1 return into GDD_10 and control-plane rows
- `c99f9e5` Diagnose v0.3.1 defects in triage code review plan

All docs-only; check_docs 27/27 + RNG guard green on each.

## Plans for next session

- Owner discussion first: R6 zoom (tune again vs promote `B6-INPUT` sliders),
  R2 settle-timer/poll cadence values, R3 clamp floor, R8 outline
  widths/colors, and whether `v0.3.2` waits for all fixes or ships the
  gamepad-only subset early.
- Then the fix stream in review order R1 -> R4 -> R2/R3 -> R5/R7 -> R6 -> R8,
  full suite + scene checks per code commit (fixes are code, not docs).
- Diagnostics (`V030-NG-FOCUS`, `V030-DSP-TRACE`) and F8 stay until their
  gates close; remove in one release-cleanup commit.
- Cut `v0.3.2` focused rerun afterwards.
