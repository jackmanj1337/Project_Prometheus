# Session Note - 2026-07-09c

## Summary

Cut the focused rerun build the 2026-07-09b note queued, on `v0.3.0-features`.
The owner confirmed the "v0.3.1" they meant is this focused live rerun and
asked to label it **`v0.3.0.d`**. Bumped release metadata, wrote the
self-contained playtester handbook, exported the Windows debug exe, and recorded
the manifest.

## What Changed

- Bumped release metadata to `v0.3.0.d`: `export_presets.cfg` (preset
  name/export path/`product_version`), Main Menu `VersionLabel`, and the
  `environment_setup.md` export commands.
- Added the self-contained focused-rerun handbook
  `playtest_checklist_v0.3.0.d.md` — required equipment, log-return
  instructions, and six sections: (1) controller fixes, (2) New Game focus
  holdout with `V030-NG-FOCUS`, (3) suspend/continue, (4) one-axis display drag
  + maximize + relaunch persistence with `V030-DSP-TRACE`, (5) MRD-7 F8
  shared-cell pick, (6) recorded-request do-not-refile list.
- Exported `builds/Project_Prometheus_v0.3.0.d_debug.exe` and wrote
  `playtest_build_v0.3.0.d.md` with size/SHA-256.
- Added a "Focused rerun build CUT" pointer to the GDD_10 Next Work Queue and to
  the `VAL-V030-GAMEPAD` / `VAL-V023-DISPLAY` control-plane rows.

## Build Artifact

- Path: `builds/Project_Prometheus_v0.3.0.d_debug.exe`
- Source commit: `e19ac9b`
- build_info.json: version `0.3.0.d`, commit `e19ac9b`, built_at
  `2026-07-09T15:55:07Z`
- Godot: `4.6.stable.official.89cea1439`
- Size: `101496496` bytes
- SHA-256: `cff6a6bcb67c8f7b58471b462d54bc8bfafa115112dea463bce244d5d7627efd`

## Commits

- `e19ac9b` - Cut v0.3.0.d focused rerun metadata + handbook.
- (this note + build manifest + roadmap/control-plane pointers) - see closeout
  commit below.

## Verification

- Release-metadata test: PASS - 5/5 (preset name/path/product version, Main Menu
  label, checklist exists, environment setup names the build) all at `v0.3.0.d`.
- check_docs: PASS - all documentation checks green.
- Full source suite: PASS - pre-commit hook on `e19ac9b`, all suites green.
- Export: PASS - Windows debug exe built headless; `build_info.json` packed.

## Next

1. Deliver the exe + `playtest_checklist_v0.3.0.d.md` to the tester (two files).
2. On return, triage the per-launch `godot.log`: use `V030-NG-FOCUS` for the New
   Game focus holdout and `V030-DSP-TRACE` for the one-axis drag residue; do not
   guess from headless results.
3. Take the MRD-7 shared-cell pick, make the chosen mode the shipped default, and
   remove the temporary F8 cycle + diagnostic log lines.
4. Flip `VAL-V030-GAMEPAD`, `VAL-V023-DISPLAY`, and `B1-SUSPEND` only on the live
   pass evidence.
