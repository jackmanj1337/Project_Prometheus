# Session Note 2026-07-06e - B1-CST CampaignRules consolidation kickoff

## What was done

Built the **`B1-CST` CampaignRules consolidation kickoff** on
`v0.3.0-features`.

- `GameState` now owns one live `campaign_rules: CampaignRules` object.
- Removed the loose per-save rule fields from `GameState` instead of leaving
  delegation shims:
  - `permadeath_enabled`
  - `leveling_method`
  - `auto_promote_at_max_level`
  - `pair_up_enabled`
  - `max_skills`
  - `max_inventory`
- Migrated runtime and UI call sites to the rule object:
  - death handling, level-up method, auto-promotion prompt, skill cap
  - Pair Up gating in `PairUpRegistry` and `ActionMenu`
  - New Game rule persistence and rule-control seeding
- Added `CampaignRules.rewind_charges_per_map = 4`.
- Expanded `SaveData` campaign-rule defaults so the save envelope now carries
  the same live rule set: death mode, leveling method, auto-promote, Pair Up,
  skill/inventory caps, EXP faction list, hit formula, and rewind charges.
- Kept the old-save tolerance for legacy `permadeath_enabled` ->
  `death_mode`.
- Added the Godot `.uid` sidecars generated for the recent Band 1 scripts,
  because `check_docs.py` enforces tracked sidecars.

Tests updated:

- `test_game_state.gd` now asserts CampaignRules defaults and verifies the loose
  `GameState` rule fields are absent.
- `test_save_data.gd` locks the expanded rule defaults.
- Pair Up, New Game, Unit progression, HUD/details, combat, and map-cursor test
  stubs now write/read `campaign_rules`.

Docs/tracking updated:

- `GDD_01` records live `GameState.campaign_rules` ownership.
- `GDD_02` points Leveling and Permadeath at `GameState.campaign_rules.*`.
- `GDD_10` next queue now starts with `B1-SUSPEND`, then `B6-INPUT`.
- Feature Index and Project Control Plane mark live CampaignRules save
  ownership as Implemented while keeping author profile registries in Band 3.

## Commit

- `625c00d` - `Consolidate live campaign rules`

## Verification

- Loose-field grep sweep:
  - no live `gs.<loose_rule>` or `gs.get/set("<loose_rule>")` call sites remain.
  - remaining `permadeath_enabled` references are the `CampaignRules` field and
    `SaveData` legacy-load compatibility.
- Full Godot suite green:
  - `58 suites`, all passed.
  - Note: I accidentally invoked the full wrapper four times when trying to run
    focused files; all four full-suite runs passed.
- `python3 AGENT/Docs/check_docs.py` green.
- `bash scripts/ci/check_rng_usage.sh` green.
- `git diff --cached --check` green.
- Commit pre-hook reran docs, RNG guard, and all 58 suites green.

## Next

1. **`B1-SUSPEND` mid-battle suspend capstone**: consume the locked runtime
   fields and the live CampaignRules seam.
2. Keep the v0.2.8 tester return as a preempting release-line gate if it
   arrives.
3. After `B1-SUSPEND`, return to the queued `B6-INPUT` gamepad slice and the
   ObjectDB leak audit priority check.
