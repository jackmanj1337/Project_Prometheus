# Session Note - 2026-07-15y

## What was done

- Implemented `B6-PER-MAP-OVERRIDES` and the shared mutable campaign-state
  substrate required by the later `CampaignStatusRecord` consumer.
- Added the open three-layer resolver: active mid-map override, then node/map
  override, then effective campaign default. Campaign mandates short-circuit
  both lower-authority overlays.
- Added `apply_rule_flip(rule_id, value, reason, revert_scope)` with the enforced
  `end_of_map|permanent` vocabulary. Permanent flips append deterministic patch
  records; temporary flips clear at the map boundary.
- Added open carry-forward facts and imported-record identity to the same store,
  avoiding a parallel status persistence mechanism.
- Persisted permanent state in campaign saves and active layers in suspend and
  ledger entries, including Retry/Rewind rollback and old-save empty migration.
- Added a documentation guard for the vocabulary and required F1 rows. All 96
  suites pass, including the new 10-assertion mutable-state suite.

## Commit

- `Add mutable campaign rule state` - resolver, patch/fact store, save/ledger
  integration, authored node overrides, documentation, and regressions.

## Next

Build `CampaignStatusRecord` export, compatible-record scan, choose-none/import,
manual import validation, and New Game fact seeding on top of this store. Then
finish the defeat action-menu contract and run the final requirement audit.
