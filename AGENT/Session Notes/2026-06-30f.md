# Session Note - 2026-06-30f

## Topic
Band 1 implementation questions officially resolved.

## What was done
- Resolved the combat RNG event-record question by adopting the explicit
  pre-move tile handoff:
  `TurnManager.get_action_start_tile(unit)` plus
  `resolve_combat(attacker, defender, event_record := [])`.
- Resolved `[CST-13]` to hooks-only inside the first campaign/save spine:
  rule/charge persistence, defeat-menu entry point, and save fields in
  `B1-CST`; full Turnwheel mechanic as the immediate follow-on after
  `SaveCodec`, `SaveData`, and CampaignRules charge persistence exist.
- Updated the Band 1 implementation plan, Band 1 implementation questions
  review, CST register, and generated docs catalogs.

## Validation
- `python3 AGENT/Docs/gen_docs_index.py`
- `python3 AGENT/Docs/check_docs.py`
- `git diff --check`
- Commit hook also ran `check_docs.py` and `check_rng_usage`; docs-only change
  skipped the Godot suite.

## Commits this thread
- `76e47ec` - `Resolve Band 1 implementation questions`

## Next session
- Start `B1-PKGA` Step 1 unless priority changes.
- First coding move: add the official combat event-record plumbing and tests
  before migrating combat RNG rolls.
