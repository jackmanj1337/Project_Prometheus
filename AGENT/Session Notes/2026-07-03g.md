# Session Note - 2026-07-03g

## Topic
Partial resolution pass over the `[PUG-1..10]` parametric unit generation register.

## What was done
Read the latest session note (`2026-07-03f`), `AGENT/Docs/REGISTERS.md`, the `[PUG]`
register, the scope-map skirmish section, the `[PHB]` container register, and the arena
plan's shared-generator slice. Then updated
`AGENT/Docs/registers/parametric_unit_generation_open_questions_2026-07-03.md` with the
owner's generator decisions.

## Decisions captured
- `[PUG-1]` RESOLVED: `UnitSpec` supports three stat-generation modes: explicit stat
  ranges, class-growth rolled stats, and fixed growths.
- `[PUG-2]` RESOLVED: `generate_unit(spec, seed)` takes an explicit seed supplied by
  `RngService` for runtime consumers.
- `[PUG-3]` RESOLVED: use one spawn seam that accepts either `unit_data_path` or an
  already-built in-memory `UnitData`, not a separate generated-spawn path.
- `[PUG-9]` RESOLVED: equipment supports fixed loadouts and weighted equipment tables.
- `[PUG-10]` RESOLVED: one generator service backs any system that needs a fresh unit;
  consumers differ by trigger, source spec, and persistence policy, not by parallel
  generator implementations.

## Still open
`[PUG-4]`, `[PUG-5]`, `[PUG-6]`, and `[PUG-7]` stay OPEN/deferred because the owner wants a
separate campaign-map structure pass before locking skirmish/encounter formats.
`[PUG-8]` also stays OPEN: generator reuse is implied, but editor bake/provenance policy
was not fully walked this session.

## Validation
- `python3 AGENT/Docs/gen_docs_index.py`
- `python3 AGENT/Docs/check_docs.py` - PASS, all 21 checks green.

## Commits
- (this commit) - partial `[PUG]` register resolution and session note.

## Next Session
- Walk campaign-map structure before settling skirmish data format (`ForceSpec`,
  `EncounterDef`, map source, and scaling defaults).
- Then close the remaining `[PUG]` items and convert the register into a real
  implementation plan.
