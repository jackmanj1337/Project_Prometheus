# Session Note - 2026-08-01 — v0.6.0 combined visual-pass build source

## Branch context

- Branch: `agent/from-integration/v0.6.0-visual-pass` (throwaway build source)
- Base branch: `agent/integration` (tip `01662a53`)
- Merges: `agent/from-integration/viewport-anchoring` + `agent/from-integration/text-entry-implementation`
- Coordination Work IDs: `IMPL-VIEWPORT-ANCHORING-2026-07-31`, `IMPL-FILEDIALOG-ESCAPE-TEXTINPUT-2026-07-29`

## What was done

Owner asked to combine the viewport-anchoring and text-entry visual passes into one build.
Built a single validation build source off `agent/integration` merging both feature branches,
plus the v0.6.0 checklist and Windows bundle.

- **Merged both features.** viewport-anchoring merged clean bar the session-note INDEX; text-entry
  added two conflicts — INDEX (interleaved newest-first) and `SettingsManager.gd` (both inserted
  `static func`s at the same spot: the content-scale trio vs `normalize_text_entry_mode` — kept
  both). Full suite green on the merged tree: settings_manager 37, settings_screen 33, text_entry
  28, menu_scale OK.
- **v0.6.0 scaffolding + version lockstep.** `export_presets.cfg` → v0.6.0; the
  `test_release_metadata` lockstep (MainMenu `VersionLabel`, test `expected_version`,
  `environment_setup.md` current-build refs + Last verified). `playtest_checklist_v0.6.0.md`
  covers both domains ([VIEWPORT]/[TEXTENTRY]) AND all five carry-forwards from
  `playtest_v0.6.0_carryforward_2026-07-29.md` (controller hot-plug + logging need the log
  bundle; package-save validation now carries the concrete `%APPDATA%…installed\<id>\` path).
  `playtest_build_v0.6.0.md` build record.
- **Exported + bundled.** `prepare_build.sh` baked build_info (v0.6.0, commit 2ba99d09);
  headless Windows debug export PASS (PE32+ x86-64, 102238056 bytes; baked version verified in
  the pack). Bundle `builds/Project_Prometheus_v0.6.0_playtest_bundle.zip` (34575581 bytes,
  SHA-256 240f7bf4…) = exe + BUILD_INFO.json + checklist + build record + both campaign fixtures
  + SHA256SUMS.txt. `builds/` is gitignored, so the zip is delivered out-of-band.

## Commits claimed

- `2ba99d09457d5c7eec381f01dd7e63941e8480de` — v0.6.0 build scaffolding + version-bump lockstep
- `9713ec7fafd49a475174f8a15d285b88b70a1af2` — v0.6.0 build record: fill export provenance (exe/campaign/bundle hashes)

(The two merge commits `eb5dac14` / `2c9e4fa8` are merges, exempt from claiming.)

## Gates

- `bash run_tests.sh` — PASS on the merged tree (release_metadata 5/5 after the lockstep).
- `python3 AGENT/Docs/check_docs.py` — PASS. Export: PASS.

## Next

Hand the bundle to the owner for the combined visual/input pass. Each feature merges back to
`agent/integration` independently once its half of `playtest_checklist_v0.6.0.md` passes; a
failure in one does not block the other. This build source is throwaway — do not merge it back
wholesale. The two feature rows stay `in_progress` until their halves return.
