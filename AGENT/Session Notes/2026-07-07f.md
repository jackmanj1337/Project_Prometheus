# Session Note 2026-07-07f - v0.2.9 section 1.6-only rerun build CUT

## What was done

Cut the v0.2.9 Windows debug playtest build and its section-1.6-only handbook, following
the section 1.6 fix pass from 2026-07-07e (`65dde85`). Two commits: source/metadata +
handbook (`3dddefc`), then the filled manifest + generated sidecars (`1d06c56`).

### Version bump (single source of truth = export preset)

- `export_presets.cfg`: preset name / `export_path` / `application/product_version` → v0.2.9.
- `scenes/ui/MainMenu.tscn` `VersionLabel` → `v0.2.9`.
- `AGENT/Docs/guides/environment_setup.md`: "Currently at `v0.2.9`", export commands,
  checklist reference.

### Handbook + manifest

- `playtest_checklist_v0.2.9.md` — self-contained tester handbook. **Part I = §1.6 only**
  (§1.3/§1.4 passed live in v0.2.8; everything else already passed earlier). §1.6 covers:
  (a) custom-size readout now shows **`client W×H`** with no bogus `→ applied` re-clamp
  (V028-02), and the size vocabulary (preset request / client size / native display size);
  (b) the **Windows maximize button** — panel stays centered through maximize (reactive
  re-center), maximize is not saved as a resolution, and un-maximize restores the windowed
  size (V028-03). Part II skipped; Part III light regression + the `%APPDATA%` BUILD-STAMP
  log request.
- `playtest_build_v0.2.9.md` — build manifest.

### Build

- `bash scripts/tools/prepare_build.sh` stamped `build_info.json` (version 0.2.9, commit
  `3dddefc`), then `godot --headless --export-debug "Project Prometheus v0.2.9"`.
- Artifact: `builds/Project_Prometheus_v0.2.9_debug.exe` (git-ignored).
  - Size: `101375272` bytes
  - SHA-256: `fb064f2a24bab20d8854c5ecbeda04f078463e170e0be9f22ec3c8e5816cf3f8`
  - Source commit `3dddefc`; `build_info.json` (version 0.2.9, commit 3dddefc) confirmed
    packed into the exe.
- Ships as **two files**: the exe + `playtest_checklist_v0.2.9.md`.

## Verification

- `test_release_metadata.gd`: 5/0 — preset name/path/product version, Main Menu label,
  checklist presence, and setup guide all agree at `v0.2.9`.
- `check_docs.py` green; docs index regenerated.
- Export exit 0; SHA/size recorded; build_info packed confirmed via exe grep.
- Reverted the export's class-cache reorder churn (no new `class_name`). Tracked the
  import/uid sidecars the export scan generated for already-committed files (3 v0.2.8
  evidence PNG `.import`, 2 test `.uid`) to match the tracked-sidecar convention.

## Next

Ship the two files (exe + `playtest_checklist_v0.2.9.md`) to the tester. On return, triage
§1.6; flip `VAL-V023-DISPLAY` only when §1.6 passes on a real Windows screen (ideally incl.
the Windows maximize button at 2.0× Menu Scale + a non-preset drag-resize), then proceed
`REL-V023-MERGE` / `B6-WEB-DEBUG`. Feature-branch alternatives while the tester runs:
`B6-INPUT` input-mode/focus seam, ObjectDB leak audit.
