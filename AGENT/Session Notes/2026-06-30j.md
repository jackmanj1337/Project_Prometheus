# Session Note - 2026-06-30j

## Topic
Wrote the Band 4 implementation-plan next-session handoff.

## What was done
- Reviewed the full Band 4 control-plane block (14 rows) and traced the
  intra-band dependency order.
- Wrote `AGENT/Docs/plans/band4_implementation_plan_handoff_2026-06-30.md`,
  modeled on the Band 3 handoff: purpose, gating reality, rows to cover,
  bootstrap order, read-first list, recommended plan shape, decisions not to
  reopen, owner questions, and watchouts.
- Identified the two internal sub-foundations that gate the rest of the band —
  `B4-IEQ` (items) and `B4-MAP-OBJECTS` (activation) — and recommended a
  12-step build order ending in `B4-CAMPAIGN-LOOP` (integration), with
  `B4-PROMOTION-UI` deferred until content needs 3+ promotion paths.
- Surfaced four owner questions for the drafting session: one-combined-plan vs
  split (recommended: split IEQ + map-objects as sub-plans plus a thin
  campaign-loop plan), the minimum playable loop for v1, convoy/shop UI/UX
  timing, and the dialogue v1 command set scope.
- Verified every referenced plan/design/register link resolves.

## Commits this thread
- (this note + handoff + index row committed together)

## Validation
- `python3 AGENT/Docs/gen_docs_index.py` (INDEX/REGISTERS regenerated).
- `python3 AGENT/Docs/check_docs.py` — all 21 checks green.
- `git diff --check` — clean.
- Docs-only change; the Godot suite was not exercised.

## Current State
- Band 1, 2, 3 implementation plans exist; Band 3 plan reviewed and stable
  (Q1-Q4 closed; per-map overrides tracked as deferred `B6-PER-MAP-OVERRIDES`).
- Band 4 now has a next-session handoff; no Band 4 plan written yet.

## Next Session Target
Write the Band 4 implementation plan(s) per
`band4_implementation_plan_handoff_2026-06-30.md`. All four owner questions are
resolved (D1-D4 in the handoff), so draft directly — **start with the two
sub-foundation plans `B4-IEQ` (staged migration) and `B4-MAP-OBJECTS`**, then the
separate `B4-CONVOY` / `B4-SHOP-ECONOMY` plans and the thin `B4-CAMPAIGN-LOOP`
integration plan, following the handoff bootstrap order. Implementation stays
gated behind Band 1-3; `B1-PKGA` Slice 1a remains the code start point.

## Read First Next Session
1. `AGENT/Session Notes/2026-06-30j.md` (this note).
2. `AGENT/Docs/plans/band4_implementation_plan_handoff_2026-06-30.md`.
3. The read-first list inside that handoff.

## Watchouts
- Do not start Band 4 implementation before Band 1-3 contracts are accepted and
  the named Band 2/3 services exist.
- `B4-IEQ` is a migration off the existing item model, not greenfield.
- Keep `B4-IEQ` and `B4-MAP-OBJECTS` first; most of the band depends on them.

## Addendum — owner decisions recorded into the handoff (same session)
Discussed the four handoff owner questions; all resolved and written into the
handoff "Resolved Owner Decisions" section:
- **D1 — separate plans** (not one combined): distinct `B4-IEQ`,
  `B4-MAP-OBJECTS`, `B4-CONVOY`, `B4-SHOP-ECONOMY`, and a thin
  `B4-CAMPAIGN-LOOP` integration plan; smaller consumers grouped pragmatically;
  `B4-PROMOTION-UI` deferred.
- **D2 — sequencing:** convoy and shop are separable from the first playable
  loop for playtesting but follow closely (convoy fast-follow, shop close after).
- **D3 — convoy/shop UI timing:** functional panels on `B3-PHB` now,
  keyboard+mouse-first and rough, consuming a thin selector `B6-INPUT` later
  fills; firm commitments that full control-scheme support and UI polish both
  land eventually. Convoy panel UI requirements captured as `[CNV-8]` in the
  convoy register (per-unit inventory view; author groups + All category + author
  sorting; author-selected row fields name/uses/count/base value/stack value;
  focused-item detail pane).
- **D4 — dialogue v1 command set:** line/choice(goto+set_flag)/label +
  `set_background` + positioned static portraits (enter/move/exit/replace) + MET
  passthrough (set_var/grant_item/recruit) + `[REQ]` gating at option +
  conversation scope (hidden|shown_disabled) + manual pacing + atomic playback;
  animation tiers, reflect, auto/skip pacing, runtime set_layer, mid-conversation
  suspend, and the editor deferred. Recorded against `DLG-7`.
- Explained branch gating with examples (recruit eligibility, item/stat-gated
  options, flag-driven story segments); owner approved including it in v1.
