# Session Note - 2026-07-03c

## Topic
Draft the **Band 6 implementation plans** (the agreed next-session target from
2026-07-03b) for the rows whose design the Q8-Q13 walkthrough (2026-07-01) settled.

## Scope decision
Read the last session's wrap + the Q8-Q13 walkthrough decisions + the Band 6
control-plane rows. Confirmed all six candidate registers are RESOLVED and the
existing `campaign_status_property_recruitment_plan_2026-06-29.md` is a
decisions-capture note, **not** a real implementation plan. Owner chose (via
scope question) the **Q8-Q13 settled set** — three grouped plans — deferring
FOW/DTR/prep-progression (register-resolved but not in the v1 walkthrough) to a
follow-up. Rows already covered were left alone: `B6-INPUT` (3 plans),
`B6-WEB-DEBUG` (plan), `B6-SPRITE-IMPORTER` (Q12 held), `B6-RESCUE-CARRY` /
`B6-PROPERTY-CAPTURE` (narrowed to expansion; foundations moved to B5).

## What was done
Wrote three grouped Band 6 plans in the established per-slice shape (Purpose /
Scope / Non-Goals / Source Docs / Decisions-Not-To-Reopen / Dependency Note /
Existing Code Touchpoints / ordered slices with Files/Steps/Tests/F1/DoD /
Implementation Commit Order / Verification Checklist):

- `AGENT/Docs/plans/band6_mutable_campaign_state_implementation_plan_2026-07-03.md`
  — `B6-CAMPAIGN-SHARING` + `B6-CAMPAIGN-STATUS` + `B6-PER-MAP-OVERRIDES`, unified
  per **Q13 Implication 2** ("one mutable campaign state store, not two carry-forward
  mechanisms"). Slices: (1) the **3-layer rule resolver** — mid-map triggered
  override (= Band 3 `apply_rule_flip` + a new `revert_scope` param) > per-map rules
  > campaign defaults, with mandate short-circuit; (2) the mutable-campaign-state
  store (permanent-flip patch-log + carry-forward facts = one F1 region, suspend-safe
  override state); (3) campaign package export/import (self-contained pack via
  `AssetResolver`, validation predicate, load/repair fallback); (4) `CampaignStatusRecord`
  carry-forward (scan/choose/manual-import; facts seed TCV; second import consumer).
- `AGENT/Docs/plans/band6_relationships_min_implementation_plan_2026-07-03.md`
  — `B6-RELATIONSHIP-MIN` (**Q9** / `[REL-1..9]`). Slice 0 extracts the `[PXP]`
  resolver **pair-agnostic** (`[REL-9]`); Slice 1 pair-keyed graph + `RelationshipProfileDef`
  ranks + per-pair per-map anti-grind cap + exclusive-top-rank toggle; Slice 2 the
  three pair-keyed gain sources (`co_deployment_survival` / `end_turn_proximity` /
  `class_exp_relationship_share`) + the shared proximity radius primitive; Slice 3
  the per-unit directional rank-scaled proximity **benefit aura** (independent of
  pair-up bonus + Charm) + on-crossing `unlock_conversation`/`set_flag` via TCV.
  Reuses the PXP resolver (not its per-unit storage); class EXP stays separate.
- `AGENT/Docs/plans/band6_map_readability_implementation_plan_2026-07-03.md`
  — `B6-MRD` (**Q10** / `[MRD-1..6]` + `[TUR-1..4]`), sequenced by save-dependency.
  Slices 1-5 are **no-save view state** buildable now: (1) `get_unit_threat_tiles`
  extraction + faction regression; (2) `_watch_set`/`_danger_mode` cycle + render +
  the **precedence-ordered overlay registry** (`[MRD-1]` C: range/threat blend,
  hover+arrows opaque on top); (3) hold-to-peek (compute once on press, cached);
  (4) path arrows over `get_movement_path`; (5) `grid_dim` slider. Slice 6 (watch-set
  save serialization) is the **only** save-touching slice and trails `B1-SUSPEND`.

Grounded touchpoints against the live tree: `CampaignRules.gd` (Stage-4.3 stub;
`[CST-4]` B migration assumed), `GridManager.gd` (`get_unit_threat_tiles` extraction
target, `get_enemy_danger_tiles` l.546 already builds aggregate danger,
`_paint_overlay`/`OVERLAY_DARK_RED` l.516-519, `get_movement_path` l.328), and the
absence of any relationship system / PXP / `apply_rule_flip` in code (drafted against
planned Band 1/3 APIs, same as the Band 5 plans).

## Validation
- `python3 AGENT/Docs/gen_docs_index.py` then `check_docs.py`: 21/21 green.
- Docs-only change; no Godot suite run (planning artifacts, no code).

## Commits
- (this commit) — three Band 6 plans + control-plane wiring (5 rows:
  `B6-CAMPAIGN-SHARING`/`-STATUS`/`-PER-MAP-OVERRIDES` → mutable-state plan;
  `B6-RELATIONSHIP-MIN`; `B6-MRD` — all flipped to Planned, `B6-PER-MAP-OVERRIDES`
  Deferred→Planned per Q13) + INDEX regen.

## Next Session
- **Remaining Band 6 rows for a design/scope pass:** `B6-FOW`, `B6-DTR`,
  `B6-PREP-PROGRESSION` (bonus-EXP + training halls) — registers are RESOLVED but
  they were NOT part of the Q8-Q13 v1 walkthrough, so confirm v1-scope with the
  owner before drafting plans. If in v1, they are writeable (design settled).
- Small Band 5 tidy-ups still pending (C6 berserk+scorer test, C7
  disabled-with-reason, manifest open content picks) can ride along.
- Parallel thread unchanged: when the v0.2.5 playtest returns, triage
  `VAL-V023-DISPLAY` → flip → `VAL-V023-MERGE-CHECK`. Ungated build option:
  `B1-PKGA` (RngService), the root gate.

## Read First Next Session
1. This note's scope section (which Band 6 rows are planned vs still open).
2. `band5_plus_preimplementation_questions_review_2026-06-30.md` → Q8-Q13 (the
   settled Band 6 design behind the three new plans).
3. The three `band6_*_implementation_plan_2026-07-03.md` plans.
4. For FOW/DTR/prep: `fog_of_war_los_open_questions_2026-06-21.md`,
   `destructible_terrain_open_questions_2026-06-21.md`,
   `bonus_exp_arena_open_questions_2026-06-27.md` + `training_halls_open_questions_2026-06-27.md`.
