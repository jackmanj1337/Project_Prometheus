# Session Notes — 2026-05-21g

## Focus
Finish M6 promotion implementation and add promotion-item restrictions by class
and class group.

## What was done
- Completed the playable M6 promotion flow on `class-skill-rebuild`.
- Added `PromotionScreen` and wired it into `GameMap` / `MapCursor` so:
  auto-promotion prompts and promotion-item use both open the same modal.
- Added promotion input bracketing through `EventBus.promotion_started` /
  `promotion_finished` so the cursor stays suppressed while the modal is open.
- Ensured promotion prompts queue behind the level-up screen instead of opening
  on top of it.
- Added promotion-item support to `ItemHandler` with eligibility checks via
  `can_apply_item()`, plus inventory consumption on confirm.
- Added class-restricted and class-group-restricted promotion items:
  `allowed_classes` and `allowed_class_groups`.
- Added `ClassData.class_groups` and `DataManager` validation for both
  `allowed_classes` and `allowed_class_groups`.
- Added promoted class data for:
  `ranger`, `sniper`, `bow_knight`, `paladin`, `vanguard`, `bishop`,
  `paragon`, `war_monk`, `war_cleric`, `general`, `great_knight`,
  `mage_knight`, `sage`, `dark_knight`, `hero`, `sentinel`.
- Added promotion items:
  `master_seal`, `orion_bolt`, `guiding_ring`.
- Added promoted-skill `.tres` resources and registered their effect ids as
  current stubs in `SkillHandler` for the later M9 skill-effects work.
- Updated the main plan docs so M6 is marked done and the next tracked work is
  N6, then M7, then deferred hotseat.

## Tests run
- `test_promotion_screen.gd`
- `test_unit_stats.gd`
- `test_skill_item_handler.gd`
- `test_data_manager.gd`
- `test_data_layer.gd`
- `test_action_menu.gd`
- `test_level_up_screen.gd`
- `test_game_map_scene.gd`

All passed.

## Commits
- `7545a12` `Complete M6 promotion flow and promotion items`
- Documentation + session-note status update committed after this note edit.

## Notes / issues
- `test_game_map_scene.gd` still logs noisy unknown weapon/skill errors during
  scene bootstrap in `--script` mode, but the assertions pass. This looks like
  the existing autoload/setup mismatch in that harness, not an M6 regression.

## Plans for next session
- Implement **N6** so level-1 class skills are granted on unit creation.
- Review whether the noisy `test_game_map_scene` bootstrap errors should be
  cleaned up in the harness.
- After N6, the saved next milestone is **M7 — Second Seal Reclassing &
  Demotion**.
- Hotseat remains deferred until the class/skill track is sorted out.
