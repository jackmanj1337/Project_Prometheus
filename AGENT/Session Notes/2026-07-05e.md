# Session Note 2026-07-05e - Audit fix pass

## What was done

Fixed the actionable issues from the 2026-07-05 full project audit.

Spawn seam:

- Runtime now rejects enemy placements that provide both `unit_data` and
  `unit_data_path`, matching `DataManager` validation.
- Inline/path enemy placements now preserve the resolved `UnitData.ai_profile`
  unless the placement explicitly provides an `ai_profile` override.
- `test_spawn_seam.gd` now covers exact-one source validation, fresh duplication,
  profile preservation, and explicit placement override.
- `MapData.gd`, `GDD_01`, `GDD_06`, and the `B4-ENCOUNTER-MODEL` control-plane row
  now document the implemented contract.

Small drift fixes:

- Added root `tools/**` to the Windows export exclusion and updated the active
  setup/web-debug docs that repeat the export shape.
- Removed the stale hardcoded "10 units" Map 950 registry description.
- Updated the manual playbook to match the current five-skill Map 950 skill-cap
  fixture.
- Added a soft session-closeout reminder to the full-audit procedure: if the last
  full audit is nearing the 4-week / 30-commit threshold, add "full audit due soon"
  to the next-session note. This is not a CI or pre-commit gate.

## Commits

- `34384ae` - `Fix enemy placement spawn seam contract`
- `03cc8cf` - `Fix audit drift items`

## Verification

- `godot --headless --path /workspace --script res://scripts/tests/test_spawn_seam.gd`
  - PASS, 8/0.
- `godot --headless --path /workspace --script res://scripts/tests/test_data_manager.gd`
  - PASS, 22/0.
- `godot --headless --path /workspace --script res://scripts/tests/test_map_950_promotion_validation.gd`
  - PASS, 8/0.
- `godot --headless --path /workspace --script res://scripts/tests/test_release_metadata.gd`
  - PASS, 5/0.
- `python3 AGENT/Docs/check_docs.py` - PASS.
- `git diff --check` - PASS.
- Both commits' pre-commit hooks ran docs, RNG guard, and the full 51-suite Godot
  test set green.

## Not fixed in this pass

- The broader closed-dispatch/open-registry issue is architectural work already
  owned by the registry migration rows (`B2-REGISTRY`, `B5-AI-COMPOSITION`,
  `B3-STAT-REGISTRY`, etc.). This pass did not add new closed branches.
- The `gdlint`/`gdformat` gate still needs a pip-capable environment with
  `gdtoolkit`, then a one-time whole-repo format and CI/hook wiring. This
  environment still has no `pip`, so it cannot run the tool.

## Next session

No narrow audit fix remains open from the 2026-07-05 rollup. Resume the planned
`v0.3.0-features` work: MRD visual polish/live verification and gamepad R3 as
appropriate, while treating any future author-facing vocabulary addition as the
trigger to build the relevant registry slice rather than adding another switch case.
