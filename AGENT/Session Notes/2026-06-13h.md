# Session Notes - 2026-06-13h  (Stage 3.3–3.8 — STAGE 3 COMPLETE)

## Focus

Finish the documentation consolidation Stage 3: rewrite the remaining system chapters
(GDD_04, GDD_05, GDD_01, GDD_06, GDD_07, GDD_08) to the section template + status
vocabulary, finalize their adoption-matrix rows, and pin every Stage-3 anchor. This
completes the Phase 3 ⨉ 4 chapter work; next is Stage 4 (roadmap + Package G).

## What was done

**Stage 3.3 — GDD_04 Weapons & Items.** Full rewrite to the template. Owns weapon
families + the **rank-scaled triangle bonus table** (SET-003/RULE-013, reproduced
concisely as Target), WEXP thresholds/gain/migration (SET-004/RULE-003/004), S-Rank bonus
with the **move-to-engine + retire `s_rank_mastery`** target (SET-005/RULE-002),
effectiveness, items/economy/forging, inventory, stationary weapons, broken-weapon
degraded mode (OPEN-5, Deferred). Combat *application* cross-referenced to GDD_02; schemas
to GDD_01.

**Stage 3.4 — GDD_05 Skills.** Rewrite to the template; **added the missing §Pair Up &
Support System** (pass 1 Implemented; value migration Planned RULE-012/SET-010; Dual
Strike/Guard Target; supports 4–8 Deferred OPEN-1), plus §Skill Acquisition, §Skill
Activation & RNG (proc draws from the event RNG at its trigger slot — RNG-1/OPEN-2), and
§Condition / Skill Precedence (per-skill exceptions home; general rule stays in GDD_02).
Cleric "Light E" Open decision cross-ref to GDD_03 (RULE-009/OPEN-10).

**Stage 3.5 — GDD_01 Architecture.** Added the header status block + ownership statement
and **added §CampaignRules Contract** (live `GameState` rule fields Implemented;
consolidation + `exp_gaining_factions` OPEN-4 / rewind-charge pool RNG-3 / follow-up-
threshold override Target; code stub deferred to **Stage 4.3**). The two-RN save-compat
note and the `RngService` autoload-order insertion were already present (§Determinism,
Implementation Notes — landed 3.1). The accurate resource/serialization/scene/function
reference was **kept as Reference tracking the code** rather than churned.

**Stage 3.6 — GDD_06 Maps & Objectives.** Header + status labels; **added §Terrain &
Movement schema** owning the terrain-type/movement-cost data with **split tables**
(project Implemented + corpus Target, SET-008/RULE-010) and the terrain-ID-mapping **Open
decision** (RULE-011 → AWR-8). Combat *effects* of terrain stay in GDD_02 §Terrain.
Status-labeled Objective System, Map 001 reference, Phase 3 maps (M16), Doors/Chests + Fog
(Planned).

**Stage 3.7 — GDD_07 UI/UX.** Header + status labels across input/cursor/screens/state-
machine/feedback; **added §Accessibility & Input Parity** (keyboard/mouse + hotseat parity
Implemented; key rebind + UI-scale Planned). Platform/renderer targets cross-referenced to
GDD_00 (OPEN-8/11). No corpus-adoption rows (UI is project-specific).

**Stage 3.8 — GDD_08 Enemy AI.** Header + status labels; **added §AI Determinism & Parity**
(pure-function decisions, stable tie-breaks, host-authoritative online RNG-4, EXP parity
OPEN-4), **§Performance Constraints** (per-unit Dijkstra budget + scaling guardrails that
must preserve determinism), and **§Enemy Generation & Autolevel** (Not reviewed). Tactical
scoring Planned.

**Bookkeeping each step.** Pinned the chapter's adoption-matrix rows + the feature-index
anchors (code/test anchors verified per chapter); flipped decision_index rows to
`Applied`; ticked the master checklist; appended DONE markers to the lifecycle table.
**Final sweep:** pinned the leftover GDD_02/GDD_03 `(TBD)` matrix anchors from the 3.1/3.2
session — the matrix now has **0 `(TBD)` section anchors** and every Stage-3 rule has one
pinned numbered-GDD owner.

## Tests and verification

- Commit hook: PASS on every commit (38 suites green). Documentation-only; no game code.
- `grep` confirms no `(TBD)` section anchors remain in `GDD_Adoption_Matrix.md` and no
  `§…(TBD)` anchors in `GDD_Feature_Index.md`. Remaining `TBD (S4)` (roadmap owner) and
  `TBD (S3)` (manual-test coverage) cells are intentional — filled in Stage 4 / Stage 5–6.

## Commits made

- `Stage 3.3: rewrite GDD_04 …` (255d64e)
- `Stage 3.4: rewrite GDD_05 …` (d2ddadc)
- `Stage 3.5: GDD_01 header + status markers; add CampaignRules contract …` (530fc5e)
- `Stage 3.6: GDD_06 … add Terrain & Movement schema …` (19a857a)
- `Stage 3.7: GDD_07 … add Accessibility & Input Parity contract` (73adff2)
- `Stage 3.8: GDD_08 … add AI determinism/parity, performance, enemy-gen …` (0585741)
- (this note + the matrix anchor-pinning sweep — final commit)

## Decision worth noting (PL#8)

These 3.x commits documented **Target design** (no behavior changed, no code written), so
**no `GDD_10` roadmap status flip** was required (same call as 3.0–3.2). Roadmap
reconciliation is Stage 4. Per PL#8, a future chapter edit that implies a status change
must flip GDD_10 in the same commit.

## Watch for / known follow-ups

- **"current X / Implemented" idiom in Status: lines.** The split-status phrasing
  (e.g. "current values **Implemented**; corpus values **Target design**") uses the word
  "current" as a noun-qualifier. It is pervasive across GDD_02–08 (established by the
  3.1/3.2 session). DOC-003 prohibits unqualified "current/complete/canonical" in status-
  bearing sections — the **DOC-011 CI check (Stage 6.1)** should decide whether to lint
  this idiom; if so, sweep all chapters uniformly (don't half-fix).
- **GDD_01 combat-context binding contract:** GDD_02 §Combat Modifier Pipeline Order points
  to "GDD_01 (combat context schema)", but GDD_01's binding home for that is the
  `CombatResolver.gd` file header, not a dedicated section. Consider a short GDD_01
  §Combat Context section in a later pass (low priority; matrix row now notes the header).
- Still open/deferred (don't author): RULE-011 terrain mapping (AWR-8), OPEN-9 Soldier
  (AWR-2), OPEN-10 Cleric Light (RULE-009).

## NEXT SESSION — start here (Stage 4)

Read order: this note → `documentation_consolidation_master_checklist_2026-06-13.md`
(Stages 0–3 ticked) → `decision_index.md` → `GDD_10_Roadmap.md` + `GDD_10a_Overview.md`.

**Stage 4 — Roadmap & task-tracking consolidation:**
- **4.1** Make `GDD_10_Roadmap.md` the sole roadmap owner; merge unique ordering/history
  from `GDD_10a_Overview.md`, then **delete `GDD_10a`** and repair its inbound links
  (`README.md:19`, `GDD_00:22,36`, `GDD_09:52`) per the Stage 1 lifecycle table (DOC-004).
- **4.2** Stable IDs + bug/task hygiene; ensure every Target GDD section has a roadmap
  owner or explicit "not scheduled". Several `TBD (S4)` feature-index cells get filled here.
- **4.3** Land the Package G release decisions as roadmap owners: 1.0 definition (D-B) +
  M11 re-scope; rename gate (D-A); legal/licensing gate (DOC-012/OPEN-12); renderer/platform
  (OPEN-8/11); campaign prerequisites (D-D); **`CampaignRules` stub** with `exp_gaining_factions`
  (OPEN-4) — code stub + the GDD_01 §CampaignRules Contract it realizes; backlog items
  (broken-weapon OPEN-5; SFX PL#9).

Then Stage 5 (guides + gated moves/deletes: GDD_09/Assumptions/Manual_Tasks/update-ref) →
6 (DOC-011 CI checks) → 7 (verify plan §12).
