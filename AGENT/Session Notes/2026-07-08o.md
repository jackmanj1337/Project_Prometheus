# Session Note - 2026-07-08o

## Summary

Walked the v0.3.0 triage owner review Q1-Q5 with the owner and recorded all
five decisions; the fix passes are now fully scoped.

## Decisions (recorded in `playtest_v0.3.0_triage_review_2026-07-08.md`)

- **Q1 (Input Mode semantics):** relabel the Settings row **Input Mode →
  Input Prompts**; behavior unchanged (devices never blocked, prompts + menu
  focus only). Internal `input_mode` keys/vocabularies untouched.
- **Q2 (PS glyphs):** brand-correct WORDS now ("Square"/"Cross") + brand the
  rebind rows; real button glyphs ride the `UI-INSPECTION` font/theme pass.
- **Q3 (overlay concurrency):** registry-compose plumbing unconditional
  (selection/targeting/menus repaint through `repaint_overlays`; "D" markers
  render whenever the watch set is non-empty). For SHARED cells, prototype
  BOTH border-through and second-`TileMapLayer` stacking behind a toggle,
  compare via `UI-INSPECTION` headless screenshots, then let the rerun's live
  pass pick. Recorded as [MRD-7] RESOLVED.
- **Q4 (maximized readout):** show live `Maximized (WxH)` from the actual
  client size while maximized (mirrors `native WxH`); persistence unchanged.
- **Q5 (cursor-traced pathing):** backlogged with the perception/fog work
  ([PER]) as [MRD-8]; every future handbook carries a short "recorded
  requests" note so the tester sees it tracked.

## What Changed

- `playtest_v0.3.0_triage_review_2026-07-08.md`: status → DECIDED; Walkthrough
  Decisions section filled.
- `map_readability_open_questions_2026-06-21.md`: extended to MRD-1..8 —
  [MRD-7] overlay concurrency (RESOLVED with the Q3 decision + root cause:
  `MapCursorSelection.gd:53`/`:104` still call the pre-registry overlay API),
  [MRD-8] cursor-traced pathing (Open decision, deferred to [PER]).
- Triage plan sequencing item 1 marked DECIDED with the routing; rerun-build
  step now carries the [MRD-7] comparison toggle + [MRD-8] handbook note.
- Control plane: `B6-INPUT` (Q1/Q2 routing), `B6-MRD` (MRD-7/8), and
  `VAL-V023-DISPLAY` (Q4 label decision) rows updated; GDD_10 queue rows
  updated to DECIDED.
- `INDEX.md`/`REGISTERS.md` regenerated (register now Split MRD-1..7/MRD-8).

## Commits

- This commit - Record v0.3.0 triage walkthrough decisions.

## Verification

- `python3 AGENT/Docs/check_docs.py` PASS (26 checks).
- `bash scripts/ci/check_rng_usage.sh` PASS.

## Next

1. V030-SUS-01 suspend/resume fix pass with failing-first
   `test_suspend_map_runtime.gd` repros (top defect).
2. V030-GP-01/02/03 + V030-INP-02 fix pass under `B6-INPUT`, folding in the
   Q1 relabel and Q2 word-label rebind rows.
3. V030-DSP-01 fix (one-axis write-back/readout repro + `Maximized (WxH)`
   readout state).
4. [MRD-7] compose plumbing + both shared-cell prototypes with headless
   screenshot comparison.
5. ONE focused rerun build: controller Parts I-II, suspend section 9,
   section 1.6 with relaunch-persistence step, the [MRD-7] toggle, and the
   [MRD-8] "recorded requests" note.
