# Session Notes — 2026-06-14

## What was done

Processed the returned **v0.1.4 playtest package** (an `AGENT/Incoming/` drop):
triaged it, then investigated and fixed every implementable defect it surfaced.

### Triage (commit `09e47fb`)
- Moved the three returned artifacts out of `AGENT/Incoming/` into `AGENT/Docs/`:
  the filled-in handbook (`playtest_checklist_v0.1.4_returned_2026-06-14.md`, kept
  verbatim as evidence), the promotion screenshot (`950MERC Promotion.png`), and a
  **deduplicated** `godot_v0.1.4_2026-06-14_sample.log` (the raw 9.4 MB log was not
  committed, per decision — sample + counts only). Deleted the raw log + emptied dir.
- Recorded the findings as a tracked **"Current Playtest / Bug-Fix Round — v0.1.4"**
  section in `GDD_10_Roadmap.md` (user chose roadmap over a separate fix-plan doc):
  6 defects + 3 lower-severity observations.

### Investigation (full diagnosis delivered to the user)
Root-caused all 6 defects + 3 observations against the code. **Correction:** during
triage I called Defect 3 (pair-up bonuses) a "correct code path" — that was wrong. A
follow-up unit test (see below) proved it a **real bug**: the static read missed that
`add_modifier` removes all modifiers sharing a source, and the pair-up code reused one
source for every stat, so only the last bonus survived. Lesson: write the asserting
test before declaring a pipeline correct.

### Fixes — five defects, each its own commit with a regression test + docs DoD
1. **`iron_axe` unknown weapon (#1, `c69347b`)** — no axe `WeaponData` existed but
   four Fighter units referenced `iron_axe`; `DataManager.get_weapon` push_error'd
   11,829×/pass. Added `data/weapons/iron_axe.tres` + manifest entry. New durable
   guard `test_unit_inventory_refs.gd` scans every UnitData under `data/maps` +
   `data/roster` for dangling weapon/item refs.
2. **Pair Up Swap no-op (#2, `4924dde`)** — `swap_roles()` only flipped registry
   role labels; `MapCursor._commit_swap_roles` never moved the units. Now mirrors
   `_on_pair_up_resolved` (new lead → on-map tile + visible, old lead → OFF_MAP_TILE
   + hidden). Strengthened the swap test to assert positions/visibility.
3. **Allied-Rout never ends + AI (1,1) beeline (#4, `8c2ff81`)** — two coupled bugs:
   (A) `TurnManager._eval_rout` used `get_living_units_of` (excludes paired supports)
   → added `GameState.get_all_living_units_of` (true liveness) and use it; (B) added
   an `OFF_MAP_TILE` backstop in `EnemyAI._living_hostiles_for_faction` so a desynced
   off-map unit can't drag enemies to the top-left corner. Guards in
   `test_turn_manager.gd` + `test_enemy_ai.gd`.
4. **New Game settings not persisted (#1.2, `2080b29`)** — rules committed only on
   Start. Added `NewGameScreen._persist_rules`, wired to each toggle's `item_selected`
   (Start now shares it). Map/roster stay Start-only. Guard in `test_new_game_screen.gd`.
5. **Promotion modal off-screen (#5 = 8.7/8.11, `e7afb51`)** — left-pinned fixed-offset
   panel + long unwrapped stat rows. Centered via anchors with symmetric grow +
   `autowrap_mode` on option buttons. Guard in `test_promotion_screen.gd` (asserts
   on-screen + centered). Headless geometry only — a real-build visual confirm is
   still worth doing.

### Follow-up — Defect 3 (pair-up bonuses) was a real bug, now fixed
Writing the asserting unit test (`test_combat.gd`: paired lead's `preview_combat`
damage must rise by the support's strength contribution) **failed** at first — proving
the bonus never reached combat. Root cause: `CombatResolver._apply_pair_up_bonuses`
stamped every stat under one shared modifier source, and `add_modifier` removes all
modifiers sharing a source before adding, so each stat wiped the previous one and only
the last (luck) survived. Fixed with a distinct source per stat
(`pair_up:<support_id>:<stat>`) — the same pattern SkillHandler's Resolve already used.
Committed `b53a385` with the regression test and the GDD_05/GDD_10 doc updates.

### Lower-severity observations — all three closed
- **2.4 combat-preview overlap (`4c15aa0`)** — `AttackPreview._reposition_for` now
  nudges the panel clear of the visible HUD panels' screen rects via a pure, unit-tested
  helper `_place_clear_of`; degrades to the plain viewport clamp when the HUD isn't
  reachable.
- **8.3 Battle Speed not shown (`517b98a`)** — `preview_combat` returns
  `attacker/defender_battle_speed` + `follow_up_threshold`; the Damage field's More Info
  shows both sides' Battle Speed, the +5 threshold, and who doubles.
- **8.6 Soldier reclass grants no skill (`f51e8db`)** — resolved as **not a bug**:
  per your decision, the placeholder Soldier intentionally authors no `skill_unlocks`.
  Documented in GDD_03; the reclass code is correct.

**Every v0.1.4 round item (6 defects + 3 observations) is now resolved.**

### Pre-ship hardening (asked: "any more problems before shipping back?")
First scanned the two highest-risk areas for **sibling bugs** of the fixes — both clean:
no other objective evaluator misused the support-excluding liveness query (only
`_eval_rout` did), and no other `add_modifier` caller reused one source across stats
(Resolve already uses distinct sources). Then implemented three pre-ship items:
- **Skill-stub warning dedupe (`a4f44c4`)** — `_warn_stub_once` warns once per skill id
  instead of every combat; cuts the godot.log flood (was 107 warnings).
- **Pair Up bonus indicator (`913d39e`)** — the unit-info panel now shows
  `Paired  +N Str …` for a paired lead (queried on demand, since the bonus is
  combat-only). Closes the 8.5 "no change post-pairup" confusion.
- **v0.1.5 re-test addendum (`b51e8fe`)** —
  `AGENT/Docs/playtest_v0.1.5_retest_addendum.md`: changelog + re-verify table +
  2 new checks for the next build (build SHA TBD until export).

### Code review + v0.1.5.0 release prep
- **Code review (`fd6017d`)** of all changes since the playtest came back
  (`AGENT/Code Reviews/code_review_2026-06-14.md`): 8.5/10, no critical/high. Applied
  action items **#1** (center `ReclassScreen.tscn` — same un-centered layout class as
  the promotion bug) and **#2** (Swap resolves partner before flipping roles) in
  `92a171b`. Items **#3–#5** explicitly **deferred to next session** (objective-liveness
  helper/lint, AttackPreview HUD decoupling, ScrollContainer/fixpoint — all Low, none
  blocking).
- **v0.1.5.0 release prep:** bumped the version everywhere (export preset, MainMenu
  label, env setup — `test_release_metadata`-enforced), wrote the full
  `playtest_checklist_v0.1.5.0.md` handbook (re-verify summary, updated 8.3/8.5/8.6,
  new §12 features+roadmap), and **exported the Windows debug build** from this
  container: `builds/Project_Prometheus_v0.1.5.0_debug.exe`, 101,209,032 bytes,
  SHA-256 `d91ca65f…c5c5`. Manifest: `AGENT/Docs/playtest_build_v0.1.5.0.md`. The
  v0.1.5 addendum is superseded by the handbook. Build verified headlessly (version
  strings, no source-content leak, clean embedded-pack boot).

## Commits made this session
- `09e47fb` triage / evidence archive / roadmap tracking
- `c69347b` Defect 1 (iron_axe)
- `4924dde` Defect 2 (Swap)
- `8c2ff81` Defect 4 (rout liveness + AI off-map guard)
- `2080b29` Defect 6 (New Game persistence)
- `e7afb51` Defect 5 (promotion modal)
- `b53a385` Defect 3 (pair-up bonus source collision) + preview-delta test
- `4c15aa0` Obs 2.4 (combat-preview HUD avoidance)
- `517b98a` Obs 8.3 (Battle Speed in preview)
- `f51e8db` Obs 8.6 (Soldier no-skill decision, docs only)
- `a4f44c4` Pre-ship: skill-stub warning dedupe
- `913d39e` Pre-ship: Pair Up bonus indicator on unit-info
- `b51e8fe` Pre-ship: v0.1.5 re-test addendum (docs only)
- `fd6017d` Code review doc (+ `92a171b` review follow-ups #1/#2)
- `5ccb508` Bump to v0.1.5.0 (version strings + handbook) — build source commit
- (this commit) v0.1.5.0 build manifest + handbook SHA fill + addendum supersede

## Tests / gates
- Full GDScript suite green after every code commit (pre-commit hook ran it).
- `check_docs.py` green; PL#8 honored on each change (GDD section + GDD_10 status
  flipped, `Last verified` bumped on GDD_02/03/04/05/06/07/08/10).
- `test_release_metadata` green: version label, export preset, handbook, and env
  setup all agree on `v0.1.5.0`.

## Plans for next session
- **Visually confirm the v0.1.5.0 build on real Windows** (no Wine here): version
  label, promotion modal (#5), Swap (#2), preview placement (#2.4), and the new Battle
  Speed / Pair Up readouts were verified headlessly only. Distribute the `.exe` +
  `playtest_checklist_v0.1.5.0.md` together once eyeballed.
- **Code-review follow-ups #3–#5** (deferred): objective-liveness helper/lint;
  AttackPreview HUD decoupling via `setup()`; ScrollContainer/fixpoint avoidance.
- Optional pre-1.0 tidy: export from a clean `.godot` cache so the embedded uid/class
  cache doesn't carry test-script path references (harmless today; noted in the manifest).
