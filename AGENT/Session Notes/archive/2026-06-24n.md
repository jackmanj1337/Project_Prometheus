> **Superseded** by [A1 design arc (consolidated)](../2026-06-25d.md) — kept as the historical per-session record.

# Session Note — 2026-06-24n  (A1: combined-preview UX `[STY-10]` resolved → **A1 DESIGN COMPLETE**; player+authoring design doc)

**Topic:** Walked **how players use** the source+style model and **how designers build** it; resolved the
last open A1-design item `[STY-10]` (combined-preview UX). `[STY]` = STY-1..17, all resolved bar STY-11
(battalion entity → A2). Branch `docs-reorg-2026-06-23`. (Continues 2026-06-24m.)

---

## Grounding (code-verified)
- Live `ActionMenu` = Attack / Staff / Item / Equip / Wait …; `WeaponMenu` for source select.
- Live `AttackPreview` = a **two-sided 1v1 strike forecast** (Attacker/Defender boxes: `Dmg×hits /
  Hit% / Crit% / Effective`, each a clickable More-Info link). This is what STY-10 generalizes.

## `[STY-10]` Combined-preview UX — RESOLVED (designer forks)
- **One generalized "effect forecast" panel** — evolve `AttackPreview` into a single component:
  **1v1 `strike` = today's two-box view (back-compat); utility/no-counter = single-sided readout;
  AoE = footprint + focused-target.** One code path, More-Info retained.
- **AoE/multi-target = footprint highlight + focused-target forecast you CYCLE** through affected units
  (extends today's single forecast rather than replacing it).
- Panel always lists **every `[STY-16]` effect** with its gate ("+ Poison (3t) on hit", self-heal) +
  the **total multi-resource combo cost** (`[STY-5]`); non-strike effects show their own readout.

## Player flow + authoring surface (new design doc)
`design/source_style_player_and_authoring_2026-06-24.md` (illustration/navigation, NOT a second spec):
- **Player:** ActionMenu framing (Attack vs Staff/Use) → source (CEX-20 union via WeaponMenu) → style
  (skipped when only plain) → target (single or AoE footprint) → effect-forecast → confirm. Vanilla
  attack stays a single confirm; Attack stays distinct from Equip.
- **Designer:** authoring table (art/gambit/capture = a `StyleDef`; poison/Nosferatu = source `effects[]`;
  staves = source effects; relationships = `FactionData`; battalion entity = A2). Illustrative `StyleDef`
  + `EffectSpec` shapes; `DataManager.validate` warnings (bad weapon_type_gate, unknown condition,
  AoE w/o target_filter, bad rectangle dims, non_lethal w/o strike).

## Files
- `[STY]` register: STY-10 RESOLVED; status line ("A1 design complete; only STY-11 open"); resolved-in.
- **New** `design/source_style_player_and_authoring_2026-06-24.md`.
- Atlas: A1 bullet → **"A1 DESIGN COMPLETE 2026-06-24n"**; exit checklist relabeled **A1 BUILD checklist**
  (CEX-22 revisit · STY pipeline · AoE+preview · full F5 · faction matrix).
- Indexes regenerated (new doc indexed); `check_docs` green.

## Status
**A1 design is complete.** Remaining A1 work is the **BUILD** (the 5-item checklist above) + STY-11 is A2.

## Next session
Start the **A1 build** (or pivot to **A2 design** — battalion entity `[STY-11]` + capture carry + movement
assists). Then A4, A5 → **F1 schema-lock** (Phase B).
