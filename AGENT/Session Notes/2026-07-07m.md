# Session Note 2026-07-07m - v0.3.0 Pillar 1 code review

## What was done

Picked up the interrupted v0.3.0 code review from the 2026-07-07l handoff and
`AGENT/Code Reviews/code_review_prep_2026-07-07.md`. Reviewed the non-test
`scripts/**/*.gd` surface changed since the previous review snapshot `914dd025`,
with emphasis on save/suspend, RNG determinism, B6 input, `CampaignRules`, and the
previous spawn-seam findings.

Wrote `AGENT/Code Reviews/code_review_2026-07-07.md` using the Pillar 1 procedure.
Score: **7/10**.

## Findings

- **High:** fresh maps never call `RngService.start_map()` in production, so map RNG
  can start with seed/history defaults or inherit prior-map history.
- **Medium:** changing Settings -> Input Mode writes the setting but does not refresh
  `InputModeManager` immediately, so prompts can lag until another input/device event.
- **Medium:** the rebind UI omits shipped gameplay actions: `more_info`, `peek_range`,
  `zoom_reset`, `zoom_in`, and `zoom_out`.
- **Medium, carried forward:** objective types, AI profiles, and item effects still
  use closed author-facing dispatch lists instead of open registries.

Confirmed the two July 5 spawn-seam findings are fixed: inline placements preserve
authored `ai_profile` unless explicitly overridden, and runtime placement resolution
now matches validation by requiring exactly one unit source.

## Commits

- `a0c1efc` - Add v0.3.0 code review

## Verification

The review commit's pre-commit hook passed:

- `check_docs.py`
- `check_rng_usage.sh`
- Godot suite skipped by hook as docs-only

No code changes or gameplay tests were run in this session beyond the hook checks.
The existing dirty draft `AGENT/Docs/playtests/playtest_checklist_v0.3.0.md` was
left untouched and unstaged.

## Next

Fix the review findings in priority order before cutting v0.3.0:

1. Wire fresh-map startup to `RngService.start_map()` while preserving suspend resume
   restore behavior, with scene-level regression coverage.
2. Add an immediate Settings -> Input Mode refresh path into `InputModeManager` and
   test the no-extra-input case.
3. Add the missing player-facing actions to the rebind UI or create an explicit
   rebindable-action registry/exemption test.
4. After fixes, expand `playtest_checklist_v0.3.0.md` and cut the build.
