# Session Note - 2026-07-09b

## Summary

Continued the v0.3.0 recovery fix stream on `v0.3.0-features`. Read the latest
session notes first, then landed the remaining MRD-7 overlay work: unconditional
selection/targeting compose plumbing, shared-cell prototype render modes, and a
debug-only F8 cycle for the focused live comparison.

## What Changed

- Added focused rerun diagnostics for the remaining live-only holdouts:
  `V030-NG-FOCUS` in `NewGameScreen` and `V030-DSP-TRACE` in
  `SettingsManager`, plus the focused rerun checklist.
- Routed `MapCursorSelection` and `MapCursorTargeting` through overlay layer
  specs so `MapCursor` can compose movement/target overlays with retained
  threat/watch overlays instead of clearing them.
- Added MRD-7 shared-cell prototype modes:
  - `single_layer` keeps the existing default.
  - `border_through` uses combined threat-center/range-border overlay sources.
  - `stacked` paints threat on the base overlay and range on a second
    `TileMapLayer`.
- Added six generated shared-cell overlay PNGs and updated
  `overlay_tileset.tres`.
- Added debug-only F8 cycling for the live comparison, with a log line for the
  active mode.
- Updated GDD, roadmap, control-plane, gamepad-plan, triage, and focused-rerun
  docs to reflect the MRD-7 status and live pick step.

## Commits

- `2165959` - Add v0.3.0 focused rerun diagnostics.
- `bf22d61` - Compose MRD overlays during selection.
- `ad744aa` - Prototype MRD shared-cell overlays.

## Verification

- Pre-commit on `bf22d61`: docs, RNG guard, and full 67-suite run PASS.
- Pre-commit on `ad744aa`: docs, RNG guard, and full 67-suite run PASS.
- Focused before commit:
  `test_map_cursor.gd`, `test_grid_manager.gd`, `test_mrd_scene.gd`,
  `test_input_bindings.gd`, `test_settings_screen.gd`, and
  `test_game_map_scene.gd` all PASS.

## Next

1. Cut and run the focused live rerun: controller Parts I-II, suspend section 9,
   display section 1.6, and MRD-7 F8 shared-cell comparison.
2. Use the returned `V030-NG-FOCUS` logs to diagnose the New Game focus holdout;
   do not guess from headless results alone.
3. Use `V030-DSP-TRACE` logs for the one-axis drag readout residue and verify
   relaunch persistence.
4. After the MRD-7 live pick, make the chosen shared-cell presentation the
   shipped mode and remove the temporary F8 cycle.
