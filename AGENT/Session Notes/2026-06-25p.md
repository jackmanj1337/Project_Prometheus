# Session Note — 2026-06-25p  (A4 COMPLETE — provoke `[PRV-1..7]` + capture-victory `[VIL-9]`)

**Topic:** Finished the **A4 walk** (story / event-driven map content). Firmed the **provoke /
runtime faction-relationship MET action** `[PRV-1..7]` (the `[STY-17]` transition side) and closed the
**Capture-victory pin** with the win-condition objective type `[VIL-9]`. **A4 design is now complete**
(all four sub-clusters firmed across 2026-06-25n/o/p). Branch `docs-reorg-2026-06-23`.

**Outcome:** new register `[PRV-1..7]` RESOLVED; `[VIL]` extended to `[VIL-9]`; atlas A4 bullet marked
**DESIGN ✅ COMPLETE** + F1 reserve line updated; INDEX/REGISTERS regen; **19 `check_docs` green**.
3 commits (`d6654f9`, `d8243ec`, + this note).

---

## 1. Provoke / runtime-relationship action `[PRV-1..7]`
**Method:** end-shape via questions first. Owner calls: granularity = **faction-pair + per-unit
override**; persistence = **map-local default + campaign option** (mirrors F6); reactive provoke =
**pin the hook here as a shared primitive**.

**Verdict — one transition primitive, two callers.** `[STY-17]` (A1) owns the directed 3-state matrix
+ resolver + authored data; **PRV owns the runtime override store + a single
`set_relationship(from,to,stance,scope)` primitive.** The **`set_relationship` MET action** (scripted)
and a reactive **`provoke_on_attacked`** stance flag (combat-reactive) are its two callers — not two
systems. Items:
- **PRV-1** one primitive over the STY-17-reserved store.
- **PRV-2** **two-layer** granularity: a **per-unit** override layer + a **faction-pair** edge layer,
  checked before the STY-17 authored matrix / alliance default. Resolver gains a unit-aware entry
  `relationship_for(actor_unit, target)` over STY-17's faction-keyed `relationship()` (extends, not
  relitigates). Lets one attacked villager turn hostile while its faction stays neutral.
- **PRV-3** `set_relationship` MET action — directed by default (`mutual` convenience), faction-pair or
  unit target, trigger-agnostic, joins the vocab beside `set_ai`.
- **PRV-4** reactive `provoke_on_attacked` flag — per-unit vs whole-faction provoke authorable; **AI
  initiate/target behavior firms with the AI pass** (hook pinned here).
- **PRV-5** full 3-state set (provoke→hostile / make-peace→neutral / ally-mid-battle→allied);
  **counter-regardless-of-stance** kept (STY-17 lean; stance only gates *initiation*).
- **PRV-6** persistence = map default + campaign option; **F1 reserves the store with both scopes ×
  both layers** (firms STY-17's "reserve a runtime override store").
- **PRV-7** AI reads `relationship()` fresh per activation — mid-map flips picked up next activation,
  like `set_ai`; no new machinery.
- **Reconciliation:** provoke (relationship **stance**) is **distinct** from recruit (`[RCR-1]` **team**
  flip) — different store, different save surface.

## 2. Capture-victory objective type `[VIL-9]`
The `[DSP]` Capture-victory pin had two halves: removal-disposition (firmed `[VIL-8]` last session) +
**win-by-capture** (this). **No fork** — a new **`ObjectiveCondition.type=capture`** over `unit_ids`,
satisfied when each named target is in the captured state (its `captured:<id>` flag, set by the **A2**
capture-carry / `[STY-6]` non-lethal mechanic). Mirrors `seize`/`defeat_boss`; reuses the `unit_ids`
watcher; no new evaluation machinery. *How* a unit becomes captured stays A2; this only reads the flag.

## 3. Commits
- `d6654f9` — `[PRV-1..7]` provoke/runtime-relationship action (register + atlas A4 bullet + F1 reserve
  two-layer/two-scope + A4 progress note).
- `d8243ec` — `[VIL-9]` capture-victory objective type (closes the `[DSP]` pin; VIL-1..8 → VIL-1..9).
- (this) — session note + INDEX + atlas A4 "DESIGN ✅ COMPLETE" marker.

## 4. A4 SCORECARD (complete)
| Thread | Register | Session |
|---|---|---|
| Village #11 (+ interactive-trigger keystone `[VIL-2]`) | `[VIL-1..8]` | 2026-06-25n |
| Recruit conversation side (+ dialogue foundation **F15**) | `[RCV-1..6]` | 2026-06-25o |
| Provoke / runtime relationship | `[PRV-1..7]` | 2026-06-25p |
| Capture-victory objective type | `[VIL-9]` | 2026-06-25p |

**A4 net-new shared substrates surfaced:** the **`[VIL-2]` interactive-trigger** (Visit/Talk/Shop/
Activate), **foundation F15 dialogue/conversation** (rough end-shape), the **objective removal-
disposition rule `[VIL-8]`**, and the **two-layer/two-scope relationship-override store `[PRV-6]`**.

## 5. NEXT SESSION — **A5** (last sync-cluster before F1)
**A5 — Campaign meta-rules & EXP/economy** (shared: F4 CampaignRules, hub/PHB, EXP economy, death/
permadeath). Threads: Difficulty + Casual/Phoenix (#12) · Bonus-EXP (#18) · Arena (#14) · PvP (#7) ·
the style/source **loadout cap** (`[CEX-7]`/`[STY-3]`) · the **death/removal-disposition path** — now
co-owned by **`[VIL-8]`** (objective side) + **`[BAT-16]`** (battalion host-death) + the
**death-inventory rule set** (single `handle_death` hook) · the `[AGT §6]` non-combat-action EXP pin ·
battalion-EXP pricing `[BAT-6]`. **Method:** end-shape-first (memory `feature_walk_end_shape_first`).

**Then F1 schema-lock.** The **define-all sweep before F1 owes**: F14 `[STM]` stat model · **F15
dialogue v1 schema** · `[BAT-14..16]` battalion content/lifecycle. **No builds until F1 is planned.**
