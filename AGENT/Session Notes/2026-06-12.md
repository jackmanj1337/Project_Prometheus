# Session Notes - 2026-06-12

## Focus

Implement the decisions from the GDD/codebase alignment review, synchronize the
live GDD and roadmap with the resulting rules, commit the work, and push it.

## What was done

- Corrected `WHOLE_PHASE` round counting so `turn_number` advances only when
  the full authored faction cycle wraps back to blue.
- Removed the superseded Seize `allowed_unit_ids` path. `UnitData.can_seize` is
  now the sole unit eligibility rule.
- Removed implicit group Rout elimination. Rout defeats must be authored
  explicitly; map 001 and validation maps now author allied Rout where required.
- Added shared `Faction - Controller` phase labels for the HUD and phase banner.
- Changed the default equipped-skill cap from 4 to 5.
- Changed the follow-up threshold from 4 to 5 Battle Speed.
- Changed all current class WEXP caps to A rank and made WEXP gain stop at the
  active class's authored cap. The global S-rank threshold remains available
  for future explicit S-cap classes.
- Added and updated regression tests for round timing, explicit Rout, Seize,
  labels, skill defaults, follow-up boundaries, and class WEXP caps.
- Added a dated decision-log addendum for all settled and deferred review items.
- Updated GDD authority, progression, maps/objectives, UI, AI, skills, settings,
  resource fields, roadmap status, and historical checklist references.
- Marked tactical target scoring as a separate deferred AI task.
- Recorded flying movement as a future terrain movement-cost category system.
- Deferred the hotseat CLI/dev controller override.
- Added an Awakening corpus authority notice and project adoption matrix.
- Replaced corpus sandbox links with repository-relative links.
- Renamed `New_Contet_expansion` to `New_Content_Expansion` and
  `Old-deffered` to `Old_Deferred`, updating repository references.
- Left the four unrelated untracked combat-preview screenshot/import files
  unchanged.

## Tests run

- Focused TurnManager, data-layer, combat, GameState, HUD, and unit-stat suites.
- `bash run_tests.sh`
  - PASS: all 36 suites green.
- Code commit pre-hook:
  - PASS: all 36 suites green.
- Documentation commit pre-hook:
  - PASS: all 36 suites green.
- `git diff --check` and `git diff --cached --check`:
  - PASS.

## Commits made

- `fef8da0` - `Align gameplay rules with settled GDD decisions`
- `76f24bc` - `Synchronize GDD with settled project rules`

## Plans for next session

1. Treat tactical AI scoring as its own task.
2. Keep flying movement deferred until terrain movement-cost categories are
   designed and implemented.
3. Add campaign-owned overrides for skill cap and follow-up threshold when the
   campaign settings/save contract is built.
4. Run the remaining manual hotseat acceptance checklist when live validation
   is scheduled.
