# Session Note — 2026-06-22e

**Topic:** Planning-only. **Walked AND resolved the second AI cluster — Group B `[AIP-11..16]`.**
With Group A already closed (first-build spec), this finishes the AIP register: **the AI cluster is
now FULLY design-complete** (first build + full vision). Nothing AI-side is planning-blocked. Docs-
only; check_docs **17/17**; no `__pycache__`.

---

## 1. Six Group B resolutions (all owner-decided this session)
- **[AIP-11] Difficulty bands = NUMBERS ONLY.** A band may scale **stats** and change **roster
  size** (extra units / extra-or-earlier reinforcements) **only** — **no AI-option swaps at all**
  (not activation, not disposition, **not the engagement tier**). Simpler than the vision §3 rec
  (which allowed engagement-tier swaps). **Owner override.**
- **[AIP-12] Action-preview gating = ALL THREE, layered/OR.** Per-chapter author flag · per-band
  marker · player accessibility toggle (accessibility wins over chapter/band defaults). Preview
  stays **non-binding** ("may change" UX).
- **[AIP-13] Disposition = CHARACTER-SHEET item + More Info page**, **not** an always-on map glyph
  in v1. Reuses the existing `I`-sheet + More Info pattern. **Adjusts a vision §1 pillar:** the
  on-map intent telegraph now rests on the danger-zone (built) + threat-range (designed, TUR)
  overlays; *disposition* is an inspection lookup, not a default map badge.
- **[AIP-14] Combat-AI workstream = DEFER to its own later milestone.** Engagement stays the v1
  "attack best target" seam through the first AI build + a playable campaign. A smarter tier (incl.
  ML Option A) is gated **per-chapter/global, NOT a difficulty band** (forced by [AIP-11]). The
  "engagement-tier ↔ band coupling" thread is **dropped**.
- **[AIP-15] `unit_hp_below` MET trigger = DEFER** to a later MET extension. Bosses/summoners use
  `turn_reached`→`spawn`/`set_ai` meanwhile. (Closes the last open MET-growth piece.)
- **[AIP-16] Enemy/allied Pair-Up & Rescue = DEFER; player-only in v1.** Revisit post-playable-
  campaign.

## 2. Two cross-cutting consequences (recorded everywhere)
1. **[AIP-11] kills the engagement-tier↔band coupling** in [AIP-14] and re-gates the ML Option-A
   "smarter tier" (vision §6) → per-chapter author opt-in / global setting, never a band.
2. **[AIP-13] reframes the vision §1 "disposition visible by default" pillar** → "disposition
   *inspectable* on the sheet"; on-map telegraph = the danger/threat overlays only.

## 3. Edits
- `ai_profiles_open_questions_2026-06-21.md` §10 — Group B flipped **COMPLETE 2026-06-22e**, all six
  resolved with text + consequences; added a "CLUSTER COMPLETE" banner to the §10 intro.
- `ai_system_design_vision_2026-06-22.md` — §1 telegraph pillar reframed (AIP-13) + action-preview
  gating resolved (AIP-12); §3 difficulty model = numbers-only (AIP-11); §5 open sub-decisions all
  flipped RESOLVED; §6 ML-tier gating note (per-chapter/global, not a band).
- `GDD_10_Roadmap.md` §H — new "Group B RESOLVED 2026-06-22e — AI cluster FULLY design-complete"
  block; ML §6 line re-gated.
- **DoD#1:** planning only (no behavior change) — `GDD_08` updates at build time. **DoD#2:** no
  mechanical/checkable rule ratified (these are scoping/gating/UX-placement design calls; the
  preset/policy value-set `check_docs` guards still land with the code at build).

## 4. Verification
check_docs.py **17/17 PASS**; docs-only; no `__pycache__`.

## Session status: OPEN (pending commit/push).
**The AIP register is fully resolved** (`[AIP-1..16]` + `[AIP-A11/A12]`); the AI cluster is design-
complete for both the first build and the full vision. **Eight planning registers now resolved**
(PKGA · DCH · DTR · MET · FOW · AIP). **Next session:** return to the remaining open planning items
— **STW** (inherits DCH's `map_objects`), **IMP** (sprite importer), **DMR** (DataManager refactor),
**BWN** (broken-weapon), **MRD** (map-readability), **REN/LEG** (release gates, owner-input-blocked).
Build track unchanged: **Package A Step 1 first**.
