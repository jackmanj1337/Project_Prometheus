# Session Note — 2026-06-24b  (Firm the author-flexible weapon triangle — `[CEX-9..12, 17]`)

**Topic:** Continued the Wave-1 player-facing firmings (atlas §3b). Firmed the **author-flexible
weapon triangle** (`[CEX]` cluster C) + **reaver-style reversal**. Docs-only; check_docs **19/19**,
working tree clean. Branch `docs-reorg-2026-06-23`.

---

## Grounding (code + GDD)
- `GameConstants.WEAPON_TRIANGLE` is **already an arbitrary directed matrix** (`dark` beats all three
  anima) — so the "arbitrary graph" shape is the current storage, not a new idea.
- Magnitude is **hardcoded flat** (`CombatResolver`: +10/−10 Hit, +2/−2 Atk), rank-independent.
- **GDD_04 already specifies a rank-scaled target table** (SET-003/RULE-013), explicitly "Not yet
  implemented." Families are a fixed `VALID_COMBAT_FAMILIES` list validated by DataManager.

## What we firmed — the F4 `triangle` profile
Lift the triangle into a **`CampaignRules` `triangle` profile** (the F4 author-profile mechanism):
- **`families`** — author-extensible; becomes the `triangle_family` validation source (replaces the
  fixed `VALID_COMBAT_FAMILIES`); no matrix row = neutral (bows/knives/staves unchanged).
- **`matrix`** — arbitrary directed graph (`[CEX-11]`); "multiple triangles" = disjoint subgraphs.
- **`effects`** — **arbitrary stat-mods** for advantage/disadvantage (`[CEX-10]`; reuse the modifier
  path, no new engine); flat OR a **per-rank table** keyed by the wielder's trained WEXP rank.
- **`reaver_multiplier`** — see reaver below.

**Owner decisions:**
- **`[CEX-10]` effect vocabulary → arbitrary stat-mods v1**; **condition application deferred to the
  F5 (`ConditionManager`) build** (rides the same `effects` slot when F5 lands).
- **`[CEX-12]` scaling → flat default (non-breaking) + opt-in built-in `rank_scaled` profile** (the
  GDD_04 SET-003/RULE-013 table). Honors F4's non-breaking rule *and* the GDD_04 target without
  changing current combat math until a campaign opts in.
- **`[CEX-9]` readout (rec)** — combat preview shows net triangle stat deltas + an
  advantage/disadvantage/**reversed** indicator; more-info shows the matchup; data-driven so custom
  stats render generically.
- **`[CEX-11]` shape (rec)** — keep the arbitrary matrix (already the storage).
- **`[CEX-17]` reaver — FIRMED (owner asked to firm now):** a per-weapon
  **`weapon_component.reverses_triangle: bool`**. Let **R** = equipped reaver weapons across the two
  combatants (0–2): **R odd → invert the result (adv↔disadv; neutral stays neutral) AND
  ×`reaver_multiplier` (default 2)**; **R even (0/2) → normal** (two reavers cancel, GBA-style). A
  reaver only matters where a relationship already exists.

## Reconciled docs
Register `candidate_systems_open_questions_2026-06-23.md`: block C resolved in place (`[CEX-9..12]`
`[RESOLVED]` + new **`[CEX-17]`**; frontmatter `CEX-1..16`→`CEX-1..17`). Candidate-systems design doc
block C → RESOLVED banner. Atlas Cluster-B row → firmed. **GDD_02 §Weapon Triangle** + **GDD_04
§Weapon Families & Triangle** → "Design firmed" notes (profile-driven, flat default + opt-in
rank-scaled, stat-mod effects, reaver); Last-verified bumped to 2026-06-24. No status flip (build
pending, behavior unchanged).

## Build hand-off (when scheduled)
- **Net-new code:** the F4 `triangle` profile type + resolver; `effects` application (reuse modifier
  path); the rank-scaled lookup; `weapon_component.reverses_triangle` + the parity branch in
  `_get_triangle_result`/magnitude; DataManager family validation reads the profile `families`;
  data-driven preview/more-info readout.
- **Reserve (IEQ):** `reverses_triangle` on `weapon_component`. **Conditions slice:** waits on F5.

## Commits
1. `…` (this commit) — firm `[CEX-9..12, 17]` flexible weapon triangle + reaver; reconcile GDD_02/04,
   atlas, candidate-systems design doc.

## Verification
`check_docs.py` **19/19 PASS**; RNG guard PASS; manifests regenerated; no new `__pycache__`.

## Next — continue Wave 1
**Per-map-use items `[CEX-13]`** (small, dep IEQ✓) is the natural next; then learned spells
`[CEX-5..8]` (after F7✓, XL) and story items `[CEX-14..16]`. Wave-0 **F1 save-schema lock** still
pending before any build. Session task list tracks the queue.
