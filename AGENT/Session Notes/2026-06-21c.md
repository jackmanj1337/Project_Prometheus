# Session Note — 2026-06-21c

**Topic:** Planning-only. **Started the TECHNICAL side of the §2 Campaign/Save cluster.** Read the
code first, drafted the technical implementation plan + a code-facing decisions register, then
walked the register one-by-one with the user and **resolved all of [CST-1..12]** (one follow-on
[CST-13] deferred to execution kickoff). Docs-only; check_docs 17/17; no `__pycache__`.

## Method
Per the standing note, read the actual code before drafting: `GameState.gd` (roster/gold/items in
memory only; `_snapshot_unit_data` stores Vector2i + live `InventoryEntry` Resources = NOT
JSON-safe; `_map_start_snapshot` is the in-memory Retry path), `CampaignRules.gd` (unwired stub),
`NewGameScreen.gd` (single-map launch + 4 rule toggles), `GameOverScreen.gd` (Retry/Quit, no
next-map), `GameMap.gd` launch path (`_spawn_units` respawns enemies FRESH), `PairUpRegistry`
(`serialize`/`restore` reusable), `MapData.gd` + `map_registry.json`, `InventoryEntry`/`UnitData`
field sets, `TurnManager.gd` (the **`_unit_states {Unit: READY/DONE}`** acted-tracking +
`_active_faction_idx`/`_turn_order`/`_activation_mode` activation cursor — the key suspend finding),
and `SettingsManager` (ConfigFile autoload-owns-disk pattern).

## Deliverables (2 new docs)
- **`AGENT/Docs/campaign_save_technical_plan_2026-06-21.md`** — draft architecture (SaveManager
  autoload + SaveData seam + SaveCodec, CampaignData graph, prep/results/defeat screens), JSON
  save schema shape, flows, the suspend design, and **slice 0 (Package A) + slices 1–7**.
- **`AGENT/Docs/campaign_save_open_decisions_2026-06-21.md`** — register `[CST-1..13]`, dependency-
  ordered, each option/rec/resolution, walked with the user (mirrors the §1 `input_controls_open_decisions` pattern).

## Resolved decisions (walked one-by-one, 4 batches)
- **Batch 1 (foundational):** [CST-1] **A** new `SaveManager` autoload + I/O-free `SaveData` seam.
  [CST-2] **B** (user override) ONE JSON-safe serializer for BOTH Retry and save → `InventoryEntry`
  lossless round-trip is now a hard test obligation. [CST-3] **A+JSON** new `CampaignData` graph
  referencing map ids. [CST-4] **B** (user override) hard-migrate all rule call sites to
  `gs.campaign_rules.*` (~70 refs; `pair_up_enabled` 26/15 files) + delete loose fields.
- **Batch 2 (flow):** [CST-5] **B** deployment constraints (required/excluded/cap) all on the
  CampaignData node; `player_start_tiles` stay on MapData. [CST-6] **"everything is a campaign"** —
  every map auto-wraps as a 1-node campaign (one code path, no debug launcher); selector defaults
  to last-started/most-recently-imported; **NEW: each CampaignData carries rule MANDATES (locked)
  or DEFAULTS (editable)** (expands [CST-4]/G2). [CST-7] **A** split MapResultsScreen (victory) /
  GameOverScreen (defeat menu); **preserve standings renderer for a future PvP scenario mode**.
- **Batch 3 (save model):** [CST-8] **between committed actions, any human-controlled faction**
  (upgraded from phase-start after the code check — delta is only `_unit_states` + activation
  cursor; **F9 hotseat ⇒ free mid-enemy-phase suspend**; gate = idle FREE + human-driven; still
  excludes mid-move/anim/attack-resolution + AI phases). [CST-9] **A + author-designatable
  protected fields** (two SHA-256 hashes; `CampaignData.protected_fields` on a mandatory baseline).
  [CST-10] **A** `user://saves/` manual slots + dedicated autosave + suspend + `saves_index.json`
  last-played pointer.
- **Batch 4 (forward seams):** [CST-11] **A** story-flip seam + `campaign_rule_flipped`
  notification only (trigger deferred). [CST-12] **C** (user override) **Package A (`RngService`)
  built FIRST, then §2** → re-sequences execution; makes suspend `rng` fields real; unblocks the
  rewind mechanic → surfaced **[CST-13]** (fold Turnwheel into §2 vs hooks-only+next; rec hooks-
  only; deferred to §2 execution kickoff).

## Scope expansions captured (user-driven)
- Campaign-owned rule mandates/defaults (CST-6) · author-designatable protected fields (CST-9) ·
  between-action human-controlled suspend incl. hotseat-enemy-phase (CST-8) · PvP/scenario mode
  reusing the preserved standings renderer (CST-7, → `planning_backlog §2b`).

## Backlog/sequencing updates
`planning_backlog_2026-06-20.md`: §2 header now points at the technical plan + register and flags
**Package A as the next execution step**; §2b rewind-mechanic sequencing question RESOLVED
(Package A first) + [CST-13] noted; **PvP/scenario mode** added as a NEW deferred item.

## Verification
check_docs.py **17/17 PASS**; docs-only; no `__pycache__`.

## Plans for next session
**Next EXECUTION step is Package A (`RngService`)**, NOT §2 slice 1 — per [CST-12 → C]. So the next
*planning* step (if continuing planning) is the **Package A implementation plan** from
`rng_determinism_design_2026-06-11.md`; the next *build* step is Package A itself. After Package A
lands, §2 slices 1–7 are build-ready (resolve [CST-13] at that kickoff). Alternatively, the §1
input/controls execution track or the v0.2.3 verify/Web track remain available.
</content>
