# Session Note 2026-07-08e — v0.3.0 full-scan Pass 5

## Goal

Continue the resumable v0.3.0 full code-pillar scan
(`AGENT/Code Reviews/v030_full_scan/`). Cadence is **one pass per invocation**;
`_TRACKER.md` is the resume anchor. The first TODO was **Pass 5 — Map/turn core**
(5 files), which carries the two biggest deferred items: the carried High
`RngService.start_map()` fix site, and the `MapCursor` arm-on-decode ordering
flagged in Pass 3.

## Progress

- Read `_TRACKER.md` + the Pass 4 note; working tree == head `b7bcfd2` for these
  5 files (confirmed `git diff b7bcfd2 HEAD` empty for them; HEAD was `35a1813`,
  docs only).
- Read at head: `GameMap.gd` (351), `MapCursorTargeting.gd` (351) in full;
  `MapCursor.gd` (1946), `TurnManager.gd` (1297), `GridManager.gd` (683) via full
  diffs (`+733`/`+208`/`+115`) + targeted region reads. Cross-read `RngService.gd`,
  `MapCursorInput.gd`, and the Pass-5 test suites.
- Wrote `AGENT/Code Reviews/v030_full_scan/05_map_turn_core.md`: **1 High (carried,
  re-CONFIRMED) + 4 Low (1 nit). No new correctness bugs.**
  - **H1 (carried High, re-CONFIRMED — fix site is here):** `GameMap._ready()` on
    the fresh (not-resuming) path snapshots at `:115` and calls
    `_turn_manager.start_map()` at `:127`, but **nothing calls
    `RngService.start_map()`** (grep-confirmed: no production caller; `TurnManager.start_map`
    doesn't call it either). So `map_seed` stays `0` (zero cross-session entropy) and
    `history_hash` bleeds across maps within a session; the Retry snapshot at `:115`
    captures stale RNG. Resume is unaffected (`_apply_suspend_resume` restores via
    `from_save_dict`). **Fix now fully localized:** seed on the fresh branch only,
    before `take_map_snapshot` (`GameMap.gd:114-115`), null-guarded for headless.
  - **L1:** dead `TurnManager._array_from_variant` (`:231`) — no in-file caller
    (SaveData has its own static copy). Delete.
  - **L2:** unused overlay `blend` metadata + `overlay_layer_blends()` accessor
    (`GridManager.gd:562-576`) — `repaint_overlays` uses precedence + tile opacity,
    never the flag; the doc comment promises an "exclusive opaque top layer" semantic
    that isn't enforced. Drop it, or wire `repaint_overlays` to honor it.
  - **L3 (nit):** `_on_promotion_item_cancelled` (`MapCursor.gd:1439`) doesn't clear
    `_pending_item_id`; benign today (every confirm path re-sets it first). Clear for
    symmetry.
  - **L4:** keyboard-held zoom now auto-repeats via `_poll_held_zoom`
    (`MapCursor.gd:1499`) because it polls action strength device-agnostically — a
    behavior change from discrete keyboard zoom (no double-step on the press frame;
    traced). Likely intended parity with the trigger; flagged to confirm.
- **Positives recorded:** (1) no raw `randi/randf/randomize` in any of the 5 files
  (determinism E clean); (2) **the Pass-3 arm-on-decode concern resolves CLEAN** —
  `_handle_discrete_press` arms `_held_dir` synchronously before the same-frame
  `_process` poll, so `poll_direction` sees `dir == _held_dir` and ticks the DELAY
  timer instead of stepping (no keyboard/d-pad edge + analog poll double-move); (3)
  `_original_tiles.erase()` on DONE / `_refresh_faction_units` / `end_alternating_activation`
  defuses the stale pre-move-tile desync in the RNG record path (Pass 2 positive
  re-confirmed via `get_action_start_tile`); (4) both quit paths dedup through
  `_return_to_main_menu`; (5) the overlay precedence registry is a proper open
  registry (matches AGENTS.md).
- Flipped tracker Pass 5 → DONE + Log line.
- Pre-commit green: `check_docs.py` all 26, RNG guard PASS, Godot skipped (docs-only).

## Next

- **Pass 6 — UI screens, selection & misc data (18 files):** `SelectionCursor`,
  `ModalScreen`, `UnitDetailsScreen`, `AttackPreview`, `HUD`, `LevelUpScreen`,
  `MenuScale`, `MainMenu`, `MapMenu`, `NewGameScreen`, `GameOverScreen`, `ActionMenu`,
  `DataManager`, `PairUpRegistry`, `MapData`, `Unit`, `generate_placeholder_assets`,
  `generate_tilesets`. Carries the Pass-6-scoped carried Medium (author-facing closed
  dispatch lists / registry debt → `DataManager`). No blocker carried past Pass 5 —
  H1 is the last determinism fix site and is now localized to `GameMap._ready()`.
- Then Pass 7 — general/integration & rollup (`code_review_v0.3.0_full_scan_*.md`,
  38/38 coverage assertion + updated score).

## Commits

- `b9463ee` — v0.3.0 full-scan Pass 5: map/turn core (1 High carried, 4 Low).
- Session note + INDEX row in the closeout commit after `b9463ee`.
