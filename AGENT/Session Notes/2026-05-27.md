# Session Notes — 2026-05-27

## Focus
Bring the numbered GDD back in sync with the current code so a new developer can
use the GDD as a reliable onboarding reference instead of stitching together
roadmap notes, architecture notes, and session history.

## What was done
- Audited the numbered GDD against the current runtime and recent session notes.
- Updated `AGENT/GDD/GDD_00_Overview.md` so the top-level scope section reflects
  the shipped baseline instead of the older MVP-only snapshot.
- Updated `AGENT/GDD/GDD_02_Core_Mechanics.md` to document:
  - faction-driven turn order
  - hotseat-aware controller flow
  - current WEXP storage
  - the currently shipped action set, including Pair Up / Swap / Separate and
    objective actions
- Updated `AGENT/GDD/GDD_05_Skills.md` to reflect the live handler surface:
  - current equipped-skill cap behavior
  - implemented M9a skills/helpers (`stat_bonus`, `prescience`, `patience`,
    `focus`, `discipline`, `healtouch`)
  - corrected trigger descriptions for `nihil`, `resolve`, and `wrath`
- Updated `AGENT/GDD/GDD_07_UI_UX.md` to document the live UI rather than the
  earlier MVP plan:
  - New Game map selector and rule setup
  - objective panel
  - faction-driven phase labels/banners
  - contextual action menu entries
  - More Info + Unit Details behavior
  - current attack preview behavior
  - Quit to Menu and results/standings flow
- Updated `AGENT/GDD/GDD_08_Enemy_AI.md` for the shipped per-faction AI dispatch
  and hostility model.
- Updated the `GDD_09_Checklist.md` status snapshot date so the file no longer
  points at an older refresh pass.
- Did a second onboarding-focused cleanup pass on the remaining weak spots:
  - rewrote `README.md` into a real project/onboarding entry point
  - added onboarding read order, launch-flow notes, export-safe manifest rules,
    and common gotchas to `AGENT/GDD/GDD_01_Architecture.md`
  - corrected stale class/progression assumptions in
    `AGENT/GDD/GDD_03_Units_Classes.md`, especially around mounted behavior,
    New Game setup, class-add flow, and the default roster framing
- Centralized the highest-friction cross-document workflows into evergreen docs:
  - added `AGENT/Docs/map_authoring_guide.md` for `MapData`, registry,
    roster-policy, export-manifest, and validation expectations
  - added `AGENT/Docs/testing_guide.md` for automated-vs-manual coverage,
    validation-map roles, and regression guidance
  - added `AGENT/Docs/campaign_rules.md` for New Game gameplay-rule ownership
    and current `GameState` rule fields
  - linked those new guides from `README.md`, `GDD_01_Architecture.md`,
    `GDD_06_Maps_Objectives.md`, and `GDD_Manual_Tasks.md` so developers have
    one clear entry point per workflow
- Ran a fresh code review across the current runtime and wrote
  `AGENT/Code Reviews/code_review_2026-05-27.md`.
- Main findings from that review:
  - Pair Up release/separate flows desync logical tile state from node position
  - empty or failed roster loads can silently fall back to the default roster
  - hotseat map-menu close/cancel paths still treat local non-blue control like
    AI control and can leave the cursor locked
  - `DataManager` still silently accepts duplicate content ids by overwriting
    earlier resources during load
- Implemented explicit roster launch tracking and removed the silent default-roster
  fallback from `GameMap`:
  - `GameState` now tracks whether the roster was explicitly prepared, whether
    roster prep failed, and which roster policy/source produced the active roster
  - roster loads are now atomic: invalid or missing roster content fails the
    load instead of partially populating `player_roster`
  - `NewGameScreen` now refuses to start the map when roster preparation fails
  - `GameMap` now requires `is_roster_ready_for_launch()` and fails loud when
    launch state and roster state do not match
  - updated headless tests that were previously relying on implicit roster
    bootstrap to construct their roster state explicitly
  - synced `README.md`, `GDD_01_Architecture.md`, and
    `AGENT/Docs/map_authoring_guide.md` with the new launch contract
- Implemented the next batch of review-driven bug fixes:
  - fixed Pair Up support restoration so real `Unit` nodes are snapped back onto
    the map visually and logically after lead death / separation paths
  - fixed hotseat local-control menu handling so non-blue hotseat factions now
    unlock correctly after map-menu close and Quit-to-Menu cancel
  - hardened `DataManager` catalogue loading to reject duplicate resource ids
    instead of silently overwriting earlier content
  - added targeted regression coverage for all three fixes in
    `test_pair_up_registry`, `test_map_cursor`, and `test_data_manager`

## Tests run
- `git commit` pre-commit hook
  - `PASS: all suites green`
- Documentation-only centralization pass
  - no code test run yet in the working tree; rely on the next commit hook run
- Fresh code review
  - no code execution beyond source inspection; findings documented only
- Explicit roster launch hardening
  - `bash run_tests.sh`
  - `PASS: all suites green`
- Review-driven bug fixes
  - `bash run_tests.sh`
  - `PASS: all suites green`

## Commits made this session
- `f6c67fd` — `Refresh GDD for current implemented systems`
- `4a80fab` — `Record GDD refresh session note`
- `749cc7e` — `Tighten onboarding documentation`
- `ab43a0e` — `Centralize onboarding workflow docs`
- `c901cab` — `Record workflow docs centralization`
- `5212f67` — `Add 2026-05-27 code review`
- `20d91dc` — `Record code review session note`

## Plans for next session
1. Do a second onboarding-focused doc pass on any remaining numbered chapters if
   new drift is found during active development.
2. Keep folding shipped roadmap work back into the numbered GDD quickly so
   `GDD_10*` stays planning-focused instead of becoming the only accurate source.
3. If more onboarding friction shows up, next likely targets are a contributor
   setup section, a short content-authoring guide for classes/skills/items, and
   further reduction of dated-note duplication.
4. If the team wants follow-up implementation work, the highest-value fixes are
   the Pair Up placement bug, roster bootstrap hardening, and hotseat menu
   unlock logic from `code_review_2026-05-27.md`.
5. Next likely code follow-up is the hotseat menu/control ownership fix, now
   that roster launch no longer relies on implicit fallback behavior.
6. Remaining likely follow-up from the 2026-05-27 review is broader content
   validation hardening beyond duplicate ids, especially more map-facing
   authoring checks.
