# Session Notes — 2026-05-26

## Focus
Audit the roadmap docs against the session notes, then promote `M9a` ahead of
`M8` and start the first skill-engine implementation slice.

## What was done
- Reviewed the recent session-note chain, especially the 2026-05-20 through
  2026-05-25 planning and implementation notes, against:
  - `AGENT/GDD/GDD_10_Roadmap.md`
  - `AGENT/GDD/GDD_10a_Overview.md`
- Updated `GDD_10_Roadmap.md` so the Phase 2 status snapshot now reflects the
  shipped state of `M14` and `M16`, the current validation-only state of
  `M15 Part A`, and the implementation-ready state of `M8` and `M9`.
- Updated the roadmap's M15 Part A section to replace the stale
  "implementation plan ready" wording with the current reality: core hotseat
  code landed on 2026-05-21; remaining work is validation content + manual
  checklist closure.
- Updated `GDD_10a_Overview.md` refresh metadata and clarified that the
  2026-05-25 design-lock pass locked the remaining open design decisions for
  `M8`, `M9`, `M15 Part A`, and `Maps 002–005`.
- Promoted the published execution order to:
  `M14 stages 1–3 → M16 → M14 stages 4–5 → M9a → M8 → M9b → M10 ...`
  while keeping the public milestone content unchanged.
- Implemented the first `M9a` engine slice in `scripts/skills/SkillHandler.gd`:
  - `stat_bonus` now applies combat-duration stat modifiers instead of staying
    a stub
  - `prescience`, `patience`, and `focus` now affect combat context
  - added passive helper seams for `discipline` (WEXP multiplier) and
    `healtouch` (staff-heal bonus)
- Wired the passive helper seams into live runtime callers:
  - `Unit.add_wexp()` now asks `SkillHandler` for WEXP multipliers
  - `Unit.perform_staff_heal()` now asks `SkillHandler` for heal bonuses
- Added focused tests for the new `M9a` handlers/helpers in:
  - `scripts/tests/test_skill_item_handler.gd`
  - `scripts/tests/test_unit_stats.gd`
- Normalized the active manual playtest sections in
  `AGENT/GDD/GDD_Manual_Tasks.md` so each now includes:
  - explicit setup
  - run order
  - expected-result standard
  - failure-note format
  This was added to:
  `Post-2026-05-19 Regression Sweep`,
  `Class / Skill Live Playtest`,
  `Pair Up Pass 1 Playtest`,
  `More Info Phase 1 Live Playtest`,
  and `M15 Part A — Hotseat Validation Playtest`.
- Merged the latest `Play_tester_comments.md` items with the earlier playtest
  findings and existing backlog in `AGENT/GDD/GDD_Manual_Tasks.md`, then
  deduplicated and triaged them into:
  - high-priority live bugs
  - medium-priority debug gaps
  - content / documentation gaps
  - feature requests already tracked elsewhere
  This also replaced the stale raw comment dump with a closed historical list
  plus a single current open-work section.
- Added the previously-untracked project files to version control:
  - `AGENT/GDD/Play_tester_comments.md`
  - `AGENT/Session Notes/2026-05-25b.md`
  - the new `.uid` metadata files under `scripts/shared/` and `scripts/tests/`
- Wrote `AGENT/Docs/playtest_triage_execution_plan_2026-05-26.md` to turn the
  merged triage list into an ordered execution plan covering:
  - hotseat phase handoff
  - hotseat combat preview
  - reclass overflow
  - debug `Force Level-up` on staff use
  - selector/manual-task documentation gaps
  - the map-menu `Exit to Main Menu` button
- Implemented the first bugfix slice from that plan:
  - `TurnManager.start_player_phase()` now refreshes Blue via a shared
    `_refresh_faction_units()` helper
  - `TurnManager.start_enemy_phase()` now refreshes each acting non-Blue
    whole-phase faction before its controller runs, fixing the stale `DONE`
    state that left Green unrefreshable on later hotseat rounds
  - `MapCursor._on_phase_changed()` now updates the cursor's controlling
    faction from the emitted `faction_id`, so control cleanly returns to Blue
    when Blue phase resumes after hotseat/AI handoff
  - tightened hotseat regression coverage in:
    `scripts/tests/test_turn_manager.gd`
    `scripts/tests/test_map_cursor.gd`
- Tracked the remaining 2026-05-25 design-lock document updates covering:
  - `campaign_rules_firming_notes_2026-05-25.md`
  - `hotseat_test_map_plan_2026-05-21.md`
  - `GDD_02_Core_Mechanics.md`
  - `GDD_05_Skills.md`
  - `GDD_06_Maps_Objectives.md`
- Implemented the next bugfix slice for debug `Force Level-up` on staff use:
  - `Unit.add_exp()` now routes all EXP gains through a shared debug override,
    so the debug aid affects staff healing and other non-combat EXP paths the
    same way it already affected combat
  - added a regression test in `scripts/tests/test_unit_stats.gd` proving a
    debug-enabled staff heal forces an immediate level-up
- Implemented the next triaged UI / validation pass:
  - `ReclassScreen` now places the option list inside a `ScrollContainer`, so
    long class lists remain reachable instead of running off-screen
  - `MapMenu` now exposes `Exit to Main Menu`, and `MapCursor` routes it
    through a confirmation dialog before returning to `Boot.tscn`
  - `test_targeting.gd` now includes a live `Map 900` regression proving a
    Green move-then-attack opens a populated combat preview on the real
    validation map
  - `GDD_Manual_Tasks.md` now records the expected rosters for the current
    validation maps and marks the landed triage items as closed
  - `run_tests.sh` now includes the new `test_map_menu.gd` suite

## Tests run
- `godot --headless --path /workspace --script res://scripts/tests/test_skill_item_handler.gd`
- `godot --headless --path /workspace --script res://scripts/tests/test_unit_stats.gd`
- `bash run_tests.sh`
- `godot --headless --path /workspace --script res://scripts/tests/test_reclass_screen.gd`
- `godot --headless --path /workspace --script res://scripts/tests/test_map_menu.gd`
- `godot --headless --path /workspace --script res://scripts/tests/test_targeting.gd`
- `bash run_tests.sh`
- None for the checklist-formatting follow-up (docs-only)
- None for the merged playtest-triage pass (docs-only)
- `bash run_tests.sh` via pre-commit while tracking the previously-untracked files
- `godot --headless --path /workspace --script res://scripts/tests/test_turn_manager.gd`
- `godot --headless --path /workspace --script res://scripts/tests/test_map_cursor.gd`
- `bash run_tests.sh`
- `godot --headless --path /workspace --script res://scripts/tests/test_unit_stats.gd`
- `bash run_tests.sh`
- `godot --headless --path /workspace --script res://scripts/tests/test_turn_manager.gd`
- `godot --headless --path /workspace --script res://scripts/tests/test_data_layer.gd`
- `godot --headless --path /workspace --script res://scripts/tests/test_snapshot_coverage.gd`
- `godot --headless --path /workspace --script res://scripts/tests/test_game_map_scene.gd`
- `godot --headless --path /workspace --script res://scripts/tests/test_new_game_screen.gd`
- `bash run_tests.sh`

## Commits made this session
- `dc01fd7` — `Promote M9a and land first skill engine slice`
- `b39e9d9` — `Normalize manual playtest checklists`
- `25f84c3` — `Triage merged playtest backlog`
- `803ef5a` — `Record session commit hashes`
- `1c42a62` — `Track current untracked project files`
- `3735ae2` — `Plan playtest triage execution`
- `fed8c45` — `Fix hotseat phase handoff refresh`
- `9ff5605` — `Record hotseat fix commit hash`
- `e7e262f` — `Track design lock documentation updates`
- `1682f81` — `Fix debug force levelup staff exp`
- `f72266b` — `Record debug exp fix commit hash`
- `bfed6d6` — `Land remaining triaged UI fixes`

## Plans for next session
1. Continue `M9a` with the next engine-safe handlers: terrain-gated combat
   skills, durability overrides, and any shared helper seams they need.
2. Keep `M8` positioned immediately after the engine slice unless a new
   dependency appears.
3. When `M9a` has enough coverage, start the `M9b` content-authoring pass
   against the now-locked handler surface.
4. Use the new merged playtest triage list to drive the next manual-fix pass,
   starting with the hotseat phase-handoff bug and the missing hotseat combat
   preview.
5. Follow `AGENT/Docs/playtest_triage_execution_plan_2026-05-26.md` in order,
   starting with Workstreams A and B on `Map 900`.
6. Re-test `Map 900` manually to confirm the phase handoff fix resolves the
   original live bug, then continue with the combat-preview issue if it still
   reproduces.
7. Continue through the remaining merged bug list: hotseat combat preview,
   reclass overflow, and the map-menu exit flow.
8. Next remaining runtime target is the smaller residual backlog: any manual
   live validation fallout from the new map-menu exit flow, plus the still-open
   content/docs gaps around objective coverage on future maps.

## Follow-up
- Authored `Maps 002–005` as the planned objective-showcase set:
  - `Map 002 - Seize`
  - `Map 003 - Defeat Boss`
  - `Map 004 - Escape`
  - `Map 005 - Defend`
- Registered the new maps in `data/maps/map_registry.json` so they appear in
  the New Game selector without scene edits.
- Added `UnitData.can_seize` and updated `TurnManager` so Seize supports the
  locked per-unit tag while remaining backward-compatible with existing
  `allowed_unit_ids` authored tests.
- Marked the first default-roster unit as `can_seize = true` so the Seize map
  is immediately playable with the shipped default party.
- Extended runtime coverage:
  - `test_turn_manager.gd` now proves the per-unit `can_seize` gate works
  - `test_data_layer.gd` and `test_snapshot_coverage.gd` account for the new
    `UnitData` field
  - `test_game_map_scene.gd` now boots all four new objective maps through the
    selected-map override path
  - `test_new_game_screen.gd` now expects the expanded registry population
- Updated roadmap/manual-task docs so `Maps 002–005` are tracked as landed
  instead of still blocked/planned.
- Diagnosed the exported debug-build startup failure:
  - instant defeat on `Map 001` was caused by export-unsafe `DirAccess`
    enumeration on `res://` in both `GameState.load_roster_from_directory()`
    and `DataManager._load_directory()`
  - that left the packaged build with an empty player roster and missing
    content catalogues, which explains the immediate defeat and broken retry /
    re-entry flow
- Fixed the export path by adding per-directory `resource_manifest.json` files
  for:
  - classes
  - weapons
  - items
  - skills
  - default roster
  - `Map 900` fixed test roster
  - `Map 950` fixed test roster
- Added `scripts/shared/ResourceManifest.gd` and updated:
  - `DataManager._load_directory()` to read manifest-listed resources first and
    only fall back to raw directory enumeration when no manifest exists
  - `GameState.load_roster_from_directory()` to use the same manifest-backed
    path, preserving authored roster order in exports
- Added regression coverage:
  - `test_data_manager.gd` now verifies the live catalogue manifests enumerate
    the expected counts and that export-critical ids like `iron_lance` and
    `discipline` resolve after boot
  - `test_game_state.gd` now verifies the default roster manifest preserves
    deployment order
- Bumped the Windows debug export metadata from `0.1` to `0.1.1` in
  `export_presets.cfg` in preparation for the rebuilt playtester package
- Diagnosed and fixed the second exported-build lifecycle bug:
  - quitting from an in-progress map back to the main menu left stale
    `GameState` battle state (`all_units`, faction buckets, snapshots, pair-up
    state) alive across the scene transition
  - starting a new map in the same app session could then boot on top of freed
    unit nodes and corrupt the second map entry path
- Fixed the lifecycle path in three places:
  - `GameMap._ready()` now resets map-scoped `GameState` state before spawning
    a fresh battle
  - `MapCursor`'s `Quit to Main Menu` flow now resets map-scoped state before
    changing back to `Boot.tscn`
  - `GameOverScreen`'s quit path now does the same
- Added regression coverage in `test_game_map_scene.gd` proving a fresh map boot
  clears stale `GameState` map state instead of appending on top of it
