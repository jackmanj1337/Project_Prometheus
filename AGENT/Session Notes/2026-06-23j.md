# Session Note — 2026-06-23j  (Documentation sorting / storage / retrieval reorg)

**Topic:** Designed + implemented a better doc system for `AGENT/Docs/` (the owner-requested
next-session task from the 2026-06-23i rollup). Design-walk style: audit → ratified design
register → incremental implementation. **Docs/tooling only; check_docs now 19/19.**

---

## What was done

**Audit (commit 1).** `AGENT/Docs/` was 126 flat entries, ~half historical evidence sitting
next to ~20 live driver docs. Pain points: no active/archive split; **no catalog of the ~19
`[XXX-n]` registers** (roadmap §H tracked them in prose only — `decision_index.md` never knew
that namespace); invisible supersession (the 2026-06-23a base+overlay model reversal); grep-only
retrieval; ~40 files with no Status header. Findings doc:
`AGENT/Docs/governance/documentation_system_audit_2026-06-23.md`.

**Design register (commit 1).** Owner ratified 4 forks via the design walk:
full **by-type** layout · **generated-from-headers** manifests · **archive-everything** (no
deletion) · write the design register first. Recorded as DSR-1..5 in
`AGENT/Docs/governance/documentation_system_design_2026-06-23.md`.

**Implementation (commits 2–8, one logical step each):**
- **Generator + checks (commit 2):** `AGENT/Docs/gen_docs_index.py` scans every doc header
  (fenced block preferred, filename/body heuristic fallback) → generates `INDEX.md` (doc map)
  + `REGISTERS.md` (registers catalog). `check_docs.py` +check 18 (manifests match
  `gen_docs_index.build()` — no-diff guard on the existing hook/CI) and +check 19 (archive/
  docs need a Historical/Superseded marker; `Superseded by [..](path)` targets must resolve);
  `_is_historical()` now also matches `Superseded`; `sys.dont_write_bytecode` so the check-18
  import never drops `__pycache__`.
- **Headers pass (commit 3):** curated fenced `Type/Status/Register/Resolved-in` headers on the
  16 registers + the driver docs the heuristic mis-tagged. REGISTERS.md is now authoritative —
  one row per register with OPEN/RESOLVED + resolved-where.
- **Moves (commits 4–7, all `git mv` → history preserved, nothing deleted):**
  archive/* (~70 files, Historical-marked) → live type folders `registers/ design/ plans/
  playtests/` → `guides/` (with `check_docs._ACTIVE_GUIDE_FILES` repaired in-commit) →
  `governance/ decisions/` (with the ~10 documentation_governance refs + decision_index home
  paths repaired). 38 broken backtick-paths in GDD/guides repaired; `Last verified` bumped on
  GDD_00–08 whose link-only edits tripped the stale-date check (no rule changes).
- **Process rule (commit 8):** AGENTS.md now documents the by-type layout + retrieval map +
  the "regenerate INDEX/REGISTERS in the same change" discipline (enforced by check 18).

## Final layout
```
AGENT/Docs/  INDEX.md REGISTERS.md check_docs.py gen_docs_index.py
  guides/(8) governance/(6) decisions/(2) registers/(16) design/(13) plans/(8) playtests/(4)
  archive/ consolidation/(8) plans/(11) playtests/(28) handoffs/(5) reference/(9) evidence/(10)
```

## Commits
1. audit findings + ratified design register
2. generator + manifest/archive checks (DSR-3/4)
3. curated headers on registers + drivers; generator fixes
4. move historical/superseded docs to archive/ (by-type)
5. move live docs into registers/ design/ plans/ playtests/
6. move active guides into guides/
7. move governance/ and decisions/; fix homes paths
8. AGENTS.md doc-layout rule + this session note

## Retrieval (the new canonical path)
- *What's active?* → `AGENT/Docs/INDEX.md`
- *Where was a `[XXX-n]` decided / is register Y open?* → `AGENT/Docs/REGISTERS.md`
- *Governance IDs (DOC/RULE/SET/OPEN/RNG/AWR)?* → `AGENT/Docs/decisions/decision_index.md`
- *Is a doc live?* → its header `Status` + (if archived) its `Superseded by`/`Historical` marker

## Verification
`check_docs.py` → **19/19 PASS** (was 17/17 — +2 new checks). Docs/tooling only; no
`__pycache__`; every move via `git mv`; nothing deleted (archive only). Working tree clean.

## Next session
- Content thread is unchanged and intact (scope map, foundations doc, DMR/ICO registers, the
  a–i session notes). Resume there: firm the **prep-as-hub** framing → convoy→shop economy
  spine; avatar feasibility dive available (see 2026-06-23i).
- Two register OPEN sets still need walking when prioritized: `[LEG-1..5]`, `[IMP-1..6]`,
  `[REN-1..5]`, `[CST-13]` (see REGISTERS.md).
- Optional doc follow-up: cross-link `decision_index.md` ↔ `REGISTERS.md` headers; backfill
  fenced headers on archive docs (currently heuristic-classified, which is fine).
</content>
