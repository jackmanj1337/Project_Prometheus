# Session Note 2026-07-08c — v0.3.0 full-scan Pass 3

## Goal

Continue the resumable v0.3.0 full code-pillar scan
(`AGENT/Code Reviews/v030_full_scan/`). Cadence is **one pass per invocation**;
`_TRACKER.md` is the resume anchor. First TODO was **Pass 3 — Input model &
settings persistence** (3 files), which must also re-confirm the carried Medium
(Settings Input Mode changes don't refresh prompts immediately).

## Progress

- Read `_TRACKER.md` + the Pass 2 note; confirmed working tree = head `b7bcfd2`
  for review (no production lands after it; HEAD was `15d4ca3`, docs only).
- Read at head: `InputModeManager.gd`, `SettingsManager.gd`, `MapCursorInput.gd`,
  plus `test_input_mode_manager` / `test_settings_manager` / `test_map_cursor_input`,
  and cross-read `SettingsScreen`'s `input_mode` row (Pass 4) for the consumer side.
- Wrote `AGENT/Code Reviews/v030_full_scan/03_input_model.md`: **1 Medium
  (carried, re-CONFIRMED), 2 Low.** No new correctness bugs.
  - **M1 (carried Medium, re-CONFIRMED):** changing Settings → Input Mode does not
    refresh the active mode / prompts until the next input event.
    `InputModeManager._refresh_active_input_mode` (`:53`) is private and only runs
    on `_ready`/`_input`(via `note_detected_input_mode`)/joy-hotplug; the
    `input_mode` schema row (`SettingsScreen.gd:104-113`) has **no `"apply"` hook**,
    so `_apply_enum` only does `sm.set("input_mode", …)`. Setting looks inert until
    an unrelated event nudges the resolver. Root cause = **no public re-resolve
    entry point** on `InputModeManager`. Fix spans Pass 3 (add public
    `refresh_from_settings()` wrapping `_refresh_active_input_mode`) + Pass 4 (call
    it from the row's `"apply"` hook and from `reset_section_to_defaults("controls")`).
  - **L1:** input-mode vocab (`VALID_INPUT_MODES`) + `normalize_input_mode`
    duplicated across `SettingsManager` and `InputModeManager` — two sources of
    truth, drift risk; extends Pass 1 L4 / Pass 2 L2 duplication theme.
  - **L2:** `_refresh_active_input_mode` rebuilds `available_modes()` +
    re-queries `OS.has_feature` on every input event; cache once (session-fixed).
- Flagged a **cross-file seam for Pass 5** (not a Pass-3 finding): `MapCursorInput`
  now steps from two paths (`decode()` MOVE edge + `poll_direction` analog poll);
  since cursor actions bind both keys and stick axes, a held keyboard arrow
  satisfies both. Safe **only if** `MapCursor` arms the repeat timer on the same
  frame it consumes the decode MOVE (so poll sees `dir == _held_dir` → `tick`, not
  a second `arm_repeat`+step). Verify the arm-on-decode ordering in Pass 5's
  `MapCursor`; `MapCursorInput` itself is internally correct.
- Flipped tracker Pass 3 → DONE (1 Med, 2 Low) + Log line.
- Pre-commit green: `check_docs.py` all 26, RNG guard PASS, Godot skipped
  (docs-only).

## Next

- **Pass 4 — Input display & rebind UI** (2 files: `SettingsScreen.gd`,
  `InputDisplay.gd`). Re-confirm the carried **Medium** — rebind UI omits
  `more_info`, `peek_range`, `zoom_in/out/reset`. Also lands the Pass-4 half of
  M1 (the `"apply"` hook wiring) as a finding, and can check `InputDisplay`'s
  brand/prompt resolution against `InputModeManager`.

## Commits

- `d5d4d3d` — v0.3.0 full-scan Pass 3: input model & settings persist (1 Med, 2 Low).
- Session note + INDEX row in the closeout commit after `d5d4d3d`.
