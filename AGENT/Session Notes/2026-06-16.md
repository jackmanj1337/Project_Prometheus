# Session Notes — 2026-06-16 (initial playtester feedback slice)

Continues from 2026-06-15d on branch `hud-panel-layout-item4`.

## What was done

- Implemented the first actionable playtester responses:
  - `UnitDetailsScreen` compact stats now show effective-display totals, including
    Pair Up and other combat-only stat contributions, without requiring More Info.
  - Paired leads show a small on-map `PU` badge.
  - The character sheet adds `View Support` / `View Lead` for paired partners.
  - New Game rule toggles still persist immediately, while the map dropdown reopens
    from the last configured/launched `GameState.next_map_data_path` until Start.
  - Debug builds now bind F9 to an all-faction hotseat override. It shows
    `hotseat-all` in the debug banner, stops AI at safe checkpoints, and when toggled
    off during an AI-owned phase cancels transient cursor UI before resuming that
    faction's authored AI.
- Updated GDD docs in the same behavior commit:
  - `GDD_02_Core_Mechanics.md` — F9 hotseat override turn-control behavior.
  - `GDD_05_Skills.md` — Pair Up signal/UI surfaces.
  - `GDD_07_UI_UX.md` — effective compact stats, paired partner access, `PU` badge,
    New Game last-launched map semantics, F9 input.
  - `GDD_10_Roadmap.md` — 2026-06-16 playtest follow-up status and F9 cleanup anchors.

## Verification

- `python3 AGENT/Docs/check_docs.py` — PASS
- `TEST_JOBS=8 ./run_tests.sh` — PASS, 47 suites
- `git diff --check` — PASS
- Pre-commit reran docs, RNG guard, and all 47 suites — PASS

## Commits

- `514f0fa` — Address initial playtest feedback
- Follow-up note/index commit records this session.

## Ready for visual confirmation

- `I` sheet compact stats visibly change for paired leads and match More Info totals.
- `View Support` / `View Lead` opens the paired partner and returns to the lead.
- `PU` badge reads clearly on common map backgrounds and faction tints.
- New Game map dropdown returns to the last launched map after Back without Start.
- F9 toggles all factions into hotseat midgame; F9 off during Red/AI phase backs out
  menus/selections and resumes Red AI cleanly.

## Next

- Package or run a non-headless build for the visual confirmation list above.
- The older combat-preview enhancement remains open: show attacker/defender weapon
  names in the forecast.
