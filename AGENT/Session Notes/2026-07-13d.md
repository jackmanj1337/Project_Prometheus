# Session Note - 2026-07-13d

## What was done

Implemented `B2-OCCUPANCY` map-start transaction groundwork in commit `69184bc`.

- Added structured `OccupancyContext` and `PlacementResult` contracts.
- Added the registry-dispatched `OccupancyService` autoload.
- Implemented `require_empty`, deterministic `nearest_free`, runtime-only
  `delay`, and `skip`; reserved `swap`, `overlap_hidden`, and `object_unit` as
  structured `not_implemented` results.
- Routed normal map-start player/enemy spawns through occupancy before the
  private instancing seam; exact suspend restoration remains separate.
- Added player/enemy start-overlap validation and a DoD#2 spawn-bypass guard.
- Updated the architecture, maps, roadmap, feature index, and control plane.

## Validation

- `test_occupancy_service.gd`: 7 passed, 0 failed.
- `test_game_map_scene.gd`: 47 passed, 0 failed.
- `test_data_manager.gd`: 26 passed, 0 failed.
- Full `run_tests.sh`: 73 suites passed.
- `AGENT/Docs/check_docs.py`: 29/29 passed.
- `scripts/ci/check_rng_usage.sh` and `git diff --check`: passed.

## Next

- The next Band 2 plan-order slice is `B2-DEATH-LIFECYCLE`.
- Persistent delayed placement waits for an F1 row and a real reinforcement
  consumer; swap, hidden overlap, and object-unit behavior stay with their
  owning slices.
- A returned v0.3.2 live playtest still preempts new v0.4 work at a clean commit.
