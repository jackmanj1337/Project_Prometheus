# Session Note - 2026-07-13j

## What was done

Completed the remaining v0.4.0 review fix handoff and closed the code-review
gate.

- Fixed the Low cleanup batch: optional action parameters use neutral defaults;
  failed projections clear stale Attack Preview selector state; failed refunds
  report no unapplied deltas; fresh maps clear runtime-only delayed placement
  requests; the unused `DataManager.is_registered_id()` seam was deleted; and
  the Attack Preview fixture comment now describes the real projection route.
- Added focused regressions for each behavioral cleanup.
- Strengthened projection no-mutation coverage with byte snapshots of both
  combatants' HP, active modifiers, skill counters, and inventory. Documented
  that the cheap per-call runtime guard intentionally covers only RNG history and
  party gold.
- Made `RegistryManager.reload_presets(source)` strict-replace all three required
  registry families from the selected content root. Missing families stay empty
  and report loud errors. `DataManager.select_campaign_source()` now reloads
  registries before validation and reports those errors through its shared
  channel.
- Documented the suspend spawn path's occupancy bypass as intentional exact-state
  restoration.
- Re-checked all seven release-checklist slices, marked the review CLOSED with no
  remaining High/Medium/Low finding, and advanced the checklist to the release
  metadata/build/smoke/handoff stages.

## Commits

- `07b3351` - Fix Band 2 review cleanup findings.
- `ab05e3a` - Prove projection unit-state preservation.
- `859ae28` - Reload registries from campaign sources.

## Validation

- `bash scripts/check_env.sh`: passed; expected local `.env` warning only.
- `python3 AGENT/Docs/check_docs.py`: passed all 29 checks.
- `bash scripts/ci/check_rng_usage.sh`: passed.
- `bash run_tests.sh`: all 75 suites passed after each logical commit.
- Focused suites: action/effect 7/7, resource ledger 9/9, Attack Preview selector
  19/19, GameMap scene 50/50, projection 5/5, registry manager 9/9, and
  DataManager 25/25.
- Headless editor scan exited successfully with no script/resource load failure.
  The container could not write `/home/vscode/.cache/godot`, so it emitted editor
  cache warnings; generated import/UID artifacts were removed afterward.

## Next

Continue the v0.4.0 release checklist at release metadata, then build, smoke, and
handoff. The v0.3.2 live rerun still preempts at this clean commit if evidence
returns.
