# Session Note 2026-07-08b — v0.3.0 full-scan Pass 2

## Goal

Continue the resumable v0.3.0 full code-pillar scan
(`AGENT/Code Reviews/v030_full_scan/`). Cadence is **one pass per invocation**;
`_TRACKER.md` is the resume anchor. First TODO was **Pass 2 — Determinism:
state capture + RNG + combat** (7 files).

## Progress

- Read `_TRACKER.md` + Pass 1 note; confirmed no production lands after `b7bcfd2`
  (working tree = head for review).
- Read at head `b7bcfd2`: `RngService.gd`, `GameState.gd`, `CombatResolver.gd`,
  `SkillHandler.gd`, `EnemyAI.gd`, `CampaignRules.gd`, `UnitData.gd`, plus the
  RNG/suspend test suites, and cross-read the `GameMap`/`TurnManager` seam
  (Pass 5) to re-confirm the carried High in-context.
- Wrote `AGENT/Code Reviews/v030_full_scan/02_determinism.md`: **1 High, 2 Low.**
  - **H1 (carried High, re-CONFIRMED):** fresh maps never call
    `RngService.start_map()`. Grep-proven: `GameMap.gd:127` calls
    `TurnManager.start_map` (a different method); nothing calls
    `RngService.start_map` outside tests. So `map_seed` stays `0` (no
    cross-session entropy — same moves → byte-identical dice across runs) and
    `history_hash` bleeds between maps. Snapshot/resume are unaffected (resume
    restores via `from_save_dict`). Tests seed manually, masking the gap. **Fix
    site is Pass 5 `GameMap`:** seed on the fresh-map branch *before*
    `take_map_snapshot()` (before `GameMap.gd:115`).
  - **L1:** stale `_current_hit_formula` comment (`CombatResolver.gd:124-126`) —
    says `campaign_rules` "not yet wired", but B1-CST (`625c00d`) already landed it.
  - **L2:** `_string_array_from_variant` duplicated ×4 (GameState/TurnManager/
    SaveCodec/SaveData); extends Pass 1 L4.
- Chased and **cleared** a suspected desync: `get_action_start_tile` reads the
  `_original_tiles` undo buffer, but it's erased on `set_unit_state(DONE)`
  (`TurnManager.gd:609`) and faction refresh (`:386,592`), *after* the record is
  built — logged as a positive.
- Flipped tracker Pass 2 → DONE (1 High, 2 Low) + Log line.
- Pre-commit green: `check_docs.py` all 26, RNG guard PASS, Godot skipped
  (docs-only).

## Next

- **Pass 3 — Input model & settings persistence** (3 files:
  `InputModeManager`, `SettingsManager`, `MapCursorInput`). Re-confirm the carried
  **Medium** — Settings Input Mode changes don't refresh prompts immediately.

## Commits

- `8792436` — v0.3.0 full-scan Pass 2: determinism state+RNG+combat (1 High, 2 Low).
- Session note + INDEX row in the closeout commit after `8792436`.
