# Session Note 2026-07-05g - v0.2.7 playtest return: sort, triage, owner walkthrough Q1-Q7

## What was done

The v0.2.7 playtest RETURNED. Per the 2026-07-05b two-track note, switched from
`v0.3.0-features` to the release line (`docs-reorg-2026-06-23`) and ran the full
return cycle: sorted the artifacts into permanent homes, diagnosed every finding
against live source, wrote the triage plan + owner review docs, and walked the
seven decisions with the owner one at a time. **No fixes built yet** — this was
the diagnosis + decision session; the build pass is next.

### Return sorted (commit `914f195`)

- `AGENT/v0.2.7 playtest incoming/` → gone. Returned checklist →
  `Docs/playtests/playtest_checklist_v0.2.7_returned_2026-07-05.md`; 4 screenshots
  + 4 logs (renamed descriptively) → `Docs/archive/evidence/` (incl. one stray
  v0.2.6 session log); the incoming build-manifest copy was byte-identical to the
  repo's — dropped.
- **Gate outcome: does NOT close.** §1.1 + §1.5 PASS (both v0.2.6 §1.1 loose ends
  `V026-01d/e` close — "everything seems fine", no flicker), §3.1 regression
  clean, log returned again with intact BUILD STAMPs. **§1.3, §1.4, §1.6 FAILED**
  with new specifics → `VAL-V023-DISPLAY` stays Pending; rerun shrinks to those
  three (v0.2.8).

### Diagnoses (`V027-01..05`, triage plan has the detail)

- **V027-02 (§1.3)** action menu drifts OVER the unit as zoom rises — confirmed
  geometry bug: the V025-03 gap cap measures one UNZOOMED tile from the tile's
  LEFT edge, so at zoom > 1 the menu sits inside the 96-256px zoomed tile.
- **V027-03a (§1.4)** first-open dead space below the forecast rows — the known
  first-show inflated RichTextLabel-minimum trap (V025-05a family).
- **V027-03b (§1.4)** LEFT-wall misplacement at max zoom — NOT pinned from
  source (right-wall flush held; no mirror found); candidates recorded, needs a
  headless repro.
- **V027-04a (§1.6)** windowed 1440p→4K at 2.0× stretches the settings panel
  off-screen once per boot — NOTHING connects `size_changed`, so a post-resize
  content-min change grows the scroll frame with no recenter (V026-01a shape,
  resize-triggered).
- **V027-04b (§1.6)** applied-size readout ignores OS drag-resize — the handbook
  §1.6 explainer promised write-back + recentre that was NEVER BUILT
  (`applied_windowed_size()` only derives from the saved request). Handbook
  overpromise from the v0.2.7 doc pass.
- **V027-05** feature asks: (a) MainMenu exempt from Menu Scale, (c) gray out the
  Resolution row outside Windowed. Log notes: only known M9 skill stubs + one
  `ObjectDB instances leaked at exit` (noted for a future leak audit).

### Docs + owner walkthrough (commit below)

- `Docs/playtests/playtest_v0.2.7_results_triage_plan_2026-07-05.md` (diagnosis +
  evidence + sequencing) and `Code Reviews/playtest_v0.2.7_triage_review_2026-07-05.md`
  (Q1-Q7, options/drawbacks/recommendation-first). INDEX regenerated; 21/21 green.
- **Decisions (all recorded in the review doc):** Q1→A far-edge anchor
  (AttackPreview's model) for `_place_menu_near`; Q2→A one-frame deferred re-pass
  on forecast first show; Q3→A repro-first (headless left-wall scene check) THEN
  the deferred re-anchor self-heal; Q4→A viewport `size_changed` → coalesced
  `_apply_menu_scale()`; **Q5→B full write-back (against recommendation)** — OS
  drag writes the applied size into the saved resolution, dropdown gains a
  "Custom W×H" state, but NO drag-triggered recentre (that handbook sentence gets
  corrected); Q6→A gray out Resolution + native readout outside Windowed; Q7→A
  both §1.1 notes routed to `UI-INSPECTION` (control-plane row updated).

## Commits

- `914f195` - `Sort v0.2.7 playtest return; add diagnosis triage plan + owner review Q1-Q7`
- (this commit) - walkthrough decisions recorded, control-plane `UI-INSPECTION`
  routing, statuses flipped, session note.

## Verification

- `python3 AGENT/Docs/check_docs.py` — PASS (21/21) after each doc change.
- No engine code touched this session; the full Godot suite runs in the
  pre-commit hook regardless.

## Next session

On `docs-reorg-2026-06-23` — the V027 fix pass, in the decided order, each with
regression tests: Q1 geometry → Q2 deferred re-pass → Q3 (left-wall headless
repro, fix what it shows, then the zoom-reposition self-heal) → Q4 resize hook →
Q5 write-back + "Custom W×H" + handbook §1.6 recentre-sentence correction
(largest item) → Q6 gray-out. Then cut **v0.2.8** as the §1.3/§1.4/§1.6-only
rerun; on its Part I pass flip `VAL-V023-DISPLAY` → `REL-V023-MERGE` /
`B6-WEB-DEBUG`. The `v0.3.0-features` track (gamepad slice 1, Band 5/6) is
unchanged and separate.
