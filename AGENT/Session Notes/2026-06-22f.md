# Session Note — 2026-06-22f

**Topic:** Planning-only. **Walked AND resolved the Stationary Weapons register `[STW-1..6]`**
(ballista/onager, GDD §5). STW is now build-ready (gated behind Package A Step 1 + the DCH
`map_objects` build it inherits). Docs-only; check_docs **17/17**; no `__pycache__`.

---

## 1. Six resolutions (all owner-decided this session)
- **[STW-1] map-object model = A (forced).** Reuse the unified `map_objects` array with a
  `type:"siege_weapon"` entry — forced by [DCH-2]'s unified model; one validation/snapshot path.
- **[STW-2] occupancy = A.** Unit ends movement ON the siege tile (FE-classic); the tile is
  passable terrain a unit stops on, "Activate/Fire" fires. Reuses `can_end_on_tile`.
- **[STW-3] move-and-fire = B (OWNER OVERRIDE of rec A).** May move onto the tile and fire the
  same turn — **no MOVED-state gate** (simpler build). Safe because the long range is an
  effective-weapon override that holds *only while on the tile* ([STW-4]); step off and it's gone,
  so nothing is ferried. Trade-off: less garrison-then-fire positional commitment.
- **[STW-4] weapon swap = A.** Scoped equipped-weapon override while on a siege tile; existing
  range/targeting math reused unchanged. (Also what makes [STW-3] B safe.)
- **[STW-5] ammo = A.** `ammo:int` on the `map_objects` siege entry (shared by any unit that mans
  it), `-1` = infinite, snapshotted in `map_objects_state`; `ammo == 0` removes Fire.
- **[STW-6] AI siege use = B (OWNER OVERRIDE of rec A).** Enemy AI mans + fires siege in v1
  (classic enemy-phase threat). **Consequence:** promotes the `siege_operator` AI profile from the
  *gated/build-later* bucket into **STW v1 scope** — couples STW to the AI composition engine
  (`ai_first_build_design`). As a composition behavior: path → mount → long-range fire → draw the
  shared ammo pool.

## 2. Cross-cutting consequence (recorded everywhere)
**[STW-6] B pulls `siege_operator` into STW v1.** It was a gated profile (listed to "land with
STW" later); now it's confirmed in-scope for the STW build, not build-later. Updated AIP §2a row +
the gated-bucket note, and GDD_10 §H gated-profiles line.

## 3. Edits
- `stationary_weapons_open_questions_2026-06-21.md` — header → **RESOLVED 2026-06-22f** + owner-
  override banner; §1 move-and-fire note (classic forbids, we allow per [STW-3] B); all six §3
  resolutions flipped with text + the two override consequences; §4 slice sketch rewritten to the
  decided build order (no MOVED gate; AI siege is now slice 5 *in v1*) with DoD#2 check_docs notes.
- `ai_profiles_open_questions_2026-06-21.md` §2a — `siege_operator` row bucket → **STW v1** (gate
  cell marks [STW-6]=B RESOLVED); gated-bucket summary note records the promotion.
- `GDD_10_Roadmap.md` §H — STW line flipped **RESOLVED 2026-06-22f** (build-ready summary); AIP
  gated-profiles line corrected (`siege_operator` pulled into STW v1).
- `planning_backlog_2026-06-20.md` §5 — STATUS UPDATE 2026-06-22f: STW added to RESOLVED list;
  still-open list shrinks to IMP/DMR/BWN/MRD (+ release gates REN/LEG).
- **DoD#1:** planning only (no behavior change) — `GDD_05`/§5 updates at build time.
- **DoD#2:** no checkable rule ratified *this session*. Two guards are pre-registered to land WITH
  the STW code (consistent with prior gated-feature sessions): the `map_objects` type value-set
  gains `"siege_weapon"`, and the AI profile value-set gains `siege_operator` — both extend
  `check_docs.py` at build, not now.

## 4. Verification
- `check_docs.py` → **17/17 PASS**, docs-only, no `__pycache__` artifacts.

## 5. Next session
- **All design registers feeding the playable campaign are now RESOLVED** (PKGA · FOW · DCH · DTR ·
  MET · AIP-fully · STW). Remaining planning: **IMP `[IMP-1..6]`, DMR `[DMR-1..3]`, BWN
  `[BWN-1..5]`, MRD `[MRD-1..6]`**; release gates REN/LEG are owner-input-blocked.
- **Build track:** **Package A Step 1 first** (critical path: Package A → §2 → DCH/AIP/FOW save
  slices → STW rides DCH's `map_objects`).
