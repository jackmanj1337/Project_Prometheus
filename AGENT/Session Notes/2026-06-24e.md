# Session Note — 2026-06-24e  (Learnable spells → weapon-source merge; CEX cluster B expanded `[CEX-20..23]`)

**Topic:** Explored learnable spells `[CEX-5..8]` and merged them, by analysis, into a broader
**non-inventory weapon-source** frame. Folded the whole thing into CEX **cluster B** for the upcoming
priority-evaluation session (kept **OPEN** — to separate/sort there). Drafted the **fists** demo
weapon. Fixed a pre-existing reorg bug blocking commits. Branch `docs-reorg-2026-06-23`.

---

## Key finding — combat is source-agnostic
Every computation in `CombatResolver` (hit/dmg/crit/triangle/effectiveness/`can_counterattack`)
consumes a **`WeaponData`** from `get_equipped_weapon()`; it **never reads inventory**. So a learned
spell, a Laguz natural weapon, and unarmed fists are the **same structural thing** — a `WeaponData`
not in inventory whose charges aren't an `InventoryEntry`. The whole seam is two `Unit.gd` functions
(`_find_equipped_weapon`, `get_equippable_weapons`) that today iterate only `data.inventory`.

## Merge direction (hybrid — working lean, non-binding until eval)
Capability = `[IEQ]` `ItemDef{WeaponComponent}` (a tome minus the slot); known/equip/learn lifecycle
reuses the **skill** model. No bespoke `known_spells`, no Cast action. Revises the leans on
`[CEX-5]` (fold cast into Equip/weapon-select), `[CEX-6]` (charge-source abstraction: entry-uses XOR
counter/pool XOR infinite), `[CEX-7]` (reuse `[SKL-3]` earned/equipped + `requires_equip`), `[CEX-8]`
(dissolved — tome & spell are the same ItemDef, different reference path).

## Verified equip reality
The weapon-swap path (`ActionMenu` "Equip" → `MapCursor._open_weapon_menu()` → `WeaponMenu` in
`GameMap.tscn` → `set_equipped_weapon`) is **fully built and wired** but **never surfaced in play**:
the Equip button needs **≥2 equippable weapons** and **every roster unit ships exactly one**.
`WeaponMenu` has **no test**; only the button-visibility toggle is covered (`test_action_menu`).

## New open items (stay OPEN for the eval session)
- **`[CEX-20]`** **two-source** weapon enumeration — **inventory** (physical: slots/trade/drop/
  durability) vs a **granted list** (every non-inventory `WeaponData`: spells, natural weapons,
  accessory/event grants). Refined from three sources after a follow-up: "source" only buys storage +
  lifecycle; the real fork is **physical object vs reference**. Provenance (`learned`/`class_innate`/
  `accessory`/`event`) is a **per-row tag reusing the `[SKL]` pattern** (`[SKL-3]` `retain_on_reclass`),
  **not** a separate path; charge backend (entry-uses/counter/pool/infinite) is an *independent* row
  property. Class-innate is **recomputed from the class**, not stored. Wires up the dangling
  `is_natural_weapon`/`natural_weapon_type`; monster innate attacks for free.
- **`[CEX-21]`** equipped-source-as-reference (today equip is **positional** — `set_equipped_weapon`
  reorders inventory, can't pick a spell/natural source); with two sources the `equipped_source`
  reference distinguishes just **inventory-entry id vs granted-list id**. + action-menu split **Attack /
  Equip Weapon / Inventory** (Inventory = consumables now + accessory-equip later, likely prep-only).
- **`[CEX-22]`** equipped attack method becomes unavailable on **another player's turn** (broke / out
  of charges / pool drained / revoked) → what does it counter with? Touches `can_counterattack`.
- **`[CEX-23]`** rework attack-selection + target-acquisition for **source/maneuver combinations**
  that alter resource use, stats, range, or targeting rules (preview must show the combined effect;
  targeting re-derives from the combo). Largest UX/structural fork; interacts with A (pools) + `[SKL]`.

## Demo asset
`data/weapons/fists.tres` — **0 Mt**, **∞ uses** (`-1`), neutral, `is_natural_weapon`; new **`fist`**
combat family + WEXP track in `GameConstants`. Validates clean; covered by `test_data_manager`.
Designers pick default-for-all vs martial-artist class via `ClassData.allowed_weapon_families`.

## Commits
1. `5cf57ed` — Fix stale doc paths in `test_release_metadata` after the docs reorg (checklist →
   `playtests/`, env setup → `guides/`; suite had been red since `1578d8d`, blocking the pre-commit).
2. `b2b0a4a` — Fold weapon-source/equip design into CEX cluster B `[CEX-20..23]` + fists demo; bump
   register `CEX-1..19` → `CEX-1..23`; regen REGISTERS.md.

## Verification
`check_docs.py` **19/19 PASS**; RNG guard PASS; **full test suite green** (48 suites) — incl. the
now-fixed `test_release_metadata` and the new `fists` check in `test_data_manager`.

## Roadmap reprioritized (owner 2026-06-24e)
Atlas §3b/§3 reordered: a **full feature-definition sweep of every un-firmed target** is now the
**priority blocker ahead of F1 schema-lock** (not just the 4 GAP/DISCUSS features). Rationale: F1 can
only lock a *complete* save schema once every persisting feature is defined. Chain = **define-all
sweep → F1 schema-lock → all builds**; only the **next playtest round** runs ahead of/beside it.
Phase A sweep = cluster B (in flight) + GAP/DISCUSS (Support #5, Recruit #4, Avatar #20, Battalions
#16 — schema-defining, take early) + the remaining ✗ worklist (Difficulty, Dancer, Movement assists,
Utility staves, Rescue, Village, PvP, Arena, Bonus-EXP, Combat arts, per-skill UX).

## Define-all sweep grouped by shared-system overlap (owner 2026-06-24e)
Phase A reorganized in atlas §3b from a flat GAP/DISCUSS+worklist list into **five sync-clusters** so
features touching the same system are firmed together (shared schema/UX designed once):
- **A1 Combat capabilities & attack/target flow** — spells/weapon-sources, combat arts (#15), utility
  staves (#10), gambit attack side (#16 ⇄ A2). Shared: `[CEX-20/21/23]`, F7 pools, F11.
- **A2 Map action-economy & movement** — Dancer (#8; Reinvigorate exists), movement assists (#17),
  Rescue (#6), battalion action side (#16 ⇄ A1). Shared: post-move action window, F10, displacement.
- **A3 Roster identity & relationships** — Support (#5), Avatar (#20), Recruit/Capture (#4 ⇄ A4).
  Shared: roster+save schema, support/conversation UI, F6. **Do first — biggest F1-schema risk.**
- **A4 Story/event map content** — Village (#11), Recruit conversation+flag side (#4 ⇄ A3). Shared:
  MET (F8) + flags (F6).
- **A5 Campaign meta-rules & EXP/economy** — Difficulty+Casual/Phoenix (#12), Bonus-EXP (#18), Arena
  (#14), PvP (#7). Shared: F4 profiles, hub/PHB, EXP economy, permadeath path.
- Cross-cutting content: per-skill UX (#M9b) alongside A1. Straddlers: Recruit (A3⇄A4), Battalions (A1⇄A2).

## Next session — start the define-all sweep
Suggested order **A3 → A1 → A2 → A4 → A5** (A3 first = highest F1-schema risk). Finish cluster B's
eval/sort (separate spell Qs `CEX-5..8` from the weapon-source/equip scope `CEX-20..23`) as the head of
A1. F1 schema-lock waits until the sweep is done (then reserves `equipped_source` + every field surfaced).
