# Session Note - 2026-07-21d

## What was done

- Added automatic playtest-host metadata to the startup log: OS/version,
  distribution and device model, CPU/thread count, locale/time zone, display
  server, rendering API, GPU name/vendor/type, and window/screen geometry.
- Logged connected-controller identity at startup and connection changes.
- Logged structured campaign context on start, restore, and node launch, including
  campaign/node and active campaign-package identity.
- Deliberately left tester identity and the final PASS/FAIL verdict as human input;
  the executable should not infer identity or decide whether observed behavior is
  acceptable.

## Commits claimed

- `3a009484307cf835130c382103bb9caf84512832` — Capture playtest environment and campaign context

## Gates

- `bash run_tests.sh` — PASS, all 103 release-line suites green.
- `bash scripts/ci/check_gdscript_style.sh` — PASS, 235 tracked scripts.
- Focused BuildInfo, Boot, and CampaignManager suites — PASS.

## Next

Cut the v0.5.3 Windows build and make the checklist treat the runtime-environment
and campaign-context log blocks as the authoritative machine-captured provenance.
