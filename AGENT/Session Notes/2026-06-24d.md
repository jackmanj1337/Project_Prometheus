# Session Note — 2026-06-24d  (Firm story / key items — `[CEX-14..16, 18, 19]`)

**Topic:** Wave-1 firming (atlas §3b), cluster E. Firmed **story/key items** — the owner expanded
scope from "tracking + lock" into a richer system (author locks, event-mutation, presence-as-flag,
convoy integration, editor validation). Docs-only; check_docs **19/19**, tree clean. Branch
`docs-reorg-2026-06-23`.

---

## Grounding
- Items carry an `item_type` enum (`key`/`sellable`/…) that the **`[IEQ]` model dissolves**; so
  story-ness is a **base flag on `ItemDef`**, orthogonal to weapon/consumable/accessory components.
- **`[MET]`** already plans a `flag` action + flag-gated triggers but has **"no flag store"** = **F6**.
  *Holder-based branching* needs F6; the "**holds item X**" predicate reads inventory **live** (no F6).
  *Tracking* (who-holds-what) is a derived roster+convoy scan — no new storage.

## What we firmed
- **`[CEX-14]` locks** — `ItemDef.story` + author-configurable **`no_sell`/`no_drop`/`no_trade`**.
  Defaults when `story=true`: no-sell + no-drop, **trade allowed** (author sets `no_trade` to bind a
  relic). **More Info auto-explains** the active locks; inventory shows a distinct tag.
- **`[CEX-15]` presence-as-flag** — "holds item X" = a live **`[MET]` predicate** (no F6) gating
  side-quests + **recruitment conversations**; persistent story-state branching rides **F6**.
- **`[CEX-16]` tracking + convoy** — convoy **"Key Items" view**; key items **exempt from
  `convoy_capacity`** (reconcile `[CNV-2]`); designer/GUI-editor key-items registry (derived scan).
- **`[CEX-18]` event mutation (NEW)** — `[MET]` item-targeting actions
  **upgrade/weaken/steal/destroy/grant_item** (relic mutated by story; upgrade/weaken reuse
  `forged_mods` + overlap `[PXP]` item-bond). Rides the MET build.
- **`[CEX-19]` authoring validation (NEW)** — warn (GUI editor + `DataManager.validate()`) when a
  key item has **finite uses AND no repair path** (Hammerne/shop/forge) → could be permanently lost.
  Lands with the item-model build (no .tres yet), not a docs check.

## Reconciled docs
CEX register cluster E resolved (`CEX-1..17`→`CEX-1..19`; status note → **A/C/D/E resolved, only B
open**). Candidate-systems design doc block E → RESOLVED banner. **IEQ** model: reserved
`story`/`no_sell`/`no_drop`/`no_trade` on `ItemDef`. **`[CNV-2]`**: key-items capacity exemption +
Key Items view. **`[MET]`**: holds-item predicate + item-mutation actions noted as MET-build additions.
Atlas Cluster-A row → firmed.

## Commits
1. `…` (this commit) — firm `[CEX-14..16, 18, 19]` story/key items; reconcile CEX/CNV/MET/IEQ/atlas.

## Verification
`check_docs.py` **19/19 PASS**; RNG guard PASS; manifests regenerated; no new `__pycache__`.

## Next session — learnable spells `[CEX-5..8]` (the last open CEX cluster, B)
Feature-sized (XL), unblocked by F7 pools. Known-list (no inventory slot), per-map/pool uses,
learning reuses `[PXP-4]`/`skill_unlocks`/`[PXP-9]`. Open Qs already drafted: CEX-5 cast interaction,
CEX-6 charge model, CEX-7 learning surfaces + forget/swap, CEX-8 (see register). Wave-0 **F1
save-schema lock** still pending before any build (now also reserves: `map_uses_remaining`,
`ItemDef.story`+lock flags, triangle profile selection).
