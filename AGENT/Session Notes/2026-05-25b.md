# Session Notes — 2026-05-25b

## Focus

Walk the four "decisions to review" buckets from `2026-05-25.md` — M8, M9,
M15 Part A, and the Objective-Map followup (Maps 002–005) — and fold every
lock back into the canonical planning docs before implementation begins on
any of those milestones.

## What was done

- Reviewed each open question one at a time, with options + pros/cons +
  recommendation per question. 17 decisions total.
- Captured the decisions and propagated them through the planning docs:
  - `AGENT/GDD/GDD_10_Roadmap.md` — added a *Locked design decisions —
    2026-05-25 review* subsection to M8, M9, M15 Part A, and the Maps 002–005
    section of M16. Updated the Berserk row in the M8 condition table, the
    `ConditionManager.tick_conditions()` example to show the Poison HP floor,
    the seize-condition description (now references the `can_seize` tag), and
    the checklists for M8, M15 Part A, and M16 to call out the new rules.
  - `AGENT/GDD/GDD_10a_Overview.md` — Bucket C rows for C4 (M8), C5 (M9), and
    C10 (M15 Part A) now name the locks; the Phase 3 Maps row carries the
    Maps 002–005 authoring rules.
  - `AGENT/GDD/GDD_02_Core_Mechanics.md` — *Status Conditions* table and
    storage schema updated for Poison's 1 HP floor (with `can_be_lethal`
    opt-in), Berserk's projected-damage targeting, Silence's tome/staff
    weapon-type filter, and the minimal `{ type, turns_remaining }` schema.
  - `AGENT/GDD/GDD_05_Skills.md` — *Full Skill Reference* preamble carries
    the four M9 rules (M9a/M9b split, strict trigger reuse with flags first,
    hybrid effect calc, Pair Up / Rescue fully deferred).
  - `AGENT/GDD/GDD_06_Maps_Objectives.md` — `seize` and `escape` condition
    entries name the `can_seize` tag and the escape semantics; a new
    *Phase 3 Maps 002–005 — Authoring Rules* section captures the showcase
    plan, single-primary rule, and authored-defeat standard.
  - `AGENT/Docs/hotseat_test_map_plan_2026-05-21.md` — new §8 captures the
    four M15 Part A locks (keybinds skipped, per-map + CLI override, HUD
    text label, `ALTERNATING` fully out).
  - `AGENT/Docs/campaign_rules_firming_notes_2026-05-25.md` — appended a
    *Milestone review locks — 2026-05-25 (afternoon pass)* section with one
    table per milestone listing all 17 decisions.

## Decisions locked this session

### M8 — Status Conditions

| # | Decision |
|---|---|
| 1 | **Poison lethality:** configurable per source — default floors at 1 HP, `can_be_lethal` opt-in. |
| 2 | **Berserk targeting:** highest projected damage → nearest → lowest unit id. |
| 3 | **Silence scope:** tomes and staves only (weapon-type filter; no per-skill flag). |
| 4 | **Condition schema:** minimal `{ type, turns_remaining }`; extend only when needed. |

### M9 — Skill Content

| # | Decision |
|---|---|
| 1 | **Internal M9a / M9b split** (engine then content); public roadmap unchanged. |
| 2 | **Trigger discipline:** strict reuse, flags first. |
| 3 | **Effect computation:** hybrid — dynamic for state/threshold, stored for static passives. |
| 4 | **Pair Up / Rescue:** fully out of M9. |

### M15 Part A — Hotseat

| # | Decision |
|---|---|
| 1 | **Per-player keybindings:** skipped for Part A. |
| 2 | **Hotseat assignment:** per-map data + CLI/dev override; no lobby UI. |
| 3 | **HUD phase banner:** `Faction — Controller` text. |
| 4 | **`ALTERNATING` mode:** fully out of Part A. |

### Objective-Map followup (Maps 002–005)

| # | Decision |
|---|---|
| 1 | **Showcase plan:** one map per primary objective — Seize / Defeat Boss / Escape / Survive-Defend. |
| 2 | **One primary objective per early map.** |
| 3 | **Seizer rule:** per-unit `can_seize` tag (not class, not allowlist). |
| 4 | **Escape semantics:** alive, removed, no further actions this map. |
| 5 | **Authored defeats:** each map must declare ≥1 authored defeat beyond rout. |

## Tests run

- None — documentation-only session.

## Commits made this session

- *(pending)* — single docs commit covering the seven file updates listed in
  *What was done*. Reviewer should sanity-check the locked-decisions blocks
  before committing.

## Plans for next session

1. Land the M9a engine work first behind a minimal authored skill set, before
   bulk M9b `.tres` authoring.
2. When the campaign-rules / save / prep work picks up, return to
   `campaign_rules_firming_notes_2026-05-25.md` to specify Pair Up / Support /
   Rescue against the now-locked M9 boundary.
3. Author Maps 002–005 against the authoring rules now in
   `GDD_06_Maps_Objectives.md`; add the `can_seize` tag on `UnitData` and gate
   the existing Seize action on it.
4. Schedule the M15 Part A live-validation pass once the HUD banner shows the
   new `Faction — Controller` text.
