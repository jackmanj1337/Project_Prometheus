# Session Note - 2026-07-09d

## Summary

While the `v0.3.0.d` focused-rerun build is out for playtest (all four Next Work
Queue gates are blocked on live evidence), picked up the highest-value headless
track from the "While Waiting" queue: the open-registry debt stream, AI axis
(`B5-AI-COMPOSITION`). Landed **build-slice steps 1-2** of
`ai_first_build_design_2026-06-22.md` — the AISpec composition seam that replaces
the two closed AI switches — and **queued step 3** with its save-slice gate.

This clears the recurring `M3` "closed author-facing vocabulary" finding that
every v0.3.0 code review deferred, without touching save schema or front-running
the F1 schema-lock.

## What Changed (commit `a187783`)

- **`scripts/core/AISpec.gd`** (new) — resolved `{activation, disposition,
  engagement}` struct; plain `RefCounted`, no `class_name` (no headless
  class-cache entry needed).
- **`scripts/core/AIProfileRegistry.gd`** (new) — open profile-id -> AISpec
  registry + `is_valid_profile()`/`resolve_ai_spec()`. Adding a profile is one
  entry, not a DataManager/EnemyAI `match` edit (invariant 1). Scoped to the
  three shipped profiles only — no vocabulary opened ahead of its behavior.
- **`scripts/autoloads/DataManager.gd`** — boot validation now queries the
  registry; the closed `_VALID_AI_PROFILES` const is gone.
- **`scripts/core/EnemyAI.gd`** — `_act()` resolves the AISpec and dispatches via
  a `disposition -> handler` table; the inline `basic` body + `_act_passive` +
  `_act_healer` became `_disposition_pursue_unit`/`_disposition_hold_tile`/
  `_disposition_heal`. Behavior + RNG chain unchanged.
- **`scripts/tests/test_ai_profile_registry.gd`** (new, 7 tests) — validation,
  resolution, unknown-profile fallback.
- **Docs (DoD#1):** GDD_08 §Architecture rewritten to the seam; GDD_10 marks the
  AI row in progress and adds a **"Gated build items"** table placing step 3+
  (territorial/tethered/flee/seek_tile + `target_policy` + the `ai_awake` save
  field) behind the Band 1 / F1 save-slice.

## Scope Boundary (why it stopped at step 2)

Steps 1-2 touch no save schema, so they are unblocked. Step 3 needs the
`ai_awake` per-unit (per-`group_id`) snapshot field ([AIP-5]) — a schema-affecting
change that must wait for the F1 schema-lock, not front-run it. The pure
`plan_action(unit, board)` extraction (the deferred action-preview dry-run +
`[VAL]` prerequisite) also rides step 3; the handlers still plan-and-execute
inline for now (invariant 1 satisfied; invariant-4 engagement seam preserved).

## Verification

- `test_ai_profile_registry` 7/0, `test_enemy_ai` 24/0, `test_data_manager` 22/0
  (unknown "berserk" profile still rejected via the registry), RNG determinism
  suites green.
- Pre-commit hook on `a187783`: check_docs 26/26, RNG-usage guard, and the full
  68-suite run all green.

## Next

1. Playtest-gated work (unchanged, still primary): deliver `v0.3.0.d` exe +
   handbook; on return triage `V030-NG-FOCUS` / `V030-DSP-TRACE` logs + the MRD-7
   pick; flip `B1-SUSPEND` / `VAL-V030-GAMEPAD` / `VAL-V023-DISPLAY` on live
   evidence.
2. Open-registry stream can continue headless: next safe target is `[STM]` stat
   registry (`_VALID_STATS` in DataManager) or `[AIP]` `target_policy`
   scaffolding. Objective conditions `[TCV-4]` stay planning-only (pre-F1
   schema-affecting).
3. Resume AI step 3 once the F1 save envelope admits `ai_awake` (see GDD_10
   "Gated build items").
