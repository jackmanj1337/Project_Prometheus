# Session Note - 2026-07-04b

## Topic
Walked the v0.2.5 triage review Q1-Q14 with the owner, then implemented the whole
v0.2.6 fix pass.

## What was done
- **Q1-Q14 walkthrough (owner):** all 14 took the recommended option, plus two owner
  additions — Q2 (also widen the Settings panel) and Q14 (add 10 EXP-grind enemy units
  to Map 950). Recorded in `AGENT/Code Reviews/playtest_v0.2.5_triage_review_2026-07-04.md`
  (flipped to RESOLVED, "Walkthrough Decisions" section) and routed into the triage
  plan. Commit `d42b10c`.
- **v0.2.6 fix pass (built + tested, one commit per workstream):**
  - `45e923e` **E1-E3 + G** — E1 (V025-05a): dropped autowrap on `LabelStats` + added
    `MenuScale.apply_to_deferred` (size after a layout frame, held transparent for the
    flash frame). E2 (V025-05b): level-up clicks handled in `_gui_input` on the STOP
    root; Panel subtree set `mouse_filter=IGNORE`. E3 (V025-05c): PromotionScreen Options
    wrapped in a ScrollContainer + re-apply scale after `_rebuild_options` (also
    ReclassScreen). G (V025-08): `mouse_filter=IGNORE` on the three terrain
    RichTextLabels.
  - `e975f16` **A** — Menu Scale applies on `drag_ended` only (label previews during
    drag); settings h-scroll disabled + panel widened 600→760px.
  - `09415bc` **B1-B2 + Q3** — sheet MainScroll h-scroll off + autowrapped section
    labels; Back/Pair buttons shrink-centered; More-Info reordered numbers-over-prose
    with a full-height prose box.
  - `c049c99` **D3 + C** — AttackPreview stores its anchor defender + `reposition()`,
    wired into MapCursor's zoom hook; `_place_menu_near` caps the placement gap at one
    unzoomed tile + keeps its side sticky across repositions.
  - `38f5639` **E5** — 5th skill (`swordfaire`) on M950_Hero_SkillCap + steel_sword/
    iron_axe; steel_sword on the Lvl19 merc; 10 grind fodder enemies (g1-g10) placed
    along the top/bottom rows.
  - `8fa3109` **F + Q11 + Q13 + Q5/Q6 routing** — `SettingsManager.applied_windowed_size`
    + a "→ applied WxH" label by the Resolution row (V025-06); new
    `display_and_settings_guide.md` (also answers V025-09); the input-routing
    test-fidelity rule in `testing_guide.md` (V025-13); Q5 (forecast-row registry) + Q6
    (green effectiveness default) recorded in Band 5 Slice 3 (not built — no builds
    before that schema lock).
- Every input-routing fix ships a structural-invariant / event-routing test (the Q13
  rule); tests added/updated across level_up, promotion, hud, settings_screen,
  settings_manager, unit_details, map_cursor, attack_preview_position, map_950.

## Commits
`d42b10c`, `45e923e`, `e975f16`, `09415bc`, `c049c99`, `38f5639`, `8fa3109`, + this note.

## Validation
- Full `run_tests.sh` green (ran in each commit's pre-commit hook + a standalone run).
- `gen_docs_index.py` + `check_docs.py` — all 21 checks green.

## Next Session
- **Cut the v0.2.6 build** (build manifest + a v0.2.6 playtest checklist), with a
  prominent top-of-checklist `godot.log` request (exact `%APPDATA%` path) and live
  confirmation lines for the input-routing fixes (level-up click dismissal, terrain
  page-cycling) per the Q13 rule.
- Display rerun: `VAL-V023-DISPLAY` flips only when §1.1/§1.2/§1.3/§1.5/§1.6/§1.8 pass
  live.
- After the playtest thread: resume `[CNC-3]` node type vocabulary.
