# Session Note - 2026-06-30l

## Topic
Wrote the Band 4 convoy and shop economy implementation plans.

## What was done
- Re-read the latest session note (`2026-06-30k.md`), the Band 4 handoff, and
  the convoy/shop read-first docs.
- Added `AGENT/Docs/plans/band4_convoy_implementation_plan_2026-06-30.md` for
  `B4-CONVOY`.
  - Plans the migration from `GameState.party_items: Array[String]` to a
    state-preserving convoy store of `InventoryEntry` instances.
  - Adds slices for `ConvoyService`, capacity/overflow, prep transfer
    operations, the rough PHB convoy panel, shared selector/detail-pane work,
    death-disposition/reward hooks, and migration cleanup.
- Added `AGENT/Docs/plans/band4_shop_economy_implementation_plan_2026-06-30.md`
  for `B4-SHOP-ECONOMY`.
  - Plans resource-keyed shop stock/config, ResourceLedger quote/commit,
    shopper-bound dynamic pricing/conditional stock, buy/sell destinations,
    the rough PHB shop panel, on-map shop trigger integration, and the dialogue
    command hook.
- Updated the Project Control Plane rows for `B4-CONVOY` and
  `B4-SHOP-ECONOMY` to link the new plans.
- Corrected the shop row dependency list to include `B3-RESOURCE-POOLS` and
  `B4-CONVOY`, because the shop plan needs the keyed wallet and convoy overflow
  destination.
- Regenerated the docs index so both new plans are listed.

## Commits this thread
- `f5c2a13` - `Plan Band 4 convoy and shop economy`
- (this note + session index row committed separately)

## Validation
- `python3 AGENT/Docs/gen_docs_index.py`
- `python3 AGENT/Docs/check_docs.py` - all 21 checks green
- `git diff --check` - clean
- Pre-commit reran docs checks and RNG usage guard; docs-only change skipped the
  Godot suite.

## Current State
- Active Band 4 implementation plans now exist for:
  - `B4-IEQ`
  - `B4-MAP-OBJECTS`
  - `B4-CONVOY`
  - `B4-SHOP-ECONOMY`
- Remaining Band 4 plan writing still owed: the smaller grouped consumer plan(s)
  (`B4-PXP`, `B4-DIALOGUE-V1`, `B4-DCH`, `B4-VILLAGE`,
  `B4-RECRUIT-BASIC`, `B4-DIFFICULTY-DEATHMODE`,
  `B4-PREP-DEPLOYMENT`) and the thin `B4-CAMPAIGN-LOOP` integration plan.
- Implementation remains gated behind Band 1-3. `B1-PKGA` Slice 1a remains the
  code start point unless priority changes.

## Next Session Target
Continue Band 4 planning with the grouped consumer plan(s). Recommended next
chunk: a `B4-CAMPAIGN-CONSUMERS` or similarly named plan covering
`B4-PXP`, `B4-DIALOGUE-V1`, `B4-DCH`, `B4-VILLAGE`,
`B4-RECRUIT-BASIC`, `B4-DIFFICULTY-DEATHMODE`, and
`B4-PREP-DEPLOYMENT`, followed by the separate thin `B4-CAMPAIGN-LOOP`
integration plan.

## Read First Next Session
1. `AGENT/Session Notes/2026-06-30l.md` (this note).
2. `AGENT/Docs/plans/band4_implementation_plan_handoff_2026-06-30.md`.
3. The four existing Band 4 plans:
   `band4_items_equipment_implementation_plan_2026-06-30.md`,
   `band4_map_objects_implementation_plan_2026-06-30.md`,
   `band4_convoy_implementation_plan_2026-06-30.md`, and
   `band4_shop_economy_implementation_plan_2026-06-30.md`.
4. Registers for the grouped consumers named above.

## Watchouts
- Do not start Band 4 implementation before Band 1-3 gates land.
- Keep the convoy/shop shared `PanelSelector` overlap explicit so `B6-INPUT`
  does not rebuild the same selector later.
- Shop on-map and dialogue-entry slices must wait for `B4-MAP-OBJECTS` and
  `B4-DIALOGUE-V1` respectively.
