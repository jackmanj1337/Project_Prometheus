# Session Note - 2026-07-24b

## What was done

- Continued the owner design discussion over the campaign-library owner-questions
  packet (dependency-ordered branches A–K). Resolved **Branch F — Runs & saves detail**
  (CL-SAVE-02/03/04/05). Branch F was largely a confirm-and-nail-down pass: the save-type
  taxonomy is already **data-driven** in code (`scripts/resources/CampaignRules.gd`
  `save_slot_classes` + `autosave_rules`) and the persistence/ledger work already drew the
  line between browsable saves and in-memory internals.
- Owner decisions:
  - **CL-SAVE-02** — the run/save browser shows exactly the **persisted** classes: manual
    **Campaign Saves**, **Campaign Autosave**, **Suspend** (presented as *Resume* — single
    slot, `consumed_on_load`, gone after use), plus **completed / imported status records**.
    The **rewind ledger stays hidden** (in-memory, mid-map; recovery-mode only). Reads 1:1
    from `save_slot_classes` + `autosave_rules` + the status store, so the taxonomy stays
    open-registry (a campaign adds a slot class with no engine enum edit). Rejected
    player-facing-only and show-everything.
  - **CL-SAVE-03** — renameable = **run label + manual Campaign Save label**. Autosave and
    Suspend labels stay **system-owned** (pool/keep policy); the **campaign name is fixed**
    (from the pack; the `hash → nickname` map from CL-LIFE-03 covers "which copy"). Consequence:
    a renameable **per-instance label** is a new additive field on the save-index entry and
    run header (slot classes carry only a class-level `label` today).
  - **CL-SAVE-04** — **lean v1**: runs **collapsed + newest-first**, **no search/filter and
    no archive**. Runs leave the list only by delete. Search + archive → **demand-gated
    backlog**; within-run save search graduates first if playtest shows long save lists.
  - **CL-SAVE-05** — per run/save actions = **Resume, Inspect, Rename, Export, Delete**.
    **Archive not adopted** (kept coherent with the no-archive scaling choice). Export is
    the run/save half of the inbox exchange (Branches C/D). **Duplicate deferred to Branch K**.
- No premature standalone tracker rows spun out (folds into the consolidated Library
  implementation pass, like Branch E); the two concrete code touches — per-instance rename
  label, and the browser reading its set from `save_slot_classes`/`autosave_rules`/status
  store — are noted inline in the decisions doc. Only the deferred search + archive work got
  a backlog row. No runtime, scene, save-schema, or release-line behaviour changed — design
  and planning only.

## Commits claimed

- `f9afb5ac1d9f0ffa800362621e5170f58a569424` — Record campaign-library Branch F runs & saves decisions (CL-SAVE-02/03/04/05)

## Gates

- Pre-commit hook: analyzer tests 12/12, scene-integrity PASS, session-claims PASS,
  evidence-matrices PASS, gdscript style PASS (238 files); docs-only change so the Godot
  suite was skipped by the hook.
- Workspace `coordination/check_tasks.py`: 137 tasks valid, no conflicts (container-repo
  commit `d9e93aa` on `agent/from-staging-area/campaign-research-findings-tracker` — advances
  the DISCUSS row to A–F and adds `BACKLOG-RUNSAVE-SEARCH-ARCHIVE-2026-07-24`).

## Next

Resume at **Branch G — Missing / incompatible content** (CL-MISSING-01…05; CL-MISSING-03
direction and CL-MISSING-04 fingerprint case are pre-answered in Branch D). Then H → K. Copy
each branch's ids/answers into `AGENT/Docs/design/campaign_library_ux_decisions_2026-07-24.md`
and create implementation tracker rows only for accepted scope.
