# Session Note — 2026-06-23l  (Items/equipment + proficiency/XP + candidate systems + foundations — design marathon)

**Topic:** Began as the queued ground-up item/equipment review, then expanded (owner-driven) into a
full design marathon: the unified **items/equipment composition model** `[IEQ]`, the **proficiency/XP
framework** `[PXP]`, **resource pools** `[CEX-A]`, the **skill-model expansion** `[SKL]`, a
**feature-dependency atlas + foundations** (F1–F12, with F4/F5/F6/F7/F12 end-shapes decided), a
**player-facing target-coverage walk**, and a **dependency-ordered firming roadmap**. Docs-only;
check_docs **19/19** at every commit, working tree clean. (Spanned into 2026-06-24.)

---

## Part 1 — Grounded audit (start-grounded per the queued brief)
Audited `WeaponData`, `ItemData`, `InventoryEntry`, `ItemHandler`, `CombatResolver`
(`_apply_equip_item_modifiers` @225), the WEXP/rank path in `Unit.gd` (`_can_equip_rank`,
`add_wexp`, `get_active_wexp`), and GDD_04. **Finding:** weapons + WEXP are healthy and match
their "Implemented" claims; **equipment is half-built + model-split**:
1. Two disconnected "equip" reps (`ItemData.item_type=="equip"` vs `InventoryEntry` inline
   4 fields read by CombatResolver — never touches ItemData; no `make_equip()`).
2. "equip" is a misnomer (passive-while-held, no slot/action/exclusivity, unbounded stack).
3. `until_unequipped` orphaned (label rendered, zero producers → bonuses invisible on sheet).
4. Stale comment: `InventoryEntry.gd:21` header conflates accessories w/ forging (the EQP
   register's "no code reads this yet = false" claim was a mis-attribution — that comment is
   on `forged_mods`, which is accurately unread; the real fix is the line-21 header).
5. Named items (Knight Ring move, Iron Rune immunity, Laguz/Wing Guard) exceed the 4 fields.
6. GDD_04 lists equip as "Planned (Phase 2)" — understates the half-built code.

## Part 2 — Deliverable shape (owner-chosen)
Unified **design doc + fresh register** (not piecemeal). Created
`design/items_equipment_unified_model_2026-06-23.md` (the map) + register `[IEQ-1..9]`;
**superseded `[EQP-1..5]`** (Status: Superseded banner, body kept as code-audit provenance,
EQP→IEQ question map recorded).

## Part 3 — Register walk (dependency order, re-evaluating after the keystone)
Owner framing set up front: support **both** held + equipped conferral; legality = parallel
item-proficiency track **+** per-item flags; benefits **differ/expand** by flag/experience.

- **`[IEQ-7]` keystone — COMPOSITION.** Owner went bigger than the 3 drafted options: one
  **`ItemDef` base** + optional `weapon_component`/`consumable_component`/`accessory_component`
  sub-resources; **multi-component allowed**. `InventoryEntry` stays the thin per-slot
  **instance** (`def_id`, `uses_remaining`, equipped pointers, `forged_mods`). Only the
  *definition* layer consolidates. Cleanup wins: `ItemData.item_type` enum dissolves
  (`sellable`/`cost` → base; "key" = no functional component); one DataManager registry +
  `get_item_def(id)`. **Re-evaluated downstream** → reframed IEQ-3 + surfaced 2 net-new Qs.
- **`[IEQ-3]` legality — per-component, independent.** weapon side keeps WEXP+family
  (unchanged); accessory side = new item-proficiency track (`UnitData.item_wexp`,
  reuses `minimum_wexp_for_rank`) **AND** `req_flags`. Item-WEXP gain source **deferred**.
- **Multi-component — ships v1.** Independent concurrent capabilities; **weapon-equipped
  dual item gets its accessory bonus free** (no separate slot).
- **`[IEQ-2]` slots — TYPED.** `accessory_component.slot_type` + per-type capacity map
  (helmet ×1, ring ×2, "not two helmets"). Capacity = **campaign base default → ClassData
  add/remove override**; per-unit slot modifiers **deferred**.
- **Locked w/ recs:** `[IEQ-1]` conferral enum (held|equipped|both); `[IEQ-4]` tier table
  (rank/flag → modifier-set); `[IEQ-5]` general modifiers + effect-hooks (4 flat fields
  migrate in); `[IEQ-6]` wire `until_unequipped` → sheet-visible; `[IEQ-8]` save reservation
  (`def_id`, per-slot-type equipped pointers, `item_wexp`, slot-capacity map); `[IEQ-9]`
  code-debt + GDD_01/04/10 reconcile **with the build**.

**Build is STAGED** (define `ItemDef`+components → migrate weapons → consumables →
accessories), not one drop — protects healthy combat code (~15 weapon + ~11 item call
sites, 11+8 `.tres`, DataManager validators, manifests, tests).

## Part 4 — Effects/XP review → unified Proficiency/XP Framework `[PXP-1..8]` (RESOLVED)
Owner asked to review what benefits/effects/uses items can express + how XP is stored/gained.
**Grounded audit:** three existing effect subsystems the unified model can REUSE — stat
modifiers (`add_modifier`, any UnitData stat, `until_unequipped` wiring = on-equip add /
on-unequip remove, no engine change), weapon `effect_tags` (effective_*/heal; poison/crit-
immunity/ignores-def still unimplemented), and the skill trigger/dispatch system (~18 impl,
~12 stubs) — so accessory effects = **modifiers + skill-grants**, not a new engine. Iron Rune
crit-immunity is the one real gap (needs a combat hook or `on_defend` skill). WEXP today =
`weapon_wexp` dict + hardcoded `WEXP_RANK_THRESHOLDS` (E0..S500) + per-use gain + class caps +
hardcoded S→`s_rank_mastery`.

**Owner expanded scope** → a unified XP/rank framework for **weapons AND items**, firmed as
`[PXP-1..8]`:
- **PXP-1** one `UnitData.proficiency_xp` store (weapon_wexp migrates in).
- **PXP-2** three track kinds: weapon-family / **item-group** (skill-with-shields) / **item-bond**
  (relic); item declares its tracks, can feed both.
- **PXP-3** rank profiles = **CampaignRules** (thresholds **+ names**), named, per-track; default
  profile = E–S (non-breaking).
- **PXP-4** per-threshold **on-crossing events** (grant_skill); S→s_rank_mastery becomes a
  default-profile trigger.
- **PXP-5** author-defined **multi-source gain** (active_use / class_exp_share / per_map_carry /
  action_gated), **track-default + per-item override**; weapons default to active_use.
- PXP-6 caps (profile ceiling + preserved weapon class caps), PXP-7 GDD reconcile (revisits
  GDD_04 SET-004/RULE-004 **non-breakingly**), PXP-8 save reservation.
- **Intentionally revisits firmed weapon WEXP** (owner's call) — non-breaking via the default
  profile. IEQ-3/8 + design doc + scope-map row "WEXP & equipment legality" reconciled to hand
  proficiency/ranks/gain to PXP.

## Part 5 — Training halls + skill effect/trigger coverage
- **`[PXP-9]` Training halls** (RESOLVED) — a 5th, player-driven progression avenue: a **PHB option
  panel** granting authored `{track, xp_amount, resource cost}` into the unified `proficiency_xp`
  store (resource-keyed cost reuses `[SHP-1]`; on-crossing PXP-4 events still fire; eligibility may
  reuse `req_flags`). Shares an `advance_proficiency` path with the **Arms Scroll** item (the
  consumable twin). Scope-map #19 reconciled (proficiency-XP slice firmed; other benefit types open).
- **IEQ design doc §2f — effect/trigger build checklist.** Grounded in GDD_05 (skills owner;
  **trigger discipline** = reuse a wired trigger + `context.flags` before adding one; the defensive
  hook `on_combat_apply_modifiers` is **already wired**) + GDD_10 item semantics. Each drafted item
  → mechanism → the **effect_id** to add (no new triggers needed): `negate_effectiveness`
  (Full/Wing/Laguz Guard, group-param), `negate_crit` (Iron Rune), `advance_proficiency` (Arms
  Scroll), `permanent_stat` (Boots/Dracoshield/etc. — current `stat_buff` is **temporary** only).
  **Two real model gaps flagged:** canto/move-after-action (Knight Ring → M11 mechanic, not an
  effect) + growth-**rate** modifiers (Knight Ward +30% SPD growth ≠ flat stat modifier). Plus
  designer-anticipated gaps: condition immunity, unbuilt weapon `effect_tags` (poison/heal_on_hit/
  ignores_def/always_hits), movement-type grant, conditional stat bonuses. Owners at build:
  GDD_05 (skill effect_ids) / GDD_04+ItemHandler (consumable effect_ids) / GDD_02 (combat hooks) / M11 (canto).

## Part 6 — Model-gap fold + candidate-systems exploration
- **Folded the two model gaps into the build plan** (step 6): growth-rate modifier channel (lands
  with IEQ-5) + canto/M11 dependency (blocks Knight Ring). Marked **priority re-eval pending**.
- **Drafted 5 candidate systems** (owner asked for initial designs + feasibility/scope + a
  player-interaction question list to come back with — NOT firmed):
  **(A)** shared resource pools (stamina/mana/HP cost model on components; Moderate) ·
  **(B)** 3H learned spells (known-list, no inventory slot, per-map uses or pool; learning reuses
  `[PXP-4]`/`skill_unlocks`/`[PXP-9]`; High, feature-sized) ·
  **(C)** author-flexible weapon triangle (move table to `CampaignRules` like PXP profiles; effects
  generalize to stat-mods + condition application; Moderate, condition slice blocked on
  `ConditionManager`) ·
  **(D)** per-map-use items (reuse `skill_use_counters`/`reset_map_state`; Easy) ·
  **(E)** story/plot-item tracking (flag+sell-lock+panel = cheap; branching needs a campaign-flag/
  story-state store that **doesn't exist**; ties `[MET]`). Captured in
  `design/candidate_systems_2026-06-23.md` + `[CEX-1..16]` OPEN; scope-map #22 added.
- **Cross-cutting deps surfaced for the re-eval:** `ConditionManager` (stub) wanted by C + the
  poison/immunity gaps → trending **foundational**; A underpins B; the missing **story-state store**
  gates E's branching.

## Part 7 — Feature dependency atlas + foundations (planning for the scheduling session)
Owner: "build out all the plans so we know how big each feature is, group by what systems they
rely on/modify, start fleshing out foundations + decide end-shapes" — scheduling/priority itself
is a **later** session.
- **`plans/feature_dependency_atlas_2026-06-23.md`** — sizing+dependency atlas (NOT full per-feature
  plans; those come at scheduling). Every planned feature grouped into clusters A–G by foundation,
  T-shirt sized (S/M/L/XL). **11 foundations (F1–F11)** tabled with state + what-rests-on-them.
  Key signal: 4 undecided foundations gate the most; F4 is cheap+broad → start there.
- **Decided 3 foundation end-shapes** (owner) → `design/foundations_end_shapes_2026-06-23.md`:
  - **F4** = ONE generic CampaignRules **author-profile mechanism** (rank/triangle/pool/difficulty
    are *types*; default profiles non-breaking). Cheap, high-leverage.
  - **F5** = **FULL author-extensible status system** (data-defined `ConditionData`: effect kinds,
    tick, stacking, immunity, sheet display; built-in 5 + author additions). Load-bearing for
    triangle-conditions / poison / immunity / debuff staves.
  - **F6** = **generic campaign-flag/variable store** (key→value, save-persisted; MET/recruit/
    branching/story-items read+write). The single unlock for narrative branching.
- **Still-open foundations: F7** (resource pools — decide with the learned-spell `[CEX-A]` question)
  **+ F10** (canto scope). F1 (save schema) flagged as "lock reserved fields first."

## Part 8 — Player-facing target coverage walk + dependency-ordered firming (start)
Owner: "walk all player-facing features, make sure we know what targets we need to support."
- **Coverage walk** (GDD_Feature_Index 17 groups + 2 backlog + scope-map worklist + this session):
  **firmed core has known targets** (combat/progression/map/items/economy/hub/save/UI/AI + F4/F5/F6);
  the **gap** = the v1 worklist #4–12 (recruit, support, rescue, dancer, canto, staves, village,
  difficulty, PvP) + DISCUSS #14–20, which carry **only one-line intent, no firmed target**.
- Owner: **firm them in dependency order, ⚠ list first.** Captured a **target-firming roadmap**
  (atlas §3b): Wave 0 = open foundations (F1 schema-lock, F7 pools, F10 canto); Wave 1 = ⚠ CEX items
  (triangle/per-map/spells-after-pools/story) + M9b; Wave 2 = ✗ v1 worklist dep-ordered.
- **Firmed resource pools `[CEX-1..4]` = foundation F7** (first roadmap item): CampaignRules pool
  types (F4) + class/unit maxes; component AND skill declare `{pool, amount}` (multi-pool ok); gates
  weapon/spell use **+ skill activation**; sheet+preview surface, cost pre-commit; **author-selected
  refill mode** (per-map reset v1 + "persist until author refills" for back-to-back climaxes) +
  `restore_pool` items (mana vulnerary) + Regen skills. **Only F10 (canto) remains open** of F1–F11.

## Part 9 — Skill-model expansion `[SKL-1..6]` (RESOLVED) + foundation F12
Owner: add personal skills (own sheet slot), class-level-gated class skills, and add/remove special
skills on triggers (story/shop/item/skill/attack). Grounded in the existing model (`skills` capped +
`earned_skills` pool + `mastery_skills` always-on-no-cap + `ClassData.skill_unlocks` level→skill).
Firmed:
- **SKL-1 universal rule:** a skill the player **can't unequip via the standard menu** → its **own
  sheet category**, **not counting against `max_skills`** (cap = player-managed equipped only).
  Categories: Equipped (capped) · Personal · Class · Mastery · Granted (distinct sheet sections).
- **SKL-2 personal_skills** — authored, always-on, own slot, no cap, changeable only by trigger.
- **SKL-3 class skills** — extend `skill_unlocks` with **two author flags** per unlock:
  `retain_on_reclass` (kept vs lost) + `requires_equip` (slot vs always-active); available = current
  class unlocks at/below current level.
- **SKL-4 grant/revoke API** (= **foundation F12**) — general `grant_skill`/`revoke_skill` (Granted
  category, permanent or duration-bound) from story events (F6/MET), shops, item effect_ids,
  skill-grants-skill, attack on_hit/on_kill, and `[PXP-4]` crossings.
- **SKL-5/6** save reservation (`personal_skills`/`granted_skills` persist; note `mastery_skills` is
  snapshot-only today) + GDD_05/03/01 reconcile with build.
Atlas gains **F12** + a skill-model row; scope-map skills row updated (commit `46f9a77`).

---

# THINGS TO FIRM UP NEXT  (the forward queue — atlas §3b is the live ordering)

**Firmed THIS session (done):** `[IEQ-1..9]` items/equipment composition · `[PXP-1..9]` proficiency/XP
+ training halls · `[CEX-1..4]` resource pools (= F7) · `[SKL-1..6]` skill model (= F12). Foundation
end-shapes decided: **F4, F5, F6, F7, F12**.

### Wave 0 — open foundations (do first; gate the most)
- **F10 — Canto / move-after-action** — scope (which classes/effects grant it; action-flow shape).
  Gates Rescue (#6) + Knight Ring. **Only foundation of F1–F12 still undecided.**
- **F1 — Save-schema lock** — consolidate + reserve every field this session generated before any
  build reserves ad-hoc: `proficiency_xp`, equipped-slot pointers, accessory slot-capacity map,
  **pool state**, `known_spells`, condition state, **campaign flags (F6)**, `personal_skills`,
  `granted_skills`. (`mastery_skills` is snapshot-only today — these need real persistence.)

### Wave 1 — ⚠ candidate systems (deps now decided)
- **Flexible weapon triangle** `[CEX-9..12]` — deps F4✓/F5✓; **ready now**.
- **Per-map-use items** `[CEX-13]` — dep IEQ✓; small.
- **Learned spells (3H-style)** `[CEX-5..8]` — now unblocked by F7✓; feature-sized (XL).
- **Story items** `[CEX-14..16]` — tracking ready; branching after F6 build.
- **Per-skill UX (M9b)** — content behavior/feedback for the skill set.

### Wave 2 — ✗ v1 worklist (dependency-ordered)
Difficulty modes (#12, dep F4✓) · Dancer/refresh (#8) · Movement assists (#17) · Utility staves (#10) ·
**Canto (#9 = F10) → Rescue (#6)** · Recruit/Capture (#4, after F6) · Village/house visit (#11, MET✓+F6) ·
Support system (#5, XL) · PvP/scenario (#7) · Arena (#14) · Bonus-EXP (#18) · Combat arts (#15, after F7✓) ·
Battalions/gambits (#16) · Avatar/My Unit (#20).

### Track B — older OPEN registers + gates (pre-existing, need owner input)
- `[CST-1..13]` save-cluster **technical** decisions · `[IMP-1..6]` map-sprite importer ·
  `[LEG-1..5]` legal/licensing (**needs owner: source corpus**) · `[REN-1..5]` public-identity rename
  (**needs owner: names list**). Forging (M10) stays a deferred target.

---

## Next session — STAGED BUILD of the unified items/equipment model
The model is firmed; next is implementation, phased to protect healthy code:
1. **Define** `ItemDef` base + `WeaponComponent`/`ConsumableComponent`/`AccessoryComponent`
   resources; one DataManager registry + `get_item_def(id)`; `InventoryEntry.def_id`. Tests.
2. **Migrate weapons** → `weapon_component` (fields unchanged); update CombatResolver/Unit
   call sites + `.tres` + manifests; keep `_can_equip_rank` legality intact.
3. **Migrate consumables** → `consumable_component` (ItemHandler effect dispatch).
4. **Build accessories** → `AccessoryComponent` (conferral, typed slots, item-proficiency
   track + req_flags, tier table, modifier+effect-hook model = reuse modifiers + skill-grants,
   `until_unequipped` wiring = on-equip add / on-unequip remove); retire the inline 4 fields;
   reserve §2 save fields; status-fix GDD_04 + fix the `InventoryEntry.gd:21` header. DoD#1+#2 each phase.
5. **Proficiency/XP Framework `[PXP-1..9]`** (interleaves with the above): unified
   `proficiency_xp` store (migrate `weapon_wexp`) + CampaignRules **rank profiles** (default =
   E–S, non-breaking) feeding `weapon_rank_for_wexp`/`minimum_wexp_for_rank`; then the
   multi-source gain model (active_use first = current behavior, then class_exp_share via
   `add_exp`, per_map_carry via map-completion, action_gated via action execution); then
   item-group + item-bond tracks; on-crossing `grant_skill` triggers (move `s_rank_mastery` to a
   default-profile trigger); training-hall panel (`[PXP-9]`) + `advance_proficiency`. Revisits GDD_03/04 non-breakingly.
6. **Two model-gap prerequisites** (folded in 2026-06-23l; **priority re-eval pass pending**):
   - **Growth-rate modifier channel** — a 2nd modifier kind affecting level-up rolls
     (`growth_rates`/`growth_accumulators`), distinct from flat `active_modifiers`. Needed by
     Knight Ward (+30% SPD growth) and any growth-affecting accessory/effect. Lands with the
     IEQ-5 effect model (step 4); until then growth effects stay consumable/weapon-only.
   - **Canto / move-after-action mechanic (M11)** — a turn/action-flow capability; Knight Ring
     and any "move after acting" effect **grant** it, they don't implement it. Knight Ring is
     **blocked** until canto lands. Tracked as an M11 dependency, not an accessory build task.
**Reconcile-don't-relitigate:** weapon triangle/WEXP (GDD_04), `break_behavior` (`[BWN]`),
convoy `Array[InventoryEntry]` (`[CNV]`), resource-keyed `cost` (`[SHP-1]`); forging (M10)
stays deferred + distinct (`forged_mods` overlays `weapon_component`).

**Reconcile-don't-relitigate:** weapon triangle/WEXP→`[PXP]` (GDD_04), `break_behavior` (`[BWN]`),
convoy `Array[InventoryEntry]` (`[CNV]`), resource-keyed `cost` (`[SHP-1]`); forging (M10)
stays deferred + distinct (`forged_mods` overlays `weapon_component`).

---

## Commits (chronological; session-note commits omitted)
1. `f402a65` / `c5a279c` — `[IEQ-1..9]` open + resolve (composition model); supersede `[EQP-1..5]`
2. `062fbd9` — `[PXP-1..8]` proficiency/XP framework; reconcile IEQ + scope map
3. `c037649` — `[PXP-9]` training halls + IEQ §2f effect/trigger checklist
4. `ebb23d1` — fold model-gap prerequisites (growth-rate channel, canto/M11) into the build plan
5. `9b63c41` — draft 5 candidate systems + `[CEX-1..16]` open questions
6. `416cb56` — feature dependency atlas + foundations (F1–F11)
7. `1aa4fd4` — decide F4/F5/F6 foundation end-shapes + initial designs
8. `3e986a4` — target-firming roadmap (atlas §3b)
9. `cdbfd16` — firm resource pools `[CEX-1..4]` = foundation F7
10. `46f9a77` — firm skill-model `[SKL-1..6]` (personal/class-level/granted) + foundation F12
(+ interleaved session-note commits, and the close-out commit for this cleanup.)

## Verification
`check_docs.py` **19/19 PASS** at every commit; manifests regenerated each register; RNG guard PASS;
no `__pycache__`; working tree clean. Branch `docs-reorg-2026-06-23`.
