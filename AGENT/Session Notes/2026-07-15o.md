# Session Note - 2026-07-15o

## What was done

- Completed the campaign archive pipeline with deterministic ZIP export from a
  validated pack tree.
- Export derives its allow-list from the manifest, Tier-2 catalogue, and
  approved `assets/` media, writes normalized entries in lexical order, and
  excludes saves, suspend state, caches, and unrelated files by construction.
- Every produced artifact must pass `CampaignArchivePreflight` before it is
  finalized.
- Added tests proving repeated exports are byte-identical, central-directory
  order is lexical, forbidden state/cache files are absent, missing required
  content produces no artifact, and export/preflight/install preserves every
  admitted byte.

## Next

Implement installed-pack discovery and validated summary caching, expose packs
through explicit New Game selection, and activate the chosen source only through
the existing `DataManager` seam. Persist selected pack identity in campaign
saves; installation itself must remain inert.

