# Session Note - 2026-06-30b

## Topic
Band 1 implementation-planning handoff.

## Purpose
Next session should write the implementation plan for the next few Band 1
steps, not reopen the already-ratified designs. The plan should turn the
existing `B1-PKGA` / `B1-F1` / `B1-SAVECODEC` / `B1-CST` source docs into a
code-ready execution sequence.

## Start Point
- Active queue now starts at `B1-PKGA` in `AGENT/GDD/GDD_10_Roadmap.md`.
- Control-plane source of truth:
  `AGENT/Docs/plans/project_control_plane_2026-06-29.md`.
- Band 0 rewrite/feature-index cleanup is done; do not spend next session
  re-reviewing it unless new drift is found.

## Read First
1. `AGENT/Session Notes/2026-06-30.md` and this note.
2. `AGENT/GDD/GDD_10_Roadmap.md` -> Next Work Queue.
3. `AGENT/Docs/plans/project_control_plane_2026-06-29.md` -> Band 1 rows.
4. `AGENT/Docs/design/rng_determinism_design_2026-06-11.md`.
5. `AGENT/Docs/registers/package_a_rngservice_open_questions_2026-06-21.md`.
6. `AGENT/Docs/design/f1_save_schema_lock_design_2026-06-28.md`.
7. `AGENT/Docs/design/f1_save_schema_manifest_contract_2026-06-28.md`.
8. `AGENT/Docs/plans/f1_schema_source_inventory_2026-06-28.md`.
9. `AGENT/Docs/plans/campaign_save_technical_plan_2026-06-21.md`.
10. `AGENT/Docs/registers/campaign_save_open_decisions_2026-06-21.md`.

## Recommended Deliverable
Write one short active plan in `AGENT/Docs/plans/`, tentatively named
`band1_determinism_save_implementation_plan_2026-07-XX.md`.

Recommended shape:
- frontmatter: `Type: plan`, `Status: Active - implementation plan`,
  `Last verified: <date>`
- scope and non-goals
- source docs and decisions not to reopen
- code-grounding checklist
- ordered implementation slices
- tests per slice
- doc/update obligations per slice
- open questions, only where a decision is genuinely still missing

After adding the plan, run `python3 AGENT/Docs/gen_docs_index.py` and commit the
generated index with the plan.

## Plan Scope To Write

### Slice 1 - `B1-PKGA` Step 1
Plan the `RngService` autoload and migration sweep.

Use the existing RNG design as the build contract:
- add `scripts/autoloads/RngService.gd`
- register it after `EventBus` and before `GameState`
- implement deterministic mix/hash, `begin_event`, `commit_event`,
  `to_save_dict`, and `from_save_dict`
- migrate hit, crit, level-up growths, and skill activation off raw engine RNG
- ship two-RN hit with Step 1 per `[PKGA-4]`
- add/extend the raw-RNG guard so future gameplay RNG cannot bypass
  `RngService`

The plan must identify the exact code files and tests to touch before coding.
Expected code targets include `project.godot`, `CombatResolver.gd`, `Unit.gd`,
`SkillHandler.gd`, and the test/CI scripts.

### Slice 2 - `B1-PKGA` Step 2
Plan the snapshot contract needed before campaign/save work.

Recommendation: keep this slice narrow. Add `RngService` state to the existing
Retry/snapshot path so deterministic replay survives snapshot/restore. Do not
build the whole `SaveCodec` here; that belongs to `B1-F1` / `B1-SAVECODEC`.

The plan should name the specific deep-equal/replay tests that prove
`map_seed` and `history_hash` restore correctly.

### Slice 3 - `B1-F1`
Plan the F1 save-schema manifest lock.

This is a docs/schema lock before feature code starts adding save state. The
implementation plan should say how to turn the source inventory into a manifest
with exact paths, scopes, defaults, serializers, fixtures, and explicit
no-save rows.

Use the recommendations already in `f1_save_schema_lock_design_2026-06-28.md`
unless a real blocker appears:
- active conditions default to map/suspend state
- cross-map condition persistence only if condition data declares it later
- action/rate-limit counters get `map_runtime`/suspend reserves
- `campaign.pvp` may be a dormant optional block
- `conversations_seen` stays no-save unless direct non-MET invocation becomes v1
- runtime facts live in `map_runtime`; resume/UI facts live in `suspend`

### Slice 4 - `B1-SAVECODEC` / `B1-CST` kickoff
Plan only the kickoff sequence, not the whole campaign loop in one step.

Recommended order:
1. `SaveCodec` JSON-safe primitives and unit/inventory round-trip fixtures.
2. `SaveData` envelope and old-save default fixtures.
3. Retry snapshot rebuild onto the shared codec.
4. CampaignRules consolidation plan for the loose `GameState` rule fields.
5. Campaign graph / prep / results flow planning as later CST slices.

Keep `B1-SUSPEND` and `B1-CAMPAIGN-RULES-SAVE` as follow-on slices unless the
owner explicitly asks to fold them into the first implementation plan.

## Decisions Not To Reopen
- `[PKGA-1]`: Package A is the first execution milestone.
- `[PKGA-2]`: only Package A Steps 1-2 gate campaign/save.
- `[PKGA-3]`: `rewind_charges` lands with CampaignRules consolidation, not
  Package A Step 1.
- `[PKGA-4]`: two-RN hit ships with Package A Step 1.
- `[CST-12]`: Package A precedes the campaign/save spine.

## Questions For The Owner Only If Needed
1. Should the next-session output be one combined Band 1 plan or separate
   per-track plans?
   Recommendation: one combined Band 1 execution plan with clear slices.
2. Should coding start in the same session after the plan is written?
   Recommendation: only if the plan is accepted and the first slice is small
   enough to complete with tests and docs in the same turn.

## DoD For The Next Session
- New plan added under `AGENT/Docs/plans/`.
- `AGENT/Docs/INDEX.md` regenerated in the same change.
- Control-plane rows updated only if build source/status changes.
- If behavior changes during coding, update the affected `GDD_01`-`GDD_08`
  section and matching control-plane row in the same commit.
- Run `python3 AGENT/Docs/check_docs.py` and `git diff --check`.
- Add a session note and newest-first index row before stopping.

## Commits This Thread Before This Handoff
- `d2eb897` - `Close Band 0 tracking review`
- `ab69a14` - `Record Band 0 tracking review session`

## Next Session
Write the Band 1 implementation plan first. The first code target after that is
`B1-PKGA` Step 1: `RngService` plus the raw-RNG migration sweep.
