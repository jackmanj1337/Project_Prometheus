# Session Note - 2026-07-15a

## What was done

- Reconfirmed the v0.4.0 Windows playtest has **not** returned (newest commit on
  any branch is still `69e5d22` from the prior 2026-07-15 design session), so this
  work was not preempted.
- **Turned the persistence/undo design handoff into a sequenced BUILD/SCRAP
  implementation plan** — the job the design session
  (`persistence_undo_unified_handoff_2026-07-15.md`, commit `62bd043`) left for
  this one. New doc:
  `AGENT/Docs/plans/persistence_undo_implementation_plan_2026-07-15.md`.
- **Re-verified the handoff's code seams against the branch tip** before writing
  (the handoff explicitly said not to trust its line numbers). Confirmed and
  anchored:
  - `capture_suspend_save` (`GameState.gd:474`) is the suspend-complete
    serializer; `take_map_snapshot` (`:735`) is party-only (roster + gold + items
    + pair-up + RNG). The party-vs-all-factions gap is exactly what Phase 1 closes.
  - Retry's only consumer is `GameOverScreen.gd:211-212`; the snapshot is taken at
    `GameMap.gd:130`.
  - The suspend storage API to scrap is all present:
    `SaveManager.gd:15/17/39/76/82/88` + the `LAST_PLAYED_SUSPEND` branch
    (`:52`, `:341`). `is_valid_slot_id` (`:113`) is the keeper.
  - `CampaignRules.gd:41` has only `rewind_charges_per_map` (authored-only, **no
    consumer** — rewind genuinely unbuilt); the file has **no `to_dict`/`from_dict`**
    — the rules codec lives in `GameState._campaign_rules_to_dict`, so new budgets
    must be wired there.
  - The Control Plane has **no** dedicated persistence/undo row (parked under
    `B1-PKGA`/`B1-CST`), confirming the "formalize the tracker row" step.
- **Plan shape: six phases.** Phase 0 housekeeping (add a proposed `B1-LEDGER`
  control-plane row + the Phase-1 byte measurement that gates Rewind) prepended to
  the handoff's five design steps. Each phase carries verified anchors, an explicit
  SCRAP list, an exit test, and a per-commit DoD. **Phase 3 (Rewind) is the single
  deferrable unit**, gated on measuring one serialized board snapshot in Phase 1;
  Phases 1/2/4/5 each leave the game green independently. Recommended order
  0 → 1 → 2 → (4 → 5), with 3 slotted after 2 only if the measurement clears.
- **Sequencing call flagged in the plan:** land Phase 1-2 before or with B4
  Slice 2 (the Retry→prep reroute, design-only commit `1df833e`), because Retry
  becomes a ledger read — writing the reroute against the old
  `restore_map_snapshot` path would mean re-plumbing it.
- Registered the plan in the doc role manifest ownership map (check 30), regenerated
  `INDEX.md`. Gates green: `check_docs.py` PASS (all 31 checks). No code touched, so
  the Godot suite was not the gate this commit.

## Commits

- (pending) — Add the persistence/undo sequenced implementation plan + role-manifest
  ownership entry + regenerated INDEX.

On `agent/claude/2026-07-14/prep-deployment` (tip was `69e5d22`). Docs-only. No PR
opened; human review/merge remain manual.

## Next session

Two independent tracks are now queued; pick per what the user wants:

1. **Build the persistence/undo plan** — start at Phase 0 (land the `B1-LEDGER`
   control-plane row) then Phase 1 (generalize the snapshot to suspend-completeness
   + take the byte measurement that decides Rewind's fate). Everything reads Phase 1,
   so it is the true foundation.
2. **B4-PREP-DEPLOYMENT Slices 2-3 still open** (PrepScreen destination + reroute
   `CampaignManager.launch_current_node`; manual save over `write_campaign_slot`).
   Note the interaction: B4 Slice 2's Retry reroute wants persistence Phase 1-2
   landed first (see the plan's guardrails), so if both are in scope, sequence the
   ledger foundation ahead of the reroute.
3. A returned v0.4.0 playtest still preempts either track and belongs on the v0.4.0
   build branch.
4. Unchanged standing item: the Load Game picker has still never been rendered live
   — fold into the next Windows playtest.
