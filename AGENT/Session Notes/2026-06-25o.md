# Session Note — 2026-06-25o  (A4 cont. — Recruit conversation side `[RCV-1..6]` + dialogue foundation F15)

**Topic:** Continued the **A4 walk** with the **recruit/capture conversation + flag side** (#4 ⇄ A3),
end-shape-first. Firmed `[RCV-1..6]` and — because the `dialogue` action needs a conversation system
that doesn't exist — pinned a **rough forward end-shape for a Dialogue/Conversation system as new
foundation F15**. Branch `docs-reorg-2026-06-23`.

**Outcome:** new register `[RCV-1..6]` **RESOLVED**; foundation **F15** added to atlas §1 (🔶 rough,
not fully firmed); atlas critical-path note + recruit row + A4 progress note updated; INDEX/REGISTERS
regen; **19 `check_docs` green**. 1 commit (`6412b7c`).

---

## 1. Method & owner calls
End-shape via questions first. Owner: dialogue = **"pin a rough plan so we build a useful foundation,
not something to be replaced"** (design end-shape, defer build); talk direction = **author's choice
per recruit**; damage-forfeit = **author-composed MET condition only**.

## 2. Verdict
**The recruit conversation side is thin** — a `talk` config of the `[VIL-2]` interactive-trigger
substrate + a `recruit` MET action over the `[RCR-3]` API + author-composed conditions. **The one
substantial new thing — the Dialogue/Conversation system — is NOT recruit-specific:** it's a shared
foundation (recruit · village `[VIL-4]` · support `[REL-6]` · MCH name-sub · story). **Roster side
`[RCR-1..7]` NOT relitigated.** Code check: no dialogue system exists anywhere; F13 was the only
related layer and explicitly waited on "the dialogue-data format" — F15 is that format.

## 3. The register `[RCV-1..6]`
- **RCV-1 (keystone)** Dialogue/Conversation system = **new foundation F15** (rough end-shape, build
  deferred). Conversation = id-referenced ordered list of **forward-compatible entries**: `line` (v1)
  | `choice` (reserved — player options→flag/jump) | `command` (reserved — run a MET action mid-scene).
  `line` = {speaker (unit_id|named-key), text (**F13 key, never concatenated**), portrait/expression,
  side}. Played by a `dialogue` MET action by `conversation_id`. **Data format + action hook now;
  presentation deferred; choice/command reserved so v1 line-only data survives.** Realizes F13's
  deferred dialogue-data format. Shared by recruit/village/support/MCH/story.
- **RCV-2** `talk` trigger = a **`[VIL-2]` config** (unit-targeted twin of village Visit); reuse, not
  a new trigger. Firing-conditions ride MET + F6 (`[RCR-4]`).
- **RCV-3** directionality = **author's choice per recruit**: `directed` (named recruiter must be the
  actor) | `symmetric` (either initiates; condition checks the pair).
- **RCV-4** `recruit` MET action = calls `[RCR-3]` `recruit(unit)`; **trigger-agnostic** (talk /
  village Visit / turn_reached / flag), talk = canonical interactive case; may chain `dialogue` +
  `flag`. `capture` twin's carry stays A2; combat non-lethal = `[STY-6]`.
- **RCV-5** damage-forfeit = **author-composed MET condition only**; no baked rule, no engine
  `was_attacked_by_player` flag.
- **RCV-6** F1/save: nothing new beyond `[RCR-7]`; one-time dialogue rides the event's
  `map_events_fired` `once` latch — reserve `conversations_seen` **only if** conversations become
  directly invokable (flagged, not added). Directionality flag + conversation data = authoring.

## 4. Commit
`6412b7c` — A4 recruit conversation side firmed `[RCV-1..6]` + dialogue foundation F15 (register +
atlas F15/critical-path/recruit-row/A4-note + INDEX/REGISTERS regen; 19 checks green).

## 5. NEXT SESSION — finish the A4 walk
- **`[STY-17]` provoke-MET action** (task #3) — a MET action that sets a faction **relationship** +
  AI neutral→hostile "provoke" transition. Consumes the F1 runtime-relationship-override reserve.
  Ground in the directed 3-state faction relationship matrix (`relationship()`/`are_hostile`, A1) +
  the AI `set_ai` action precedent (`[MET-3]`/`[AIP-8]`).
- **Capture-victory pin** (task #4) — **mostly absorbed by `[VIL-8]`** (objective removal-disposition
  rule). Remainder is the build-side `ObjectiveCondition.type=capture` + `captured:<id>` flag.
- **Trajectory:** finish A4 → **A5** (death/removal disposition co-owned by `[VIL-8]` + `[BAT-16]` +
  death-inventory; loadout cap; `[AGT §6]` non-combat EXP) → **F1 schema-lock**. **Define-all sweep
  before F1 now also owes F15 (dialogue v1 schema)** alongside F14 (`[STM]`) + `[BAT-14..16]`. **No
  builds until F1 is planned.**
