# Session Note — 2026-06-27a  (Prediction-engine review → full perception/masking walk `[PER-1..12]` + contest-system durability fix)

**Topic:** Answered the teed-up prediction-engine detour (note 26j), then walked the whole
**perception/masking** design to a resolved register. Branch `docs-reorg-2026-06-23`. Design-capture only
(nothing built; no GDD/roadmap behavior change).

---

## 1. What was done

**Prediction engine reviewed.** It = `CombatResolver.preview_combat()` (scripts/core/CombatResolver.gd:553)
→ `_collect_combat_modifiers(context, preview=true, dry_run=true)` (:134). The "hide-from-forecast,
fire-for-real" split already exists at `SkillHandler.apply_trigger:172` (random-activation skills excluded
from preview). **Correction to a standing assumption:** the AI does **not** read the forecast today —
`EnemyAI` picks targets by **nearest distance** (`_find_nearest`), no scoring. So forecast-fidelity as an
AI lever is downstream of a not-yet-built valuation AI (`[CVR-4]`/`[RCT-1]`).

**New register `[PER-1..12]`** — `registers/perception_masking_open_questions_2026-06-27.md`, **RESOLVED**
(only PER-4 inert, see §3). The model: AI/player decision = a **3-stage pipeline**, manipulate inputs at
one stage:
- **Stage 1 ENUMERATION** (`GridManager.get_attackable_enemies_from_tile`) — the ninja "isn't a target."
  Hard mask; **works vs today's AI** (shippable now).
- **Stage 2 VALUATION** — the defender "looks juicier than the wizard" (`ai_target_weight`). **INERT**
  until the valuation AI exists.
- **Stage 3 FORECAST** (`preview_combat` + the preview branch) — blind the exchange outcome. Stage 3
  already exists.

Key resolutions: knob lives on the **perceived unit** (PER-2); hard flag + soft weight both (PER-3/4);
**PER-7** reveal = **union** of winning observers' share-scopes (additive → no precedence); **PER-8**
occupancy in v1 (`stealth_pathing: around|through` + `on_cross`/`on_stop`, all reusing the **Displacement
framework** `[DSP-1/2/12/14]` — no new collision code); **PER-9** forecast fidelity = a **two-channel
CampaignRules constant** (channel A = what humans see, channel B = what the AI evaluates, may be equal) +
a **debug `forecast_reveal_all` override**, sibling of `[FOW-3]` `ai_respects_fog`; **PER-10** perception
filters projection inputs only, **never `resolve_combat`**; **PER-11** no AI softlock (verified) but the
**two-hook finding** (mask must filter BOTH `_living_hostiles_for_faction` approach AND
`get_attackable_enemies_from_tile` attack); **PER-12** detection (find the hidden) vs appraisal (judge the
disguised) = **two F16 contest axes** — author points them at one term (unified "perception") or two
(`sight` vs `insight`).

**THE durability fix — the contest system.** The pierce-vs-stealth contest was re-derived from scratch
**3×** in one session before we found it: it = **`[REQ-10]` `chance`** in the **F16 Requirement system**
(`requirement_predicate_system_open_questions_2026-06-25.md`), a resolved "shared contest/check gate." It
got lost because it's filed under "Requirement/Predicate," not "contest." Durability measures landed:
bidirectional cross-refs (PER↔REQ, PER↔FOW), a DO-NOT-RE-DERIVE callout in the PER header, and **two
durable auto-memories** (`reference-contest-chance-primitive`, `project-perception-masking`).

**Campaign-creator-guide note.** Added a "Deceiving the player" section to
`guides/map_authoring_guide.md`: to replicate the deceit **against the player** with today's primitives —
**(1)** real-unit **swap via switch-teleport** (decoy out, true unit in at the reveal), or **(2)** a **MET
map-event condition change** at the reveal moment. Swap when *identity* changes, condition-change when the
same unit's *threat* changes. Prefer engine-native `[PER-9]`/`[PER-12]` for runtime deceit once built.

## 2. Commits this session
- `c3ae39a` Add `[PER-1..11]` register; wire contest to F16/`[REQ-10]`
- `ec41082` Settle `[PER-7/8/11]`
- `aab86ea` Resolve `[PER-8/9]` (occupancy in v1 + two-channel fidelity)
- `48849bf` Add `[PER-12]` (detection vs appraisal)
- (this note + the map-authoring-guide section) — see HEAD
All commits: INDEX/REGISTERS regenerated, 19 doc checks + RNG guard green.

## 3. State / dependencies
- `[PER]` is RESOLVED **except PER-4** (the valuation-bias lever), which is a *dependency* not an open
  question: **inert until the forecast-driven valuation AI `[CVR-4]`/`[RCT-1]` exists.** PER-12's appraisal
  axis also lands its value with that AI.
- Two build-time revisits: PER-9's shared display widget (lands with FoW / the §2 CampaignRules
  consolidation) and a PER-11 all-masked-board test.

## 4. NEXT SESSION (owner plan: clear context first, then)
**Return to the AI / prediction model** — review how it works and see if it can be improved / given more
detail. This is the **pre-A5 valuation-AI path** (`[CVR-4]`/`[RCT-1]`) that PER-4 + PER-12 both wait on;
the whole perception detour fed straight into it. Grounding: `EnemyAI.gd` (nearest-target today, no
scoring), `CombatResolver.preview_combat()` (the forecast the upgraded AI should read), `[CVR-4]`/`[RCT-1]`.
