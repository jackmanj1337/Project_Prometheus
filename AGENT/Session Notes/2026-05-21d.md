# Session Notes — 2026-05-21d

## Context

User asked how the planned map selector should handle rosters, requested the docs
 be updated to define that behavior explicitly, and then asked to begin
 implementing the selector.

## What Was Done

- Clarified the selector plan so registry entries now explicitly carry
  `roster_policy`, with the first-pass selector using `default_roster` unless a
  map opts into a different rule.
- Added the first selector foundation to `NewGameScreen`:
  - a map `OptionButton`
  - two initial selectable maps (`map_001`, `map_001_c3_factions`)
- Added `GameState` launch-state fields and `configure_next_map()` so the
  selected map path and roster policy survive the menu handoff into `GameMap`.
- Updated `GameMap` to honor `GameState.next_map_data_path` before falling back
  to its exported `map_data_path`.
- Extended `test_new_game_screen.gd` and `test_game_state.gd` for the new
  selector foundation.

## Verification

- `godot --headless --path /workspace --script res://scripts/tests/test_new_game_screen.gd`
  passed: 4 passed, 0 failed.
- `godot --headless --path /workspace --script res://scripts/tests/test_game_state.gd`
  passed: 15 passed, 0 failed.
- `bash run_tests.sh` passed: all suites green.

## Commits

- `49f7420` — Clarify selector roster policy
- Pending

## Plans for Next Session

- Move the hardcoded selector entries into a dedicated registry source.
- Author `map_900_hotseat_validation` and add it to the selector.
- Expand the selector once the validation map exists to include per-entry
  descriptions and future roster policies like `fixed_test_roster`.
