# Session Note — 2026-06-27b  (AI / prediction model review → AI Combat Valuation / Engagement Brain walked, `[VAL-1..13]` RESOLVED)

**Topic:** Returned to the owner's teed-up "review the AI / prediction model" plan. Reviewed the current
AI end-to-end, then ran a broad end-shape-first walk of the AI decision-making systems and captured the
result as a new keystone register `[VAL]`. Branch `docs-reorg-2026-06-23`. Design-capture only (nothing
built; no GDD/roadmap behavior change).

---

## 1. What was done

**Reviewed the current AI (the baseline).** `EnemyAI._act()` is a flat `match ai_profile`
(`basic`/`passive`/`healer`). Target = nearest by Dijkstra cost (`_find_nearest`); move = any tile it can
attack anyone from, tie-broken by nearness; **move and attack decided separately/greedily; the forecast
(`CombatResolver.preview_combat` :553) is never read.** Activation order = registration/placement order
(`GameState.get_living_units_of` :239) — a non-decision today. Confirmed two dedicated AI design docs
already exist and are build-ready: `ai_system_design_vision_2026-06-22.md` (north star: AI = composition
of 3 axes + ML-eval §6) and `ai_first_build_design_2026-06-22.md` (the AIP MVP: a **pure
`plan_action`** planner seam, still **unbuilt**). The deferred engagement/combat-AI workstream `[AIP-14]`
had **no register** — it is the keystone `[PER-4]`/`[PER-12]`/`[CVR-4]`/`[RCT-1]` all wait on.

**New register `[VAL-1..13]`** — `registers/ai_valuation_engagement_open_questions_2026-06-27.md`,
**RESOLVED** (end-shape locked; build staged). The AI turn = one question, "of all legal actions, which is
best?", scored against the forecast — mapping exactly onto the `[PER-1]` enumeration/valuation/forecast
pipeline (perception was implicitly deriving this). **Four owner forks:**
- **Fork A `[VAL-1]` — whole-action scoring.** Candidate = `{move_tile, action, target, weapon}` scored as
  one; folds weapon-select in for free. (Replaces the greedy two-step.)
- **Fork B `[VAL-2/3/4]` — horizon = configurable `search_depth`** (owner reframe of my myopic-vs-search
  binary). Engagement-axis param, negamax shell; **leaf = immediate forecast + post-move threat-exposure
  term** `[VAL-2]`. **Owner staging: seam now, depth=0 v1**, recursion + the **pure board forward-model
  `[VAL-4]`** (triple-use: depth>0 + action-preview AIP-12 + ML harness) deferred. Campaign-guide perf
  warning required.
- **Fork C `[VAL-5/6]` — score = F16/REQ-16 term-tree** ("A-plus": engine computes features, exposes them
  as **forecast term-sources** `[VAL-6]` — `expected_damage`/`would_kill`/`damage_taken`/
  `post_move_exposure`/`target_value`, the **one new vocabulary**, REQ-15 projection pattern; v1 ships a
  default weighted-sum AS a tree). Deterministic by construction.
- **Fork D `[VAL-8]` — activation order = author-selectable campaign rule** `ai_activation_order ∈
  {fixed, priority_sort, greedy_best_first, random}`. Owner: **build all four**, authors pick like other
  rules. **greedy_best_first reuses the scorer and needs NO forward-model** (executes for real, re-scores
  remaining); **random = seeded Package-A shuffle** (never engine RNG — determinism/lockstep). + per-preset
  `activation_priority` bias + `group_id` contiguity.

Plus: `[VAL-7]` closes PER-4 (`ai_target_weight` = the Stage-2 multiplier term); `[VAL-9]` determinism
(fixed-point + stable tie-breaks; ML eval feeds a ranking only); `[VAL-10]` **the AIP pure-planner seam is
the unbuilt prerequisite** (don't build the brain into today's `match`); `[VAL-11]` non-combat actions fold
into the scorer — **dancer/refresher timing = the known hard edge (PARTIAL)**; `[VAL-12]` consumers wired;
`[VAL-13]` forward DoD#2 (value-set guards + depth/node ceilings) + guide + DoD#1.

**Cross-refs wired:** PER-4 + PER "Net" now point at `[VAL-7]`/`[VAL]`; CVR-4 and RCT-1 forecast-reading
lines point at `[VAL-12]`; `ai_first_build_design` §10 AIP-14 line notes the `[VAL]` walk + the seam
prerequisite.

## 2. Commits this session
- (this commit) Add `[VAL-1..13]` AI valuation/engagement register; wire PER/CVR/RCT/AIP cross-refs;
  regen INDEX/REGISTERS. 19 doc checks green.

## 3. State / dependencies
- `[VAL]` end-shape RESOLVED. **Deferred build:** `[VAL-4]` forward-model (+ depth>0). **PARTIAL:**
  `[VAL-11]` dancer-timing. **Forward-reqs:** the AIP pure-planner seam (`[VAL-10]`), F16 forecast
  term-sources (`[VAL-6]`), F16 v1 vocab build.
- Closes the perception/interceptor parks: PER-4 (→VAL-7), PER-12 appraisal value, CVR-4/RCT-1 forecast
  read (→VAL-12).

## 4. NEXT SESSION
Two viable threads: **(a)** plan/build the **AIP first-build pure-planner seam** (`[VAL-10]` prerequisite)
so the brain becomes additive; or **(b)** keep walking residual AI facets — the `[VAL-6]` forecast
term-source vocabulary against the F16 v1 build, and the `[VAL-11]` dancer-timing edge. Owner's pick. Still
pre-A5 on the F1 schema-lock path.
