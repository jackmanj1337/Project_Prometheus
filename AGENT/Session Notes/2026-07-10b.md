# Session 2026-07-10b - v0.3.0.d playtest return triage

Branch: `v0.3.0-features`. Returned `v0.3.0.d` debug build results were in
`AGENT/v0.3.0d/`; raw intake folder was left untracked after copying durable
evidence into `AGENT/Docs/`.

## What was done

- Archived returned artifacts:
  - Returned checklist to `AGENT/Docs/playtests/playtest_checklist_v0.3.0.d_returned_2026-07-10.md`.
  - Two logs to `AGENT/Docs/archive/evidence/`.
  - Five screenshots to `AGENT/Docs/archive/evidence/`.
- Wrote `AGENT/Docs/playtests/playtest_v0.3.0.d_results_triage_plan_2026-07-10.md`.
- Wrote `AGENT/Code Reviews/playtest_v0.3.0.d_triage_review_2026-07-10.md`.
- Updated `AGENT/GDD/GDD_10_Roadmap.md` and the Project Control Plane:
  - `B1-SUSPEND` moved to Implemented / live validated.
  - `VAL-V030-GAMEPAD` stays Pending validation.
  - `VAL-V023-DISPLAY` stays Pending validation.
  - `B6-MRD` routes to a `stacked_perimeter` MRD-7 candidate.
  - Main Menu 2.0x overlap evidence routes to `UI-INSPECTION`.
- Regenerated `AGENT/Docs/INDEX.md`.

## Diagnosis summary

- Suspend/Continue passed live: units move after Continue, pair-up supports stay hidden,
  turn state restores, and relaunch persistence works.
- Gamepad still fails: Settings scrolls but has no repeat; New Game focus leaks to Main
  Menu controls; joystick cannot cycle attack / Pair Up targets; trigger zoom still wants
  a conservative tune.
- Display still fails: one-axis drag is not observed by the existing resize path, and
  maximize shows size but labels as `Custom`; relaunch persistence passed.
- MRD-7: single-layer is bad; stacked and border-through are acceptable; next candidate
  is stacked fill plus one perimeter outline around the whole threatened area.

## Commit

- `cb8247e` - `Triage v0.3.0.d playtest return`

## Validation

- `python3 AGENT/Docs/gen_docs_index.py`
- `python3 AGENT/Docs/check_docs.py`
- `git diff --check`
- Pre-commit also ran `check_docs.py` and `check_rng_usage`; Godot suite skipped because
  the commit was docs-only.

## Next time

1. Fix ModalScreen vertical repeat and add the `MenuRepeatPolicy.clear()` neutral latch.
2. Add reusable modal focus containment for Main Menu / New Game and a live-parent focus
   regression test.
3. Route polled stick movement to attack / Pair Up target cycling.
4. Fix display live maximized readout refresh and one-axis OS-window-size detection.
5. Implement MRD-7 `stacked_perimeter`; remove temporary F8 after acceptance.
6. Cut `v0.3.0.e` focused rerun for gamepad, display, and MRD-7.
