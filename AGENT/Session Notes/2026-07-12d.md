# Session Note - 2026-07-12d

## What was done

Cut the isolated **v0.3.2 focused rerun** on
`agent/codex/2026-07-12/v0.3.2-build`.

- Created a separate worktree so the existing `CLAUDE.md` working-tree edit on
  the v0.4-prep checkout was not staged or overwritten.
- Reverted the unrelated Band 2 registry foundation and MenuScale theme-fix
  commits from the release tree, then removed their orphaned `.uid` sidecars.
- Bumped the Windows preset, artifact path, product version, Main Menu label,
  and environment guide to v0.3.2.
- Added the focused retest checklist for popup containment, character-sheet
  navigation/scrolling, menu/zoom cadence, one-axis resize persistence, and the
  MRD-7 `dual_outline` decision.
- Exported `Project_Prometheus_v0.3.2_debug.exe` from source `8340318`:
  101780192 bytes, SHA-256
  `7059cb2af3bc7d9de0c2bdfc3d25f76a39fc55be4a8f41962202f859162c7838`.
- Repaired the local Godot template alias required by 4.6.3
  (`4.6.3.stable` -> `4.6.3-stable`).

All hooks passed on the isolation and metadata commits: documentation 27/27,
RNG guard, and 69-suite full test run.

## Commits

- `69a0cb3` Revert "Implement Band 2 registry foundation"
- `3452d18` Revert "Fix MenuScale theme-clobber bug (V030-BUG-01)"
- `8340318` Cut v0.3.2 focused rerun metadata and checklist
- Build manifest/session closeout commit follows this note.

## Plans for next session

- Deliver the executable with `playtest_checklist_v0.3.2.md` and triage the
  returned log/screenshots.
- On a passing return, close `VAL-V030-GAMEPAD` and `VAL-V023-DISPLAY`, select
  the accepted MRD-7 default, then remove F8 and the temporary diagnostics.
- Resume registry and theme work only on the separate v0.4 stream.

