# Session Note - 2026-07-15l

## What was done

- Completed archive-pipeline slice 2 with pure `CampaignArchivePreflight` ZIP
  inspection and no extraction or installed-state writes.
- Parsed the actual central directory to reject traversal/absolute/backslash/NUL
  paths, duplicates and case collisions, multi-disk/encrypted/unsupported ZIPs,
  symlinks/special files, and caller-bounded entry/byte totals.
- Validated the manifest, canonical Tier-2 index, every structured document and
  whole-pack cross-reference in memory; rejected unindexed files and save-shaped
  JSON while admitting approved raw media under `assets/`.
- Added focused coverage using both a real ZIP and hostile metadata fixtures that
  Godot's ZIP writer cannot create.

## Next

Implement slice 3 from the B6 archive handoff: unique staging, second-pass tree
validation, atomic promotion, existing-version rejection, and rollback tests.
