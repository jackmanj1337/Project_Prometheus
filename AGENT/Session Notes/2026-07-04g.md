# Session Note - 2026-07-04g

## Topic
Promoted the freshly-resolved `[CNC]`/`[PUG]` design into a real implementation plan and
wired it to the control plane.

## What was done
- **Wrote `skirmish_encounter_generation_implementation_plan_2026-07-04.md`** in the
  established per-slice plan shape (Purpose / Scope / Non-Goals / Source Docs / Decisions
  Not To Reopen / Dependency Note / Existing Code Touchpoints / 5 slices / Commit Order /
  Verification Checklist). Track IDs `B4-ENCOUNTER-MODEL` + `B7-SKIRMISH`. Five slices:
  1. **Spawn seam generalization** (`[PUG-3]`) — `_spawn_units` accepts an in-memory
     `UnitData` or a path. Buildable now against the live tree.
  2. **`BattleMapDef` + `BattleEncounterDef` split** (`[CNC-4]`) — the terrain/payload
     split, `enemy_start_tiles`, `encounter_id -> battle_map_id`, legacy `map_id` adapter.
     Trails `B1-CST`.
  3. **`ForceSpec` + scaling** (`[PUG-4]`/`[PUG-7]`) — first-class roster resource rolled
     through the shared `generate_unit`; open registry of scaling terms over `[DIF]`/`[TCV]`.
  4. **Generated encounter modes + skirmish `[PHB]` panel** (`[PUG-5]`/`[PUG-6]`) —
     `force_spec`/`map_pool` modes; the panel fires an encounter via the shared
     `[CNC-8]`/`[CNC-9]` launch primitive (resolve map → generate force → spawn → reward).
  5. **Editor generate-and-bake** (`[PUG-8]`) — freeze a rolled unit to a pack `UnitData`.
  - Explicitly **not** re-planning the generator core: `generate_unit`/`UnitSpec` stays
    Slice 2 of the arena plan (`B7-ARENA`); this plan reuses it (`[PUG-10]`).
  - Grounded the touchpoints against live `GameMap._spawn_units` (`GameMap.gd:168` — only
    the placement-resolution loop needs the path-vs-instance branch; `_spawn_unit` already
    takes a `UnitData`) and `MapData.gd` (field split map terrain vs payload).
- **Added two control-plane rows** (both Planned): `B4-ENCOUNTER-MODEL` (Band 4, dep
  `B1-CST`) and `B7-SKIRMISH` (Band 7, deps `B4-ENCOUNTER-MODEL`, `B7-ARENA`, `B3-PHB`,
  `B1-PKGA`, `B3-TCV`), both pointing at the new plan — same one-plan/multiple-rows pattern
  as the band6 mutable-campaign-state plan.
- **Cross-linked back:** the `[PUG]` §4 slice sketch and the `[CNC-4]` implication now point
  at the plan + its two control-plane rows.

## Commits (this session)
- (pending) — new skirmish/encounter/generation plan + two control-plane rows +
  register cross-links + regen INDEX/REGISTERS + this note.

## Validation
- `check_docs.py` 21/21 green (incl. check 20 control-plane schema/prefix/reachability and
  check 18 generated manifests). All new backtick Track IDs resolve; register IDs written
  in the `[XXX]`-inside-backtick form so they are not parsed as Track IDs.

## Next Session
- **Nothing more to plan on this thread** — the encounter/generation design is resolved
  (`[CNC]`/`[PUG]`) and now has a build plan + tracked rows. Implementation is gated behind
  `B1-PKGA` (RngService) + `B1-CST` + `B7-ARENA` Slice 2 + `B3-PHB`; Slice 1 (spawn seam) is
  the one piece buildable against the live tree today if a build phase opens.
- **Still outstanding from 2026-07-04d:** the **live Windows display rerun** of
  `playtest_checklist_v0.2.6.md` (input-routing §1.5/§1.7 + self-contained log §3.2) —
  needs a real desktop pass; `VAL-V023-DISPLAY` flips only when Part I passes.
