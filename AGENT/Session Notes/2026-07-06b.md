# Session Note 2026-07-06b - B1-F1 save-schema manifest lock

## What was done

Built **`B1-F1`** on `v0.3.0-features`: the first F1 save-schema manifest is
now locked at `AGENT/Docs/plans/f1_save_schema_manifest_2026-07-06.md`.

The manifest assigns field-family paths, owners, scopes, lifecycles,
defaults/migration behavior, serializer owners, Retry behavior, suspend
behavior, fixtures, row status, and explicit no-save guard rows. It keeps the
top-level save shape aligned to the F1 lock design:
`format_version`, `save_label`, `integrity`, `header`, `campaign`, `party`,
`roster`, `map_runtime`, and `suspend`.

Also updated:

- F1 manifest contract and source-inventory template to include row status.
- Band 1 plan references from the `2026-07-XX` placeholder to the real manifest.
- Project Control Plane `B1-F1` row to `Implemented`.
- `GDD_10` next queue so `B1-SAVECODEC` is now first.
- `GDD_Feature_Index` save/retry row and follow-up wording.
- `AGENT/Docs/INDEX.md` via `gen_docs_index.py`.
- `AGENT/Docs/check_docs.py` check 23, validating the F1 manifest table shape,
  scope vocabulary, fixture vocabulary, row-status vocabulary, and unfinished
  marker bans.

## Commit

- `35b2a02` - `Lock F1 save schema manifest`

## Verification

- `python3 AGENT/Docs/gen_docs_index.py`
- `python3 AGENT/Docs/check_docs.py` green, including new check 23.
- `git diff --check` green with the new manifest included.
- Commit pre-hook green: docs checks, RNG-usage guard; Godot suite skipped by
  hook because the change is docs-only.

## Next

1. Start **`B1-SAVECODEC` Slice 4**: add the JSON-safe codec foundation and
   move Retry unit/inventory snapshot internals behind it.
2. Then **`B1-SAVECODEC` Slice 5**: add the `SaveData` envelope and old-save
   defaults.
3. If the v0.2.8 tester return arrives, triage the release-line gate first.
