# Session Notes - 2026-06-13e

## Focus

Two things: (1) verify whether the existing handoff checklist actually covered the
whole consolidation, and rebuild it as a complete plan; (2) execute all of Stage 1.

## What was done

**Rebuilt the checklist (complete).** The prior `..._handoff_2026-06-13b.md` only
covered Phases 1–3; its Step 4 collapsed Phases 2–7 into one line and dropped Phases 5
(roadmap), 6 (supporting-doc cleanup), 7 (validation), plus Package G and most of B–F.
Wrote `documentation_consolidation_master_checklist_2026-06-13.md` — the full
Phase 1→7 + Package G sequence in 7 stages, with a §11 commit-sequence mapping, scope
boundary (code for B–F is roadmap work; only their adoption-matrix/GDD prose is in
scope), and Appendix A carried forward. Marked handoff_b superseded.

**Executed Stage 1 (all four items):**

1. **1.1 Lifecycle/link-migration table** — `documentation_lifecycle_2026-06-13.md`.
   Sections A (move/merge/delete files + live inbound refs + repair stage), B (live GDD
   rewrite-in-place), C (active Docs), D (corpus), E (historical bulk via category
   rules). Re-verified live inbound refs to the 6 moving files (README:19, GDD_00,
   GDD_01:30, testing_guide, GDD_10).
2. **1.2 Two-RN RNG update (RULE-001)** — edited `rng_determinism_design_2026-06-11.md`:
   §5 hit roll now two 0–99 draws, `floor((r1+r2)/2) < displayed_hit`, both always
   consumed; crit only after a hit (miss=2 rolls, hit=3); §6 GDD_02 row + header
   "Supersedes" updated; T7 fixture freezes the two-RN draw count; §11 save-compat note
   (no migration needed — single-roll never shipped). **File kept untracked** per the
   preservation policy; its move→Docs + merge→GDD is the Stage 3.1 action.
3. **1.3 `decision_index.md`** — DOC-009 schema; rows for every DOC/RULE/SET/D/RNG/
   OPEN/PL/AWR ID with status + home + supersession (D-C → DOC-001; single-roll →
   RULE-001).
4. **1.4 `GDD_Feature_Index.md`** — DOC-005 seed; plan §5 columns + 17 feature groups +
   backlog. Anchors deliberately left `TBD (S3)` (verified during chapter rewrites).

Ticked Stage 1 boxes in the master checklist.

## Tests and verification

- Commit hook: PASS on both commits, all 38 suites green. Documentation-only; no game
  code touched.
- Inbound-ref claims for moving files re-checked by grep, not just trusted from the
  prior sweep.

## Commits made

- `Build complete consolidation master checklist (Phase 1-7 + Package G)` (5f8cc8a)
- `Complete Stage 1: lifecycle table, two-RN RNG update, decision + feature indices`
  (6b76286)
- (this note)

## State preserved

- `AGENT/GDD/rng_determinism_design_2026-06-11.md` — **content updated to two-RN** but
  left **untracked**; move/merge is Stage 3.1.
- `AGENT/GDD/gdd_update_reference_2026-06-12.md` — untracked; archived in Stage 5.2.
- Combat-preview screenshots + `.import` files in `AGENT/Docs` — untouched.

## Plans for next session

Stage 1 is complete. Next is **Stage 2** (Phase 2):

1. Rewrite `GDD_00_Overview.md` — authority model (DOC-001), status vocabulary,
   section template, document map, link to `GDD_Feature_Index.md`, known-issue summary,
   1.0 definition (D-B) + platform targets (OPEN-8/11 — Compatibility/OpenGL, Steam Deck
   letterbox).
2. Add the PL#8 doc-lifecycle DoD rule to `AGENTS.md`.

Then Stage 3 chapter rewrites (start `GDD_02`, which is where the RNG contract merges in
and the two-RN/pipeline/combat-ruling decisions apply).
