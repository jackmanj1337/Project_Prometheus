# Session Note - 2026-07-24e

## What was done

- Continued the owner design discussion over the campaign-library owner-questions packet.
  Resolved **Branch I — Navigation & accessibility** (CL-NAV-02…07). Unlike A–H this was a
  *confirm-against-code* pass: the branch maps onto infrastructure that already exists and should be
  **reused, not re-implemented** — `ModalScreen` (shared modal base, 7 screens extend it),
  `SelectionCursor` (shared nav core across the More-Info surfaces), `MenuScale` (crisp type-scaling,
  reaches 2.0×), `InputDisplay` (brand-aware pad glyphs) + `InputModeManager`, and the data-driven
  `SettingsScreen` schema (remappable keybinds auto-listed from the InputMap).
- Owner decisions:
  - **CL-NAV-02** — stable global controller mapping + a live brand-correct legend via
    `InputDisplay.live_action_prompt`. Two riders: add **menu-semantic** section-switch actions rather
    than overloading gameplay `next_unit`/`prev_unit`; and an **input-map defect** was found —
    `confirm`=joy(1,0) and `cancel`=joy(2,1) share **button 1 (right face)**, so a menu leaning on both
    sees one button as accept *and* back. Recorded as a playtest-gated investigation.
  - **CL-NAV-03** — explicit initial + return focus and neighbors; destructive actions never default
    (matches `LoadGameScreen`'s existing delete-confirm focus restore).
  - **CL-NAV-04** — filters + sortable headings only; **no free-text search in v1** (coheres with the
    already-backlogged search). Rename text-entry stays (a rare explicit edit ≠ a browse-path hit).
  - **CL-NAV-05** — 200% + long-label + missing-image stress; 2.0× already exists in
    `MENU_SCALE_LEVELS`, so this is **layout fixtures, not new infra**.
  - **CL-NAV-06** — text + icon, colour supplementary — the only feasible call (no colourblind /
    high-contrast mode exists); badges draw from registry display metadata, not a `match`.
  - **CL-NAV-07** — **threshold rule**: determinate bar + **cancel-before-commit** on the long ops
    (inbox scan, pack import), indeterminate spinner + no cancel on the fast ops (export, status-record
    write). Cancel is cheap (`install_zip` already stages to `.staging` + `_remove_tree` on failure +
    atomic `_promote()`); **async is the real cost**. Verified against the platform targets: it **must
    be a web-safe cooperative chunked coroutine (`await` a frame every N entries), NOT threads** — web
    is a shipping target (playtest channel + portfolio demo; Compatibility renderer chosen because web
    export requires it) and Godot web is single-threaded unless SharedArrayBuffer + cross-origin
    isolation is available, which a demo can't assume. Same chunking avoids Android ANR (mobile deferred
    post-1.0) and the browser "page unresponsive" prompt.
- Also surveyed **all** player-facing surfaces for systems that will eventually need the same
  campaign-library-style research + owner-questions-packet treatment, and marked rows so they can be
  discussed as time allows (container-repo tracker):
  - Updated the six not-started `DISCUSS-*` rows so each explicitly owns "build the questions list
    first, then walk branch-by-branch".
  - Added seven rows for surfaces with **no** discussion row: `DISCUSS-PREP-HUB-UX`,
    `DISCUSS-PREP-ACTIVITIES-UX`, `DISCUSS-FORGING-UX`, `DISCUSS-PVP-MODE-UX`,
    `DISCUSS-COMBAT-ACTIONS-UX`, `DISCUSS-AVATAR-MYUNIT-UX`, `DISCUSS-MINIGAMES-SEAM-UX`.
  - Branch I spun off three implementation/backlog rows: `BACKLOG-INPUTMAP-CONFIRM-CANCEL-DOUBLEBIND`,
    `IMPL-ASYNC-PROGRESS-CANCEL`, `PLAN-UIUX-REUSE-PASS` (the previously-untracked UI/UX pass, scoping a
    shared record-list + master-detail + action-menu widget as its structural deliverable; the visual
    `UiThemeDef`/`AssetResolver` half stays gated on the art open-questions).
- No runtime, scene, save-schema, or release-line behaviour changed — design and planning only.

## Commits claimed

- `83b52f777b9a4cca68af835faa8208552614906d` — Record campaign-library Branch I navigation & accessibility decisions (CL-NAV-02..07)

## Gates

- Pre-commit hook: documentation checks, RNG-usage guard, analyzer tests, scene-integrity,
  session-claims, evidence-matrices, gdscript style all PASS; docs-only change so the Godot suite was
  skipped by the hook.
- Workspace `coordination/check_tasks.py`: 148 tasks valid, no conflicts (container-repo commit
  `052e396` on `agent/from-staging-area/campaign-research-findings-tracker` advances the DISCUSS row to
  A–I, updates the six DISCUSS triggers, and adds the ten new rows).

## Next

Resume at **Branch J — Safety, trust, privacy** (CL-SAFETY-01…04; CL-SAFETY-01 wording pre-answered,
and the redacted-report scrubber CL-MISSING-05 relies on is owned here). Then K (author/advanced
surfaces + editor integration). Copy each branch's ids/answers into
`AGENT/Docs/design/campaign_library_ux_decisions_2026-07-24.md` and create implementation tracker rows
only for accepted scope.
