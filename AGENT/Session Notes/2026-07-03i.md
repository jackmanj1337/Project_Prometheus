# Session Note - 2026-07-03i

## Topic
Resolved `[CNC-2]` node reference slots.

## What was done
Updated `AGENT/Docs/registers/campaign_node_composition_open_questions_2026-07-03.md`
to resolve `[CNC-2]`: campaign nodes use explicit reference slots, and `prep_panels`
entries point to specific panel instances when needed. Updated the campaign/save
technical plan with the same rule.

## Decisions captured
- `[CNC-2]` RESOLVED: use explicit node slots: `chapter_id`, `prep_panels`,
  `encounter_id`, plus temporary legacy `map_id` while `MapData` remains monolithic.
- `prep_panels` is an array of panel references, not just strings. A panel ref can name
  `panel_type` plus `panel_instance_id`, e.g. a specific shop stock list or arena roster.
- Panel types with no per-instance config, such as a generic convoy panel, may omit
  `panel_instance_id`.

## Validation
- `python3 AGENT/Docs/gen_docs_index.py`
- `python3 AGENT/Docs/check_docs.py` - PASS, all 21 checks green.

## Commits
- (this commit) - resolve `[CNC-2]` panel instance refs.

## Next Session
- Continue the one-by-one `[CNC]` walk with `[CNC-3]` node type vocabulary.
