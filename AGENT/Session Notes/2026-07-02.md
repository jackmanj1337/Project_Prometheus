# Session Note - 2026-07-02

## Topic
Code-review-style audit of the Band 1-6 implementation plans; findings recorded
as a review doc.

## What was done
- Read all nine band plan docs (Band 1, Band 2, Band 3 + handoff, the four
  Band 4 plans + handoff) and the Band 5/6 control-plane rows.
- Spot-checked the plans' code-grounding claims against the working tree
  (autoload order, raw-RNG sites, `TurnManager` commit points,
  `party_items`/`party_gold`, `StatBreakdown.gd`, `check_docs.py` check 21) —
  all accurate except the findings below.
- Wrote `AGENT/Code Reviews/band_plans_audit_2026-07-02.md`: 22 findings
  (2 coverage, 5 high, 8 medium, 7 low). Headlines:
  - **C1:** Bands 5-6 have no implementation plans or handoff while Band 7
    forging already has a plan that depends on an unwritten Band 5 decision.
  - **C2:** the `B4-CAMPAIGN-LOOP` integration plan named by handoff D1 was
    never written, and the seven smaller Band 4 consumers have no recorded
    grouping/owner.
  - **1-1:** Band 1 Slice 0 presents `TurnManager.get_action_start_tile` as an
    existing API; it does not exist (Slice 1b adds it).
  - **2-1:** Band 2 Slice 5 reads `GameState.permadeath_enabled`, which Band 1
    Slice 6 (`[CST-4]`) deletes before Band 2 starts.
  - **4I-1:** IEQ modifier sources need an `InventoryEntry` instance id that no
    slice creates.
  - **4S-1:** shop sell-price source for non-stock items is undefined — owner
    decision needed before shop Slice 2 (candidate `SHP` register entry).

## Validation
- `python3 AGENT/Docs/check_docs.py`: 21/21 green.
- `git diff --check`: clean.

## Commits
- `158c650` — Add Bands 1-6 implementation-plan audit review.

## Plans / Next Session
- Surface the two owner decisions (4S-1 sell-price model; C2 Band 4 grouping).
- Apply the high plan edits (1-1, 2-1, 4I-1), then the mediums.
- Draft the Band 5 handoff (C1) and the thin `B4-CAMPAIGN-LOOP` plan (C2) in
  the Band 3/4 handoff pattern.

## Read First Next Session
1. `AGENT/Code Reviews/band_plans_audit_2026-07-02.md` (the findings + fix order).
2. `AGENT/Docs/plans/project_control_plane_2026-06-29.md` Band 4-6 rows.
