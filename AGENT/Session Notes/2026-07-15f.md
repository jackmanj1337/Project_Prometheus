# Session Note - 2026-07-15f

## Owner direction

Continue next session in this order:

1. Make the existing five-map `proving_grounds` campaign player-testable from
   New Game.
2. After the campaign entry/test slice is closed, begin campaign package export
   and import (`B6-CAMPAIGN-SHARING`).

## Resume point - multi-map campaign entry

The campaign data and runtime exist, but the normal New Game route still launches
one selected map directly. No player-facing production call currently invokes
`CampaignManager.start_campaign("proving_grounds")`.

Build the smallest player-facing campaign entry slice:

- Add `The Proving Grounds` as a New Game campaign choice without removing the
  existing developer/single-map selection path.
- Start through `CampaignManager.start_campaign`, preserve the selected campaign
  rules, and call `launch_current_node`, which now routes to PrepScreen.
- Cover start -> prep -> map, victory -> next prep, campaign manual save,
  Continue/Load -> prep, and terminal completion across the five authored nodes.
- Cut a focused Windows playtest only after automated coverage is green. The live
  checklist must include PrepScreen focus, scrolling/text fit, redeployment on
  Retry, manual save/load, and all five node transitions.

## Following work - campaign package export/import

Use the existing `B6-CAMPAIGN-SHARING` authority rather than writing a new design:

- `AGENT/Docs/plans/band6_mutable_campaign_state_implementation_plan_2026-07-03.md`
  Slice 3 is the implementation sequence.
- `AGENT/Docs/plans/project_control_plane_2026-06-29.md` owns the track row.
- Campaign package means portable content, never a run save. Export a self-contained
  manifest + canonical data + media artifact; import validates compatibility,
  installs it under `user://campaigns/<pack_id>/`, reports repairable optional
  asset failures, and registers the campaign for selection.

Start that work with a dependency preflight. The tracker currently requires
`B1-CST`, `B2-DATAMANAGER-SEAMS`, and `AssetResolver`. Confirm what is actually
implemented at the new head; if a required load/resolver seam is absent, land the
smallest prerequisite slice first rather than embedding a second loader inside
`CampaignPackager`.

Do not fold `CampaignStatusRecord` carry-forward into the first package slice.
That is the next consumer (`B6-CAMPAIGN-STATUS`, plan Slice 4) after the reusable
package importer exists.

## Current branch state

- Prep/manual-save implementation commit: `6c80b16`.
- Branch: `agent/codex/2026-07-15/prep-save-followup`.
- At note creation the branch is one local commit ahead of its remote; push/review
  remains outstanding.
