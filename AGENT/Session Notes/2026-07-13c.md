# Session Note - 2026-07-13c

## What was done

Implemented the next v0.4 plan-order slice, `B2-RESOURCE-LEDGER`, as fixed-wallet
contract groundwork.

- Added `CostSpec` and structured `ResourceTransaction` records.
- Added the registry-backed `ResourceLedger` autoload with side-effect-free quote,
  transient non-mutating reserve, atomic commit, and recorded-delta refund APIs.
- Added party and unit wallet adapters over the existing `GameState.party_gold` and
  `UnitData.gold` fields; dynamic formulas and custom pools remain deferred.
- Migrated victory gold credits through the ledger while preserving existing item
  reward appends.
- Added a documentation guard against new direct `party_gold` writes outside the
  state owner and ledger.
- Updated `GDD_01`, `GDD_02`, `GDD_04`, `GDD_10`, the feature index, and the
  control-plane row.

## Validation

- `test_resource_ledger.gd`: 8 passed, 0 failed.
- `test_turn_manager.gd`: 64 passed, 0 failed.
- Full `run_tests.sh`: 72 suites passed.
- `AGENT/Docs/check_docs.py`: 28/28 passed.
- `scripts/ci/check_rng_usage.sh`: passed.
- `git diff --check`: passed.

## Next

- The next Band 2 plan-order slice is `B2-OCCUPANCY`.
- Keep dynamic resource formulas, persistent reservations, custom pools, shops,
  and training consumers with `B3-REQ`, `B3-RESOURCE-POOLS`, and their owning tracks.
- A returned v0.3.2 live playtest still preempts new v0.4 work at a clean commit.
