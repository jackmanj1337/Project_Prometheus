# Session Note - 2026-07-14e

## What was done

- Confirmed v0.4.0 is genuinely green while the Windows playtest is out:
  `check_docs.py`, `check_rng_usage.sh`, and `run_tests.sh` all pass on the build
  commit. Only the live smoke gate and the reviewer handoff gate remain open, and
  both need returned evidence.
- Pushed all outstanding agent work to `origin`. Nothing now exists only on the
  local disk:
  - `agent/codex/2026-07-14/v0.4.0-windows-build` (was entirely unpushed - it
    holds the playtest build) is now published.
  - `agent/claude/2026-07-12/v0.4-prep` (was 17 commits ahead) is now published.
  - `v0.3.0-features` was left alone: agents may push only `agent/**` branches,
    and its one commit (`f676fa5`, the MenuScale fix) is already an ancestor of
    every agent branch, so nothing is stranded.
- No PRs opened; human review and merge remain manual.
- Selected `B1-CST` (campaign / save spine) as the next slice and wrote its
  handoff. It is the largest unblocked bottleneck: its three dependencies are all
  Implemented and eleven open tracks depend on it. It also does not collide with
  a playtest return, which will land on UI/input surfaces rather than the
  campaign/save layer.
- Grounded the handoff against the code: `SaveManager`, `SaveCodec`, `SaveData`,
  and `CampaignRules` already exist and are wired; `CampaignData` does not exist
  yet, which is where the next slice starts.

## Commits

- Session commit - Add `B1-CST` save spine handoff and point the control plane at it.

## Next session

1. If v0.4.0 playtest evidence has returned, triage it first - it preempts this
   work and belongs on the v0.4.0 build branch, not the save-spine branch.
2. Otherwise follow `AGENT/Docs/plans/b1_cst_save_spine_handoff_2026-07-14.md`,
   starting at Slice 1 (`CampaignData` seeding).
3. Keep each slice on its own commit; do not widen a slice to absorb a blocker.
