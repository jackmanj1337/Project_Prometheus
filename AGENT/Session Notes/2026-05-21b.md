# Session Notes — 2026-05-21b

## Context

User asked to begin M15 Part A hotseat control work.

## What Was Done

- Renamed the shared controller entry point from `EnemyAI.run_ai_phase()` to
  `run_phase()` and updated the `TurnManager` call site and tests.
- Added `HotseatController.gd` and wired `GameMap` to inject it into
  `TurnManager`.
- Generalized phase-ending flow in `TurnManager` with `phase_committed`,
  `request_end_phase()`, and `are_all_units_done(faction_id)`.
- Generalized auto-end so blue and `HOTSEAT` factions both auto-commit when all
  locally controlled units are done.
- Updated `MapCursor` End Turn handling to use the active faction instead of
  assuming blue.
- Updated `FactionData` docs and removed the obsolete `controller = "HUMAN"`
  line from the C3 faction demo map resource.
- Added tests for hotseat routing, generic phase commit behavior, the
  `HotseatController`, cursor faction handoff, and cursor End Turn routing.

## Verification

- `godot --headless --path /workspace --script res://scripts/tests/test_turn_manager.gd`
  passed: 51 passed, 0 failed.
- `godot --headless --path /workspace --script res://scripts/tests/test_map_cursor.gd`
  passed: 35 passed, 0 failed.
- `bash run_tests.sh` passed: all suites green.

## Commits

- `4a81518` — Rename AI controller phase entrypoint

## Plans for Next Session

- Add a dedicated WHOLE_PHASE hotseat content map for manual verification.
- Run the in-game hotseat acceptance pass from
  `AGENT/Docs/implementation_plan_2026-05-21.md`.
- Decide whether to mark the relevant M15 roadmap checklist items complete after
  manual verification.
