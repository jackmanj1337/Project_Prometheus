# Session Notes - 2026-06-13g  (Stages 3.0–3.2 — HANDOFF)

## Focus

Continue the documentation consolidation: Stage 3.0 (adoption-matrix structure),
3.1 (GDD_02 + RNG merge), and 3.2 (GDD_03), then hand off.

## What was done

**Stage 3.0 — adoption matrix.** Expanded the 7-row corpus-folder stub into a
systematic `GDD_Adoption_Matrix.md` covering every plan-§4 area, with two axes
(**Adoption**: adopted target / w-variation / rejected / deferred / not reviewed /
implemented — distinct from **Impl. status**), GDD owner, project variation, and
decision IDs. **Home decision:** moved out of the corpus folder into the live GDD set
(`AGENT/GDD/`, beside the feature index) per DOC-001; repaired 3 corpus inbound links;
linked from GDD_00.

**Stage 3.1 — GDD_02 + RNG merge.** Rewrote `GDD_02_Core_Mechanics.md` to the section
template + status labels. Applied two-RN (RULE-001), modifier pipeline order, OPEN-2/3/6/7,
WEXP/EXP/promotion-timing decisions as split status (corpus = Target, pointing to the
matrix, not duplicated). **Merged the RNG contract:** binding rules → new
`GDD_01 §Determinism, Snapshot & Online Contract` + GDD_02 combat section; the file
moved to `AGENT/Docs/` and was re-scoped as the **implementation plan** (now tracked —
closes the untracked-file thread). Fixed the two inline single-roll refs in GDD_01.

**Stage 3.2 — GDD_03.** Rewrote `GDD_03_Units_Classes.md` to the template. Corpus class
adoption (SET-009/RULE-007/008) as Target (AWR-2); progression counters
(`displayed_level`/`exp_basis_level`/`lifetime_levels_gained`, RULE-006/SET-007);
promotion targets (SET-006) with **trigger timing cross-referenced to GDD_02**
(RULE-005, no duplication); reclass + growth-to-caps (D-E); Light/Dark (RULE-009) +
Soldier (OPEN-9) deferred; Cleric Light (OPEN-10) Open decision. Compressed the six
verbose per-unit stat blocks into one reference table (`.tres` files remain the
authoritative source — avoids a one-owner-per-fact violation).

Bookkeeping each step: ticked checklist 3.0/3.1/3.2; updated lifecycle table; marked
applied decisions in `decision_index.md`; pinned now-real section anchors in the feature
index + adoption matrix.

## Tests and verification

- Commit hook: PASS on every commit (38 suites green). Documentation-only; no game code.
- Verified no **live** doc contradicts the new rules — stale single-roll lines exist only
  in GDD_09/GDD_Assumptions, already demoted to Historical for Stage 5.2 deletion.

## Commits made

- `Stage 3.0: expand adoption matrix into systematic index; move to live GDD set` (d2b10d1)
- `Stage 3.0: add expanded GDD_Adoption_Matrix, repair corpus links, link from GDD_00` (8952cdb)
- `Stage 3.1: rewrite GDD_02 (two-RN, rulings); merge RNG contract into GDD_01/02; move impl plan to Docs` (2184ba7)
- `Stage 3.2: rewrite GDD_03 (class adoption, progression, promotion/reclass relationships)` (473b89d)
- (this note)

## State preserved

- `AGENT/Docs/rng_determinism_design_2026-06-11.md` — now **tracked** (moved from GDD,
  re-scoped as impl. plan). Archived once `RngService` + tests land.
- `AGENT/GDD/gdd_update_reference_2026-06-12.md` — still untracked; archived in Stage 5.2.
- Combat-preview screenshots + `.import` in `AGENT/Docs` — untouched.

## Decision worth noting (PL#8)

These 3.x commits changed **no behavior** (decisions documented as Target design; no code
written), so no `GDD_10` roadmap status flip was required. Roadmap reconciliation is
Stage 4. If a future chapter rewrite implies a status change, flip GDD_10 in the same
commit per PL#8.

## NEXT SESSION — start here (Stage 3.3)

Read order: this note → `documentation_consolidation_master_checklist_2026-06-13.md`
(Stages 0–2, 3.0–3.2 ticked) → `documentation_governance_2026-06-13.md` (template +
vocab) → `GDD_Adoption_Matrix.md` (your Target-design source) → `decision_index.md`.

**Stage 3.3 — rewrite `GDD_04_Weapons_Items.md`:**
1. Finalize this chapter's adoption-matrix rows (Weapon triangles & WEXP group; weapons/
   items group) **before/with** the rewrite — pin the `(TBD)` GDD_04 anchors.
2. Apply: rank-scaled triangle bonuses for both triangles (SET-003), hybrid magnitude via
   equipped WEXP track (RULE-013), WEXP thresholds/caps (SET-004) + gain timing (RULE-004)
   + migration (RULE-003), S-rank bonus via combat engine / retire `s_rank_mastery`
   (SET-005/RULE-002), broken-weapon degraded mode backlog (OPEN-5).
3. Use the section template + status labels; keep current item/economy data as
   Implemented, corpus as Target pointing to the matrix (don't duplicate corpus tables).
4. Anchors: `data/weapons/`, `data/items/`, `WeaponData.gd`, `ItemData.gd`,
   `ItemHandler.gd`, `DataManager.get_weapon_triangle_result`. Reference corpus:
   `awakening_weapons_physical.md`, `awakening_weapons_magic.md`, `awakening_items.md`,
   `awakening_lookup_tables.md`.
5. Update decision_index/feature-index/matrix anchors; tick checklist 3.3.

Then 3.4 GDD_05 (skills, Pair Up, conditions), 3.5 GDD_01 (full architecture rewrite +
CampaignRules contract), 3.6 GDD_06, 3.7 GDD_07, 3.8 GDD_08 → Stage 4 (roadmap + Package
G) → 5 (moves/deletes + guides) → 6 (DOC-011 CI) → 7 (verify §12).

**Watch for:** still-open/deferred — RULE-011 terrain mapping (AWR-8), OPEN-9 Soldier
(AWR-2), OPEN-10 Cleric Light (RULE-009). Don't author them; mark Deferred/Open decision.
