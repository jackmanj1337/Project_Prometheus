# Session Note - 2026-07-24c

## What was done

- Continued the owner design discussion over the campaign-library owner-questions packet.
  Resolved **Branch G — Missing / incompatible content** (CL-MISSING-01…05). Branch G is
  mostly Branch D cashing out: the integrity-vs-validity check-point contract already fixed
  where each check runs, and two prior v1 decisions prune the packet's lists — *no Disable*
  (CL-LIFE-06) removes Enable/disabled; *no migration engine* (CL-LIFE-04) removes
  migration-unavailable.
- Code grounding: the save index already mirrors a **header** out of each save doc
  (`SaveManager._slot_index_row`, carrying `campaign_id` + `package_id`/`package_version`),
  so a missing-pack save can be listed and categorised without loading the save or pack; and
  `PP-V053-CAMPAIGN-ERROR-DIAG` (completed) already moved the engine toward distinct campaign
  diagnostics.
- Owner decisions:
  - **CL-MISSING-01/02** — a save whose pack is missing stays **visible, disabled, in its
    original campaign group** with a **"Missing content" badge** (rendered from the header
    index; placeholder campaign row derived from the header if the campaign itself is gone).
    Repair = **Import required content** (deep-link pre-filtered to the needed id/version) /
    **Inspect** / **Export** (rescue the run without its pack) / **Delete** (secondary,
    double-confirm if run not completed). No Enable; **no separate Missing-Content filter**
    (orphans show inline, keeping CL-SAVE-04 lean v1 coherent) — the filter is absorbed by the
    existing `BACKLOG-RUNSAVE-SEARCH-ARCHIVE` row.
  - **CL-MISSING-03** — five distinct stable failure categories for v1: **Missing content ·
    Version not installed · Modified (fingerprint) · Invalid pack · Corrupt save**. A *fixed
    engine diagnostic set* (not author-extensible content vocabulary), so a closed set is
    correct — not the `[EXT]` closed-enum smell.
  - **CL-MISSING-04** — confirms the Branch D layered override policy: **Modified →
    non-blocking warning** (answers the packet's open "always block fingerprint mismatch?" =
    No); **Missing / Version not installed → blocked** until imported; **Invalid references /
    schema → never overridable**, hard-fails at point of use with a diagnostic.
  - **CL-MISSING-05** — **plain summary + expandable, copyable, path-redacted report**
    (relativised to `user://`, usernames/absolute home paths stripped); the redaction is the
    Branch J (CL-SAFETY) privacy scrubber, recorded here as the requirement.
- No premature standalone tracker rows (folds into the consolidated Library implementation
  pass); no new backlog row (the missing-content filter is absorbed by the existing
  search/archive row). No runtime, scene, save-schema, or release-line behaviour changed —
  design and planning only.

## Commits claimed

- `60d8ed1383d7c50d74325b1a031bb46fd1d5a934` — Record campaign-library Branch G missing/incompatible-content decisions (CL-MISSING-01..05)

## Gates

- Pre-commit hook: analyzer tests, scene-integrity, session-claims, evidence-matrices,
  gdscript style all PASS; docs-only change so the Godot suite was skipped by the hook.
- `python3 AGENT/Docs/check_docs.py`: all documentation checks green (INDEX regen no-op).
- Workspace `coordination/check_tasks.py`: 137 tasks valid, no conflicts (container-repo
  commit on `agent/from-staging-area/campaign-research-findings-tracker` advances the DISCUSS
  row to A–G).

## Next

Resume at **Branch H — Transfers: import/export/backup/restore** (CL-TRANSFER-01…06; simplified
by the no-migration-engine decision — still needs concrete artifact names/extensions for the
inbox scanner's extension-routing). Then I → K. Copy each branch's ids/answers into
`AGENT/Docs/design/campaign_library_ux_decisions_2026-07-24.md` and create implementation tracker
rows only for accepted scope.
