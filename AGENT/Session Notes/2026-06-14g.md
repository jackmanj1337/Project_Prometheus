# Session Notes — 2026-06-14g

## Topic
Sorted out all four remaining open decisions and executed them.

## Decisions taken (user-confirmed) + outcome
1. **PL#8/PL#9 → DoD#1/DoD#2** (`d908a07`). `decision_index.md:29` defines `PL#` as
   the June parking-lot namespace; AGENTS.md had reused PL#8/PL#9 for two rules, and
   PL#9 collided with parking-lot "Minimal SFX". Renamed both DoD rules out of the
   PL# namespace (DoD#1 = doc-lifecycle, DoD#2 = enforcement) across AGENTS.md,
   decision_index, GDD_00, and all Review Procedures. Historical/dated records left
   as provenance. Bumped GDD_00 `Last verified`.
2. **GDScript lint/format gate (gdlint + gdformat)** — **blocked here**: no `pip` /
   `gdtoolkit` in this environment, and committing the gate would fail CI on the
   un-reformatted codebase. Documented the path in `new_machine_transfer_checklist.md`
   §C + Review Procedures §10 (Remaining): on a pip-capable machine run
   `gdformat scripts/ tools/` once, commit, then wire `gdformat --check` + `gdlint`.
3. **Scene-integrity CI gate** (`bed848a`). New `scripts/ci/check_scene_integrity.py`
   (stdlib, reuses the analyzer) fails if any scene-attached `@onready` `$`-path no
   longer resolves. Wired into both CI workflows; added to the new-machine verify
   list. Validated 17 scene-attached scripts → PASS.
4. **Procedure fix pass** (`30f208d`). Applied meta-review §4 to the master procedure:
   toolchain probe (MR-4) + exit-code-before-pipe note (MR-8) in §3, tree-completeness
   preflight (MR-1), delta-discovery-by-glob (MR-2), same-day report naming (MR-3),
   anchored score header (MR-6), permanent friction-reporting. Wired two new checks:
   `check_docs.py` **check 11** (every top-level dir named in the §2 coverage map) and
   **check 12** (rollup carries an anchored `Overall health … N/10`). §10 backlog now
   splits Landed vs Remaining.

## check_docs.py
Now **12 checks**, all green. (9 .uid, 10 version↔tag, 11 tree-cover, 12 rollup-score.)

## Git
- Feature `awakening-compatability-refactor` carries all of the above.
- PR branch `integrate-awakening-into-main` re-synced so the PR to `main` is complete.

## Remaining
- **gdformat gate** — land on a pip-capable machine (see #2).
- Minor meta-review MR-9 pillar-doc polish (`.uid` mechanism note, empty-dir scope,
  DOC-002 deviation wording) — Low, deferred.
- Merge the `integrate-awakening-into-main` PR.
