# Copy to .env and fill in the keys you need.
# Login-based auth (claude login / codex login) is the recommended path
# and persists in the developer-home Docker volume; leave these blank
# in that case.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
